node_modules/
dist/
*.tsbuildinfo
.claude/settings.local.json
.claude/settings.json
.claude/scheduled_tasks.lock
.claude/worktrees/

# Phase 51 bench reports (transient, not git-tracked)
.planning/benchmarks/reports/

# Phase 76+ migration ledger (runtime state, per-host, not git-tracked)
.planning/migration/

# Playwright MCP traces (transient, large)
.playwright-mcp/

# Local screenshots / image clutter (operator's local files, not project assets)
Screenshot*.png
amazon-*.png
reuzel-*.png
*-product.png

clawcode.yaml
