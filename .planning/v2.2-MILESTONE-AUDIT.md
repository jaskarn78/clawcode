---
milestone: v2.2
milestone_name: OpenClaw Parity & Polish
audited: 2026-04-23T22:50:00Z
status: tech_debt
scores:
  requirements: 7/7  (all requirement areas wired)
  phases: 7/7  (verification status per-phase)
  integration: 7/7  (cross-phase wiring clean)
  flows: 7/7  (no broken E2E flows detected)
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 89-agent-restart-greeting
    items:
      - "Human UAT pending: live Discord greeting smoke test (restart a real agent, observe webhook-attributed embed <500 chars in bound channel)"
      - "Human UAT pending: dormancy skip smoke test (restart an agent idle >7d, observe no Discord message)"
      - "Deferred: /clawcode-greet slash toggle (runtime enable/disable without YAML edit) — follow-up phase"
      - "Deferred: greeting observability metrics (sent/skipped/failed counters) — nice-to-have, not blocking"
      - "Deferred: per-channel greet policy — currently per-agent only"
      - "Deferred: edit-in-place greeting mode — chose new-message in D-15"
  - phase: 82-pilot-cutover-completion (pre-existing, v2.1)
    items:
      - "v1.1 legacy: 12 of 15 phases missing formal VERIFICATION.md (docs-only phases, not v2.2 scope)"
nyquist:
  compliant_phases: 0
  partial_phases: 0
  missing_phases: 7
  overall: missing
  note: "VALIDATION.md files not generated for v2.2 phases. Each phase's 89-RESEARCH.md / SUMMARY / VERIFICATION contain test strategy + UAT items inline. Not a blocker — the v2.2 milestone predates Nyquist-as-separate-file convention, but all 7 phases' tests + must_haves are pinned in their VERIFICATION.md and SUMMARY.md artifacts."
---

# v2.2 OpenClaw Parity & Polish — Milestone Audit

**Date:** 2026-04-23
**Scope:** Phases 83, 84, 85, 86, 87, 88, 89 (7 phases)
**Status:** `tech_debt` — all requirements met, no blockers, 2 deferred human UAT items for Phase 89

## Executive Summary

All 7 phases of v2.2 shipped their goal. Cross-phase integration is clean — no wiring drift, no dangling exports, no shadowed Discord commands, no DI order violations, no schema-stacking conflicts. The canary blueprint (synchronous caller + fire-and-forget + `.catch` log-and-swallow) is applied consistently across 4 SDK-mutating setters (setEffort, setModel, setPermissionMode, sendRestartGreeting).

Phase 89 is the only phase with `human_needed` status. Its 10/10 automated must-haves pass; the 2 pending items are live-Discord smoke tests that require operator interaction with a running daemon. These are tracked in `89-HUMAN-UAT.md` and will surface in `/gsd:progress` and `/gsd:audit-uat` until resolved.

## Phase-by-Phase Status

| Phase | Name | VERIFICATION.md Status | Score | Integration |
|-------|------|------------------------|-------|-------------|
| 83 | Extended-Thinking Effort Mapping | passed | — | WIRED |
| 84 | Skills Library Migration | passed | — | WIRED |
| 85 | MCP Tool Awareness & Reliability | passed | — | WIRED |
| 86 | Dual Discord Model Picker (Core) | passed | — | WIRED |
| 87 | Native CC Slash Commands | passed | — | WIRED |
| 88 | Skills Marketplace | passed | — | WIRED |
| 89 | Agent Restart Greeting | human_needed | 10/10 automated | WIRED |

## Requirements Integration Map (from gsd-integration-checker)

| Area | Integration Path | Status |
|------|------------------|--------|
| EFFORT (Phase 83) | `effortSchema` → `agentSchema.effort` → `RELOADABLE_FIELDS` → `setEffortForAgent` → `handle.setEffort` → `q.setMaxThinkingTokens` | WIRED |
| SKILL migration (Phase 84) | `writeClawcodeYaml` → ledger → `scanSkillsDirectory` → `linkAgentSkills` → `manager.setSkillsCatalog` | WIRED |
| TOOL/MCP (Phase 85) | `performMcpReadinessHandshake` → `mcpStateByAgent` → `handle.setMcpState` → `clawcode-tools` inline → IPC `list-mcp-status` | WIRED |
| MODEL picker (Phase 86) | `allowedModels` → `updateAgentModel` YAML writer → `handle.setModel` → `ModelNotAllowedError` (w/ `IpcError.data`) → StringSelectMenuBuilder | WIRED |
| CMD dispatch (Phase 87) | `buildNativeCommandDefs` → `mergeAndDedupe` → per-guild register → control-plane vs prompt-channel dispatch | WIRED |
| MKT marketplace (Phase 88) | `loadMarketplaceCatalog` → `handleMarketplaceInstallIpc` → `installSingleSkill` → `updateAgentSkills` → `/clawcode-skills-browse` | WIRED |
| GREET (Phase 89) | `setWebhookManager` DI → `restartAgent(line 938)` → `classifyRestart` → `sendRestartGreeting` → `WebhookManager.sendAsAgent` | WIRED |

## Cross-Phase Architectural Consistency Checks

### 1. DI Order in daemon.ts — CLEAN
SessionManager → setSkillsCatalog → WebhookManager construction (3 branches) → setWebhookManager → startAll. Greetings are no-ops before setWebhookManager fires; graceful degradation confirmed.

### 2. Schema Stacking — CLEAN
Five new additive-optional fields (effort, allowedModels, greetOnRestart, greetCoolDownMs, marketplace skills source) all resolve via loader defaults. v2.1 migrated fleet (15 agents, none of the new fields) parses unchanged. RELOADABLE_FIELDS correctly lists 4 reloadable; `agents.*.model` correctly NON_RELOADABLE.

### 3. Discord Command Namespace — CLEAN
Inline-handler short-circuit order in slash-commands.ts: `clawcode-tools` → `clawcode-model` → `clawcode-permissions` → `clawcode-skills-browse` → `clawcode-skills` → CONTROL_COMMANDS → native/prompt-channel → `clawcode-effort`. No shadowing. Discord 100-per-guild cap gated by pre-flight assertion at 90. `clawcode-compact` + `clawcode-usage` correctly migrated from `DEFAULT_SLASH_COMMANDS` to `buildNativeCommandDefs` (Phase 87 CMD-04). Zero duplicate registration risk.

### 4. Canary Blueprint — CLEAN
Four SDK-mutating setters share byte-identical shape:
- `setEffort` → `q.setMaxThinkingTokens`
- `setModel` → `q.setModel`
- `setPermissionMode` → `q.setPermissionMode`
- `sendRestartGreeting` → `WebhookManager.sendAsAgent`

All use `void ...catch(log.warn)`. Restart success (Phase 89) and session setter calls (83/86/87) never block on their fire-and-forget side effects.

### 5. SkillInstallOutcome Wire Shape — CLEAN
`SkillInstallOutcomeWire` type in slash-commands.ts is structurally identical to the original `SkillInstallOutcome` in install-single-skill.ts. All 8 discriminated-union variants match. Intentional redeclaration (documented) keeps the module tree narrow.

### 6. Phase 89 Greeting Guard — CLEAN
`restartAgent` gates greeting on `webhookManager && convStore`. `convStore` populated by `memory.initMemory(name, config)` at startAgent:389, which runs inside restartAgent:989 before the greeting call. `stopAgent` clears `greetCoolDownByAgent.delete(name)` at line 887. No races.

## Tech Debt

### Phase 89 — 2 pending UAT items (human-only)
Tracked in `.planning/phases/89-agent-restart-greeting-active-discord-send-of-prior-context-summary-on-restart/89-HUMAN-UAT.md`:
1. Live Discord greeting on restart (visual verification of webhook identity + <500 char embed + color)
2. Dormancy skip (restart agent idle >7d → no Discord message)

### Phase 89 — Deferred follow-ups (not v2.2 scope)
- `/clawcode-greet` runtime slash toggle
- Observability metrics (sent/skipped/failed counters)
- Per-channel greet policy (currently per-agent)
- Edit-in-place greeting mode (chose new-message in D-15)

### Pre-existing (v1.1 era, NOT v2.2 scope)
- 12 of 15 v1.1 phases missing formal VERIFICATION.md (docs-only phases; legacy carry-over noted in STATE.md)

## Nyquist Compliance

Seven v2.2 phases have no separate VALIDATION.md file. Test strategy, UAT items, and Nyquist test matrices are embedded inline in each phase's RESEARCH.md / SUMMARY.md / VERIFICATION.md. Not flagged as a blocker — the milestone pre-dates the VALIDATION-as-separate-file convention, and all 7 phases' tests + must_haves are pinned with grep-verifiable automated checks.

Operators can optionally run `/gsd:validate-phase N` retroactively on any phase that needs a formal Nyquist matrix file.

## Status Determination

Per audit status rules:
- `passed` — all requirements met, no critical gaps, minimal tech debt
- `gaps_found` — critical blockers exist (none found)
- `tech_debt` — no blockers but accumulated deferred items need review

v2.2 status: **`tech_debt`** — no blockers; Phase 89 UAT items are low-risk smoke tests that the operator can sign off at leisure. All other phases fully verified. Recommend proceeding to `/gsd:complete-milestone v2.2` with acknowledged tech debt.

## Next Steps

1. **Complete milestone** (recommended) — accept tech debt, archive v2.2. Phase 89 UAT items continue to surface in `/gsd:progress` until operator validates.
   `/gsd:complete-milestone v2.2`

2. **Address Phase 89 UAT first** — operator runs the 2 smoke tests, updates `89-HUMAN-UAT.md` to `status: resolved`, then completes milestone.
   `/gsd:verify-work 89` then `/gsd:complete-milestone v2.2`
