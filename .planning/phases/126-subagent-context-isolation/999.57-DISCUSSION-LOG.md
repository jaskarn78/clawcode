# Phase 999.57: subagent-context-isolation - Discussion Log

> **Audit trail only.** Decisions are captured in CONTEXT.md.

**Date:** 2026-05-15
**Phase:** 999.57-subagent-context-isolation
**Mode:** `--auto` (no interactive questions; decisions auto-selected from BACKLOG.md + recommended options)
**Areas auto-decided:** Inheritance override scope, override factory shape, backwards compatibility, test coverage, deferred items

---

## Auto-selected decisions (--auto mode)

| Area | Selected Path | Rationale |
|---|---|---|
| Override scope | All subagents (both delegated and non-delegated) | Architecturally cleaner; the inheritance leak affects both forms |
| Override structure | New `buildSubagentOverrides` factory at `src/discord/subagent-config-overrides.ts` | Single-source-of-truth for the override list; mirrors Phase 99 disallowedTools pattern |
| Heartbeat override | `enabled: false` (with valid stub for other fields) | HeartbeatRunner already gates on this; no runner-side change needed |
| Memory override | `memoryAutoLoad: false`, `memoryRetrievalTopK: 0`, `memoryRetrievalTokenBudget: 0`, `memoryScannerEnabled: false` | Eliminates Run 1's vector (HeyGen content from memory retrieval surfacing in turn 1) |
| Inbox override | `inboxEnabled: false` (exact field name to be confirmed in research) | Chokidar watcher doesn't fire on subagent JSONL drops |
| Backwards compat | No yaml-side opt-out for tonight | No production demand for cross-task memory inheritance; ship safer default first |
| Per-spawn `inheritMemory: true` flag | Deferred | No real use case demonstrated; over-engineering for now |
| Subagent-aware gates inside HeartbeatRunner / InboxSource | Deferred (defense-in-depth, not needed) | Config-override at spawn is the cleaner contract |

## Already-shipped (do not re-implement)

- Commit `07bd5b0` (2026-05-15) — auto-relay gate when `delegateTo` is set + `subagent_complete` instruction injection into delegated subagent's threadContext.
- 4 tests pinning the contract (DEL-11..14).

## Deferred ideas

- `inheritMemory: true` per-spawn opt-in flag.
- Subagent-aware gating inside `HeartbeatRunner` / `InboxSource` (defense-in-depth).
- `memoryRetrievalExcludeTags` defaults for subagents (softer alternative to `topK: 0`).
- Inbox watcher cleanup on subagent session stop.
- Cross-agent inbox identity drift (Phase 999.36 sub-bug C adjacency).
