---
phase: 999.57-subagent-context-isolation
plan: 02
type: execute
wave: 2
depends_on: [999.57-01]
files_modified:
  - src/discord/subagent-thread-spawner.test.ts
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-09]
tags: [subagent, regression-tests, heartbeat, memory-retrieval]

must_haves:
  truths:
    - "DEL-15 pins heartbeat override + heartbeatRunner.setAgentConfigs call: subagent spawn results in startedConfig.heartbeat.enabled === false AND mock heartbeatRunner.setAgentConfigs is called exactly once with [subagentConfig]. Test runs for BOTH delegateTo-set and delegateTo-unset spawns (D-06)."
    - "DEL-16 pins memory retrieval suppression: startedConfig.memoryAutoLoad === false AND memoryRetrievalTopK === 0 AND memoryRetrievalTokenBudget === 0 AND memory.conversation.flushIntervalMinutes === 0 (when source had a conversation block)."
    - "DEL-17 pins defensive markers (set-membership only, NOT behavioral per RESEARCH Correction 3): startedConfig.memoryScannerEnabled === false AND startedConfig.memoryFlushIntervalMs === 900_000."
    - "DEL-18 pins the Finding 2 reframe — RESEARCH §Implication §(iii): assert the subagent's stored config produces a heartbeat-gate skip, by directly invoking the same gate logic against the subagent's stored config (we cannot run real heartbeat checks in unit tests, but we CAN assert: heartbeatRunner.setAgentConfigs received the subagent config AND that subagent config's heartbeat.enabled === false). Together these two assertions guarantee the runner-side gate at runner.ts:271 will skip the subagent. The integration-style runner-tick test is deferred to Plan 03 live verification (operator runs real journalctl grep on clawdy)."
    - "DEL-19 pins no-upward-leakage: parentConfig.heartbeat / parentConfig.memoryRetrievalTopK / parentConfig.memoryAutoLoad / parentConfig.memory.conversation are UNCHANGED after the spawn (deep-equal compare on a snapshot taken before spawn)."
    - "All new tests live in the existing `spawnInThread with delegateTo` describe block (or sibling describe for non-delegated) at subagent-thread-spawner.test.ts:1516+. Follow the existing DEL-NN naming convention."
    - "Plan 01 wiring is now pinned: a future refactor that removes the factory call OR drops the heartbeatRunner.setAgentConfigs call OR changes a field default will fail at least one DEL-15..19 test."
  artifacts:
    - path: "src/discord/subagent-thread-spawner.test.ts"
      provides: "DEL-15..DEL-19 regression tests (~5 new test cases + delegated/non-delegated variants for D-06)"
      contains: "DEL-15"
  key_links:
    - from: "DEL-15"
      to: "buildSubagentOverrides factory"
      via: "assertion on startedConfig.heartbeat.enabled + heartbeatRunner.setAgentConfigs spy"
      pattern: "DEL-15"
    - from: "DEL-19"
      to: "parentConfig immutability"
      via: "deep-equal snapshot compare before/after spawn"
      pattern: "DEL-19"
---

<objective>
Add regression tests DEL-15..DEL-19 to `src/discord/subagent-thread-spawner.test.ts` pinning the Plan 01 production wiring.

Purpose: Plan 01 ships the behavior change without new assertions. This plan adds the tests that turn a future regression (removing the factory call, dropping the runner registration, weakening a field default) into a CI failure rather than a silent production leak. Tests pin BOTH the override field values AND the runner-registration call — covering the failure mode RESEARCH Pitfall 1 names explicitly ("Test mocks heartbeatRunner.tick() not at all, just asserts startedConfig.heartbeat.enabled === false. Production has the gate-falsy bug.").

Output:
- 5 new test cases (with delegated + non-delegated variants for DEL-15 and DEL-16 per D-06) appended to the existing `spawnInThread with delegateTo` describe block, plus a sibling describe block for the non-delegated path.
- All previously-green tests stay green.
- New tests fail cleanly if Plan 01's wiring is removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.57-subagent-context-isolation/999.57-CONTEXT.md
@.planning/phases/999.57-subagent-context-isolation/999.57-RESEARCH.md
@.planning/phases/999.57-subagent-context-isolation/999.57-01-SUMMARY.md

@src/discord/subagent-thread-spawner.test.ts
@src/discord/subagent-config-overrides.ts

<interfaces>
<!-- Extracted from codebase + Plan 01 output. Executor uses directly. -->

From src/discord/subagent-thread-spawner.test.ts:18-83 — `makeAgentConfig` helper:
- Already produces ResolvedAgentConfig with `memory: { compactionThreshold, searchTopK, consolidation, decay, deduplication }` and `heartbeat: { enabled, intervalSeconds, checkTimeoutSeconds, contextFill }`.
- Does NOT set `memory.conversation` by default — Plan 02 tests for the conversation-override path must extend the helper or build a one-off config with `memory.conversation: { flushIntervalMinutes: 15, ...others }` set.

From src/discord/subagent-thread-spawner.test.ts:88+ — `makeMockSessionManager`:
- `startAgent: vi.fn((name, config) => { ... })` captures the started config — DEL-15..18 assertions read from `startAgent.mock.calls[0][1]`.
- `stopAgent: vi.fn()`, etc.

From Plan 01 — `makeMockHeartbeatRunner` helper (added to this test file in Plan 01):
```typescript
function makeMockHeartbeatRunner() {
  return {
    setAgentConfigs: vi.fn(),
  } as unknown as HeartbeatRunner;
}
```

The Plan 01 spawn site:
```typescript
await this.sessionManager.startAgent(sessionName, subagentConfig);
this.heartbeatRunner.setAgentConfigs([subagentConfig]);
```

So `sessionManager.startAgent.mock.calls[0][1]` and `heartbeatRunner.setAgentConfigs.mock.calls[0][0][0]` are the SAME object — assert against either, but DEL-18 should assert BOTH paths converge.

From src/heartbeat/runner.ts:268-273 — the gate we are implicitly testing (do NOT call directly in unit tests; assert against the inputs that drive it):
```typescript
const agentConfig = this.agentConfigs.get(agentName);
if (agentConfig && agentConfig.heartbeat.enabled === false) {
  continue;
}
```

So the test contract is: "the runner WILL skip this subagent IF (a) setAgentConfigs was called with the subagent config AND (b) that config's heartbeat.enabled === false." Plan 02 pins both.
</interfaces>

<test_layout_decision>
Plan 02 adds tests in TWO describe blocks:

1. Existing `describe("spawnInThread with delegateTo", ...)` at line 1516+ → DEL-15-delegated, DEL-16-delegated, DEL-17, DEL-18, DEL-19.

2. New SIBLING describe `describe("spawnInThread without delegateTo (non-delegated)", ...)` → DEL-15-non-delegated, DEL-16-non-delegated.

D-06 requires the overrides apply to BOTH spawn paths. The delegated path has dense existing tests; the non-delegated path needs new coverage for D-06.
</test_layout_decision>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DEL-15..DEL-19 regression tests for subagent inheritance overrides</name>
  <files>src/discord/subagent-thread-spawner.test.ts</files>
  <read_first>
    - src/discord/subagent-thread-spawner.test.ts (FULL — large file, ~1700+ lines. Specifically: lines 18-100 helpers, line 1516+ existing `spawnInThread with delegateTo` describe block, and the existing DEL-11..DEL-14 tests as a style precedent)
    - src/discord/subagent-config-overrides.ts (the Plan 01 factory — confirm exact field values that tests assert against)
    - .planning/phases/999.57-subagent-context-isolation/999.57-01-SUMMARY.md (Plan 01's actual implementation — line numbers, any deviations from this plan's interface block)
  </read_first>
  <behavior>
    **DEL-15-delegated** (in existing describe):
    - Setup: spawn a delegated subagent (`delegateTo: "research"`) where the source `research` agent's config has `heartbeat: { enabled: true, intervalSeconds: 60, ... }`.
    - Assert 1: `sessionManager.startAgent.mock.calls[0][1].heartbeat.enabled === false`.
    - Assert 2: `heartbeatRunner.setAgentConfigs` was called exactly once with `[<the same config>]`.
    - Assert 3: `heartbeatRunner.setAgentConfigs.mock.calls[0][0][0]` is referentially identical to (or deep-equal to) `sessionManager.startAgent.mock.calls[0][1]`.
    - Assert 4: The original source `research` config object still has `heartbeat.enabled === true` (no upward mutation).

    **DEL-15-non-delegated** (in new sibling describe):
    - Same as DEL-15-delegated but with `delegateTo` unset. Confirms D-06.

    **DEL-16-delegated** (in existing describe):
    - Setup: source `research` config has `memoryAutoLoad: true`, `memoryRetrievalTopK: 8`, `memoryRetrievalTokenBudget: 2000`, AND `memory.conversation: { flushIntervalMinutes: 15, ...required-fields }`.
    - Assert: `startedConfig.memoryAutoLoad === false`.
    - Assert: `startedConfig.memoryRetrievalTopK === 0`.
    - Assert: `startedConfig.memoryRetrievalTokenBudget === 0`.
    - Assert: `startedConfig.memory.conversation.flushIntervalMinutes === 0`.
    - Assert: `startedConfig.memory.conversation` retains other fields from source (spread preserves them).

    **DEL-16-non-delegated** (in new sibling describe):
    - Same as DEL-16-delegated but with `delegateTo` unset. Confirms D-06.

    **DEL-17** (in existing describe):
    - Setup: source config has `memoryScannerEnabled: true` and `memoryFlushIntervalMs: 60_000`.
    - Assert: `startedConfig.memoryScannerEnabled === false` (defensive marker — Correction 3).
    - Assert: `startedConfig.memoryFlushIntervalMs === 900_000` (schema-default no-op).
    - Comment in test body MUST cite RESEARCH.md Finding 3 explaining these are set-membership assertions, NOT behavioral assertions.

    **DEL-18** (in existing describe):
    - Setup: any delegated spawn.
    - Assert combined contract (RESEARCH §Implication §(iii) reframe):
      * `heartbeatRunner.setAgentConfigs.mock.calls.length === 1`.
      * `const registeredConfig = heartbeatRunner.setAgentConfigs.mock.calls[0][0][0]`.
      * `registeredConfig.heartbeat.enabled === false`.
      * `registeredConfig.name === <subagent session name>` (NOT the source agent name — confirms the gate at runner.ts:271 will find this entry when called with the subagent session name).
    - Comment cites RESEARCH Finding 2: there is no `inboxEnabled` field; the inbox leak is closed transitively via heartbeat-runner registration. The live integration check (real `runner.tick()` produces no result for subagent session) runs in Plan 03 on clawdy.

    **DEL-19** (in existing describe):
    - Setup: capture deep-clone of parentConfig + sourceConfig BEFORE spawn.
    - Spawn.
    - Assert deep-equal: parentConfig / sourceConfig unchanged after spawn (specifically: `parentConfig.heartbeat`, `parentConfig.memoryAutoLoad`, `parentConfig.memoryRetrievalTopK`, `parentConfig.memory.conversation` all unchanged; same for sourceConfig).
    - Uses `expect(parentConfig).toEqual(parentConfigSnapshot)` and `expect(sourceConfig).toEqual(sourceConfigSnapshot)`.
  </behavior>
  <action>
    Append all tests to `src/discord/subagent-thread-spawner.test.ts`. Two locations:

    1. Inside the existing `describe("spawnInThread with delegateTo", ...)` at line 1516+: append DEL-15-delegated, DEL-16-delegated, DEL-17, DEL-18, DEL-19.

    2. Add a NEW sibling `describe("spawnInThread without delegateTo (non-delegated)", ...)` block AFTER the delegated describe. Inside: DEL-15-non-delegated, DEL-16-non-delegated. Use the existing makeMockSessionManager + makeMockHeartbeatRunner helpers. Source config is just the parent's own config (no separate delegate config).

    For DEL-16, extend `makeAgentConfig` OR build a one-off config that includes `memory.conversation`:
    ```typescript
    memory: {
      ...defaultMemoryBlock, // existing fields
      conversation: {
        flushIntervalMinutes: 15,
        // include any other required fields from the ResolvedAgentConfig.memory.conversation type
      },
    },
    ```

    Read `src/shared/types.ts` for the conversation block's required fields before constructing the test object.

    For DEL-19, use `structuredClone(parentConfig)` and `structuredClone(sourceConfig)` to snapshot. Compare with `toEqual` after spawn completes.

    **Reference test style from existing DEL-11..DEL-14** at the same describe block. Match their setup pattern (`makeMockSessionManager`, `makeAgentConfig`, spawn the subagent, await spawn completion, read mock.calls).

    Each test MUST include a clear comment block at the top of the test body:
    - DEL-15: "Phase 999.57 / D-01 (corrected per RESEARCH Finding 1) — heartbeat override AND runner registration together close the gate at runner.ts:271."
    - DEL-16: "Phase 999.57 / D-02 + D-04 + Finding 4 — memory retrieval, MEMORY.md auto-load, and conversation flush all suppressed for subagent."
    - DEL-17: "Phase 999.57 / D-03 (Correction 3 per RESEARCH Finding 3) — set-membership defensive markers. memoryScannerEnabled has no runtime consumer for subagents (daemon-boot-only registration); test pins the field as documentation-of-intent."
    - DEL-18: "Phase 999.57 / D-05 (reframed per RESEARCH Finding 2) — no `inboxEnabled` field exists. Inbox heartbeat-check leak is transitively closed via heartbeatRunner.setAgentConfigs registration above. Live integration verification runs in Plan 03 on clawdy."
    - DEL-19: "Phase 999.57 / D-09 — no upward leakage. Parent and source agent configs must remain immutable across spawn."
  </action>
  <verify>
    <automated>npx vitest run src/discord/subagent-thread-spawner.test.ts -t "DEL-1[5-9]"</automated>
  </verify>
  <done>
    - 5 new test names (DEL-15, DEL-16, DEL-17, DEL-18, DEL-19) plus 2 non-delegated variants pass.
    - Existing DEL-01..DEL-14 + Phase 99/100/106 tests stay green.
    - `npx tsc --noEmit` clean.
  </done>
  <acceptance_criteria>
    - `grep -v '^//' src/discord/subagent-thread-spawner.test.ts | grep -c "DEL-15"` >= 2 (delegated + non-delegated variants).
    - `grep -v '^//' src/discord/subagent-thread-spawner.test.ts | grep -c "DEL-16"` >= 2.
    - `grep -v '^//' src/discord/subagent-thread-spawner.test.ts | grep -c "DEL-17"` >= 1.
    - `grep -v '^//' src/discord/subagent-thread-spawner.test.ts | grep -c "DEL-18"` >= 1.
    - `grep -v '^//' src/discord/subagent-thread-spawner.test.ts | grep -c "DEL-19"` >= 1.
    - `npx vitest run src/discord/subagent-thread-spawner.test.ts` reports a higher passing-test count than before Plan 02 by at least 7 (5 new + 2 non-delegated variants).
    - DEL-15 / DEL-18 assertions on `heartbeatRunner.setAgentConfigs` use the mock spy from Plan 01's `makeMockHeartbeatRunner`.
    - DEL-19 snapshot uses `structuredClone` (or equivalent deep-clone), not just object reference.
    - DEL-17 comment explicitly cites "RESEARCH.md Finding 3" so a future reader understands why the field is asserted without a behavioral test.
    - DEL-18 comment explicitly cites "RESEARCH.md Finding 2" so a future reader understands why there is no `inboxEnabled` assertion.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Same boundaries as Plan 01 (test plan covers the same surface).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.57-07 | Information Disclosure (regression) | Plan 01 wiring removed in future refactor | mitigate | DEL-15..DEL-19 fail in CI if factory call OR heartbeatRunner registration OR field defaults are removed/weakened. |
| T-999.57-08 | Information Disclosure (test-only false positive — Pitfall 1) | Test asserts only `heartbeat.enabled === false` shape, missing runner registration | mitigate | DEL-15 + DEL-18 both assert on `heartbeatRunner.setAgentConfigs` call — production cannot regress to Finding 1 dead-code state without breaking these tests. |
</threat_model>

<verification>
- `npx vitest run src/discord/subagent-thread-spawner.test.ts` — all tests pass.
- Test count increases by >= 7 (5 new + 2 non-delegated variants).
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- DEL-15..DEL-19 land and pass.
- Existing test suite remains green.
- Tests would fail if Plan 01's wiring is reverted (verifiable by manually commenting out the factory call OR the setAgentConfigs call locally — DO NOT commit such a revert; just confirm mentally that the assertion shape forces it).
- Build clean.
</success_criteria>

<output>
After completion, create `.planning/phases/999.57-subagent-context-isolation/999.57-02-SUMMARY.md`.
</output>
