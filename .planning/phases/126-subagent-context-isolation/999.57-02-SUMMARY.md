---
phase: 126-subagent-context-isolation (originally 999.57)
plan: 02
type: execute
status: complete
wave: 2
completed: 2026-05-15
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-09]
tags: [subagent, regression-tests, heartbeat, memory-retrieval, phase-126]
dependency-graph:
  requires:
    - 126-01 (factory + heartbeatRunner wiring — already shipped in Plan 01)
  provides:
    - "DEL-20..DEL-24 regression tests pinning Plan 01 production wiring"
    - "Heartbeat override + runner registration combined contract"
    - "Memory retrieval suppression on both delegated AND non-delegated spawn paths (D-06)"
    - "Defensive markers (memoryScannerEnabled, memoryFlushIntervalMs schema-default no-op)"
    - "No-upward-leakage assertion (parent + source configs immutable across spawn)"
  affects: []
key-files:
  created: []
  modified:
    - src/discord/subagent-thread-spawner.test.ts (+265 lines, 7 new test cases)
---

# Plan 02 Summary — Regression tests for subagent inheritance overrides

## What landed

Added 7 new regression tests to `src/discord/subagent-thread-spawner.test.ts`:

**In existing `describe("spawnInThread with delegateTo", ...)` block:**
- `DEL-20-delegated` — heartbeat override + `heartbeatRunner.setAgentConfigs([subagentConfig])` upsert. Asserts: (a) `startedConfig.heartbeat.enabled === false`, (b) `setAgentConfigs.mock.calls.length === 1` with `[subagentConfig]`, (c) referential identity between `startAgent.mock.calls[0][1]` and `setAgentConfigs.mock.calls[0][0][0]`, (d) source `research` config unchanged.
- `DEL-21-delegated` — memory retrieval suppression. Asserts `memoryAutoLoad === false`, `memoryRetrievalTopK === 0`, `memoryRetrievalTokenBudget === 0`, `memory.conversation.flushIntervalMinutes === 0`. Source-side `conversation: {flushIntervalMinutes: 15}` confirms the conditional-spread (Pitfall 3).
- `DEL-22-delegated` — defensive markers per Correction 3 (RESEARCH Finding 3). Asserts `memoryScannerEnabled === false` (no runtime consumer for subagents) and `memoryFlushIntervalMs === 900_000` (schema-default no-op carried explicitly).
- `DEL-23-delegated` — combined heartbeat-gate skip contract (RESEARCH §Implication §(iii) reframe per Finding 2). Asserts `setAgentConfigs.mock.calls.length === 1`, registered config's `heartbeat.enabled === false`, AND `registeredConfig.name === result.sessionName` (NOT the source agent's name — confirms the gate at `runner.ts:271` will find this entry when the subagent's heartbeat tick fires).
- `DEL-24-delegated` — no upward leakage. `structuredClone` snapshot of parent + source configs before spawn, `toEqual` compare after spawn.

**In new sibling `describe("spawnInThread without delegateTo (non-delegated) — Phase 126 D-06 inheritance overrides", ...)` block:**
- `DEL-20-non-delegated` — heartbeat override + runner registration applied on non-delegated path (D-06).
- `DEL-21-non-delegated` — memory retrieval suppression applied on non-delegated path (D-06).

## Numbering deviation from plan

Plan 02 specified DEL-15..DEL-19. Renumbered to DEL-20..DEL-24 because DEL-15..DEL-18 already exist in this test file from 999.58 (DEL-15/16 — `clawcode_share_file` instruction) and 999.61 (DEL-17/18 — `<system-reminder>` discrimination + TASK frame) hotfix work that landed BEFORE Plan 02 executed. All Plan 02 numbering inside the test file uses DEL-20..DEL-24; this SUMMARY.md and future cross-references update accordingly.

## Test verification

```
$ npx vitest run src/discord/subagent-thread-spawner.test.ts -t "DEL-2[0-4]"
 Test Files  1 passed (1)
      Tests  7 passed | 53 skipped (60)

$ npx vitest run src/discord/subagent-thread-spawner.test.ts
 Test Files  1 passed (1)
      Tests  60 passed (60)

$ npx tsc --noEmit
(clean — no output)
```

All 7 new tests pass on first run. No regression in the existing 53 tests (60 total — 53 prior + 7 new).

## Acceptance criteria

- `grep -v '^//' src/discord/subagent-thread-spawner.test.ts | grep -c "DEL-20"` → ≥ 2 ✓ (delegated + non-delegated variants)
- `grep -v '^//' src/discord/subagent-thread-spawner.test.ts | grep -c "DEL-21"` → ≥ 2 ✓
- `grep -v '^//' src/discord/subagent-thread-spawner.test.ts | grep -c "DEL-22"` → ≥ 1 ✓
- `grep -v '^//' src/discord/subagent-thread-spawner.test.ts | grep -c "DEL-23"` → ≥ 1 ✓
- `grep -v '^//' src/discord/subagent-thread-spawner.test.ts | grep -c "DEL-24"` → ≥ 1 ✓
- Test count increase: 60 (after) − 53 (before) = 7 ✓ (5 plan-spec tests + 2 D-06 non-delegated variants)
- DEL-20 + DEL-23 use `heartbeatRunner.setAgentConfigs` spy from Plan 01's `makeMockHeartbeatRunner` helper ✓
- DEL-24 snapshot uses `structuredClone` ✓
- DEL-22 comment explicitly cites "RESEARCH Finding 3" ✓
- DEL-23 comment explicitly cites "RESEARCH Finding 2" ✓
- `npx tsc --noEmit` clean ✓

## Threat mitigation

Per `999.57-02-PLAN.md` threat model:
- **T-999.57-07** (Plan 01 wiring removed in future refactor) — mitigated. DEL-20..DEL-24 fail in CI if factory call OR heartbeatRunner registration OR field defaults are removed/weakened.
- **T-999.57-08** (test-only false positive — Pitfall 1) — mitigated. DEL-20 + DEL-23 assert on BOTH `heartbeat.enabled === false` AND the `heartbeatRunner.setAgentConfigs` registration call. Production cannot regress to RESEARCH Finding 1's dead-code state without breaking these tests.

## Open items / handoff

- **Plan 03** (deploy + live verification on clawdy) — `autonomous: false` per plan frontmatter. Deploy-gated per `feedback_ramy_active_no_deploy` + `feedback_no_auto_deploy`. Operator runs `scripts/deploy-clawdy.sh` in the same turn as explicit "deploy" / "ship it" intent; then spawns a fresh research subagent on the original failing prompt; journalctl confirms no heartbeat tick for subagent session name.
- **DEL-20-non-delegated test edge** — the test triggers `subagent auto-relayed to parent` log line because `delegateTo` is unset and the v2.9 hotfix `07bd5b0` only gates auto-relay when `delegateTo` is set. This is correct per back-compat pin DEL-13. Documented here so future readers don't think DEL-20-non-delegated reveals a bug.
- **`memoryRetrievalTokenBudget` cast as `(startedConfig as any)`** — the field exists on `ResolvedAgentConfig` at runtime per the factory but the test-fixture helper's return type narrows it out. Not a production concern; documented for future test-helper widening if assertions like this multiply.

## Self-Check: PASSED

- `src/discord/subagent-thread-spawner.test.ts` exists; modified.
- DEL-20 / DEL-21 / DEL-22 / DEL-23 / DEL-24 grep gates all return ≥1 match.
- All 60 tests in the file pass.
- `npx tsc --noEmit` clean.
- New describe block for non-delegated D-06 coverage present.
- Renumbering rationale documented in this SUMMARY.
