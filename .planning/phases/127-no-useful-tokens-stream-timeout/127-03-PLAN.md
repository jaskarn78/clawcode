---
phase: 127-no-useful-tokens-stream-timeout
plan: 03
type: execute
wave: 3
depends_on: [127-01, 127-02]
autonomous: false
requirements: [D-07, D-09]
files_modified: []
tags: [deploy, live-verification, clawdy, phase-127]

must_haves:
  truths:
    - "Build is clean (npm run build + full vitest suite + tsc --noEmit all green)."
    - "Operator confirms Ramy-quiet status before deploy per feedback_ramy_active_no_deploy."
    - "Operator types explicit 'deploy' / 'ship it' intent in the same turn as the deploy step per feedback_no_auto_deploy."
    - "scripts/deploy-clawdy.sh executes — build + stage + sudo cp + restart + md5 verify."
    - "Post-deploy journalctl grep `phase127-resolver` confirms each agent boots with a resolved threshold (sourcedFrom annotated)."
    - "Synthetic stall probe — operator-triggered test that holds an Anthropic stream open with keepalives only (or replays the 2026-05-14 fin-acq scenario) — produces a `phase127-stream-stall` journal entry AND a Discord '⚠️ stream stall — turn aborted' message."
    - "Real Anthropic advisor consult on Opus (Phase 117 native backend) does NOT false-positive the stall — fin-acquisition with effort=high + advisor consult completes a normal turn without trip."
    - "D-09 — operator pulls 24h of post-deploy traces.db to compute p99 inter-token gap; documents in 127-03-SUMMARY.md whether the 180000ms / per-model defaults are appropriately conservative or need tuning."
  artifacts:
    - path: ".planning/phases/127-no-useful-tokens-stream-timeout/127-03-SUMMARY.md"
      provides: "Operator deploy confirmation + live-verify evidence (journalctl excerpts + Discord screenshot link + p99 inter-token-gap measurement)"
      contains: "phase127-stream-stall"
---

<objective>
Deploy Plans 01 + 02 to clawdy and verify the supervisor extension fires on a real stall pattern in production. Operator-deploy-gated per `feedback_ramy_active_no_deploy` + `feedback_no_auto_deploy` — NOT autonomous.

Resolves D-07 (advisor false-positive check on Opus) and D-09 (threshold tuning from real telemetry).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="checkpoint" name="T-01: Operator deploy confirmation">
  <description>
    Operator confirms Ramy is offline. Operator types explicit deploy intent. Runs `scripts/deploy-clawdy.sh` (full path defined in CLAUDE.md Deploy section). md5 verify passes; clawdy restart confirmed.
  </description>
</task>

<task type="checkpoint" name="T-02: D-05a-style resolver log verification">
  <description>
    `ssh clawdy "journalctl -u clawcode --since '5 min ago' -g 'phase127-resolver'"`
    Expect: one line per agent with `agent`, `threshold`, `sourcedFrom` keys. Paste 5-10 representative lines into SUMMARY.md.
  </description>
</task>

<task type="checkpoint" name="T-03: Synthetic stall probe">
  <description>
    Two options:
    (a) **Replay the 2026-05-14 scenario:** ask `fin-acquisition` an effort=high, advisor-likely question (something requiring deep research); observe whether the stall pattern reproduces today AND whether the trip fires correctly if it does.
    (b) **Synthetic injection:** add a one-off integration test agent in clawcode.yaml that runs against a mocked Anthropic endpoint emitting keepalives only; trip should fire within `streamStallTimeoutMs + 30s` and a Discord embed should land.

    Operator captures: (i) journalctl excerpt showing `phase127-stream-stall` payload, (ii) Discord screenshot of the `⚠️ stream stall — turn aborted, send the message again` message, (iii) confirmation that the next operator message to the agent starts a fresh turn cleanly.
  </description>
</task>

<task type="checkpoint" name="T-04: D-07 — Opus advisor consult does not false-positive">
  <description>
    Operator asks `admin-clawdy` (on opus) a question that triggers an advisor consult (Phase 117 native backend). Confirm: (a) the parent turn completes normally, (b) no `phase127-stream-stall` entry in journalctl during the consult window.

    If the consult DOES trip the stall (false positive): file a follow-up backlog item to wire `AdvisorService.isActive(agentName)` into the tracker so the clock pauses while advisor is active. Per-model 300000ms Opus default was the fallback; if it's insufficient, the wiring follow-up becomes mandatory.
  </description>
</task>

<task type="checkpoint" name="T-05: D-09 — threshold tuning from real telemetry">
  <description>
    24h post-deploy, operator pulls inter-token gap distribution from traces.db (or session JSONL inter-token timestamps if Phase 50/52 hasn't captured granular enough data):
    ```
    ssh clawdy "sqlite3 /opt/clawcode/traces.db 'SELECT max(diff) FROM (SELECT trace_id, julianday(ts) - LAG(julianday(ts)) OVER (PARTITION BY trace_id ORDER BY ts) AS diff FROM spans WHERE name LIKE \"%stream%\" AND ts > date(\"now\", \"-24 hours\")) WHERE diff IS NOT NULL GROUP BY trace_id ORDER BY max(diff) DESC LIMIT 100;'"
    ```
    Document p99 inter-token gap in SUMMARY.md. If p99 > thresholdMs/2 for any model, raise the default for that model. If p99 << thresholdMs/4, the threshold can drop (faster stall detection) — but only tighten if operator pain warrants (faster timeouts also mean more false positives on legitimate slow turns).
  </description>
</task>

</tasks>

<verification>
- Operator-confirmed deploy + restart + md5 verify.
- T-02 journalctl evidence — N agents, all with resolver log lines.
- T-03 stall probe evidence — journalctl + Discord screenshot.
- T-04 advisor consult evidence — clean completion, no false trip.
- T-05 threshold tuning documented (or "no tuning needed — defaults conservative" if p99 << threshold).
- 127-03-SUMMARY.md written by operator with all 5 evidence blocks.
</verification>

<success_criteria>
- Live stall pattern reproducible (or NOT reproducible — also a valid outcome if other systemic fixes since 2026-05-14 have closed the underlying Anthropic upstream bug).
- D-07 confirmed: Opus advisor consult does not false-positive at 300000ms.
- D-09 threshold defaults validated against real telemetry.
- Phase 127 closes cleanly when 127-03-SUMMARY.md is committed with the evidence blocks.
</success_criteria>

<output>
Operator creates `.planning/phases/127-no-useful-tokens-stream-timeout/127-03-SUMMARY.md` with the deploy + verify evidence after the deploy window.
</output>
