---
phase: 18-mcp-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp/server.ts
  - src/mcp/server.test.ts
  - src/cli/commands/mcp.ts
  - src/cli/index.ts
autonomous: true
requirements: [MCPB-01, MCPB-02]
---

<objective>
Expose ClawCode tools as an MCP stdio server for external Claude Code sessions.

Purpose: Allow external Claude Code instances to query agent status, search memory, send messages, and list schedules.
Output: MCP server with tools, CLI command to start it.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MCP server with tools and CLI command</name>
  <files>src/mcp/server.ts, src/mcp/server.test.ts, src/cli/commands/mcp.ts, src/cli/index.ts</files>
  <action>
    1. Create src/mcp/server.ts implementing McpServer with stdio transport
    2. Register tools: agent_status, send_message, list_schedules, list_agents
    3. Each tool delegates to the existing IPC client to communicate with the daemon
    4. Create src/mcp/server.test.ts testing tool definition metadata
    5. Create src/cli/commands/mcp.ts with clawcode mcp command
    6. Register in cli/index.ts
  </action>
</task>

</tasks>
