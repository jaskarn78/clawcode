---
phase: 122
plan: 01
wave: 1
depends_on: []
autonomous: true
type: standard
requirements: [DISC-01]
files_modified:
  - src/discord/markdown-table-wrap.ts                       # SC-3 nested-fence escalation
  - src/discord/webhook-manager.ts                           # sendAsAgent embed.description wrap
  - src/discord/bridge.ts                                    # sendDirect fallback channel.send wrap
  - src/manager/daemon.ts                                    # inline BotDirectSender.sendText + sendEmbed wrap
  - src/discord/__tests__/markdown-table-wrap.test.ts        # SC-3 nested-fence test added
  - src/discord/__tests__/122-universal-wrap-coverage.test.ts # NEW static-grep regression sentinel
  - src/discord/__tests__/webhook-manager.test.ts            # NEW or extended (sendAsAgent embed wrap)
  - .planning/phases/122-discord-table-auto-wrap-universalization/122-DEVIATION.md # SC-3 helper extension rationale
---

# Phase 122 Plan 01: Universal Markdown Table Wrap at Discord Transport Boundary

## Objective

Apply `wrapMarkdownTablesInCodeFence` universally at every Discord send chokepoint — one wrap per transport primitive, never per call site. Plug the four gaps left after Phase 100 follow-up:

1. **`WebhookManager.sendAsAgent`** — embed body bypasses the wrap.
2. **`bridge.ts sendDirect` fallback** (channel.send path, no webhook) — bypasses the wrap.
3. **Inline `BotDirectSender` impl in `daemon.ts:7232-7253`** — `sendText` and `sendEmbed` both bypass the wrap.
4. **`wrapMarkdownTablesInCodeFence` helper itself** — hardcodes 3-backtick `` ```text `` fence; a cell containing ` ``` ` breaks out (SC-3 gap).

The static-grep regression test (T-05) is the long-term sentinel against regressions — it pins each chokepoint by file+line vicinity.

## Context

@.planning/phases/122-discord-table-auto-wrap-universalization/122-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

## Orientation Findings (executor-verified, not in CONTEXT)

| Site                                                    | Status today                       | Plan action                          |
|---------------------------------------------------------|------------------------------------|--------------------------------------|
| `WebhookManager.send` (`webhook-manager.ts:88`)         | ALREADY WRAPS (Phase 100-fu)       | Pin via static-grep                  |
| `WebhookManager.sendAsAgent` / `attemptSendAsAgent`     | Takes `EmbedBuilder` — no wrap     | Wrap `embed.data.description` if set |
| `bridge.ts:736` (chunked plain-send path)               | ALREADY WRAPS                      | Pin via static-grep                  |
| `bridge.ts:1280, 1287` (`sendDirect` fallback)          | NO WRAP — gap                      | Wrap `response` at top of method     |
| `daemon.ts:7245` inline `botDirectImpl.sendText`        | NO WRAP — gap                      | Wrap `content` at top of method      |
| `daemon.ts:7233` inline `botDirectImpl.sendEmbed`       | NO WRAP — gap                      | Wrap `embed.data.description` if set |
| `daemon-ask-agent-ipc.ts:286` (mirror bot-direct)       | Inherits via `botDirectSender.sendText` | No edit — inherits from daemon.ts |
| `daemon-post-to-agent-ipc.ts:220` (Phase 119 A2A-01)    | Inherits via `botDirectSender.sendText` | No edit — inherits from daemon.ts |
| `daemon.ts:2962-3009` `triggerDeliveryFn`               | Inherits via `wm.send` + `sendText`     | No edit — inherits                |
| `bridge.ts:420` budget-alert embed (no `description`)   | N/A — no agent-generated body      | No edit — out of scope            |

**Critical correction vs operator-dictated plan:** `BotDirectSender` is a TYPE (`restart-greeting.ts:95`), not a class. The single concrete implementation is the inline object literal in `daemon.ts:7232-7253` — that is where the wrap must go.

## Tasks

### T-01 — Extend helper for SC-3 (nested triple-backtick escalation) [TDD]
**type=auto, tdd=true**

CONTEXT D-03 explicitly permits extending the helper for SC-3 (overrides D-02 lock). Document the extension upfront in `122-DEVIATION.md`.

**Behavior:** scan each table block for the longest run of consecutive backticks in any cell. Outer fence uses `max(longest-run + 1, 3)` backticks. Language tag stays `text` (Discord renders monospace).

**RED:** Add test `MTW-11: table with a cell containing triple-backtick fence is wrapped with 4+ outer backticks`. Expect failure on current helper.
**GREEN:** Modify `wrapMarkdownTablesInCodeFence` to compute `outerFenceLen = max(longestBacktickRun + 1, 3)` over the collected table block, emit `'`'.repeat(outerFenceLen) + 'text'` and matching closing fence.
**REFACTOR:** Keep helper pure.

Commits:
- `test(122-01): MTW-11 RED — nested-fence escalation pinned`
- `feat(122-01): wrapMarkdownTablesInCodeFence escalates outer fence for nested backticks (SC-3)`

### T-02 — Wrap `sendAsAgent` embed description
**type=auto**

In `attemptSendAsAgent` (the path `sendAsAgent` always calls — pre-reprovision AND post-reprovision retry), if `embed.data.description` is a non-empty string, replace it via `embed.setDescription(wrapMarkdownTablesInCodeFence(embed.data.description))` BEFORE `client.send`. This is the single chokepoint for embed body content per D-01.

Test: extend or add `src/discord/__tests__/webhook-manager.test.ts` — construct an `EmbedBuilder().setDescription('| A | B |\n| - | - |\n| 1 | 2 |')`, mock `WebhookClient`, call `sendAsAgent`, assert dispatched embed description contains ` ```text `.

Commit: `feat(122-01): wrap embed.description in WebhookManager.sendAsAgent (single chokepoint)`

### T-03 — Wrap `bridge.ts sendDirect` channel.send fallback
**type=auto**

In `sendDirect` (line 1261), after the webhook branch (which already inherits the wrap from `WebhookManager.send`), wrap `response` once with `wrapMarkdownTablesInCodeFence` BEFORE the `channel.send` / split path. Single wrap site covers both the `length <= MAX_LENGTH` branch (line 1280) and the chunked branch (line 1287).

Test: add to `bridge.test.ts` (or new test file if none exists for sendDirect specifically) — mock channel.send, call sendDirect with a markdown-table response and no resolvedAgent, assert dispatched content contains ` ```text `. If existing bridge.test infrastructure is missing or heavy, deferral note in SUMMARY.

Commit: `feat(122-01): wrap channel.send fallback in bridge.sendDirect`

### T-04 — Wrap inline `BotDirectSender` impl in daemon.ts
**type=auto**

In the `botDirectImpl` object literal at `daemon.ts:7232-7253`:
- `sendText` (line 7245): wrap `content` at top of method.
- `sendEmbed` (line 7233): if `embed.data.description` is non-empty, mutate via `setDescription(wrap(...))` before send.

Both wraps are required so the Phase 119 A2A-01 bot-direct rung (`daemon-post-to-agent-ipc.ts:220`), the 999.12 ask-agent mirror (`daemon-ask-agent-ipc.ts:286`), and the Phase 90.1 restart-greeting fallback all inherit the wrap.

Test: this chokepoint is hard to unit-test in isolation (inline closure, builds at daemon construction time). The static-grep sentinel in T-05 covers it. Document in SUMMARY.

Commit: `feat(122-01): wrap content + embed.description in inline BotDirectSender (daemon.ts)`

### T-05 — Static-grep regression sentinel (SC-1)
**type=auto**

New test file: `src/discord/__tests__/122-universal-wrap-coverage.test.ts`.

For each canonical site, read the file, locate a stable anchor (function/method name or distinctive string), and assert `wrapMarkdownTablesInCodeFence` appears within a small line window after the anchor. Anchors:

| File                                    | Anchor                                          |
|-----------------------------------------|-------------------------------------------------|
| `src/discord/webhook-manager.ts`        | `async send(agentName: string, content: string)` |
| `src/discord/webhook-manager.ts`        | `attemptSendAsAgent`                            |
| `src/discord/bridge.ts`                 | `private async sendDirect(`                     |
| `src/discord/bridge.ts`                 | line 736 vicinity (existing wrap before chunked channel.send) |
| `src/manager/daemon.ts`                 | `botDirectImpl` declaration vicinity            |

Test fails when a future commit removes the wrap from any anchored region. This IS the long-term universal-wiring sentinel per CONTEXT D-04.

Commit: `test(122-01): static-grep regression sentinel for universal wrap coverage`

### T-06 — SUMMARY.md
**type=auto**

Write `122-01-SUMMARY.md`. Include:
- Commit SHAs (per task)
- Test paths added/extended + run output (last lines)
- Deviation: SC-3 helper extension (link to `122-DEVIATION.md`)
- Phase 119 inheritance verification (T-04 wraps the inline impl that T-05's grep anchors)
- SC-2 status: `BLOCKED-deploy-pending` (Ramy active; per `feedback_ramy_active_no_deploy`)
- Pre-existing test failures in `slash-commands*.test.ts` and `verifier.test.ts` — out of scope, NOT introduced by this plan.

Commit: `docs(122-01): SUMMARY for universal table wrap`

## Verification

- `npx vitest run src/discord src/manager/__tests__/post-to-agent-ipc.test.ts src/manager/__tests__/ask-agent-ipc.test.ts` — all 122-related tests pass.
- `npx vitest run src/discord/__tests__/markdown-table-wrap.test.ts` — 11/11 (was 10/10 + MTW-11).
- `npx vitest run src/discord/__tests__/122-universal-wrap-coverage.test.ts` — passes (sentinel green).
- Pre-existing failures in `slash-commands*.test.ts` + `verifier.test.ts` remain (out of scope).

## Success Criteria (ROADMAP)

- **SC-1** Static-grep regression test pins every send site. → T-05.
- **SC-2** Operator screenshots across 4 channels with the canonical 4-column fixture. → `BLOCKED-deploy-pending`.
- **SC-3** Nested triple-backtick cell wraps with 4+ outer backticks, no fence breakout. → T-01.
- **SC-4** Helper is UNCHANGED except for SC-3 extension. → enforced; documented in `122-DEVIATION.md`.

## Deploy Hold

DO NOT DEPLOY. Ramy active in `#fin-acquisition` (per `feedback_ramy_active_no_deploy`). Local-only landing this turn.
