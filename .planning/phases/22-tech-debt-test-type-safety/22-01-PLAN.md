---
phase: 22-tech-debt-test-type-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/heartbeat/checks/__tests__/tier-maintenance.test.ts
  - src/heartbeat/checks/__tests__/consolidation.test.ts
  - src/discord/thread-manager.test.ts
  - src/discord/__tests__/bridge-attachments.test.ts
  - src/memory/__tests__/compaction.test.ts
  - src/memory/__tests__/consolidation.test.ts
  - src/memory/__tests__/embedder.test.ts
  - src/cli/commands/fork.test.ts
  - src/cli/commands/send.test.ts
  - src/cli/commands/webhooks.test.ts
  - src/cli/commands/mcp.test.ts
autonomous: true
requirements: [DEBT-05, DEBT-06]

must_haves:
  truths:
    - "All test fixtures include required fields without type escape hatches"
    - "No `as unknown as` casts remain in test files (excluding worktrees)"
    - "CLI commands fork, send, webhooks, mcp each have unit tests covering success and error paths"
  artifacts:
    - path: "src/cli/commands/fork.test.ts"
      provides: "Fork CLI command tests"
    - path: "src/cli/commands/send.test.ts"
      provides: "Send CLI command tests"
    - path: "src/cli/commands/webhooks.test.ts"
      provides: "Webhooks CLI command tests"
    - path: "src/cli/commands/mcp.test.ts"
      provides: "MCP CLI command tests"
  key_links:
    - from: "test fixtures"
      to: "source type definitions"
      via: "direct type conformance without as-unknown-as"
      pattern: "no `as unknown as` in test files under src/"
---

<objective>
Fix test fixtures to use proper types (eliminating `as unknown as` casts) and add unit tests for the four untested CLI commands (fork, send, webhooks, mcp).

Purpose: DEBT-05 requires fixtures with all required fields and no type escape hatches. DEBT-06 requires unit tests for every CLI command.
Output: Updated test fixtures with proper types, new test files for fork/send/webhooks/mcp CLI commands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/heartbeat/types.ts
@src/manager/session-manager.ts
@src/cli/commands/fork.ts
@src/cli/commands/send.ts
@src/cli/commands/webhooks.ts
@src/cli/commands/mcp.ts
@src/cli/commands/schedules.test.ts

<interfaces>
<!-- Key types the executor needs -->

From src/heartbeat/types.ts:
```typescript
export type CheckContext = {
  readonly agentName: string;
  readonly sessionManager: SessionManager;
  readonly registry: Registry;
  readonly config: HeartbeatConfig;
  readonly threadManager?: ThreadManager;
};
```

From src/cli/commands/fork.ts:
```typescript
type ForkResponse = {
  readonly ok: boolean;
  readonly forkName: string;
  readonly parentAgent: string;
  readonly sessionId: string;
};
```

From src/cli/commands/send.ts:
```typescript
type SendResponse = {
  readonly ok: boolean;
  readonly messageId: string;
};
```

From src/cli/commands/webhooks.ts:
```typescript
export function formatWebhooksTable(data: WebhooksResponse): string;
type WebhooksResponse = { readonly webhooks: readonly WebhookEntry[] };
type WebhookEntry = {
  readonly agent: string;
  readonly displayName: string;
  readonly avatarUrl?: string;
  readonly hasWebhookUrl: boolean;
};
```

Existing CLI test pattern (from schedules.test.ts, threads.test.ts):
- Tests import format functions directly (not the register function)
- Tests call format functions with mock data and assert output content
- Tests verify table headers, colors (ANSI codes), edge cases (empty data, truncation)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix test fixtures to eliminate as-unknown-as casts</name>
  <files>
    src/heartbeat/checks/__tests__/tier-maintenance.test.ts
    src/heartbeat/checks/__tests__/consolidation.test.ts
    src/discord/thread-manager.test.ts
    src/discord/__tests__/bridge-attachments.test.ts
    src/memory/__tests__/compaction.test.ts
    src/memory/__tests__/consolidation.test.ts
    src/memory/__tests__/embedder.test.ts
  </files>
  <read_first>
    src/heartbeat/types.ts
    src/heartbeat/checks/__tests__/tier-maintenance.test.ts
    src/heartbeat/checks/__tests__/consolidation.test.ts
    src/discord/thread-manager.test.ts
    src/discord/__tests__/bridge-attachments.test.ts
    src/memory/__tests__/compaction.test.ts
    src/memory/__tests__/consolidation.test.ts
    src/memory/__tests__/embedder.test.ts
    src/manager/session-manager.ts
  </read_first>
  <action>
    For each test file containing `as unknown as` casts, replace the cast with a properly-typed mock that satisfies the interface:

    1. **tier-maintenance.test.ts** (line 15): The `sessionManager` mock has `getTierManager` and `getRunningAgents`. Read the `SessionManager` class public methods and add all required fields with `vi.fn()` stubs so the mock satisfies `CheckContext["sessionManager"]` directly. Remove the `as unknown as CheckContext["sessionManager"]` cast. Use vitest's `vi.fn()` for each public method of SessionManager that the CheckContext expects.

    2. **consolidation.test.ts** (heartbeat, line 44): Same pattern -- the `sessionManager` mock already has many methods. Check the full SessionManager public API and add any missing methods as `vi.fn()` stubs. Remove the `as unknown as CheckContext["sessionManager"]` cast.

    3. **thread-manager.test.ts** (lines 90, 140): The `makeMockSessionManager` function returns a mock with typed methods. It uses `as unknown as SessionManager & {...}`. Add the missing SessionManager public methods to the mock object so the cast becomes unnecessary. At minimum the mock needs all public methods that ThreadManager calls.

    4. **bridge-attachments.test.ts** (lines 44, 59, 69): These cast discord.js Collection and Message objects. For these discord.js types, use `vi.fn()` mocks that match the specific properties used in the test (id, attachments, content, etc.). Use `Partial<>` generics or create minimal type-compatible objects rather than `as unknown as`.

    5. **compaction.test.ts** (lines 35, 44, 52, 62, 116, 122): Multiple mocks for MemoryStore, EmbeddingService, SessionLogger, Logger. For each, read the actual interface/type and add all required fields. Remove all `as unknown as` casts.

    6. **consolidation.test.ts** (memory, line 34): EmbeddingService mock. Same approach.

    7. **embedder.test.ts** (line 65): Mock for pipeline return type. Use proper typing with the actual return shape.

    Approach for each: Read the target type's public API, add vi.fn() stubs for every method the type requires, so TypeScript accepts the object without type escape hatches. Where an interface has many methods the test doesn't exercise, still add stubs (vi.fn()) for completeness.

    For discord.js types (bridge-attachments.test.ts), where the full discord.js types are very complex, it is acceptable to use `as Partial<Type> as Type` or create a test utility type `TestMessage` etc. -- the goal is eliminating `as unknown as`, not necessarily satisfying every property of discord.js classes. Use `vi.fn()` for method stubs.
  </action>
  <verify>
    <automated>cd /home/jjagpal/.openclaw/workspace-coding && npx vitest --run src/heartbeat/checks/__tests__/tier-maintenance.test.ts src/heartbeat/checks/__tests__/consolidation.test.ts src/discord/thread-manager.test.ts src/discord/__tests__/bridge-attachments.test.ts src/memory/__tests__/compaction.test.ts src/memory/__tests__/consolidation.test.ts src/memory/__tests__/embedder.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Zero `as unknown as` casts in the 7 test files under src/ (grep returns empty)
    - All 7 test files pass: `npx vitest --run` exits 0
    - TypeScript compiles without errors: `npx tsc --noEmit` exits 0 for these files
  </acceptance_criteria>
  <done>All test fixtures use properly-typed mocks. Zero `as unknown as` casts remain in test files under src/ (excluding node_modules and .claude/worktrees). All existing tests continue to pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for fork, send, webhooks, and mcp CLI commands</name>
  <files>
    src/cli/commands/fork.test.ts
    src/cli/commands/send.test.ts
    src/cli/commands/webhooks.test.ts
    src/cli/commands/mcp.test.ts
  </files>
  <read_first>
    src/cli/commands/fork.ts
    src/cli/commands/send.ts
    src/cli/commands/webhooks.ts
    src/cli/commands/mcp.ts
    src/cli/commands/schedules.test.ts
    src/cli/commands/threads.test.ts
    src/cli/commands/skills.test.ts
    src/cli/output.ts
  </read_first>
  <action>
    Create unit tests for the four untested CLI commands following the established pattern from schedules.test.ts, threads.test.ts, and skills.test.ts.

    **webhooks.test.ts** -- Test the exported `formatWebhooksTable` function:
    - Empty webhooks returns "No webhook identities configured"
    - Table contains column headers: AGENT, DISPLAY NAME, AVATAR, STATUS
    - Webhook with avatarUrl shows "yes" in avatar column
    - Webhook without avatarUrl shows "no" in avatar column
    - hasWebhookUrl=true shows "active", false shows "no url"
    - Multiple webhooks render correctly in same table

    **fork.test.ts** -- The fork command only has a register function that calls IPC, no exported format function. Test approach: mock `sendIpcRequest` and `cliLog`/`cliError` from `../output.js`, then invoke the registered command action programmatically:
    - Use vitest `vi.mock` for `../../ipc/client.js` and `../output.js`
    - Create a commander Command, call `registerForkCommand(program)`
    - Use `program.parseAsync(['node', 'test', 'fork', 'agent-name'])` to trigger the action
    - Assert `cliLog` was called with "Forked" output on success
    - Assert error handling: when sendIpcRequest throws ManagerNotRunningError, cliError is called with manager-not-running message
    - Assert error handling: when sendIpcRequest throws generic Error, cliError is called with error message

    **send.test.ts** -- Same pattern as fork:
    - Mock `sendIpcRequest` and `cliLog`/`cliError`
    - Success: `program.parseAsync(['node', 'test', 'send', 'agent-name', 'hello'])` logs "Message sent to agent-name"
    - ManagerNotRunningError path: logs manager not running message
    - Generic error path: logs error message
    - Options: --from and --priority are passed through to sendIpcRequest params

    **mcp.test.ts** -- The MCP command dynamically imports the server module:
    - Mock `../../mcp/server.js` with vi.mock
    - Success: startMcpServer resolves, no error logged
    - Error: startMcpServer rejects, cliError called with "Error starting MCP server: ..."

    For fork.test.ts and send.test.ts, mock `process.exit` with `vi.spyOn(process, 'exit').mockImplementation(() => { throw new Error('exit') })` to prevent actual process exit and verify it was called with code 1 on errors.
  </action>
  <verify>
    <automated>cd /home/jjagpal/.openclaw/workspace-coding && npx vitest --run src/cli/commands/fork.test.ts src/cli/commands/send.test.ts src/cli/commands/webhooks.test.ts src/cli/commands/mcp.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - fork.test.ts exists with tests for success output formatting, ManagerNotRunningError handling, generic error handling
    - send.test.ts exists with tests for success output, --from/--priority options, error handling
    - webhooks.test.ts exists with tests for formatWebhooksTable (empty, headers, avatar yes/no, status active/no url)
    - mcp.test.ts exists with tests for success and error paths
    - All 4 new test files pass: `npx vitest --run` exits 0
  </acceptance_criteria>
  <done>All four CLI commands (fork, send, webhooks, mcp) have unit tests that verify output formatting and error handling. Combined with existing tests for schedules, skills, threads, memory, and usage, every CLI command listed in DEBT-06 has test coverage.</done>
</task>

</tasks>

<verification>
1. `grep -rn "as unknown as" src/ --include="*.test.ts" | grep -v node_modules | grep -v ".claude/worktrees"` returns zero matches
2. `npx vitest --run` -- all tests pass (existing + new)
3. `npx tsc --noEmit` -- zero TypeScript errors
</verification>

<success_criteria>
- Zero `as unknown as` casts in test files under src/ (DEBT-05)
- All 9 CLI commands (schedules, skills, send, threads, webhooks, fork, memory, mcp, usage) have test files (DEBT-06)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/22-tech-debt-test-type-safety/22-01-SUMMARY.md`
</output>
