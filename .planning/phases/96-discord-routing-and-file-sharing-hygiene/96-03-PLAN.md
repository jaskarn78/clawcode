---
phase: 96-discord-routing-and-file-sharing-hygiene
plan: 03
plan_title: "clawcode_list_files auto-injected tool + findAlternativeFsAgents helper + ToolCallError (permission) for fs reads"
type: execute
wave: 2
depends_on: ["96-01"]
status: pending
estimated_hours: 3.0
files_modified:
  - src/manager/tools/clawcode-list-files.ts
  - src/manager/__tests__/clawcode-list-files.test.ts
  - src/manager/find-alternative-fs-agents.ts
  - src/manager/__tests__/find-alternative-fs-agents.test.ts
  - src/manager/session-config.ts
autonomous: true
gap_closure: false
requirements: [D-07, D-08]
must_haves:
  truths:
    - "Every agent automatically advertises clawcode_list_files in its tool list — auto-injected at src/manager/session-config.ts:421 (verified site, NOT the non-existent agent-bootstrap.ts per RESEARCH.md Pitfall 1) alongside Phase 94's clawcode_fetch_discord_messages and clawcode_share_file registrations"
    - "clawcode_list_files({path, depth?, glob?}) returns {entries: [{name, type, size?, mtime?}], truncated: boolean} — depth max 3, entries max 500 (D-07 token guard); truncation message at limit: '[...truncated, use glob filter or specific subpath]'"
    - "Path validation goes through 96-01's checkFsCapability(rawPath, snapshot, deps) BEFORE any fs.readdir call — single-source-of-truth boundary check (D-06). Out-of-snapshot paths refused with ToolCallError{errorClass: 'permission'} carrying findAlternativeFsAgents lookup result in alternatives field"
    - "findAlternativeFsAgents(absPath, deps) is a pure parallel of Phase 94's findAlternativeAgents — verbatim copy + s/CapabilityProbeSnapshot/FsCapabilitySnapshot/ — returns ranked list of agent names whose snapshot has the path readable (status='ready'). Used in D-08 ToolCallError suggestion text"
    - "clawcode_list_files is DI-pure: deps.checkFsCapability, deps.readdir, deps.stat, deps.findAlternativeFsAgents, deps.getFsCapabilitySnapshot — production wires real fs/snapshot at daemon edge; tests stub all"
    - "Glob filter is case-sensitive substring (RESEARCH.md Open Question 6 — picomatch NOT in deps; substring fallback per CONTEXT.md Discretion). Description: 'glob match is case-sensitive substring'"
    - "Failures wrap via Phase 94's wrapMcpToolError from src/manager/tool-call-error.ts — 5-value ErrorClass enum locked (transient | auth | quota | permission | unknown); fs ENOENT/EACCES → permission for boundary, unknown for depth/entries with rich suggestion"
  artifacts:
    - path: "src/manager/tools/clawcode-list-files.ts"
      provides: "clawcodeListFiles tool definition + handler + CLAWCODE_LIST_FILES_DEF schema; DI-pure; uses 96-01 checkFsCapability for boundary; depth/entries guards (3/500); case-sensitive substring glob filter; truncation message at limit"
      min_lines: 220
    - path: "src/manager/__tests__/clawcode-list-files.test.ts"
      provides: "TDD: 12+ tests — happy listing within snapshot, depth guard, entries guard with truncation message, glob filter (case-sensitive substring), boundary refusal with errorClass=permission and alternatives populated, ENOENT wrap, EACCES wrap, mtime+size populated for files, depth=0 returns just root, immutability"
      min_lines: 320
    - path: "src/manager/find-alternative-fs-agents.ts"
      provides: "findAlternativeFsAgents(absPath, deps): readonly string[] — pure-fn parallel of Phase 94's findAlternativeAgents; iterates per-agent fs-capability snapshots, returns sorted list of agent names whose snapshot has the path readable; Object.frozen output"
      min_lines: 110
    - path: "src/manager/__tests__/find-alternative-fs-agents.test.ts"
      provides: "TDD: 6+ tests — empty stateProvider; agent with path 'ready' included; agent with path 'degraded' excluded; multiple agents with path returned sorted; canonical-path normalization; immutability"
      min_lines: 180
    - path: "src/manager/session-config.ts (EXTEND)"
      provides: "Auto-injection at line 421+ — extend the existing block where Phase 94's CLAWCODE_FETCH_DISCORD_MESSAGES_DEF and CLAWCODE_SHARE_FILE_DEF are registered (imports at lines 58-59); add CLAWCODE_LIST_FILES_DEF as third auto-injected tool definition; add toolDefinitionsStr line for description in stable prefix"
  key_links:
    - from: "src/manager/session-config.ts (line 421+)"
      to: "src/manager/tools/clawcode-list-files.ts CLAWCODE_LIST_FILES_DEF"
      via: "Auto-injection alongside Phase 94's two tools — same site (RESEARCH.md confirms session-config.ts:421, NOT non-existent agent-bootstrap.ts)"
      pattern: "CLAWCODE_LIST_FILES_DEF|clawcode_list_files"
    - from: "src/manager/tools/clawcode-list-files.ts"
      to: "src/manager/fs-capability.ts checkFsCapability"
      via: "Boundary check BEFORE any fs.readdir; D-06 single-source-of-truth"
      pattern: "checkFsCapability\\("
    - from: "src/manager/tools/clawcode-list-files.ts"
      to: "src/manager/find-alternative-fs-agents.ts"
      via: "Permission-refusal ToolCallError populates alternatives via findAlternativeFsAgents"
      pattern: "findAlternativeFsAgents\\("
    - from: "src/manager/tools/clawcode-list-files.ts"
      to: "src/manager/tool-call-error.ts wrapMcpToolError"
      via: "Failures wrapped via Phase 94 5-value enum (transient | auth | quota | permission | unknown); size/missing/depth-exceeded → unknown with suggestion"
      pattern: "wrapMcpToolError\\("
---

<objective>
Implement D-07 (clawcode_list_files auto-injected tool) and D-08 (ToolCallError permission with alternative-agent suggestion). Every agent gets a third auto-injected tool alongside Phase 94's clawcode_fetch_discord_messages and clawcode_share_file — clawcode_list_files lets agents drill into directories the system-prompt block (96-02) advertises at the path-root level. Out-of-allowlist refusals carry a list of alternative agents whose snapshot has the path readable (cross-agent routing suggestion — Phase 94 plan 04 D-07 mirror).

Purpose: D-07 closes the "I see the path is RO but I can't see WHAT'S in it" gap — agent calls clawcode_list_files({path:'/home/jjagpal/.openclaw/workspace-finmentum/clients/'}) → sees tara-maffeo/, maffeo-household/, etc. — then drills further. Token-guarded (depth max 3, entries max 500). D-08 closes the "agent can't help, but maybe another agent can" gap — when fin-acquisition can't read /home/X but admin-clawdy can, the LLM gets that hint structured into ToolCallError.alternatives.

Critical correction from RESEARCH.md (Pitfall 1): The auto-injection site is `src/manager/session-config.ts:421-440` — NOT the non-existent `src/manager/agent-bootstrap.ts` referenced in CONTEXT.md. Verified: imports at session-config.ts:58-59, descriptions at lines 435-436. This plan extends that EXACT site with a third tool registration.

Output: clawcode-list-files.ts new tool + find-alternative-fs-agents.ts pure helper + auto-injection wiring at session-config.ts:421. TDD per tool. Reuses 96-01's checkFsCapability boundary + Phase 94's wrapMcpToolError.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-discord-routing-and-file-sharing-hygiene/96-CONTEXT.md
@.planning/phases/96-discord-routing-and-file-sharing-hygiene/96-RESEARCH.md
@.planning/phases/96-discord-routing-and-file-sharing-hygiene/96-01-PLAN.md
@.planning/phases/94-tool-reliability-self-awareness/94-04-PLAN.md
@.planning/phases/94-tool-reliability-self-awareness/94-05-PLAN.md

@src/manager/session-config.ts
@src/manager/tools/clawcode-fetch-discord-messages.ts
@src/manager/tools/clawcode-share-file.ts
@src/manager/tool-call-error.ts
@src/manager/find-alternative-agents.ts
</context>

<rules>
<rule id="1" name="Context">
This plan depends on 96-01 (FsCapabilitySnapshot type + checkFsCapability boundary). Wave 2 alongside 96-02 (system-prompt block — independent path) and 96-04 (clawcode_share_file extension — independent path).

Reuse:
- 96-01 checkFsCapability boundary primitive — single-source-of-truth (D-06); EVERY fs read site MUST go through this
- Phase 94 plan 04 wrapMcpToolError + 5-value ErrorClass enum (transient | auth | quota | permission | unknown) — REUSED VERBATIM; D-08 permission case fits within existing enum; size/missing/depth-exceeded/entries-exceeded → unknown with rich suggestion field
- Phase 94 plan 04 findAlternativeAgents pure-fn at src/manager/find-alternative-agents.ts — VERBATIM MIRROR for findAlternativeFsAgents (s/CapabilityProbeSnapshot/FsCapabilitySnapshot/, s/capabilityProbe.status/entry.status/)
- Phase 94 plan 05 auto-injection site at src/manager/session-config.ts:421-440 — VERIFIED: imports at lines 58-59 (CLAWCODE_FETCH_DISCORD_MESSAGES_DEF + CLAWCODE_SHARE_FILE_DEF); descriptions rendered at lines 435-436

Critical correction from RESEARCH.md (Pitfall 1): CONTEXT.md §code_context says agent-bootstrap.ts is the auto-injection site. THAT FILE DOES NOT EXIST. The actual site is session-config.ts:421-440.

Critical correction from RESEARCH.md (Pitfall 3): Phase 94's 5-value ErrorClass enum is locked. NO new errorClass values. Use 'permission' for boundary refusal, 'unknown' with rich suggestion for size/missing/depth-exceeded.
</rule>

<rule id="2" name="Approach">
TDD: Task 1 writes 18+ failing tests across two test files. Task 2 implements both modules + auto-injection wiring to GREEN.

Order in Task 2:
1. find-alternative-fs-agents.ts pure helper (smaller — input → ReadonlyMap iteration → sorted output)
2. clawcode-list-files.ts tool (boundary check + readdir + entries guard + glob filter + ToolCallError wrap)
3. Auto-injection wiring in session-config.ts at lines 58-59 (imports) + ~line 421 (registration block) + ~lines 435-436 (description rendering)

Path validation rules (clawcode_list_files):
1. Validate input shape (depth 0-3, glob string optional)
2. Call deps.checkFsCapability(input.path, deps.getFsCapabilitySnapshot()) — D-06 boundary
3. If allowed:false → wrap as ToolCallError errorClass='permission', message="path is outside this agent's fileAccess allowlist", suggestion includes "ask operator to add to clawcode.yaml fileAccess, or check if another agent has it in scope", alternatives=findAlternativeFsAgents(canonicalPath)
4. If allowed:true → proceed with readdir at canonicalPath
5. Recurse to depth (capped at MAX_LIST_FILES_DEPTH=3); accumulate entries (capped at MAX_LIST_FILES_ENTRIES=500)
6. Apply glob filter (case-sensitive substring) during enumeration
7. Return {entries, truncated: count >= MAX_ENTRIES}

Glob filter: case-sensitive `name.includes(glob)` if glob provided. Linux fs is case-sensitive; production target Linux.

Token guards:
- depth > 3 → ToolCallError errorClass='unknown', suggestion="depth max is 3; use a more specific subpath"
- depth = 0 → return immediate children (1-level listing)
- entries collected when count reaches 500 → stop adding; set truncated=true
</rule>

<rule id="3" name="Invariants — DO NOT BREAK">
- **Zero new npm deps.** `git diff package.json` empty. Reuses node:fs/promises (DI'd at edge), Phase 94 wrapMcpToolError, 96-01 checkFsCapability.
- **Both new modules DI-pure.** No direct `import * as fs from "node:fs"`, no `from "discord.js"` in tool body. Pinned by `! grep -E "from \"node:fs|from \"discord\\.js\"" src/manager/tools/clawcode-list-files.ts src/manager/find-alternative-fs-agents.ts`.
- **Auto-injected for ALL agents.** Pinned by `grep -q "CLAWCODE_LIST_FILES_DEF" src/manager/session-config.ts` exits 0; description rendered: `grep -c "clawcode_list_files" src/manager/session-config.ts` ≥ 2.
- **Single-source-of-truth boundary check.** clawcode_list_files MUST call deps.checkFsCapability BEFORE deps.readdir. Pinned by mock.invocationCallOrder test asserting order.
- **D-07 depth max LOCKED at 3.** Pinned by `grep -q "MAX_LIST_FILES_DEPTH = 3" src/manager/tools/clawcode-list-files.ts`.
- **D-07 entries max LOCKED at 500.** Pinned by `grep -q "MAX_LIST_FILES_ENTRIES = 500" src/manager/tools/clawcode-list-files.ts`.
- **Truncation message LOCKED.** `grep -q "\\[\\.\\.\\.truncated, use glob filter or specific subpath\\]" src/manager/tools/clawcode-list-files.ts`.
- **Phase 94 ErrorClass enum NOT EXTENDED.** Pinned by `! grep -E "errorClass.*\"size\"|errorClass.*\"missing\"" src/manager/tools/clawcode-list-files.ts` — size/missing/depth-exceeded → errorClass='unknown' with rich suggestion.
- **Failure path returns ToolCallError (not throws).** Pinned by tests asserting return shape kind === 'ToolCallError' for every error case.
- **D-08 alternatives populated.** Pinned by test "LF-PERMISSION-ALTERNATIVES" — boundary refuses; deps.findAlternativeFsAgents returns ['fin-tax']; ToolCallError carries alternatives.
- **Glob filter case-sensitive substring.** Pinned by test "LF-GLOB-CASE-SENSITIVE" — entry names ['Foo.pdf', 'foo.pdf']; glob='foo' → only 'foo.pdf' matches.
- **No-LEAK:** tool description doesn't reveal hardcoded internal paths.
- **Immutability (CLAUDE.md):** outputs Object.frozen; tool definition `as const`.
</rule>
</rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tool definitions + 18 failing tests for list-files + alternatives (RED)</name>
  <files>src/manager/__tests__/clawcode-list-files.test.ts, src/manager/__tests__/find-alternative-fs-agents.test.ts</files>
  <read_first>
    - .planning/phases/96-discord-routing-and-file-sharing-hygiene/96-CONTEXT.md (D-07 list-files spec; D-08 alternatives spec)
    - .planning/phases/96-discord-routing-and-file-sharing-hygiene/96-RESEARCH.md Pitfall 1 (auto-injection site is session-config.ts:421, NOT agent-bootstrap.ts) and Pitfall 3 (5-value ErrorClass enum locked)
    - .planning/phases/96-discord-routing-and-file-sharing-hygiene/96-01-PLAN.md (FsCapabilitySnapshot type + checkFsCapability surface)
    - .planning/phases/94-tool-reliability-self-awareness/94-04-PLAN.md (ToolCallError schema + 5-value enum + findAlternativeAgents shape)
    - .planning/phases/94-tool-reliability-self-awareness/94-05-PLAN.md (auto-injection pattern at session-config.ts:421-440 — verified line numbers)
    - src/manager/find-alternative-agents.ts (Phase 94 — VERBATIM MIRROR target)
    - src/manager/tool-call-error.ts (Phase 94 — wrapMcpToolError signature; 5-value enum literal)
    - src/manager/tools/clawcode-share-file.ts (Phase 94 plan 05 — auto-injected tool pattern reference)
    - src/manager/session-config.ts (lines 58-59 imports; line 421+ registration; lines 435-436 descriptions)
  </read_first>
  <behavior>
    Tests must fail (clawcode-list-files.ts and find-alternative-fs-agents.ts don't exist yet).

    **clawcode-list-files.test.ts (12 tests):**
    - LF-HAPPY: deps.checkFsCapability resolves with {allowed:true, canonicalPath:'/home/clawcode/.clawcode/agents/x/'}; deps.readdir returns [{name:'a.pdf', isFile:()=>true, isDirectory:()=>false}, {name:'subdir', isFile:()=>false, isDirectory:()=>true}]; deps.stat returns {size:1024, mtime: new Date('2026-04-25')}; input={path:'/home/clawcode/.clawcode/agents/x/'} → output.entries has 2 items, output.truncated === false, files have size+mtime, dirs only have name+type
    - LF-DEPTH-DEFAULT: input WITHOUT depth → recurse depth 1 (immediate children only)
    - LF-DEPTH-EXCEEDED: input.depth = 4 → return ToolCallError errorClass='unknown' with suggestion contains "depth max is 3"
    - LF-DEPTH-3: input.depth = 3 → readdir called recursively up to 3 levels deep
    - LF-ENTRIES-EXCEEDED: deps.readdir returns 600 entries; depth=1 → output.entries.length === 500, output.truncated === true
    - LF-GLOB-CASE-SENSITIVE: deps.readdir returns [{name:'Foo.pdf'}, {name:'foo.pdf'}, {name:'README.md'}]; input.glob='foo' → output.entries has 1 item ('foo.pdf' only — case-sensitive substring)
    - LF-GLOB-PDF: glob='.pdf' → entries with '.pdf' substring; 'foo.pdf' yes, 'foo.PDF' no, 'README.md' no
    - LF-BOUNDARY-REFUSED: deps.checkFsCapability resolves with {allowed:false, reason:'EACCES'}; input.path='/etc/passwd'; deps.findAlternativeFsAgents returns ['admin-clawdy', 'fin-tax'] → output is ToolCallError with errorClass='permission', message contains "outside" or "permission", suggestion mentions "fileAccess" and "alternatives"
    - LF-BOUNDARY-NO-ALTERNATIVES: deps.checkFsCapability refuses; deps.findAlternativeFsAgents returns [] → ToolCallError with empty alternatives; suggestion still mentions fileAccess
    - LF-ENOENT-WRAP: deps.checkFsCapability resolves with {allowed:true}; deps.readdir rejects with ENOENT → ToolCallError errorClass='unknown' with verbatim ENOENT message
    - LF-EACCES-AT-READDIR: deps.checkFsCapability resolves with {allowed:true} (snapshot says ready); deps.readdir rejects with EACCES (race condition) → ToolCallError errorClass='permission'
    - LF-ORDER-CHECK-BEFORE-READDIR: spy/mock asserts deps.checkFsCapability called BEFORE deps.readdir via mock.invocationCallOrder. D-06 single-source-of-truth boundary pin.

    **find-alternative-fs-agents.test.ts (6 tests):**
    - FAFS-1 EMPTY-PROVIDER: deps.listAgentNames returns []; findAlternativeFsAgents('/path', deps) → []
    - FAFS-2 SINGLE-READY: deps.listAgentNames=['fin-tax']; deps.fsStateProvider('fin-tax') returns Map([['/home/jjagpal/.openclaw/workspace-finmentum/', {status:'ready', mode:'ro', ...}]]); query absPath='/home/jjagpal/.openclaw/workspace-finmentum/' → ['fin-tax']
    - FAFS-3 DEGRADED-EXCLUDED: deps.fsStateProvider('fin-tax') returns Map([['/path', {status:'degraded'}]]); query '/path' → []
    - FAFS-4 MULTIPLE-SORTED: deps.listAgentNames=['zeta', 'alpha', 'middle']; all three have '/path' as ready → ['alpha', 'middle', 'zeta'] (sorted ASCII-ascending)
    - FAFS-5 SELF-EXCLUDED: deps.currentAgentName='fin-acquisition'; listAgentNames includes 'fin-acquisition' with '/path' ready → 'fin-acquisition' NOT in result
    - FAFS-6 IMMUTABLE: result is Object.frozen
  </behavior>
  <action>
    1. **Create `src/manager/__tests__/clawcode-list-files.test.ts`** with the 12 LF- tests:
       - Import from `../tools/clawcode-list-files.js` (file does not exist → import fails RED)
       - Import `ListFilesInput`, `ListFilesOutput`, `ListFilesDeps`, `MAX_LIST_FILES_DEPTH`, `MAX_LIST_FILES_ENTRIES`, `LIST_FILES_TRUNCATION_MESSAGE`, `clawcodeListFiles`, `CLAWCODE_LIST_FILES_DEF`
       - Use `vi.fn()` for all deps members
       - For LF-ORDER: use `expect(checkSpy.mock.invocationCallOrder[0]).toBeLessThan(readdirSpy.mock.invocationCallOrder[0])`
       - All paths use `/home/clawcode/.clawcode/agents/x/` or `/home/jjagpal/.openclaw/workspace-finmentum/` (CONTEXT.md fin-acquisition example)

    2. **Create `src/manager/__tests__/find-alternative-fs-agents.test.ts`** with the 6 FAFS- tests:
       - Import from `../find-alternative-fs-agents.js` (RED)
       - Use `vi.fn()` for deps.listAgentNames + deps.fsStateProvider

    3. Run `npx vitest run src/manager/__tests__/clawcode-list-files.test.ts src/manager/__tests__/find-alternative-fs-agents.test.ts --reporter=dot` — must FAIL.
  </action>
  <verify>
    <automated>npx vitest run src/manager/__tests__/clawcode-list-files.test.ts src/manager/__tests__/find-alternative-fs-agents.test.ts --reporter=dot 2>&1 | tail -10 | grep -E "failed|Cannot find module"</automated>
  </verify>
  <acceptance_criteria>
    - Test files exist with combined ≥ 18 it-blocks (12 + 6)
    - `grep -q "MAX_LIST_FILES_DEPTH\\|depth.*3" src/manager/__tests__/clawcode-list-files.test.ts` exits 0 (D-07 depth pin)
    - `grep -q "MAX_LIST_FILES_ENTRIES\\|500\\|truncated" src/manager/__tests__/clawcode-list-files.test.ts` exits 0 (D-07 entries pin)
    - `grep -q "case.sensitive\\|Foo\\.pdf" src/manager/__tests__/clawcode-list-files.test.ts` exits 0 (LF-GLOB pin)
    - `grep -q "errorClass.*permission\\|alternatives" src/manager/__tests__/clawcode-list-files.test.ts` exits 0 (D-08 pin)
    - `grep -q "invocationCallOrder\\|D-06" src/manager/__tests__/clawcode-list-files.test.ts` exits 0 (LF-ORDER pin)
    - `grep -q "ToolCallError" src/manager/__tests__/clawcode-list-files.test.ts` exits 0
    - `grep -q "sorted\\|ASCII" src/manager/__tests__/find-alternative-fs-agents.test.ts` exits 0 (FAFS-4 sorted-output pin)
    - `npx vitest run src/manager/__tests__/clawcode-list-files.test.ts src/manager/__tests__/find-alternative-fs-agents.test.ts --reporter=dot` exits NON-ZERO (RED gate)
  </acceptance_criteria>
  <done>18 failing tests covering happy path + boundary refusal with alternatives + depth/entries guards + case-sensitive glob + ENOENT/EACCES wrap + boundary-before-readdir order + cross-agent alternatives. RED gate confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: implement clawcode-list-files + find-alternative-fs-agents + auto-injection wiring (GREEN)</name>
  <files>src/manager/tools/clawcode-list-files.ts, src/manager/find-alternative-fs-agents.ts, src/manager/session-config.ts</files>
  <read_first>
    - src/manager/__tests__/clawcode-list-files.test.ts (the behaviors)
    - src/manager/__tests__/find-alternative-fs-agents.test.ts
    - src/manager/find-alternative-agents.ts (Phase 94 — VERBATIM MIRROR target; copy structure, replace types)
    - src/manager/tool-call-error.ts (Phase 94 wrapMcpToolError signature + 5-value enum)
    - src/manager/tools/clawcode-share-file.ts (Phase 94 plan 05 — auto-injected tool pattern; isPathInsideRoots equivalent for boundary)
    - src/manager/session-config.ts (lines 58-59: existing CLAWCODE_FETCH_DISCORD_MESSAGES_DEF + CLAWCODE_SHARE_FILE_DEF imports; lines 421+: registration block; lines 435-436: description rendering — extend each)
    - src/manager/fs-capability.ts (96-01 — checkFsCapability signature)
    - src/manager/persistent-session-handle.ts (FsCapabilitySnapshot type)
  </read_first>
  <action>
    **1. Implement `src/manager/find-alternative-fs-agents.ts`** — verbatim mirror of Phase 94 findAlternativeAgents:

    Copy src/manager/find-alternative-agents.ts structure. Replace:
    - Type import: `CapabilityProbeSnapshot` → `FsCapabilitySnapshot`
    - Status check: `entry?.capabilityProbe?.status === "ready"` → `entry?.status === "ready"` (FsCapabilitySnapshot has status at top level, not nested under capabilityProbe)
    - Comment: Phase 94 → Phase 96 D-08
    - Interface name: rename to `FindAlternativeFsAgentsDeps`, function to `findAlternativeFsAgents`

    Sort by agent name ASCII-ascending; exclude currentAgentName; Object.freeze the output.

    **2. Implement `src/manager/tools/clawcode-list-files.ts`:**

    DI-pure module. Defines:
    - `MAX_LIST_FILES_DEPTH = 3`
    - `MAX_LIST_FILES_ENTRIES = 500`
    - `LIST_FILES_TRUNCATION_MESSAGE = "[...truncated, use glob filter or specific subpath]"`
    - `CLAWCODE_LIST_FILES_DEF` const tool definition (name, description, input_schema with path/depth/glob)
    - `ListFilesInput`, `ListFilesEntry`, `ListFilesOutput`, `ListFilesDeps` exported types
    - `clawcodeListFiles(input, deps)` async function

    Implementation flow:
    1. Validate `depth ?? 1` — if > MAX_LIST_FILES_DEPTH return ToolCallError errorClass='unknown' suggestion "depth max is 3; use a more specific subpath"; if < 0 return error
    2. Get snapshot via `deps.getFsCapabilitySnapshot()`
    3. Call `deps.checkFsCapability(input.path, snapshot)` — D-06 boundary
    4. If `!result.allowed`: call `deps.findAlternativeFsAgents(input.path)`; build ToolCallError errorClass='permission' with suggestion text "Ask operator to add to clawcode.yaml fileAccess, or check if another agent has it in scope; alternatives: [list]" and pass alternatives array (verify wrapMcpToolError signature accepts alternatives — if not, embed in suggestion text only)
    5. If `result.allowed`: recurse into `listAtDepth(canonicalPath, depth, collected, glob, deps)` helper
    6. Helper: `await deps.readdir(absPath, {withFileTypes:true})`; iterate dirents; apply glob (`name.includes(glob)` if provided); for files call `deps.stat()` for size+mtime; for dirs recurse if `remainingDepth > 0`; stop adding when collected.length >= MAX_LIST_FILES_ENTRIES
    7. Return `Object.freeze({entries: Object.freeze(collected.slice(0, 500)), truncated: collected.length >= 500})`
    8. Wrap any thrown error via wrapMcpToolError with errorClass='unknown' or 'permission' depending on error message (`/EACCES/i` → permission, others → unknown)

    Tool description must NOT include hardcoded /home/clawcode paths — only abstract references like "the agent's fileAccess allowlist".

    **3. Wire auto-injection in `src/manager/session-config.ts`:**

    a) **Add import after line 59** (after Phase 94's two imports):
    ```ts
    import { CLAWCODE_LIST_FILES_DEF } from "./tools/clawcode-list-files.js";
    ```

    b) **Locate the auto-injection registration block at line 421-440**. Phase 94 plan 05 wires CLAWCODE_FETCH_DISCORD_MESSAGES_DEF + CLAWCODE_SHARE_FILE_DEF in this block. Add CLAWCODE_LIST_FILES_DEF in the same array/block following the existing pattern.

    c) **Add description line after lines 435-436** (Phase 94's two description lines):
    ```ts
    toolDefinitionsStr += `- **${CLAWCODE_LIST_FILES_DEF.name}**: ${CLAWCODE_LIST_FILES_DEF.description}\n`;
    ```

    d) **Production handler dispatch** — wire `toolHandlers["clawcode_list_files"]` with deps:
    - `checkFsCapability` from src/manager/fs-capability.ts (96-01) curried with current agent's deps
    - `readdir` from `node:fs/promises.readdir`
    - `stat` from `node:fs/promises.stat`
    - `getFsCapabilitySnapshot` from `handle.getFsCapabilitySnapshot.bind(handle)` (96-01 SessionHandle method)
    - `findAlternativeFsAgents` curried with deps from agent registry: `(absPath) => findAlternativeFsAgents(absPath, {listAgentNames, fsStateProvider, currentAgentName: agent})`
    - `log` from existing logger

    NOTE on Phase 94 ToolCallError `alternatives` field: read src/manager/tool-call-error.ts during impl. If wrapMcpToolError accepts alternatives via second-argument extension, pass directly. If not, embed alternatives in the suggestion string verbatim (e.g., "alternatives: [fin-tax, admin-clawdy]") — same operator-visible content, no schema extension required. Choose the simpler option that keeps the static-grep regression at exactly 5 ErrorClass values.

    **4. Run all tests:** `npx vitest run src/manager/__tests__/clawcode-list-files.test.ts src/manager/__tests__/find-alternative-fs-agents.test.ts --reporter=dot` — all 18+ tests pass.
  </action>
  <verify>
    <automated>npx vitest run src/manager/__tests__/clawcode-list-files.test.ts src/manager/__tests__/find-alternative-fs-agents.test.ts --reporter=dot && grep -q "CLAWCODE_LIST_FILES_DEF" src/manager/session-config.ts && grep -q "MAX_LIST_FILES_DEPTH = 3" src/manager/tools/clawcode-list-files.ts && grep -q "MAX_LIST_FILES_ENTRIES = 500" src/manager/tools/clawcode-list-files.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export async function clawcodeListFiles" src/manager/tools/clawcode-list-files.ts` exits 0
    - `grep -q "export function findAlternativeFsAgents" src/manager/find-alternative-fs-agents.ts` exits 0
    - `grep -q "MAX_LIST_FILES_DEPTH = 3" src/manager/tools/clawcode-list-files.ts` exits 0 (D-07 depth pinned)
    - `grep -q "MAX_LIST_FILES_ENTRIES = 500" src/manager/tools/clawcode-list-files.ts` exits 0 (D-07 entries pinned)
    - `grep -q "\\[\\.\\.\\.truncated, use glob filter or specific subpath\\]" src/manager/tools/clawcode-list-files.ts` exits 0 (truncation message pinned)
    - `grep -q "checkFsCapability" src/manager/tools/clawcode-list-files.ts` exits 0 (D-06 boundary call)
    - `grep -q "findAlternativeFsAgents" src/manager/tools/clawcode-list-files.ts` exits 0 (D-08 alternatives lookup)
    - `grep -q "wrapMcpToolError" src/manager/tools/clawcode-list-files.ts` exits 0 (94-04 reuse)
    - `! grep -E "errorClass.*\"size\"|errorClass.*\"missing\"" src/manager/tools/clawcode-list-files.ts` (Phase 94 5-value enum NOT extended)
    - `! grep -E "from \"node:fs\"" src/manager/tools/clawcode-list-files.ts` (DI-PURE — no fs imports in tool body)
    - `! grep -E "from \"discord\\.js\"" src/manager/tools/clawcode-list-files.ts` (DI-PURE)
    - `! grep -E "from \"node:fs\"" src/manager/find-alternative-fs-agents.ts` (DI-PURE)
    - `grep -q "CLAWCODE_LIST_FILES_DEF" src/manager/session-config.ts` exits 0 (auto-injection wired)
    - `grep -c "clawcode_list_files" src/manager/session-config.ts | tr -d ' '` ≥ 2 (import + description rendering)
    - `grep -q "case-sensitive substring\\|name\\.includes" src/manager/tools/clawcode-list-files.ts` exits 0 (glob impl)
    - `npx vitest run src/manager/__tests__/clawcode-list-files.test.ts src/manager/__tests__/find-alternative-fs-agents.test.ts --reporter=dot` exits 0 (≥ 18 tests pass)
    - `git diff package.json` empty (zero new npm deps)
  </acceptance_criteria>
  <done>clawcode-list-files.ts (DI-pure tool with D-06 boundary + D-07 token guards + D-08 alternatives suggestion) + find-alternative-fs-agents.ts (Phase 94 mirror) + auto-injection at session-config.ts:421 (third tool alongside fetch_discord_messages + share_file); 18+ tests green.</done>
</task>

</tasks>

<verification>
- Run: `npx vitest run src/manager/__tests__/clawcode-list-files.test.ts src/manager/__tests__/find-alternative-fs-agents.test.ts --reporter=dot` — ≥ 18 tests pass
- Verify: `! grep -rE "from \"node:fs\"|from \"discord\\.js\"" src/manager/tools/clawcode-list-files.ts src/manager/find-alternative-fs-agents.ts` — DI-purity
- Verify: `grep -q "CLAWCODE_LIST_FILES_DEF" src/manager/session-config.ts` — auto-injection
- Verify: `! grep -E "errorClass.*\"size\"|errorClass.*\"missing\"" src/manager/tools/clawcode-list-files.ts` — Phase 94 5-value enum NOT extended
- Verify: `git diff package.json` empty
</verification>

<success_criteria>
D-07 (clawcode_list_files): ✓ Auto-injected for every agent at session-config.ts:421 alongside Phase 94's two tools; depth max 3; entries max 500; case-sensitive substring glob; truncation message at limit; 96-01 checkFsCapability boundary BEFORE readdir.
D-08 (alternatives): ✓ findAlternativeFsAgents pure-fn mirror of Phase 94 findAlternativeAgents; permission-refusal ToolCallError carries alternatives via deps.findAlternativeFsAgents(); cross-agent routing suggestion in suggestion text.
DI-purity: ✓ Both modules free of node:fs / discord.js imports; production wires at daemon edge.
Phase 94 5-value ErrorClass enum NOT extended: ✓ Permission for boundary; unknown for size/missing/depth-exceeded with rich suggestion.
Auto-injection site verified: ✓ session-config.ts:421 (NOT non-existent agent-bootstrap.ts per RESEARCH.md Pitfall 1).
Zero new npm deps: ✓ git diff package.json empty.
</success_criteria>

<pitfalls>
**Static-grep regression pins (≥ 4 required):**
- `grep -q "MAX_LIST_FILES_DEPTH = 3" src/manager/tools/clawcode-list-files.ts` — D-07 depth max
- `grep -q "MAX_LIST_FILES_ENTRIES = 500" src/manager/tools/clawcode-list-files.ts` — D-07 entries max
- `grep -q "checkFsCapability" src/manager/tools/clawcode-list-files.ts` — D-06 boundary call site
- `! grep -E "errorClass.*\"size\"|errorClass.*\"missing\"" src/manager/tools/clawcode-list-files.ts` — Phase 94 5-value enum locked
- `! grep -E "from \"node:fs\"|from \"discord\\.js\"" src/manager/tools/clawcode-list-files.ts src/manager/find-alternative-fs-agents.ts` — DI-purity

**Behavioral traps to avoid:**
- DO NOT throw from clawcodeListFiles. Always return ToolCallError on failure. Throwing breaks SDK tool-result contract.
- DO NOT skip the boundary check. clawcode_list_files MUST call deps.checkFsCapability BEFORE deps.readdir — D-06 single-source-of-truth. Skipping creates security hole + prompt-cache mismatch.
- DO NOT extend Phase 94's 5-value ErrorClass enum. Phase 94 plan 04 has a static-grep regression at exactly 5 values. Adding 'size'/'missing' cascades through plans 04/05/07 consumers.
- DO NOT hardcode `/home/clawcode/` paths in the tool description. Description references abstract concepts ("the agent's fileAccess allowlist") so it works across deployment hosts (clawdy, dev box, etc.).
- DO NOT use case-INsensitive glob filter. Linux fs is case-sensitive; production is Linux. CONTEXT.md Open Question 6 settled this.
- DO NOT skip the auto-injection — without it, the LLM never sees clawcode_list_files in its tool list.
- DO NOT wire to the non-existent `src/manager/agent-bootstrap.ts`. RESEARCH.md Pitfall 1 verified the actual site is session-config.ts:421-440 with imports at lines 58-59.
- DO NOT add `findAlternativeFsAgents` to the Phase 94 plan 04 module. It's a NEW module (`src/manager/find-alternative-fs-agents.ts`) parallel to find-alternative-agents.ts — not extending Phase 94's file (which deals with MCP capability, different shape).
- DO NOT recurse without depth-cap. The recursion guard in `listAtDepth` MUST decrement remainingDepth and refuse recursion at 0 — otherwise a 100-level deep symlink chain DoS's the agent.
</pitfalls>

<self_review_checklist>
- [ ] D-07 honored: clawcode_list_files auto-injected at session-config.ts:421 (NOT agent-bootstrap.ts); depth max 3; entries max 500; truncation message present
- [ ] D-08 honored: findAlternativeFsAgents pure mirror of Phase 94 findAlternativeAgents; permission ToolCallError carries alternatives
- [ ] D-06 honored: checkFsCapability called BEFORE readdir (single-source-of-truth boundary)
- [ ] Phase 94 5-value ErrorClass enum NOT extended (no size/missing variants — those map to unknown with suggestion)
- [ ] Auto-injection site verified: session-config.ts:421-440 (Phase 94 plan 05 confirmed)
- [ ] Glob filter is case-sensitive substring (`name.includes(glob)`)
- [ ] DI-purity: no node:fs / discord.js imports in tool body or alternatives module
- [ ] Failure path returns ToolCallError (never throws)
- [ ] Tool description abstract — no hardcoded /home/clawcode paths
- [ ] Zero new npm deps
- [ ] 18+ tests green; existing session-config tests still green
</self_review_checklist>

<output>
After completion, create `.planning/phases/96-discord-routing-and-file-sharing-hygiene/96-03-SUMMARY.md` documenting:
- clawcode_list_files — third auto-injected tool at session-config.ts:421 (verified site)
- D-07 token guards: depth max 3, entries max 500, case-sensitive substring glob, truncation message at limit
- D-06 boundary check call: checkFsCapability(path, snapshot) BEFORE readdir
- D-08 alternatives: findAlternativeFsAgents pure mirror of Phase 94 findAlternativeAgents; populated in ToolCallError suggestion text
- Phase 94 5-value ErrorClass enum NOT extended: permission for boundary refusal; unknown for size/missing/depth-exceeded with rich suggestion
- DI-purity: no fs/discord.js imports in tool body
- Auto-injection site verification: session-config.ts:421-440 (NOT non-existent agent-bootstrap.ts)
- Production wiring at daemon edge (readdir/stat from node:fs/promises; getFsCapabilitySnapshot from SessionHandle)
</output>
</content>
</invoke>