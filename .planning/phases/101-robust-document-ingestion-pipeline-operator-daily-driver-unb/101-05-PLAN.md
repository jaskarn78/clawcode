---
phase: 101-robust-document-ingestion-pipeline-operator-daily-driver-unb
plan: 05
type: execute
wave: 5
depends_on: [101-01, 101-02, 101-03, 101-04]
files_modified:
  - .planning/phases/101-robust-document-ingestion-pipeline-operator-daily-driver-unb/101-DEPLOY-LOG.md
  - .planning/phases/101-robust-document-ingestion-pipeline-operator-daily-driver-unb/101-PON-UAT-LOG.md
autonomous: false
requirements: [SC-1, SC-2, SC-3, SC-4, SC-5, SC-6, SC-7, SC-8, SC-10]

must_haves:
  truths:
    - "scripts/deploy-clawdy.sh runs cleanly on clawdy with tesseract-ocr installed; service restarts; md5 verify passes"
    - "Pon UAT: ingest_document on fin-acquisition agent over Pon 2025 tax return produces ExtractedTaxReturn matching ≥95% of operator truth values"
    - "phase101-ingest telemetry visible in journalctl on clawdy (grep tag works as documented)"
    - "Performance targets met: text-PDF ≥ 5 pages/sec; scanned-PDF Tesseract ≥ 0.5 pages/sec; retrieval p95 ≤ 200ms with reranker"
    - "24h soak: no QUEUE_FULL coalescer storms (per Phase 105 lessons), no recordIngestAlert severity=error events beyond known-acceptable, no memory growth anomalies on the agent processes"
    - "Phase-end push: `git push origin master` only after Plan 05 closes the phase per feedback_push_at_phase_end.md"
  artifacts:
    - path: ".planning/phases/101-.../101-DEPLOY-LOG.md"
      provides: "deploy command output + md5 verification + timestamps"
    - path: ".planning/phases/101-.../101-PON-UAT-LOG.md"
      provides: "UAT field-by-field comparison vs truth fixture + accuracy %"
  key_links:
    - from: "scripts/deploy-clawdy.sh"
      to: "clawdy systemd service"
      via: "scp + sudo cp + systemctl restart"
      pattern: "deploy-clawdy.sh"
    - from: "ingest_document MCP tool (fin-acquisition)"
      to: "tests/fixtures/pon-2024-tax-return.pdf"
      via: "live invocation, taskHint:'high-precision', extract:'both'"
      pattern: "ingest_document"
---

<objective>
Ship Plans 01-04 to clawdy via the canonical deploy script. Run the Pon UAT live against the deployed daemon. Verify the performance targets. Open a 24h soak window. Close the phase with the operator-gated phase-end push.

Purpose: Plans 01-04 are code-only; Phase 101 does not ship until the operator runs `scripts/deploy-clawdy.sh` on clawdy. This plan owns the deploy + verify + soak choreography. Per `feedback_no_auto_deploy.md` and the Ramy active-channel gate, deploy is non-autonomous.

Output: 101-DEPLOY-LOG.md (deploy artifacts), 101-PON-UAT-LOG.md (UAT results), and the phase-end push to origin/master.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/101-robust-document-ingestion-pipeline-operator-daily-driver-unb/101-CONTEXT.md
@.planning/phases/101-robust-document-ingestion-pipeline-operator-daily-driver-unb/101-01-SUMMARY.md
@.planning/phases/101-robust-document-ingestion-pipeline-operator-daily-driver-unb/101-02-SUMMARY.md
@.planning/phases/101-robust-document-ingestion-pipeline-operator-daily-driver-unb/101-03-SUMMARY.md
@.planning/phases/101-robust-document-ingestion-pipeline-operator-daily-driver-unb/101-04-SUMMARY.md
@CLAUDE.md
@scripts/deploy-clawdy.sh
@tests/fixtures/pon-2024-truth.json
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-box → clawdy host | deploy SCP + sudo cp + systemd restart cross host boundary |
| Pon UAT artifact → fin-acquisition agent | real client document crosses into the agent's workspace |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-15 | Tampering | deploy bytes diverge from build | mitigate | scripts/deploy-clawdy.sh already runs md5 verification post-cp; capture md5 in 101-DEPLOY-LOG.md |
| T-101-16 | Repudiation | deploy without operator confirmation | mitigate | per `feedback_no_auto_deploy.md` + Ramy gate — this plan is `autonomous: false`; every deploy task pauses for explicit operator confirmation in the same turn |
| T-101-17 | Information Disclosure | UAT-log includes Pon's real tax data | mitigate | 101-PON-UAT-LOG.md records only field NAMES + match/no-match status + accuracy %, never field values; explicit guard in Task 2's action |
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 (T01-DEPLOY-CHECKPOINT): Operator confirms deploy window + Ramy not active</name>
  <what-built>
    Plans 01-04 are committed to master locally. Deploy is operator-gated. Before running `scripts/deploy-clawdy.sh`, the operator must explicitly confirm:

    1. **Ramy gate (`feedback_ramy_active_no_deploy.md`)**: Ramy is NOT currently in an active client thread on #fin-acquisition. Restart would disrupt a live conversation; deploy is held until Ramy confirms quiet OR a genuine emergency authorizes the disruption.
    2. **Deploy window**: operator has time to monitor the 24h soak that follows. A deploy at 4:30pm on a Friday with no monitoring availability is NOT acceptable.
    3. **Tesseract precheck readiness**: clawdy already has `tesseract-ocr` installed (or the operator is ready to `ssh clawdy 'sudo apt-get install -y tesseract-ocr'` if the deploy-clawdy.sh precheck from Plan 01 surfaces the gap).
  </what-built>
  <how-to-verify>
    Operator inspects:
    1. `#fin-acquisition` channel — is Ramy mid-thread?
    2. Calendar — next 24h available for soak monitoring?
    3. `ssh clawdy 'which tesseract'` returns a path? If not, `ssh clawdy 'sudo apt-get install -y tesseract-ocr'` first.
  </how-to-verify>
  <resume-signal>
    Type "deploy" or "ship it" to authorize the deploy. Type "hold — ramy active" or "hold — no window" to pause. Type "hold — fix tesseract first" to install before proceeding.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2 (T02): Execute scripts/deploy-clawdy.sh + capture artifacts</name>
  <files>
    .planning/phases/101-robust-document-ingestion-pipeline-operator-daily-driver-unb/101-DEPLOY-LOG.md
  </files>
  <read_first>
    scripts/deploy-clawdy.sh,
    CLAUDE.md (Deploy section)
  </read_first>
  <action>
    Run the deploy via the canonical script:
    ```bash
    scripts/deploy-clawdy.sh 2>&1 | tee /tmp/phase101-deploy.log
    ```

    Capture the following into a new file `.planning/phases/101-.../101-DEPLOY-LOG.md`:
    - Timestamp (UTC).
    - Full stdout/stderr of the deploy run.
    - Local build md5 (`md5sum dist/<output>` — read the script for exact artifact path).
    - Remote md5 after cp (`ssh clawdy 'md5sum /opt/clawcode/...'`).
    - `ssh clawdy 'systemctl status clawcode-manager --no-pager | head -20'` output.
    - `ssh clawdy 'journalctl -u clawcode-manager --since "5 min ago" | grep -E "phase101-ingest|ERROR|started"'` to confirm the new code is live and no startup errors.

    If md5 mismatch: ABORT, rollback via `scripts/deploy-clawdy.sh --no-build` to re-stage, and surface a structured failure to the operator — DO NOT proceed to Pon UAT.

    Verify the tesseract precheck did its job: search the deploy log for either "tesseract precheck passed" (if the script logs it explicitly) or absence of the "ERROR: tesseract-ocr not installed" line.
  </action>
  <verify>
    <automated>ssh clawdy 'systemctl is-active clawcode-manager' | grep -q "^active$" && echo "service-active"</automated>
  </verify>
  <done>
    Service is `active`; md5 matches; recent journalctl shows no startup ERROR; 101-DEPLOY-LOG.md committed.
  </done>
  <acceptance_criteria>
    - `ssh clawdy 'systemctl is-active clawcode-manager'` returns `active`
    - md5 dev box vs md5 clawdy match (recorded verbatim in deploy log)
    - `grep -c "ERROR" .planning/phases/101-.../101-DEPLOY-LOG.md` reports only known-acceptable lines (none from the manager startup window)
    - `grep -c "phase101-ingest" .planning/phases/101-.../101-DEPLOY-LOG.md` ≥ 1 (telemetry tag visible in journalctl probe)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3 (T03): Run Pon UAT live against deployed fin-acquisition agent (SC-8)</name>
  <files>
    .planning/phases/101-robust-document-ingestion-pipeline-operator-daily-driver-unb/101-PON-UAT-LOG.md
  </files>
  <read_first>
    tests/fixtures/pon-2024-truth.json,
    .planning/phases/101-...101-CONTEXT.md (D-06, Pon UAT acceptance)
  </read_first>
  <action>
    Live invocation via fin-acquisition's Discord channel OR via direct CLI/IPC probe — pick the shape that matches CLAUDE.md operational practice. Recommend Discord-channel path because that's the real operator workflow.

    Steps:
    1. Operator copies `tests/fixtures/pon-2024-tax-return.pdf` (or the corresponding local path) onto clawdy at a known location: `scp tests/fixtures/pon-2024-tax-return.pdf clawdy:/home/clawcode/.clawcode/agents/fin-acquisition/inbox/pon-2024.pdf`.
    2. Operator types in #fin-acquisition: `please ingest /home/clawcode/.clawcode/agents/fin-acquisition/inbox/pon-2024.pdf with the ingest_document MCP tool, taskHint='high-precision', extract='both', and post the resulting ExtractedTaxReturn JSON back to the channel`.
    3. fin-acquisition invokes the MCP tool; the result lands in the channel.
    4. Operator (or a follow-up automation step) compares the returned ExtractedTaxReturn against `tests/fixtures/pon-2024-truth.json` field-by-field.

    Write `.planning/phases/101-.../101-PON-UAT-LOG.md` recording (PII guard T-101-17 — record NAMES + STATUS only, never values):
    ```
    | field path | match (Y/N) | notes |
    |---|---|---|
    | taxYear | Y | |
    | taxpayerName | Y | |
    | box1Wages | Y | |
    | scheduleC.netProfit | Y | |
    | scheduleC.grossReceipts | Y | |
    | scheduleC.expenses[*].category | Y (3/3) | |
    | scheduleC.expenses[*].amount | Y (3/3) | |
    | backdoorRoth.amount | Y | |
    | backdoorRoth.year | Y | |
    | iraDeduction | Y | |
    | qbi.deduction | Y | |
    | extractionSchemaVersion | Y | "v1" |

    Accuracy: 12/12 = 100% (≥95% required for SC-8)
    Telemetry observed: phase101-ingest docSlug=pon-2024 type=text-pdf|scanned-pdf pages=N ocrUsed=<backend> chunksCreated=N p50_ms=N p95_ms=N
    ```

    If accuracy < 95%: log the missing fields, surface a structured failure to the operator. Phase does NOT close. Options: (a) re-extract with `force: true` after a config tweak, (b) operator opens a follow-up phase for schema refinement.

    Also probe retrieval after ingestion (SC-6 + SC-10 live check): send a follow-up message "what was Pon's Schedule C net profit?" — observe that fin-acquisition surfaces the correct value via Phase 90 RRF + reranker. Capture the agent's reply (PII-redacted) + the journalctl `phase101-ingest reranker-applied` event proving the reranker fired.
  </action>
  <verify>
    <automated>grep -c "Accuracy:.*≥95%\\|Accuracy: 10[0-9]%\\|Accuracy: 9[5-9]" .planning/phases/101-*/101-PON-UAT-LOG.md</automated>
  </verify>
  <done>
    101-PON-UAT-LOG.md shows ≥95% accuracy; follow-up retrieval probe demonstrates cross-ingest+reranker working live.
  </done>
  <acceptance_criteria>
    - 101-PON-UAT-LOG.md exists and lists all D-06 fields by name
    - PII guard: `grep -E '\\$[0-9]+|[0-9]{3}-[0-9]{2}-[0-9]{4}' 101-PON-UAT-LOG.md` returns 0 matches (no field values committed)
    - Reported accuracy ≥ 95%
    - journalctl on clawdy shows `phase101-ingest reranker-applied` at least once during the UAT window
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 (T04-SOAK-CHECKPOINT): 24h soak gate</name>
  <what-built>
    After Tasks 1-3 close, the operator opens a 24h soak window. During the window, monitor for regressions:
  </what-built>
  <how-to-verify>
    24h after Task 2 deploy timestamp, operator runs:
    1. `ssh clawdy 'journalctl -u clawcode-manager --since "24h ago" | grep -c "QUEUE_FULL"'` → expect 0 (Phase 105 lessons; ingest_document MUST NOT trigger coalescer storms).
    2. `ssh clawdy 'journalctl -u clawcode-manager --since "24h ago" | grep -c "phase101-ingest-alert.*severity.*error"'` → expect 0 OR only known-acceptable (operator reviews each one).
    3. `ssh clawdy 'systemctl status clawcode-manager --no-pager'` → service has not crash-restarted.
    4. Per-agent memory check (`ssh clawdy 'ps aux | grep claude | awk "{print \$6}"'`) — no anomalous growth beyond baseline.
    5. Performance probe: `grep "phase101-ingest.*p95_ms" recent journalctl` → median p95 across the soak window ≤ 200ms (with reranker enabled).

    Operator records observations in the 101-SUMMARY.md (created during the orchestrator wrap-up).
  </how-to-verify>
  <resume-signal>
    Type "soak passed — close phase" to authorize the phase-end push. Type "soak issue: <description>" to open follow-up work before closing.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 5 (T05): Phase-end push (`feedback_push_at_phase_end.md`)</name>
  <files>
    .planning/phases/101-robust-document-ingestion-pipeline-operator-daily-driver-unb/101-DEPLOY-LOG.md
  </files>
  <read_first>
    .planning/phases/101-...101-DEPLOY-LOG.md,
    .planning/phases/101-...101-PON-UAT-LOG.md
  </read_first>
  <action>
    Per `feedback_push_at_phase_end.md`: no push between plans during 01-04; batch push only after Plan 05 SUMMARY commits.

    Steps:
    1. `git log --oneline -20` to show the Phase 101 commit fan (Plans 01 T01..T06, Plan 02 T02..T05, Plan 03 T01..T03, Plan 04 T01..T02, Plan 05 T02 + T03 + the phase SUMMARY).
    2. `git status` should be clean.
    3. `git push origin master`.
    4. Append the push timestamp + commit SHA range to 101-DEPLOY-LOG.md.

    Per `feedback_no_auto_deploy.md`: this push does NOT trigger a redeploy. The clawdy host already received the deploy in Task 2; the push is repository-sync only.
  </action>
  <verify>
    <automated>git log origin/master..HEAD --oneline | wc -l | grep -q '^0$' && echo "pushed-clean"</automated>
  </verify>
  <done>
    `origin/master` is in sync with local; 101-DEPLOY-LOG.md updated with the push timestamp + SHA range; phase is closed.
  </done>
  <acceptance_criteria>
    - `git log origin/master..HEAD --oneline` returns no commits (everything pushed)
    - 101-DEPLOY-LOG.md contains the push timestamp + commit SHA range as an appended section
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Plan 05 produces 2 autonomous commits (T02 deploy log, T03 UAT log) + the push step (T05).
- T01 and T04 are operator-gated checkpoints with no commit.
- Final state: service active on clawdy; Pon UAT ≥95% in the log; 24h soak clean; origin/master pushed.
</verification>

<success_criteria>
- SC-1..SC-7 + SC-10 verified via live deploy + journalctl telemetry probes.
- SC-8 (Pon UAT ≥95% accuracy) recorded in 101-PON-UAT-LOG.md with PII-redacted field-by-field table.
- 24h soak gate passes: no QUEUE_FULL events, no severity=error ingest alerts, no crash-restarts, no memory anomalies, retrieval p95 ≤ 200ms with reranker.
- Phase-end push complete per `feedback_push_at_phase_end.md`.
- No auto-deploy without operator confirmation per `feedback_no_auto_deploy.md` + Ramy gate.
</success_criteria>

<output>
Create `.planning/phases/101-robust-document-ingestion-pipeline-operator-daily-driver-unb/101-05-SUMMARY.md` when done, then the orchestrator creates the phase-level `101-SUMMARY.md` aggregating Plans 01-05. The phase-level summary records: final accuracy %, deploy SHA range, soak observations, any follow-up phases opened (Phase 101.5 if U8 / DocumentStore-FTS surfaces, Phase 101.x if Pon UAT regression refinement needed, Phase 999.43 unblock for auto-ingest of Discord attachments).
</output>
