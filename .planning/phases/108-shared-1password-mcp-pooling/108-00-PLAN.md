---
phase: 108-shared-1password-mcp-pooling
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/__fakes__/fake-pooled-child.ts
  - tests/__fakes__/fake-broker-socket.ts
  - src/mcp/broker/__tests__/broker.test.ts
  - src/mcp/broker/__tests__/pooled-child.test.ts
  - src/mcp/broker/__tests__/shim-server.test.ts
  - src/mcp/broker/__tests__/integration.test.ts
  - src/cli/commands/__tests__/mcp-broker-shim.test.ts
  - src/heartbeat/checks/__tests__/mcp-broker.test.ts
autonomous: true
requirements: [POOL-01, POOL-02, POOL-03, POOL-04, POOL-05, POOL-06, POOL-07, POOL-08]
must_haves:
  truths:
    - "Every Phase 108 production behavior has at least one RED vitest case before any production file is written"
    - "All RED tests fail with a deterministic 'not yet implemented' shape (no flakes, no syntax errors)"
    - "Existing test suite stays green — no Phase 108 file imports break a pre-existing test"
  artifacts:
    - path: tests/__fakes__/fake-pooled-child.ts
      provides: "child_process.ChildProcess-shaped fake with controllable stdin capture, stdout 'data' event injection, exit-code injection"
      min_lines: 60
    - path: tests/__fakes__/fake-broker-socket.ts
      provides: "net.Socket-shaped duplex pair for shim ↔ broker tests"
      min_lines: 40
    - path: src/mcp/broker/__tests__/broker.test.ts
      provides: "RED suites for token-grouping (POOL-01), lifecycle SIGTERM+respawn (POOL-04), per-agent semaphore (POOL-05), audit log fields (POOL-06)"
      contains: "describe"
    - path: src/mcp/broker/__tests__/pooled-child.test.ts
      provides: "RED suites for id-rewriting (POOL-03) + initialize cache-and-replay (Pitfall 1) + crash → in-flight error fanout"
      contains: "describe"
    - path: src/mcp/broker/__tests__/shim-server.test.ts
      provides: "RED suites for connection handshake (agent name + tokenHash) and per-connection refcount"
      contains: "describe"
    - path: src/mcp/broker/__tests__/integration.test.ts
      provides: "RED integration suite for POOL-08 (5 agents × 2 tokens → 2 children) and synthetic burst fan-out"
      contains: "describe"
    - path: src/cli/commands/__tests__/mcp-broker-shim.test.ts
      provides: "RED suite for shim stdio-bridge (agent stdin ↔ broker socket ↔ agent stdout) and reconnect-on-daemon-restart"
      contains: "describe"
    - path: src/heartbeat/checks/__tests__/mcp-broker.test.ts
      provides: "RED suite for POOL-07 (heartbeat fails when pool dead, passes when alive)"
      contains: "describe"
  key_links:
    - from: "src/mcp/broker/__tests__/*.test.ts"
      to: "tests/__fakes__/fake-pooled-child.ts"
      via: "import { FakePooledChild } from '../../../../tests/__fakes__/fake-pooled-child.js'"
      pattern: "fake-pooled-child"
    - from: "src/mcp/broker/__tests__/shim-server.test.ts"
      to: "tests/__fakes__/fake-broker-socket.ts"
      via: "import { FakeBrokerSocketPair } from ... fake-broker-socket"
      pattern: "fake-broker-socket"
---

<objective>
Wave 0 — write the failing-test scaffolding for the entire Phase 108 broker. NO production code created in this plan. Every behavior locked in CONTEXT.md decisions and RESEARCH.md §6 gets a RED test case so subsequent waves have a clear GREEN target.

Purpose: Establish the test contract before implementation so 108-01..04 can be implemented against a fixed set of expectations. Prevents drift between implementation and intent.
Output: Two shared fakes + six RED test files. `npx vitest run src/mcp/broker/ src/cli/commands/__tests__/mcp-broker-shim.test.ts src/heartbeat/checks/__tests__/mcp-broker.test.ts` shows expected RED count; rest of suite stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-shared-1password-mcp-pooling/108-CONTEXT.md
@.planning/phases/108-shared-1password-mcp-pooling/108-RESEARCH.md

# Existing test patterns to mirror (style, not behavior)
@src/mcp/__tests__
@src/cli/commands/__tests__

<interfaces>
<!-- Anchors from RESEARCH.md — broker production code does NOT exist yet. -->
<!-- These are the SHAPES the RED tests will pin down. -->

JSON-RPC framing (verified — newline-delimited JSON, no Content-Length):
```typescript
// Per node_modules/@modelcontextprotocol/sdk/dist/cjs/shared/stdio.js
type JsonRpcRequest = { jsonrpc: "2.0"; id: number | string; method: string; params?: unknown };
type JsonRpcResponse = { jsonrpc: "2.0"; id: number | string; result?: unknown; error?: { code: number; message: string; data?: unknown } };
type JsonRpcNotification = { jsonrpc: "2.0"; method: string; params?: unknown }; // no id
```

SDK MCP stdio config (verified — no transport injection escape hatch):
```typescript
// node_modules/@anthropic-ai/claude-agent-sdk/sdk.d.ts:868-873
export declare type McpStdioServerConfig = {
    type?: 'stdio';
    command: string;
    args?: string[];
    env?: Record<string, string>;
};
```

Token-hash convention (Phase 104 SEC-07 invariant):
```typescript
// All broker logs use this short hash, NEVER the literal token.
const tokenHash = crypto.createHash("sha256").update(token).digest("hex").slice(0, 8);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared fakes (FakePooledChild + FakeBrokerSocketPair)</name>
  <files>tests/__fakes__/fake-pooled-child.ts, tests/__fakes__/fake-broker-socket.ts</files>
  <action>
    Create two shared test fakes used by every RED suite below.

    File 1 — `tests/__fakes__/fake-pooled-child.ts`:
    - Export class `FakePooledChild` that implements the subset of `child_process.ChildProcess` the broker uses: `stdin: Writable`, `stdout: Readable`, `stderr: Readable`, `pid: number`, `exitCode: number | null`, `kill(signal?: string): boolean`, plus EventEmitter `on('exit', ...)`, `on('error', ...)`.
    - Provide test-only helpers:
      - `pushStdoutLine(json: object): void` — emits `JSON.stringify(json) + "\n"` as a `'data'` event on stdout (so broker's readline picks it up).
      - `consumeStdinLines(): string[]` — drains everything written to stdin since last call, split on `\n`, returns parsed JSON objects' raw strings.
      - `simulateExit(code: number, signal?: string): void` — sets `exitCode`, emits `'exit'`.
      - `simulateError(err: Error): void` — emits `'error'`.
    - Use `node:stream` PassThrough for stdin/stdout/stderr. Do NOT import production broker code.
    - PID: incrementing counter starting at 90001 (avoids collision with anything realistic in test logs).

    File 2 — `tests/__fakes__/fake-broker-socket.ts`:
    - Export `createFakeBrokerSocketPair(): { client: net.Socket; server: net.Socket }` — two PassThrough-backed duplex streams cross-wired so client.write goes to server reads and vice versa. Mirror the surface of `net.Socket` used by the broker shim-server (read/write/end/'close'/'error').
    - Use the existing pattern from `src/ipc/__tests__/` if a similar helper exists; otherwise build from PassThrough pair.
    - Export helper `closePair(pair, reason?: string)` that emits 'close' on both halves to simulate daemon restart for Pitfall 5 tests.

    Both fakes: pure TypeScript, no production-code imports, no network, no real spawn.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "tests/__fakes__/(fake-pooled-child|fake-broker-socket)\.ts" || echo "fakes typecheck clean"</automated>
  </verify>
  <done>Both fake files exist, typecheck cleanly under project tsconfig, are importable from any vitest test, and contain zero imports from `src/mcp/broker/` (production code does not yet exist).</done>
</task>

<task type="auto">
  <name>Task 2: RED tests for PooledChild + Broker (data plane + lifecycle)</name>
  <files>src/mcp/broker/__tests__/pooled-child.test.ts, src/mcp/broker/__tests__/broker.test.ts</files>
  <action>
    Write the failing test specs that pin down the data-plane (id rewriting, initialize cache) and control-plane (token grouping, semaphore, lifecycle, audit) behaviors. Production modules `src/mcp/broker/pooled-child.ts` and `src/mcp/broker/broker.ts` do NOT exist yet — these tests will import from those paths and fail at the import step (deterministic RED).

    File 1 — `src/mcp/broker/__tests__/pooled-child.test.ts`:
    Cover (per RESEARCH.md §3 patterns 2–3):
    - `describe("PooledChild — id rewriting (POOL-03)")` — agent A sends id=1, agent B sends id=1 simultaneously; assert child sees two distinct pool ids; responses route back to correct agent.
    - `describe("PooledChild — initialize cache-and-replay (Pitfall 1)")` — first agent's `initialize` round-trips to child; second agent's `initialize` is answered from cache without reaching child; child sees exactly ONE initialize line on stdin.
    - `describe("PooledChild — crash → in-flight error fanout")` — two in-flight calls when child emits 'exit'; both agents receive a structured JSON-RPC error response (per CONTEXT decision 3) with code matching a documented broker error code constant.
    - `describe("PooledChild — notifications pass through unrewritten")` — JSON-RPC notification (no `id`) from child arriving on stdout fans out to all current agents (or follows whatever broadcast policy CONTEXT.md implies; if ambiguous, write the test to expect "drop" and add a TODO note in the test).

    Use FakePooledChild for the child handle. Import shape: `import { PooledChild } from "../pooled-child.js"`.

    File 2 — `src/mcp/broker/__tests__/broker.test.ts`:
    Cover:
    - `describe("Broker — token grouping (POOL-01)")` — three agents on token A and one agent on token B → exactly two PooledChild instances spawned; tokenHashes distinct; no cross-routing.
    - `describe("Broker — last-ref SIGTERM with drain (POOL-04, Pitfall 3)")` — single-agent broker; agent disconnects with one in-flight call → broker waits up to 2s for inflight to drain → SIGTERMs child; if drain ceiling hit, in-flight call resolves with structured error before kill.
    - `describe("Broker — auto-respawn on crash (POOL-04)")` — child emits exit while broker has live agent connections; broker spawns new child within 2s; subsequent agent request succeeds against new child PID.
    - `describe("Broker — per-agent semaphore (POOL-05)")` — single agent dispatches 5 concurrent requests; assert max 4 are 'in-flight' against pooled child at any moment; 5th waits in FIFO queue until one completes.
    - `describe("Broker — audit log fields (POOL-06)")` — wrap pino with a memory transport (or pass `log` as a vi.fn-spied child); assert every dispatched call emits a log line with `component:"mcp-broker"`, `pool:"1password-mcp:<tokenHash>"`, `agent`, `turnId`, `tool` fields.
    - `describe("Broker — token redaction (Phase 104 SEC-07)")` — pass a token literal `"ops_TESTTOKEN_FAKE_XYZ"`; capture every log line emitted; assert NO line contains the substring `ops_TESTTOKEN_FAKE_XYZ`. Assert tokenHash IS present.

    Spawn function: tests inject `spawnFn = vi.fn(() => new FakePooledChild(...))` to avoid real child_process.

    Import shape: `import { OnePasswordMcpBroker } from "../broker.js"`.

    All tests RED. Each `it()` body should be fully written (assertions, fake setup, expectations) — only the imported module is missing. This way Wave 1 GREEN is "make imports resolve and assertions pass" with no test rewrites.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/broker/__tests__/pooled-child.test.ts src/mcp/broker/__tests__/broker.test.ts 2>&1 | grep -E "FAIL|Test Files.*failed" | head -20</automated>
  </verify>
  <done>Both test files exist with all `describe`/`it` blocks fully fleshed-out; running them produces deterministic RED (failures from missing module imports, not syntax errors); the rest of the test suite (existing tests) stays green when run.</done>
</task>

<task type="auto">
  <name>Task 3: RED tests for shim-server, mcp-broker-shim CLI, integration, and heartbeat</name>
  <files>src/mcp/broker/__tests__/shim-server.test.ts, src/mcp/broker/__tests__/integration.test.ts, src/cli/commands/__tests__/mcp-broker-shim.test.ts, src/heartbeat/checks/__tests__/mcp-broker.test.ts</files>
  <action>
    Round out the RED scaffolding for the shim transport layer, end-to-end integration, agent-side CLI, and heartbeat liveness check.

    File 1 — `src/mcp/broker/__tests__/shim-server.test.ts`:
    Cover unix-socket-server-side behavior (uses `FakeBrokerSocketPair`, no real socket files):
    - `describe("ShimServer — connection handshake")` — shim opens connection, sends handshake `{agent: "fin-acquisition", tokenHash: "abc12345"}` as the first JSON line; broker accepts and registers. Missing/invalid agent name → server closes connection with error.
    - `describe("ShimServer — per-connection refcount")` — N shim connections on same tokenHash → broker increments tokenHash refcount to N; each disconnect decrements; reaches 0 → broker SIGTERMs pool child (verifies POOL-04 from socket layer).
    - `describe("ShimServer — daemon shutdown closes all connections")` — `broker.preDrainNotify()` rejects new connections; existing connections allowed to finish; `broker.shutdown(2000)` closes all sockets and SIGTERMs all pool children within ceiling.
    - `describe("ShimServer — token redaction in handshake errors")` — handshake from a connection whose env (test injection) carries literal token never echoes the token in error messages; only tokenHash visible.

    Import: `import { ShimServer } from "../shim-server.js"`.

    File 2 — `src/mcp/broker/__tests__/integration.test.ts`:
    End-to-end with all three real broker modules wired together (still using FakePooledChild for the npx layer):
    - `describe("Broker integration — POOL-08")` — boot broker, simulate 5 agent shim connections (3 on token A, 2 on token B); assert exactly 2 FakePooledChild spawns occurred; each child sees exactly one initialize.
    - `describe("Broker integration — synthetic burst (decision §smoke #3)")` — 5 agents on token A each send `tools/call password_read` simultaneously; all 5 receive correct responses; broker logs show all 5 calls dispatched to the single pool child PID.
    - `describe("Broker integration — crash + auto-respawn (decision §smoke #4)")` — kill the FakePooledChild for token A; broker respawns within 2s; next call succeeds; in-flight calls at crash moment received structured errors.
    - `describe("Broker integration — token grouping cardinality (POOL-01)")` — `broker.getPoolStatus()` returns exactly 2 entries with correct refcounts.

    Import: same as above.

    File 3 — `src/cli/commands/__tests__/mcp-broker-shim.test.ts`:
    Cover `clawcode mcp-broker-shim --pool 1password` CLI subcommand (uses FakeBrokerSocketPair as the broker side; spawns the shim's `runShim()` exported function — NOT a child_process spawn):
    - `describe("mcp-broker-shim — stdio bridge")` — agent stdin line → fake socket sees it; fake socket emits line → agent stdout sees it. Bytes are passed through; no JSON re-parsing inside the shim (per RESEARCH.md "the shim is a dumb byte pipe").
    - `describe("mcp-broker-shim — handshake on connect")` — first line shim writes to socket is handshake JSON with agent name (from `CLAWCODE_AGENT` env) and tokenHash (from `OP_SERVICE_ACCOUNT_TOKEN` env hashed in-shim).
    - `describe("mcp-broker-shim — daemon restart triggers non-zero exit (Pitfall 5)")` — fake socket closes → shim's exported `runShim` resolves with non-zero exit code so SDK reconnects.
    - `describe("mcp-broker-shim — token never logged literal")` — pino spy + assert no log line contains the test token literal `ops_TESTTOKEN_FAKE_XYZ`.

    Import: `import { runShim } from "../mcp-broker-shim.js"`.

    File 4 — `src/heartbeat/checks/__tests__/mcp-broker.test.ts`:
    Cover heartbeat check (POOL-07):
    - `describe("mcpBrokerCheck — passes when all referenced pools alive")` — broker reports all pools `alive:true`; check returns `CheckStatus.healthy`.
    - `describe("mcpBrokerCheck — fails when any referenced pool dead")` — broker reports one pool with `alive:false` AND `agentRefCount > 0`; check returns `CheckStatus.failed` with descriptive message.
    - `describe("mcpBrokerCheck — ignores dead pools with zero refs")` — pool that's been drained has alive:false but agentRefCount:0; check stays healthy (broker has correctly cleaned up).
    - `describe("mcpBrokerCheck — does NOT poll 1Password (no synthetic password_read)")` — assert the check's implementation does not call any tool dispatch path (RESEARCH.md §5 — internal liveness check only). This is best done by inspecting that the broker's `getPoolStatus()` is the only method called.

    Import: `import { mcpBrokerCheck } from "../mcp-broker.js"`.

    All four files RED. Add comment header in each: `// RED — Phase 108-00 — Wave 0 scaffolding. Production target: <path>.`
  </action>
  <verify>
    <automated>npx vitest run src/mcp/broker/__tests__/ src/cli/commands/__tests__/mcp-broker-shim.test.ts src/heartbeat/checks/__tests__/mcp-broker.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Four test files exist, deterministic RED (module-import failures or expect failures, no syntax errors). `npx vitest run` for the rest of the suite (everything outside the new test files) stays green — verify by `npx vitest run --reporter=verbose 2>&1 | grep -E "FAIL" | grep -v "src/mcp/broker\|mcp-broker-shim\|heartbeat/checks/__tests__/mcp-broker"`.</done>
</task>

</tasks>

<verification>
Run the full Wave 0 verification:

```
# All Phase 108 RED tests fail with deterministic shape
npx vitest run src/mcp/broker/ src/cli/commands/__tests__/mcp-broker-shim.test.ts src/heartbeat/checks/__tests__/mcp-broker.test.ts

# Rest of suite stays green
npx vitest run --exclude "src/mcp/broker/**" --exclude "src/cli/commands/__tests__/mcp-broker-shim.test.ts" --exclude "src/heartbeat/checks/__tests__/mcp-broker.test.ts"

# Typecheck (fakes + tests under tsconfig)
npx tsc --noEmit
```

Expected: RED count > 0 and stable across two runs. No tests outside the Wave 0 scope newly fail.
</verification>

<success_criteria>
- All 8 files in `files_modified` exist with content
- All RED tests fail deterministically (missing-module errors, NOT syntax errors)
- Token literal `ops_TESTTOKEN_FAKE_XYZ` is the only test-token shape used; no real `ops_` strings appear
- No production code under `src/mcp/broker/` or `src/cli/commands/mcp-broker-shim.ts` exists yet
- Existing tests stay green
- `npx tsc --noEmit` passes
</success_criteria>

<output>
After completion, create `.planning/phases/108-shared-1password-mcp-pooling/108-00-SUMMARY.md` with: list of RED tests created (counts per file), shape of fakes, expected GREEN gates for each downstream plan (108-01..04 know which describe blocks they need to flip GREEN), known TODOs noted in tests.

Commit message: `test(108-00): RED scaffolding for 1password-mcp broker — fakes + 6 test files`
</output>
