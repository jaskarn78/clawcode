---
phase: 999.47-homelab-inventory-canonical-source-of-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/INVENTORY.md
  - .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/NETWORK.md
  - .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/ACCESS.md
  - .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/DRIFT.md
  - .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/RETIRED.md
  - .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/.gitignore
  - .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/scripts/bootstrap-clawdy-repo.sh
autonomous: false
requirements: [SC-1, SC-6]

must_haves:
  truths:
    - "Standalone git repo at /home/clawcode/homelab/ exists on clawdy, owned by the clawcode service user (uid match with the systemd unit)."
    - "The repo contains INVENTORY.md, NETWORK.md, ACCESS.md, DRIFT.md, RETIRED.md and a scripts/ directory."
    - "INVENTORY.md, NETWORK.md, and ACCESS.md are populated with the BACKLOG anchor entries (clawdy / Unraid / OC server / Mac mini, the four VMs, the two novnc containers, the three tunnels, and the listed DNS entries)."
    - "An agent on clawdy can `cat /home/clawcode/homelab/INVENTORY.md` and read a coherent inventory of the homelab."
    - "All credential references in ACCESS.md are `op://` references — no literal secrets land in the markdown."
    - "Git history contains the initial-commit by the operator (NOT the clawcode-refresh identity)."
  artifacts:
    - path: ".planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/INVENTORY.md"
      provides: "Authored initial INVENTORY.md template populated with anchor entries from BACKLOG."
      contains: "clawdy"
    - path: ".planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/NETWORK.md"
      provides: "Authored initial NETWORK.md template with the three Tailscale IPs + three Cloudflare tunnels + DNS entries."
      contains: "100.98.211.108"
    - path: ".planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/ACCESS.md"
      provides: "Authored initial ACCESS.md template with SSH/VNC command snippets + op:// references only."
      contains: "op://"
    - path: ".planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/DRIFT.md"
      provides: "Empty DRIFT.md skeleton with `## Drift Items` and `## Refresh Failures` sections."
    - path: ".planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/RETIRED.md"
      provides: "Empty RETIRED.md skeleton."
    - path: ".planning/phases/999.47-homelab-inventory-canonical-source-of-truth/scripts/bootstrap-clawdy-repo.sh"
      provides: "One-shot bash script to be SSH-deployed to clawdy that initializes the repo, copies templates into it, sets file ownership to the clawcode user, and creates the initial git commit."
      min_lines: 40
  key_links:
    - from: "templates/ACCESS.md"
      to: "1Password vaults"
      via: "op:// reference syntax"
      pattern: "op://[a-zA-Z0-9_-]+/[^/]+/[^[:space:]]+"
    - from: "templates/INVENTORY.md"
      to: "templates/NETWORK.md"
      via: "Markdown anchor cross-links"
      pattern: "\\[.*\\]\\(NETWORK\\.md#"
---

<objective>
Author the canonical homelab markdown corpus (INVENTORY / NETWORK / ACCESS / DRIFT / RETIRED) in the workspace-coding repo as templates, then ship a one-shot bash bootstrapper that — when run on clawdy — initializes `/home/clawcode/homelab/` as a standalone git repo, copies the templates in, and creates the initial commit.

Purpose: Establish the single source of truth (D-02) with the BACKLOG anchor entries already populated (SC-6) so subsequent plans have a real target to refresh, ingest, and verify against. Authoring the templates inside the workspace-coding repo means they are bisectable here; the bootstrap script is the deploy boundary between dev box and clawdy.

Output:
- `templates/{INVENTORY,NETWORK,ACCESS,DRIFT,RETIRED}.md` in this phase directory
- `scripts/bootstrap-clawdy-repo.sh` (idempotent — refuses to overwrite an already-initialized repo)
- An initialized git repo at `/home/clawcode/homelab/` on clawdy with one operator-authored commit
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.47-homelab-inventory-canonical-source-of-truth/999.47-CONTEXT.md
@.planning/phases/999.47-homelab-inventory-canonical-source-of-truth/BACKLOG.md
@CLAUDE.md

<interfaces>
<!-- Anchor facts from CONTEXT.md / BACKLOG.md that the templates MUST embed verbatim. -->
<!-- Executor should use these directly — no further codebase exploration needed. -->

Tailscale-IP anchors (preserve, do not re-discover):
  clawdy        100.98.211.108    Linux / /opt/clawcode / clawcode systemd user
  Unraid        100.117.234.17    libvirt+docker host
  OC server     100.71.14.96      legacy OpenClaw box
  Jas's MBP     (look up at refresh)
  work MBP      (look up at refresh)
  Mac mini      (look up at refresh)

VMs on Unraid (anchor):
  WebServer, Windows11-Min, Moltbot-VM, HomeAssistant

Containers on Unraid (anchor):
  novnc-auth, novnc-win11

Cloudflare tunnels (anchor):
  vm.jjagpal.me, dashboard.finmentum.com, notify.earlscheibconcord.com

DNS / domains (anchor):
  finmentum.cloudflareaccess.com  (Cloudflare team domain)
  IONOS domain holdings           (enumerate at refresh time)

op:// reference syntax (from clawcode.yaml env interpolation):
  op://<vault>/<item>/<field>
  example: op://clawdbot/win11-vm-password/password

MEMORY.md pointer line (verbatim, used in Plan 04 not here, included for cross-doc consistency):
  - [Homelab inventory](/home/clawcode/homelab/INVENTORY.md) — canonical source of truth for hosts, VMs, containers, access
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the five canonical markdown templates</name>
  <read_first>
    - .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/999.47-CONTEXT.md (sections `<domain>`, `<specifics>`, `<decisions>` — anchor entries and op:// convention)
    - .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/BACKLOG.md (host/VM/container/tunnel/DNS lists)
    - CLAUDE.md (Deploy section — clawdy host facts must match exactly)
  </read_first>
  <files>
    .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/INVENTORY.md
    .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/NETWORK.md
    .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/ACCESS.md
    .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/DRIFT.md
    .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/RETIRED.md
    .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/.gitignore
  </files>
  <action>
    Author the five canonical markdown documents plus a .gitignore for the standalone repo. Use the partition per CONTEXT.md `Claude's Discretion`: INVENTORY = "what exists + what it does" (hosts, VMs, containers, services); NETWORK = "addressing + topology" (Tailscale IPs, port maps, DNS, Cloudflare tunnels); ACCESS = "how to log in" (SSH/VNC/RDP CLI snippets + op:// item refs). Cross-link between docs with markdown anchors (e.g. `[clawdy](INVENTORY.md#clawdy)`).

    INVENTORY.md structure (one `## <Host or Resource Name>` section per anchor entry):
    - Stable-facts subsection (`### Stable Facts`): `Purpose:` `Owner:` `Lifecycle:` `Tags:` — for anchor entries, leave `Purpose` / `Owner` as `*(operator: fill in)*` placeholders per D-01 hybrid bootstrap.
    - Live-state subsection (`### Live State`): a fenced code block marked `<!-- refresh.sh: managed -->` with `status:`, `last_seen:`, `source:` fields. For initial commit these are placeholder values like `status: unknown` and `last_seen: not-yet-refreshed`. This is the region Plan 03's refresh.sh rewrites.
    - Required anchor sections at minimum: clawdy, Unraid, OC server, Jas's MBP, work MBP, Mac mini, WebServer (VM), Windows11-Min (VM), Moltbot-VM, HomeAssistant (VM), novnc-auth (container), novnc-win11 (container), ClawCode daemon (service), OpenClaw (service).

    NETWORK.md structure:
    - `## Tailscale Nodes` table with columns `Host | Tailscale IP | OS | Notes`. Populate the three known IPs verbatim (clawdy 100.98.211.108, Unraid 100.117.234.17, OC server 100.71.14.96); mark Jas's MBP / work MBP / Mac mini IPs as `*(refresh.sh will populate)*`.
    - `## Cloudflare Tunnels` table with columns `Hostname | Tunnel ID | Service | Behind`. Rows for vm.jjagpal.me, dashboard.finmentum.com, notify.earlscheibconcord.com with `Tunnel ID` left as `*(refresh.sh will populate)*`.
    - `## DNS` section: `finmentum.cloudflareaccess.com` (Cloudflare team domain), plus an `### IONOS holdings` subsection placeholder.
    - `## Live State` fenced block (`<!-- refresh.sh: managed -->`) — same shape as INVENTORY.

    ACCESS.md structure:
    - `## <Host>` sections mirroring INVENTORY anchors. Each carries `### SSH` (fenced bash `ssh ...` snippet), `### VNC/RDP` (if applicable), and `### Credentials` listing the `op://` refs. EVERY credential MUST be an `op://<vault>/<item>/<field>` reference — NEVER a literal value. If the vault/item is unknown for an anchor, use `op://clawdbot/PLACEHOLDER-<host>-ssh/password` and add a `*(operator: confirm op-item path)*` note.

    DRIFT.md structure:
    - Top-level prose explaining that this file is auto-managed by refresh.sh and operator-reviewed.
    - `## Drift Items` (empty section, header only — Plan 03 appends to it).
    - `## Refresh Failures` (empty section, header only — Plan 03 appends to it per D-04c).

    RETIRED.md structure:
    - Top-level prose: "Items moved here on operator promotion from DRIFT.md or manual decommission. Never delete — preserve history."
    - `## Retired Items` (empty section).

    .gitignore:
    - `.refresh-last.json` (the JSON contract surface Plan 02 reads; transient, do not commit)
    - `*.tmp`, `*.swp`, `.DS_Store`

    No literal secrets anywhere. Use UTF-8, LF line endings, trailing newline. Keep each file under 400 lines (split anchor entries if necessary).
  </action>
  <verify>
    <automated>test -f .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/INVENTORY.md && test -f .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/NETWORK.md && test -f .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/ACCESS.md && test -f .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/DRIFT.md && test -f .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/RETIRED.md && grep -q "100.98.211.108" .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/NETWORK.md && grep -q "100.117.234.17" .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/NETWORK.md && grep -q "100.71.14.96" .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/NETWORK.md && grep -q "WebServer" .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/INVENTORY.md && grep -q "novnc-win11" .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/INVENTORY.md && grep -q "vm.jjagpal.me" .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/NETWORK.md && grep -q "^op://" .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/ACCESS.md || grep -q "op://" .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/ACCESS.md</automated>
  </verify>
  <acceptance_criteria>
    - All five templates exist as separate files (plus .gitignore).
    - INVENTORY.md contains anchor sections for all 6 hosts + 4 VMs + 2 containers + 2 services listed in `<interfaces>`.
    - NETWORK.md contains the three known Tailscale IPs verbatim and all three Cloudflare tunnel hostnames.
    - ACCESS.md contains zero literal secrets — grep for common secret tokens (`password:`, `BEGIN PRIVATE KEY`, `sk-`, `ghp_`) returns no matches outside `op://` reference strings.
    - DRIFT.md has `## Drift Items` and `## Refresh Failures` headers and no other content.
    - Every credential reference in ACCESS.md matches the `op://<vault>/<item>/<field>` pattern.
    - Each `<!-- refresh.sh: managed -->` block is uniquely-identifiable so Plan 03's awk/sed-style rewrite can locate it without ambiguity.
  </acceptance_criteria>
  <done>
    Templates committed to workspace-coding; `grep -E '(password|sk-[A-Za-z0-9]|ghp_)' .planning/phases/999.47-.../templates/*.md | grep -v 'op://'` returns nothing. The corpus is ready to be SSH-shipped to clawdy by Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author the bootstrap-clawdy-repo.sh deploy script</name>
  <read_first>
    - .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/templates/INVENTORY.md (just-authored by Task 1)
    - scripts/deploy-clawdy.sh (existing SSH+sudo deploy template — pattern reference for ~/.clawcode-deploy-pw usage)
    - CLAUDE.md (Deploy section)
  </read_first>
  <files>
    .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/scripts/bootstrap-clawdy-repo.sh
  </files>
  <action>
    Write a `bash` script that — when invoked locally on this dev box — SSHes into clawdy (Tailscale `100.98.211.108`) using the same sudo-pw mechanism as `scripts/deploy-clawdy.sh` (read from `~/.clawcode-deploy-pw`, chmod-600) and performs the following IDEMPOTENT bootstrap on the remote host as the `clawcode` user:

    1. **Pre-flight**: refuse to run if `/home/clawcode/homelab/.git/` already exists (echo "[skip] repo already initialized at /home/clawcode/homelab" and exit 0). Per D-02, this script initializes — it does NOT update.
    2. **Stage templates**: `scp` the contents of `.planning/phases/999.47-.../templates/` into a temp dir under `/tmp/homelab-bootstrap-$$/` on clawdy.
    3. **Create repo**: `sudo -u clawcode mkdir -p /home/clawcode/homelab && sudo chown clawcode:clawcode /home/clawcode/homelab`.
    4. **Copy templates**: `sudo -u clawcode cp -r /tmp/homelab-bootstrap-$$/. /home/clawcode/homelab/` (preserving the `.gitignore`). Then `mkdir -p /home/clawcode/homelab/scripts/` (empty — Plan 03 fills it).
    5. **Git init + initial commit**: `sudo -u clawcode bash -lc 'cd /home/clawcode/homelab && git init -b main && git add . && git -c user.name="<OPERATOR_NAME>" -c user.email="<OPERATOR_EMAIL>" commit -m "feat(homelab): initial inventory scaffold (Phase 999.47 Plan 01)"'`. The operator name/email are CLI args (`--operator-name`, `--operator-email`) — REQUIRED, no defaults — per D-02a (operator commits vs refresh commits are distinct identities).
    6. **No remote add** per D-02b (local-only repo at v1).
    7. **Verification echo**: print the new repo's `git log --oneline -1` to stdout for operator confirmation.
    8. **Cleanup**: `sudo rm -rf /tmp/homelab-bootstrap-$$`.

    Script header MUST include `set -euo pipefail`, a comment block referencing Phase 999.47 Plan 01 + D-02 + D-02a + D-02b, and a `--dry-run` flag that prints all SSH/scp commands without executing them (mirror `scripts/deploy-clawdy.sh --dry-run`). The script does NOT restart any service, does NOT touch `/opt/clawcode/`, and does NOT push to a git remote — per the no-auto-deploy and Ramy-active feedback memories.

    Failure behaviour: any non-zero exit from a remote command propagates via `set -e`; on failure the script does NOT leave a half-initialized `/home/clawcode/homelab/` (run `rm -rf` cleanup on the remote path in a `trap ERR` handler, but ONLY if the repo did not pre-exist at script start).
  </action>
  <verify>
    <automated>bash -n .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/scripts/bootstrap-clawdy-repo.sh && grep -q "set -euo pipefail" .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/scripts/bootstrap-clawdy-repo.sh && grep -q -- "--dry-run" .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/scripts/bootstrap-clawdy-repo.sh && grep -q -- "--operator-name" .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/scripts/bootstrap-clawdy-repo.sh && grep -q -- "--operator-email" .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/scripts/bootstrap-clawdy-repo.sh && grep -q "/home/clawcode/homelab" .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/scripts/bootstrap-clawdy-repo.sh && ! grep -E "systemctl|service.*restart|/opt/clawcode" .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/scripts/bootstrap-clawdy-repo.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n` syntax-check passes.
    - Script exits 0 with a skip message when `/home/clawcode/homelab/.git/` already exists (idempotent re-run).
    - `--dry-run` produces a complete plan of SSH/scp commands without contacting clawdy.
    - Script requires `--operator-name` and `--operator-email` CLI args; rejects invocation without them.
    - No mention of `systemctl`, `service restart`, or `/opt/clawcode/` anywhere in the script (refresh script must not restart services).
    - On `set -e` failure mid-bootstrap, trap cleans up `/home/clawcode/homelab/` IFF it did not pre-exist.
  </acceptance_criteria>
  <done>
    Script is syntax-clean, lint-clean (`shellcheck` exit 0 if available), and ready for the operator-gated checkpoint in Task 3 to execute it on clawdy.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Operator-gated bootstrap of /home/clawcode/homelab/ on clawdy</name>
  <action>Operator-driven verification — execute the steps in <how-to-verify> after Task 2 succeeds. This is a checkpoint, not implementation work. See `<what-built>`, `<how-to-verify>`, and `<resume-signal>` below for the operator runbook.</action>
  <what-built>
    Task 1 authored five canonical homelab templates with all BACKLOG anchor entries.
    Task 2 authored `bootstrap-clawdy-repo.sh` — a one-shot, idempotent, operator-gated SSH+sudo deploy that initializes `/home/clawcode/homelab/` on clawdy as a standalone git repo with the templates committed under the operator's identity.

    THIS CHECKPOINT IS NOT AUTONOMOUS per:
    - CLAUDE.md Ramy-active deploy hold (do not run any clawdy-touching script without operator green-light in the same turn).
    - `feedback_no_auto_deploy.md` — operator must say "deploy" / "ship it" before any production write.
  </what-built>
  <how-to-verify>
    Operator-run, in order:

    1. Confirm Ramy is NOT live in `#fin-acquisition` and a quiet deploy window exists (per `feedback_ramy_active_no_deploy.md`).
    2. From this dev box, dry-run first:
       ```
       bash .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/scripts/bootstrap-clawdy-repo.sh \
         --operator-name "Jaskarn Jagpal" \
         --operator-email "jjagpal101@gmail.com" \
         --dry-run
       ```
       Verify the printed plan touches only `/home/clawcode/homelab/` and `/tmp/homelab-bootstrap-*` on clawdy. NO `systemctl`, NO `/opt/clawcode/`.
    3. Execute live (drop `--dry-run`). Watch the final `git log --oneline -1` output — expect one commit, author = operator, subject = `feat(homelab): initial inventory scaffold (Phase 999.47 Plan 01)`.
    4. SSH to clawdy and confirm:
       ```
       ssh clawcode@100.98.211.108 \
         'ls -la /home/clawcode/homelab/ && cd /home/clawcode/homelab && git log --oneline -5 && cat INVENTORY.md | head -50'
       ```
       Expected: five `.md` files + `scripts/` dir + `.git/` + `.gitignore`; one commit by operator; INVENTORY.md anchor sections visible.
    5. Re-run the bootstrap script (without `--dry-run`) — expect `[skip] repo already initialized` and exit 0 (idempotency proof).
  </how-to-verify>
  <resume-signal>
    Operator approves with "approved" (and confirms `git log` shows the initial commit), OR describes failures with specific output for fix-and-retry.
  </resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev box → clawdy via SSH | bootstrap-clawdy-repo.sh transmits operator commit identity + template bytes |
| markdown templates → git history | ACCESS.md must contain zero literal secrets — every credential is an `op://` ref |
| sudo execution as clawcode user | `sudo -u clawcode` writes under `/home/clawcode/homelab/` only — never `/opt/clawcode/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99947-01 | Information disclosure | ACCESS.md templates | mitigate | Hard rule: every credential reference is `op://<vault>/<item>/<field>` syntax; grep gate in Task 1 acceptance criteria refuses literal `password:` / `sk-` / `ghp_` tokens outside `op://` strings. |
| T-99947-02 | Tampering | bootstrap-clawdy-repo.sh sudo flow | mitigate | Reuse the chmod-600 `~/.clawcode-deploy-pw` pattern from `scripts/deploy-clawdy.sh`; no inline passwords; require explicit operator-name + operator-email args (no defaults). |
| T-99947-03 | Denial of service | Idempotency on re-run | mitigate | Pre-flight check refuses to re-run when `/home/clawcode/homelab/.git/` already exists; `trap ERR` cleans up half-bootstrapped state IFF the repo did not pre-exist. |
| T-99947-04 | Elevation of privilege | Script touching `/opt/clawcode/` | mitigate | Static-grep gate in Task 2 acceptance criteria: script MUST NOT contain `systemctl`, `service`, or `/opt/clawcode/` strings — this is a documentation repo, not a deploy mechanism. |
| T-99947-05 | Information disclosure | Future remote push leaking internal IPs | accept | D-02b locks the repo to local-only at v1; operator can `git remote add` later when they have an offsite plan. |

</threat_model>

<verification>
- After operator runs Task 3 on clawdy: `ssh clawcode@100.98.211.108 'cd /home/clawcode/homelab && git log --pretty=format:"%an <%ae>" -1'` returns the operator's identity, NOT `clawcode-refresh`.
- `ssh clawcode@100.98.211.108 'find /home/clawcode/homelab -maxdepth 1 -type f -name "*.md"'` returns exactly the five expected `.md` files.
- `ssh clawcode@100.98.211.108 'grep -c "op://" /home/clawcode/homelab/ACCESS.md'` returns a positive integer (>0).
- `ssh clawcode@100.98.211.108 'stat -c "%U:%G" /home/clawcode/homelab'` returns `clawcode:clawcode`.
</verification>

<success_criteria>
- SC-1 fully covered: standalone repo at `/home/clawcode/homelab/` on clawdy with INVENTORY.md / NETWORK.md / ACCESS.md / DRIFT.md / RETIRED.md / `scripts/` (empty) / `.git/` and an initial commit.
- SC-6 covered: INVENTORY / NETWORK / ACCESS populated with the BACKLOG anchor entries (clawdy / Unraid / OC server / Mac mini, the four VMs, the two novnc containers, the three tunnels, the listed DNS entries).
- Repo is owned by `clawcode:clawcode`, has zero literal secrets, and is local-only (no git remote).
- Bootstrap script is idempotent on re-run.
</success_criteria>

<output>
Create `.planning/phases/999.47-homelab-inventory-canonical-source-of-truth/999.47-01-SUMMARY.md` when done. Include: list of template files authored, anchor entries embedded, the operator-run command line, the resulting initial commit SHA on clawdy, and confirmation that re-running the bootstrap is idempotent.
</output>
