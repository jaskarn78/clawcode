---
phase: 999.47-homelab-inventory-canonical-source-of-truth
plan: 04
type: execute
wave: 3
depends_on: [999.47-01]
files_modified:
  - src/agent/workspace.ts
  - src/agent/__tests__/workspace.test.ts
  - scripts/homelab/seed-memory-pointer.sh
  - scripts/homelab/__tests__/seed-memory-pointer.test.ts
autonomous: false
requirements: [SC-5]

must_haves:
  truths:
    - "Every agent's `~/.clawcode/agents/<name>/MEMORY.md` contains the verbatim pointer line: `- [Homelab inventory](/home/clawcode/homelab/INVENTORY.md) — canonical source of truth for hosts, VMs, containers, access`."
    - "New agents created via `clawcode agent-create` (`src/agent/workspace.ts`) automatically receive the pointer on first-run — no manual step required."
    - "Existing agents in the live fleet receive the pointer via the one-shot `scripts/homelab/seed-memory-pointer.sh` run on clawdy under operator control."
    - "The seed script is idempotent — re-running it on an already-seeded fleet detects existing pointers and skips (does NOT duplicate the line)."
    - "The seed script does NOT restart any agent process — it appends to MEMORY.md in-place; existing chokidar-driven hot-reload (Phase 22 ConfigWatcher / per-agent file watchers) picks up the change on next turn without disrupting any active session."
  artifacts:
    - path: "src/agent/workspace.ts"
      provides: "Workspace creation now writes the homelab pointer line into the initial MEMORY.md alongside the default content."
    - path: "scripts/homelab/seed-memory-pointer.sh"
      provides: "One-shot fleet seeder — enumerates `~/.clawcode/agents/*/MEMORY.md`, appends the pointer line if missing, exits with a per-agent summary."
      min_lines: 50
    - path: "scripts/homelab/__tests__/seed-memory-pointer.test.ts"
      provides: "Vitest spec covering: missing-MEMORY.md, pointer-already-present (idempotent), pointer-missing (append), multi-agent fleet sweep."
  key_links:
    - from: "src/agent/workspace.ts"
      to: "MEMORY.md initial content"
      via: "Pointer line embedded in the default MEMORY.md template emitted on agent creation"
      pattern: "Homelab inventory.*INVENTORY\\.md"
    - from: "scripts/homelab/seed-memory-pointer.sh"
      to: "~/.clawcode/agents/<name>/MEMORY.md"
      via: "Per-agent append-if-missing"
      pattern: "Homelab inventory.*INVENTORY\\.md"
---

<objective>
Wire the discoverability pointer line into the two MEMORY.md surfaces that satisfy D-03 / SC-5:
1. **Forward**: `src/agent/workspace.ts` — every new agent created via `clawcode agent-create` gets the pointer line in its initial MEMORY.md.
2. **Backfill**: `scripts/homelab/seed-memory-pointer.sh` — operator-gated one-shot script that idempotently appends the pointer line to every existing agent's MEMORY.md on clawdy.

Purpose: SC-5 — "Every agent's MEMORY.md has the pointer line". This plan is small and parallel to Plan 03 (both depend only on Plan 01's CONTEXT and templates — neither blocks the other).

Output:
- Updated `src/agent/workspace.ts` so future agent creation is correct-by-default.
- New seeder script with vitest coverage.
- Operator-gated deploy of the seeder against the live fleet — appends the line without restarting any agent (Ramy-safe).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.47-homelab-inventory-canonical-source-of-truth/999.47-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@src/agent/workspace.ts
@src/memory/types.ts

<interfaces>
<!-- Verbatim pointer line from BACKLOG.md / CONTEXT.md <specifics> — DO NOT paraphrase. -->
<!-- This exact string must appear in MEMORY.md, including the em-dash. -->

  - [Homelab inventory](/home/clawcode/homelab/INVENTORY.md) — canonical source of truth for hosts, VMs, containers, access

<!-- MEMORY.md location (confirmed in src/memory/types.ts:89): -->
  ~/.clawcode/agents/<agent>/MEMORY.md

<!-- Existing workspace.ts pattern (read it before editing — it writes initial files via Object.freeze'd templates). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the pointer line into new-agent MEMORY.md creation</name>
  <read_first>
    - src/agent/workspace.ts (existing MEMORY.md initialization)
    - src/templates/SOUL.md (template-loading pattern reference)
    - src/memory/types.ts (MEMORY.md path formula at line 89)
  </read_first>
  <behavior>
    - Test 1: New workspace creation → MEMORY.md contains the verbatim pointer line (`expect(content).toContain("- [Homelab inventory](/home/clawcode/homelab/INVENTORY.md) — canonical source of truth for hosts, VMs, containers, access")`).
    - Test 2: New workspace creation → MEMORY.md still contains whatever default content existed previously (regression guard — pointer is ADDED to the template, not replacing it).
    - Test 3: Pointer line appears as a markdown list item with a leading dash + space (the exact format).
  </behavior>
  <files>
    src/agent/workspace.ts
    src/agent/__tests__/workspace.test.ts
  </files>
  <action>
    Read `src/agent/workspace.ts` to locate the MEMORY.md initialization site. Append the pointer line to the default MEMORY.md content. Two implementation choices — pick one:
    - (a) Inline string append to the existing template.
    - (b) Add a `HOMELAB_POINTER_LINE` exported constant at the top of `workspace.ts` and inject it. Preferred — matches the SCREAMING_SNAKE_CASE constant convention from CONVENTIONS.md.

    The constant string MUST be exactly:
      "- [Homelab inventory](/home/clawcode/homelab/INVENTORY.md) — canonical source of truth for hosts, VMs, containers, access"

    Position the pointer line near the top of MEMORY.md (above any phase-specific content) so it's visible in any human read and lands inside the autoload window. Use the existing convention of leading-dash markdown list items.

    Add test cases to `src/agent/__tests__/workspace.test.ts` (extend the existing file if present; otherwise create it following the pattern in other `__tests__` directories — use `vi.mock` + an in-memory fs as needed; or write to a tmpdir).
  </action>
  <verify>
    <automated>npx vitest run src/agent/__tests__/workspace.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 3 behavior tests pass.
    - `npx tsc --noEmit` exits 0.
    - `grep -F "Homelab inventory](/home/clawcode/homelab/INVENTORY.md)" src/agent/workspace.ts` returns exactly 1 match.
    - Constant declared with SCREAMING_SNAKE_CASE per CONVENTIONS.md (if option-b chosen).
    - Existing workspace.ts behavior unchanged for non-MEMORY.md paths (regression: SOUL.md / IDENTITY.md initialization untouched — verify via diff scope).
  </acceptance_criteria>
  <done>
    Forward path closed — every future agent created on clawdy via `clawcode agent-create` ships with the pointer baked in.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author `seed-memory-pointer.sh` — idempotent fleet backfill</name>
  <read_first>
    - .planning/phases/999.47-homelab-inventory-canonical-source-of-truth/999.47-CONTEXT.md (D-03 dual surfaces)
    - src/memory/types.ts:89 (MEMORY.md path formula)
  </read_first>
  <files>
    scripts/homelab/seed-memory-pointer.sh
    scripts/homelab/__tests__/seed-memory-pointer.test.ts
  </files>
  <action>
    Author a bash script with `set -euo pipefail` + `shellcheck`-clean. Logic:

    1. Discover agent directories: `find ~/.clawcode/agents -maxdepth 1 -mindepth 1 -type d` (override base via `--agents-dir` arg for tests).
    2. For each agent dir:
       - If MEMORY.md doesn't exist: create it with just the pointer line + a leading `# Memory` header.
       - If MEMORY.md exists and contains the pointer line (grep -qF on the exact string): skip with structured log `{agent, action: "skip", reason: "already-seeded"}`.
       - If MEMORY.md exists without the pointer line: append it (with a leading blank line if the file doesn't already end with `\n\n`). Atomic temp+rename. Structured log `{agent, action: "appended"}`.
    3. After the sweep: print a summary table + emit a final structured line `{totalAgents, seeded, alreadyHad, created, errors}` tagged `phase999.47-homelab-seed-pointer`.
    4. Exit 0 unless any per-agent error occurred; on error, exit 1 with a list of failed agents.
    5. `--dry-run` prints intended actions per agent without writing.

    Hard rules (must NOT do):
    - Do NOT restart any agent process — append-and-exit only.
    - Do NOT use `sed -i` on the production MEMORY.md (race window where the file is empty between truncate and write). Use atomic temp+rename — write `<file>.tmp.$$`, then `mv -f`.
    - Do NOT modify SOUL.md, IDENTITY.md, USER.md, or any other file.

    Vitest spec (`scripts/homelab/__tests__/seed-memory-pointer.test.ts`):
    - Test 1: Empty agents dir → exit 0, summary `{totalAgents: 0, ...}`.
    - Test 2: Agent without MEMORY.md → file is created with `# Memory\n\n<pointer>\n`.
    - Test 3: Agent with MEMORY.md already containing the pointer → file unchanged (byte-diff identical), action = "skip".
    - Test 4: Agent with MEMORY.md without the pointer → pointer appended; original content preserved; file ends with newline.
    - Test 5: Multi-agent fleet (3 agents in mixed states) → all three end in seeded state; summary counts correct.
    - Test 6: Idempotency — run twice; second run reports all three as "skip" (no duplicate pointers).
    - Test 7: `--dry-run` prints intended actions without touching any file (diff before/after = zero bytes changed).

    Tests use a tmpdir-rooted `agents/` and the `--agents-dir` override.
  </action>
  <verify>
    <automated>npx vitest run scripts/homelab/__tests__/seed-memory-pointer.test.ts && shellcheck scripts/homelab/seed-memory-pointer.sh && bash -n scripts/homelab/seed-memory-pointer.sh && ! grep -E "systemctl|service.*restart|killall|pkill" scripts/homelab/seed-memory-pointer.sh</automated>
  </verify>
  <acceptance_criteria>
    - All 7 vitest scenarios pass.
    - shellcheck clean.
    - Script never invokes systemctl / service / killall / pkill (Ramy-safe).
    - Idempotent on re-run (Test 6).
    - Atomic write — verified by Test 7 + by reading the source for the `<file>.tmp.$$` + `mv -f` pattern.
    - `--dry-run` produces zero filesystem changes (Test 7 byte-diff).
  </acceptance_criteria>
  <done>
    Seeder is feature-complete + idempotent. Ready for operator-gated run against the live clawdy fleet.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Operator-gated fleet-wide MEMORY.md pointer seed on clawdy</name>
  <action>Operator-driven verification — execute the steps in <how-to-verify> after Task 2 succeeds. This is a checkpoint, not implementation work. See `<what-built>`, `<how-to-verify>`, and `<resume-signal>` below for the operator runbook.</action>
  <what-built>
    Task 1: `src/agent/workspace.ts` now embeds the verbatim pointer line in every new agent's MEMORY.md (forward path).
    Task 2: `scripts/homelab/seed-memory-pointer.sh` is an idempotent operator-gated fleet backfill.

    NOT AUTONOMOUS per:
    - CLAUDE.md Ramy-active deploy hold.
    - `feedback_no_auto_deploy.md` — operator gate on touching production agent state.
    - `feedback_silent_path_bifurcation.md` — single-edit-then-verify discipline (don't bulk-rewrite, then check).
  </what-built>
  <how-to-verify>
    Operator-run, in order:

    1. Confirm Ramy is quiet (per `feedback_ramy_active_no_deploy.md`).
    2. Copy the script to clawdy (manually scp or include in Plan 03's deploy if both are landing in the same window):
       ```
       scp scripts/homelab/seed-memory-pointer.sh \
         clawcode@100.98.211.108:/tmp/seed-memory-pointer.sh
       ```
    3. Dry-run on clawdy:
       ```
       ssh clawcode@100.98.211.108 \
         'bash /tmp/seed-memory-pointer.sh --dry-run'
       ```
       Expected: a per-agent action plan (appended / skip / created) summarizing the current state of the fleet. Zero writes.
    4. Execute live:
       ```
       ssh clawcode@100.98.211.108 'bash /tmp/seed-memory-pointer.sh'
       ```
       Expected: structured `phase999.47-homelab-seed-pointer` line with `{totalAgents, seeded, alreadyHad, created, errors: 0}`.
    5. Verify the pointer landed:
       ```
       ssh clawcode@100.98.211.108 \
         'for f in ~/.clawcode/agents/*/MEMORY.md; do \
            echo "=== $f ==="; \
            grep -F "Homelab inventory](/home/clawcode/homelab/INVENTORY.md)" "$f" || echo "MISSING"; \
          done'
       ```
       Expected: every agent's MEMORY.md prints the pointer line. NO agent prints `MISSING`.
    6. Idempotency proof — re-run live:
       ```
       ssh clawcode@100.98.211.108 'bash /tmp/seed-memory-pointer.sh'
       ```
       Expected: every agent reports `action: skip` / `reason: already-seeded`. Total `seeded: 0`, `alreadyHad: <full fleet count>`.
    7. Confirm NO agent was restarted: `ssh clawcode@100.98.211.108 'systemctl status clawcode --no-pager | head -10'` — uptime should reflect the existing daemon, not a restart since seeding began.
    8. Pick one agent and confirm the pointer is visible to its next turn (operator-driven Discord ping or wait for normal traffic). The autoloaded MEMORY.md should now include the homelab pointer.
    9. Cleanup: `ssh clawcode@100.98.211.108 'rm /tmp/seed-memory-pointer.sh'` once verified.
  </how-to-verify>
  <resume-signal>
    Operator approves with "approved" (confirming every agent's MEMORY.md contains the pointer + the daemon was not restarted), OR describes failures.
  </resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed-memory-pointer.sh → agent MEMORY.md files | Single-purpose append; atomic temp+rename to avoid mid-write reads by the daemon |
| forward path (workspace.ts) → new agent MEMORY.md | Constant string — no template injection vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99947-16 | Tampering | Concurrent agent process reading MEMORY.md mid-write | mitigate | Atomic temp+rename (`<file>.tmp.$$` then `mv -f`). Reader either sees the pre-state or the post-state, never a partial. |
| T-99947-17 | Denial of service | Seeder duplicating the line on re-run | mitigate | `grep -qF` exact-string check before append; Test 6 pins idempotency. |
| T-99947-18 | Elevation of privilege | Seeder restarting the daemon | mitigate | Static-grep gate: no `systemctl`, no `killall`, no `pkill`. Append-only semantics. |
| T-99947-19 | Information disclosure | Pointer line itself leaking sensitive info | accept | The pointer is a filesystem path, not a secret. Same path operators already SSH to. |
| T-99947-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies. Uses existing `bash`, `grep`, `find`, `mv` — already on clawdy. |

</threat_model>

<verification>
- `npx vitest run src/agent/__tests__/workspace.test.ts scripts/homelab/__tests__/seed-memory-pointer.test.ts` — green.
- `npx tsc --noEmit` exits 0.
- `shellcheck scripts/homelab/seed-memory-pointer.sh` clean.
- After operator runs Task 3:
  - `ssh clawcode@100.98.211.108 'grep -L -F "Homelab inventory](/home/clawcode/homelab/INVENTORY.md)" ~/.clawcode/agents/*/MEMORY.md | wc -l'` returns `0` (zero MEMORY.md files without the pointer).
  - `ssh clawcode@100.98.211.108 'systemctl show -p ActiveEnterTimestamp clawcode'` returns a timestamp predating the seed run (no restart).
</verification>

<success_criteria>
- SC-5 fully covered: every agent's MEMORY.md has the verbatim pointer line.
- Forward path: new agents get it automatically via `clawcode agent-create`.
- Backfill: existing fleet seeded via idempotent operator-gated script.
- No agent process restarted during seeding.
- 10 vitest scenarios passing (3 in workspace.test + 7 in seed-memory-pointer.test).
</success_criteria>

<output>
Create `.planning/phases/999.47-homelab-inventory-canonical-source-of-truth/999.47-04-SUMMARY.md` when done. Include: total fleet agent count, breakdown of `appended` vs `alreadyHad` vs `created` from the live seed run, confirmation of no daemon restart, and the resulting `grep -c "Homelab inventory"` total across the fleet.
</output>
