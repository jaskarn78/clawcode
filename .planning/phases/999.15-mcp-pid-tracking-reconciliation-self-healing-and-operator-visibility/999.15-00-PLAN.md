---
phase: 999.15-mcp-pid-tracking-reconciliation-self-healing-and-operator-visibility
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/mcp/__tests__/reconciler.test.ts
  - src/mcp/__tests__/process-tracker.test.ts
  - src/mcp/__tests__/proc-scan.test.ts
  - src/cli/commands/__tests__/mcp-tracker.test.ts
  - src/manager/__tests__/session-manager.test.ts
  - src/manager/__tests__/daemon-boot-mcp-scan.test.ts
autonomous: true
requirements:
  - TRACK-01
  - TRACK-02
  - TRACK-03
  - TRACK-04
  - TRACK-05
  - TRACK-06
  - TRACK-08

must_haves:
  truths:
    - "Every TRACK-01..06 + TRACK-08 behavior has a RED vitest assertion before any implementation lands"
    - "Existing 999.14 test files (proc-scan, process-tracker, orphan-reaper) keep all current GREEN cases intact"
    - "New test files compile under tsc with no implementation present (use module/function stubs marked .todo or import-from-not-yet-existing-module patterns gated behind it.skipIf where needed)"
    - "Wave 0 commit fails the new test cases but leaves all 999.14 cases passing"
  artifacts:
    - path: src/mcp/__tests__/reconciler.test.ts
      provides: "RED tests for reconcileAllAgents (5+ cases per RESEARCH 815-819, 829-831)"
      min_lines: 180
    - path: src/mcp/__tests__/process-tracker.test.ts
      provides: "Extended RED tests for updateAgent / replaceMcpPids / getRegisteredAgents / pruneDeadPids / reconcile-before-kill (~7 new cases)"
      contains: "immutable mutations"
    - path: src/mcp/__tests__/proc-scan.test.ts
      provides: "Extended RED tests for isPidAlive helper + discoverClaudeSubprocessPid minAge opts (~4 new cases)"
      contains: "isPidAlive"
    - path: src/cli/commands/__tests__/mcp-tracker.test.ts
      provides: "RED tests for clawcode mcp-tracker CLI subcommand (~3 cases per RESEARCH 833-835)"
      min_lines: 80
    - path: src/manager/__tests__/session-manager.test.ts
      provides: "Extended RED tests for polled discovery loop (TRACK-02, ~3 new cases per RESEARCH 820-822)"
      contains: "polled discovery"
    - path: src/manager/__tests__/daemon-boot-mcp-scan.test.ts
      provides: "Extended RED tests for mcp-tracker-snapshot IPC handler (~2 new cases per RESEARCH 832)"
      contains: "tracker-snapshot"
  key_links:
    - from: "src/mcp/__tests__/reconciler.test.ts"
      to: "src/mcp/reconciler.ts (will exist in Plan 02)"
      via: "import { reconcileAllAgents } from \"../reconciler.js\""
      pattern: "from \"../reconciler\\.js\""
    - from: "src/mcp/__tests__/proc-scan.test.ts"
      to: "src/mcp/proc-scan.ts (isPidAlive will exist in Plan 01)"
      via: "import { isPidAlive } from \"../proc-scan.js\""
      pattern: "isPidAlive"
    - from: "src/manager/__tests__/daemon-boot-mcp-scan.test.ts"
      to: "src/manager/daemon.ts routeMethod switch (case mcp-tracker-snapshot in Plan 03)"
      via: "IPC client call mcp-tracker-snapshot"
      pattern: "mcp-tracker-snapshot"
---

<objective>
Wave 0 RED tests for Phase 999.15. Author every assertion that pins TRACK-01..06 + TRACK-08
behavior BEFORE implementation lands in Plans 01-03. Each test must fail today and turn green
exactly when the corresponding GREEN code ships in the appropriate downstream plan.

Purpose: Nyquist-compliant feedback loop — every requirement has automated verification. RED-first
prevents implementation drift from the locked CONTEXT/RESEARCH decisions (CLI rename, register
signature, opts object, sync mutations, idempotent reconcile, diff-based logging).

Output: 6 test files (3 new, 3 extended) committed as failing tests; every 999.14 GREEN case stays
green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.15-mcp-pid-tracking-reconciliation-self-healing-and-operator-visibility/999.15-CONTEXT.md
@.planning/phases/999.15-mcp-pid-tracking-reconciliation-self-healing-and-operator-visibility/999.15-RESEARCH.md
@.planning/phases/999.14-mcp-server-child-process-lifecycle-hardening/999.14-01-SUMMARY.md

@src/mcp/proc-scan.ts
@src/mcp/process-tracker.ts
@src/mcp/orphan-reaper.ts
@src/mcp/__tests__/proc-scan.test.ts
@src/mcp/__tests__/process-tracker.test.ts

<interfaces>
<!-- Existing 999.14 surface — RED tests target the EXTENDED form below -->

From src/mcp/process-tracker.ts (CURRENT — extended in Plan 01):
```typescript
class McpProcessTracker {
  get patterns(): RegExp;
  // CURRENT: register(agentName, pids) — Plan 01 changes to:
  // register(agentName, claudePid, mcpPids)
  async register(agentName: string, pids: readonly number[]): Promise<void>;
  unregister(agentName: string): readonly number[];
  list(): readonly number[];
  listForAgent(agentName: string): readonly number[];
  async killAgentGroup(agentName: string, graceMs?: number): Promise<void>;
  async killAll(graceMs?: number): Promise<void>;
  // NEW in Plan 01:
  // updateAgent(name: string, claudePid: number): void;
  // replaceMcpPids(name: string, pids: readonly number[]): void;
  // getRegisteredAgents(): ReadonlyMap<string, { claudePid: number; mcpPids: readonly number[]; registeredAt: number }>;
  // async pruneDeadPids(name: string): Promise<{ pruned: readonly number[]; alive: readonly number[] }>;
}
```

From src/mcp/proc-scan.ts (CURRENT — extended in Plan 01):
```typescript
// CURRENT — no opts object:
export async function discoverClaudeSubprocessPid(daemonPid: number): Promise<number | null>;
// PLAN 01 EXTENSION:
// discoverClaudeSubprocessPid(daemonPid, opts?: { minAge?: number; bootTimeUnix?: number; clockTicksPerSec?: number })

export async function discoverAgentMcpPids(claudePid: number, patterns: RegExp): Promise<readonly number[]>;
// NEW in Plan 01:
// export function isPidAlive(pid: number): boolean;
```

From src/mcp/reconciler.ts (NEW in Plan 02):
```typescript
type ReconcileReason = "stale-claude" | "missing-mcps" | "agent-restart" | "agent-gone";
export async function reconcileAllAgents(args: {
  tracker: McpProcessTracker;
  daemonPid: number;
  log: pino.Logger;
  bootTimeUnix?: number;
  clockTicksPerSec?: number;
}): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author src/mcp/__tests__/reconciler.test.ts (RED — TRACK-01 + TRACK-04 + partial TRACK-08)</name>
  <files>src/mcp/__tests__/reconciler.test.ts</files>
  <behavior>
    Cover the full reconciler contract per CONTEXT.md `<specifics>` pseudocode and RESEARCH.md
    Pattern 1+4. Mock /proc via `vi.hoisted` + `vi.mock("node:fs/promises")` (mirror the existing
    proc-scan.test.ts pattern at 999.14 substrate). Mock the tracker as a fake fulfilling the
    extended McpProcessTracker shape.

    Test cases (all MUST fail at Wave 0 commit because src/mcp/reconciler.ts does not yet exist):

    1. "stale-claude → updateAgent + replaceMcpPids" (TRACK-01) — tracker has agent A with
       claudePid=100; isPidAlive(100) returns false; mocked discoverClaudeSubprocessPid resolves
       to 200; mocked discoverAgentMcpPids resolves to [201, 202]. Assert tracker.updateAgent
       called with ("A", 200), tracker.replaceMcpPids called with ("A", [201, 202]), exactly one
       log line emitted with reason="stale-claude" and the canonical envelope shape.

    2. "missing-mcps → replaceMcpPids only" (TRACK-01) — tracker has agent A with claudePid=100,
       mcpPids=[]; isPidAlive(100) returns true (claude still alive); discoverAgentMcpPids resolves
       to [301, 302, 303]. Assert tracker.replaceMcpPids called with ("A", [301, 302, 303]),
       updateAgent NOT called, one log line with reason="missing-mcps".

    3. "agent-gone → unregister + log" (TRACK-01 LOCKED in CONTEXT determinism preferences) —
       tracker has agent A with claudePid=100; isPidAlive(100) returns false;
       discoverClaudeSubprocessPid resolves to null (no surviving claude). Assert
       tracker.unregister("A") called, no updateAgent / replaceMcpPids calls, one log line with
       reason="agent-gone".

    4. "idempotent — second call after no /proc change emits zero logs and zero mutations"
       (TRACK-01 + TRACK-04) — set up fixture where /proc state matches tracker exactly
       (claudePid alive, mcpPids match discovery). Call reconcileAllAgents twice. Assert tracker
       mutation methods (updateAgent/replaceMcpPids/unregister) called zero times, log warn
       called zero times.

    5. "error swallow — reconcile failure for one agent doesn't propagate, doesn't block siblings"
       (TRACK-01) — tracker has agents A, B. Make discoverClaudeSubprocessPid throw for A only.
       Assert: log line at error/warn level with the agent name + err string, B is reconciled
       successfully, function resolves (does not reject).

    6. "one log per agent per cycle on state change" (TRACK-04) — fixture with 3 agents all
       requiring stale-claude reconcile. Assert exactly 3 log warn calls, one per agent, distinct
       agent fields.

    7. "log shape canonical envelope" (TRACK-04) — assert log payload contains EXACTLY the keys
       { component: "mcp-tracker", action: "reconcile", agent, oldClaudePid, newClaudePid,
       oldMcpCount, newMcpCount, reason } in single log call. Pin against the message string
       "tracker state reconciled" for state-change logs.

    8. "no log on no-op cycle" (TRACK-04) — same as test 4 but emphasize the diff comparison
       returns empty for { claudePid, mcpPids[].sort() }.

    9. "agent-restart — both claudePid and mcpPids changed in one cycle" — tracker has
       claudePid=100 + mcpPids=[201, 202]; reconciler discovers claudePid=300 + mcpPids=[301, 302].
       Assert reason="agent-restart" (per RESEARCH Pattern 4 diff classifier — when stale-claude
       AND mcpPids set differs from prior, use agent-restart not stale-claude).

    Use real pino with a captured-spy transport (vi.fn() collecting log calls) so log envelope
    assertions are byte-precise. Fakes for tracker should track call args via vi.fn().
  </behavior>
  <action>
    Create src/mcp/__tests__/reconciler.test.ts. Mirror the import / mock-hoist style from
    src/mcp/__tests__/proc-scan.test.ts. Use `it.skipIf(process.platform !== "linux")` only for
    integration cases (none here — these are pure unit). Reference the extended interfaces block
    above. Per D-LOCKED: `discoverClaudeSubprocessPid(daemonPid, { minAge: 10 })` is the
    reconciler's call shape — mock the function to assert opts.minAge===10. Per
    `feedback_no_auto_deploy` is unrelated here (Wave 0 = tests only).

    Use the canonical reason values: "stale-claude" | "missing-mcps" | "agent-restart" |
    "agent-gone". Pin the message string "tracker state reconciled" verbatim for state-change
    logs and "tracker entry dropped — claude proc gone" for agent-gone logs (CONTEXT specifics
    pseudocode).
  </action>
  <verify>
    <automated>npx vitest run src/mcp/__tests__/reconciler.test.ts --reporter=verbose 2>&1 | tee /tmp/reconciler-red.log; grep -E "FAIL|✗|missing|Cannot find module" /tmp/reconciler-red.log</automated>
  </verify>
  <done>
    File exists with 9 it() blocks. All 9 fail with module-not-found OR
    method-not-on-tracker errors (because reconciler.ts + extended tracker API ship in Plans 01-02).
    All 999.14 tests still green: `npx vitest run src/mcp/__tests__/proc-scan.test.ts src/mcp/__tests__/process-tracker.test.ts src/mcp/__tests__/orphan-reaper.test.ts` shows zero new failures.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend src/mcp/__tests__/process-tracker.test.ts + src/mcp/__tests__/proc-scan.test.ts (RED — TRACK-03 + TRACK-06)</name>
  <files>src/mcp/__tests__/process-tracker.test.ts, src/mcp/__tests__/proc-scan.test.ts</files>
  <behavior>
    APPEND to existing files (do not rewrite). All 999.14 cases stay byte-stable.

    process-tracker.test.ts — append a new describe block "Phase 999.15 extensions":

    PT-1. "register with new (agentName, claudePid, mcpPids) signature stores all three"
          (TRACK-03 — locked signature change per orchestrator's recommendation #2). Assert
          getRegisteredAgents().get(name) returns { claudePid, mcpPids: [...], registeredAt: <number> }.

    PT-2. "register backward-compat assertion" — old (agentName, pids) two-arg call site at
          session-manager.ts:750 will be updated in Plan 02; new tests pin the new shape. (Test:
          calling register with two args in current TS source should be a TYPE ERROR after Plan 01,
          but Wave 0 only pins runtime — assert: register(name, claudePid, mcpPids) returns void
          via Promise; calling with bare pids array as 2nd arg fails type check at compile.)
          Implement as a runtime assertion that getRegisteredAgents().get(name).claudePid equals
          the passed claudePid.

    PT-3. "updateAgent replaces tracked claudePid immutably" (TRACK-03 immutable invariant from
          CONTEXT determinism). Register agent with claudePid=100. Capture
          getRegisteredAgents().get("A") reference R1. Call updateAgent("A", 200). Capture R2.
          Assert R1 !== R2 (new object reference), R2.claudePid === 200, R1.claudePid === 100
          (R1 unchanged).

    PT-4. "replaceMcpPids replaces full mcp set immutably" — same pattern. Reference identity
          changes; old reference unchanged.

    PT-5. "getRegisteredAgents returns ReadonlyMap; mutating it is a TS error" — runtime side:
          assert returned value has .get / .has / .entries / .size; cast to any then attempt
          .set("X", ...) and verify it throws OR is a no-op (Map is the only stdlib choice;
          ReadonlyMap is type-only — pin via tsc by importing the public type).

    PT-6. "pruneDeadPids removes dead PIDs and returns alive set" — register agent with mcpPids
          [101, 102, 103]. Mock isPidAlive to return true for 101, false for 102, true for 103.
          Call await tracker.pruneDeadPids("A"). Assert returns { pruned: [102], alive: [101, 103] }
          and tracker state mcpPids becomes [101, 103].

    PT-7. "killAgentGroup reconciles before SIGTERM" (TRACK-06) — register agent with stale PIDs
          [100, 101]. Inject a reconcileFn (Plan 02 will pass via deps); fake reconcileFn updates
          tracker to fresh mcpPids [200, 201]. Spy on signal-sending (kill mock). Assert kill
          called for [200, 201], NOT [100, 101]. Assert reconcileFn called BEFORE any kill call
          (via call order spy).

    PT-8. "killAgentGroup reconcile failure falls back to recorded PIDs" (TRACK-06 safety net per
          CONTEXT determinism). reconcileFn throws. Assert kill called for the originally-recorded
          [100, 101] (fallback) and a warn log emitted with the err string.

    proc-scan.test.ts — append a new describe block "Phase 999.15 extensions":

    PS-1. "isPidAlive(0) returns false without syscall" (TRACK-03) — guard against bad input.
          Spy on process.kill to assert NOT called.

    PS-2. "isPidAlive(-1) returns false" — same guard for negative input.

    PS-3. "isPidAlive(process.pid) returns true (own pid)" (Linux-only integration) — wrap in
          it.skipIf(process.platform !== "linux"). Real process.kill(pid, 0) call.

    PS-4. "isPidAlive(99999999) returns false (ESRCH path)" (Linux-only integration) — assumes
          PID is unused. Real syscall + caught ESRCH.

    PS-5. "isPidAlive treats EPERM as alive" — mock process.kill to throw an error with code
          "EPERM". Assert isPidAlive returns true (per RESEARCH Pitfall 5 lock).

    PS-6. "discoverClaudeSubprocessPid accepts opts object with minAge" (TRACK-01 substrate +
          orchestrator recommendation #3 lock) — call with
          discoverClaudeSubprocessPid(daemonPid, { minAge: 10, bootTimeUnix: 12345, clockTicksPerSec: 100 }).
          Mock /proc to return two claude procs as children of daemonPid: PID-A age=3s, PID-B
          age=15s. Assert returns PID-B (PID-A filtered by minAge).

    PS-7. "discoverClaudeSubprocessPid no opts → existing behavior unchanged" (regression pin per
          999.14 substrate). Existing call (no opts) returns first matching claude proc regardless
          of age. Mirror an existing 999.14 test case and assert unchanged result.
  </behavior>
  <action>
    For PT-7/PT-8: the reconcile-before-kill behavior requires McpProcessTracker to accept a
    reconcileFn dependency. Plan 01 wires this via deps.reconcileAgent (optional). Wave 0 tests
    inject a fake via constructor deps `{ reconcileAgent: vi.fn() }`. If the dep is absent, kill
    falls through to existing behavior — pin THIS too (PT-9 implicit: with no reconcileFn, kill
    behaves identically to 999.14).

    For PS-1..PS-5: import { isPidAlive } from "../proc-scan.js" — this import itself fails at
    Wave 0 (the export doesn't exist yet). Confirm the test FILE compiles (tsc) by stubbing the
    import path expectation; the runtime test fails on the missing export.

    For PS-6: mock /proc/{pid}/stat to fabricate the start-time + comm fields per the existing
    proc-scan.test.ts patterns (use bootTimeUnix + clockTicksPerSec to derive ages). Assert the
    minAge filter applied AT proc-scan layer, not in the reconciler.

    Do not modify ANY existing 999.14 test cases — only append.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/__tests__/process-tracker.test.ts src/mcp/__tests__/proc-scan.test.ts --reporter=verbose 2>&1 | tee /tmp/tracker-red.log; grep -E "FAIL|✗" /tmp/tracker-red.log</automated>
  </verify>
  <done>
    process-tracker.test.ts has +8 new it() blocks failing on missing methods. proc-scan.test.ts
    has +7 new it() blocks failing on missing isPidAlive export and missing opts behavior. All
    pre-existing it() blocks in both files still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author CLI test + extend session-manager + daemon-boot test files (RED — TRACK-02 + TRACK-05)</name>
  <files>src/cli/commands/__tests__/mcp-tracker.test.ts, src/manager/__tests__/session-manager.test.ts, src/manager/__tests__/daemon-boot-mcp-scan.test.ts</files>
  <behavior>
    THREE files in this task. All RED.

    A. NEW: src/cli/commands/__tests__/mcp-tracker.test.ts (TRACK-05). Mirror the
       src/cli/commands/threads.ts subcommand registration pattern. Tests:

       CLI-1. "formatTrackerTable renders fixture snapshot" — pure formatter unit. Input:
              `[{ agent: "fin-acquisition", claudePid: 4070338, mcpPids: [4070553, 4070559, 4070570], aliveCount: 3, totalCount: 3, cmdlines: ["npx mcp-server-mysql", "node finnhub/server.js", "npm exec @takescake/1password-mcp"] }, { agent: "research", claudePid: 4068660, mcpPids: [], aliveCount: 0, totalCount: 0, cmdlines: [] }]`.
              Expected output is the table from CONTEXT.md TRACK-05 spec. Snapshot test.

       CLI-2. "exits 1 when any agent has partial MCP liveness" (TRACK-05 LOCKED in CONTEXT
              determinism: "Returns non-zero exit code if any agent has 0/N MCP procs alive").
              Mock IPC client to return a snapshot with one agent at 2/8 alive. Assert exit code 1.

       CLI-3. "exits 2 when daemon is not running" — mock IPC client to throw ECONNREFUSED.
              Assert exit code 2 with friendly stderr message.

       CLI-4. "subcommand registered as `mcp-tracker` not `mcp-status`" (LOCKED orchestrator
              recommendation #1 — name collision). Import the registration function (which
              doesn't exist yet at Wave 0); when it does in Plan 03, assert
              program.commands.find(c => c.name() === "mcp-tracker") exists, mcp-status NOT
              registered by this file (Phase 85's existing command stays).

       CLI-5. "-a <agent> filter renders verbose mode with cmdlines expanded" — pass
              { agent: "fin-acquisition" } filter; expected output has all MCP cmdlines listed
              one-per-line with PIDs.

    B. APPEND to src/manager/__tests__/session-manager.test.ts (TRACK-02). New describe:
       "Phase 999.15 polled discovery":

       SM-1. "polled discovery succeeds across SDK respawn" (TRACK-02 + TRACK-08 #1). Mock
             discoverClaudeSubprocessPid to: attempt 1 → resolves null (claude dying), attempt 2
             → resolves null (claude not yet stable), attempt 3 → resolves 200 (surviving claude).
             Mock vi.useFakeTimers + advance 5s per attempt. Assert tracker.register called once
             with ("agent-name", 200, [...mcpPids]). Total elapsed wall-clock budget ≤ 30s.

       SM-2. "polled discovery aborts when agent handle is disposed mid-wait" (TRACK-02
             determinism preference). Start polling. After 7s simulated, call
             sessionManager.stopAgent / dispose so this.sessions.has(name) returns false.
             Assert tracker.register NEVER called even when subsequent attempts would have
             succeeded.

       SM-3. "after 30s (6 attempts) with no match, logs warn and exits gracefully" (TRACK-02
             budget). Mock discovery to always resolve null. Advance 30s of fake timers. Assert:
             6 discovery calls made, 1 warn log with message matching /no claude proc settled
             within 30s/, tracker.register NOT called.

       SM-4. "polled discovery passes opts.minAge=5 to discoverClaudeSubprocessPid" — assert
             every mocked call site receives { minAge: 5 } (per RESEARCH Pattern 2 + LOCKED).

    C. APPEND to src/manager/__tests__/daemon-boot-mcp-scan.test.ts (TRACK-05 IPC). New describe:
       "Phase 999.15 mcp-tracker-snapshot IPC":

       IPC-1. "mcp-tracker-snapshot returns shape { agents: Array<...> } with liveness counts"
              — fixture daemon with 2 registered agents in tracker (one with all alive PIDs,
              one with 1/3 alive). Invoke IPC method. Assert response shape:
              `{ agents: [{ agent, claudePid, mcpPids, aliveCount, totalCount, cmdlines }, ...] }`.

       IPC-2. "mcp-tracker-snapshot includes registeredAt timestamp" — assert response per-agent
              entry has registeredAt as number (epoch ms).

       Use the existing daemon-boot-mcp-scan.test.ts harness pattern (mock IPC dispatch, fixture
       tracker state).
  </behavior>
  <action>
    Three files. Two are appends (preserve existing tests byte-stable), one is new.

    For mcp-tracker.test.ts: import from src/cli/commands/mcp-tracker.js — this module ships in
    Plan 03. Wave 0 commit will fail with module-not-found. That's the RED state.

    For session-manager.test.ts: the polled wait code currently lives at lines 728-766 with
    MCP_SPAWN_SETTLE_MS = 1000. Plan 02 replaces it. Tests use vi.useFakeTimers / runAllTimersAsync
    style. Mock discoverClaudeSubprocessPid via vi.mock("../../mcp/proc-scan.js"). Mock the
    tracker via injection through SessionManager constructor options.mcpTracker.

    For daemon-boot-mcp-scan.test.ts: read the existing file (referenced via @ in context) to
    match its harness style. Add the IPC test cases without altering existing setup/teardown.

    No new dependencies. All vitest + node built-ins.
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/__tests__/mcp-tracker.test.ts src/manager/__tests__/session-manager.test.ts src/manager/__tests__/daemon-boot-mcp-scan.test.ts --reporter=verbose 2>&1 | tee /tmp/wave0-c.log; grep -E "FAIL|✗|Cannot find" /tmp/wave0-c.log</automated>
  </verify>
  <done>
    mcp-tracker.test.ts new with 5 it() blocks all RED (module not found).
    session-manager.test.ts has +4 it() blocks RED (current MCP_SPAWN_SETTLE_MS path or polled
    discovery missing). daemon-boot-mcp-scan.test.ts has +2 it() blocks RED (IPC method
    unregistered). All pre-existing it() blocks unchanged and still green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Verify Wave 0 RED state + commit</name>
  <files>(none modified — verification only; commit step)</files>
  <behavior>
    Confirm: total new failing test count ≥ 25 (5+8+7+5+4+2 = 31 expected). 999.14 tests all
    still green. Type-check passes (since tests reference yet-to-exist modules, ensure they're
    structured so tsc skips or treats as expected-to-fail imports — vitest handles this naturally
    via dynamic import errors at runtime, not compile).
  </behavior>
  <action>
    Run full suite excluding 999.15 new files; should be all green:
      `npx vitest run --exclude "**/__tests__/reconciler.test.ts" --exclude "**/__tests__/mcp-tracker.test.ts"`
    (note the proc-scan / process-tracker / session-manager / daemon-boot extensions are append-
    only; their pre-existing cases must still pass — they will because we ONLY append new it()
    blocks. Run those files separately and confirm count(green) >= count(green at HEAD~1)).

    Run tsc:
      `npx tsc --noEmit`
    If new test files break tsc, adjust imports to `await import()` form or guard with
    @ts-expect-error comments at the missing-import lines.

    Commit per per-phase commit-doc convention:
      `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "test(999.15-00): RED tests for TRACK-01..06+08" --files src/mcp/__tests__/reconciler.test.ts src/mcp/__tests__/process-tracker.test.ts src/mcp/__tests__/proc-scan.test.ts src/cli/commands/__tests__/mcp-tracker.test.ts src/manager/__tests__/session-manager.test.ts src/manager/__tests__/daemon-boot-mcp-scan.test.ts`
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=dot 2>&1 | tail -20</automated>
  </verify>
  <done>
    tsc passes. vitest reports the expected RED count for new 999.15 cases. All 999.14 cases
    green. Single commit landed with subject `test(999.15-00): RED tests for TRACK-01..06+08`.
  </done>
</task>

</tasks>

<verification>
- All 6 test files updated/created.
- New test count: ~31 RED cases.
- All 999.14 GREEN cases still green.
- `npx tsc --noEmit` exits 0.
- Single commit on the wave-0 branch / current HEAD.
</verification>

<success_criteria>
- [ ] reconciler.test.ts: 9 it() blocks RED (file does not import a real reconciler module yet).
- [ ] process-tracker.test.ts: +8 it() blocks RED on missing tracker methods.
- [ ] proc-scan.test.ts: +7 it() blocks RED on missing isPidAlive + missing opts behavior.
- [ ] mcp-tracker.test.ts: 5 it() blocks RED on missing CLI module.
- [ ] session-manager.test.ts: +4 it() blocks RED on current 1s settle (not yet replaced with polled wait).
- [ ] daemon-boot-mcp-scan.test.ts: +2 it() blocks RED on missing IPC method.
- [ ] No 999.14 GREEN cases regressed.
- [ ] Type-check (tsc --noEmit) passes.
- [ ] Commit landed.
</success_criteria>

<output>
After completion, create `.planning/phases/999.15-mcp-pid-tracking-reconciliation-self-healing-and-operator-visibility/999.15-00-SUMMARY.md` documenting:
- Total RED test count (per-file).
- Confirmation 999.14 cases stayed green.
- Any tsc adjustments (e.g., @ts-expect-error annotations).
</output>
