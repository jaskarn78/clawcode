---
phase: 999.14-mcp-server-child-process-lifecycle-hardening
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/mcp/proc-scan.ts
  - src/mcp/process-tracker.ts
  - src/mcp/orphan-reaper.ts
  - src/mcp/__tests__/proc-scan.test.ts
  - src/mcp/__tests__/process-tracker.test.ts
  - src/mcp/__tests__/orphan-reaper.test.ts
  - src/discord/__tests__/thread-cleanup.test.ts
  - src/discord/__tests__/stale-binding-sweep.test.ts
  - src/cli/commands/__tests__/threads.test.ts
autonomous: true
requirements:
  - MCP-01
  - MCP-02
  - MCP-03
  - MCP-06
  - MCP-08
  - MCP-09
  - MCP-10
must_haves:
  truths:
    - "proc-scan.ts reads /proc/{pid}/stat correctly even when comm field contains spaces or parens (lastIndexOf(')') idiom)"
    - "proc-scan.ts derives ppid, uid, cmdline, and starttime from a single pid without ever reading /proc/{pid}/environ"
    - "process-tracker.ts exposes register(agentName, pids), unregister(agentName), list(), killAgentGroup(agentName, graceMs), and killAll(graceMs); idempotent — calling kill on a dead PID never throws"
    - "process-tracker.killGroup() calls process.kill(-pid, sig) and treats ESRCH as success"
    - "orphan-reaper.ts scanForOrphanMcps({uid, patterns}) returns only procs with ppid===1 AND uid match AND cmdline matches union regex AND age >= 5s"
    - "orphan-reaper.ts emits canonical pino warn log {component:'mcp-reaper', action:'sigterm'|'sigkill', pid, cmdline, reason} for every kill"
    - "buildMcpCommandRegexes(config.mcpServers) builds the union regex from configured server commands, NOT a hardcoded literal"
    - "MCP-08: classifyDiscordCleanupError(err) returns 'prune' for 50001|10003|404, 'retain' for 5xx|429|network, 'unknown' otherwise; pure function, total over the inputs we get from discord.js + node fetch"
    - "MCP-08: thread-cleanup helper invokes Discord setArchived, classifies any thrown error, prunes registry on 'prune', leaves registry intact on 'retain', emits {component:'thread-cleanup', action:'prune-after-discord-error', discordCode, threadId} warn"
    - "MCP-09: scanStaleBindings({registry, now, idleMs}) returns ONLY entries where now - lastActivity > idleMs; pure; idleMs=0 disables (returns []); operator can configure via defaults.threadIdleArchiveAfter"
    - "MCP-09: sweepStaleBindings({spawner, registryPath, now, idleMs, log}) iterates stale entries, calls MCP-08-fixed cleanup helper per entry, emits ONE summary warn log per cycle with {staleCount, agentBreakdown}"
    - "MCP-10: clawcode threads archive <id> [--lock] sends archive-discord-thread IPC; on Discord 50001|10003 prints success ('registry pruned'), exits 0"
    - "MCP-10: clawcode threads prune --stale-after <duration> sends a new IPC method ('threads-prune-stale') that runs MCP-09 sweep on demand; prints {staleCount, prunedCount}"
    - "MCP-10: clawcode threads prune --agent <name> [--yes] sends a new IPC ('threads-prune-agent') that force-prunes ALL bindings for the named agent without calling Discord; prompts confirmation unless --yes"
  artifacts:
    - path: "src/mcp/proc-scan.ts"
      provides: "readProcInfo(pid), listAllPids(), parseStatPpid (lastIndexOf-')' parser), buildMcpCommandRegexes(serverConfigs)"
      min_lines: 80
    - path: "src/mcp/process-tracker.ts"
      provides: "McpProcessTracker class with register/unregister/list/killAgentGroup/killAll + killGroup helper"
      min_lines: 100
    - path: "src/mcp/orphan-reaper.ts"
      provides: "scanForOrphanMcps({uid, patterns}), reapOrphans({tracker, reason, log}), startOrphanReaper({tracker, intervalMs, log})"
      min_lines: 80
    - path: "src/mcp/__tests__/proc-scan.test.ts"
      provides: "Pitfall 4 (comm with parens) regression + Linux integration test for readProcInfo on real spawned child"
    - path: "src/mcp/__tests__/process-tracker.test.ts"
      provides: "killGroup ESRCH idempotency, killAgentGroup SIGTERM-then-SIGKILL with grace, killAll iteration"
    - path: "src/mcp/__tests__/orphan-reaper.test.ts"
      provides: "Mocked scanner returns ppid=1 candidates only; canonical log shape pinned; real-orphan Linux integration"
    - path: "src/discord/__tests__/thread-cleanup.test.ts"
      provides: "MCP-08 RED tests: classifyDiscordCleanupError truth table; cleanupThreadWithClassifier prunes on 50001/10003/404; retains on 5xx/429/network; canonical 'prune-after-discord-error' warn shape pinned"
    - path: "src/discord/__tests__/stale-binding-sweep.test.ts"
      provides: "MCP-09 RED tests: scanStaleBindings filter, sweepStaleBindings invokes cleanup helper per entry, summary log shape, idleMs=0 disables sweep"
    - path: "src/cli/commands/__tests__/threads.test.ts"
      provides: "MCP-10 RED tests: archive/prune-stale/prune-agent CLI subcommands; --yes bypasses prompt; 50001 IPC error surfaces success+'registry pruned' (NOT process.exit nonzero)"
  key_links:
    - from: "src/mcp/orphan-reaper.ts"
      to: "src/mcp/proc-scan.ts"
      via: "import readProcInfo + listAllPids"
      pattern: "from \"./proc-scan\""
    - from: "src/mcp/process-tracker.ts"
      to: "process.kill(-pid, sig)"
      via: "killGroup helper with negative-pid POSIX form"
      pattern: "process\\.kill\\(-"
    - from: "src/mcp/__tests__/proc-scan.test.ts"
      to: "real /proc/{pid}/stat"
      via: "spawn real node child, read its stat, assert ppid"
      pattern: "skipIf.*platform.*linux"
    - from: "src/discord/__tests__/thread-cleanup.test.ts"
      to: "classifyDiscordCleanupError"
      via: "truth-table coverage for 50001/10003/404/5xx/429/network/unknown"
      pattern: "classifyDiscordCleanupError\\("
    - from: "src/discord/__tests__/stale-binding-sweep.test.ts"
      to: "scanStaleBindings + sweepStaleBindings"
      via: "fake clock, mock spawner, registry fixture"
      pattern: "scanStaleBindings\\(|sweepStaleBindings\\("
    - from: "src/cli/commands/__tests__/threads.test.ts"
      to: "registerThreadsCommand + new subcommands"
      via: "spy on sendIpcRequest, simulate IPC responses including 50001 path"
      pattern: "sendIpcRequest"
---

<objective>
Build the load-bearing modules that power Phase 999.14 + their RED tests:

**MCP-01..07 substrate (unchanged):**
- `src/mcp/proc-scan.ts` — Linux /proc reader with comm-field-parens-safe parser
- `src/mcp/process-tracker.ts` — per-agent PID tracker singleton with idempotent process-group kills
- `src/mcp/orphan-reaper.ts` — union-regex orphan scanner with canonical warn-log shape

**MCP-08/09/10 RED tests (NEW — Wave 0 only writes the failing tests; Wave 1 wires the GREEN code into existing modules):**
- `src/discord/__tests__/thread-cleanup.test.ts` — RED tests for the Discord-error classifier + prune-on-not-found cleanup helper
- `src/discord/__tests__/stale-binding-sweep.test.ts` — RED tests for the periodic stale-binding sweep
- `src/cli/commands/__tests__/threads.test.ts` — RED tests for the three new CLI subcommands

Purpose: This wave produces the load-bearing surface. RESEARCH.md confirmed the SDK exposes no MCP PIDs, so the daemon CANNOT intercept spawn — it must discover via /proc and reap by union-regex. The MCP-08/09/10 work is fundamentally smaller (~110 lines of GREEN code in Wave 1) and lives in already-existing files (`subagent-thread-spawner.ts`, the `archive-discord-thread` IPC handler, `daemon.ts:4029-4031`, `cli/commands/threads.ts`). Wave 0 pre-stages the RED tests so Wave 1 can wire the fixes as drop-ins.

Output: Three TypeScript modules (~260 lines total) + three test files exercising MCP-01/02/03/06 + three additional RED test files locking down MCP-08/09/10 behavior contracts. Zero new npm dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.14-mcp-server-child-process-lifecycle-hardening/999.14-CONTEXT.md
@.planning/phases/999.14-mcp-server-child-process-lifecycle-hardening/999.14-RESEARCH.md
@src/mcp/health.ts
@src/mcp/json-rpc-call.ts
@src/manager/secrets-resolver.ts
@src/discord/subagent-thread-spawner.ts
@src/discord/thread-registry.ts
@src/discord/thread-types.ts
@src/cli/commands/threads.ts
@src/manager/daemon.ts

<interfaces>
<!-- Existing types this plan integrates with — extracted from codebase. -->
<!-- Executor uses these directly; no codebase exploration required. -->

From clawcode.yaml schema (mcpServers entry shape):
```typescript
type McpServerConfig = {
  command: string;        // e.g. "npm" or "mcporter"
  args: readonly string[]; // e.g. ["exec", "mcp-server-mysql@latest"]
  env?: Record<string, string>;
};
type McpServersConfig = Record<string /*serverName*/, McpServerConfig>;
```

From pino (already in deps):
```typescript
import type { Logger } from "pino";
// log.warn({ ...fields }, "msg") — JSON output, level=40
```

Canonical reaper warn log shape (PIN in tests — from CONTEXT.md):
```json
{"level":40,"component":"mcp-reaper","action":"sigterm","pid":123,"cmdline":"sh -c mcp-server-mysql","reason":"orphan-ppid-1","msg":"reaping orphaned MCP server"}
{"level":40,"component":"mcp-reaper","action":"sigkill","pid":123,"reason":"orphan-ppid-1","graceMs":10000,"msg":"orphan MCP server force-killed after grace"}
```

Reasons enum (string literal union, exact spelling pinned):
- "orphan-ppid-1"  — periodic reaper match
- "boot-scan"      — daemon-start leftover sweep
- "shutdown"       — daemon graceful exit
- "agent-disconnect" — MCP-02 trigger from session teardown

From src/discord/thread-types.ts (existing):
```typescript
export type ThreadBinding = {
  readonly threadId: string;
  readonly agentName: string;
  readonly lastActivity: number;  // epoch ms
  // ... other fields preserved
};
export type ThreadsConfig = {
  readonly maxThreadSessions: number;
  // (NEW field added in Wave 1 schema, but RED tests below assume the contract)
  readonly threadIdleArchiveAfter?: string; // e.g. "24h", "0" disables
};
```

From src/discord/thread-registry.ts (existing exports — Wave 0 uses them only in tests):
```typescript
export function readThreadRegistry(path: string): Promise<ThreadRegistry>;
export function writeThreadRegistry(path: string, reg: ThreadRegistry): Promise<void>;
export function getBindingForThread(reg: ThreadRegistry, threadId: string): ThreadBinding | undefined;
export function removeBinding(reg: ThreadRegistry, threadId: string): ThreadRegistry;
```

From src/discord/subagent-thread-spawner.ts (existing — Wave 1 will extend it):
```typescript
class SubagentThreadSpawner {
  async archiveThread(threadId: string, opts?: { lock?: boolean }): Promise<{ bindingPruned: boolean }>;
  // Calls Discord setArchived; on success prunes registry. Today: any throw is uncaught.
}
```

Discord error code surface (discord.js v14 + raw HTTP):
- `err.code === 50001` — Missing Access (bot evicted, channel gone)
- `err.code === 10003` — Unknown Channel (deleted Discord-side)
- `err.status === 404` (DiscordAPIError) — also "thread is gone"
- `err.status >= 500` — server-side; transient, retain
- `err.code === "ECONNRESET" | "ETIMEDOUT" | "ENOTFOUND"` — network; transient, retain
- `err.status === 429` — rate-limited; transient, retain
- Anything else → "unknown" → conservative: retain (don't prune on uncertain failure)

Canonical MCP-08 prune-on-error log shape (PIN in tests):
```json
{"level":40,"component":"thread-cleanup","action":"prune-after-discord-error","discordCode":50001,"threadId":"…","agentName":"fin-acquisition","msg":"discord thread gone server-side; pruned registry binding"}
```

Canonical MCP-09 sweep summary log (PIN in tests):
```json
{"level":40,"component":"thread-cleanup","action":"stale-sweep","staleCount":7,"prunedCount":7,"agents":{"fin-acquisition":3,"fin-test":4},"idleMs":86400000,"msg":"stale-binding sweep complete"}
```

CLI surface (MCP-10):
- `clawcode threads` (existing) — list bindings
- `clawcode threads archive <threadId> [--lock]` — NEW; calls existing `archive-discord-thread` IPC
- `clawcode threads prune --stale-after <duration>` — NEW; calls new `threads-prune-stale` IPC
- `clawcode threads prune --agent <name> [--yes]` — NEW; calls new `threads-prune-agent` IPC
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: proc-scan.ts + RED tests (MCP-06 foundation, MCP-01/03 dependency)</name>
  <files>src/mcp/proc-scan.ts, src/mcp/__tests__/proc-scan.test.ts</files>
  <behavior>
    - parseStatPpid(stat): given a /proc/stat line whose comm field contains spaces ("(claude (test-agent))") OR parens ("(node) S 1 ...") OR newlines, returns the correct ppid using lastIndexOf(')'). Throws on malformed input (no closing paren, non-numeric ppid).
    - readProcInfo(pid): reads stat + cmdline + status concurrently; returns {pid, ppid, uid, cmdline (string[]), startTimeJiffies}; returns null on ENOENT/ESRCH (proc disappeared mid-read); NEVER reads /proc/{pid}/environ (security non-negotiable from RESEARCH.md and rules/security.md).
    - listAllPids(): readdir('/proc') filtered to numeric entries, returns readonly number[].
    - buildMcpCommandRegexes(serverConfigs): given a `Record<string, {command, args}>`, returns a RegExp that matches if a cmdline string contains either `command` itself OR `command + ' ' + args.join(' ')` substring; escapes regex metacharacters; rejects empty config (throws — boundary validation per coding-style).
    - matchesAnyMcpCommand(cmdline, patterns): tests cmdline string against the union regex.
    - Age helper: `procAgeSeconds({startTimeJiffies, bootTimeUnix, clockTicksPerSec})` — pure math; no syscalls; used by reaper to enforce 5s minimum age (Pitfall 3).

    RED tests in proc-scan.test.ts:
    - Test 1: parseStatPpid handles `123 (sh) S 1 123 ...` → ppid=1.
    - Test 2: parseStatPpid handles `123 (sh -c foo) S 1 123 ...` (space in comm) → ppid=1.
    - Test 3: parseStatPpid handles `123 (claude (test-agent)) S 1 123 ...` (paren in comm) → ppid=1.
    - Test 4: parseStatPpid throws on missing closing paren.
    - Test 5: buildMcpCommandRegexes({mysql:{command:"npm",args:["exec","mcp-server-mysql@latest"]}, mcporter:{command:"mcporter",args:["serve","mysql"]}}) matches "npm exec mcp-server-mysql@latest" AND "mcporter serve mysql" but NOT "npm install".
    - Test 6: buildMcpCommandRegexes throws on empty input.
    - Test 7 (Linux integration, skipIf non-linux): spawn `node -e "setInterval(()=>{},1000)"` as a child; readProcInfo(child.pid) returns {ppid: process.pid, uid: process.getuid(), cmdline starts with /node/}; child.kill() afterward.
    - Test 8 (Linux integration): readProcInfo(99999999) returns null (no throw).
    - Test 9: readProcInfo NEVER calls readFile on /proc/{pid}/environ — assert via spy on fs.readFile that no path containing 'environ' is ever requested.
  </behavior>
  <action>
    1. Create `src/mcp/proc-scan.ts` exporting all helpers above. Use `node:fs/promises` (readFile, readdir) — no other deps.
    2. parseStatPpid implementation MUST use `stat.lastIndexOf(')')` followed by `stat.slice(close + 2).split(' ')` — tail[0]=state, tail[1]=ppid. Pitfall 4 from RESEARCH.md.
    3. readProcInfo: Promise.all([readFile(stat,utf8), readFile(cmdline,utf8), readFile(status,utf8)]); catch ENOENT/ESRCH → return null; bubble other errors.
    4. cmdline parsing: split('\\0').filter(Boolean) — NUL-delimited argv, trailing NUL gives empty string we drop.
    5. UID extraction: `status.match(/^Uid:\\s*(\\d+)/m)[1]` — first numeric field is real UID.
    6. buildMcpCommandRegexes: escape regex meta with `s.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&')`; alternation join with `|`; wrap in single RegExp; throw if input map is empty.
    7. procAgeSeconds: `((Date.now()/1000) - bootTimeUnix) - (startTimeJiffies / clockTicksPerSec)` — no syscall; bootTimeUnix passed in from `/proc/stat` btime parsed once at daemon start (helper to read btime included).
    8. Create `src/mcp/__tests__/proc-scan.test.ts` with vitest. Linux-gated tests use `it.skipIf(process.platform !== 'linux')`.
    9. Per `~/.claude/rules/security.md`: explicit assertion in test 9 that `/proc/{pid}/environ` is NEVER read.

    Reference existing pattern: `src/mcp/health.ts` for module shape; `src/manager/secrets-resolver.ts` for boundary-validation idiom (throw on invalid input rather than silent default).
  </action>
  <verify>
    <automated>cd /home/jjagpal/.openclaw/workspace-coding && npx vitest run src/mcp/__tests__/proc-scan.test.ts</automated>
  </verify>
  <done>
    All 9 tests pass on Linux. parseStatPpid handles all three comm-field edge cases. buildMcpCommandRegexes builds union regex from configured commands (no hardcoded list). readProcInfo verified by spy to never request /proc/{pid}/environ. File ≤ 200 lines.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: process-tracker.ts + RED tests (MCP-01, MCP-02, MCP-04 substrate)</name>
  <files>src/mcp/process-tracker.ts, src/mcp/__tests__/process-tracker.test.ts</files>
  <behavior>
    - killGroup(pid, sig, log): calls `process.kill(-pid, sig)`; ESRCH → return true (already dead, idempotent success); EPERM → log warn + return false; refuses pid <= 1 (logs error + returns false). Other errors logged + rethrown.
    - McpProcessTracker class:
      - constructor({uid, patterns, log, clockTicksPerSec, bootTimeUnix}) — captures DI deps, mirrors SecretsResolver constructor shape.
      - register(agentName, pids: readonly number[]): adds pids to internal Map<string, Set<number>>; immutable update (replace set, don't mutate).
      - unregister(agentName): removes the agent's entry; returns the evicted PIDs as readonly number[].
      - list(): returns flattened readonly number[] across all agents.
      - listForAgent(agentName): returns readonly number[] (empty if unknown).
      - killAgentGroup(agentName, graceMs=5000): SIGTERMs each pid via killGroup; awaits min(graceMs, 5000); SIGKILLs (-pid) any whose readProcInfo still returns non-null with same starttime; emits canonical warn log per kill with `reason: "agent-disconnect"`; idempotent.
      - killAll(graceMs=5000): same as killAgentGroup but iterates all tracked PIDs across all agents; emits `reason: "shutdown"`. Used by MCP-04.
    - All kill paths emit the canonical pino warn log with `component: "mcp-tracker"` (NOT mcp-reaper — orphan-reaper.ts owns that component name). Action field: "sigterm" or "sigkill".

    RED tests in process-tracker.test.ts:
    - Test 1: killGroup with pid=1 → returns false, logs error, never calls process.kill.
    - Test 2: killGroup ESRCH (process.kill mocked to throw {code:'ESRCH'}) → returns true, no rethrow.
    - Test 3: killGroup EPERM → returns false, warn log emitted.
    - Test 4: killGroup happy path → calls process.kill with NEGATIVE pid (assert spy: `expect(killSpy).toHaveBeenCalledWith(-12345, 'SIGTERM')`).
    - Test 5: register/unregister/list/listForAgent — immutability check (mutate returned array doesn't affect tracker state).
    - Test 6: killAgentGroup SIGTERM-then-SIGKILL — fake timers; mock readProcInfo to return non-null on first poll, null after grace; assert SIGTERM called once per pid, SIGKILL only on stragglers.
    - Test 7: killAll iterates all agents — register two agents with overlapping pids; killAll fires SIGTERM exactly once per unique pid.
    - Test 8: canonical log shape — capture pino transport output, assert object matches `{component:'mcp-tracker', action:'sigterm', pid:N, reason:'agent-disconnect', cmdline:string}`.
    - Test 9: idempotent killAgentGroup — calling twice in succession does not throw; second call is a no-op (pids already evicted on first call).
  </behavior>
  <action>
    1. Create `src/mcp/process-tracker.ts`. Mirror `src/manager/secrets-resolver.ts` shape: exported class, DI via constructor, no module-level state.
    2. killGroup helper exported at module level so orphan-reaper can use it without a tracker instance (orphans are not tracked by name).
    3. Internal state: `private readonly pids = new Map<string, ReadonlySet<number>>();` — replace entries on register, never mutate sets in place (immutability per coding-style).
    4. killAgentGroup polling loop: every 250ms re-check via readProcInfo; on null OR starttime mismatch (PID recycled) → done; on grace expiry → SIGKILL via process.kill(pid, 'SIGKILL') (positive pid, individual proc, not group — group leader may already be dead).
    5. Capture cmdline at register-time (call readProcInfo per pid) so kill-time logs can include cmdline even after the proc is dead. Store as `Map<number, string>` alongside the pid set.
    6. Use the `clockTicksPerSec` and `bootTimeUnix` injected via constructor for any age math.
    7. Per coding-style: every error path logs context (component, action, pid, code); never silently swallow.

    Reference: `src/manager/secrets-resolver.ts:1-150` for class shape; `src/heartbeat/runner.ts:140-170` for clearInterval pattern (used in killAgentGroup polling).
  </action>
  <verify>
    <automated>cd /home/jjagpal/.openclaw/workspace-coding && npx vitest run src/mcp/__tests__/process-tracker.test.ts</automated>
  </verify>
  <done>
    All 9 tests pass. killGroup uses NEGATIVE pid (process-group form). killAgentGroup respects grace period via fake timers. Canonical log shape pinned in tests. File ≤ 250 lines.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: orphan-reaper.ts + RED tests (MCP-03, MCP-05 substrate)</name>
  <files>src/mcp/orphan-reaper.ts, src/mcp/__tests__/orphan-reaper.test.ts</files>
  <behavior>
    - scanForOrphanMcps({uid, patterns, minAgeSeconds=5, clockTicksPerSec, bootTimeUnix}): walks listAllPids() → readProcInfo per pid → filters: ppid===1 AND uid===uid AND matchesAnyMcpCommand(cmdline) AND procAgeSeconds() >= minAgeSeconds. Returns readonly array of {pid, cmdline, startTimeJiffies}.
    - reapOrphans({tracker, reason, log, graceMs=10000}): runs scanForOrphanMcps using tracker's stored uid+patterns; for each candidate emits `{component:'mcp-reaper', action:'sigterm', pid, cmdline:cmdline.join(' '), reason}` warn log; calls process.kill(pid, 'SIGTERM') (individual pid, NOT process-group — orphans may not retain pgid leadership; per RESEARCH.md Open Question 2); waits graceMs; rescans; SIGKILL stragglers with `action:'sigkill', graceMs` log fields.
    - startOrphanReaper({tracker, intervalMs=60_000, log, onTickAfter?: () => Promise<void>}): returns NodeJS.Timeout from setInterval. Each tick: (1) reapOrphans, (2) if `onTickAfter` callback provided, await it AFTER orphan reap completes (Wave 1 wires the MCP-09 sweep here per the locked decision "AFTER orphan reaper"). First tick fires after intervalMs (not immediately — boot scan covers t=0). Returns the handle so daemon can clearInterval on shutdown.
    - Reason field strict union: "orphan-ppid-1" (reaper interval) | "boot-scan" (one-shot at startup) — caller passes which.

    RED tests in orphan-reaper.test.ts:
    - Test 1: scanForOrphanMcps filter logic — mock listAllPids+readProcInfo to return [
        {pid:100, ppid:1, uid:999, cmdline:["sh","-c","mcp-server-mysql"], age:10},  // MATCH
        {pid:101, ppid:5000, uid:999, cmdline:["sh","-c","mcp-server-mysql"], age:10}, // skip — has parent
        {pid:102, ppid:1, uid:0, cmdline:["sh","-c","mcp-server-mysql"], age:10},   // skip — wrong uid
        {pid:103, ppid:1, uid:999, cmdline:["bash"], age:10},                        // skip — cmdline mismatch
        {pid:104, ppid:1, uid:999, cmdline:["sh","-c","mcp-server-mysql"], age:2},   // skip — too young
      ] → returns only [{pid:100, ...}].
    - Test 2: reapOrphans canonical log shape — assert SIGTERM warn log fields match exactly: `{component:'mcp-reaper', action:'sigterm', pid:100, cmdline:'sh -c mcp-server-mysql', reason:'orphan-ppid-1', msg:'reaping orphaned MCP server'}`.
    - Test 3: reapOrphans SIGKILL grace — fake timers; first scan returns 1 candidate; after SIGTERM the candidate stays alive (mock readProcInfo continues to return non-null); after graceMs, SIGKILL fires with `action:'sigkill', graceMs:10000` log.
    - Test 4: reapOrphans ESRCH idempotency — process.kill mocked to throw ESRCH; reapOrphans completes without throwing.
    - Test 5: startOrphanReaper clearInterval — start, advance fake time by intervalMs*3, observe 3 scans; clearInterval; advance again, no further scans.
    - Test 6: scanForOrphanMcps respects boot-scan reason — when called with reason='boot-scan', the resulting log entries set reason='boot-scan' (not 'orphan-ppid-1').
    - Test 7 (Linux integration, skipIf non-linux): inject a real orphan via `child_process.spawn('sleep',['30'],{detached:true,stdio:'ignore'}).unref()` then kill the parent shell node spawn → resulting proc has ppid=1 briefly. (Implementer NOTE: this test requires a controlled fixture — alternative is to spawn a shell that double-forks. Use the simpler form: spawn `bash -c 'sleep 30 & disown; exit 0'` and wait for shell exit; the sleep gets reparented to ppid=1.) Pass the sleep cmdline through the patterns (test config). Assert reapOrphans terminates it within one tick. Cleanup: ensure no leaked sleep procs.
    - Test 8 (NEW for MCP-09 wiring): startOrphanReaper with onTickAfter callback — fake timers; advance by intervalMs; assert reapOrphans finishes BEFORE onTickAfter is invoked (sequence pinned via shared call-order array). Locks today's "AFTER orphan reaper" decision.
  </behavior>
  <action>
    1. Create `src/mcp/orphan-reaper.ts` importing from `./proc-scan` and from `node:child_process` only when needed (this module shouldn't spawn — it just kills).
    2. scanForOrphanMcps: pure function — caller passes uid + patterns + age threshold. No side effects beyond /proc reads. Returns immutable array.
    3. reapOrphans: orchestrator — uses scanForOrphanMcps twice (initial + post-grace). Uses `process.kill(pid, sig)` (POSITIVE pid) per RESEARCH.md Open Question 2 — orphans don't reliably retain pgid.
    4. Sleep helper: `await new Promise(r => setTimeout(r, ms))` — pure stdlib.
    5. startOrphanReaper: `const id = setInterval(async () => { try { await reapOrphans(...); if (onTickAfter) await onTickAfter(); } catch (err) { log.error({err}, 'reaper tick failed') } }, intervalMs); return id;` — first tick AFTER intervalMs, not immediately. Errors must NOT propagate out of setInterval (would crash daemon). The onTickAfter callback is the seam Wave 1 uses to plug in the MCP-09 sweep without modifying this module again.
    6. Per coding-style: every kill is logged BEFORE the syscall (so a crashed tracker still leaves audit evidence).

    Reference: `src/heartbeat/runner.ts:140-170` for the setInterval-with-error-isolation idiom; `src/manager/secrets-resolver.ts` for DI shape.
  </action>
  <verify>
    <automated>cd /home/jjagpal/.openclaw/workspace-coding && npx vitest run src/mcp/__tests__/orphan-reaper.test.ts</automated>
  </verify>
  <done>
    All 8 tests pass on Linux. scanForOrphanMcps applies all 5 filters (ppid + uid + cmdline + age + listed). Canonical log shape pinned exactly per CONTEXT.md `<specifics>`. setInterval handle returned for daemon shutdown to clearInterval. onTickAfter seam in place for Wave 1 MCP-09 wiring. File ≤ 220 lines.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: MCP-08 thread-cleanup classifier RED tests</name>
  <files>src/discord/__tests__/thread-cleanup.test.ts</files>
  <behavior>
    Wave 1 will add (in `src/discord/thread-cleanup.ts` — new tiny module, ~40 lines):
    - `classifyDiscordCleanupError(err: unknown): "prune" | "retain" | "unknown"` — pure function. Returns:
      - "prune" when `err.code === 50001` OR `err.code === 10003` OR (DiscordAPIError) `err.status === 404`
      - "retain" when `err.status >= 500 && err.status < 600` OR `err.status === 429` OR `err.code` matches network-codes set `{ECONNRESET, ETIMEDOUT, ENOTFOUND, EAI_AGAIN}`
      - "unknown" otherwise (caller treats as retain — conservative)
    - `cleanupThreadWithClassifier({spawner, registryPath, threadId, agentName, log, lock?})`:
      - Calls `spawner.archiveThread(threadId, {lock})` (existing — already prunes registry on success).
      - Catches errors. Classifies. On "prune": call `removeBinding` on registry directly + emit `{component:'thread-cleanup', action:'prune-after-discord-error', discordCode, threadId, agentName}` warn. On "retain"/"unknown": leave registry intact + emit `{component:'thread-cleanup', action:'retain-on-transient-error', discordCode, threadId, agentName}` info log.
      - Returns `{archived: boolean, bindingPruned: boolean, classification: "success"|"prune"|"retain"|"unknown"}`.

    RED tests pin all paths. Wave 0 ONLY writes the failing tests; Wave 1 adds the implementation file.

    Tests in src/discord/__tests__/thread-cleanup.test.ts:
    - Test 1: classifyDiscordCleanupError({code:50001}) === "prune".
    - Test 2: classifyDiscordCleanupError({code:10003}) === "prune".
    - Test 3: classifyDiscordCleanupError({status:404}) === "prune".
    - Test 4: classifyDiscordCleanupError({status:500}) === "retain".
    - Test 5: classifyDiscordCleanupError({status:503}) === "retain".
    - Test 6: classifyDiscordCleanupError({status:429}) === "retain".
    - Test 7: classifyDiscordCleanupError({code:"ECONNRESET"}) === "retain".
    - Test 8: classifyDiscordCleanupError({code:"ETIMEDOUT"}) === "retain".
    - Test 9: classifyDiscordCleanupError({code:"ENOTFOUND"}) === "retain".
    - Test 10: classifyDiscordCleanupError(new Error("random")) === "unknown".
    - Test 11: classifyDiscordCleanupError(null) === "unknown".
    - Test 12: cleanupThreadWithClassifier happy path — archiveThread resolves; helper returns `{archived:true, bindingPruned:true, classification:"success"}`; no warn log emitted.
    - Test 13: cleanupThreadWithClassifier on Discord 50001 — mock spawner.archiveThread to throw {code:50001}; helper invokes registry remove path itself; returns `{archived:false, bindingPruned:true, classification:"prune"}`; canonical warn log emitted with `discordCode:50001`.
    - Test 14: cleanupThreadWithClassifier on Discord 10003 — same as Test 13 with `discordCode:10003`.
    - Test 15: cleanupThreadWithClassifier on 5xx — registry NOT pruned; returns `{archived:false, bindingPruned:false, classification:"retain"}`; info log emitted (NOT warn) so 5xx storms don't blow up logs.
    - Test 16: cleanupThreadWithClassifier on unknown error — registry NOT pruned; returns `classification:"unknown"`.
    - Test 17 (regression on agentName field): the prune-after-discord-error log includes `agentName` field (so operators can filter logs by agent during incident triage like today's fin-acquisition + fin-test).
  </behavior>
  <action>
    1. Create `src/discord/__tests__/thread-cleanup.test.ts` with vitest. Tests import from `../thread-cleanup` — file does NOT exist yet (this is intentional RED state).
    2. Use vi.fn() for spawner.archiveThread mock and a fake logger (object with vi.fn() spies on warn/info/error).
    3. For the registry-write side effects in tests 13/14: mock `removeBinding` + `writeThreadRegistry` from `../thread-registry` via vi.mock(). Verify the canonical call sequence: archive throws → classifier returns "prune" → removeBinding called with (registry, threadId) → writeThreadRegistry called with updated registry.
    4. Test 17: assert `expect(log.warn).toHaveBeenCalledWith(expect.objectContaining({component:'thread-cleanup', action:'prune-after-discord-error', discordCode:50001, threadId:expect.any(String), agentName:expect.any(String)}), expect.any(String))`.
    5. RED expectation: ALL 17 tests fail at `import` time because `../thread-cleanup` doesn't exist. That's the desired RED state — Wave 1 Task 1 creates the module and turns them green.

    Per coding-style: tests are the contract; the implementation in Wave 1 conforms to these assertions exactly.
  </action>
  <verify>
    <automated>cd /home/jjagpal/.openclaw/workspace-coding && npx vitest run src/discord/__tests__/thread-cleanup.test.ts 2>&1 | grep -qE "(Cannot find module|17 failed|17 passed)" && echo "RED-or-GREEN state confirmed"</automated>
  </verify>
  <done>
    17 RED tests written. File imports `../thread-cleanup` (not yet created — RED expected). Test asserts every classifier branch + every cleanupThreadWithClassifier path including the canonical log shape with agentName field.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: MCP-09 stale-binding sweep RED tests</name>
  <files>src/discord/__tests__/stale-binding-sweep.test.ts</files>
  <behavior>
    Wave 1 will add (in `src/discord/stale-binding-sweep.ts` — new module, ~50 lines):
    - `scanStaleBindings({registry, now, idleMs}): readonly ThreadBinding[]` — pure. Returns entries where `now - binding.lastActivity > idleMs`. If `idleMs <= 0`, returns []. Sorted by oldest first (deterministic for log output).
    - `parseIdleDuration(input: string): number` — accepts `"24h"`, `"6h"`, `"30m"`, `"0"`. Returns ms. Throws on unparseable input. `"0"` → 0 (sweep disabled).
    - `sweepStaleBindings({spawner, registryPath, now, idleMs, log})` — orchestrator:
      - reads registry, calls scanStaleBindings, for each stale entry calls `cleanupThreadWithClassifier` (from MCP-08) and counts results.
      - emits ONE summary warn log per cycle: `{component:'thread-cleanup', action:'stale-sweep', staleCount, prunedCount, agents:{[name]:count}, idleMs, msg:'stale-binding sweep complete'}`.
      - if `idleMs <= 0` → emits no log + returns early (sweep disabled).
      - if `staleCount === 0` → emits debug-level log only.

    Tests in src/discord/__tests__/stale-binding-sweep.test.ts:
    - Test 1: scanStaleBindings filter — registry with 5 bindings, lastActivity = [now-1h, now-23h, now-25h, now-30h, now-2h]; idleMs=24h; returns the 25h + 30h entries (sorted: 30h first, 25h second).
    - Test 2: scanStaleBindings idleMs=0 → returns [].
    - Test 3: scanStaleBindings idleMs negative → returns [].
    - Test 4: parseIdleDuration("24h") === 86400000.
    - Test 5: parseIdleDuration("6h") === 21600000.
    - Test 6: parseIdleDuration("30m") === 1800000.
    - Test 7: parseIdleDuration("0") === 0.
    - Test 8: parseIdleDuration("garbage") throws.
    - Test 9: sweepStaleBindings happy path — fixture registry with 3 stale (fin-acquisition×2, fin-test×1) + 2 fresh. Mock cleanupThreadWithClassifier to return `{classification:"prune", bindingPruned:true}` for all. Assert: cleanup called 3 times; summary warn log emitted with `staleCount:3, prunedCount:3, agents:{"fin-acquisition":2,"fin-test":1}, idleMs:86400000`.
    - Test 10: sweepStaleBindings with idleMs=0 → cleanup NOT called; no warn log; sweep is disabled.
    - Test 11: sweepStaleBindings staleCount=0 → cleanup NOT called; no warn log (debug-level only).
    - Test 12: sweepStaleBindings continues on individual failure — first cleanup throws unexpectedly; sweep continues with remaining stale entries; summary log reflects `staleCount:3, prunedCount:2` (the failed one didn't count as pruned).
  </behavior>
  <action>
    1. Create `src/discord/__tests__/stale-binding-sweep.test.ts` with vitest. Imports from `../stale-binding-sweep` (RED — file doesn't exist).
    2. Use a fixture registry helper at the top of the test file:
       ```typescript
       function makeRegistry(bindings: Array<{threadId: string; agentName: string; lastActivity: number}>): ThreadRegistry { ... }
       ```
    3. Lock `now` to a constant (e.g., `const NOW = 1730000000000`) so test math is deterministic. Don't use `Date.now()` directly in tests.
    4. Mock cleanupThreadWithClassifier from `../thread-cleanup` via `vi.mock('../thread-cleanup', ...)` — Wave 1 will land both modules together.
    5. Assert summary log shape EXACTLY (the agents object is sorted alphabetically by key for deterministic log readability — pin this in the test).
    6. RED expectation: 12 tests fail at import time. Wave 1 Task 2 turns them green.

    Per coding-style: pure functions for scan + parse; only the orchestrator (sweepStaleBindings) has side effects. Tests reflect that split.
  </action>
  <verify>
    <automated>cd /home/jjagpal/.openclaw/workspace-coding && npx vitest run src/discord/__tests__/stale-binding-sweep.test.ts 2>&1 | grep -qE "(Cannot find module|12 failed|12 passed)" && echo "RED-or-GREEN state confirmed"</automated>
  </verify>
  <done>
    12 RED tests written. Fixture registry helper at top. Locked `NOW` constant for deterministic math. Mocks `../thread-cleanup`. Pins agents-object-alphabetical-sort in summary log. `idleMs<=0` semantics (disable) covered.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: MCP-10 CLI subcommand RED tests</name>
  <files>src/cli/commands/__tests__/threads.test.ts</files>
  <behavior>
    Wave 1 will extend `src/cli/commands/threads.ts` adding three subcommands to the existing `threads` Commander program. The test file pre-stages all three behaviors.

    NEW IPC methods Wave 1 adds in daemon.ts:
    - `threads-prune-stale` — params: `{staleAfter: string}`. Daemon parses duration, calls sweepStaleBindings on demand. Returns `{staleCount, prunedCount, agents}`.
    - `threads-prune-agent` — params: `{agent: string}`. Daemon iterates registry, calls `removeBinding` for every binding where `agentName === params.agent`. NO Discord call. Returns `{prunedCount}`.

    The existing `archive-discord-thread` IPC method gets a Wave 1 swap-in: its handler in daemon.ts:5285 now calls `cleanupThreadWithClassifier` instead of the bare `subagentThreadSpawner.archiveThread`. Behavior change: on 50001/10003, instead of throwing, it returns `{ok:true, archived:false, bindingPruned:true, classification:'prune'}`. CLI consumes that response.

    Tests in src/cli/commands/__tests__/threads.test.ts:
    - Test 1: `clawcode threads archive <id>` — mock sendIpcRequest to return `{ok:true, archived:true, bindingPruned:true, classification:"success"}`; assert CLI prints "Archived <id> (registry pruned)" and exits 0.
    - Test 2: `clawcode threads archive <id> --lock` — assert IPC params include `{lock: true}`.
    - Test 3 (the operator-pain regression): `clawcode threads archive <id>` when daemon returns `{ok:true, archived:false, bindingPruned:true, classification:"prune"}` (Discord 50001/10003 path) — CLI prints "Discord thread already gone; registry pruned" and exits 0 (NOT nonzero).
    - Test 4: `clawcode threads archive <id>` when daemon returns `{ok:true, archived:false, bindingPruned:false, classification:"retain"}` (transient 5xx) — CLI prints "Transient Discord error; binding retained for next sweep" and exits 0 (operator can retry; not a hard failure).
    - Test 5: `clawcode threads prune --stale-after 24h` — IPC method "threads-prune-stale" sent with `{staleAfter:"24h"}`; mock response `{staleCount:5, prunedCount:5, agents:{"fin-acquisition":3,"fin-test":2}}`; CLI prints summary table (rows: agent, count) and "5 stale bindings pruned" + exits 0.
    - Test 6: `clawcode threads prune --stale-after 6h` — same but `staleAfter:"6h"`.
    - Test 7: `clawcode threads prune --agent fin-acquisition` — confirmation prompt fires; user types "n" → CLI exits 0 with "Aborted by user"; sendIpcRequest NOT called.
    - Test 8: `clawcode threads prune --agent fin-acquisition --yes` — prompt SKIPPED; IPC method "threads-prune-agent" sent with `{agent:"fin-acquisition"}`; mock response `{prunedCount:3}`; CLI prints "Pruned 3 bindings for agent fin-acquisition" + exits 0.
    - Test 9: `clawcode threads prune --agent fin-acquisition` (no --yes) — prompt fires; user types "y" → CLI proceeds; IPC sent.
    - Test 10: error path — sendIpcRequest throws ManagerNotRunningError; CLI prints "Manager is not running" and exits 1 (matches existing pattern in threads.ts:170).
  </behavior>
  <action>
    1. Create `src/cli/commands/__tests__/threads.test.ts` with vitest.
    2. Mock `sendIpcRequest` from `../../../ipc/client` (or wherever the existing threads.ts imports it from — grep first to get the path right). Use `vi.mock`.
    3. Mock `cliLog`, `cliError` so output assertions can be made via spies.
    4. For prompt tests (7 + 9): mock `readline` (or whatever the implementer uses — Wave 1 picks; spec assumes `readline.createInterface`'s `question` method). The test injects fake stdin responses ("y" / "n").
    5. Use Commander's `parseAsync` API with a synthetic argv: `await program.parseAsync(['node','clawcode','threads','archive','foo'], {from:'user'})`.
    6. RED expectation: tests fail because the new subcommands aren't registered yet on the existing `registerThreadsCommand` program. Wave 1 Task 3 adds them.
    7. Test 3 + Test 4 are the most important — they encode today's operator pain: archive-on-stale-Discord-thread must NOT error out; it must succeed-with-message. If those go green in Wave 1, the operator escape hatch works.

    Per coding-style: prompt logic in implementation gets extracted into a tiny `confirmPrompt(msg): Promise<boolean>` helper so the test can mock just that helper rather than wrestling with raw stdin. Spec the helper signature here so Wave 1 follows it.
  </action>
  <verify>
    <automated>cd /home/jjagpal/.openclaw/workspace-coding && npx vitest run src/cli/commands/__tests__/threads.test.ts 2>&1 | grep -qE "(failed|passed)" && echo "RED-or-GREEN state confirmed"</automated>
  </verify>
  <done>
    10 RED tests written. Mocks sendIpcRequest, cliLog, cliError. Tests 3 + 4 explicitly cover the operator pain: 50001/10003 paths exit 0 with friendly messages, 5xx paths exit 0 with retry-later messaging. Tests 7 + 9 cover the confirmation prompt. Test 8 covers --yes bypass. confirmPrompt helper signature specified for Wave 1.
  </done>
</task>

</tasks>

<verification>
After all 6 tasks:
```bash
cd /home/jjagpal/.openclaw/workspace-coding
# MCP-01..07 substrate (must be GREEN by end of Wave 0):
npx vitest run src/mcp/__tests__/proc-scan.test.ts src/mcp/__tests__/process-tracker.test.ts src/mcp/__tests__/orphan-reaper.test.ts

# MCP-08/09/10 RED tests (expected to FAIL — Wave 1 turns them green):
npx vitest run src/discord/__tests__/thread-cleanup.test.ts src/discord/__tests__/stale-binding-sweep.test.ts src/cli/commands/__tests__/threads.test.ts || echo "Expected RED — Wave 1 implements"

# Type-check (must pass — RED tests still need to type-check; mocked imports + non-existent modules will fail tsc):
npx tsc --noEmit
```

Type-check note: the RED tests in tasks 4/5/6 import from modules that don't exist yet (`../thread-cleanup`, `../stale-binding-sweep`, plus extended exports of `./threads`). To keep `tsc --noEmit` clean while Wave 0 is in RED state, EITHER (a) include test-only declaration shims in each test file with `declare module "../thread-cleanup" { ... }`, OR (b) accept that `tsc --noEmit` fails for these three test files until Wave 1 lands and gate the type-check on Wave 1 instead.

**Decision: pick (a) — declaration shims.** Each Wave 0 RED test file includes a top-level `declare module` block with the exact contract Wave 1 must implement. This serves DOUBLE duty: type-check stays clean, AND Wave 1 has the exact type signatures pre-specified. The shim gets deleted in Wave 1 once the real implementation exists.

All 25 MCP-01..07 tests green. RED-state tests (~39) authored with declaration shims so tsc stays clean. No regex literal hardcoding MCP server names — all from config. No imports from `@anthropic-ai/claude-agent-sdk` in any module (SDK-agnostic).
</verification>

<success_criteria>
- [ ] proc-scan.ts handles comm field with parens/spaces (Pitfall 4) — proven by tests 2 & 3
- [ ] process-tracker.ts uses NEGATIVE pid for process-group kills (MCP-01 unit) — proven by test 4
- [ ] orphan-reaper.ts uses POSITIVE pid for orphan kills (Open Question 2) — proven by mock spy
- [ ] orphan-reaper.startOrphanReaper accepts onTickAfter callback (MCP-09 wiring seam) — proven by test 8
- [ ] All kills are idempotent on ESRCH — proven across all three core test files
- [ ] No code reads /proc/{pid}/environ — proven by proc-scan test 9
- [ ] Cmdline regex built from injected config, not literal — proven by buildMcpCommandRegexes tests
- [ ] Age filter (5s minimum) — proven by orphan-reaper test 1 case 5
- [ ] Canonical reaper log shape matches CONTEXT.md `<specifics>` exactly — pinned in test 2 of orphan-reaper
- [ ] MCP-08 RED: classifyDiscordCleanupError truth table covered (50001/10003/404/5xx/429/network/unknown — 11 cases)
- [ ] MCP-08 RED: cleanupThreadWithClassifier prune/retain/unknown paths pinned + agentName field in log
- [ ] MCP-09 RED: scanStaleBindings + parseIdleDuration + sweepStaleBindings pinned; idleMs=0 disable semantics
- [ ] MCP-10 RED: archive subcommand exits 0 on Discord 50001/10003 (operator pain regression)
- [ ] MCP-10 RED: prune --stale-after + prune --agent --yes + confirmation prompt covered
- [ ] tsc --noEmit clean (declaration shims keep RED tests typed)
- [ ] All MCP-01..07 RED tests fail before implementation, all GREEN after (TDD discipline)
</success_criteria>

<output>
After completion, create `.planning/phases/999.14-mcp-server-child-process-lifecycle-hardening/999.14-00-SUMMARY.md` documenting:
- Files created: 3 modules (proc-scan, process-tracker, orphan-reaper) + 6 test files (3 GREEN core + 3 RED for MCP-08/09/10)
- Public exports of each core module (executor needs these for Wave 1 wiring)
- Declaration shim contracts in each RED test file (executor implements those exact signatures in Wave 1)
- Test count + runtime (expect ~25 GREEN + ~39 RED)
- Any deviations from RESEARCH.md (none expected)
- Confirmation that environ is never read (security checklist line)
- Confirmation that startOrphanReaper exposes the onTickAfter seam for MCP-09
</output>
