---
phase: 110-mcp-memory-reduction-shim-runtime-swap
plan: 05
type: execute
wave: 3
depends_on: ["110-04"]
files_modified:
  - .planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-05-ROLLOUT-LOG.md
  - clawcode.yaml
autonomous: false
requirements: [0B-RT-07, 0B-RT-08, 0B-RT-10, 0B-RT-11, 0B-RT-12]
must_haves:
  truths:
    - "admin-clawdy's `defaults.shimRuntime.search` flips from `\"node\"` to `\"static\"` via clawcode.yaml edit; ConfigWatcher hot-reloads without daemon restart (verified: `clawcode <pid>` does NOT change)."
    - "After admin-clawdy MCP-children cycle, the search shim spawns under `/usr/local/bin/clawcode-mcp-shim --type search`, NOT `clawcode search-mcp` (verified: `pgrep -af clawcode-mcp-shim` shows the new proc; `pgrep -af 'clawcode search-mcp'` for admin-clawdy shows nothing)."
    - "`/api/fleet-stats` reports admin-clawdy's search shim with `runtime: \"static\"` and per-shim RSS sub-15 MB."
    - "After 24-48h of dashboard watch on admin-clawdy alone (operator gate), the same flag flip applies to ALL fleet agents (`defaults.shimRuntime.search: static` at the global default level)."
    - "After full-fleet rollout, aggregate search-shim RSS is < 100 MB (vs ~1.0 GiB baseline) — measured via measure-shim-rss.sh."
    - "Rollback procedure works: flipping `static` → `node` causes ConfigWatcher hot-reload; new search MCP children spawn under Node again. No daemon restart required."
    - "Crash-fallback (LOCKED): if a Go shim crashes during the watch window, operator surfaces error via journalctl, manually flips the agent back to `node`, and files an issue. NO automatic fallback."
  artifacts:
    - path: ".planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-05-ROLLOUT-LOG.md"
      provides: "Operator-recorded rollout journal: flip times, RSS samples, dashboard observations, decision points"
    - path: "clawcode.yaml (production)"
      provides: "Per-agent override on admin-clawdy first; promoted to global default after watch window"
      contains: "shimRuntime"
  key_links:
    - from: "clawcode.yaml defaults.shimRuntime.search"
      to: "src/config/loader.ts resolveShimCommand"
      via: "ConfigWatcher hot-reload pipeline (Phase 998 commit 98ff1bc pattern)"
      pattern: "shimRuntime"
---

<objective>
Wave 3 — search-mcp rollout. Two human-gated checkpoints: (1) flip admin-clawdy's search shim to static, watch 24-48h; (2) on green dashboard, flip global default and roll search to all 11 agents.

**Locked decisions (CONTEXT.md):**
- Per-shim-type rollout order: search → image → browser (this plan does search; image and browser are plans 110-06 and 110-07)
- 24-48h dashboard watch between waves (operator gate)
- Wave 0 spike binary path was per-agent manual; Wave 3 uses the proper schema-driven flag flip with hot-reload (no daemon restart)
- Crash-fallback: fail loud — operator manual rollback only, no auto-fall-back

Purpose: prove the runtime swap pattern works on a low-traffic agent (admin-clawdy) before subjecting fin-acquisition or other production-traffic agents to risk. Establish the rollout playbook that plans 110-06 and 110-07 mirror.

Output: search shim runs as Go static binary on entire fleet; `/api/fleet-stats` shows runtime: "static" for all 11 search shims; aggregate search-shim RSS reduced from ~1.0 GiB to < 100 MB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-CONTEXT.md
@.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-VALIDATION.md
@.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-02-SUMMARY.md
@.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-03-SUMMARY.md
@.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-04-SUMMARY.md

<deploy_prerequisites>
Before starting this plan, the following MUST be true (verify in Task 1):
1. Wave 1 (plans 110-01, 110-02, 110-03) has DEPLOYED to clawdy: daemon serves `list-mcp-tools`, schema enum is widened, loader reads shimRuntime, fleet-stats classifier recognizes the binary path.
2. Wave 2 (plan 110-04) has DEPLOYED: `/usr/local/bin/clawcode-mcp-shim` exists on clawdy, is executable, and `clawcode-mcp-shim --type search --help` (or stdin EOF) responds without error.
3. ConfigWatcher hot-reload is functioning (verified by Phase 999.27 hot-fix in commit `98ff1bc`).
</deploy_prerequisites>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author rollout log scaffolding + verify deploy prerequisites</name>
  <read_first>
    - .planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-CONTEXT.md (Rollout Policy table)
    - .planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-VALIDATION.md (Manual-Only Verifications §24-48h dashboard watch)
    - .planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-04-SUMMARY.md (Wave 2 deploy artifact + binary install path)
  </read_first>
  <files>.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-05-ROLLOUT-LOG.md, scripts/integration/measure-shim-rss.sh</files>
  <action>
    Create `.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-05-ROLLOUT-LOG.md` with these sections:

    1. **Deploy prerequisites checklist** (verify ALL before proceeding):
       - [ ] Daemon on clawdy serves `list-mcp-tools` IPC method (test: `clawcode ipc call list-mcp-tools '{"shimType":"search"}' | jq '.tools | length'` returns ≥ 1)
       - [ ] Schema enum widened: `clawcode config show | grep shimRuntime` shows the option
       - [ ] Loader reads shimRuntime: a fake test agent with `shimRuntime.search: static` shows the static command in `clawcode show-mcp <agent>`
       - [ ] Binary at `/usr/local/bin/clawcode-mcp-shim` exists, executable, ≤ 12 MB on disk
       - [ ] Fleet-stats classifier recognizes binary basename: deploy a fake static spawn, check `/api/fleet-stats` returns runtime: "static"

    2. **Phase 1 — admin-clawdy canary flip** (Task 2 checkpoint):
       - Pre-flip baseline: capture admin-clawdy current search shim PID + VmRSS via `cat /proc/<pid>/status | grep VmRSS`
       - Edit clawcode.yaml: add `agents.admin-clawdy.shimRuntime.search: static` (per-agent override; global default stays node)
       - Save → ConfigWatcher should detect → log line "config reloaded" should appear in journalctl
       - admin-clawdy's search MCP child cycles (claude proc detects MCP exit, respawns under new command)
       - Verify new shim PID differs from old; new cmdline matches `clawcode-mcp-shim --type search`
       - Sample VmRSS 3 times across 30 minutes
       - Pass criteria: VmRSS ≤ 15 MB; tool calls work (smoke-test `web_search` from admin-clawdy); no journalctl errors
       - Decision recording table: time, PID, VmRSS, tool-call success/fail, journalctl error count

    3. **24-48h watch window**:
       - Dashboard URL: `/api/fleet-stats` queried daily
       - Watch for: per-shim-type RSS holding sub-15 MB, no broker error spikes, no claude-process drift, no Go shim crashes in journalctl
       - Operator decision: GREEN (proceed to Phase 2) or RED (rollback per Phase 4)

    4. **Phase 2 — fleet rollout** (Task 3 checkpoint):
       - Edit clawcode.yaml: change global default `defaults.shimRuntime.search: static`; remove the per-agent override on admin-clawdy
       - Save → ConfigWatcher reload → all 11 agents' search MCP children cycle
       - Wait 5 minutes for stabilization
       - Run `scripts/integration/measure-shim-rss.sh` → expect aggregate search-shim RSS < 100 MB
       - Verify all 11 agents' search tool calls work (smoke-test from 2-3 random agents including fin-acquisition)
       - Decision recording table

    5. **Crash-fallback policy reminder** (verbatim from CONTEXT.md): *"Fail loud, NO auto-fall-back to Node. Surface segfaults; do not silently degrade."*

    6. **Rollback procedure** (locked manual-only):
       - Edit clawcode.yaml: change `defaults.shimRuntime.search: static` back to `defaults.shimRuntime.search: node`
       - ConfigWatcher reload → next MCP child spawn uses Node again
       - File issue with journalctl excerpts + RSS measurements; do NOT auto-recover

    Create `scripts/integration/measure-shim-rss.sh`:

    ```bash
    #!/usr/bin/env bash
    # Phase 110 Stage 0b — measure aggregate shim RSS on clawdy host.
    set -euo pipefail
    SHIM_TYPE="${1:-all}"

    case "$SHIM_TYPE" in
      search|image|browser)
        PATTERN="clawcode-mcp-shim --type $SHIM_TYPE"
        ;;
      all)
        PATTERN="clawcode-mcp-shim --type"
        ;;
      *)
        echo "ERROR: unknown shim type $SHIM_TYPE (want search|image|browser|all)" >&2
        exit 64
        ;;
    esac

    PIDS="$(pgrep -f "$PATTERN" | tr '\n' ' ')"
    if [ -z "$PIDS" ]; then
      echo "No matching shims found for: $PATTERN"
      exit 0
    fi

    TOTAL_KB=0
    COUNT=0
    for PID in $PIDS; do
      KB="$(awk '/^VmRSS:/ {print $2}' /proc/$PID/status 2>/dev/null || echo 0)"
      MB=$((KB / 1024))
      echo "PID=$PID VmRSS=${KB}kB (${MB}MB)"
      TOTAL_KB=$((TOTAL_KB + KB))
      COUNT=$((COUNT + 1))
    done

    TOTAL_MB=$((TOTAL_KB / 1024))
    AVG_KB=$((TOTAL_KB / COUNT))
    AVG_MB=$((AVG_KB / 1024))
    echo "Total: ${TOTAL_KB}kB (${TOTAL_MB}MB) across $COUNT shims; avg ${AVG_KB}kB (${AVG_MB}MB)"
    ```
  </action>
  <verify>
    <automated>test -f /home/jjagpal/.openclaw/workspace-coding/.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-05-ROLLOUT-LOG.md && test -x /home/jjagpal/.openclaw/workspace-coding/scripts/integration/measure-shim-rss.sh && bash -n /home/jjagpal/.openclaw/workspace-coding/scripts/integration/measure-shim-rss.sh && grep -E "Fail loud|fail-loud" /home/jjagpal/.openclaw/workspace-coding/.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-05-ROLLOUT-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f /home/jjagpal/.openclaw/workspace-coding/.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-05-ROLLOUT-LOG.md` returns 0
    - Rollout log contains all 6 sections (prereq checklist, Phase 1, watch window, Phase 2, fail-loud reminder, rollback)
    - `grep -E "admin-clawdy" /home/jjagpal/.openclaw/workspace-coding/.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-05-ROLLOUT-LOG.md` returns at least 5 hits
    - `grep -Ei "fail loud|no auto.?fall.?back" /home/jjagpal/.openclaw/workspace-coding/.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-05-ROLLOUT-LOG.md` returns a hit
    - `grep -E "24-48h" /home/jjagpal/.openclaw/workspace-coding/.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-05-ROLLOUT-LOG.md` returns a hit
    - `grep -E "ConfigWatcher" /home/jjagpal/.openclaw/workspace-coding/.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-05-ROLLOUT-LOG.md` returns a hit
    - `test -x /home/jjagpal/.openclaw/workspace-coding/scripts/integration/measure-shim-rss.sh` returns 0
    - `bash -n /home/jjagpal/.openclaw/workspace-coding/scripts/integration/measure-shim-rss.sh` returns 0
    - `grep -E "VmRSS" /home/jjagpal/.openclaw/workspace-coding/scripts/integration/measure-shim-rss.sh` returns a hit
  </acceptance_criteria>
  <done>Rollout log scaffolded with all 6 sections, RSS measurement script executable, prerequisites checklist embedded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <action>Human-gated checkpoint. Claude prepares everything described in <what-built>; operator follows <how-to-verify> steps and responds per <resume-signal>. No autonomous code/file change in this task — the action IS the operator-driven verification recorded in the plan's referenced rollout/runbook log file.</action>
  <name>Task 2: GATE — Phase 1 admin-clawdy canary flip + 24-48h watch</name>
  <what-built>
    Wave 1+2 deployed: daemon serves list-mcp-tools, loader reads shimRuntime, Go shim binary at /usr/local/bin/clawcode-mcp-shim, fleet-stats classifier recognizes the binary, rollout log scaffolded.
  </what-built>
  <how-to-verify>
    Follow `.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-05-ROLLOUT-LOG.md` Phase 1 + watch sections:

    1. **Verify deploy prerequisites** (rollout log section 1, 5 checkboxes — all must be GREEN before flipping).

    2. **Capture pre-flip baseline**: ssh clawdy, find admin-clawdy's current search shim PID via `pgrep -af 'clawcode search-mcp' | grep -i admin-clawdy`. Record VmRSS and PID in the rollout log.

    3. **Edit clawcode.yaml** (production config on clawdy): add a per-agent override:
       ```yaml
       agents:
         admin-clawdy:
           shimRuntime:
             search: static
       ```
       Save the file.

    4. **Verify ConfigWatcher hot-reload**: `journalctl -u clawcode -f | grep -i config` should show a "config reloaded" line within ~5 seconds. Daemon PID should NOT change (`pgrep clawcode | wc -l` same as before).

    5. **Verify shim child cycle**: within ~30 seconds, admin-clawdy's MCP children cycle. Check:
       - `pgrep -af 'clawcode search-mcp' | grep admin-clawdy` returns NOTHING (old Node shim gone)
       - `pgrep -af 'clawcode-mcp-shim --type search'` shows a NEW process under admin-clawdy's claude proc
       - `cat /proc/<new-pid>/status | grep VmRSS` shows ≤ 15 MB

    6. **Smoke-test tool calls** from admin-clawdy: trigger a `web_search` tool call. Confirm it returns results identically to before.

    7. **Sample 3 times across 30 minutes**: VmRSS, tool-call latency, journalctl error count. Record in rollout log Phase 1 table.

    8. **Verify /api/fleet-stats**: `curl localhost:<dashboard-port>/api/fleet-stats | jq '.mcpFleet[] | select(.agent == "admin-clawdy" and .name == "search")'` should show `runtime: "static"` and per-shim RSS in the sub-15 MB range.

    9. **Watch window — 24 to 48 hours**: monitor:
       - `/api/fleet-stats` daily — RSS holding sub-15 MB? errors? drift?
       - `journalctl -u clawcode --since '1 hour ago' | grep -E 'clawcode-mcp-shim|panic|TEMPFAIL' ` — any crashes?
       - Tool call success rate from admin-clawdy — did anything regress?

    10. **Decision**:
        - **GREEN** (no crashes, RSS held, tool calls clean for 24-48h): respond `green-canary` to advance to Phase 2 (Task 3 fleet rollout).
        - **RED** (any anomaly: crash, RSS spike, tool failure, FD leak): respond `red-rollback` and execute the rollback section of the rollout log (flip back to node, file issue). Stage 0b stops here for search; replanning needed before retry.

    Crash-fallback policy reminder: ZERO auto-fall-back to Node. Operator manual rollback only.
  </how-to-verify>
  <resume-signal>Type `green-canary` after 24-48h watch shows admin-clawdy stable on Go shim, OR type `red-rollback` with anomaly details to halt search rollout for replanning.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <action>Human-gated checkpoint. Claude prepares everything described in <what-built>; operator follows <how-to-verify> steps and responds per <resume-signal>. No autonomous code/file change in this task — the action IS the operator-driven verification recorded in the plan's referenced rollout/runbook log file.</action>
  <name>Task 3: GATE — Phase 2 fleet rollout (search shim → all 11 agents)</name>
  <what-built>
    admin-clawdy proven stable on Go static shim for 24-48h with sub-15 MB RSS, no crashes, no tool-call regressions.
  </what-built>
  <how-to-verify>
    Follow rollout log Phase 2:

    1. **Edit clawcode.yaml** on clawdy: change global default and remove per-agent override:
       ```yaml
       defaults:
         shimRuntime:
           search: static  # was: node
       agents:
         admin-clawdy:
           # shimRuntime override removed — admin-clawdy now picks up global default
       ```

    2. **Verify ConfigWatcher hot-reload**: same as Phase 1 step 4.

    3. **Wait 5 minutes** for all 11 agents' search MCP children to cycle.

    4. **Verify all 11 agents flipped**:
       - `pgrep -af 'clawcode search-mcp' | wc -l` returns 0 (zero Node search shims fleet-wide)
       - `pgrep -af 'clawcode-mcp-shim --type search' | wc -l` returns 11 (one per fleet agent)

    5. **Run aggregate RSS measurement**: `ssh clawdy '/path/to/scripts/integration/measure-shim-rss.sh search'` → expect total < 100 MB (vs ~1.0 GiB baseline).

    6. **Smoke-test 2-3 random agents** including fin-acquisition (which is operator's primary trading agent):
       - Trigger `web_search` tool calls from each
       - Confirm responses arrive identically
       - Confirm no journalctl errors

    7. **Verify /api/fleet-stats**: ALL search shims report `runtime: "static"`; aggregate matches measure-shim-rss.sh.

    8. **Record measurements** in rollout log Phase 2 table: per-agent VmRSS, total RSS, savings vs baseline (~1.0 GiB → measured), tool-call smoke-test results.

    9. **Decision**:
        - **GREEN** (all 11 agents flipped, RSS reduced, no errors): respond `fleet-green` — search rollout complete, plan 110-06 (image) is unblocked.
        - **RED** (any agent failed to flip, or RSS exceeded expected, or tool calls regressed): respond `fleet-rollback` and revert global default to node. Search rollout halts.

    Crash-fallback policy: NO auto-fall-back. Even if 1 of 11 agents has trouble, operator decides — manual rollback for the whole fleet, or scoped rollback by adding a per-agent `shimRuntime.search: node` override for the troubled agent only.
  </how-to-verify>
  <resume-signal>Type `fleet-green` after all 11 agents on Go static shim with verified RSS < 100 MB and clean smoke tests, OR type `fleet-rollback` with anomaly details.</resume-signal>
</task>

</tasks>

<verification>
- Task 1 acceptance criteria pass
- Task 2 + Task 3 checkpoint gates produce explicit operator approval recorded in 110-05-ROLLOUT-LOG.md
- After Task 3 GREEN: aggregate search-shim RSS < 100 MB on clawdy (measured), all 11 agents on `runtime: "static"`
</verification>

<success_criteria>
- 0B-RT-07 (per-type rollout independence — only search flipped, image/browser still node), 0B-RT-08 (hot-reload no daemon restart), 0B-RT-10 (/api/fleet-stats reports static), 0B-RT-11 (rollback flag flip works), 0B-RT-12 (memory savings measured) all verified
- Search shim aggregate RSS reduced from ~1.0 GiB baseline to < 100 MB at full fleet
- Rollout playbook validated for plan 110-06 (image) and 110-07 (browser) to mirror
</success_criteria>

<output>
After completion, create `.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-05-SUMMARY.md` documenting: pre/post RSS measurements, 24-48h watch journal entries, operator decisions at both gates, any anomalies + how resolved.
</output>
