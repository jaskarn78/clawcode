---
phase: 110-mcp-memory-reduction-shim-runtime-swap
plan: 06
type: execute
wave: 4
depends_on: ["110-05"]
files_modified:
  - internal/shim/image/register.go
  - internal/shim/image/register_test.go
  - cmd/clawcode-mcp-shim/main.go
  - .planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-06-ROLLOUT-LOG.md
autonomous: false
requirements: [0B-RT-01, 0B-RT-04, 0B-RT-05, 0B-RT-06, 0B-RT-07, 0B-RT-08, 0B-RT-10, 0B-RT-11, 0B-RT-12]
must_haves:
  truths:
    - "`internal/shim/image/register.go` mirrors `internal/shim/search/register.go` byte-for-byte EXCEPT for: shimType `\"image\"` (not `\"search\"`), daemon IPC method `\"image-tool-call\"` (not `\"search-tool-call\"`)."
    - "`cmd/clawcode-mcp-shim/main.go` swaps the image stub for `image.Register(server)`."
    - "All Pitfalls §1-§6 mitigations preserved (16 MB buffer via shared ipc/client.go, stderr-only logging, exit-75 semantics, no fallback/retry)."
    - "Operator-driven rollout mirrors 110-05's two-phase pattern: admin-clawdy canary 24-48h watch → fleet rollout."
    - "After fleet rollout, aggregate image-shim RSS is < 100 MB (vs ~1.0 GiB baseline)."
  artifacts:
    - path: "internal/shim/image/register.go"
    - path: "cmd/clawcode-mcp-shim/main.go (image branch)"
    - path: ".planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-06-ROLLOUT-LOG.md"
  key_links:
    - from: "internal/shim/image/register.go"
      to: "internal/shim/ipc/client.go SendRequest()"
      via: "ipc.SendRequest(\"image-tool-call\", ...)"
      pattern: "image-tool-call"
---

<objective>
Wave 4 — image-mcp migration. Implements `internal/shim/image/register.go` (mirroring search), wires into main.go, then runs the same two-phase rollout (admin-clawdy canary → fleet) gated by 24-48h watch.

**Locked decisions (CONTEXT.md):**
- Per-shim-type rollout order: search → image → browser. Image proceeds ONLY after search rollout (plan 110-05) is GREEN.
- 24-48h watch between waves
- Crash-fallback: fail loud, manual rollback only

Purpose: extend the proven Go shim runtime from search to image. Reuses the `internal/shim/ipc/client.go` from plan 110-04 — no new IPC plumbing needed.

Output: image shim runs as Go static binary on entire fleet; aggregate image-shim RSS < 100 MB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-CONTEXT.md
@.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-VALIDATION.md
@.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-04-SUMMARY.md
@.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: image Register — mirror search Register with image-tool-call IPC method</name>
  <read_first>
    - internal/shim/search/register.go (the canonical shape; image mirrors this exactly except for two strings)
    - internal/shim/search/register_test.go (test pattern to mirror)
    - src/cli/commands/image-mcp.ts (existing Node image shim — confirm daemon IPC method name is `image-tool-call`)
    - src/image/mcp-server.ts (inner translator — confirm tool list shape returned by list-mcp-tools shimType=image)
    - .planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-RESEARCH.md (Pattern 1 §code example; same shape for image)
  </read_first>
  <files>internal/shim/image/register.go, internal/shim/image/register_test.go, cmd/clawcode-mcp-shim/main.go</files>
  <behavior>
    Mirror `internal/shim/search/register.go` tests 1-5 with image substitutions:
    - Test 1: Register fetches tools at boot via `list-mcp-tools { shimType: "image" }`.
    - Test 2: handler dispatches `image-tool-call` with `{ agent, toolName, args }`.
    - Test 3: daemon error propagates as MCP error envelope, NEVER swallowed.
    - Test 4: CLAWCODE_AGENT env required.
    - Test 5: no fallback/retry — grep verifies absence.
    - Test 6 (NEW for image): `image_generate` tool's response payload (which can be large — base64 image data) round-trips through the 16 MB IPC buffer correctly. This is a regression test for Pitfall §2 in the image-shim path specifically.
  </behavior>
  <action>
    Create `internal/shim/image/register.go` by copy-pasting `internal/shim/search/register.go` and changing TWO strings:
    - `"shimType": "search"` → `"shimType": "image"`
    - `"search-tool-call"` → `"image-tool-call"`
    - Update package name `package search` → `package image`
    - Update import path comment (if any)

    Update `cmd/clawcode-mcp-shim/main.go`:
    - Replace the `case "image":` stub branch with:
      ```go
      case "image":
          if err := image.Register(server); err != nil {
              logger.Error("register image tools", "err", err, "serverType", *serverType)
              os.Exit(SHIM_EXIT_TEMPFAIL)
          }
      ```
    - Add `"github.com/jjagpal/clawcode-shim/internal/shim/image"` import.
    - Browser `case` remains a stub pointing at plan 110-07.

    Create `internal/shim/image/register_test.go` mirroring `internal/shim/search/register_test.go` with `image` substitutions and the additional Test 6 (3 MB base64 payload round-trip via fake daemon serving `image-tool-call`).

    Per CLAUDE.md immutability: handler code is purely functional — no shared state mutation between concurrent calls.

    Per CLAUDE.md "many small files": image package is its own subdirectory, parallel to search.
  </action>
  <verify>
    <automated>cd /home/jjagpal/.openclaw/workspace-coding && go test ./internal/shim/image/... -v && go build -o /tmp/clawcode-mcp-shim-test ./cmd/clawcode-mcp-shim</automated>
  </verify>
  <acceptance_criteria>
    - `test -f /home/jjagpal/.openclaw/workspace-coding/internal/shim/image/register.go` returns 0
    - `grep -E 'package image' /home/jjagpal/.openclaw/workspace-coding/internal/shim/image/register.go` returns a hit
    - `grep -E '"shimType":\s*"image"' /home/jjagpal/.openclaw/workspace-coding/internal/shim/image/register.go` returns a hit
    - `grep -E '"image-tool-call"' /home/jjagpal/.openclaw/workspace-coding/internal/shim/image/register.go` returns a hit
    - `! grep -E '"search-tool-call"|"shimType":\s*"search"' /home/jjagpal/.openclaw/workspace-coding/internal/shim/image/register.go` (no leftover search references)
    - `grep -E 'image\.Register\(server\)' /home/jjagpal/.openclaw/workspace-coding/cmd/clawcode-mcp-shim/main.go` returns a hit
    - `! grep -rE "fmt\.Println|os\.Stdout\.Write" /home/jjagpal/.openclaw/workspace-coding/internal/shim/image/`
    - `! grep -E "(retry|fallback.*node|MaxRetries)" /home/jjagpal/.openclaw/workspace-coding/internal/shim/image/register.go`
    - All 6 tests pass: `go test ./internal/shim/image/...` exits 0
    - Binary builds: `go build ./cmd/clawcode-mcp-shim` exits 0
    - Binary size still ≤ 12 MB
  </acceptance_criteria>
  <done>image Register implemented as parallel of search Register, wired into main.go, 6 tests pass including 3 MB payload regression for Pitfall §2.</done>
</task>

<task type="auto">
  <name>Task 2: Author image rollout log scaffolding</name>
  <read_first>
    - .planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-05-ROLLOUT-LOG.md (the search rollout log — image rollout mirrors this exactly except shim type)
    - .planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-05-SUMMARY.md (search rollout outcome — confirm GREEN before proceeding)
  </read_first>
  <files>.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-06-ROLLOUT-LOG.md</files>
  <action>
    Copy `.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-05-ROLLOUT-LOG.md` to `110-06-ROLLOUT-LOG.md` and substitute `search` → `image` throughout. Specifically:
    - Section 1 prereq: "search shim rollout (plan 110-05) MUST be GREEN before image starts" (NEW prereq)
    - Per-agent override: `agents.admin-clawdy.shimRuntime.image: static`
    - Global default flip: `defaults.shimRuntime.image: static`
    - Smoke-test tool: change from `web_search` to `image_generate`
    - measure-shim-rss.sh argument: change `search` to `image`
    - All other sections (24-48h watch, fail-loud reminder, rollback) preserved verbatim
  </action>
  <verify>
    <automated>test -f /home/jjagpal/.openclaw/workspace-coding/.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-06-ROLLOUT-LOG.md && grep -c "image" /home/jjagpal/.openclaw/workspace-coding/.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-06-ROLLOUT-LOG.md && ! grep -E "shimRuntime\.search:" /home/jjagpal/.openclaw/workspace-coding/.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-06-ROLLOUT-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f /home/jjagpal/.openclaw/workspace-coding/.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-06-ROLLOUT-LOG.md` returns 0
    - `grep -c "image" /home/jjagpal/.openclaw/workspace-coding/.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-06-ROLLOUT-LOG.md` returns ≥ 10 (image referenced throughout)
    - `! grep -E "shimRuntime\.search:|web_search" /home/jjagpal/.openclaw/workspace-coding/.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-06-ROLLOUT-LOG.md` (no leftover search references)
    - `grep -E "image_generate" /home/jjagpal/.openclaw/workspace-coding/.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-06-ROLLOUT-LOG.md` returns a hit
    - `grep -Ei "fail loud" /home/jjagpal/.openclaw/workspace-coding/.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-06-ROLLOUT-LOG.md` returns a hit
    - `grep -E "110-05" /home/jjagpal/.openclaw/workspace-coding/.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-06-ROLLOUT-LOG.md` returns a hit (search prereq referenced)
  </acceptance_criteria>
  <done>Image rollout log scaffolded as parallel of search rollout log; all section structure preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <action>Human-gated checkpoint. Claude prepares everything described in <what-built>; operator follows <how-to-verify> steps and responds per <resume-signal>. No autonomous code/file change in this task — the action IS the operator-driven verification recorded in the plan's referenced rollout/runbook log file.</action>
  <name>Task 3: GATE — Phase 1 admin-clawdy image canary flip + 24-48h watch</name>
  <what-built>
    image Register implemented + tested, wired into main.go, binary ≤ 12 MB; rollout log scaffolded.
  </what-built>
  <how-to-verify>
    Follow `.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-06-ROLLOUT-LOG.md` Phase 1 + watch sections:
    1. Verify search rollout (plan 110-05) is GREEN — image starts only after.
    2. Edit clawcode.yaml: `agents.admin-clawdy.shimRuntime.image: static`
    3. ConfigWatcher hot-reload — daemon PID unchanged, new image-shim spawns under `clawcode-mcp-shim --type image`.
    4. Smoke-test `image_generate` from admin-clawdy. Verify base64 image data returns through 16 MB buffer cleanly (Pitfall §2 regression check on real workload).
    5. Sample VmRSS 3 times across 30 minutes. Pass: ≤ 15 MB.
    6. Watch 24-48h: dashboard + journalctl + tool-call success rate.
    7. Decision: `green-canary-image` to advance, `red-rollback-image` to halt.
  </how-to-verify>
  <resume-signal>Type `green-canary-image` after 24-48h watch shows admin-clawdy stable on Go image shim, OR type `red-rollback-image` to halt.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <action>Human-gated checkpoint. Claude prepares everything described in <what-built>; operator follows <how-to-verify> steps and responds per <resume-signal>. No autonomous code/file change in this task — the action IS the operator-driven verification recorded in the plan's referenced rollout/runbook log file.</action>
  <name>Task 4: GATE — Phase 2 fleet rollout (image shim → all 11 agents)</name>
  <what-built>admin-clawdy stable on Go image shim for 24-48h.</what-built>
  <how-to-verify>
    Follow rollout log Phase 2:
    1. Edit clawcode.yaml: `defaults.shimRuntime.image: static`; remove admin-clawdy override.
    2. ConfigWatcher hot-reload; wait 5 min for cycle.
    3. Verify `pgrep -af 'clawcode image-mcp' | wc -l` = 0 and `pgrep -af 'clawcode-mcp-shim --type image' | wc -l` = 11.
    4. `scripts/integration/measure-shim-rss.sh image` → expect total < 100 MB.
    5. Smoke-test image_generate from 2-3 agents including fin-acquisition.
    6. /api/fleet-stats: ALL image shims report `runtime: "static"`.
    7. Decision: `image-fleet-green` to unblock plan 110-07 (browser), `image-fleet-rollback` to halt.
  </how-to-verify>
  <resume-signal>Type `image-fleet-green` or `image-fleet-rollback` with anomaly details.</resume-signal>
</task>

</tasks>

<verification>
- All Go tests pass; binary still ≤ 12 MB
- Image rollout log scaffolded
- Both gates produce explicit operator approval
- After Task 4 GREEN: aggregate image-shim RSS < 100 MB; all 11 agents on `runtime: "static"`
</verification>

<success_criteria>
- 0B-RT-01/04/05/06 (image-specific implementation), 0B-RT-07/08/10/11/12 (rollout-shaped) all addressed
- Image shim aggregate RSS reduced ~1.0 GiB → < 100 MB at full fleet
- Plan 110-07 (browser) unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-06-SUMMARY.md` documenting: image Register diff vs search Register (2-string substitution), rollout decisions, RSS measurements.
</output>
