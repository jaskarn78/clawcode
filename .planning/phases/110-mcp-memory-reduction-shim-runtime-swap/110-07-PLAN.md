---
phase: 110-mcp-memory-reduction-shim-runtime-swap
plan: 07
type: execute
wave: 5
depends_on: ["110-06"]
files_modified:
  - internal/shim/browser/register.go
  - internal/shim/browser/register_test.go
  - cmd/clawcode-mcp-shim/main.go
  - .planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-07-ROLLOUT-LOG.md
autonomous: false
requirements: [0B-RT-01, 0B-RT-04, 0B-RT-05, 0B-RT-06, 0B-RT-07, 0B-RT-08, 0B-RT-10, 0B-RT-11, 0B-RT-12]
must_haves:
  truths:
    - "`internal/shim/browser/register.go` mirrors search/image Register byte-for-byte EXCEPT: shimType `\"browser\"`, daemon IPC method `\"browser-tool-call\"`."
    - "`cmd/clawcode-mcp-shim/main.go` swaps the browser stub for `browser.Register(server)`."
    - "`browser_screenshot` tool returns ≥ 200 KB base64 PNG payloads correctly via the 16 MB IPC buffer (Pitfall §2 regression — most likely tool to expose buffer issues)."
    - "Two-phase rollout (admin-clawdy canary 24-48h → fleet) mirrors plans 110-05 and 110-06."
    - "After fleet rollout, aggregate browser-shim RSS is < 100 MB (vs ~1.0 GiB baseline)."
    - "Browser SESSION STATE (Playwright/Chrome) remains daemon-side and untouched — only the shim translator process is migrated. RED tier preserved."
    - "After this plan: Stage 0b deploy is COMPLETE — total Stage 0b savings ≥ 2.7 GiB (3 × ~900 MB per shim type)."
  artifacts:
    - path: "internal/shim/browser/register.go"
    - path: "cmd/clawcode-mcp-shim/main.go (browser branch)"
    - path: ".planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-07-ROLLOUT-LOG.md"
  key_links:
    - from: "internal/shim/browser/register.go"
      to: "internal/shim/ipc/client.go SendRequest()"
      via: "ipc.SendRequest(\"browser-tool-call\", ...)"
      pattern: "browser-tool-call"
---

<objective>
Wave 5 — browser-mcp migration. Lowest blast radius LAST, per CONTEXT.md rollout order. Browser is the highest-risk shim (screenshots = large payloads = Pitfall §2 most likely to surface; Playwright session state is RED tier and CANNOT regress).

**Key constraints from CONTEXT.md / RESEARCH.md:**
- Browser SESSION STATE (Playwright/Chrome lifecycle) stays daemon-side and untouched — only the shim translator process migrates.
- `browser_screenshot` tool is THE Pitfall §2 regression case (200 KB - 1 MB base64 inline payloads).
- After this plan: Stage 0b is complete; total fleet savings ≥ 2.7 GiB.

Output: browser shim runs as Go static binary on entire fleet; all three shim types now Go-based.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-CONTEXT.md
@.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-VALIDATION.md
@.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-04-SUMMARY.md
@.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-05-SUMMARY.md
@.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: browser Register — mirror search/image Register with browser-tool-call IPC method + screenshot regression test</name>
  <read_first>
    - internal/shim/search/register.go (canonical shape)
    - internal/shim/image/register.go (parallel implementation — confirm shape matches before mirroring)
    - src/cli/commands/browser-mcp.ts (existing Node browser shim — confirm daemon IPC method is `browser-tool-call`)
    - src/browser/mcp-server.ts (inner translator — list of tools including browser_screenshot)
    - .planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-RESEARCH.md (Common Pitfalls §2 — buffer size for screenshot payloads is THE regression case)
  </read_first>
  <files>internal/shim/browser/register.go, internal/shim/browser/register_test.go, cmd/clawcode-mcp-shim/main.go</files>
  <behavior>
    Mirror image/search Register tests 1-5 with browser substitutions. ADDITIONAL test:
    - Test 6 (CRITICAL): `browser_screenshot` tool's response payload — 1 MB base64 PNG — round-trips through the IPC buffer correctly. Fake daemon serves a `browser-tool-call` response containing `strings.Repeat("A", 1024*1024)` base64 string. Handler must return CallToolResult containing the full 1 MB without truncation. This is THE Pitfall §2 regression test against browser specifically. Failure mode: `bufio.Scanner: token too long` or JSON parse error indicating truncation.
  </behavior>
  <action>
    Create `internal/shim/browser/register.go` by copying `internal/shim/image/register.go` and substituting:
    - `package image` → `package browser`
    - `"shimType": "image"` → `"shimType": "browser"`
    - `"image-tool-call"` → `"browser-tool-call"`

    Update `cmd/clawcode-mcp-shim/main.go`:
    - Replace `case "browser":` stub with:
      ```go
      case "browser":
          if err := browser.Register(server); err != nil {
              logger.Error("register browser tools", "err", err, "serverType", *serverType)
              os.Exit(SHIM_EXIT_TEMPFAIL)
          }
      ```
    - Add `"github.com/jjagpal/clawcode-shim/internal/shim/browser"` import.
    - All three shim types (`search`, `image`, `browser`) are now wired with real Register calls. The `default:` branch remains the unknown-type error.

    Create `internal/shim/browser/register_test.go` mirroring image/search tests with browser substitutions and Test 6 (1 MB base64 round-trip).

    Per CLAUDE.md immutability + many-small-files: browser package is its own subdirectory; handlers are pure functions.
  </action>
  <verify>
    <automated>cd /home/jjagpal/.openclaw/workspace-coding && go test ./internal/shim/browser/... -v && go build -o /tmp/clawcode-mcp-shim-test ./cmd/clawcode-mcp-shim</automated>
  </verify>
  <acceptance_criteria>
    - `test -f /home/jjagpal/.openclaw/workspace-coding/internal/shim/browser/register.go` returns 0
    - `grep -E 'package browser' /home/jjagpal/.openclaw/workspace-coding/internal/shim/browser/register.go` returns a hit
    - `grep -E '"shimType":\s*"browser"' /home/jjagpal/.openclaw/workspace-coding/internal/shim/browser/register.go` returns a hit
    - `grep -E '"browser-tool-call"' /home/jjagpal/.openclaw/workspace-coding/internal/shim/browser/register.go` returns a hit
    - `! grep -E '"image-tool-call"|"search-tool-call"|"shimType":\s*"image"|"shimType":\s*"search"' /home/jjagpal/.openclaw/workspace-coding/internal/shim/browser/register.go` (no leftover refs)
    - `grep -E 'browser\.Register\(server\)' /home/jjagpal/.openclaw/workspace-coding/cmd/clawcode-mcp-shim/main.go` returns a hit
    - `! grep -rE "fmt\.Println|os\.Stdout\.Write" /home/jjagpal/.openclaw/workspace-coding/internal/shim/browser/`
    - `! grep -E "(retry|fallback.*node|MaxRetries)" /home/jjagpal/.openclaw/workspace-coding/internal/shim/browser/register.go`
    - All 6 tests pass: `go test ./internal/shim/browser/...` exits 0
    - Test 6 (1 MB payload) specifically verified by name: `go test ./internal/shim/browser/... -run "TestScreenshot.*Roundtrip"` exits 0 (screenshot regression case)
    - Binary builds clean; size ≤ 12 MB
    - `go test ./...` (all Go tests across all shim types) exits 0 — no regression to search/image
  </acceptance_criteria>
  <done>browser Register implemented as parallel of search/image, wired into main.go, 6 tests pass including the 1 MB screenshot regression for Pitfall §2.</done>
</task>

<task type="auto">
  <name>Task 2: Author browser rollout log scaffolding</name>
  <read_first>
    - .planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-05-ROLLOUT-LOG.md
    - .planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-06-ROLLOUT-LOG.md
    - .planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-06-SUMMARY.md (image rollout outcome — confirm GREEN before browser starts)
  </read_first>
  <files>.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-07-ROLLOUT-LOG.md</files>
  <action>
    Copy `110-06-ROLLOUT-LOG.md` to `110-07-ROLLOUT-LOG.md` and substitute `image` → `browser` throughout:
    - Section 1 prereq: "image shim rollout (plan 110-06) MUST be GREEN before browser starts"
    - Per-agent override: `agents.admin-clawdy.shimRuntime.browser: static`
    - Global default: `defaults.shimRuntime.browser: static`
    - Smoke-test tool: `browser_screenshot` (specifically — most likely to expose Pitfall §2 in production)
    - measure-shim-rss.sh argument: `browser`
    - Add NEW prereq verification: "Playwright session state preserved — verify a complex browser session (open page, navigate, take screenshot) round-trips identically pre- vs post-flip on admin-clawdy."

    Add an explicit RED-tier reminder near top: *"Browser SESSION STATE (Playwright/Chrome lifecycle) is RED tier and stays daemon-side. This rollout migrates ONLY the IPC translator shim. If session state regresses, it's a bug in the migration (rollback immediately) — session state is OUT of scope for any change."*
  </action>
  <verify>
    <automated>test -f /home/jjagpal/.openclaw/workspace-coding/.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-07-ROLLOUT-LOG.md && grep -c "browser" /home/jjagpal/.openclaw/workspace-coding/.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-07-ROLLOUT-LOG.md && grep -E "browser_screenshot" /home/jjagpal/.openclaw/workspace-coding/.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-07-ROLLOUT-LOG.md && grep -Ei "RED tier|session state" /home/jjagpal/.openclaw/workspace-coding/.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-07-ROLLOUT-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f /home/jjagpal/.openclaw/workspace-coding/.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-07-ROLLOUT-LOG.md` returns 0
    - `grep -c "browser" /home/jjagpal/.openclaw/workspace-coding/.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-07-ROLLOUT-LOG.md` returns ≥ 10
    - `grep -E "browser_screenshot" /home/jjagpal/.openclaw/workspace-coding/.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-07-ROLLOUT-LOG.md` returns a hit
    - `grep -Ei "RED tier|session state" /home/jjagpal/.openclaw/workspace-coding/.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-07-ROLLOUT-LOG.md` returns at least 1 hit (Playwright session-state warning present)
    - `grep -E "110-06" /home/jjagpal/.openclaw/workspace-coding/.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-07-ROLLOUT-LOG.md` returns a hit (image prereq referenced)
    - `! grep -E "shimRuntime\.image:|image_generate" /home/jjagpal/.openclaw/workspace-coding/.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-07-ROLLOUT-LOG.md` (no leftover image refs)
    - `grep -Ei "fail loud" /home/jjagpal/.openclaw/workspace-coding/.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-07-ROLLOUT-LOG.md` returns a hit
  </acceptance_criteria>
  <done>Browser rollout log scaffolded; RED-tier session-state warning present; image prereq referenced.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <action>Human-gated checkpoint. Claude prepares everything described in <what-built>; operator follows <how-to-verify> steps and responds per <resume-signal>. No autonomous code/file change in this task — the action IS the operator-driven verification recorded in the plan's referenced rollout/runbook log file.</action>
  <name>Task 3: GATE — Phase 1 admin-clawdy browser canary flip + 24-48h watch (CRITICAL Pitfall §2 regression window)</name>
  <what-built>browser Register implemented + tested with 1 MB screenshot regression, wired into main.go, rollout log scaffolded.</what-built>
  <how-to-verify>
    Follow `110-07-ROLLOUT-LOG.md` Phase 1 + watch:
    1. Verify image rollout (110-06) is GREEN.
    2. Edit clawcode.yaml: `agents.admin-clawdy.shimRuntime.browser: static`
    3. ConfigWatcher hot-reload — daemon PID unchanged, new browser-shim spawns under `clawcode-mcp-shim --type browser`.
    4. Smoke-test sequence:
       a. `browser_navigate` to a page admin-clawdy uses regularly
       b. `browser_screenshot` — verify the screenshot returns intact (image renders correctly when posted to Discord). This is THE production-traffic Pitfall §2 check.
       c. `browser_extract` to pull text from the page
       d. Repeat with a heavy page (large screenshot — find one that produces ≥ 500 KB base64)
    5. Sample VmRSS 3 times across 30 minutes. Pass: ≤ 15 MB.
    6. Watch 24-48h: dashboard + journalctl (specifically grep for `token too long` or buffer-related errors) + tool-call success rate including screenshot-heavy turns.
    7. Decision: `green-canary-browser` to advance, `red-rollback-browser` to halt.

    SPECIAL VIGILANCE: if Pitfall §2 manifests in production (truncation), it appears as `Error: invalid character ',' looking for beginning of value` in journalctl. This is THE failure case the 1 MB regression test guards against — but production payloads can be larger. If observed, immediate rollback + investigate buffer size.
  </how-to-verify>
  <resume-signal>Type `green-canary-browser` after 24-48h watch shows admin-clawdy stable on Go browser shim including screenshot-heavy turns, OR type `red-rollback-browser` with anomaly details (especially Pitfall §2 truncation evidence).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <action>Human-gated checkpoint. Claude prepares everything described in <what-built>; operator follows <how-to-verify> steps and responds per <resume-signal>. No autonomous code/file change in this task — the action IS the operator-driven verification recorded in the plan's referenced rollout/runbook log file.</action>
  <name>Task 4: GATE — Phase 2 fleet rollout (browser shim → all 11 agents) — Stage 0b COMPLETE</name>
  <what-built>admin-clawdy stable on Go browser shim 24-48h including screenshot-heavy turns.</what-built>
  <how-to-verify>
    Follow rollout log Phase 2:
    1. Edit clawcode.yaml: `defaults.shimRuntime.browser: static`; remove admin-clawdy override.
    2. ConfigWatcher hot-reload; wait 5 min for cycle.
    3. Verify `pgrep -af 'clawcode browser-mcp' | wc -l` = 0 and `pgrep -af 'clawcode-mcp-shim --type browser' | wc -l` = 11.
    4. `scripts/integration/measure-shim-rss.sh browser` → expect total < 100 MB.
    5. `scripts/integration/measure-shim-rss.sh all` → expect total < 300 MB across all three types (vs ~3.0-3.2 GiB baseline) — Stage 0b CUMULATIVE win.
    6. Smoke-test browser_screenshot from 2-3 agents including fin-acquisition (which uses browser screenshots heavily).
    7. /api/fleet-stats: ALL browser shims report `runtime: "static"`; aggregate Stage 0b RSS reduction confirmed.
    8. **Final Stage 0b summary table**:
       | Shim type | Pre RSS | Post RSS | Savings |
       | search    | ~1.0 GiB | < 100 MB | > 900 MB |
       | image     | ~1.0 GiB | < 100 MB | > 900 MB |
       | browser   | ~1.0 GiB | < 100 MB | > 900 MB |
       | **TOTAL** | **~3.0 GiB** | **< 300 MB** | **> 2.7 GiB** |
    9. Cgroup memory pressure should drop dramatically — `/api/fleet-stats` cgroupMemory.usagePct should be ~30-40% (vs 97.8% pre-Stage-0b).
    10. Decision: `stage-0b-complete` (Stage 0b ships; plan 110-08 cleanup unblocked) or `browser-fleet-rollback`.
  </how-to-verify>
  <resume-signal>Type `stage-0b-complete` after all 11 agents on Go browser shim with verified aggregate RSS savings ≥ 2.7 GiB and clean smoke tests, OR type `browser-fleet-rollback` with anomaly details.</resume-signal>
</task>

</tasks>

<verification>
- All Go tests pass; binary still ≤ 12 MB
- Browser rollout log scaffolded with RED-tier session-state warning
- Both gates produce explicit operator approval
- After Task 4 GREEN: aggregate Stage 0b RSS savings ≥ 2.7 GiB measured on clawdy
</verification>

<success_criteria>
- 0B-RT-01/04/05/06 (browser-specific implementation), 0B-RT-07/08/10/11/12 (rollout-shaped) all addressed
- All three shim types (search/image/browser) on Go static runtime — Stage 0b structural deploy COMPLETE
- Plan 110-08 (cleanup) unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/110-mcp-memory-reduction-shim-runtime-swap/110-07-SUMMARY.md` documenting: browser Register diff vs image Register, Pitfall §2 production observations (or absence thereof), final Stage 0b cumulative savings table, cgroup memory pressure delta.
</output>
