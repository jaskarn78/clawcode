---
phase: 999.8-dashboard-graph-node-cap-tier-colors-tier-maintenance
verified: 2026-04-30T02:00:00Z
status: passed
score: 14/14 must-haves verified
re_verification: false
---

# Phase 999.8: Dashboard graph node cap + tier colors + heartbeat discovery Verification Report

**Phase Goal:** 3 bundled issues — (1) lift hardcoded 500-node cap on memory-graph IPC to configurable 5000, (2) distinct tier colors + legend on dashboard graph, (3) restore heartbeat-check discovery via static registry (fixes silent production failure where all 11 checks logged checkCount:0).
**Verified:** 2026-04-30T02:00:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | memory-graph IPC accepts optional `limit` param without breaking existing callers | VERIFIED | `handleMemoryGraphIpc` in `src/manager/memory-graph-handler.ts` uses `typeof params.limit === "number" ? params.limit : 5000`; daemon case delegates to it; server.ts sends only `{agent}` — zero collision |
| 2 | Default cap is 5000 (raised from 500); `LIMIT 500` is gone | VERIFIED | `grep -c "LIMIT 500" daemon.ts` = 0; `MEMORY_GRAPH_DEFAULT_LIMIT = 5000` exported at line 31 of handler; `LIMIT ?` parameterized binding confirmed |
| 3 | Out-of-range limits (< 1, > 50000, non-integer) reject with ManagerError | VERIFIED | `resolveLimit()` uses `Number.isInteger` + range guard; error message `"must be integer in [1, 50000]"` at handler line 95; 10/10 IPC tests green |
| 4 | Dashboard `/api/graph/:agent` route unchanged (picks up new default) | VERIFIED | `grep -n "limit" src/dashboard/server.ts` returns no un-commented hits; git diff confirms byte-identical to baseline |
| 5 | 4 distinct tier colors: hot=#e06c75, warm=#7f6df2, cold=#5a8db8, orphan=#444 | VERIFIED | `src/dashboard/static/graph-color.js` exports `nodeClr` with exact switch/fallback; `#5a8db8` confirmed in graph.html legend swatch; 9/9 color tests green |
| 6 | Top-right legend with 4 legend-row blocks, color swatches, live count ids | VERIFIED | `graph.html` lines 402-410 contain `.graph-legend`, `<details open>`, 4 `data-tier` rows (hot/warm/cold/orphan), ids ct-hot/ct-warm/ct-cold/ct-orphan; 6/6 legend tests green |
| 7 | Live legend counts recompute every `render(data)` call | VERIFIED | counts loop at graph.html lines 558-567 runs after linkCount assignment inside `render(data)` |
| 8 | `discoverChecks(any)` returns static CHECK_REGISTRY of exactly 11 modules | VERIFIED | `discovery.ts` is a one-line shim returning `CHECK_REGISTRY`; 3/3 discovery tests green |
| 9 | check-registry.ts has 11 static imports + Object.freeze; no try/catch | VERIFIED | `grep -c '^import .* from "./checks/'` = 11; `Object.freeze` present; no `try {` in file |
| 10 | Daemon boot log emits checkCount:11 + checks array + "heartbeat checks registered" | VERIFIED | `runner.ts` lines 98-104: `checks: this.checks.map((c) => c.name)` + `"heartbeat checks registered"`; HB-04 test green |
| 11 | inbox.ts retains named export `setInboxSourceActive` (daemon.ts:2120 safety) | VERIFIED | `grep setInboxSourceActive src/heartbeat/checks/inbox.ts` = line 26; inbox-named-export.test.ts 2/2 green; no check files modified |
| 12 | Lockstep regression test detects future registry/disk drift | VERIFIED | check-registry.test.ts 5 tests; drift detector uses `readdirSync` bidirectionally; disk and registry match (11 files, 11 registry entries) |
| 13 | Production bundle contains tier-maintenance (was 0 hits before Phase 03) | VERIFIED | `grep -c "tier-maintenance" dist/cli/index.js` = 3; bundle timestamp 2026-04-30T01:17 post-phase |
| 14 | Unrelated quick fix ea17390 (restart-greeting) has no impact on phase goal | VERIFIED | Only touches `src/manager/restart-greeting.ts` + its test file; zero overlap with any phase 999.8 artifact |

**Score:** 14/14 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/manager/memory-graph-handler.ts` | Pure handler with LIMIT ? and [1,50000] validation | VERIFIED | 160 lines; exports `handleMemoryGraphIpc`, `MEMORY_GRAPH_DEFAULT_LIMIT=5000`, `MEMORY_GRAPH_MIN_LIMIT=1`, `MEMORY_GRAPH_MAX_LIMIT=50000` |
| `src/manager/__tests__/memory-graph-ipc.test.ts` | 10+ tests covering CAP-01..CAP-03 | VERIFIED | 10 `it()` blocks; 213 lines; all 10 pass |
| `src/manager/daemon.ts` | memory-graph case dispatches to handler (LIMIT 500 gone) | VERIFIED | Case at line 5915 is a 3-line dispatch; `LIMIT 500` = 0 grep hits |
| `src/dashboard/static/graph-color.js` | Exports `nodeClr` with 4-color palette | VERIFIED | 22 lines; single `export function nodeClr`; correct hex values |
| `src/dashboard/static/graph.html` | `<script type="module">`, legend overlay, counts loop | VERIFIED | Script tag at line 429 is `type="module"`; import at line 431; legend at lines 402-410; counts loop lines 558-567 |
| `src/dashboard/__tests__/graph-color.test.ts` | 9 unit tests for COLOR-01 | VERIFIED | 9/9 pass |
| `src/dashboard/__tests__/graph-legend.test.ts` | 6 static-HTML parse tests for COLOR-02 | VERIFIED | 6/6 pass |
| `src/heartbeat/check-registry.ts` | 11 static imports + Object.freeze CHECK_REGISTRY | VERIFIED | 53 lines; 11 imports; frozen readonly array; no try/catch |
| `src/heartbeat/discovery.ts` | Shim returning CHECK_REGISTRY, signature preserved | VERIFIED | 22 lines; `async function discoverChecks(_checksDir: string): Promise<readonly CheckModule[]>` returns `CHECK_REGISTRY` |
| `src/heartbeat/runner.ts` | Boot log includes checks array + "heartbeat checks registered" | VERIFIED | lines 98-104; `checks: this.checks.map((c) => c.name)`; message changed from "discovered" |
| `src/heartbeat/__tests__/check-registry.test.ts` | 5 lockstep tests (HB-01, HB-05, HB-06) | VERIFIED | 5/5 pass |
| `src/heartbeat/__tests__/inbox-named-export.test.ts` | 2 regression tests for HB-03 | VERIFIED | 2/2 pass |
| `src/heartbeat/__tests__/discovery.test.ts` | 3 tests for static shim contract (HB-02) | VERIFIED | 3/3 pass |
| `src/heartbeat/__tests__/runner.test.ts` | HB-04 boot log shape test added | VERIFIED | test at line 625 asserts `checkCount: 11`, `checks: expect.arrayContaining([...])`, `"heartbeat checks registered"` |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `daemon.ts case "memory-graph"` | `memory-graph-handler.ts` | `handleMemoryGraphIpc(params, store.getDatabase())` | WIRED | One-line dispatch at daemon.ts:5925 |
| `memory-graph-handler.ts resolveLimit()` | `ManagerError` | `throw new ManagerError(...)` | WIRED | Line 94-96 in handler |
| `memory-graph-handler.ts SQL` | `better-sqlite3 LIMIT ?` | parameterized `.all(limit)` | WIRED | Line 127 in handler |
| `graph.html <script type="module">` | `graph-color.js` | `import { nodeClr } from "./graph-color.js"` | WIRED | graph.html line 431 |
| `graph.html render(data)` | DOM `#ct-hot/#ct-warm/#ct-cold/#ct-orphan` | counts loop after linkCount assignment | WIRED | graph.html lines 558-567 |
| `check-registry.ts` | `src/heartbeat/checks/*.ts` (all 11) | 11 static `import ... from "./checks/...js"` | WIRED | All 11 default-export imports present; disk/registry lockstep confirmed |
| `discovery.ts` | `check-registry.ts` | `import { CHECK_REGISTRY } from "./check-registry.js"` | WIRED | discovery.ts line 2 |
| `runner.ts initialize()` | logger.info with checks array | `checks: this.checks.map(c => c.name)` | WIRED | runner.ts line 101 |
| `daemon.ts:2120` | `inbox.ts setInboxSourceActive` | dynamic import — named export preserved | WIRED | `setInboxSourceActive` at inbox.ts:26; test guards it |

---

### Behavioral Spot-Checks (Step 7b)

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All 7 phase test files green (48 tests) | `npx vitest run [7 test files]` | 7 passed, 48 passed | PASS |
| TS error count unchanged from baseline | `npx tsc --noEmit \| grep -c "error TS"` | 109 (matches pre-existing baseline) | PASS |
| tier-maintenance in production bundle | `grep -c "tier-maintenance" dist/cli/index.js` | 3 | PASS |
| LIMIT 500 eliminated from daemon.ts | `grep -c "LIMIT 500" src/manager/daemon.ts` | 0 | PASS |
| nodeClr inline definition removed from graph.html | `grep -c "function nodeClr" graph.html` | 0 | PASS |
| check-registry has exactly 11 check imports | `grep -c '^import .* from "./checks/' check-registry.ts` | 11 | PASS |
| Disk check files match registry 1-to-1 | Manual sort comparison | Identical 11-item sorted lists | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| CAP-01 | Plan 01 | `memory-graph` IPC accepts optional `limit: number` param | SATISFIED | `resolveLimit(params.limit)` in handler; 10 tests exercise the contract |
| CAP-02 | Plan 01 | Default cap is 5000 (raised from 500) | SATISFIED | `MEMORY_GRAPH_DEFAULT_LIMIT = 5000`; LIMIT 500 grep = 0 |
| CAP-03 | Plan 01 | `limit` clamped to [1, 50000]; out-of-range throws ManagerError | SATISFIED | `Number.isInteger` + range guard + `"must be integer in [1, 50000]"` |
| CAP-04 | Plan 01 | Dashboard frontend unchanged (picks up new default) | SATISFIED | server.ts has zero `limit` references; unchanged by git diff |
| COLOR-01 | Plan 02 | `nodeClr` returns 4 distinct colors keyed on tier+linkCount | SATISFIED | graph-color.js switch with hot/cold/warm/default + orphan-dominates; 9 unit tests |
| COLOR-02 | Plan 02 | Legend renders 4 tier swatches + labels + live counts in top-right | SATISFIED | graph.html legend at lines 402-410; CSS `position:absolute; top:12px; right:12px`; counts at lines 558-567 |
| HB-01 | Plan 03 | `check-registry.ts` exports `CHECK_REGISTRY: readonly CheckModule[]` with 11 static imports | SATISFIED | 11 imports + `Object.freeze` + `readonly CheckModule[]` |
| HB-02 | Plan 03 | `discoverChecks(checksDir)` signature preserved, returns CHECK_REGISTRY | SATISFIED | Async signature unchanged; one-line body; 3 tests confirm referential equality |
| HB-03 | Plan 03 | `inbox.ts` retains named export `setInboxSourceActive` | SATISFIED | Line 26 confirmed; no check files modified; guard test green |
| HB-04 | Plan 03 | Daemon boot log includes `checkCount + checks array + "heartbeat checks registered"` | SATISFIED | runner.ts lines 98-104; HB-04 test at runner.test.ts:625 |
| HB-05 | Plan 03 | Lockstep regression test detects disk/registry drift | SATISFIED | check-registry.test.ts drift detector (Test 4); bidirectional using `readdirSync` |
| HB-06 | Plan 03 | `auto-linker` retained in registry | SATISFIED | auto-linker.ts on disk and in registry import list |

---

### Anti-Patterns Scan

No blockers found. Full scan of phase-modified files:

- `src/manager/memory-graph-handler.ts` — no TODOs, no hardcoded empty returns, `LIMIT ?` parameterized correctly, no string interpolation in SQL
- `src/heartbeat/check-registry.ts` — no `try/catch`, no `try {` blocks, `Object.freeze` + `readonly` (coding-style.md immutability rule followed), no placeholders
- `src/heartbeat/discovery.ts` — `readdirSync` and dynamic `import()` fully removed (0 grep hits); `_checksDir` correctly underscored for `noUnusedParameters`
- `src/heartbeat/runner.ts` — log message changed from `"discovered"` to `"registered"` (clean grep marker for operators)
- `src/dashboard/static/graph-color.js` — pure function with no side effects; correct orphan-dominates ordering
- `src/dashboard/static/graph.html` — inline `nodeClr` removed (0 hits), legend not inside `<svg>` (pitfall avoided), `<details>` idiom used (consistent with Forces panel)

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | — | — | — | — |

---

### Human Verification Required

The following items cannot be verified programmatically:

#### 1. Visual legend appearance in browser

**Test:** Open the dashboard, click an agent with memories, observe the top-right corner
**Expected:** Collapsible legend with 4 colored circles (red, purple, muted blue, grey) + labels (Hot, Warm, Cold, Orphan) + live counts; legend collapses/expands with click on "Tiers" summary
**Why human:** CSS rendering and visual legibility cannot be verified from source text; `pointer-events` behavior and zoom/drag non-interference need browser interaction

#### 2. Post-deploy heartbeat activation (production gate)

**Test:** After `npm run build && systemctl restart clawcode`, run `journalctl -u clawcode -n 50 | grep "heartbeat checks registered"`
**Expected:** `"checkCount":11,"checks":["attachment-cleanup","auto-linker",...],"msg":"heartbeat checks registered"` — 11 checks, not 0
**Why human:** Requires production deploy authorization (D-PLN-03 explicitly defers this to operator decision); cannot test without restarting the live daemon

---

### Isolation Verification — Quick Fix ea17390

Commit `ea17390` (`fix(restart-greeting): bypass Haiku summary on API-error-dominated sessions`) modifies only:
- `src/manager/restart-greeting.ts`
- `src/manager/__tests__/restart-greeting.test.ts`

No overlap with phase 999.8 artifacts (memory-graph-handler.ts, graph.html, graph-color.js, check-registry.ts, discovery.ts, runner.ts, or any heartbeat/dashboard tests). This commit does not impact phase goal achievement.

---

## Summary

All 3 bundled issues are fully implemented and verified:

1. **Cap lift (CAP-01..04):** `LIMIT 500` eliminated from daemon.ts; pure handler extracted with validated default-5000 cap; dashboard frontend picks up new default without changes. 10/10 IPC tests green.

2. **Tier colors + legend (COLOR-01..02):** `nodeClr` extracted to `graph-color.js` with 4-color palette (hot red, warm purple, cold blue NEW, orphan grey); graph.html converted to `<script type="module">` to import it; top-right collapsible legend with live tier counts recomputed each `render()`. 15/15 color+legend tests green.

3. **Heartbeat discovery restoration (HB-01..06):** Dynamic `readdir + import()` scan (which silently returned 0 in production due to tsup's `splitting:false`) replaced with a static import registry. All 11 checks are now compile-time visible to tsup and appear in `dist/cli/index.js` (tier-maintenance: 3 hits, was 0). Lockstep drift detector prevents future silent omissions. 13/13 heartbeat test files green, 111+ heartbeat tests pass.

**TS baseline:** 109 pre-existing errors — zero new errors introduced by phase work.

**Production deploy** (systemctl restart + journalctl verification) is deferred per D-PLN-03 pending operator authorization. The bundle is rebuild-ready.

---

_Verified: 2026-04-30T02:00:00Z_
_Verifier: Claude (gsd-verifier)_
