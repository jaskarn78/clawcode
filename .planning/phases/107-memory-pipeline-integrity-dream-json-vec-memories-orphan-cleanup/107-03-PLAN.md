---
phase: 107
plan: 03
type: execute
wave: 2
depends_on: ["107-01", "107-02"]
files_modified: []
autonomous: true
requirements:
  - DREAM-OUT-01
  - DREAM-OUT-03
  - DREAM-OUT-04
  - VEC-CLEAN-01
  - VEC-CLEAN-02
  - VEC-CLEAN-03
  - VEC-CLEAN-04

must_haves:
  truths:
    - "Full `npm test` suite is GREEN before any deploy attempt"
    - "Build (`npm run build`) succeeds locally before deploy"
    - "Deploy ONLY proceeds when all bound channels have been silent ≥30 min on non-bot `messageCreate` events"
    - "If gate is NOT satisfied within 6 hours of polling (5-min poll cadence), NO deploy occurs and outcome is recorded as held in SUMMARY"
    - "ssh deploy on clawdy uses rsync of `dist/` + `systemctl restart clawcode` (matches Phase 106 deploy idiom; per project convention `git pull && npm ci && npm run build && systemctl restart` is also acceptable if dist isn't shipped)"
    - "Post-deploy smoke: synthetic dream pass returning prose emits `parse-failed` warn line with `responsePreview`/`responsePrefix` field, NO error-level log, daemon does not crash"
    - "Post-deploy smoke: manual memory delete via IPC → `vec_memories` query for that rowid returns 0 rows (cascade verified)"
    - "Post-deploy smoke: `clawcode memory cleanup-orphans` runs cleanly, reports orphan count (likely > 0 from historical migration drops on production DBs)"
    - "SUMMARY records deploy timestamp, smoke results, orphan count cleaned per agent"
  artifacts:
    - path: ".planning/phases/107-memory-pipeline-integrity-dream-json-vec-memories-orphan-cleanup/107-03-SUMMARY.md"
      provides: "Deploy result: deployed (with timestamp + smoke results + orphan counts) OR held (with reason + last gate-check timestamp)"
      contains: "deploy"
  key_links:
    - from: "deploy gate (local)"
      to: "remote clawdy journalctl"
      via: "ssh jjagpal@100.98.211.108 'journalctl -u clawcode --since \"30 min ago\" --no-pager | grep messageCreate | grep \"\\\"bot\\\":false\" | wc -l'"
      pattern: "journalctl.*messageCreate.*bot.*false"
    - from: "smoke (dream parse-failed)"
      to: "Plan 107-01 DREAM-OUT-03 warn-level structured log"
      via: "Synthetic prose dispatch → grep journalctl for `component=dream-pass` `action=parse-failed` `responsePrefix` (≤80 chars). Daemon must NOT have crashed (systemctl status clawcode = active)"
      pattern: "parse-failed"
    - from: "smoke (vec_memories cascade)"
      to: "Plan 107-02 MemoryStore.delete + cleanupOrphans"
      via: "sqlite3 query on agent memories.db post-IPC-delete: `SELECT COUNT(*) FROM vec_memories WHERE memory_id = ?` returns 0"
      pattern: "SELECT COUNT.*vec_memories"
    - from: "smoke (cleanupOrphans CLI)"
      to: "Plan 107-02 CLI subcommand"
      via: "ssh clawdy 'sudo -u clawcode node /opt/clawcode/dist/cli/index.js memory cleanup-orphans' — output captured into SUMMARY"
      pattern: "memory cleanup-orphans"
---

<objective>
Wave 2 — final ship. Verify full test suite + build green, wait on the deploy gate (all bound channels silent ≥30 min on non-bot `messageCreate` events), ssh-deploy to clawdy (`/opt/clawcode`), restart systemd unit, run post-deploy smoke covering both pillars (DREAM-OUT-03 warn-level recovery + VEC-CLEAN-03 cascade + cleanup CLI), record outcome in SUMMARY.

Purpose: Phase 107 is two small daemon-side fixes. Bundled deploy is cheaper than two. The deploy gate is the operator's safety net — channels stay quiet during deploy because hot rollovers are disruptive when agents are actively serving Ramy / Manni / operator. Post-deploy smoke proves both pillars work in production: dream pass survives prose without crashing, memory deletes cascade, operator has the cleanup CLI for historical orphan recovery.

Output: Production deployment of plans 107-01 + 107-02 on clawdy, smoke results documented, historical orphan count captured. OR a held-deploy SUMMARY if channels stay busy through the polling window.

**Critical:** This plan runs autonomously. The deploy gate is real — if channels are active, plan WAITS (5-min poll cadence, max 6 hours). If the gate never opens before cap-hit, ship is held and reported. NO deploy under disruptive conditions. Per `feedback_no_auto_deploy` global rule: deploy proceeds only on satisfied gate (autonomous overnight rule) — during operator-active hours, surface to operator for explicit deploy phrase before shipping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-memory-pipeline-integrity-dream-json-vec-memories-orphan-cleanup/107-CONTEXT.md
@.planning/phases/107-memory-pipeline-integrity-dream-json-vec-memories-orphan-cleanup/107-RESEARCH.md
@.planning/phases/107-memory-pipeline-integrity-dream-json-vec-memories-orphan-cleanup/107-01-PLAN.md
@.planning/phases/107-memory-pipeline-integrity-dream-json-vec-memories-orphan-cleanup/107-02-PLAN.md

<interfaces>
Deploy gate criterion (per Phase 106 + CONTEXT.md non-negotiables):

```
ssh jjagpal@100.98.211.108 "journalctl -u clawcode --since '30 min ago' --no-pager | grep messageCreate | grep '\"bot\":false' | wc -l"
```
- Returns `0` → channels silent → deploy.
- Returns >0 → channels active → wait 5 min, recheck. Loop with overall cap of 6 hours from gate-attempt-start.

Server topology (per `reference_clawcode_server.md`):
- Host: clawdy (Tailscale IP 100.98.211.108)
- User: jjagpal (ssh) → sudo -u clawcode for service operations
- Install: /opt/clawcode (git checkout)
- Service: systemd unit `clawcode` (`systemctl restart clawcode`)
- Agent memory DBs: `~clawcode/.clawcode/agents/<agent>/memory/memories.db`

Deploy idiom — match what Phase 106 used (preferred: rsync dist/) OR fall back to git pull + npm ci + npm run build + systemctl restart if rsync isn't viable. Look at the most recent deploy SUMMARY in `.planning/phases/106-*/106-04-SUMMARY.md` (or whichever deploy plan was most recent) to confirm exact commands.

Local pre-deploy gauntlet:
- `npm test` — full suite green
- `npm run typecheck` — clean
- `npm run build` — produces dist/

Post-deploy smoke commands (executed via ssh):

1. DREAM smoke — synthetic prose response triggering DREAM-OUT-03 warn path:
   - Easiest: wait for natural dream cron OR force a dream pass on test-agent. If no IPC trigger exists, observe natural logs over a 30-60 min window post-deploy.
   - Verify: `ssh clawdy 'journalctl -u clawcode --since "30 min ago" --no-pager | grep -E "dream-pass|parse-failed"'`
   - Pass criterion: if a `parse-failed` line appears, it MUST be at warn level (level: 40 in pino JSON), MUST contain `component`, `action`, `responsePrefix`, `agent` fields, and `systemctl status clawcode` MUST show `active (running)`. If NO parse-failed line in window: cleanly green (model behaved). Pass either way.

2. VEC cascade smoke — verify a real delete cascades:
   - Pick a test-agent memory id (small, throwaway). Use existing IPC method `memory-delete` if available, OR write a tiny memory entry first then delete it.
   - Query post-delete: `ssh clawdy 'sudo -u clawcode sqlite3 ~clawcode/.clawcode/agents/test-agent/memory/memories.db "SELECT COUNT(*) FROM vec_memories WHERE memory_id = X;"'` — expect 0.
   - If no IPC delete method exists today (per RESEARCH.md, MemoryStore.delete is the only delete path — check daemon for an IPC method exposing it), this smoke can be replaced by inspecting an existing recent delete from journalctl + querying for its memory_id.

3. cleanupOrphans CLI smoke — verify the new subcommand runs:
   - `ssh clawdy 'sudo -u clawcode node /opt/clawcode/dist/cli/index.js memory cleanup-orphans'`
   - Expected output (per Plan 107-02 CLI format): per-agent line, e.g. `test-agent: removed N orphans (M vec_memories remaining)` OR `test-agent: no orphans (M total)`. Likely N>0 for one or more agents (historical migration drops per RESEARCH.md pitfall 4).
   - Capture full output for SUMMARY.

Channel-activity gate channels (per Phase 106 reference):
- 1481670479017414767 (fin-acquisition / #finmentum-client-acquisition) — Ramy
- 1486348188763029648 (finmentum-content-creator) — operator content workflow
- 1494117043367186474 (Admin Clawdy) — operator orchestration
- The journalctl grep covers ALL bound channels — no per-channel scoping needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-deploy gauntlet — full test suite + typecheck + build green</name>
  <files>(none — read-only validation)</files>
  <action>
    Run the full validation gauntlet locally. ABORT (record failure in SUMMARY, do NOT deploy) if any of these fail.

    Step 1: Full test suite
    ```
    npm test
    ```
    Expect: ALL tests pass (Phase 107-01 + 107-02 new tests + all existing). If any fail: write FAILED gauntlet to SUMMARY with the failing test names + counts. Exit. Do NOT proceed to gate poll.

    Step 2: Typecheck
    ```
    npm run typecheck
    ```
    Expect: 0 errors. If errors: write FAILED gauntlet to SUMMARY. Exit.

    Step 3: Build
    ```
    npm run build
    ```
    Expect: tsup completes, `dist/` is regenerated, `dist/cli/index.js` exists, `dist/dashboard/static/` populated by copy-assets script. If build errors: write FAILED gauntlet to SUMMARY. Exit.

    Step 4: Confirm dist/ artifacts include the changed surfaces:
    ```
    grep -l "memory-cleanup-orphans" dist/ -r
    grep -l "parse-failed" dist/ -r
    ```
    Expect: at least one match per pattern (the bundled output should contain both Phase 107 surfaces).

    All four steps GREEN → proceed to Task 2 (deploy gate poll). Any failure → write SUMMARY with `outcome: aborted-pre-deploy`, list which step failed and why. STOP. Do NOT touch clawdy.
  </action>
  <verify>
    <automated>npm test &amp;&amp; npm run typecheck &amp;&amp; npm run build &amp;&amp; grep -q "memory-cleanup-orphans" dist/cli/index.js 2>/dev/null &amp;&amp; grep -q "parse-failed" dist/manager/dream-pass.js 2>/dev/null</automated>
  </verify>
  <done>
    All four gauntlet steps GREEN. `dist/` regenerated with both Phase 107 surfaces present. Ready for deploy gate poll.
  </done>
</task>

<task type="auto">
  <name>Task 2: Deploy gate — channel-silence poll (max 6 hours, 5-min cadence)</name>
  <files>(none — gate poll only)</files>
  <action>
    Poll the deploy gate. Continue ONLY when all bound channels have been silent ≥30 min on non-bot `messageCreate` events.

    Gate command:
    ```
    ssh jjagpal@100.98.211.108 "journalctl -u clawcode --since '30 min ago' --no-pager | grep messageCreate | grep '\"bot\":false' | wc -l"
    ```

    Polling protocol:
    - Record `gate_start_at = now()`.
    - Loop:
      - Run gate command. Capture output as integer N.
      - If N == 0: gate satisfied. Record `gate_satisfied_at = now()`. Break loop. Proceed to Task 3.
      - If N > 0: log `[gate] N=<N> active messageCreate events in last 30 min — waiting 5 min` to SUMMARY draft. Sleep 5 minutes (300 seconds). Continue loop.
      - If `now() - gate_start_at >= 6 hours`: HOLD. Write SUMMARY with `outcome: held-channels-active`, `gate_start_at`, `last_gate_check_at`, `last_N_observed`. STOP. Do NOT deploy.

    Operator-active-hours fallback (per `feedback_no_auto_deploy`):
    - This plan runs autonomously. The 30-min channel-silence rule is the autonomous-overnight gate.
    - If running during clearly operator-active hours AND the gate has never been satisfied within the first 30 min of polling, surface to operator with: "Phase 107 ready to deploy. Channels still active. Send `deploy 107` or `ship it` to override the gate." Then wait for explicit deploy phrase OR continue polling — operator's call.

    On gate satisfied → proceed to Task 3.
  </action>
  <verify>
    <automated>echo "Gate poll is wall-clock — verification is the absence of a held-deploy SUMMARY at this point in the run; visible in Task 3 starting"</automated>
  </verify>
  <done>
    Gate satisfied (N=0 messageCreate events from non-bot users in past 30 min) OR plan held with reason recorded in SUMMARY. If held, plan stops cleanly here; subsequent tasks not executed.
  </done>
</task>

<task type="auto">
  <name>Task 3: ssh deploy to clawdy — pull, build (or rsync dist/), restart systemd</name>
  <files>(remote: /opt/clawcode on clawdy)</files>
  <action>
    Deploy to clawdy. Use the project's standard deploy idiom — preferred is git-pull-build-restart on the remote (matches Phase 106 + reference_clawcode_server.md):

    ```
    ssh jjagpal@100.98.211.108 "cd /opt/clawcode && sudo -u clawcode git pull && sudo -u clawcode npm ci && sudo -u clawcode npm run build && sudo systemctl restart clawcode"
    ```

    Capture stdout + stderr to a deploy log file `~/.tmp/107-deploy.log` (or in SUMMARY draft directly).

    Alternative (if rsync is the project's actual idiom — verify by checking the most recent successful deploy SUMMARY in `.planning/phases/`):
    ```
    rsync -av --delete dist/ jjagpal@100.98.211.108:/tmp/clawcode-dist-107/
    ssh jjagpal@100.98.211.108 "sudo -u clawcode rsync -av --delete /tmp/clawcode-dist-107/ /opt/clawcode/dist/ && sudo systemctl restart clawcode"
    ```

    Post-deploy validation (within 10 seconds):
    ```
    ssh jjagpal@100.98.211.108 "systemctl status clawcode --no-pager | head -20"
    ```
    Expect: `active (running)`.

    If status is NOT active → ABORT smoke. Write SUMMARY with `outcome: deploy-failed-service-not-active`, capture journalctl tail for diagnosis:
    ```
    ssh jjagpal@100.98.211.108 "journalctl -u clawcode --since '5 min ago' --no-pager | tail -50"
    ```

    Record `deploy_completed_at = now()` in SUMMARY draft. Proceed to Task 4 (smoke).
  </action>
  <verify>
    <automated>ssh jjagpal@100.98.211.108 "systemctl is-active clawcode" 2>&amp;1 | grep -q "^active$"</automated>
  </verify>
  <done>
    clawcode service is `active (running)` on clawdy post-deploy. Deploy log captured. Ready for smoke.
  </done>
</task>

<task type="auto">
  <name>Task 4: Post-deploy smoke — DREAM warn-level + VEC cascade + cleanupOrphans CLI</name>
  <files>(remote queries; SUMMARY draft updates)</files>
  <action>
    Run three smoke checks. Capture all outputs to SUMMARY draft.

    Smoke 1 — DREAM-OUT-03 warn-level recovery proof.

    Wait window: 30-60 minutes post-deploy (or until a natural dream cron fires; see Phase 95 dream-cron schedule). During the window:
    ```
    ssh jjagpal@100.98.211.108 "journalctl -u clawcode --since '30 min ago' --no-pager | grep -E 'dream-pass|parse-failed' | tail -50"
    ```

    Outcomes:
    - If `parse-failed` line(s) appear:
      - Verify level is warn (pino level 40, NOT 50/error). Each line MUST be JSON-shaped with `component=dream-pass`, `action=parse-failed`, `responsePrefix` (≤80 chars), `agent=<name>`.
      - Verify `systemctl is-active clawcode` still returns `active`.
      - SUMMARY: `dream-warn-smoke: PASS — N parse-failed warns observed, daemon active.`
    - If NO `parse-failed` line: model behaved this window. SUMMARY: `dream-warn-smoke: CLEAN — no parse failures observed in window; recovery path latent. Daemon active.` Both outcomes pass.

    NEVER expect to see `error`-level dream-pass parse-failed logs — if any appear, the deploy did NOT take effect (Phase 107-01 should have downgraded these to warn). Investigate.

    Smoke 2 — VEC cascade proof.

    Inspect a recent delete from production OR generate a synthetic one. Easiest path:
    ```
    ssh jjagpal@100.98.211.108 "sudo -u clawcode sqlite3 ~clawcode/.clawcode/agents/test-agent/memory/memories.db 'SELECT COUNT(*) FROM vec_memories vm WHERE vm.memory_id NOT IN (SELECT id FROM memories);'"
    ```
    This counts CURRENT orphans. SUMMARY: `vec-orphan-baseline: <count>`.

    If a delete path exposes an IPC method, exercise it (write tiny memory → delete via IPC → query for that id → expect 0). Otherwise, this smoke is verified indirectly via Smoke 3's pre/post counts (cleanup CLI is the proof point that the cascade works going forward).

    Smoke 3 — cleanupOrphans CLI run.

    For each bound agent, run the cleanup CLI and capture per-agent output:
    ```
    ssh jjagpal@100.98.211.108 "sudo -u clawcode node /opt/clawcode/dist/cli/index.js memory cleanup-orphans"
    ```
    Expected output shape (per Plan 107-02 CLI):
    ```
    test-agent: removed 17 orphans (845 vec_memories remaining)
    research: no orphans (0 vec_memories total)
    fin-research: removed 3 orphans (412 vec_memories remaining)
    ...
    ```
    Per `RESEARCH.md` pitfall 4, expect `removed > 0` for one or more agents (historical migration drops). Per CONTEXT.md, Admin Clawdy already manually patched current state on at least one agent — that one may show 0 orphans.

    Run a SECOND time immediately:
    ```
    ssh jjagpal@100.98.211.108 "sudo -u clawcode node /opt/clawcode/dist/cli/index.js memory cleanup-orphans"
    ```
    Idempotency assertion: every agent line MUST show `removed 0 orphans`. SUMMARY: `cleanup-cli-smoke: PASS — first run cleaned N total orphans, second run 0 (idempotent).`

    Aggregate orphan count across all agents goes into SUMMARY for operator visibility (this number tells the operator how much historical migration drift had accumulated).
  </action>
  <verify>
    <automated>ssh jjagpal@100.98.211.108 "sudo -u clawcode node /opt/clawcode/dist/cli/index.js memory cleanup-orphans" 2>&amp;1 | tee /tmp/107-cleanup-1.log &amp;&amp; ssh jjagpal@100.98.211.108 "sudo -u clawcode node /opt/clawcode/dist/cli/index.js memory cleanup-orphans" 2>&amp;1 | tee /tmp/107-cleanup-2.log &amp;&amp; ! grep -E "removed [1-9]" /tmp/107-cleanup-2.log</automated>
  </verify>
  <done>
    All three smoke results captured in SUMMARY draft. Smoke 1: PASS or CLEAN. Smoke 2: orphan baseline count recorded. Smoke 3: PASS — first run reports per-agent counts, second run idempotently reports 0 across all agents. Daemon `systemctl is-active` still `active`.
  </done>
</task>

<task type="auto">
  <name>Task 5: Write 107-03-SUMMARY.md and commit</name>
  <files>.planning/phases/107-memory-pipeline-integrity-dream-json-vec-memories-orphan-cleanup/107-03-SUMMARY.md</files>
  <action>
    Write the deploy SUMMARY at `.planning/phases/107-memory-pipeline-integrity-dream-json-vec-memories-orphan-cleanup/107-03-SUMMARY.md`. Structure:

    ```
    # Phase 107-03 — Deploy gate + smoke — SUMMARY

    **Outcome:** deployed | held-channels-active | aborted-pre-deploy | deploy-failed-service-not-active
    **Deploy timestamp:** <ISO-8601 if deployed>
    **Gate poll duration:** <minutes from gate_start to gate_satisfied or cap-hit>

    ## Pre-deploy gauntlet
    - npm test: PASS (N tests)
    - npm run typecheck: PASS
    - npm run build: PASS (dist/ regenerated)
    - dist/ surfaces verified (memory-cleanup-orphans + parse-failed)

    ## Deploy gate
    - gate_start_at: <ts>
    - gate_satisfied_at: <ts> | <NULL if held>
    - polls: <N>
    - last_N_observed: <messageCreate count>

    ## Deploy
    - command: <git pull / npm ci / npm run build / systemctl restart  OR  rsync + restart>
    - service status: active (running)
    - deploy log path: <if captured>

    ## Smoke results
    ### DREAM-OUT-03 warn-level recovery
    - Outcome: PASS (N parse-failed warns observed, daemon active) OR CLEAN (no failures in window)
    - Sample log line: <one example structured warn JSON>

    ### VEC cascade
    - Pre-cleanup orphan count (test-agent): <N>
    - <other agents per memories.db inspection>

    ### cleanupOrphans CLI
    - First run output:
      ```
      <per-agent lines>
      ```
    - Second run output (idempotency):
      ```
      <per-agent lines — all should show "removed 0 orphans">
      ```
    - Total orphans cleaned across all agents: <N>

    ## Notes
    - DREAM-OUT-02 deferred per Plan 107-01. Anchors recorded in 107-01-PLAN.md `<deferred>` section for future-phase pickup.
    - Historical orphan count (Smoke 3 first run) reflects accumulated drift from CHECK-constraint table-recreation migrations (RESEARCH.md pitfall 4). Going forward, MemoryStore.delete is the only delete path and cascades atomically.

    ## Held-deploy fallback (only if outcome=held)
    - Reason: <channel activity / cap hit>
    - Operator action required: re-run plan during quiet window OR confirm explicit deploy override.
    ```

    Then commit:
    ```
    git add .planning/phases/107-memory-pipeline-integrity-dream-json-vec-memories-orphan-cleanup/107-03-SUMMARY.md
    git commit -m "docs(107-03): deploy gate + smoke — <outcome>"
    ```

    Commit message body should embed the outcome (`deployed` / `held-channels-active` / `aborted-pre-deploy` / `deploy-failed-service-not-active`) for easy log scanning.
  </action>
  <verify>
    <automated>test -f .planning/phases/107-memory-pipeline-integrity-dream-json-vec-memories-orphan-cleanup/107-03-SUMMARY.md &amp;&amp; grep -E "Outcome:|Deploy timestamp:|Smoke results" .planning/phases/107-memory-pipeline-integrity-dream-json-vec-memories-orphan-cleanup/107-03-SUMMARY.md</automated>
  </verify>
  <done>
    SUMMARY written with outcome, gate stats, deploy log, smoke results (all 3), and orphan totals. Committed with `docs(107-03): deploy gate + smoke — <outcome>`.
  </done>
</task>

</tasks>

<verification>
Plan-level checks (executed inline in tasks):

1. Pre-deploy gauntlet: `npm test && npm run typecheck && npm run build` — all GREEN before gate.
2. Deploy gate: 30-min channel silence on non-bot messageCreate events.
3. Deploy: clawcode service `active (running)` post-restart.
4. Smoke (DREAM): parse-failed lines (if any) at warn level with structured fields, daemon active.
5. Smoke (VEC): cleanupOrphans CLI runs, second run idempotent (0 across all agents).
6. SUMMARY committed with outcome documented.

Held-deploy outcome is also a valid verification result (gate not satisfied, plan stops cleanly, SUMMARY records reason). Operator picks up the next morning.
</verification>

<success_criteria>
- [ ] Pre-deploy gauntlet GREEN: `npm test`, `npm run typecheck`, `npm run build` all pass
- [ ] dist/ contains both `memory-cleanup-orphans` and `parse-failed` strings
- [ ] Deploy gate satisfied (≥30 min channel silence on non-bot messageCreate) OR plan held with reason recorded
- [ ] If deployed: clawcode systemd unit `active (running)` post-restart
- [ ] If deployed: dream smoke PASS or CLEAN (no error-level parse-failed lines)
- [ ] If deployed: cleanupOrphans CLI runs cleanly first time + idempotently 0 second time
- [ ] If deployed: total orphan count cleaned recorded in SUMMARY
- [ ] SUMMARY committed with explicit outcome label
- [ ] If held: NO deploy attempted, NO mutations on clawdy
</success_criteria>

<output>
After completion, `.planning/phases/107-memory-pipeline-integrity-dream-json-vec-memories-orphan-cleanup/107-03-SUMMARY.md` is committed with:
- Outcome (deployed | held-channels-active | aborted-pre-deploy | deploy-failed-service-not-active)
- Pre-deploy gauntlet results
- Gate poll stats (start_at, satisfied_at or cap-hit, poll count, last N observed)
- Deploy command + service status
- All three smoke results (DREAM warn, VEC baseline orphan count, cleanupOrphans CLI first + second run)
- Total historical orphans cleaned across all agents (operator-visible drift indicator)

Commit message:
```
docs(107-03): deploy gate + smoke — <outcome>

Phase 107 ship — bundled DREAM JSON enforcement (107-01) + vec_memories
orphan cleanup (107-02). Pre-deploy gauntlet [PASS|FAIL]. Gate
[satisfied|held|skipped]. Deploy [completed|held|aborted]. Smoke
[passed|partial|skipped].

Historical orphan count cleaned: <N> across <M> agents.
DREAM-OUT-02 deferred (anchors in 107-01-PLAN.md).
```
</output>
