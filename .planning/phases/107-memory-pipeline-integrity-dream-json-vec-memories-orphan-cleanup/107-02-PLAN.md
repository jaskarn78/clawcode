---
phase: 107
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/memory/store.ts
  - src/memory/__tests__/store-orphan-cleanup.test.ts
  - src/memory/__tests__/store.test.ts
  - src/ipc/protocol.ts
  - src/ipc/__tests__/protocol.test.ts
  - src/manager/daemon.ts
  - src/cli/commands/memory.ts
autonomous: true
requirements:
  - VEC-CLEAN-01
  - VEC-CLEAN-02
  - VEC-CLEAN-03
  - VEC-CLEAN-04

must_haves:
  truths:
    - "Audit confirms `MemoryStore.delete(id)` at store.ts:312-331 is the ONLY `DELETE FROM memories` site in production code and is already atomic+cascading"
    - "`MemoryStore.cleanupOrphans()` method exists, returns `{ removed: number, totalAfter: number }`, and runs the count+delete inside a single `db.transaction()`"
    - "`cleanupOrphans` is idempotent — second invocation removes 0 rows"
    - "`cleanupOrphans` is directional — deletes from `vec_memories` where `memory_id NOT IN (SELECT id FROM memories)`. Does NOT touch `memories` (cold-archived rows survive)"
    - "IPC method `memory-cleanup-orphans` is registered in `IPC_METHODS` array (protocol.ts) AND dispatched in daemon.ts (closure intercept BEFORE `routeMethod`) with optional `agent` filter"
    - "CLI subcommand `clawcode memory cleanup-orphans [-a <agent>]` exists, calls the IPC method, formats per-agent results, and exits 0 on success / 1 on ManagerNotRunningError"
    - "Vitest coverage: removes-orphans, idempotent, preserves-cold (intentional excise), atomic (rollback on mid-transaction failure), regression-cascade (existing delete still cascades)"
    - "All existing tests stay green (Phase 80, 95, 99-mdrop, 999.6, 999.12, 999.14, 999.15, 106)"
    - "No new npm deps"
  artifacts:
    - path: "src/memory/store.ts"
      provides: "`cleanupOrphans()` method on MemoryStore"
      contains: "cleanupOrphans"
    - path: "src/memory/__tests__/store-orphan-cleanup.test.ts"
      provides: "Vitest coverage for cleanupOrphans behavior"
      min_lines: 150
    - path: "src/ipc/protocol.ts"
      provides: "`memory-cleanup-orphans` registered in IPC_METHODS"
      contains: "memory-cleanup-orphans"
    - path: "src/manager/daemon.ts"
      provides: "Daemon dispatch handler for `memory-cleanup-orphans`"
      contains: "memory-cleanup-orphans"
    - path: "src/cli/commands/memory.ts"
      provides: "`clawcode memory cleanup-orphans` CLI subcommand"
      contains: "cleanup-orphans"
  key_links:
    - from: "MemoryStore.cleanupOrphans"
      to: "vec_memories vtab (sqlite-vec)"
      via: "DELETE FROM vec_memories WHERE memory_id NOT IN (SELECT id FROM memories) — directional, inside db.transaction()"
      pattern: "DELETE FROM vec_memories"
    - from: "CLI `clawcode memory cleanup-orphans`"
      to: "Daemon `memory-cleanup-orphans` handler"
      via: "sendIpcRequest(SOCKET_PATH, 'memory-cleanup-orphans', { agent? })"
      pattern: "memory-cleanup-orphans"
    - from: "Daemon dispatch handler"
      to: "MemoryStore.cleanupOrphans()"
      via: "manager.getMemoryStore(agent) → store.cleanupOrphans()"
      pattern: "getMemoryStore.*cleanupOrphans"
    - from: "Cold archival (episode-archival.ts:53-66)"
      to: "cleanupOrphans NOT touching `memories`"
      via: "Cold archive deletes vec but KEEPS memory row (memory present, vec absent — opposite of orphan)"
      pattern: "preserves-cold"
---

<objective>
Pillar B — vec_memories orphan cleanup. Audit memory-delete paths (VEC-CLEAN-01), confirm transactional atomicity (VEC-CLEAN-02), add an idempotent operator-runnable `cleanupOrphans()` method exposed via IPC + CLI subcommand (VEC-CLEAN-03), and cover all of it with unit tests (VEC-CLEAN-04).

Purpose: sqlite-vec virtual tables don't support FK CASCADE — application code must explicitly cascade. Per RESEARCH.md, `MemoryStore.delete(id)` (store.ts:312-331) IS the only `DELETE FROM memories` site in production code today, and it's already atomic + cascading. The real work is:
- Forward-going: REGRESSION test that pins this invariant so future code can't add a leaky path
- Historical orphans: production agent SQLite DBs accumulated orphan `vec_memories` rows during CHECK-constraint table-recreation migrations (`migrateSchema` / `migrateEpisodeSource` at store.ts:651-755 drop+recreate `memories` without touching `vec_memories`). Operator needs a one-shot CLI to clean them.

Output: An idempotent `cleanupOrphans()` method on `MemoryStore`, exposed through `memory-cleanup-orphans` IPC + `clawcode memory cleanup-orphans [-a <agent>]` CLI, with unit tests pinning atomicity, idempotency, and directional preservation of cold-archived memories.

**Critical pitfall to avoid (RESEARCH.md pitfall 3):**
- The SQL MUST be directional: `DELETE FROM vec_memories WHERE memory_id NOT IN (SELECT id FROM memories)`.
- NEVER delete from memories where id NOT IN vec_memories — that would erase cold-archived memories (intentional excise where memory STAYS but vec is removed; episode-archival.ts:53-66).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-memory-pipeline-integrity-dream-json-vec-memories-orphan-cleanup/107-CONTEXT.md
@.planning/phases/107-memory-pipeline-integrity-dream-json-vec-memories-orphan-cleanup/107-RESEARCH.md

<interfaces>
Existing memory delete site (already atomic + cascading per RESEARCH.md audit):

```
// src/memory/store.ts:312-331 — MemoryStore.delete (ONLY DELETE FROM memories site)
delete(id: string): boolean {
  return this.db.transaction(() => {
    const result = this.db.prepare("DELETE FROM memories WHERE id = ?").run(id);
    if (result.changes === 0) return false;
    this.db.prepare("DELETE FROM vec_memories WHERE memory_id = ?").run(id);
    return true;
  })();
}
```

vec_memories schema (sqlite-vec virtual table):

```
-- src/memory/store.ts:639-642
CREATE VIRTUAL TABLE IF NOT EXISTS vec_memories USING vec0(
  memory_id TEXT PRIMARY KEY,
  embedding float[384]
);
```

NOT memory-delete (intentional excise — cold archive). Must be PRESERVED:
- src/memory/episode-archival.ts:53-66 — sets tier=cold AND deletes from vec_memories. memories row STAYS. Opposite shape from an orphan.

NOT memory-delete (delete+reinsert atomic merge):
- src/memory/dedup.ts:117-121 — DELETE FROM vec_memories + INSERT new — atomic merge, not an orphan source.

IPC namespace pattern — protocol.ts:75-86:
- Add string literal "memory-cleanup-orphans" alongside other memory-* entries.

Daemon dispatch — closure intercept BEFORE routeMethod:
- src/manager/daemon.ts:2820-3140 — IPC dispatch block
- Pattern: Phase 95 DREAM-07 (line ~2846), Phase 96 PFS- (~2854), Phase 999.15 mcp-tracker (~2920)
- Shape: `if (method === "...") { /* handle */ return result; }` BEFORE the generic routeMethod call.

CLI command group pattern — memory.ts:247-336:
- registerMemoryCommand(program) chains subcommands via `memoryCmd.command("...").description(...).option(...).action(async (opts) => {...})`
- Reuse imports: sendIpcRequest, SOCKET_PATH, cliLog, cliError, ManagerNotRunningError.

Test scaffolding — store.test.ts uses `new MemoryStore(":memory:")`:
- Insert fixture chunks via existing insert path with embeddings.
- Query vec_memories directly via raw db handle for assertions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Write store-orphan-cleanup test file (failing against missing cleanupOrphans method)</name>
  <files>src/memory/__tests__/store-orphan-cleanup.test.ts, src/memory/__tests__/store.test.ts</files>
  <behavior>
    store-orphan-cleanup.test.ts (NEW):
    - removes-orphans — Insert 3 memory chunks (each gets a paired vec_memories row via the existing insert path). Manually `db.exec("DELETE FROM memories WHERE id = ?")` for 1 chunk (simulating pre-fix state OR a future leak path), bypassing MemoryStore.delete so vec is NOT cascaded. Call `store.cleanupOrphans()`. Expect `{ removed: 1, totalAfter: 2 }`. Query vec_memories: 2 rows, neither is the deleted chunk's id.
    - idempotent — Setup as above, call `cleanupOrphans()` once (returns `{ removed: 1, ... }`), then call AGAIN. Expect second call returns `{ removed: 0, totalAfter: 2 }`. Query vec_memories: still 2 rows.
    - preserves-cold — Insert 2 memory chunks. Manually replicate cold-archive shape (`UPDATE memories SET tier='cold' WHERE id=?` + `DELETE FROM vec_memories WHERE memory_id=?`) for 1 chunk. Call `cleanupOrphans()`. Expect `{ removed: 0, ... }` — cold-archived has memory present + vec absent (opposite of orphan). Query memories: still 2 rows. This is the DIRECTIONAL pitfall test.
    - atomic — Setup with N orphans. Use `vi.spyOn(store['db'], 'prepare')` to throw mid-transaction (after the DELETE prepare but before run, or before the post-COUNT). Call `cleanupOrphans()`. Expect throw. Query vec_memories: original count unchanged (rollback).
    - no-orphans-clean-state — Fresh store with N memories all paired correctly. Call `cleanupOrphans()`. Expect `{ removed: 0, totalAfter: N }`.

    store.test.ts (EXTEND with regression test):
    - delete-cascades-vec — Insert 1 memory chunk. Verify vec_memories has 1 row. Call `store.delete(id)`. Verify memories has 0 rows AND vec_memories has 0 rows for that id (paired delete inside same transaction).
  </behavior>
  <action>
    Create `src/memory/__tests__/store-orphan-cleanup.test.ts` patterned on `src/memory/__tests__/store.test.ts:1-200` (existing scaffolding using `new MemoryStore(":memory:")`).

    Test setup:
    - `beforeEach` creates a fresh in-memory `MemoryStore(":memory:")` — provides isolated db handle per test
    - Helper to insert N chunks with embeddings (use existing insert path — find via grep on store.ts; likely `insertChunk` or similar)
    - Helper to access raw db: cast or expose test-only getter (match pattern in existing store.test.ts)
    - Helper to count vec_memories rows: prepared SELECT COUNT(*) FROM vec_memories

    Write 5 `it(...)` blocks per the behaviors above. Key assertion patterns:
    - `expect(store.cleanupOrphans()).toEqual({ removed: 1, totalAfter: 2 })`
    - Query vec_memories directly to confirm orphan removed / cold preserved / counts match
    - For atomic test: spyOn db.prepare to throw, expect cleanupOrphans to throw, expect post-throw vec_memories count unchanged

    Then extend `src/memory/__tests__/store.test.ts` with a new describe block "Phase 107 VEC-CLEAN-01 regression — delete cascades to vec_memories" containing the delete-cascades-vec test.

    Both files WILL FAIL: store-orphan-cleanup.test.ts because `cleanupOrphans` doesn't exist yet (TS compile error or runtime "is not a function"). This is the RED.

    Do NOT add `cleanupOrphans` in this task. Commit RED test files.
  </action>
  <verify>
    <automated>npx vitest run src/memory/__tests__/store-orphan-cleanup.test.ts 2>&amp;1 | grep -E "FAIL|fail|cleanupOrphans" || (echo "Expected RED failures — none seen" &amp;&amp; exit 1)</automated>
  </verify>
  <done>
    `src/memory/__tests__/store-orphan-cleanup.test.ts` exists with at least 5 `it(...)` blocks. `store.test.ts` extended with `delete-cascades-vec` regression test. Tests fail (RED) because `MemoryStore.cleanupOrphans()` is not yet implemented.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Add cleanupOrphans() method to MemoryStore (VEC-CLEAN-01 + VEC-CLEAN-02 + VEC-CLEAN-03 method-side)</name>
  <files>src/memory/store.ts</files>
  <behavior>
    - `MemoryStore.cleanupOrphans(): { removed: number; totalAfter: number }` exists on the class
    - Uses `this.db.transaction(() => { ... })()` for atomicity
    - SQL is directional: `DELETE FROM vec_memories WHERE memory_id NOT IN (SELECT id FROM memories)` — NEVER touches `memories`
    - Returns `{ removed: result.changes, totalAfter: post-count }`
    - Idempotent: second call removes 0
    - Task 1 RED tests turn GREEN
    - Existing store tests still GREEN (delete cascade, dedup merge, archival, migrations)
    - VEC-CLEAN-01 audit confirmed via grep
  </behavior>
  <action>
    Open `src/memory/store.ts`. Add the `cleanupOrphans` method to the `MemoryStore` class. Place near other operational helpers (e.g. near `bumpAccess` or `getMemoryFileSha256` per RESEARCH.md guidance).

    Implementation:
    ```
    cleanupOrphans(): { removed: number; totalAfter: number } {
      return this.db.transaction(() => {
        const result = this.db
          .prepare(
            "DELETE FROM vec_memories WHERE memory_id NOT IN (SELECT id FROM memories)",
          )
          .run();
        const afterRow = this.db
          .prepare("SELECT COUNT(*) AS n FROM vec_memories")
          .get() as { n: number };
        return {
          removed: result.changes as number,
          totalAfter: afterRow.n,
        };
      })();
    }
    ```

    Add a JSDoc comment explaining: Phase 107 VEC-CLEAN-03; idempotent; atomic; directional (only touches vec_memories, never memories — cold archives must be preserved).

    Then run the RED tests. They should turn GREEN. Run the FULL store test file to ensure no regressions:
    `npx vitest run src/memory/__tests__/store.test.ts src/memory/__tests__/store-orphan-cleanup.test.ts`

    All MUST be GREEN.

    Audit step (VEC-CLEAN-01) — read-only validation. Run grep to confirm `MemoryStore.delete` is still the ONLY production `DELETE FROM memories` site:
    ```
    grep -rn "DELETE FROM memories" src/ --include="*.ts" | grep -v __tests__
    ```
    Expected: 1 hit (store.ts inside `delete(id)`). RESEARCH.md confirms; this grep is the on-the-fly final audit.
  </action>
  <verify>
    <automated>npx vitest run src/memory/__tests__/store.test.ts src/memory/__tests__/store-orphan-cleanup.test.ts &amp;&amp; npm run typecheck &amp;&amp; (test "$(grep -rn 'DELETE FROM memories' src/ --include='*.ts' | grep -v __tests__ | wc -l)" -eq 1 || (echo "Expected exactly 1 production DELETE FROM memories site" &amp;&amp; exit 1))</automated>
  </verify>
  <done>
    `MemoryStore.cleanupOrphans` implemented with directional SQL inside `db.transaction()`. Task 1 RED tests GREEN. Existing store tests GREEN. Typecheck clean. Audit confirms exactly 1 production `DELETE FROM memories` site.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: VEC-CLEAN-03 wiring — IPC method registration + daemon dispatch handler + CLI subcommand</name>
  <files>src/ipc/protocol.ts, src/ipc/__tests__/protocol.test.ts, src/manager/daemon.ts, src/cli/commands/memory.ts</files>
  <behavior>
    - `IPC_METHODS` array in `src/ipc/protocol.ts` includes the string `"memory-cleanup-orphans"` in the memory namespace block
    - `src/manager/daemon.ts` IPC dispatch block has a closure intercept handler for `"memory-cleanup-orphans"` BEFORE `routeMethod`
    - Handler accepts optional `params.agent` (string). If provided, scopes to that one agent; otherwise iterates all resolved agents.
    - For each target agent: calls `manager.getMemoryStore(agent).cleanupOrphans()`, collects `{ agent, removed, totalAfter }`. On per-agent error: logs `[memory-cleanup-orphans] <agent> failed: <msg>` and pushes `{ agent, removed: 0, totalAfter: -1 }` (sentinel for failure).
    - Returns `{ results: Array<{ agent, removed, totalAfter }> }`
    - CLI subcommand `clawcode memory cleanup-orphans [-a <agent>]` exists under existing `memory` group
    - CLI formats output: per-agent line indicating removed count or no orphans or cleanup failed
    - CLI exits 1 on `ManagerNotRunningError` with operator-friendly message
    - IPC test asserts `IPC_METHODS` includes the new entry
    - All tests stay GREEN
  </behavior>
  <action>
    Step 1: Register IPC method — `src/ipc/protocol.ts`. Find the `IPC_METHODS` array around line 75-86 (memory namespace). Add the string literal alongside other `memory-*` entries:
    ```
    "memory-cleanup-orphans",
    ```

    Extend `src/ipc/__tests__/protocol.test.ts` with an assertion (in an existing describe or a new "Phase 107 VEC-CLEAN-03" describe):
    ```
    expect(IPC_METHODS).toContain("memory-cleanup-orphans");
    ```

    Step 2: Daemon dispatch handler — `src/manager/daemon.ts`. Locate the IPC dispatch block (daemon.ts:2820-3140). Find a Phase 999.15-style closure intercept (e.g. mcp-tracker-snapshot at ~line 2920). Add the new handler ALONGSIDE existing memory-namespace handlers, BEFORE the generic routeMethod dispatch:

    ```
    if (method === "memory-cleanup-orphans") {
      const agentParam = typeof params["agent"] === "string"
        ? (params["agent"] as string)
        : null;
      const targets = agentParam
        ? [agentParam]
        : resolvedAgents.map((a) => a.name);
      const results: Array<{ agent: string; removed: number; totalAfter: number }> = [];
      for (const agent of targets) {
        const store = manager.getMemoryStore(agent);
        if (!store) {
          results.push({ agent, removed: 0, totalAfter: 0 });
          continue;
        }
        try {
          const r = store.cleanupOrphans();
          results.push({ agent, removed: r.removed, totalAfter: r.totalAfter });
        } catch (err) {
          const msg = err instanceof Error ? err.message : String(err);
          log.error(`[memory-cleanup-orphans] ${agent} failed: ${msg}`);
          results.push({ agent, removed: 0, totalAfter: -1 });
        }
      }
      return { results };
    }
    ```

    Adapt variable names (`resolvedAgents`, `manager`, `log`) to match the local scope at the chosen insertion point — read lines 2820-3140 first to find the live names.

    Step 3: CLI subcommand — `src/cli/commands/memory.ts`. Locate `registerMemoryCommand(program)` (around line 247-336). Add a new chained subcommand:

    ```
    memoryCmd
      .command("cleanup-orphans")
      .description("Remove orphan vec_memories rows whose memory_id no longer exists in memories")
      .option("-a, --agent <name>", "Filter to one agent (omit for all agents)")
      .action(async (opts: { agent?: string }) => {
        try {
          const params: Record<string, unknown> = {};
          if (opts.agent) params.agent = opts.agent;
          const result = (await sendIpcRequest(
            SOCKET_PATH,
            "memory-cleanup-orphans",
            params,
          )) as { results: Array<{ agent: string; removed: number; totalAfter: number }> };
          for (const r of result.results) {
            if (r.totalAfter < 0) {
              cliError(`${r.agent}: cleanup failed`);
            } else if (r.removed === 0) {
              cliLog(`${r.agent}: no orphans (${r.totalAfter} vec_memories total)`);
            } else {
              cliLog(`${r.agent}: removed ${r.removed} orphans (${r.totalAfter} vec_memories remaining)`);
            }
          }
        } catch (error) {
          if (error instanceof ManagerNotRunningError) {
            cliError("Manager is not running. Start it with: clawcode start-all");
            process.exit(1);
            return;
          }
          const msg = error instanceof Error ? error.message : String(error);
          cliError(`Error: ${msg}`);
          process.exit(1);
        }
      });
    ```

    Reuse existing imports (`sendIpcRequest`, `SOCKET_PATH`, `cliLog`, `cliError`, `ManagerNotRunningError`) from the file's top — match the pattern of existing subcommands in `memory.ts`.

    Step 4: Run tests + typecheck.
    `npx vitest run src/memory src/ipc src/cli` — all GREEN.
    `npm run typecheck` — clean.
  </action>
  <verify>
    <automated>npx vitest run src/memory src/ipc src/cli &amp;&amp; npm run typecheck &amp;&amp; grep -q '"memory-cleanup-orphans"' src/ipc/protocol.ts &amp;&amp; grep -q 'memory-cleanup-orphans' src/manager/daemon.ts &amp;&amp; grep -q 'cleanup-orphans' src/cli/commands/memory.ts</automated>
  </verify>
  <done>
    `IPC_METHODS` includes `"memory-cleanup-orphans"`. Daemon dispatches the method to `MemoryStore.cleanupOrphans` per-agent. CLI subcommand `clawcode memory cleanup-orphans` exists and formats per-agent results. All tests GREEN. Typecheck clean.
  </done>
</task>

</tasks>

<verification>
Plan-level checks (run after all 3 tasks complete):

1. Full memory + ipc + cli test suite GREEN:
   ```
   npx vitest run src/memory src/ipc src/cli
   ```

2. Typecheck clean:
   ```
   npm run typecheck
   ```

3. Audit — exactly one production `DELETE FROM memories` site:
   ```
   grep -rn "DELETE FROM memories" src/ --include="*.ts" | grep -v __tests__ | wc -l
   ```
   Expect: 1.

4. Static grep — directional SQL (NEVER `DELETE FROM memories WHERE id NOT IN`):
   ```
   grep -rn "DELETE FROM memories WHERE id NOT IN" src/ --include="*.ts"
   ```
   Expect: 0 matches (NO results — that pattern would erase cold archives).

5. IPC method registration:
   ```
   grep -c '"memory-cleanup-orphans"' src/ipc/protocol.ts
   ```
   Expect: ≥1.

6. Daemon dispatch handler:
   ```
   grep -c "method === \"memory-cleanup-orphans\"" src/manager/daemon.ts
   ```
   Expect: ≥1.

7. CLI subcommand:
   ```
   grep -c '"cleanup-orphans"' src/cli/commands/memory.ts
   ```
   Expect: ≥1.
</verification>

<success_criteria>
- [ ] VEC-CLEAN-01: audit confirms `MemoryStore.delete(id)` is the only production `DELETE FROM memories` site (grep returns exactly 1 hit)
- [ ] VEC-CLEAN-02: regression test pins atomic cascade behavior in `MemoryStore.delete`
- [ ] VEC-CLEAN-03: `MemoryStore.cleanupOrphans()` method, IPC method `memory-cleanup-orphans`, daemon dispatch handler, CLI subcommand `clawcode memory cleanup-orphans [-a <agent>]` all wired
- [ ] VEC-CLEAN-04: vitest covers removes-orphans, idempotent, preserves-cold (directional), atomic (rollback), regression-cascade
- [ ] All existing memory + ipc + cli tests GREEN
- [ ] `npm run typecheck` clean
- [ ] No new npm dependencies introduced
- [ ] No `DELETE FROM memories WHERE id NOT IN` pattern anywhere in src/
</success_criteria>

<output>
After completion, create `.planning/phases/107-memory-pipeline-integrity-dream-json-vec-memories-orphan-cleanup/107-02-SUMMARY.md` with:
- Tasks completed (1-3)
- Files modified
- Test results (counts of new tests added)
- Audit result (production DELETE FROM memories site count)
- Commit hash(es)

Commit message template:
```
feat(107-02): vec_memories orphan cleanup — cleanupOrphans method + IPC + CLI

VEC-CLEAN-01: Audit confirms MemoryStore.delete(id) at store.ts:312-331 is
  the ONLY production DELETE FROM memories site, and is already atomic +
  cascading. Add regression test pinning the invariant.

VEC-CLEAN-02: Existing transaction wrapper preserved. cleanupOrphans
  inherits the same pattern.

VEC-CLEAN-03: Add MemoryStore.cleanupOrphans() — directional SQL DELETE
  FROM vec_memories WHERE memory_id NOT IN (SELECT id FROM memories) inside
  db.transaction(). Idempotent. Returns { removed, totalAfter }. NEVER
  deletes from memories (would erase cold archives — episode-archival.ts).

  Wire IPC method memory-cleanup-orphans (protocol.ts) + daemon dispatch
  handler (daemon.ts, closure intercept before routeMethod, supports
  optional agent filter) + CLI subcommand `clawcode memory cleanup-orphans
  [-a <agent>]` (memory.ts).

VEC-CLEAN-04: src/memory/__tests__/store-orphan-cleanup.test.ts covers
  removes-orphans, idempotent, preserves-cold (directional pitfall),
  atomic (rollback on mid-transaction failure). store.test.ts extended
  with delete-cascades-vec regression.

Refs: Phase 95 (DREAM-07 IPC pattern), Phase 999.15 (mcp-tracker CLI pattern),
Phase 999.6 (atomic write semantics).
```
</output>
