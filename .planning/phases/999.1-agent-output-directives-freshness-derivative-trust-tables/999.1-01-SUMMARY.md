---
phase: 999.1-agent-output-directives-freshness-derivative-trust-tables
plan: 01
subsystem: config/schema (DEFAULT_SYSTEM_PROMPT_DIRECTIVES) — extends Phase 94 D-10 directive injection rail
tags: [directives, system-prompt, agent-behavior, tdd, additive]
requires:
  - Phase 94 D-10 (DEFAULT_SYSTEM_PROMPT_DIRECTIVES rail + override merge)
  - Phase 99 (subagent-routing pattern)
  - Phase 100-fu (5 prior directive landings establishing test pattern)
provides:
  - DEFAULT_SYSTEM_PROMPT_DIRECTIVES["freshness"] (FRESH-01..03)
  - DEFAULT_SYSTEM_PROMPT_DIRECTIVES["derivative-work"] (DERIV-01..03)
  - DEFAULT_SYSTEM_PROMPT_DIRECTIVES["trusted-operator"] (TRUST-01..02)
  - DEFAULT_SYSTEM_PROMPT_DIRECTIVES["discord-format"] (TABLE-01..02)
affects:
  - src/manager/context-assembler.ts (downstream consumer — no code change, auto-picks up new entries)
  - src/config/loader.ts:resolveSystemPromptDirectives (downstream consumer — no code change)
  - All 14 fleet agents at next session boot (stable prefix gains 4 directives ~375 tokens, prompt-cache reseeds once)
tech-stack:
  added: []
  patterns: [Object.freeze immutability, static-grep canonical-phrase pin, additive-optional schema]
key-files:
  created: []
  modified:
    - src/config/__tests__/schema-system-prompt-directives.test.ts (+100 lines, -3 lines)
    - src/config/schema.ts (+69 lines, 1705 → 1774 lines)
decisions:
  - Used D-PLN-01 strict TDD pattern (RED → GREEN as 2 atomic commits)
  - Verbatim canonical phrases per D-FR-04/D-DR-03/D-TR-03/D-TB-03 (no paraphrasing)
  - Source-order: appended after propose-alternatives (resolver sorts alphabetically at runtime)
  - Did NOT touch clawcode.example.yaml (defaults baked in code, YAML overrides remain optional)
  - Did NOT add per-agent override fixture tests (REG-OVERRIDE-PARTIAL covers it generically)
  - Did NOT deploy (per D-PLN-02 + feedback_no_auto_deploy.md — operator authorizes separately)
metrics:
  duration: ~4 minutes
  completed: 2026-04-29
  tasks: 2
  commits: 2
---

# Phase 999.1 Plan 01: Agent Output Directives Summary

Landed 4 new agent-output directives — freshness, derivative-work, trusted-operator, discord-format — into the existing Phase 94 `DEFAULT_SYSTEM_PROMPT_DIRECTIVES` injection rail via strict TDD. Counter-instructs Claude Code default behaviors that misfire in this trusted-operator workspace (training-cutoff anchoring, over-applied "don't add features" rule on delegated work, gratuitous CYA disclaimers, unrenderable Discord markdown tables).

## What Was Built

**Test file** (`src/config/__tests__/schema-system-prompt-directives.test.ts`):
- 4 new `describe(...)` blocks with 3 assertions each (12 new assertions total):
  - `FRESH-DIR-{1,2,3}` — pins canonical phrase `"Do not anchor on training-cutoff knowledge"`
  - `DERIV-DIR-{1,2,3}` — pins canonical phrase `"are all in-scope work product"`
  - `TRUST-DIR-{1,2,3}` — pins canonical phrase `"Skip all CYA language"`
  - `TABLE-DIR-{1,2,3}` — pins canonical phrase `"prefer bullets, numbered lists, or definition-style prose"`
- Updated `REG-DEFAULTS-PRESENT` (line 43) — sorted-array literal extended from 7 keys to 11 keys
- Updated `REG-V25-BACKCOMPAT` (line 180) — sorted-array literal extended from 7 keys to 11 keys

**Schema source** (`src/config/schema.ts`):
- 4 new `Object.freeze({ enabled: true, text: "..." })` entries inside `DEFAULT_SYSTEM_PROMPT_DIRECTIVES`
- Each entry preceded by a comment block documenting operator-observed pain + canonical phrase pin
- Inserted after `propose-alternatives` (line 273), before record's closing `});`
- Source-order is for human readability; `resolveSystemPromptDirectives` (loader.ts:653) sorts alphabetically at runtime for prompt-cache hash stability

## Commits Landed

| Hash | Type | Message |
|------|------|---------|
| `4bbdf68` | RED | `test(999.1): RED — pin freshness/derivative-work/trusted-operator/discord-format directives + extend 11-key membership` |
| `da592a9` | GREEN | `feat(999.1): GREEN — land freshness/derivative-work/trusted-operator/discord-format directives` |

## Test Counts

- **RED commit (4bbdf68):** 14 failing assertions — `REG-DEFAULTS-PRESENT` (1), `REG-V25-BACKCOMPAT` (1), plus 12 directive assertions. 21 prior assertions still passed (existing 7-directive tests unchanged).
- **GREEN commit (da592a9):** 35 directive tests + 4 assembler-integration tests = **39 passed, 0 failed**.
- **Full suite:** 5560 passed / 36 failed across 440 test files. The 36 failures are pre-existing baseline (zero overlap with directive or systemPrompt subsystem — confirmed via `grep -iE "directive|systemPrompt"` against FAIL list).

## TypeScript Baseline

- **Before plan:** 108 errors
- **After RED commit:** 108 errors (test file changes type-check cleanly)
- **After GREEN commit:** 108 errors (4 entries conform to `systemPromptDirectiveSchema`)
- **Result:** Zero new TS errors introduced. Baseline preserved.

## Token Cost (D-TOK-01 verification)

| Directive | Chars | ~Tokens |
|-----------|-------|---------|
| `freshness` | 342 | ~85 |
| `derivative-work` | 360 | ~90 |
| `trusted-operator` | 356 | ~89 |
| `discord-format` | 444 | ~111 |
| **Total** | **~1,502** | **~375** |

Comfortably under D-TOK-01 budget (400-500 tokens). Cache-amortized to ~0/turn on prompt-cache hits. One cache-miss per agent on next session boot when sorted directive block changes order (acceptable per Phase 94 D-04 trade-off).

## Canonical-Phrase Uniqueness Check

Each phrase is unique to its directive's `text` field — no overlap with neighbors:

| Phrase | Directive Match | Schema-File Occurrences |
|--------|-----------------|--------------------------|
| `"Do not anchor on training-cutoff knowledge"` | `freshness` | 2 (1 comment + 1 text) |
| `"are all in-scope work product"` | `derivative-work` | 2 (1 comment + 1 text) |
| `"Skip all CYA language"` | `trusted-operator` | 2 (1 comment + 1 text) |
| `"prefer bullets, numbered lists, or definition-style prose"` | `discord-format` | 2 (1 comment + 1 text) |

Note: 2× per phrase = once in the leading comment block (`// "phrase" (D-XX-XX verbatim)` documentation pin) and once in the directive's `text` field. The static-grep tests use `.toContain(...)` against the `text` field specifically, so no false-positive risk — and each phrase is unique to its directive (no cross-directive collisions).

## Deviations from Plan

**None — plan executed exactly as written.**

All 4 directive `text` fields used the verbatim wording from RESEARCH.md §"Recommended Directive Text" (lines 315-359). All test blocks used the verbatim 3-assertion pattern from `memory-recall-before-uncertainty` (test file lines 73-97). All membership-array updates used the 11-key sorted list specified in PLAN.md `<interfaces>`.

## Success Criteria Check

- [x] FRESH-01: `freshness` entry exists with `enabled: true`
- [x] FRESH-02: Static-grep test pins "Do not anchor on training-cutoff knowledge" (FRESH-DIR-3 GREEN)
- [x] FRESH-03: Time-sensitive categories listed (`grep -c "anything dated within ~6 months" src/config/schema.ts` = 1)
- [x] DERIV-01: `derivative-work` entry exists with `enabled: true`
- [x] DERIV-02: Static-grep test pins "are all in-scope work product" (DERIV-DIR-3 GREEN)
- [x] DERIV-03: Scope-clarification text per D-DR-02 present (`grep -c "delegated a task by another agent OR by the operator" src/config/schema.ts` = 1)
- [x] TRUST-01: `trusted-operator` entry exists with `enabled: true`
- [x] TRUST-02: Static-grep test pins "Skip all CYA language" (TRUST-DIR-3 GREEN)
- [x] TABLE-01: `discord-format` entry exists with `enabled: true`
- [x] TABLE-02: Static-grep test pins "prefer bullets, numbered lists, or definition-style prose" (TABLE-DIR-3 GREEN)
- [x] Membership pins updated: REG-DEFAULTS-PRESENT + REG-V25-BACKCOMPAT both assert 11-key sorted array
- [x] No NEW vitest failures vs baseline; assembler integration test passes unchanged
- [x] No NEW TS errors vs 108 baseline
- [x] Two atomic commits landed in order: RED → GREEN
- [x] Token cost honored (~375 tokens, under 400-500 ceiling)

## Operator-Confirmed Deploy

**NOT included in this plan.** Per D-PLN-02 + saved feedback memory `feedback_no_auto_deploy.md`, production deploy requires explicit operator authorization in a separate turn. Local commits only. Operator will run `npm ci && npm run build` on `/opt/clawcode` and `systemctl restart clawcode` after explicitly authorizing.

## Deferred Follow-Ups

None — research confirmed architecture is locked-additive. No new follow-up items emerged during execution.

The deferred items already documented in CONTEXT.md remain deferred (per-agent SOUL.md template guidance, programmatic table-to-bullet rewriter, dynamic today-date injection in FRESH directive, DERIV opt-in-per-task) — none became blockers during this plan.

## Self-Check: PASSED

- [x] FOUND: src/config/__tests__/schema-system-prompt-directives.test.ts (modified, 401 lines)
- [x] FOUND: src/config/schema.ts (modified, 1774 lines)
- [x] FOUND: commit 4bbdf68 (RED)
- [x] FOUND: commit da592a9 (GREEN)
- [x] FOUND: 11 `Object.freeze` entries in DEFAULT_SYSTEM_PROMPT_DIRECTIVES
- [x] FOUND: All 4 canonical phrases each appear in schema.ts text field exactly once
- [x] PASSED: vitest 39/39 directive+assembler tests
- [x] PASSED: TS baseline 108 errors (no new)
