---
phase: 999.6
plan: "00"
subsystem: manager/snapshot-manager
tags: [tdd, wave-0, red, snapshot, daemon-lifecycle, autostart]
requires: []
provides:
  - "RED tests pinning snapshot-manager write/read/consume contract (SNAP-01..05)"
  - "RED tests pinning daemon.ts wiring (SNAP-WIRE-1..5)"
  - "RED tests pinning defaults.preDeploySnapshotMaxAgeHours schema field"
affects:
  - "src/manager/__tests__/ (new test files added)"
  - "src/config/__tests__/ (new sibling schema test)"
tech-stack:
  added: []
  patterns:
    - "Source-grep daemon wiring assertions (mirrors daemon-autoStart-boot-loop.test.ts)"
    - "Real-fs in mkdtemp() per-test for atomic-rename verification"
    - "Pino in-memory transport via Writable sink (mirrors daemon-boot-secrets-degraded.test.ts)"
    - "@ts-expect-error directive for not-yet-existent module imports"
key-files:
  created:
    - src/manager/__tests__/snapshot-manager.test.ts
    - src/manager/__tests__/daemon-snapshot-restore.test.ts
    - src/config/__tests__/schema-snapshot-defaults.test.ts
  modified: []
key-decisions:
  - "Real fs in os.tmpdir() over mocked fs/promises — atomic-rename proof requires real filesystem semantics; mock-based atomicity tests are theatre."
  - "Sibling schema-snapshot-defaults.test.ts file rather than augmenting an existing schema.test.ts — keeps the diff isolated and grep-able by phase, mirrors schema-autoStart.test.ts (Phase 100 follow-up)."
  - "Source-grep over end-to-end daemon boot test for wiring — daemon's runtime surface (Discord, MCP, embedder) needs ~30s + token; structural assertions catch contract drift in milliseconds and give Wave 1 unambiguous textual targets."
  - "@ts-expect-error directives placed inline (closing-line for multi-line imports; per-line for schema field accesses) so npm run typecheck survives the Wave 0 → Wave 1 gap. Vitest still fails at runtime — that's the intended RED."
requirements-completed: [SNAP-01, SNAP-02, SNAP-03, SNAP-04, SNAP-05]
duration: ~25 min
completed: "2026-04-30"
---

# Phase 999.6 Plan 00: Wave 0 RED — snapshot-manager + daemon wiring + schema field — Summary

**One-liner:** Three failing test files pin every behavioral contract for Phase 999.6 (snapshot write atomicity, read+consume idempotency, stale guard, daemon wiring, zod field) before Wave 1 implementation exists — Wave 1 GREEN cannot drift.

## Outcome

Wave 0 RED tests committed and verified failing. Three new test files anchor SNAP-01..05 in executable specifications. Wave 1 GREEN now has unambiguous targets:

1. **Module to create:** `src/manager/snapshot-manager.ts` exporting `writePreDeploySnapshot`, `readAndConsumePreDeploySnapshot`, `preDeploySnapshotSchema`, `DEFAULT_PRE_DEPLOY_SNAPSHOT_PATH`.
2. **Daemon wiring to add:** snapshot writer at top of `shutdown()` (before `manager.drain`), reader before `resolvedAgents.filter` (using `config.defaults.preDeploySnapshotMaxAgeHours`), import from `./snapshot-manager.js`.
3. **Schema field to add:** `preDeploySnapshotMaxAgeHours: z.number().int().positive().default(24).optional()` on `defaultsSchema` near line ~1340.

## Test Counts (per file)

| File | Lines | Test Cases | RED Result |
|------|-------|-----------|-----------|
| `src/manager/__tests__/snapshot-manager.test.ts` | 645 | 5 describe blocks, 22 `it()` cases | Suite fails at import-time (module not found) — entire file RED as designed |
| `src/manager/__tests__/daemon-snapshot-restore.test.ts` | 104 | 5 SNAP-WIRE assertions | All 5 fail (substrings absent from daemon.ts) |
| `src/config/__tests__/schema-snapshot-defaults.test.ts` | 87 | 7 zod-parse assertions | All 7 fail (field absent on defaultsSchema) |

**Combined RED suite:** 3 test files failed, 12 individual test failures (5 SNAP-WIRE + 7 schema; snapshot-manager.test.ts file-level import failure rolls up as 1 suite failure with no per-it count).

## Tasks Executed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | snapshot-manager unit RED tests (write/read/consume + edges) | `6d7168e` | src/manager/__tests__/snapshot-manager.test.ts |
| 2 | daemon source-grep wiring RED tests (SNAP-WIRE-1..5) | `6527610` | src/manager/__tests__/daemon-snapshot-restore.test.ts |
| 3 | schema field RED test (preDeploySnapshotMaxAgeHours) | `93f8720` | src/config/__tests__/schema-snapshot-defaults.test.ts |
| —  | typecheck fixes (`@ts-expect-error` directives) | `004fb24` | src/manager/__tests__/snapshot-manager.test.ts, src/config/__tests__/schema-snapshot-defaults.test.ts |

## RED-on-purpose Failure Modes (Wave 1 targets)

1. **Module not found** — `src/manager/__tests__/snapshot-manager.test.ts` throws `Cannot find module '../snapshot-manager.js'` at test-file load time. Wave 1 creates `src/manager/snapshot-manager.ts` with the exports listed in the plan's `<interfaces>` block. All 22 cases then execute against the real module.

2. **Wiring substrings absent** — `daemon-snapshot-restore.test.ts` runs but every `expect(DAEMON_SRC).toMatch(...)` / `indexOf(...) > 0` fails because `daemon.ts` contains none of:
   - `writePreDeploySnapshot(`
   - `readAndConsumePreDeploySnapshot(`
   - `config.defaults.preDeploySnapshotMaxAgeHours`
   - `manager.getRunningAgents()` in the writer's neighborhood
   - `from "./snapshot-manager"` import line
   Wave 1 adds these via the diff shapes in 999.6-RESEARCH.md Examples 4 + 5.

3. **Schema field absent** — `schema-snapshot-defaults.test.ts` runs but `result.data.preDeploySnapshotMaxAgeHours` is `undefined` on the existing `defaultsSchema`. Wave 1 adds the field at `src/config/schema.ts` ~line 1340 following the established `greetCoolDownMs` / `memoryFlushIntervalMs` blueprint.

## TypeScript Directive Inventory

Strict mode (`tsconfig.json` → `"strict": true`) flags the not-yet-existent module / not-yet-existent field as compile errors. Per the plan's success criteria ("if tsc errors, add `@ts-expect-error` directive"), the following directives were added so `npm run typecheck` passes:

| File | Line | Reason |
|------|------|--------|
| `src/manager/__tests__/snapshot-manager.test.ts` | inside import block, on the closing `} from "../snapshot-manager.js";` line | TS2307 (Cannot find module) fires on the `from` clause for multi-line imports — the directive must be the line immediately preceding the error site. Wave 1 removes this directive when the module exists. |
| `src/config/__tests__/schema-snapshot-defaults.test.ts` | 3× before each `result.data.preDeploySnapshotMaxAgeHours` access | TS2339 (Property does not exist) on the new field. Wave 1 removes these when the field is added to `defaultsSchema`. |

Inline annotation `(a: { name: string }) => a.name` was added in `snapshot-manager.test.ts` line 216 to silence the implicit-any cascade caused by the unresolved import. Wave 1 can keep or remove the annotation (it's compatible with the real `PreDeploySnapshot["runningAgents"][number]` shape).

## Verification

```bash
# Combined Wave 0 RED suite — 3 files, 12 individual failures, 1 import-time failure
npx vitest run \
  src/manager/__tests__/snapshot-manager.test.ts \
  src/manager/__tests__/daemon-snapshot-restore.test.ts \
  src/config/__tests__/schema-snapshot-defaults.test.ts
# Test Files: 3 failed (3) | Tests: 12 failed (12)

# Existing daemon source-grep test still green (no collateral damage)
npx vitest run src/manager/__tests__/daemon-autoStart-boot-loop.test.ts
# Test Files: 1 passed (1) | Tests: 4 passed (4)

# Typecheck on the three new files — no new errors from this plan
npx tsc --noEmit | grep -E "snapshot-manager|snapshot-defaults|snapshot-restore"
# (no output — clean)
```

## Collateral Damage Check

Pre-existing failures in the working tree (unrelated to this phase): 5 manager test files (`bootstrap-integration`, `daemon-openai`, `daemon-warmup-probe`, `dream-prompt-builder`, `restart-greeting`) and 2 config test files (`clawcode-yaml-phase100*`, `loader.test`, `schema.test PR11`) fail. Verified these failures exist on the working tree state (modified files from prior 999.10 / 999.11 / secrets work) — not introduced by Plan 999.6-00.

The neighbouring source-grep test that this phase deliberately mirrors (`daemon-autoStart-boot-loop.test.ts`, Phase 100 follow-up) remains green: 4/4 passed.

## Deviations from Plan

None — plan executed exactly as written. The only adjustment was the placement of the `@ts-expect-error` directive (the plan called for `// @ts-expect-error wave 0 — snapshot-manager not yet created` on the import line; TS reports TS2307 on the multi-line import's `from` clause, so the directive was placed on the closing line where it actually suppresses the error). This is a syntactic mechanic, not a behavioral change — the plan's success criterion ("`npm run typecheck` passes") is met.

## Authentication Gates

None — Wave 0 is pure local file authoring. No external services, secrets, or auth flows touched.

## Known Stubs

None — Wave 0 deliberately produces failing-by-design tests against a not-yet-existent module. This is the documented RED state, not a stub. Wave 1 GREEN will produce the implementation.

## Self-Check

**Files created (verified on disk):**
- ✅ `src/manager/__tests__/snapshot-manager.test.ts` (645 lines, ≥ min 200)
- ✅ `src/manager/__tests__/daemon-snapshot-restore.test.ts` (104 lines, ≥ min 60)
- ✅ `src/config/__tests__/schema-snapshot-defaults.test.ts` (87 lines, ≥ min 30)

**Commits in git log:**
- ✅ `6d7168e` — test(999.6-00): wave 0 RED — snapshot-manager unit tests (SNAP-01..05)
- ✅ `6527610` — test(999.6-00): wave 0 RED — daemon source-grep wiring (SNAP-WIRE-1..5)
- ✅ `93f8720` — test(999.6-00): wave 0 RED — defaults.preDeploySnapshotMaxAgeHours schema (SNAP-04)
- ✅ `004fb24` — fix(999.6-00): @ts-expect-error directives so tsc survives wave 0 RED gap

**RED state verified:**
- ✅ snapshot-manager.test.ts → import-time module-not-found
- ✅ daemon-snapshot-restore.test.ts → all 5 SNAP-WIRE assertions fail
- ✅ schema-snapshot-defaults.test.ts → all 7 schema assertions fail

**No collateral damage:**
- ✅ daemon-autoStart-boot-loop.test.ts still 4/4 green
- ✅ Pre-existing test failures (bootstrap, daemon-openai, etc.) unchanged — not introduced by this plan

## Self-Check: PASSED

## Next

Ready for Wave 1 GREEN (Plan 999.6-01) — implements `src/manager/snapshot-manager.ts` and turns all RED tests in this plan green.
