---
phase: 07-memory-relevance-deduplication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/memory/schema.ts
  - src/memory/decay.ts
  - src/memory/relevance.ts
  - src/memory/__tests__/decay.test.ts
  - src/memory/__tests__/relevance.test.ts
autonomous: true
requirements: [AMEM-04, AMEM-05]

must_haves:
  truths:
    - "A memory not accessed for 60 days has a lower relevance score than one accessed today, given equal importance"
    - "Relevance decay follows exponential half-life formula with configurable halfLifeDays"
    - "Combined scoring re-ranks search results using semantic similarity * 0.7 + relevance * 0.3"
    - "Config schema accepts decay and deduplication settings with sensible defaults"
  artifacts:
    - path: "src/memory/decay.ts"
      provides: "calculateRelevanceScore pure function"
      exports: ["calculateRelevanceScore"]
    - path: "src/memory/relevance.ts"
      provides: "scoreAndRank re-ranking function, distanceToSimilarity helper"
      exports: ["scoreAndRank", "distanceToSimilarity"]
    - path: "src/memory/schema.ts"
      provides: "decayConfigSchema, dedupConfigSchema added to memoryConfigSchema"
      contains: "decayConfigSchema"
    - path: "src/memory/__tests__/decay.test.ts"
      provides: "Unit tests for decay function"
    - path: "src/memory/__tests__/relevance.test.ts"
      provides: "Unit tests for combined scoring"
  key_links:
    - from: "src/memory/relevance.ts"
      to: "src/memory/decay.ts"
      via: "import calculateRelevanceScore"
      pattern: "import.*calculateRelevanceScore.*from.*decay"
    - from: "src/memory/schema.ts"
      to: "memoryConfigSchema"
      via: "decay and deduplication fields added"
      pattern: "decay.*decayConfigSchema"
---

<objective>
Decay scoring, combined relevance ranking, and config schema extensions for Phase 7.

Purpose: Establish the pure-function scoring layer that search integration (Plan 03) will wire in. Config schema changes provide the settings both decay and dedup features need.
Output: decay.ts, relevance.ts with tests; schema.ts extended with decay + dedup config sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-memory-relevance-deduplication/07-CONTEXT.md
@.planning/phases/07-memory-relevance-deduplication/07-RESEARCH.md

@src/memory/schema.ts
@src/memory/types.ts
@src/memory/search.ts
@src/config/schema.ts

<interfaces>
<!-- Key types the executor needs -->

From src/memory/types.ts:
```typescript
export type MemoryEntry = {
  readonly id: string;
  readonly content: string;
  readonly source: MemorySource;
  readonly importance: number;
  readonly accessCount: number;
  readonly tags: readonly string[];
  readonly embedding: Float32Array | null;
  readonly createdAt: string;
  readonly updatedAt: string;
  readonly accessedAt: string;
};

export type SearchResult = MemoryEntry & {
  readonly distance: number;
};
```

From src/memory/schema.ts:
```typescript
export const memoryConfigSchema = z.object({
  compactionThreshold: z.number().min(0).max(1).default(0.75),
  searchTopK: z.number().int().min(1).default(10),
  consolidation: consolidationConfigSchema.default(() => ({
    enabled: true,
    weeklyThreshold: 7,
    monthlyThreshold: 4,
  })),
});
```

From src/config/schema.ts:
```typescript
export const defaultsSchema = z.object({
  // ...
  memory: memorySchema.default(() => ({
    compactionThreshold: 0.75,
    searchTopK: 10,
    consolidation: { enabled: true, weeklyThreshold: 7, monthlyThreshold: 4 },
  })),
  // ...
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config schema extensions, decay function, and decay tests</name>
  <read_first>src/memory/schema.ts, src/config/schema.ts, src/memory/types.ts, src/shared/types.ts</read_first>
  <files>src/memory/schema.ts, src/config/schema.ts, src/shared/types.ts, src/memory/decay.ts, src/memory/__tests__/decay.test.ts</files>
  <behavior>
    - calculateRelevanceScore(importance=0.8, accessedAt=now, now, {halfLifeDays:30}) returns 0.8 (no decay)
    - calculateRelevanceScore(importance=0.8, accessedAt=30daysAgo, now, {halfLifeDays:30}) returns ~0.4 (half-life)
    - calculateRelevanceScore(importance=0.8, accessedAt=60daysAgo, now, {halfLifeDays:30}) returns ~0.2 (two half-lives)
    - calculateRelevanceScore(importance=1.0, accessedAt=0daysAgo, now, {halfLifeDays:30}) returns 1.0 (max)
    - calculateRelevanceScore with future accessedAt returns importance unchanged (no negative decay)
    - Result is always clamped to [0, 1]
    - decayConfigSchema validates halfLifeDays >= 1, semanticWeight 0-1, decayWeight 0-1, defaults to 30/0.7/0.3
    - dedupConfigSchema validates enabled boolean, similarityThreshold 0-1, defaults to true/0.85
  </behavior>
  <action>
    1. Extend src/memory/schema.ts (per D-01 through D-08):
       - Add `decayConfigSchema = z.object({ halfLifeDays: z.number().int().min(1).default(30), semanticWeight: z.number().min(0).max(1).default(0.7), decayWeight: z.number().min(0).max(1).default(0.3) })`
       - Add `dedupConfigSchema = z.object({ enabled: z.boolean().default(true), similarityThreshold: z.number().min(0).max(1).default(0.85) })`
       - Add both to memoryConfigSchema: `decay: decayConfigSchema.default(...)` and `deduplication: dedupConfigSchema.default(...)`
       - Export both new schemas and their inferred types: `DecayConfig`, `DedupConfig`

    2. Update src/config/schema.ts defaults to include decay and deduplication defaults in the `defaultsSchema.memory` default value.

    3. Update src/shared/types.ts ResolvedAgentConfig.memory type to include decay and deduplication fields (same pattern as consolidation addition in Phase 06).

    4. Create src/memory/decay.ts (per D-02, D-04):
       - Export `DecayParams` type: `{ readonly halfLifeDays: number }`
       - Export `calculateRelevanceScore(importance: number, accessedAt: string, now: Date, config: DecayParams): number`
       - Formula: `importance * Math.pow(0.5, daysSinceAccess / config.halfLifeDays)` (per D-02)
       - Guard: if daysSinceAccess <= 0, return importance (handles future dates)
       - Clamp result to [0, 1] with Math.max(0, Math.min(1, ...))

    5. Create src/memory/__tests__/decay.test.ts:
       - Test exact half-life behavior at 30 days
       - Test double half-life at 60 days
       - Test no decay when just accessed
       - Test clamping
       - Test future accessedAt edge case
       - Test schema validation (halfLifeDays min, weight bounds, defaults)
  </action>
  <acceptance_criteria>
    - grep -q "decayConfigSchema" src/memory/schema.ts
    - grep -q "dedupConfigSchema" src/memory/schema.ts
    - grep -q "calculateRelevanceScore" src/memory/decay.ts
    - grep -q "Math.pow(0.5" src/memory/decay.ts
    - grep -q "halfLifeDays" src/memory/__tests__/decay.test.ts
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/memory/__tests__/decay.test.ts --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>Decay function passes all unit tests. Config schemas parse with defaults. TypeScript compiles clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Combined scoring function and relevance tests</name>
  <read_first>src/memory/decay.ts, src/memory/types.ts, src/memory/search.ts</read_first>
  <files>src/memory/relevance.ts, src/memory/__tests__/relevance.test.ts</files>
  <behavior>
    - distanceToSimilarity(0) returns 1.0 (identical)
    - distanceToSimilarity(1) returns 0.0 (orthogonal)
    - distanceToSimilarity(2) returns 0.0 (opposite, clamped)
    - distanceToSimilarity(0.3) returns 0.7
    - scoreAndRank with two results: one recently accessed (high relevance) and one stale (low relevance), both same distance -- recently accessed ranks first
    - scoreAndRank with default weights (0.7 semantic + 0.3 relevance) produces expected combined scores
    - scoreAndRank returns frozen array of frozen objects with relevanceScore and combinedScore fields
    - scoreAndRank with empty input returns empty frozen array
  </behavior>
  <action>
    1. Create src/memory/relevance.ts:
       - Import `calculateRelevanceScore` from `./decay.js`
       - Import `SearchResult` from `./types.js`
       - Import `DecayConfig` from `./schema.js`
       - Export `distanceToSimilarity(distance: number): number` -- `Math.max(0, 1 - distance)`
       - Export `RankedSearchResult` type: `SearchResult & { readonly relevanceScore: number; readonly combinedScore: number }`
       - Export `ScoringConfig` type: `{ readonly semanticWeight: number; readonly decayWeight: number; readonly halfLifeDays: number }`
       - Export `scoreAndRank(results: readonly SearchResult[], config: ScoringConfig, now: Date): readonly RankedSearchResult[]`
         - Map each result: compute `semanticSimilarity = distanceToSimilarity(result.distance)`, `relevanceScore = calculateRelevanceScore(result.importance, result.accessedAt, now, { halfLifeDays: config.halfLifeDays })`, `combinedScore = semanticSimilarity * config.semanticWeight + relevanceScore * config.decayWeight`
         - Sort descending by combinedScore
         - Return Object.freeze(sorted.map(r => Object.freeze(r)))
       - All returned objects must be frozen (project immutability convention)

    2. Create src/memory/__tests__/relevance.test.ts:
       - Test distanceToSimilarity edge cases (0, 1, 2, negative clamping)
       - Test scoreAndRank re-ordering: create two SearchResult objects with same cosine distance but different accessedAt dates. Verify recently accessed one ranks higher after scoring.
       - Test scoreAndRank with custom weights
       - Test empty input
       - Test that returned objects are frozen
       - Use helper functions from existing test patterns (manual SearchResult construction, not MemoryStore)
  </action>
  <acceptance_criteria>
    - grep -q "distanceToSimilarity" src/memory/relevance.ts
    - grep -q "scoreAndRank" src/memory/relevance.ts
    - grep -q "combinedScore" src/memory/relevance.ts
    - grep -q "RankedSearchResult" src/memory/relevance.ts
    - grep -q "scoreAndRank" src/memory/__tests__/relevance.test.ts
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/memory/__tests__/relevance.test.ts --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>scoreAndRank correctly re-ranks results by combined semantic+relevance score. distanceToSimilarity handles edge cases. All tests pass, TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
npx vitest run src/memory/__tests__/decay.test.ts src/memory/__tests__/relevance.test.ts --reporter=verbose
npx tsc --noEmit
</verification>

<success_criteria>
- calculateRelevanceScore returns ~0.4 for a memory at exactly one half-life (30 days)
- scoreAndRank re-orders results so recently accessed memories outrank stale ones
- Config schemas validate with defaults and reject invalid values (halfLifeDays < 1)
- All new code follows immutability convention (Object.freeze)
- Zero new dependencies added
</success_criteria>

<output>
After completion, create `.planning/phases/07-memory-relevance-deduplication/07-01-SUMMARY.md`
</output>
