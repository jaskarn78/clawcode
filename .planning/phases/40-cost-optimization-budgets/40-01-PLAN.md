---
phase: 40-cost-optimization-budgets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/usage/pricing.ts
  - src/usage/tracker.ts
  - src/usage/types.ts
  - src/cli/commands/costs.ts
  - src/cli/commands/costs.test.ts
  - src/dashboard/server.ts
  - src/memory/importance.ts
  - src/memory/importance.test.ts
  - src/memory/store.ts
  - src/memory/search.ts
autonomous: true
requirements: [COST-01, COST-02]

must_haves:
  truths:
    - "clawcode costs shows per-agent/per-model token usage with USD cost estimates"
    - "Dashboard /api/costs endpoint returns per-agent cost JSON"
    - "New memories inserted via MemoryStore.insert() receive automatic importance scores (0.0-1.0)"
    - "SemanticSearch results are weighted by importance score"
  artifacts:
    - path: "src/usage/pricing.ts"
      provides: "Hardcoded price-per-token map for haiku/sonnet/opus"
      exports: ["MODEL_PRICING", "estimateCost"]
    - path: "src/cli/commands/costs.ts"
      provides: "CLI costs command with --period and --agent flags"
      exports: ["registerCostsCommand"]
    - path: "src/memory/importance.ts"
      provides: "Pure importance scoring function"
      exports: ["calculateImportance"]
  key_links:
    - from: "src/cli/commands/costs.ts"
      to: "src/usage/tracker.ts"
      via: "IPC request to daemon which calls UsageTracker"
      pattern: "sendIpcRequest.*costs"
    - from: "src/memory/store.ts"
      to: "src/memory/importance.ts"
      via: "calculateImportance called in insert()"
      pattern: "calculateImportance"
    - from: "src/memory/search.ts"
      to: "importance"
      via: "importance weighting in score calculation"
      pattern: "importance"
---

<objective>
Add per-agent/per-model cost tracking with CLI and dashboard visibility, plus automatic importance scoring for new memories.

Purpose: Give operators cost awareness across the agent fleet and improve memory retrieval by scoring memory importance on insert.
Output: `clawcode costs` CLI, dashboard /api/costs endpoint, importance scoring on memory insert, importance-weighted search.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-cost-optimization-budgets/40-CONTEXT.md
@.planning/phases/40-cost-optimization-budgets/40-RESEARCH.md

@src/usage/tracker.ts
@src/usage/types.ts
@src/cli/commands/usage.ts
@src/memory/store.ts
@src/memory/search.ts
@src/memory/relevance.ts
@src/dashboard/server.ts

<interfaces>
From src/usage/types.ts:
```typescript
export type UsageEvent = {
  readonly id: string;
  readonly agent: string;
  readonly timestamp: string;
  readonly tokens_in: number;
  readonly tokens_out: number;
  readonly cost_usd: number;
  readonly turns: number;
  readonly model: string;
  readonly duration_ms: number;
  readonly session_id: string;
};

export type UsageAggregate = {
  readonly tokens_in: number;
  readonly tokens_out: number;
  readonly cost_usd: number;
  readonly turns: number;
  readonly duration_ms: number;
  readonly event_count: number;
};
```

From src/cli/commands/usage.ts (pattern to follow):
```typescript
export function registerUsageCommand(program: Command): void {
  program
    .command("usage <agent>")
    .option("--period <period>", ...)
    .action(async (agent, opts) => {
      const result = await sendIpcRequest(SOCKET_PATH, "usage", { ... });
      cliLog(formatUsageTable(result));
    });
}
```

From src/memory/search.ts:
```typescript
export class SemanticSearch {
  search(queryEmbedding: Float32Array, topK: number): readonly RankedSearchResult[]
}
```

From src/memory/relevance.ts:
```typescript
export function scoreAndRank(results: readonly SearchResult[], config: ScoringConfig, now: Date): RankedSearchResult[]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pricing map, cost aggregate queries, importance scorer</name>
  <files>src/usage/pricing.ts, src/usage/types.ts, src/usage/tracker.ts, src/memory/importance.ts, src/memory/importance.test.ts, src/cli/commands/costs.test.ts</files>
  <read_first>src/usage/tracker.ts, src/usage/types.ts, src/cli/commands/usage.ts, src/memory/relevance.ts</read_first>
  <behavior>
    - estimateCost("haiku", 1000000, 0) returns 0.25
    - estimateCost("sonnet", 0, 1000000) returns 15.0
    - estimateCost("unknown-model", 1000, 1000) returns 0
    - getCostsByAgentModel with period "today" returns rows grouped by agent+model with sum of tokens_in, tokens_out, cost_usd
    - calculateImportance("short") returns low score (< 0.3)
    - calculateImportance(longTextWithCodeAndProperNouns) returns high score (> 0.6)
    - calculateImportance always returns value between 0.0 and 1.0
    - calculateImportance of empty string returns 0.2 (recency boost only)
  </behavior>
  <action>
    1. Create `src/usage/pricing.ts`: Export `MODEL_PRICING` record (haiku: 0.25/1.25, sonnet: 3.0/15.0, opus: 15.0/75.0 per million tokens) and `estimateCost(model, tokensIn, tokensOut): number` function. Add `ModelPricing` type. Include comment with "Last verified: 2026-04-10".

    2. Extend `src/usage/types.ts`: Add `CostByAgentModel` type with fields: agent, model, tokens_in, tokens_out, cost_usd (all readonly).

    3. Extend `src/usage/tracker.ts`: Add `getCostsByAgentModel(startTime: string, endTime: string): readonly CostByAgentModel[]` method. Uses a prepared statement that GROUP BY agent, model with SUM of tokens_in, tokens_out, cost_usd filtered by timestamp >= startTime AND timestamp < endTime. Add the prepared statement to PreparedStatements type and prepareStatements().

    4. Create `src/memory/importance.ts`: Export `calculateImportance(content: string): number`. Formula:
       - lengthScore = min(content.length / 500, 0.3)
       - entityScore = min(codeBlocks*0.1 + numbers*0.02 + properNouns*0.03, 0.4)
       - recencyBoost = 0.2 (always applied on insert)
       - return clamp(lengthScore + entityScore + recencyBoost, 0, 1)
       Use matchAll for regex extraction (per Phase 36 decision).

    5. Create `src/memory/importance.test.ts`: Test calculateImportance with empty string, short text, long text with entities, code blocks, boundary cases (ensures 0-1 range).

    6. Create `src/cli/commands/costs.test.ts`: Test formatCostsTable with mock data, verify table output includes agent/model/tokens/cost columns.
  </action>
  <verify>
    <automated>npx vitest run src/memory/importance.test.ts src/cli/commands/costs.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function estimateCost" src/usage/pricing.ts
    - grep -q "export const MODEL_PRICING" src/usage/pricing.ts
    - grep -q "getCostsByAgentModel" src/usage/tracker.ts
    - grep -q "GROUP BY agent, model" src/usage/tracker.ts
    - grep -q "export function calculateImportance" src/memory/importance.ts
    - grep -q "CostByAgentModel" src/usage/types.ts
  </acceptance_criteria>
  <done>Pricing map exports MODEL_PRICING and estimateCost. UsageTracker has getCostsByAgentModel method. Importance scorer passes all test cases with scores in 0-1 range.</done>
</task>

<task type="auto">
  <name>Task 2: CLI costs command, dashboard endpoint, importance wiring</name>
  <files>src/cli/commands/costs.ts, src/dashboard/server.ts, src/memory/store.ts, src/memory/search.ts</files>
  <read_first>src/cli/commands/usage.ts, src/dashboard/server.ts, src/memory/store.ts, src/memory/search.ts, src/memory/relevance.ts</read_first>
  <action>
    1. Create `src/cli/commands/costs.ts`: Following the exact pattern of usage.ts —
       - `registerCostsCommand(program: Command)` registers `clawcode costs` with options `--period <period>` (today|week|month, default "today") and `--agent <name>` (optional filter).
       - Sends IPC request `"costs"` with `{ period, agent }`.
       - `formatCostsTable(rows: CostByAgentModel[]): string` formats as aligned table with columns: Agent, Model, Tokens In, Tokens Out, Cost (USD). Use fixed-width formatting. Show total row at bottom.
       - Handle ManagerNotRunningError same as usage.ts.

    2. Extend `src/dashboard/server.ts`: Add `/api/costs` GET endpoint that:
       - Accepts `?period=today|week|month` query param (default "today").
       - Calls UsageTracker.getCostsByAgentModel with computed time range.
       - Returns JSON array of CostByAgentModel objects.
       - Wire into existing SSE stream as "costs" event type for live dashboard updates.

    3. Modify `src/memory/store.ts`: In the `insert()` method, if importance is not explicitly provided in CreateMemoryInput (or is the default 0.5), call `calculateImportance(content)` from `src/memory/importance.ts` to compute it. Import calculateImportance at the top. Existing memories keep their scores (no migration).

    4. Modify `src/memory/search.ts`: In the `search()` method, after scoreAndRank produces ranked results, apply importance weighting. Use multiplicative boost: `combinedScore * (0.7 + 0.3 * importance)` — this ensures high importance boosts ranking but cannot override strong semantic matches. Apply before the final topK slice.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "registerCostsCommand" src/cli/commands/costs.ts
    - grep -q "formatCostsTable" src/cli/commands/costs.ts
    - grep -q "/api/costs" src/dashboard/server.ts
    - grep -q "calculateImportance" src/memory/store.ts
    - grep -q "importance" src/memory/search.ts
    - grep -q "0.7 + 0.3" src/memory/search.ts
  </acceptance_criteria>
  <done>CLI `clawcode costs` command registered with period/agent flags. Dashboard serves /api/costs endpoint. MemoryStore.insert() auto-calculates importance. SemanticSearch applies importance weighting with multiplicative boost. Full test suite passes.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` — all tests pass
- `grep -r "registerCostsCommand" src/cli/` — command is registered
- `grep "calculateImportance" src/memory/store.ts` — importance wired into insert
- `grep "getCostsByAgentModel" src/usage/tracker.ts` — aggregate query exists
</verification>

<success_criteria>
- `clawcode costs` CLI command exists with --period and --agent options
- Dashboard /api/costs endpoint returns per-agent/per-model cost JSON
- MemoryStore.insert() calculates importance automatically using deterministic heuristics
- SemanticSearch results are boosted by importance score (multiplicative weighting)
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/40-cost-optimization-budgets/40-01-SUMMARY.md`
</output>
