---
phase: 106
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/discord/__tests__/subagent-delegates-scoping.test.ts
  - src/manager/__tests__/session-manager-warmup-timeout.test.ts
  - src/ipc/__tests__/protocol.test.ts
autonomous: true
requirements:
  - DSCOPE-02
  - DSCOPE-03
  - STALL-02
  - TRACK-CLI-01

must_haves:
  truths:
    - "RED test fails because subagentConfig still spreads `delegates` from sourceConfig"
    - "RED test fails because no warmup-timeout sentinel exists in startAgent"
    - "RED test fails because `mcp-tracker-snapshot` is missing from IPC_METHODS expected tuple"
    - "All other existing tests stay green"
  artifacts:
    - path: "src/discord/__tests__/subagent-delegates-scoping.test.ts"
      provides: "DSCOPE-02 RED snapshot — spawned subagent config does NOT carry `delegates`"
      contains: "DSCOPE"
    - path: "src/manager/__tests__/session-manager-warmup-timeout.test.ts"
      provides: "STALL-02 RED — fake-timer test for 60s warmup-timeout sentinel emitting structured pino-warn"
      contains: "warmup-timeout"
    - path: "src/ipc/__tests__/protocol.test.ts"
      provides: "TRACK-CLI-01 RED — expected tuple updated to include `mcp-tracker-snapshot`"
      contains: "mcp-tracker-snapshot"
  key_links:
    - from: "src/discord/__tests__/subagent-delegates-scoping.test.ts"
      to: "src/discord/subagent-thread-spawner.ts"
      via: "spawning a subagent and asserting captured ResolvedAgentConfig has no `delegates` field"
      pattern: "expect.*subagentConfig.*delegates.*toBeUndefined"
    - from: "src/manager/__tests__/session-manager-warmup-timeout.test.ts"
      to: "src/manager/session-manager.ts"
      via: "vitest fake timers + spy on log.warn"
      pattern: "vi\\.useFakeTimers|advanceTimersByTime"
    - from: "src/ipc/__tests__/protocol.test.ts"
      to: "src/ipc/protocol.ts"
      via: "expect(IPC_METHODS).toEqual([...,'mcp-tracker-snapshot'])"
      pattern: "mcp-tracker-snapshot"
---

<objective>
Wave 0 RED — write failing tests that lock in the three behaviors before any production code changes. Tests MUST fail today (the bugs are still there) and turn GREEN exactly when the corresponding Wave 1 fix lands.

Purpose: Nyquist-compliant feedback. Each Wave 1 GREEN plan has a pre-existing automated `<verify>` command pointing to one of these test files; that command is RED before the fix and GREEN after.

Output: Two new test files (`subagent-delegates-scoping.test.ts`, `session-manager-warmup-timeout.test.ts`) + one extended expected-tuple in `protocol.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/106-agent-context-hygiene-bundle-delegate-scoping-research-stall-cli-hotfix/106-CONTEXT.md
@.planning/phases/106-agent-context-hygiene-bundle-delegate-scoping-research-stall-cli-hotfix/106-RESEARCH.md

@src/discord/subagent-thread-spawner.ts
@src/manager/session-manager.ts
@src/ipc/protocol.ts
@src/ipc/__tests__/protocol.test.ts

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase via grep. -->
<!-- Use these directly — no codebase exploration needed. -->

From src/ipc/protocol.ts (current state, end of IPC_METHODS tuple):
```typescript
  // Phase 104 SEC-06 — daemon-side op:// secret cache telemetry +
  // operator-driven cache invalidation surface.
  "secrets-status",
  "secrets-invalidate",
] as const;

export type IpcMethod = (typeof IPC_METHODS)[number];
```

From src/ipc/__tests__/protocol.test.ts:140-153 (current expected list, the test we extend):
```typescript
  "secrets-status",
  "secrets-invalidate",
]);
```

From src/discord/subagent-thread-spawner.ts:454-465 (the spread we'll lock against):
```typescript
const subagentConfig: ResolvedAgentConfig = {
  ...sourceConfig,
  name: sessionName,
  model,
  channels: [],
  soul: (config.systemPrompt ?? sourceConfig.soul ?? "") + threadContext,
  schedules: [],
  slashCommands: [],
  webhook,
  threads: parentConfig.threads,
  disallowedTools: ["mcp__clawcode__spawn_subagent_thread"],
};
await this.sessionManager.startAgent(sessionName, subagentConfig);
```

From src/manager/session-manager.ts:988 (the log line that signals "warm-path ready"):
```typescript
this.log.info({ ... }, "warm-path ready — agent started");
```

The STALL-02 sentinel will fire `this.log.warn(fields, "agent warmup-timeout — boot stalled, no warm-path-ready")` if 60s elapses without that log. RED test asserts:
1. With fake timers + a `startAgent` mock that never resolves (or resolves after >60s): warn IS called.
2. With normal startAgent that resolves in <60s: warn is NOT called.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write DSCOPE RED test — subagent config strips `delegates`</name>
  <files>src/discord/__tests__/subagent-delegates-scoping.test.ts</files>
  <behavior>
    - Test 1 ("DSCOPE: spawned subagent config does not carry delegates"): construct a SubagentThreadSpawner with a parent config that has `delegates: { research: "fin-research" }` and a sessionManager mock that captures the `subagentConfig` argument to `startAgent`. Trigger spawn. Assert captured `subagentConfig.delegates` is undefined.
    - Test 2 ("DSCOPE: regression — sourceConfig delegates remain unmodified after spawn"): same setup; assert that `sourceConfig.delegates` (the parent's config) is still `{ research: "fin-research" }` after spawn. The strip is destructure-only, must not mutate the source.
  </behavior>
  <action>
    Per DSCOPE-02 / DSCOPE-03, create `src/discord/__tests__/subagent-delegates-scoping.test.ts`. Mirror the test infrastructure in `src/discord/__tests__/bridge-turn-dispatcher.test.ts` (same area).

    Locate the SubagentThreadSpawner class export in `src/discord/subagent-thread-spawner.ts` and find its `spawn` (or equivalent) entry point used by `mcp__clawcode__spawn_subagent_thread`. Build a minimal harness:
    1. Mock `sessionManager` with a vi.fn() captor for `startAgent(sessionName, subagentConfig)`.
    2. Pass a `parentConfig` / `sourceConfig` shape that includes `delegates: { research: "fin-research" }` and other required fields (use a fixture matching `ResolvedAgentConfig`).
    3. Call the spawn entry point with whatever args the caller in daemon.ts passes (look at `daemon.ts` callers of `subagentSpawner.spawn` to mirror).
    4. Assert `startAgent.mock.calls[0][1].delegates` is `undefined`.
    5. Assert the parent `sourceConfig.delegates` is unchanged (`toEqual({ research: "fin-research" })`).

    These tests MUST fail against current code (the spread carries `delegates`). Verify locally that they fail before committing.

    Reference test at `src/manager/__tests__/context-assembler.test.ts:1240+` for fixture shape conventions.

    No production code edits in this task — pure RED.
  </action>
  <verify>
    <automated>npx vitest run src/discord/__tests__/subagent-delegates-scoping.test.ts 2>&1 | grep -E "FAIL|✗|expected.*undefined" | head -5</automated>
  </verify>
  <done>
    File exists. Both tests RED (fail). Failure mode is clearly "subagentConfig still has delegates field" — not a setup error or import error. Other test files unaffected (`npm test` shows only the 2 new failures).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write STALL-02 RED test — warmup-timeout sentinel</name>
  <files>src/manager/__tests__/session-manager-warmup-timeout.test.ts</files>
  <behavior>
    - Test 1 ("STALL-02: warmup-timeout fires at 60s when warm-path never reached"): spy on the SessionManager's pino logger; install vitest fake timers; invoke `startAgent` with an adapter mock whose `createSession` returns a never-resolving promise (simulating SDK hang). Advance timers by 60_001ms. Assert `log.warn` was called with the structured payload `{ agent, elapsedMs: 60000, lastStep, mcpServersConfigured, mcpServersLoaded, mcpServersPending }` and message `"agent warmup-timeout — boot stalled, no warm-path-ready"`.
    - Test 2 ("STALL-02: sentinel cleared on warm-path-ready"): same harness, but adapter resolves quickly and warm-path probe succeeds within 5s simulated. Advance timers past 60s. Assert `log.warn` was NEVER called with the warmup-timeout message.
    - Test 3 ("STALL-02: lastStep reports adapter-create-session when adapter hangs"): same as Test 1 but assert `log.warn.mock.calls[0][0].lastStep === "adapter-create-session"`.
  </behavior>
  <action>
    Per STALL-02, create `src/manager/__tests__/session-manager-warmup-timeout.test.ts`. Reference existing session-manager test scaffolding in `src/manager/__tests__/daemon-autoStart-boot-loop.test.ts` and `src/manager/__tests__/agent-provisioner.test.ts` for SessionManager construction patterns.

    Setup:
    1. `vi.useFakeTimers()` in beforeEach; `vi.useRealTimers()` in afterEach.
    2. Construct SessionManager with a stub pino logger captured via `vi.spyOn(logger, "warn")`.
    3. Stub adapter.createSession with a vi.fn() returning either a never-resolving promise (Test 1, 3) or an immediate resolution with a fake handle (Test 2).
    4. Stub the warm-path machinery so it either never fires (Test 1, 3) or fires immediately (Test 2). Look at `src/manager/warm-path-check.ts` exports and stub appropriately.
    5. Provide a config with `mcpServers: [{ name: "brave-search" }, { name: "playwright" }]` to populate `mcpServersConfigured`.

    Test 1: `await flushPromises(); vi.advanceTimersByTime(60_001); await flushPromises();` then assert `log.warn` called once with the expected structured payload. Use `expect.objectContaining({ agent: "research", elapsedMs: 60_000, lastStep: expect.any(String), mcpServersConfigured: ["brave-search", "playwright"] })` for the first arg, and the message string for the second.

    Test 2: drive the path to warm-path-ready, advance 60_001ms, assert `log.warn` NOT called with the warmup message (use `expect(log.warn).not.toHaveBeenCalledWith(expect.anything(), "agent warmup-timeout — boot stalled, no warm-path-ready")`).

    Test 3: assert `lastStep === "adapter-create-session"` since the hang is at `adapter.createSession`.

    These tests MUST fail against current code (no sentinel exists). Verify locally.

    No production code edits in this task — pure RED.
  </action>
  <verify>
    <automated>npx vitest run src/manager/__tests__/session-manager-warmup-timeout.test.ts 2>&1 | grep -E "FAIL|✗|warmup-timeout" | head -10</automated>
  </verify>
  <done>
    File exists. All 3 tests RED. Failure mode is "log.warn was not called" / "expected warmup-timeout, received nothing" — not a setup error. Other tests unaffected.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend protocol.test.ts expected tuple — TRACK-CLI RED</name>
  <files>src/ipc/__tests__/protocol.test.ts</files>
  <behavior>
    - Test 1 ("includes all required methods"): expected tuple now includes `"mcp-tracker-snapshot"` at the position after `"secrets-invalidate"`.
    - Test 2 ("ipcRequestSchema accepts all valid methods"): if there's an iteration over IPC_METHODS to validate each is acceptable, ensure mcp-tracker-snapshot is exercised (likely already automatic since it iterates the enum).
  </behavior>
  <action>
    Per TRACK-CLI-01 with direct precedent commit `a9c39c7` (96-05 fix for probe-fs/list-fs-status).

    Edit `src/ipc/__tests__/protocol.test.ts` at line 152 (the closing of the expected array following `"secrets-invalidate"`). Append the new entry inside the expected list:

    ```typescript
          "secrets-status",
          "secrets-invalidate",
          // Phase 106 TRACK-CLI-01 — restore mcp-tracker IPC. Daemon
          // dispatch + CLI client wired in 999.15-03; this entry was
          // missed (deploy-blocking "Invalid Request"). Mirrors commit
          // a9c39c7 fix for probe-fs / list-fs-status.
          "mcp-tracker-snapshot",
        ]);
    ```

    Do NOT touch `src/ipc/protocol.ts` in this task. The point is to make the test RED (expect tuple now demands an entry that doesn't exist yet in the source enum). When Plan 03 adds the entry to `protocol.ts`, this test turns GREEN.

    Verify the failure is "expected array to deeply equal" (test asserts current tuple vs new expected — current is missing the entry).
  </action>
  <verify>
    <automated>npx vitest run src/ipc/__tests__/protocol.test.ts 2>&1 | grep -E "FAIL|✗|expected.*equal" | head -5</automated>
  </verify>
  <done>
    Expected list updated. Test RED with "expected array to deeply equal" diff showing missing `"mcp-tracker-snapshot"` entry. No other test changes.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks:
- `npx vitest run src/discord/__tests__/subagent-delegates-scoping.test.ts src/manager/__tests__/session-manager-warmup-timeout.test.ts src/ipc/__tests__/protocol.test.ts` — all 3 files RED.
- `npm test` — only the 2 new files + the 1 extended test in protocol.test.ts are RED. All other tests remain GREEN. (Total expected REDs: ~6 — DSCOPE 2 + STALL 3 + TRACK-CLI 1.)
</verification>

<success_criteria>
- 2 new test files created
- 1 existing test file extended (protocol.test.ts)
- All 3 files contain RED tests that fail with the EXACT failure mode predicted (subagent has delegates / no warmup-timeout warn / expected tuple missing entry)
- No production code touched in Wave 0
- `npm test` shows exactly the predicted REDs and zero collateral damage
</success_criteria>

<output>
After completion, create `.planning/phases/106-agent-context-hygiene-bundle-delegate-scoping-research-stall-cli-hotfix/106-00-SUMMARY.md`
</output>
