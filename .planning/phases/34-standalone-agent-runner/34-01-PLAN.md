---
phase: 34-standalone-agent-runner
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent/runner.ts
  - src/agent/__tests__/runner.test.ts
autonomous: true
requirements:
  - RUNNER-01
  - RUNNER-02

must_haves:
  truths:
    - "AgentRunner class exists in src/agent/runner.ts"
    - "AgentRunner.start() creates SDK session and starts DiscordBridge"
    - "AgentRunner.stop() closes session and stops bridge"
    - "AgentRunner retries on crash with exponential backoff (max 3 attempts)"
  artifacts:
    - path: "src/agent/runner.ts"
      provides: "AgentRunner class"
      contains: "AgentRunner"
---

<objective>
Build AgentRunner class that wraps SdkSessionAdapter + DiscordBridge for a single agent.
Provides start/stop lifecycle with crash recovery.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: AgentRunner with lifecycle and crash recovery</name>
  <files>src/agent/runner.ts, src/agent/__tests__/runner.test.ts</files>
  <action>
    1. Write tests using MockSessionAdapter and mock DiscordBridge
    2. Implement AgentRunner:
       - constructor({ config, sessionAdapter, discordBridge, backoff?, log? })
       - start(): creates session, sets up onError handler with backoff retry
       - stop(): closes session, stops bridge
       - maxRestarts: 3 (configurable)
       - Backoff: 1s, 2s, 4s (doubles each attempt)
    3. Run tests
  </action>
</task>
</tasks>
