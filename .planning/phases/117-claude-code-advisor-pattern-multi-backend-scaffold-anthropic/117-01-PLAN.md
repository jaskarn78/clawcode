---
phase: 117
plan: 01
title: src/llm/CompletionProvider interface seed + README (no implementations)
wave: 1
depends_on: []
files_modified:
  - src/llm/provider.ts   # NEW
  - src/llm/README.md     # NEW
autonomous: true
estimated_hours: 1
type: execute
must_haves:
  - src/llm/provider.ts declares CompletionProvider interface (no impls)
  - Interface fields match RESEARCH.md §1 / eventual-questing-tiger.md `Interfaces` block
  - README documents the seam, first consumer (Phase 118 PortableForkAdvisor), and explicit "no implementations in this phase"
  - npm run build green (typecheck only)
---

# Plan 117-01: `src/llm/CompletionProvider` interface seed

## Goal

Land the provider-neutral `CompletionProvider` interface as the seed for future non-Anthropic LLM endpoints. Interface only — zero implementations. First consumer is Phase 118 `PortableForkAdvisor`; subsequent providers (OpenAI, Bedrock, Vertex, Ollama) land in Phase 119+.

## Why

- CONTEXT.md `decisions.Architecture` (LOCKED): "Provider-neutral interface at `src/advisor/` and `src/llm/`. Call sites talk to `AdvisorService`, not the SDK directly. Future non-Anthropic providers slot into `src/llm/` without touching advisor code."
- RESEARCH.md §3 file map row for `src/llm/provider.ts`: "NEW. Declares `CompletionProvider` interface only. No implementations."
- eventual-questing-tiger.md lines 117–128: interface contract.

## Files touched

- `src/llm/provider.ts` (NEW)
- `src/llm/README.md` (NEW)

## Tasks

<task id="117-01-T01">
  <objective>Create `src/llm/provider.ts` declaring `CompletionProvider` interface — interface only, zero impls.</objective>
  <action>
    1. Create new file `src/llm/provider.ts`.
    2. Declare exactly the interface from eventual-questing-tiger.md:117–128:
       ```typescript
       export interface CompletionProvider {
         readonly id: string;
         readonly capability: { advisorTool: boolean; toolUse: boolean };
         complete(req: {
           model: string;
           system: string;
           messages: ReadonlyArray<{ role: "user" | "assistant"; content: string }>;
           maxTokens?: number;
         }): Promise<{ text: string; tokensIn: number; tokensOut: number }>;
       }
       ```
    3. Add a top-of-file doc-comment block referencing Phase 117 (scaffold), Phase 118 (first consumer), and the "no implementations in this phase" rule.
    4. Export `CompletionProvider` as a named export only.
    5. NO impls, NO factory functions, NO default exports.
  </action>
  <verify>`npm run build` passes (TypeScript compile clean); grep confirms zero `class .* implements CompletionProvider` in repo.</verify>
</task>

<task id="117-01-T02">
  <objective>Create `src/llm/README.md` documenting the seam.</objective>
  <action>
    1. Create new file `src/llm/README.md` (~25–40 lines).
    2. Sections:
       - **Purpose** — "Provider-neutral LLM seam for ClawCode. Phase 117 lands the interface; first consumer is Phase 118 `PortableForkAdvisor`."
       - **Current state** — "Interface only. No implementations exist in this phase."
       - **Planned providers** (table with status column): `AnthropicDirectProvider` (Phase 118), `OpenAIProvider` (Phase 119+), `BedrockProvider` / `VertexProvider` / `OllamaProvider` (Phase 119+).
       - **Why not the agent SDK** — note that `AnthropicSdkAdvisor` uses the Claude Agent SDK directly; `CompletionProvider` is for paths that bypass the SDK (e.g., raw provider calls in portable-fork transcript replay).
       - **Reference** — link to `/home/jjagpal/.claude/plans/eventual-questing-tiger.md`, CONTEXT.md, and RESEARCH.md §1, §3.
  </action>
  <verify>File exists and renders cleanly (manual look); no broken markdown.</verify>
</task>

## Verification

- `npm run build` — green (compile-time only; no test files in this plan).
- `grep -rn "implements CompletionProvider" src/` — zero matches.

## Out of scope

- ANY implementation of `CompletionProvider` (deferred to Phase 118 `AnthropicDirectProvider` and beyond).
- Any change to `src/advisor/` — that lives in Plan 117-02.
- Any change to the SDK Options surface — that lives in Plan 117-04.

## Reference

- `.planning/phases/117-claude-code-advisor-pattern-multi-backend-scaffold-anthropic/117-CONTEXT.md` (`<decisions>` — Architecture LOCKED)
- `.planning/phases/117-claude-code-advisor-pattern-multi-backend-scaffold-anthropic/117-RESEARCH.md` (§1, §3 file map row for `src/llm/`)
- `/home/jjagpal/.claude/plans/eventual-questing-tiger.md` (Interfaces §, lines 117–128)
