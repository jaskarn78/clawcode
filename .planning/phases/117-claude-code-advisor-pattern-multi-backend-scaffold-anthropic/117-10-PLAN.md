---
phase: 117
plan: 10
title: Migration cleanup — CLAUDE.md, clawcode.example.yaml, CHANGELOG.md, phase SUMMARY.md + manual smoke
wave: 11
depends_on: ["117-01", "117-02", "117-03", "117-04", "117-05", "117-06", "117-07", "117-08", "117-09", "117-11"]
files_modified:
  - CLAUDE.md                                                                                  # MODIFIED — add "Advisor pattern" section
  - clawcode.example.yaml                                                                      # MODIFIED — add commented defaults.advisor block + per-agent override example
  - CHANGELOG.md                                                                               # MODIFIED — add Phase 117 entry
  - .planning/phases/117-claude-code-advisor-pattern-multi-backend-scaffold-anthropic/SUMMARY.md  # NEW
autonomous: false
estimated_hours: 3
type: execute
must_haves:
  - CLAUDE.md gains "Advisor pattern" section documenting backend resolution + rollback procedure (agent.advisor.backend: fork + clawcode reload) + /clawcode-verbose slash command
  - clawcode.example.yaml shows commented defaults.advisor block and one commented per-agent override (e.g., fin-acquisition advisor.backend: fork)
  - CHANGELOG.md gets a Phase 117 entry summarising AnthropicSdkAdvisor + LegacyForkAdvisor + PortableForkAdvisor (scaffold) + /clawcode-verbose
  - SUMMARY.md lives at the phase directory root (not under per-plan); captures what shipped, what's deferred to Phase 118, manual smoke results
  - Manual smoke results recorded: both backends tested on test-agent; subagent-thread untouched; standalone-runner advisor silence noted
  - NO automatic production deploy (feedback_no_auto_deploy, feedback_ramy_active_no_deploy)
---

# Plan 117-10: Migration cleanup + manual smoke + phase SUMMARY

## Goal

Cap Phase 117 with documentation updates and the operator-gated manual smoke on `test-agent`. Captures what shipped, what's deferred, what was tested, and what an operator needs to know to flip backends or toggle `/verbose`.

## Why

- CONTEXT.md `<scope>`: docs + SUMMARY are part of the phase scope.
- CONTEXT.md user memories `feedback_no_auto_deploy` + `feedback_ramy_active_no_deploy`: production deploy is operator-gated — this plan documents and stages but DOES NOT deploy.
- RESEARCH.md §5 Plan 117-10 — manual smoke verification anchors.
- RESEARCH.md §6 Pitfall 6 — `test-agent.autoStart: false`; bring-up step is REQUIRED.

## Files touched

- `CLAUDE.md` (MODIFIED)
- `clawcode.example.yaml` (MODIFIED)
- `CHANGELOG.md` (MODIFIED)
- `.planning/phases/117-claude-code-advisor-pattern-multi-backend-scaffold-anthropic/SUMMARY.md` (NEW)

## Tasks

<task id="117-10-T01">
  <objective>Add "Advisor pattern" section to `CLAUDE.md`.</objective>
  <action>
    1. Add a new section near the existing "MCP shim runtime (Phase 110 Stage 0b)" section pattern (same depth and structure).
    2. Content sections:
       - **Phase 117 — Advisor pattern** header.
       - **Backends** — `native` (Anthropic SDK `advisorModel` in-request; default), `fork` (legacy fork-to-Opus; operator rollback path). Note `portable-fork` is scaffold-only (Phase 118).
       - **Config knobs** — `defaults.advisor.{backend, model, maxUsesPerRequest, caching}` + per-agent override `agents[].advisor.{...}`. Same `clawcode reload` flow as `shimRuntime`.
       - **Rollback procedure** — "If native shows regressions: `agents[<name>].advisor.backend: fork` in `clawcode.yaml` + `clawcode reload`. No daemon restart needed."
       - **Discord visibility** — 💭 reaction on triggering message + footer on response. `/clawcode-verbose level:on` shows inline advice block instead of footer (admin-scoped slash command, ephemeral replies).
       - **What's NOT changed** — `ask_advisor` MCP tool schema, `AdvisorBudget` daily cap (10/day), 2000-char truncation, `subagent-thread` skill, escalation logic.
       - **Reference** — link to `.planning/phases/117-.../SUMMARY.md` and `/home/jjagpal/.claude/plans/eventual-questing-tiger.md`.
  </action>
  <verify>`grep -c "Advisor pattern" CLAUDE.md` ≥ 1; section structure matches existing Phase 110 section depth.</verify>
</task>

<task id="117-10-T02">
  <objective>Update `clawcode.example.yaml` with `defaults.advisor` block and a per-agent override example.</objective>
  <action>
    1. Add (commented-out) at the appropriate `defaults:` level:
       ```yaml
       # defaults:
       #   advisor:
       #     backend: native         # "native" (default) | "fork" (legacy rollback)
       #     model: opus             # alias resolves to claude-opus-4-7
       #     maxUsesPerRequest: 3    # native-only — passed as advisor tool max_uses (RESEARCH §13.5)
       #     caching:
       #       enabled: true         # native-only — advisor-side prompt cache
       #       ttl: 5m
       ```
    2. Add (commented) one per-agent override example (mirror `shimRuntime` placement):
       ```yaml
       # agents:
       #   - name: fin-acquisition
       #     advisor:
       #       backend: fork          # operator rollback lever — flips this one agent
       ```
    3. Doc-comment in YAML referencing CLAUDE.md "Advisor pattern" section.
  </action>
  <verify>`grep -n "advisor:" clawcode.example.yaml` returns matches near `defaults:` AND under an agent example.</verify>
</task>

<task id="117-10-T03">
  <objective>Add Phase 117 entry to `CHANGELOG.md`.</objective>
  <action>
    1. Add at the top of the changelog (or under the appropriate version heading):
       ```
       ## Phase 117 — Claude Code advisor pattern (multi-backend scaffold; Anthropic complete)

       **Date:** 2026-05-13 (planned) — ship date deferred (operator-gated deploy).

       **Highlights:**
       - New `AnthropicSdkAdvisor` (default) — uses Claude Agent SDK's `advisorModel` option for in-request advisor consults with prompt caching.
       - `LegacyForkAdvisor` preserved as `advisor.backend: fork` rollback path.
       - `PortableForkAdvisor` scaffold landed (interface-conformant stub; Phase 118 fills it in).
       - `src/llm/CompletionProvider` interface seeded for future non-Anthropic providers (no impls).
       - Discord visibility: 💭 reaction + footer on assistant responses that consulted the advisor; `/clawcode-verbose` toggle for inline advice display.
       - Capability manifest now lists `advisor` so agents know to reach for it on hard decisions.

       **Preserved contracts:** `ask_advisor` MCP tool schema, `AdvisorBudget` 10/day cap, 2000-char truncation, `subagent-thread` skill independence, escalation logic.

       **Deferred to Phase 118:** Full `PortableForkAdvisor` implementation; first `CompletionProvider` impl; removal of fork-based code.
       ```
  </action>
  <verify>`grep -c "Phase 117" CHANGELOG.md` ≥ 1.</verify>
</task>

<task id="117-10-T04" type="checkpoint:human-verify" gate="blocking">
  <objective>Manual smoke on `test-agent` (operator-gated; do NOT auto-deploy).</objective>
  <what-built>
    All Plans 117-01..117-09 + 117-11 merged. Documentation updates from 117-10-T01..T03 staged.
  </what-built>
  <how-to-verify>
    PREREQ: `feedback_ramy_active_no_deploy` — only proceed when Ramy is NOT active in `#fin-acquisition`. This is a LOCAL smoke against `clawdy` source build, NOT a production deploy.

    1. `npm run build && npm test` — full suite green.
    2. `clawcode start test-agent` (autoStart=false per current config — RESEARCH §6 Pitfall 6).
    3. **Awareness check.** In Discord channel `1491623782807244880`, ask: "what's in your capability list?" — confirm response mentions `advisor` (validates Plan 117-08).
    4. **Native backend visibility — advisor_result variant.**
       - Ask a hard question that should trigger the advisor: e.g., "Design a worker pool — consult your advisor before coding."
       - Confirm: 💭 reaction on the triggering user message; response carries `*— consulted advisor (Opus) before responding*` footer; NO new thread spawned.
       - Tail logs: confirm an `advisor` `server_tool_use` block in the SDK stream; NO `forkSession` log line; `manager/advisor-budget.db.calls_used` incremented by 1.
    5. **No-advisor turn.** Send a trivial question ("hello"). Confirm: NO 💭, NO footer.
    6. **Verbose toggle.**
       - `/clawcode-verbose level:on` in the channel → ephemeral reply "verbose: verbose for this channel".
       - Trigger advisor again. Confirm: response carries the fenced advice block (≤500 chars).
       - `/clawcode-verbose level:off` → ephemeral reply "verbose: normal for this channel".
       - Trigger advisor again. Confirm: response back to plain footer.
       - `/clawcode-verbose level:status` → ephemeral reply with current level + ISO timestamp.
    7. **Fork backend rollback test.**
       - Edit `clawcode.yaml`: `agents[test-agent].advisor.backend: fork`.
       - `clawcode reload`.
       - Repeat step 4 — confirm: `forkSession` log line returns; 💭 + footer still appear (visibility is backend-agnostic).
    8. **Subagent-thread independence.** From `test-agent`, invoke `spawn_subagent_thread` (via skill) — confirm a Discord thread spawns (skill untouched by Phase 117).
    9. **Standalone-runner silence (acceptable; not a regression).** Per RESEARCH §13.9 + Pitfall 8, `clawcode run test-agent --once "..."` does NOT fire `advisor:invoked` events. Confirm no errors; absence of footer is expected.
  </how-to-verify>
  <resume-signal>Reply with "smoke pass" + any anomalies, or "smoke fail" with details.</resume-signal>
</task>

<task id="117-10-T05">
  <objective>Write phase `SUMMARY.md`.</objective>
  <action>
    1. Create `.planning/phases/117-claude-code-advisor-pattern-multi-backend-scaffold-anthropic/SUMMARY.md`.
    2. Sections:
       - **Shipped** — list of plans 117-01..117-09, 117-11 with one-line outcomes.
       - **Deferred to Phase 118** — `PortableForkAdvisor` full impl; first `CompletionProvider` impl; removal of fork code (after ≥1 week of `native` in prod without rollback).
       - **Preserved contracts** — `ask_advisor` MCP schema, `AdvisorBudget` cap, 2000-char truncation, `subagent-thread` skill, escalation logic.
       - **Manual smoke results** — paste the outcome of 117-10-T04 verbatim.
       - **Operator notes** — rollback procedure (one-line: `advisor.backend: fork` + `clawcode reload`); `/clawcode-verbose` use; known soft-cap risk (RESEARCH §13.5 A — `maxUsesPerRequest` may exceed daily cap by ≤maxUsesPerRequest on a single turn).
       - **References** — link CONTEXT.md, RESEARCH.md, `/home/jjagpal/.claude/plans/eventual-questing-tiger.md`.
       - **Production deploy status** — operator-gated; not deployed in this phase.
  </action>
  <verify>SUMMARY.md exists, contains all six sections, references the canonical artifacts.</verify>
</task>

## Verification

- `npm run build && npm test` — full suite green.
- Manual smoke per 117-10-T04 documented as `smoke pass`.
- Docs grep:
  - `grep -c "Advisor pattern" CLAUDE.md` ≥ 1.
  - `grep -c "advisor:" clawcode.example.yaml` ≥ 2 (defaults + agent example).
  - `grep -c "Phase 117" CHANGELOG.md` ≥ 1.
- `.planning/phases/117-.../SUMMARY.md` exists.

## Out of scope

- Production deployment (operator-gated; held until explicit "deploy"/"ship it" + quiet `fin-acquisition`).
- Phase 118 work.

## Reference

- `.planning/phases/117-claude-code-advisor-pattern-multi-backend-scaffold-anthropic/117-CONTEXT.md` (user-memory constraints `feedback_no_auto_deploy`, `feedback_ramy_active_no_deploy`)
- `.planning/phases/117-claude-code-advisor-pattern-multi-backend-scaffold-anthropic/117-RESEARCH.md` (§5 Plan 117-10 verification anchors; §6 Pitfall 6; §13.9)
- `/home/jjagpal/.claude/plans/eventual-questing-tiger.md` (Verification §, lines 180–214)
