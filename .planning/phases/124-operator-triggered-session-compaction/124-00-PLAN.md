---
phase: 124-operator-triggered-session-compaction
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/124-operator-triggered-session-compaction/124-00-SDK-PROBE.md  # NEW
autonomous: true
requirements:
  - SC-1   # blocking precondition (CLI compact relies on the primitive named here)
  - SC-4   # blocking precondition (mid-turn safety relies on knowing whether the primitive bypasses the turn loop)
must_haves:
  truths:
    - "The exact Claude Agent SDK primitive name + signature for daemon→worker compaction is recorded in writing."
    - "Plan 124-01 can reference one named primitive (or one named fallback) without re-grepping the SDK."
    - "If no usable primitive exists, a BLOCKED-sdk-feature annotation is filed in 124-01 with `forkSession` recommended as fallback per D-11."
  artifacts:
    - path: ".planning/phases/124-operator-triggered-session-compaction/124-00-SDK-PROBE.md"
      provides: "Named primitive + signature + recommendation for Plan 01."
      contains: "Primitive:"
  key_links:
    - from: "124-00-SDK-PROBE.md"
      to: "124-01-PLAN.md (worker-side compaction call)"
      via: "Named primitive referenced in 124-01 T-03 action body"
      pattern: "Primitive: .+"
---

<objective>
Validate (BEFORE any IPC handler is written) which Claude Agent SDK primitive the daemon will use to trigger conversation-window compaction on a live worker session. Per CONTEXT D-11 this MUST happen first — if the SDK lacks the primitive, Plan 01's worker-side call is wrong and we need the `forkSession` fallback annotated up-front.

Purpose: Resolve the single largest unknown in this phase before downstream plans burn context implementing against a guess.
Output: `124-00-SDK-PROBE.md` naming the primitive (or naming the fallback + BLOCKED-sdk-feature item).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-operator-triggered-session-compaction/124-CONTEXT.md
@.planning/phases/124-operator-triggered-session-compaction/124-BACKLOG-SOURCE.md

<interfaces>
<!-- SDK file to grep — DO NOT read the whole thing, use grep with line numbers. -->
node_modules/@anthropic-ai/claude-agent-sdk/sdk.d.ts

Initial scout (already run during planning, listed here so the executor does not re-grep blindly):
- Line 622: `export declare function forkSession(_sessionId: string, _options?: ForkSessionOptions): Promise<ForkSessionResult>;`
- Line 1297: `forkSession?: boolean;` (option flag in another type)
- Line 1918: `compact_summary: string;` (on a result type — `The conversation summary produced by compaction`)
- Line 2485-2492: `subtype: 'compact_boundary'` event with `compact_metadata { messagesToKeep, anchor_uuid }`
- Line 3508: `export declare type SDKStatus = 'compacting' | 'requesting' | null;`
- Line 3515-3516: `compact_result?: 'success' | 'failed'; compact_error?: string;`
- Line 3750: `source: 'startup' | 'resume' | 'clear' | 'compact';`
- Line 5119: "Auto-compact window size" option
- Line 5309: "Automatically compact conversation when context fills" option

The executor's job is to determine: what is the *callable* (function / method / SDK control message) that triggers compaction on a running session? The status enum + result fields tell us compaction is a first-class SDK concept; we need the verb.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Probe SDK type definitions for daemon→worker compaction primitive</name>
  <files>.planning/phases/124-operator-triggered-session-compaction/124-00-SDK-PROBE.md  # NEW</files>
  <read_first>
    - Re-grep `node_modules/@anthropic-ai/claude-agent-sdk/sdk.d.ts` for these tokens, capturing line numbers and 5 lines of surrounding context for each hit:
      - `compact` (case-insensitive)
      - `Compact`
      - `forkSession`
      - `SessionControl`
      - `session-control`
      - `interrupt`
      - `sendControl`
    - For each callable / class method matching, read enough context to capture (a) full signature, (b) parameter types, (c) return type.
    - Read CLAUDE.md §"Advisor pattern (Phase 117)" once to confirm the daemon→worker channel pattern Plan 01 will reuse.
    - DO NOT read other SDK files; stay scoped to `sdk.d.ts`.
  </read_first>
  <action>
    Write `124-00-SDK-PROBE.md` with EXACTLY this structure:

    ```markdown
    # Phase 124 — SDK Compaction Primitive Probe
    **Probed:** {YYYY-MM-DD}
    **SDK version:** {read from package.json: `@anthropic-ai/claude-agent-sdk`}
    **SDK file:** node_modules/@anthropic-ai/claude-agent-sdk/sdk.d.ts

    ## Hits

    | Line | Token | Context (1 line) |
    |------|-------|------------------|
    | 622  | forkSession | export declare function forkSession(...) |
    | ...  | ...   | ...              |

    ## Callable Primitives Identified

    For each callable (function or method) that could plausibly trigger compaction:

    ### Primitive: `<name>`
    - Signature: `<full TS signature>`
    - Parameters: `<param: type — meaning>`
    - Returns: `<return type>`
    - Bypasses turn loop?: yes / no / unclear
    - Source line: `<line number>`

    ## Recommendation for Plan 124-01 T-03

    **Primitive:** `<name>`
    **Why:** <2-3 sentences>
    **Fallback if primitive fails at runtime:** `forkSession` (per D-11)
    **BLOCKED-sdk-feature annotation required for 124-01?:** yes / no
      - If yes: <one-paragraph annotation Plan 01 should embed verbatim>
    ```

    Rules:
    - If grep finds a verb-shaped callable (e.g. `compact(sessionId)` or `sessionControl.compact()`), name it as the primary primitive.
    - If grep finds ONLY result/status/event types (`compact_summary`, `SDKStatus = 'compacting'`, `compact_boundary` events) but no verb to trigger compaction, that means compaction is driven by SDK *option flags* (`autoCompact: true`) not a callable control method. In that case:
      - Primary recommendation: `forkSession` as the daemon→worker control verb, with `compact_summary` extracted from the resulting session.
      - Set "BLOCKED-sdk-feature annotation required" = yes, with text: "SDK 0.2.x does not expose a callable `compact()` verb; daemon will use `forkSession` + read `compact_summary` from the fork result. Revisit when SDK exposes a public compact control API."
    - DO NOT invent primitives. Only list what `sdk.d.ts` actually declares.
    - DO NOT speculate about runtime behavior. Type signatures only.
  </action>
  <verify>
    <automated>test -s .planning/phases/124-operator-triggered-session-compaction/124-00-SDK-PROBE.md && grep -q "^**Primitive:**" .planning/phases/124-operator-triggered-session-compaction/124-00-SDK-PROBE.md && grep -q "^**Fallback" .planning/phases/124-operator-triggered-session-compaction/124-00-SDK-PROBE.md</automated>
  </verify>
  <done>
    124-00-SDK-PROBE.md exists with: (a) at least one row in the Hits table, (b) at least one Callable Primitive entry OR an explicit "no callable verb found" finding, (c) a named Recommendation primitive Plan 01 can call, (d) explicit yes/no on whether 124-01 needs a BLOCKED-sdk-feature annotation.
  </done>
</task>

</tasks>

<verification>
- `124-00-SDK-PROBE.md` exists at the documented path.
- The "Recommendation" section names exactly one primitive (or names `forkSession` as the fallback per D-11).
- The BLOCKED-sdk-feature yes/no decision is unambiguous.
</verification>

<success_criteria>
- Plan 01 can copy the named primitive directly into its T-03 action body without re-grepping the SDK.
- If the SDK lacks a callable compact verb, the BLOCKED annotation text is already written for Plan 01 to embed.
</success_criteria>

<operator_notes>
- **D-11 enforcement:** Plan 01 MUST NOT start until this probe completes. Wave 0 → Wave 1 dependency is hard.
- **Deploy hold (D-10):** This plan writes a markdown file. No production impact. Safe to run during Ramy-active hours.
</operator_notes>

<output>
After completion, create `.planning/phases/124-operator-triggered-session-compaction/124-00-SUMMARY.md` summarizing: probed SDK version, primitive selected, fallback status, BLOCKED-sdk-feature decision.
</output>
