---
phase: 124-operator-triggered-session-compaction
plan: 04
type: execute
wave: 2
depends_on:
  - 124-01
files_modified:
  - src/ipc/protocol.ts                                       # MODIFIED (extend status payload)
  - src/manager/daemon-status-ipc.ts                          # MODIFIED (emit session_tokens + last_compaction_at)
  - src/cli/commands/status.ts                                # MODIFIED (render new fields)
  - src/manager/compaction-event-log.ts                       # NEW (in-memory store of last_compaction_at per agent)
  - src/dashboard/tiles/tokens-sparkline.ts                   # NEW (Phase 116 chart primitive reuse)
  - src/dashboard/index.ts                                    # MODIFIED (register tile)
  - src/__tests__/status-compaction-telemetry.test.ts         # NEW
  - src/__tests__/dashboard-tokens-sparkline.test.ts          # NEW
autonomous: true
requirements:
  - SC-6   # Telemetry: session_tokens + last_compaction_at surface in clawcode status and on the dashboard
must_haves:
  truths:
    - "`clawcode status` (CLI) shows `session_tokens` (current) and `last_compaction_at` (ISO timestamp or `never`) per agent."
    - "The status IPC payload is extended with these two fields per agent (additive — existing consumers unaffected)."
    - "`last_compaction_at` is updated automatically when Plan 01's `compaction.completed` event fires."
    - "The dashboard renders a tokens-used sparkline tile per agent, alongside the existing context-fill gauge."
    - "When no compaction has occurred for an agent, `last_compaction_at` is `null` in the IPC payload and `never` in the CLI rendering."
  artifacts:
    - path: "src/manager/compaction-event-log.ts"
      provides: "In-memory map of `agentName → lastCompactionAt: ISO string`, updated by subscribing to the `compaction.completed` event bus from Plan 01."
      exports: ["CompactionEventLog", "getLastCompactionAt"]
    - path: "src/dashboard/tiles/tokens-sparkline.ts"
      provides: "Dashboard tile rendering tokens-used trend per agent (sparkline)."
    - path: "src/__tests__/status-compaction-telemetry.test.ts"
      provides: "Tests for status payload extension + last_compaction_at update on compaction event."
    - path: "src/__tests__/dashboard-tokens-sparkline.test.ts"
      provides: "Smoke test the sparkline tile renders with sample data."
  key_links:
    - from: "src/manager/compaction-event-log.ts"
      to: "Plan 01's event bus (subscribes to `compaction.completed`)"
      via: "Event bus subscription at daemon startup"
      pattern: "compaction.completed"
    - from: "src/manager/daemon-status-ipc.ts"
      to: "src/manager/compaction-event-log.ts (reads last_compaction_at)"
      via: "Status handler reads from the in-memory log"
      pattern: "getLastCompactionAt"
    - from: "src/cli/commands/status.ts"
      to: "extended status IPC payload"
      via: "Renders new fields in human-readable + JSON output"
      pattern: "session_tokens|last_compaction_at"
    - from: "src/dashboard/tiles/tokens-sparkline.ts"
      to: "Phase 116 chart primitive library"
      via: "Imports from `src/dashboard/charts/*` (Phase 116 surface)"
      pattern: "Sparkline"
---

<objective>
Surface compaction-relevant telemetry so operators can see *before* sessions slow down: extend `clawcode status` with `session_tokens` and `last_compaction_at` per agent, and add a tokens-used sparkline tile to the dashboard. Consumes the `compaction.completed` event emitted by Plan 01. Closes SC-6.

Purpose: BACKLOG-SOURCE root-cause analysis identified "operators have no way to see context getting heavy without /compact-rouletting." This plan ships the visibility layer.
Output: Extended status payload, updated CLI rendering, new dashboard tile, new in-memory compaction event log.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-operator-triggered-session-compaction/124-CONTEXT.md
@.planning/phases/124-operator-triggered-session-compaction/124-BACKLOG-SOURCE.md
@.planning/phases/124-operator-triggered-session-compaction/124-01-SUMMARY.md
@.planning/phases/103-clawcode-status-rich-telemetry-and-usage-panel-operator-observability/
@.planning/phases/116-dashboard-charts-and-realtime-tiles/

<interfaces>
<!-- Precedents to read ONCE. -->

Phase 103 status surface (target for extension):
- `src/manager/daemon-status-ipc.ts` — current status payload builder. Per-agent block likely already includes context-fill / usage. Extension: ADD `session_tokens` and `last_compaction_at`.
- `src/ipc/protocol.ts` — status payload type definition. Add the two new fields, both optional/nullable for backward compatibility (existing consumers must continue parsing).
- `src/cli/commands/status.ts` — CLI rendering. Add columns / lines for the new fields.

Phase 116 chart primitives (reuse target for sparkline):
- `src/dashboard/charts/*` (or wherever Phase 116 SUMMARY pointed) — sparkline / line-chart primitives. T-04 imports and renders.

Plan 01's event:
- `compaction.completed` with payload `{agent, tokens_before, tokens_after, summary_written, at}`. Subscribe at daemon startup. Store latest `at` per agent in the new `compaction-event-log.ts`.

Per-agent token tracking:
- The status payload already exposes context-fill / token-usage per Phase 103. `session_tokens` is either an existing field renamed/surfaced or derivable from existing data — INSPECT the current payload before adding a new field. If `session_tokens` is already exposed under a different name, just alias / rename in the response. Do not duplicate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: In-memory compaction event log + status IPC extension</name>
  <files>src/manager/compaction-event-log.ts  # NEW, src/manager/daemon-status-ipc.ts  # MODIFIED, src/ipc/protocol.ts  # MODIFIED</files>
  <behavior>
    - `CompactionEventLog` subscribes to `compaction.completed` events at daemon startup.
    - On each event, store `agentName → at` in an in-memory map.
    - `getLastCompactionAt(agent): string | null` returns the ISO timestamp or null.
    - Status IPC payload per agent now includes `session_tokens: number` and `last_compaction_at: string | null`.
    - `session_tokens` is read from the existing Phase 103 surface (aliasing if needed) — do NOT introduce a new token counter.
    - Existing status consumers continue to parse the payload without modification (additive extension).
  </behavior>
  <action>
    1. Read `src/manager/daemon-status-ipc.ts` to identify the current per-agent status payload shape. Determine whether `session_tokens` is already exposed (under any name).
    2. Create `src/manager/compaction-event-log.ts`:
       - Class `CompactionEventLog` with `Map<string, string>` of agent → ISO timestamp.
       - Constructor takes an event-bus reference and subscribes to `compaction.completed`.
       - `getLastCompactionAt(agent: string): string | null`.
       - Export a default singleton OR a factory — match existing daemon-internal patterns (executor: check how Phase 117 advisor-budget singleton is wired).
    3. Wire the singleton at daemon startup (look for the existing startup-wiring file — same place Phase 117's advisor-budget gets instantiated).
    4. Extend `src/ipc/protocol.ts` per-agent status type with `session_tokens: number` and `last_compaction_at: string | null`. Make both optional in the type if Phase 103's existing shape is strict, to preserve back-compat.
    5. In `daemon-status-ipc.ts`, populate the new fields per agent. `session_tokens` from the existing usage surface; `last_compaction_at` from `CompactionEventLog.getLastCompactionAt(agent)`.

    Test (`src/__tests__/status-compaction-telemetry.test.ts`):
    - Case A: no compaction event fired → status payload has `last_compaction_at: null`.
    - Case B: fire a `compaction.completed` event for agent X → status payload has `last_compaction_at: <iso>` for X, null for others.
    - Case C: fire two events for X in sequence → status reflects the LATEST `at`.
    - Case D: `session_tokens` is present and a number for every agent.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/status-compaction-telemetry.test.ts 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    All 4 cases pass. `tsc --noEmit` clean. Existing status-payload consumers still parse the payload (verified by running pre-existing status tests if any: `npx vitest run -t status`).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CLI rendering + dashboard sparkline tile</name>
  <files>src/cli/commands/status.ts  # MODIFIED, src/dashboard/tiles/tokens-sparkline.ts  # NEW, src/dashboard/index.ts  # MODIFIED, src/__tests__/dashboard-tokens-sparkline.test.ts  # NEW</files>
  <behavior>
    - `clawcode status` human-readable output shows two new lines (or columns) per agent: `tokens: <session_tokens>` and `last compaction: <iso or "never">`.
    - `clawcode status --json` includes both fields in the per-agent block.
    - Dashboard renders a tokens-used sparkline tile per agent alongside the existing context-fill gauge.
    - The sparkline pulls historical samples from existing telemetry (Phase 116 chart primitive expects an array of `{t, v}` points — feed it from the existing usage history surface if one exists; else sample every status poll into a bounded ring buffer of last N points).
  </behavior>
  <action>
    1. Update `src/cli/commands/status.ts`:
       - In the human-readable formatter, add the two lines per agent.
       - In the JSON path (if `--json` flag exists), the fields flow through automatically from the extended payload.
       - Format `last_compaction_at` as `never` when null; otherwise render relative time + raw ISO (e.g., `last compaction: 3m ago (2026-05-14T18:42:11Z)`). Use `date-fns` per stack docs.
    2. Create `src/dashboard/tiles/tokens-sparkline.ts`:
       - Import the Phase 116 sparkline primitive (executor: locate via `grep -r "Sparkline\|sparkline" src/dashboard/`).
       - Component / function: takes `agent: string` + `samples: {t: number, v: number}[]` and renders the sparkline.
       - Samples source: bounded ring buffer (e.g., 60 points = 60 status polls). Buffer lives in the dashboard's data-fetch layer; new samples added each time `clawcode status` is polled by the dashboard.
    3. Register the tile in `src/dashboard/index.ts` adjacent to the existing context-fill gauge.
    4. Test (`src/__tests__/dashboard-tokens-sparkline.test.ts`):
       - Render the tile with a fixed sample array; assert the rendered output (snapshot test or assert specific DOM/text shape per the project's dashboard test pattern).
       - Render with empty samples → tile shows "no data" placeholder.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/dashboard-tokens-sparkline.test.ts 2>&1 | tail -15 && npx tsx src/cli/index.ts status --json 2>&1 | head -50</automated>
  </verify>
  <done>
    Sparkline test passes. `clawcode status --json` output (run against the local daemon if available; otherwise unit-test the formatter directly) includes both new fields. Human-readable output shows `tokens:` and `last compaction:` lines per agent.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Daemon → CLI / dashboard | Local UNIX socket / WebSocket. Trusted. |
| Compaction event bus → log | In-process. Trusted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-12 | Information Disclosure | `session_tokens` / `last_compaction_at` in status payload | accept | Status surface is operator-only (single-user host + UNIX socket perms — same threat model as Phase 103). |
| T-124-13 | Tampering | Malicious `compaction.completed` event injected | accept | Event bus is in-process; no untrusted publisher. Phase 105's dispatcher is the same boundary. |
| T-124-14 | Denial of Service | Ring buffer for sparkline grows unbounded | mitigate | Bounded ring buffer (T-02 specifies max 60 samples per agent). |
</threat_model>

<verification>
- Status telemetry tests pass.
- Sparkline tile renders for sample data + handles empty-data case.
- Existing status-payload consumers unaffected (back-compat: new fields optional).
</verification>

<success_criteria>
- SC-6 closed: `clawcode status` surfaces `session_tokens` + `last_compaction_at`; dashboard renders the sparkline.
</success_criteria>

<operator_notes>
- **Dependency on Plan 01:** `compaction.completed` event MUST exist on the bus before this plan executes. Wave 2 → Wave 1 dependency is hard.
- **Anti-pattern reminder (`feedback_silent_path_bifurcation.md`):** This phase was burned 3× in 2026 by adding telemetry / IPC / handler code for paths production didn't actually execute. Before claiming `last_compaction_at` updates work, the integration test MUST fire a real `compaction.completed` event through the bus (NOT a mocked direct write to the in-memory map). Verify the SUBSCRIPTION fires, not the storage primitive.
- **Phase 103 backward compatibility:** Status payload extension is additive. Existing consumers must continue to parse without modification — make new fields optional in TypeScript types.
- **D-10 deploy hold (Ramy active):** All tests run locally. Dashboard sparkline can be smoke-tested against a local dev daemon; production dashboard waits for operator clearance.
</operator_notes>

<output>
After completion, create `.planning/phases/124-operator-triggered-session-compaction/124-04-SUMMARY.md` covering: status payload diff (before/after), event subscription wiring evidence, sparkline tile screenshot or text-render snippet, SC-6 closure evidence.
</output>
