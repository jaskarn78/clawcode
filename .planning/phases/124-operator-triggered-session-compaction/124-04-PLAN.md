---
phase: 124-operator-triggered-session-compaction
plan: 04
type: execute
wave: 2
depends_on:
  - 124-01
  - 124-02
files_modified:
  - src/ipc/protocol.ts                                       # MODIFIED (extend heartbeat-status payload type)
  - src/manager/daemon.ts                                     # MODIFIED (heartbeat-status emits new fields; compact-session records to log; runner wired with trigger)
  - src/manager/compaction-event-log.ts                       # NEW (in-memory record of last_compaction_at per agent)
  - src/cli/commands/status.ts                                # MODIFIED (render new fields)
  - src/heartbeat/types.ts                                    # MODIFIED (CheckContext.compactSessionTrigger)
  - src/heartbeat/runner.ts                                   # MODIFIED (setCompactSessionTrigger; threads trigger into CheckContext)
  - src/heartbeat/checks/context-fill.ts                      # MODIFIED (auto-trigger when fill >= autoCompactAt, with cooldown)
  - src/dashboard/client/src/components/AgentTile.tsx         # MODIFIED (add tokens sparkline tile)
  - src/dashboard/client/src/hooks/useApi.ts                  # MODIFIED (add useAgentTokensHistory hook)
  - src/manager/__tests__/compaction-event-log.test.ts        # NEW
  - src/manager/__tests__/heartbeat-status-telemetry.test.ts  # NEW
  - src/heartbeat/checks/__tests__/context-fill-auto-trigger.test.ts  # NEW
autonomous: true
requirements:
  - SC-6   # Telemetry: session_tokens + last_compaction_at surface + autoCompactAt wired
must_haves:
  truths:
    - "`clawcode status` (and `heartbeat-status` IPC) surfaces `session_tokens` (current) and `last_compaction_at` (ISO timestamp or null) per agent."
    - "`last_compaction_at` is recorded by the daemon directly after `handleCompactSession` returns `ok: true` (no event bus dependency)."
    - "When `autoCompactAt > 0` AND `currentContextFill >= autoCompactAt` AND no compaction occurred in the last 5 minutes, the heartbeat `context-fill` check dispatches `handleCompactSession` asynchronously (fire-and-forget)."
    - "Auto-trigger is opt-out per agent: `autoCompactAt = 0` disables it entirely."
    - "Auto-trigger dispatch logs the sentinel keyword `[124-04-auto-trigger]` for silent-path-bifurcation verification (production journalctl grep)."
    - "Dashboard `AgentTile` renders a tokens-history sparkline alongside the existing activity sparkline."
  artifacts:
    - path: "src/manager/compaction-event-log.ts"
      provides: "In-memory map `agent → ISO timestamp` with `record(agent)`, `getLastCompactionAt(agent)`, and `getMillisSinceLast(agent, now)` accessors."
      exports: ["CompactionEventLog"]
    - path: "src/heartbeat/checks/__tests__/context-fill-auto-trigger.test.ts"
      provides: "Verifies auto-trigger fires above threshold, skips below, respects 5-min cooldown, and respects `autoCompactAt = 0` opt-out."
  key_links:
    - from: "src/manager/daemon.ts (case 'compact-session')"
      to: "src/manager/compaction-event-log.ts"
      via: "Daemon records ISO timestamp after handler returns ok:true"
      pattern: "compactionEventLog.record"
    - from: "src/heartbeat/checks/context-fill.ts"
      to: "src/manager/daemon-compact-session-ipc.ts (via injected trigger closure)"
      via: "CheckContext.compactSessionTrigger fire-and-forget"
      pattern: "compactSessionTrigger(agent).catch"
---

<objective>
Surface compaction-relevant telemetry AND wire the `autoCompactAt` per-agent setting from Plan 124-02 to actually fire `handleCompactSession` when the heartbeat context-fill check detects threshold-cross. Closes SC-6 and wires Plan 124-02's previously-dead `autoCompactAt` field into the runtime hot path.

Purpose: BACKLOG-SOURCE root cause was "operators have no way to see context getting heavy + auto-compaction never fires". This plan ships BOTH the visibility (telemetry) AND the trigger (auto-fire).
Output: Extended `heartbeat-status` IPC payload, updated CLI rendering, dashboard tokens sparkline, heartbeat auto-trigger wiring with cooldown, new in-memory compaction log.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/124-operator-triggered-session-compaction/124-CONTEXT.md
@.planning/phases/124-operator-triggered-session-compaction/124-01-SUMMARY.md
@.planning/phases/124-operator-triggered-session-compaction/124-02-SUMMARY.md

<interfaces>
- `handleCompactSession` at `src/manager/daemon-compact-session-ipc.ts` — synchronous primitive. Returns `{ok:true,...}` or `{ok:false,error}`. Used by both the IPC dispatch and the new auto-trigger closure.
- `case "compact-session"` in `src/manager/daemon.ts:10320` — IPC handler. Plan 04 extends this to record into `CompactionEventLog` after `ok:true`.
- `case "heartbeat-status"` in `src/manager/daemon.ts:8442` — already returns per-agent rich payload (zone, fillPercentage). Plan 04 extends per-agent block with `session_tokens` + `last_compaction_at`.
- `src/heartbeat/checks/context-fill.ts` — current check. Plan 04 adds the auto-trigger branch.
- `SessionManager.getAgentConfig(name)` at `src/manager/session-manager.ts:2257` — returns `ResolvedAgentConfig` (has `autoCompactAt: number` from Plan 124-02).
- `SessionManager.getContextFillProvider(name)` — already used by the check; returns the ratio used to detect threshold-cross.
- `AgentTile.tsx` `ActivitySparkline` precedent — pattern for adding a second sparkline.

NO event bus exists in the daemon. The original plan assumed `compaction.completed` events; revised approach: daemon records into `CompactionEventLog` directly after `ok:true`. Both manual (IPC) and auto (heartbeat) paths flow through the same recording site since the auto-trigger calls the same handler.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CompactionEventLog + heartbeat-status IPC extension + CLI rendering</name>
  <files>src/manager/compaction-event-log.ts # NEW, src/ipc/protocol.ts # MODIFIED, src/manager/daemon.ts # MODIFIED, src/cli/commands/status.ts # MODIFIED, src/manager/__tests__/compaction-event-log.test.ts # NEW, src/manager/__tests__/heartbeat-status-telemetry.test.ts # NEW</files>
  <behavior>
    - `CompactionEventLog` class with:
      - `record(agent: string, at?: string): string` — stores ISO timestamp; returns the stored value.
      - `getLastCompactionAt(agent: string): string | null` — returns stored ISO or null.
      - `getMillisSinceLast(agent: string, now: number): number | null` — returns null if never compacted; else `now - parsed`.
    - Daemon constructs ONE singleton at boot; injects into:
      1. `case "compact-session"` — records after `ok:true`.
      2. `case "heartbeat-status"` — reads via `getLastCompactionAt` per agent.
    - `heartbeat-status` per-agent block adds two fields:
      - `session_tokens: number | null` — derived from existing `getContextFillProvider(agent)?.getContextFillPercentage()` × max-context / 4 (chars-per-token, matches existing primitive at `daemon-compact-session-ipc.ts:155`).
      - `last_compaction_at: string | null` — from `CompactionEventLog`.
    - CLI `clawcode status` already pulls `heartbeat-status` for zone data; extend the zone-merge block to surface tokens + last-compaction as an additional row beneath each agent. Render `last_compaction_at` as `never` when null.
    - Existing consumers parse unchanged (new fields are optional).
  </behavior>
  <action>
    1. Create `src/manager/compaction-event-log.ts` (small, immutable shape — new Map per instance, no shared module-level state).
    2. Add `compaction-event-log.test.ts`: record/get/cooldown unit tests.
    3. Extend daemon: instantiate `CompactionEventLog` at boot (alongside other singletons), pass into `case "compact-session"` callsite (record after `result.ok === true`), read from `case "heartbeat-status"` to inject `last_compaction_at` and `session_tokens` per-agent.
    4. Add `heartbeat-status-telemetry.test.ts`: builds a fixture handler, fires compaction record, asserts payload shape.
    5. Update `src/cli/commands/status.ts` to render `tokens: <N>` and `last compaction: <iso|never>` lines per agent under the table. Keep human-readable diff minimal — append rows, no column reflow.
  </action>
  <verify>
    <automated>npx vitest run src/manager/__tests__/compaction-event-log.test.ts src/manager/__tests__/heartbeat-status-telemetry.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    Both test files pass. `tsc --noEmit` clean on touched files. Heartbeat-status payload extension is additive.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auto-trigger wiring in heartbeat context-fill check (with 5-min cooldown + opt-out)</name>
  <files>src/heartbeat/types.ts # MODIFIED, src/heartbeat/runner.ts # MODIFIED, src/heartbeat/checks/context-fill.ts # MODIFIED, src/manager/daemon.ts # MODIFIED (wire trigger into runner), src/heartbeat/checks/__tests__/context-fill-auto-trigger.test.ts # NEW</files>
  <behavior>
    - `CheckContext` gains an optional `compactSessionTrigger?: (agent: string) => Promise<void>` field.
    - `HeartbeatRunner.setCompactSessionTrigger(fn)` setter mirrors `setSecretsResolver` pattern; runner threads the trigger into every `CheckContext` it builds.
    - `context-fill.ts` check, after computing `fillPercentage`:
      - Looks up `autoCompactAt` via `sessionManager.getAgentConfig(agentName)?.autoCompactAt`.
      - If `autoCompactAt > 0` AND `fillPercentage >= autoCompactAt` AND (last-compaction unknown OR older than 5 min) AND `compactSessionTrigger` is wired:
        - Logs `[124-04-auto-trigger] agent=<name> fill=<pct>% threshold=<pct>%` via `console.info` (silent-path-bifurcation sentinel).
        - Calls `compactSessionTrigger(agentName).catch(...)` — fire-and-forget.
      - Skips trigger if `autoCompactAt === 0` (opt-out).
      - Skips trigger if within cooldown window (last compaction < 5 min ago).
    - The check's PRIMARY return (status/message/metadata) is UNCHANGED — auto-trigger is a side-effect, not a status mutation.
    - Daemon wires `compactSessionTrigger` to a closure that calls `handleCompactSession` with the same deps as the IPC handler.
  </behavior>
  <action>
    1. Extend `CheckContext` with optional fields:
       - `compactSessionTrigger?: (agent: string) => Promise<void>`
       - `getLastCompactionAt?: (agent: string) => string | null`
       - `now?: () => number` (testability)
       - `cooldownMs?: number` (default 5*60*1000 in the check)
    2. Update `HeartbeatRunner` to add `setCompactSessionTrigger(fn)` + `setCompactionEventLog(log)` setters; thread fields into every `CheckContext` built in `tick()`.
    3. Modify `context-fill.ts`:
       - Read `autoCompactAt` from `sessionManager.getAgentConfig(agentName)`.
       - Read `getLastCompactionAt(agent)` from context.
       - Apply gates: opt-out, threshold, cooldown.
       - Fire trigger (fire-and-forget; never await; never let it block the return).
       - Log sentinel keyword on dispatch.
    4. Wire daemon: after constructing `CompactionEventLog` + the trigger closure, call `heartbeatRunner.setCompactSessionTrigger(...)` and `heartbeatRunner.setCompactionEventLog(...)`.
    5. Test `context-fill-auto-trigger.test.ts`:
       - Case A: fill 80% + autoCompactAt 0.7 + no prior compaction → trigger fires once; sentinel logged.
       - Case B: fill 60% + autoCompactAt 0.7 → trigger does NOT fire.
       - Case C: autoCompactAt 0 → trigger does NOT fire even at 100%.
       - Case D: prior compaction 2 min ago → trigger does NOT fire (cooldown).
       - Case E: prior compaction 6 min ago → trigger fires.
       - Case F: trigger throws → check still returns its normal result (no propagation).
  </action>
  <verify>
    <automated>npx vitest run src/heartbeat/checks/__tests__/context-fill-auto-trigger.test.ts src/heartbeat/__tests__/context-fill.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    All 6 cases pass. Pre-existing `context-fill.test.ts` still passes (back-compat: trigger wiring is optional). Sentinel keyword `[124-04-auto-trigger]` appears in test stdout for fired cases.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Dashboard tokens sparkline tile (minimal addition)</name>
  <files>src/dashboard/client/src/components/AgentTile.tsx # MODIFIED</files>
  <behavior>
    - `AgentTile` renders a SECOND sparkline beneath the existing `ActivitySparkline`, showing recent `session_tokens` history.
    - Initial implementation: pulls the most-recent `session_tokens` from useApi heartbeat data on every poll; maintains a bounded ring buffer (≤60 samples) in component state.
    - Empty state (no samples): renders the kit's "no tokens yet" mono fallback (matches the activity-sparkline empty pattern).
    - **Scope-bounded:** No new backend endpoint, no SSE plumbing, no extra API hook surface. Uses the existing heartbeat-status SSE/poll cycle from Task 1's IPC extension.
  </behavior>
  <action>
    1. Read the existing `ActivitySparkline` block in `AgentTile.tsx`.
    2. Add a `TokensSparkline` sibling component that:
       - Reads `agent.session_tokens` from the parent's agent payload (Task 1 surfaces this).
       - Maintains a ring buffer with `useState` + `useEffect` keyed on the value.
       - Renders the existing `Sparkline` primitive at `src/dashboard/client/src/components/mission-control/Sparkline.tsx` if applicable; else inline tiny Recharts area chart matching ActivitySparkline's pattern.
       - Empty buffer → "no tokens yet" mono fallback.
    3. Slot it adjacent to `ActivitySparkline` in the tile layout.
    4. No new test file — this is a thin UI addition; the parent tile tests cover render-without-crash.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/client/src/components 2>&1 | tail -10</automated>
  </verify>
  <done>
    Pre-existing AgentTile tests still pass. Component compiles. (Visual verification deferred to operator deploy window — Ramy active.)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: SUMMARY</name>
  <files>.planning/phases/124-operator-triggered-session-compaction/124-04-SUMMARY.md # NEW</files>
  <action>
    Write SUMMARY listing commits, test paths, deviations, open items per the template.
  </action>
  <verify>
    <automated>test -f .planning/phases/124-operator-triggered-session-compaction/124-04-SUMMARY.md && echo OK</automated>
  </verify>
  <done>Summary present.</done>
</task>

</tasks>

<verification>
- Telemetry IPC + CLI tests pass.
- Auto-trigger tests pass with sentinel keyword present.
- Pre-existing context-fill + status tests still pass (back-compat).
- Dashboard build compiles.
</verification>

<success_criteria>
- SC-6 closed: `clawcode status` surfaces `session_tokens` + `last_compaction_at`; dashboard tile renders tokens history; `autoCompactAt` wired from Plan 124-02's schema field into the heartbeat hot path; sentinel keyword `[124-04-auto-trigger]` enables operator-side journalctl verification of wiring.
- Plan 124-02's previously-dead `autoCompactAt` field now influences runtime.
</success_criteria>

<operator_notes>
- **D-10 deploy hold (Ramy active):** All tests run locally; no deploy.
- **Silent-path-bifurcation anti-pattern (feedback_silent_path_bifurcation.md):** Sentinel keyword `[124-04-auto-trigger]` is the production-side proof-of-wiring grep — required after deploy clearance.
- **No event bus invented.** Direct recording at the daemon's `case "compact-session"` body keeps Plan 124-04 from depending on infrastructure that doesn't exist.
- **Auto-trigger is fire-and-forget.** Heartbeat tick must never block on compaction.
- **5-min cooldown** prevents auto-compaction storms when an agent lingers near threshold.
- **autoCompactAt = 0 → opt-out.** Per CONTEXT D-06 the default is 0.7; agents explicitly setting 0 disable auto.
</operator_notes>

<output>
After completion, create `.planning/phases/124-operator-triggered-session-compaction/124-04-SUMMARY.md` covering: commit SHAs per task, test paths + last-run summary, deviation list, open items, sentinel-keyword verification command for production.
</output>
