---
phase: 124-operator-triggered-session-compaction
plan: 01
type: execute
wave: 1
depends_on:
  - 124-00
files_modified:
  - src/cli/commands/session-compact.ts             # NEW
  - src/cli/index.ts                                 # MODIFIED (register subcommand)
  - src/manager/daemon-compact-session-ipc.ts        # NEW (mirrors daemon-ask-agent-ipc.ts shape)
  - src/manager/daemon.ts                            # MODIFIED (dispatch handleCompactSession)
  - src/ipc/protocol.ts                              # MODIFIED (add compact-session IPC method)
  - src/worker/compact-session.ts                    # NEW (worker-side SDK control call)
  - src/__tests__/session-compact-mid-turn.test.ts   # NEW (SC-4 integration)
  - src/__tests__/session-compact-memory-preserved.test.ts  # NEW (SC-3 integration)
autonomous: true
requirements:
  - SC-1   # CLI subcommand `clawcode session compact <agent>` exists and works
  - SC-3   # `memory.db` byte count unchanged across compaction (verified by test)
  - SC-4   # Mid-turn compaction queues behind in-flight tool chain (verified by test)
must_haves:
  truths:
    - "Running `clawcode session compact <agent>` against a live agent prints tokens_before / tokens_after / summary_written to stdout and exits 0."
    - "Running it against a stopped/missing agent exits 1 with a recognizable `worker unreachable` error message."
    - "Running it mid-tool-chain causes the compaction to QUEUE — the in-flight tool sequence completes intact, then compaction runs."
    - "If a single turn exceeds the configured budget (default 10 min), the CLI exits 2 with error code `ERR_TURN_TOO_LONG`."
    - "After compaction, byte count of the agent's `agent-memory.db` is unchanged AND a memory-recall probe returns a chunk created before compaction."
    - "The daemon emits a structured `compaction.completed` event (consumed by Plan 04 telemetry)."
  artifacts:
    - path: "src/cli/commands/session-compact.ts"
      provides: "CLI subcommand entry point for `clawcode session compact <agent>`."
      exports: ["sessionCompactCommand"]
    - path: "src/manager/daemon-compact-session-ipc.ts"
      provides: "`handleCompactSession` IPC handler mirroring Phase 117's `handleAskAdvisor`."
      exports: ["handleCompactSession"]
    - path: "src/worker/compact-session.ts"
      provides: "Worker-side SDK control call using the primitive named in 124-00-SDK-PROBE.md."
      exports: ["compactWorkerSession"]
    - path: "src/__tests__/session-compact-mid-turn.test.ts"
      provides: "SC-4 integration test — fires compact mid-tool-chain, asserts tool sequence intact."
    - path: "src/__tests__/session-compact-memory-preserved.test.ts"
      provides: "SC-3 integration test — asserts `agent-memory.db` byte count invariant + recall probe."
  key_links:
    - from: "src/cli/commands/session-compact.ts"
      to: "src/manager/daemon-compact-session-ipc.ts (handleCompactSession via IPC)"
      via: "IPC method `compact-session`"
      pattern: "compact-session"
    - from: "src/manager/daemon-compact-session-ipc.ts"
      to: "src/worker/compact-session.ts (worker-side call)"
      via: "Manager→worker channel (same channel as Phase 117 advisor)"
      pattern: "compactWorkerSession"
    - from: "src/worker/compact-session.ts"
      to: "@anthropic-ai/claude-agent-sdk (primitive named in 124-00-SDK-PROBE.md)"
      via: "SDK control-call primitive"
      pattern: "<primitive-name from probe>"
---

<objective>
Ship the core operator primitive: a CLI subcommand backed by a daemon IPC handler and a worker-side SDK control call that compacts a live agent's conversation window without touching `agent-memory.db`. Mid-turn safety via Phase 105 dispatch queueing. Closes SC-1, SC-3, SC-4.

Purpose: Today the operator has no clean way to compact a long-running session. `restart` is destructive; `send <agent> "/compact"` has unknown semantics. This plan replaces both with a tested primitive.
Output: A working `clawcode session compact <agent>` command + IPC + worker call + two integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-operator-triggered-session-compaction/124-CONTEXT.md
@.planning/phases/124-operator-triggered-session-compaction/124-BACKLOG-SOURCE.md
@.planning/phases/124-operator-triggered-session-compaction/124-00-SDK-PROBE.md
@.planning/phases/117-claude-code-advisor-pattern-multi-backend-scaffold-anthropic/117-SUMMARY.md
@.planning/phases/105-trigger-policy-default-allow-and-queue_full-coalescer-storm-fix/

<interfaces>
<!-- Precedent files to read ONCE during execution. Do not re-read. -->

Phase 117 IPC pattern (REQUIRED reading, this is the shape we mirror):
- `src/manager/daemon-ask-agent-ipc.ts` — `handleAskAdvisor` function. Mirror its shape exactly for `handleCompactSession`. Same: validator at IPC boundary, agent-resolution from registry, manager→worker dispatch, structured response shape, error code propagation.

Phase 105 dispatch hot path (REQUIRED reading for T-03):
- Wherever the trigger-policy queue lives (executor: grep `src/manager/` for "queue" / "trigger-policy" — surfaced in Phase 105 SUMMARY). Compaction enqueues behind in-flight turn the same way.

IPC protocol:
- `src/ipc/protocol.ts:168` — `ask-advisor` IPC method declaration. Add `compact-session` adjacent to it, same shape.

CLI dispatch:
- `src/cli/index.ts` — find the subcommand registration block (look for `init / start / stop / send / fork / threads`). Add `session compact <agent>` registration referencing `src/cli/commands/session-compact.ts`.

SDK primitive (REQUIRED — comes from Plan 00):
- `124-00-SDK-PROBE.md` "Recommendation" section names the primitive. T-03 references it by name.
- If the probe set `BLOCKED-sdk-feature: yes`, embed that annotation verbatim at the top of `src/worker/compact-session.ts` as a JSDoc comment block.

Memory DB location (REQUIRED for T-04):
- Per stack docs, per-agent SQLite at `agent-memory.db` (better-sqlite3). Path is workspace-relative per agent. Resolve through the agent registry.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CLI subcommand `clawcode session compact &lt;agent&gt;`</name>
  <files>src/cli/commands/session-compact.ts  # NEW, src/cli/index.ts  # MODIFIED</files>
  <behavior>
    - `clawcode session compact <agent>` exits 0 on success and prints JSON `{tokens_before, tokens_after, summary_written}` to stdout.
    - Exit 1 with stderr message `worker unreachable: <agent>` when the IPC call to the daemon returns "agent not running" / "worker unreachable".
    - Exit 2 with stderr message containing `ERR_TURN_TOO_LONG` when the daemon returns that error code (mid-tool-chain past budget).
    - `--agent` argument is required; missing arg yields exit 64 (EX_USAGE) with usage line.
    - `--json` flag (optional) emits machine-readable JSON instead of human-readable lines.
  </behavior>
  <action>
    Create `src/cli/commands/session-compact.ts`:
    - Export `sessionCompactCommand` (mirror the structure of an existing single-action subcommand — `fork.ts` is the closest precedent per BACKLOG-SOURCE).
    - Parse argv: positional `<agent>`, optional `--json` flag.
    - Open IPC client (reuse existing CLI→daemon client; do not reinvent — `fork.ts` shows the pattern).
    - Send IPC method `compact-session` with payload `{agent}`.
    - Map daemon response:
      - `{ok: true, tokens_before, tokens_after, summary_written}` → print and exit 0.
      - `{ok: false, code: 'WORKER_UNREACHABLE', message}` → stderr, exit 1.
      - `{ok: false, code: 'ERR_TURN_TOO_LONG', message}` → stderr, exit 2.
      - Any other error → stderr, exit 3.
    - Register in `src/cli/index.ts` adjacent to `fork` / `restart` subcommands. Group under a `session` namespace if other `session *` subcommands exist; otherwise register as top-level `session-compact` AND alias `session compact` per the BACKLOG-SOURCE requested ergonomic.

    NO direct SDK calls in this file. CLI is a thin wrapper; daemon does the work.
  </action>
  <verify>
    <automated>npx tsx src/cli/index.ts session compact --help 2>&1 | grep -qi 'compact' && npx vitest run src/__tests__/session-compact-cli.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    `clawcode session compact --help` shows the new subcommand. Vitest unit test for argv parsing + exit-code mapping passes. The CLI compiles under tsc strict.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Daemon IPC handler `handleCompactSession` (mirrors Phase 117)</name>
  <files>src/manager/daemon-compact-session-ipc.ts  # NEW, src/manager/daemon.ts  # MODIFIED, src/ipc/protocol.ts  # MODIFIED</files>
  <behavior>
    - IPC method `compact-session` with payload `{agent: string}` is registered in the protocol.
    - `handleCompactSession({agent})` resolves agent from registry; returns `WORKER_UNREACHABLE` if absent.
    - On agent found: dispatches to worker via existing manager→worker channel (same channel as `ask-advisor`); awaits worker result with timeout.
    - Returns structured `{ok, tokens_before, tokens_after, summary_written}` on success, or `{ok: false, code, message}` on failure.
    - Validator at the IPC boundary rejects malformed payloads (zod, per stack convention).
  </behavior>
  <action>
    Per CONTEXT D-02, mirror `src/manager/daemon-ask-agent-ipc.ts` (`handleAskAdvisor`) shape exactly:
    1. Read `daemon-ask-agent-ipc.ts` first. Note: zod validator at top, agent resolution helper, error-code constants, response shape.
    2. Create `src/manager/daemon-compact-session-ipc.ts` with the parallel structure:
       - Zod schema `CompactSessionRequestSchema` with `{agent: string}`.
       - `handleCompactSession(request, deps)` function.
       - Error-code constants `WORKER_UNREACHABLE`, `ERR_TURN_TOO_LONG`.
       - Response type `CompactSessionResult = {ok: true, ...} | {ok: false, code, message}`.
    3. Extend `src/ipc/protocol.ts` (adjacent to `ask-advisor` declaration at line 168 per Phase 117 SUMMARY): add `compact-session` method declaration with request/response types.
    4. Wire dispatch in `src/manager/daemon.ts`: import `handleCompactSession`, register in the IPC method router parallel to `ask-advisor`'s registration.

    IMPORTANT — do NOT invent a new IPC channel. Reuse the existing manager→worker channel.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/daemon-compact-session-ipc.test.ts 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    Unit test passes: handler returns `WORKER_UNREACHABLE` for unknown agent, returns success shape when worker mock returns success, propagates `ERR_TURN_TOO_LONG` from worker. `tsc --noEmit` clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Worker-side SDK control call + mid-turn queue + SC-4 integration test</name>
  <files>src/worker/compact-session.ts  # NEW, src/__tests__/session-compact-mid-turn.test.ts  # NEW</files>
  <behavior>
    - `compactWorkerSession({sessionId, turnBudgetMs})` calls the SDK primitive named in `124-00-SDK-PROBE.md` (or fallback per the probe).
    - If the agent is mid-tool-chain when the call arrives, the call ENQUEUES behind the current turn (same dispatch pattern as Phase 105). The in-flight tool sequence completes intact; compaction then runs.
    - If a single turn exceeds `turnBudgetMs` (default 10 * 60 * 1000 ms, configurable via `auto-compact-at` neighborhood YAML knob — schema lives in Plan 02), return `{ok: false, code: 'ERR_TURN_TOO_LONG'}`.
    - On successful compaction, emit a daemon event `compaction.completed` with `{agent, tokens_before, tokens_after, summary_written, at}` for Plan 04 to consume.
    - Return `{ok: true, tokens_before, tokens_after, summary_written}`.
  </behavior>
  <action>
    1. Read `124-00-SDK-PROBE.md` "Recommendation" section. Use the named primitive.
    2. If the probe set BLOCKED-sdk-feature=yes: embed the probe's annotation verbatim as the file header JSDoc, then implement against `forkSession` as fallback per D-11.
    3. Look up the Phase 105 dispatch queue location (grep `src/manager/` for trigger-policy queue helper). Enqueue compaction behind in-flight turn using that helper — do NOT invent a new queue.
    4. Wrap the SDK call in a turn-budget timer; if the in-flight turn does not complete within `turnBudgetMs`, return `ERR_TURN_TOO_LONG` (do NOT cancel the turn; let it complete, but signal the operator the budget was exceeded).
    5. Read `tokens_before` from worker session state pre-call. After compaction, read `tokens_after` from session state. Read `summary_written` from the SDK result (`compact_summary` field per the probe).
    6. Emit `compaction.completed` event via the daemon event bus (same bus Phase 117 uses for advisor events — if Phase 117 emits any).

    SC-4 integration test (`src/__tests__/session-compact-mid-turn.test.ts`):
    - Spin up a synthetic agent worker with a mock SDK that simulates a multi-tool-call turn (e.g., 3 tool invocations spaced 200ms apart).
    - Mid-turn (after tool 1, before tool 2), invoke `compactWorkerSession`.
    - Assert: all 3 tool invocations complete IN ORDER. Compaction runs AFTER tool 3. Returned `tokens_after < tokens_before`.
    - Second case: turn never completes (mock stalls past `turnBudgetMs` set to 100ms for the test). Assert return value is `{ok: false, code: 'ERR_TURN_TOO_LONG'}`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/session-compact-mid-turn.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    Both test cases pass: mid-turn queueing preserves tool-call order; turn-budget overflow returns `ERR_TURN_TOO_LONG`. `compaction.completed` event emitted on success path (assertable via event-bus spy).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: SC-3 memory-preservation integration test</name>
  <files>src/__tests__/session-compact-memory-preserved.test.ts  # NEW</files>
  <behavior>
    - Before compaction: write a known memory chunk to `agent-memory.db` (e.g., text "phase-124-canary-{nanoid}") with a known ID. Capture `stat -c '%s' agent-memory.db` byte count.
    - Run compaction via `compactWorkerSession`.
    - After compaction: re-stat the file — byte count MUST be unchanged. Run a memory-recall query for the canary text — MUST return the chunk with the known ID.
  </behavior>
  <action>
    Per CONTEXT D-04 (memory.db is OFF LIMITS):
    1. Create test that uses the test-harness synthetic agent. Open the agent's `agent-memory.db` with better-sqlite3, insert a canary memory chunk via the standard memory write path (do not bypass — use the same write API the agent uses).
    2. Capture `fs.statSync(memDbPath).size` as `bytesBefore`.
    3. Capture chunk IDs: `SELECT id FROM memory_chunks ORDER BY id` → `idsBefore`.
    4. Invoke `compactWorkerSession` against the agent.
    5. Re-capture `bytesAfter` and `idsAfter`.
    6. Assert: `bytesBefore === bytesAfter` AND `idsBefore` deep-equals `idsAfter` AND a memory-recall query for the canary text returns the chunk.

    NOTE: If `bytesBefore !== bytesAfter` due to SQLite WAL checkpointing being orthogonal to compaction, the test STILL fails — investigate and fix worker-side code to ensure no DB writes happen during compaction. This is the intent of D-04.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/session-compact-memory-preserved.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    Test passes: byte count invariant, chunk IDs invariant, recall probe succeeds. SC-3 closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → daemon IPC | Local UNIX socket. Trusted (operator-controlled). |
| daemon → worker | In-process manager → child-process worker. Trusted. |
| worker → SDK | Trusted local SDK call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-01 | Tampering | `compact-session` IPC payload | mitigate | Zod schema validation at IPC boundary (mirror `daemon-ask-agent-ipc.ts`); reject malformed `agent` field. |
| T-124-02 | Denial of Service | Repeated compact requests during in-flight turn | mitigate | Mid-turn queue dedupes; second compact within an in-flight turn is coalesced (Phase 105 dispatch behaviour). |
| T-124-03 | Information Disclosure | `summary_written` field could leak chat content via daemon logs | mitigate | Log only `summary_written: boolean` at daemon log level; full summary path returned to caller only. |
| T-124-04 | Repudiation | Operator-initiated compaction not traceable | mitigate | Emit `compaction.completed` event with `{agent, at, initiator: 'cli'|'discord'}` to event bus (Plan 04 surfaces it). |
| T-124-05 | Elevation of Privilege | CLI socket reachable by non-operator local user | accept | Single-user host; UNIX socket perms inherited from daemon (matches existing `clawcode` CLI threat model). |
</threat_model>

<verification>
- `clawcode session compact <agent>` works against a live agent (manual; deploy-hold means local synthetic agent only per D-10).
- SC-3 memory-preservation test passes.
- SC-4 mid-turn integration test passes.
- IPC handler matches Phase 117's shape (peer-review against `daemon-ask-agent-ipc.ts`).
</verification>

<success_criteria>
- SC-1 closed: CLI subcommand exists, reports tokens_before/tokens_after/summary_written.
- SC-3 closed: memory-preservation test green.
- SC-4 closed: mid-turn integration test green; `ERR_TURN_TOO_LONG` path tested.
- `compaction.completed` event emitted on success (consumed by Plan 04).
</success_criteria>

<operator_notes>
- **D-11 enforcement:** This plan CANNOT start until `124-00-SDK-PROBE.md` exists and names a primitive. The probe is the input to T-03.
- **D-04 hard rule:** `agent-memory.db` is OFF LIMITS. T-04 asserts byte-count invariance. If the test fails, debug the worker, not the test.
- **D-02 hard rule:** Mirror `handleAskAdvisor` shape. Do NOT invent a new IPC channel. Do NOT invent new error codes beyond `WORKER_UNREACHABLE` and `ERR_TURN_TOO_LONG`.
- **D-10 deploy hold (Ramy active):** Local code + tests only. No production deploy until operator confirms.
- **Anti-pattern reminder (`feedback_silent_path_bifurcation.md`):** Before claiming `compaction.completed` event flows to Plan 04 telemetry, ASSERT the event lands on the bus via a spy in the integration test. Don't assume.
</operator_notes>

<output>
After completion, create `.planning/phases/124-operator-triggered-session-compaction/124-01-SUMMARY.md` covering: files created/modified, primitive selected (from probe), error codes wired, SC-1/SC-3/SC-4 evidence (test output snippets).
</output>
