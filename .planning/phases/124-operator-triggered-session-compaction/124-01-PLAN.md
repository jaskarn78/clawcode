---
phase: 124
plan: 01
title: Hybrid compaction primitive (compactForAgent + forkSession) + CLI + IPC handler
type: execute
wave: 1
depends_on: [124-00]
files_modified:
  - src/manager/daemon-compact-session-ipc.ts  # NEW
  - src/cli/commands/session-compact.ts        # NEW
  - src/manager/daemon.ts                       # IPC handler registration
  - src/manager/__tests__/compact-session-integration.test.ts  # NEW
  - .planning/phases/124-operator-triggered-session-compaction/124-CONTEXT.md  # T-01
autonomous: true
---

## Goal

Wire the existing `SessionManager.compactForAgent()` primitive (dead-code at `src/manager/session-manager.ts:2203`) plus the SDK's `forkSession()` into a single hybrid compaction flow exposed via daemon IPC + new CLI subcommand `clawcode session compact <agent>`. Result: extracted facts persist in `memory.db`, session JSONL genuinely shrinks via fork+prepend.

## Tasks

<task id="T-01" type="docs" tdd="no">
  <description>Revise CONTEXT D-04 to allow memory.db growth (already done inline 2026-05-14).</description>
  <read_first>.planning/phases/124-operator-triggered-session-compaction/124-CONTEXT.md</read_first>
  <acceptance_criteria>D-04 wording explicitly states memory.db grows; SC-3 acceptance is "all original chunk IDs preserved + new chunks added + byte count larger."</acceptance_criteria>
</task>

<task id="T-02" type="impl" tdd="no">
  <description>New file src/manager/daemon-compact-session-ipc.ts exporting handleCompactSession. Mirror the shape of src/manager/daemon-ask-agent-ipc.ts (handleAskAdvisor). Takes {agent: string}, returns {tokens_before, tokens_after, summary_written: true, forked_to: string, memories_created: number}. Reject if SessionManager.sessions has no entry for agent (worker not running) — return {ok: false, error: "AGENT_NOT_RUNNING"}. Reject if daemon-not-ready — return {ok: false, error: "DAEMON_NOT_READY"}.</description>
  <read_first>src/manager/daemon-ask-agent-ipc.ts (Phase 117 IPC precedent — same DI shape, same error-return contract), src/manager/session-manager.ts:188 (sessions Map), src/manager/session-manager.ts:2203 (compactForAgent signature)</read_first>
  <acceptance_criteria>File exists, exports handleCompactSession, mirrors handleAskAdvisor DI shape, returns the documented payload on success, returns {ok:false, error:...} on each named failure mode.</acceptance_criteria>
</task>

<task id="T-03" type="impl" tdd="no">
  <description>Inside handleCompactSession, implement the hybrid flow:
    1. Look up handle = manager.sessions.get(agent). If missing → AGENT_NOT_RUNNING.
    2. If handle.hasActiveTurn() → enqueue via SerialTurnQueue (existing Phase 105 dispatch pattern). Safety budget: if current turn exceeds 10 min, exit with ERR_TURN_TOO_LONG.
    3. tokens_before = currentContextFill(agent) — use existing Phase 103 telemetry surface or a sibling primitive.
    4. Read conversation turns from the agent's session adapter.
    5. result = await manager.compactForAgent(agent, conversationTurns, extractMemoriesFn). extractMemoriesFn: MVP is a Haiku call (reuse Phase 95 dreaming infra if available) OR a trivial extractor (split on newlines, filter short lines >20 chars). Phase 125 replaces this with richer tiered extraction.
    6. Call sdk.forkSession(currentSessionId, { upToMessageId: lastMessageId }) — verify exact API at write-time from node_modules/@anthropic-ai/claude-agent-sdk/sdk.d.ts.
    7. Prepend result.summary as a synthetic first turn in the new fork via persistent-session-handle.ts's input chokepoint (line ~1005, the inputQueue.push call).
    8. Swap the worker reference in sessions Map to the new fork's session ID. Archive the old session ID in agent state (do NOT delete).
    9. tokens_after = currentContextFill(agent) against the new fork.
    10. Return the payload.</description>
  <read_first>src/manager/session-manager.ts:188 (sessions Map), src/manager/session-manager.ts:2203 (compactForAgent), src/memory/compaction.ts:125-181 (CompactionManager.compact internals), src/manager/persistent-session-handle.ts:994-1021 (input chokepoint shapes), node_modules/@anthropic-ai/claude-agent-sdk/sdk.d.ts (forkSession exact signature)</read_first>
  <acceptance_criteria>Hybrid flow works against a synthetic agent fixture in T-05. forkSession is called with correct args. summary is prepended as a user turn in the new fork. Old session ID archived in agent state.</acceptance_criteria>
</task>

<task id="T-04" type="impl" tdd="no">
  <description>New file src/cli/commands/session-compact.ts. Adds subcommand `clawcode session compact <agent>` to the CLI dispatcher. Calls daemon IPC handleCompactSession. Prints stdout: human-readable lines by default OR JSON if --json flag. Exit codes: 0 success, 1 AGENT_NOT_RUNNING, 2 ERR_TURN_TOO_LONG, 3 DAEMON_NOT_READY, 4 unknown error. Wire into the existing src/cli/index.ts subcommand dispatcher.</description>
  <read_first>src/cli/index.ts (subcommand dispatch pattern), an existing CLI subcommand for the precedent (e.g. src/cli/commands/status.ts or similar)</read_first>
  <acceptance_criteria>`clawcode session compact <agent>` returns the IPC payload with correct exit codes. `--json` flag prints structured output. Unknown agent → exit 1, clear error message.</acceptance_criteria>
</task>

<task id="T-05" type="test" tdd="yes">
  <description>End-to-end vitest in src/manager/__tests__/compact-session-integration.test.ts. Test fixture: synthetic agent with >=50 conversation turns + populated memory.db.
    - Capture pre-state: memories table row count, vec_memories row count, session JSONL byte size, session ID.
    - Invoke manager.compactForAgent directly (not via IPC) with a test-stub extractMemoriesFn that returns 3 hardcoded facts.
    - Post-state assertions: memories count == pre-count + 3; vec_memories grew by 3; CompactionResult.memoriesCreated === 3; CompactionResult.summary non-empty; CompactionResult.logPath exists on disk.
    - Then exercise the fork+prepend wrapper from T-03 separately (via a test seam exposing the wrapper function) and assert new session ID differs + the synthetic user-turn is the first turn in the new fork.
    - Probe recall: extract a memorable fact (e.g. "User's favorite color is purple."), then query the new fork's RRF retrieval for that fact; assert it's recalled.</description>
  <read_first>src/manager/__tests__/compaction-counter.test.ts (existing test pattern for compactForAgent), src/memory/store.ts (memoryStore.insert signature), src/memory/__tests__/*.test.ts (any RRF retrieval test fixture for the probe pattern)</read_first>
  <acceptance_criteria>All assertions pass. Test runs in <30s. No network calls. memory.db invariance per revised D-04 holds.</acceptance_criteria>
</task>

<task id="T-06" type="test" tdd="yes">
  <description>Mid-turn safety test. Fire handleCompactSession mid-tool-chain against a synthetic agent. Assert tool-chain completes intact, compaction runs AFTER turn settles. Verify ERR_TURN_TOO_LONG fires when turn exceeds budget (mock clock or synthetic long turn).</description>
  <read_first>src/manager/persistent-session-handle.ts (SerialTurnQueue), src/manager/__tests__/relay-and-mark-completed.test.ts (turn lifecycle test pattern)</read_first>
  <acceptance_criteria>Mid-turn fire queues correctly. Tool-chain completes intact. ERR_TURN_TOO_LONG path covered.</acceptance_criteria>
</task>

<task id="T-07" type="docs" tdd="no">
  <description>Write 124-01-SUMMARY.md: list commit SHAs, tests added (paths), test run output (last 5 lines of npm test), deviations from this plan (with rationale), open items (e.g. extractMemoriesFn quality is MVP — Phase 125 makes it richer).</description>
  <read_first>.planning/phases/121-subagent-ux-completion-and-chunk-boundary/121-01-SUMMARY.md (precedent format)</read_first>
  <acceptance_criteria>SUMMARY.md exists, lists all commits + deviations, references this plan.</acceptance_criteria>
</task>

## Operator Notes

- **Ramy-active deploy hold continues.** Code lands locally + tests run. Deploy waits.
- **Phase 117 IPC pattern is the precedent** — mirror `handleAskAdvisor` shape exactly.
- **D-04 revised** — `memory.db` is allowed to grow; the rule is now "all original chunk IDs preserved + new chunks added + larger byte count."
- **`compactForAgent()` is reused as-is** — do NOT modify the primitive at `src/manager/session-manager.ts:2203`.
- **Silent-path-bifurcation anti-pattern** — verify the daemon's IPC dispatch table includes `compact-session` before commit. T-05 + T-06 are the prevention.

## Verification

Closes SC-1 (CLI works), SC-3 (revised: memory.db grows + chunk IDs preserved + recall works), SC-4 (mid-turn safe).

Manual smoke after deploy:
```bash
clawcode session compact synthetic-test --json
# expect: {"tokens_before": N, "tokens_after": M, "summary_written": true, "forked_to": "<id>", "memories_created": K}
# assert: M < N, clawcode memory list synthetic-test shows pre-compaction chunks + K new ones
```
