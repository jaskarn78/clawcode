---
phase: 124-operator-triggered-session-compaction
plan: 03
type: execute
wave: 2
depends_on:
  - 124-01
files_modified:
  - src/manager/slash-commands.ts                          # MODIFIED (register /compact)
  - src/manager/slash-commands-compact.ts                  # NEW (command handler)
  - src/__tests__/slash-compact-admin-only.test.ts         # NEW (permission + happy path)
autonomous: true
requirements:
  - SC-2   # Admin can post `/compact <agent>` in Discord and get tokens_before/tokens_after/summary_written
must_haves:
  truths:
    - "Discord admin posts `/compact <agent>` → ephemeral embed shows tokens_before / tokens_after / summary_written."
    - "Discord non-admin posts `/compact <agent>` → ephemeral refusal embed; no IPC call is made."
    - "On `WORKER_UNREACHABLE` from the IPC handler, the admin gets an ephemeral error embed naming the agent and the error code."
    - "On `ERR_TURN_TOO_LONG`, the admin gets an ephemeral error embed naming the error code."
    - "The slash command is registered under the existing admin-slash-command dispatch (same path as `/clawcode-verbose` per Phase 117)."
  artifacts:
    - path: "src/manager/slash-commands-compact.ts"
      provides: "Discord `/compact <agent>` admin slash command handler."
      exports: ["handleSlashCompact", "compactCommandDefinition"]
    - path: "src/__tests__/slash-compact-admin-only.test.ts"
      provides: "Tests for admin permission gate + IPC integration + error-code propagation."
  key_links:
    - from: "src/manager/slash-commands-compact.ts"
      to: "src/manager/daemon-compact-session-ipc.ts (handleCompactSession from Plan 01)"
      via: "Direct in-process call (manager-side) — NOT a re-entry through IPC"
      pattern: "handleCompactSession"
    - from: "src/manager/slash-commands.ts"
      to: "src/manager/slash-commands-compact.ts"
      via: "Slash-command dispatcher registers `/compact`"
      pattern: "compactCommandDefinition"
---

<objective>
Add the Discord admin slash command `/compact <agent>` so operators can trigger the same compaction primitive from within a Discord channel without dropping to the CLI. Admin-only; ephemeral responses; reuses Plan 01's `handleCompactSession`. Closes SC-2.

Purpose: BACKLOG-SOURCE scenario — Jas in #fin-acquisition watching latency climb wants to compact without opening a terminal. CLI parity in Discord.
Output: New slash command handler + permission gate + integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-operator-triggered-session-compaction/124-CONTEXT.md
@.planning/phases/124-operator-triggered-session-compaction/124-BACKLOG-SOURCE.md
@.planning/phases/124-operator-triggered-session-compaction/124-01-SUMMARY.md
@.planning/phases/117-claude-code-advisor-pattern-multi-backend-scaffold-anthropic/117-SUMMARY.md

<interfaces>
<!-- Precedents to read ONCE. -->

Phase 117 `/clawcode-verbose` admin-only ephemeral command (DIRECT precedent):
- Per CLAUDE.md §"Advisor pattern (Phase 117)": admin-only, ephemeral, per-channel state in `~/.clawcode/manager/verbose-state.db`. Includes `/clawcode-verbose on|off|status` subcommands.
- Locate the implementing file under `src/manager/slash-commands*.ts`. Read it ONCE — it shows: command definition shape, permission gate, ephemeral embed construction, channel→agent resolution.

Plan 01's `handleCompactSession`:
- `src/manager/daemon-compact-session-ipc.ts`. Same-process call from the slash-command handler — no IPC round-trip needed. Pass the same `{agent}` payload, get the same `{ok, tokens_before, tokens_after, summary_written} | {ok: false, code, message}` response.

discord.js types (already a project dep per stack docs):
- `ChatInputCommandInteraction` for slash-command interactions.
- `EmbedBuilder` for ephemeral embed responses.
- Permission check pattern: read from existing admin-permission helper in `src/manager/slash-commands*.ts`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register `/compact <agent>` slash command + permission gate</name>
  <files>src/manager/slash-commands-compact.ts  # NEW, src/manager/slash-commands.ts  # MODIFIED</files>
  <behavior>
    - `/compact <agent>` slash command appears in Discord's command list (registered at startup like `/clawcode-verbose`).
    - Non-admin invoker → ephemeral refusal embed `"Operator-only command."`. No IPC call dispatched.
    - Admin invoker with unknown `<agent>` → ephemeral error embed `"Unknown agent: <name>"`.
    - Admin invoker with known agent → proceeds to T-02's happy path.
    - Command is registered via the same dispatch pattern Phase 117 used for `/clawcode-verbose`.
  </behavior>
  <action>
    Per CONTEXT D-08:
    1. Read `src/manager/slash-commands.ts` and any sibling files matching `slash-commands*.ts`. Identify:
       - The `/clawcode-verbose` command definition (export + builder pattern).
       - The admin permission helper (likely `isAdmin(interaction)` or similar).
       - The registration site in the dispatcher's command table.
    2. Create `src/manager/slash-commands-compact.ts`:
       - `compactCommandDefinition`: SlashCommandBuilder with name `compact`, one required string option `agent` (autocomplete-enabled, suggesting the live agent registry).
       - `handleSlashCompact(interaction)`: function that performs the permission check, validates the agent, calls Plan 01's `handleCompactSession`, builds the ephemeral embed.
    3. Permission gate: reuse the existing admin helper. Non-admin → `interaction.reply({ ephemeral: true, embeds: [refusalEmbed] })`. Do NOT invoke `handleCompactSession`.
    4. Register `compactCommandDefinition` in the dispatcher (same place `/clawcode-verbose` is registered).
    5. Autocomplete handler for the `agent` option: return the list of registered agent names (from the agent registry — same source `clawcode status` reads).

    Do not invent a new permission system. If the existing helper is `isAdmin(member)` style, use it as-is.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/slash-compact-admin-only.test.ts -t 'permission gate' 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    Test cases pass: non-admin → refusal, admin + unknown agent → "Unknown agent" embed, admin + known agent → proceeds (mocked `handleCompactSession`). Command registered in the dispatcher.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Happy-path + error-code propagation through ephemeral embed</name>
  <files>src/manager/slash-commands-compact.ts  # MODIFIED, src/__tests__/slash-compact-admin-only.test.ts  # MODIFIED</files>
  <behavior>
    - Admin + known agent + successful compaction → ephemeral embed titled `Compaction complete: <agent>` with fields `Tokens before`, `Tokens after`, `Summary written: yes/no`.
    - `WORKER_UNREACHABLE` from `handleCompactSession` → ephemeral error embed: title `Compaction failed`, description includes `<agent>` and `worker unreachable`.
    - `ERR_TURN_TOO_LONG` → ephemeral error embed: title `Compaction failed`, description includes `ERR_TURN_TOO_LONG` so the operator recognizes it from CLI parity.
    - Embed uses the project's existing color convention (look at `/clawcode-verbose`'s embed colors for precedent).
    - Interaction is deferred (`interaction.deferReply({ ephemeral: true })`) before the IPC call, since compaction may take seconds; followed up with `interaction.editReply(...)` once the IPC returns.
  </behavior>
  <action>
    1. In `handleSlashCompact`, after the permission gate and agent validation:
       - `await interaction.deferReply({ ephemeral: true });`
       - Call `handleCompactSession({agent})` from Plan 01.
       - Build embed based on response shape:
         - `{ok: true, tokens_before, tokens_after, summary_written}` → success embed (green, 0x2ecc71 or project equivalent).
         - `{ok: false, code: 'WORKER_UNREACHABLE', message}` → error embed (red), description mentions agent + code.
         - `{ok: false, code: 'ERR_TURN_TOO_LONG', message}` → error embed (red), description includes `ERR_TURN_TOO_LONG` verbatim.
         - Any other error → generic error embed.
       - `await interaction.editReply({ embeds: [embed] });`
    2. Tests in `src/__tests__/slash-compact-admin-only.test.ts`:
       - Mock `handleCompactSession` to return each response shape.
       - Assert the embed title + description for each case.
       - Assert `interaction.deferReply` was called with `{ ephemeral: true }`.
       - Assert `interaction.editReply` was called with the expected embed shape.
       - Assert that on `WORKER_UNREACHABLE`, the embed description contains both the agent name AND the literal `worker unreachable` (case-insensitive).
       - Assert on `ERR_TURN_TOO_LONG`, embed description contains the literal `ERR_TURN_TOO_LONG`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/slash-compact-admin-only.test.ts 2>&1 | tail -25</automated>
  </verify>
  <done>
    All test cases pass: success embed, both error embeds, defer + editReply pattern asserted. Ephemeral flag asserted on every reply.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord user → bot | Untrusted input (any user can post). Permission gate enforces admin-only. |
| Bot → daemon (in-process) | Same process; trusted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-08 | Elevation of Privilege | Non-admin invokes `/compact` | mitigate | Permission gate in T-01 (mirror Phase 117 `/clawcode-verbose` admin check). Reject before any IPC call. Ephemeral refusal embed. |
| T-124-09 | Information Disclosure | Compaction result leaks into the channel | mitigate | Every reply uses `ephemeral: true` — only the invoker sees the result. |
| T-124-10 | Tampering | `<agent>` argument injection (e.g., shell metacharacters) | mitigate | `<agent>` is matched against the registered agent registry; unknown agents short-circuit before reaching the IPC handler. No shell exec involved. |
| T-124-11 | Denial of Service | Admin spams `/compact` repeatedly | accept | Single-admin host; Phase 105 dispatch queue dedupes in-flight compactions. |
</threat_model>

<verification>
- All vitest cases in `src/__tests__/slash-compact-admin-only.test.ts` pass.
- Local Discord smoke test deferred to operator deploy clearance (D-10).
</verification>

<success_criteria>
- SC-2 closed: admin Discord command works (tested via mocked `handleCompactSession`); non-admin refused; error codes surface to the operator with parity to the CLI.
</success_criteria>

<operator_notes>
- **Dependency on Plan 01:** `handleCompactSession` MUST exist before this plan executes. Wave 2 → Wave 1 dependency is hard.
- **D-08 admin-only:** Mirror Phase 117 `/clawcode-verbose` permission gate. Do NOT invent a parallel admin system.
- **Ephemeral always:** No exception. Compaction results are operator-private; non-admins never see them even by accident.
- **D-10 deploy hold (Ramy active):** Tests run locally with mocked Discord; full Discord round-trip waits for operator clearance.
- **Anti-pattern reminder (`feedback_silent_path_bifurcation.md`):** Before claiming the Discord command works, verify the registration ACTUALLY appears in `tools/list` or its dispatcher equivalent — don't assume the dispatcher picks up new files automatically.
</operator_notes>

<output>
After completion, create `.planning/phases/124-operator-triggered-session-compaction/124-03-SUMMARY.md` covering: command registered (file + line), permission helper reused, embed colors/shape, error-code propagation evidence.
</output>
