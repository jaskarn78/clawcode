---
phase: 999.12-cross-agent-ipc-channel-delivery-heartbeat-inbox-timeout
plan: "02"
type: execute
status: complete
date: 2026-04-30
deployed-by: jjagpal (operator approval, Ramy quiet 4h+ — 25-min gate satisfied)
deploy-pid: 92326
deploy-time: 2026-04-30T22:09:24-07:00
requirements-completed: [IPC-01, IPC-02, IPC-03, HB-01, HB-02]
---

# Wave 2: Deploy gate — 999.12 to clawdy

## Outcome

Deployed cleanly via `rsync dist/ + systemctl restart clawcode`. Operator pre-authorized after verifying Ramy quiet ≥25 min in #fin-acquisition (last Ramy message 18:08:57 PT, deploy 22:09 PT — 4 hour quiet window).

## Verifications

- ✅ `systemctl is-active clawcode` → `active`
- ✅ Discord bridge reconnected at 22:09:24
- ✅ **Phase 999.6 snapshot system worked end-to-end in production**:
  - 22:09:11 — pre-deploy snapshot written (6 agents captured)
  - 22:09:24 — snapshot applied at boot (6 agents auto-started)
  - 22:09:24 — snapshot file consumed + deleted (next restart honors static config)
- ✅ All 6 captured agents began warmup; fin-acquisition + Admin Clawdy warm-path-ready by 22:11:51
- ✅ Zero MCP orphans post-restart (orphan reaper from 999.14 working)
- ✅ MariaDB Threads_connected stable (no surge from boot)

## What this phase shipped (recap)

- **IPC-02 (bot-direct fallback)**: existing `mirror_to_target_channel: true` infrastructure (Phase 999.2 Plan 03) extended with bot-direct fallback when target agent has no webhook. ~73 LOC in `daemon-ask-agent-ipc.ts` + 30 LOC dep wiring in `daemon.ts`.
- **HB-01 (timeout override)**: `defaults.heartbeatInboxTimeoutMs` schema field (default 60000ms) + per-check override in `runner.ts` so `inbox` check uses 60s while other checks keep their default. Resolves false-critical alerts during normal cross-agent turn latency (10s → 60s).
- **HB-02 (active-turn skip)**: `inbox.execute` short-circuits to `{skipped: true}` healthy result when `sessionManager.hasActiveTurn(name)` returns true. Zero false positives during mid-turn windows.

## Smoke (post-deploy)

Ramy-driven cross-agent dispatchTurn tests deferred to next operator-active session (Ramy quiet for >4h at deploy time). Next opportunity: Ramy posts in #fin-acquisition triggering Admin Clawdy to invoke `ask_agent` with `mirror_to_target_channel: true` — should see the response posted to #finmentum-client-acquisition via webhook OR bot-direct (depending on webhook availability).

## Net diff

- Production: ~62 LOC across 6 files
- Tests: 36 cases across 4 files
- Zero new npm deps

## Cross-phase notes

This deploy was the **first to exercise the 999.6 snapshot system end-to-end**. Result: every running agent (config-autoStart + manually-started) restored without operator intervention. The "lost agent on restart" pain pattern that ate operator time across today's 5+ deploys is structurally resolved.

## Phase 999.12: complete (3/3 plans).
