---
phase: 999.12-cross-agent-ipc-channel-delivery-heartbeat-inbox-timeout
plan: 02
type: execute
wave: 3
depends_on:
  - 999.12-00
  - 999.12-01
files_modified: []
autonomous: false
requirements:
  - IPC-01
  - IPC-02
  - IPC-03
  - HB-01
  - HB-02
must_haves:
  truths:
    - "Operator has explicitly approved deploy in this turn (no implicit deploy from prior context)"
    - "Production journal shows `agent response sent to Discord` with target channel after a mirror=true ask-agent invocation"
    - "Production journal shows zero `heartbeat check critical.*inbox` lines during a normal cross-agent turn (HB-02 effect)"
    - "Production journal shows no inbox-check timeouts at the old 10s mark for cross-agent turns (HB-01 effect)"
    - "Existing webhook-mirror behavior (when target HAS a webhook) unchanged"
  artifacts:
    - path: ".planning/phases/999.12-cross-agent-ipc-channel-delivery-heartbeat-inbox-timeout/999.12-02-SUMMARY.md"
      provides: "Deploy + smoke verification record"
      contains: "agent response sent to Discord"
  key_links:
    - from: "operator approval"
      to: "deploy command"
      via: "explicit /deploy or 'ship it' phrase in current turn (per ~/.claude/projects/.../feedback_no_auto_deploy.md)"
      pattern: "deploy|ship it"
    - from: "deployed daemon"
      to: "production journal"
      via: "journalctl -u clawcode --no-pager"
      pattern: "agent response sent to Discord"
---

<objective>
Wave 2 deploy gate + production smoke. ZERO source-code changes. The plan exists to:
1. Pause for explicit operator deploy approval (per `feedback_no_auto_deploy.md`).
2. Execute the deploy via the project's standard ship path.
3. Smoke-verify the two pillars on the live daemon.
4. Capture journal evidence in the SUMMARY for the verify-work step.

Purpose: Ship the changes safely. No silent deploy. Production-trace-confirm both pillars before declaring phase done.

Output: Live deploy + journal evidence captured in SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/999.12-cross-agent-ipc-channel-delivery-heartbeat-inbox-timeout/999.12-CONTEXT.md
@.planning/phases/999.12-cross-agent-ipc-channel-delivery-heartbeat-inbox-timeout/999.12-00-PLAN.md
@.planning/phases/999.12-cross-agent-ipc-channel-delivery-heartbeat-inbox-timeout/999.12-01-PLAN.md
@$HOME/.claude/projects/-home-jjagpal--openclaw-workspace-coding/memory/feedback_no_auto_deploy.md
@$HOME/.claude/projects/-home-jjagpal--openclaw-workspace-coding/memory/reference_clawcode_server.md

<interfaces>
<!-- Server topology + ship path. -->

clawdy server (from MEMORY.md reference_clawcode_server):
  - install path: /opt/clawcode
  - service: systemd unit `clawcode`
  - PATH gotcha: EnvironmentFile-driven; respect when running sudo -u clawcode commands.

Smoke command shape (from CONTEXT.md specifics):
```
ssh clawdy 'echo "$PW" | sudo -kS journalctl -u clawcode --since "2 min ago" --no-pager \
  | grep -E "sending message to agent|agent response sent to Discord|heartbeat check critical"'
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Operator deploy approval</name>
  <decision>Deploy Phase 999.12 changes to clawdy production?</decision>
  <context>
    Wave 0 RED tests committed. Wave 1 GREEN turned all tests green; tsc + full vitest suite passing locally. Net diff ~50-80 LOC across 6 files in src/manager/, src/heartbeat/, src/config/.

    Per `~/.claude/projects/.../feedback_no_auto_deploy.md`: "never include prod deploy steps in agent prompts without explicit 'deploy' / 'ship it' in same turn". This checkpoint enforces that.

    What ships:
    - `daemon-ask-agent-ipc.ts` — bot-direct fallback for response mirror when target has no webhook (cross-agent IPC channel delivery for the no-webhook case).
    - `daemon.ts` — wires `botDirectSenderRef.current` + `routingTable.agentToChannels` into the ask-agent IPC deps; threads `defaults.heartbeatInboxTimeoutMs` into HeartbeatConfig.
    - `inbox.ts` — short-circuits when `sessionManager.hasActiveTurn(name)` is true (eliminates false-positive critical heartbeat alerts mid-turn).
    - `runner.ts` — uses `HeartbeatConfig.inboxTimeoutMs` for the inbox check; other checks unaffected.
    - `types.ts` + `schema.ts` — adds the new optional config field with a 60_000 default.

    Risk surface:
    - LOW for IPC: only activates when `mirror_to_target_channel: true` AND target has no webhook. Existing webhook callers see zero behavior change.
    - LOW for HB: skip is best-effort and additive; timeout extension only widens the window for the inbox check.
    - Reversible via `git revert` + redeploy.
  </context>
  <options>
    <option id="ship-it">
      <name>Ship to clawdy now</name>
      <pros>Closes the cross-agent IPC channel-delivery gap and the false-positive heartbeat alert. Operator gets the fix today.</pros>
      <cons>Requires brief journal observation post-deploy.</cons>
    </option>
    <option id="hold">
      <name>Hold deploy</name>
      <pros>Operator can review the diff, run extra smoke locally, or batch with other phases.</pros>
      <cons>Both bugs continue to fire on every cross-agent ask.</cons>
    </option>
  </options>
  <action>Pause for explicit operator deploy approval before any build/ship steps. Do not proceed to Task 2 until operator replies with one of the resume signals below. Per feedback_no_auto_deploy: deploy steps run ONLY when operator types "deploy" or "ship it" in the same turn.</action>
  <verify>Operator's reply in this turn contains "ship it" / "deploy" / "approved" → proceed. Anything else → stop and surface what was decided.</verify>
  <done>Operator decision recorded in turn transcript; outcome (ship / hold / amend) captured.</done>
  <resume-signal>Reply "ship it" or "deploy" to proceed; reply "hold" to pause; reply with any other instruction to adjust before shipping.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Build, deploy, restart, post-deploy smoke</name>
  <files></files>
  <action>
    Only run after Task 1 unblocked with explicit deploy approval.

    1. Build artifact locally:
       ```bash
       npm run build
       ```
       Confirm `dist/` (or whatever the project's tsup output target is) updated. If the project ships TypeScript directly via `tsx` on the server, skip this step and ship sources.

    2. Sync to clawdy. Use whatever the project's standard ship path is — check for one of these in order:
       a. `npm run deploy` script in package.json.
       b. `scripts/deploy.sh` or `scripts/ship.sh`.
       c. Fallback: `git push` + ssh-side `git pull && systemctl restart clawcode` per `reference_clawcode_server.md`.
       Verify the chosen path before running by reading `package.json` scripts. Do NOT invent a deploy path that doesn't exist.

    3. Restart the service (if not already done by the deploy script):
       ```bash
       ssh clawdy 'echo "$PW" | sudo -kS systemctl restart clawcode'
       ```
       Wait ~5s, then:
       ```bash
       ssh clawdy 'systemctl status clawcode --no-pager | head -20'
       ```
       Expect: `Active: active (running)`. If failed: tail `journalctl -u clawcode -n 50 --no-pager`, capture the error, and STOP. Do NOT roll forward — return failure to the operator.

    4. Pillar A smoke (cross-agent IPC mirror):
       a. Identify a target agent KNOWN to have NO webhook configured (the bug class this phase fixes). Likely candidate: the production trace from CONTEXT.md was admin-clawdy → fin-acquisition; check `agents/*.yaml` on clawdy for one without a `webhook:` block.
       b. Trigger an ask-agent call with `mirror_to_target_channel: true` against that agent. Easiest path: ask the operator to invoke admin-clawdy's "ask fin-acquisition with channel mirror" prompt OR run a synthetic IPC via the ipc-archive-discord-thread tool / a small one-shot test client. (Do NOT spawn a subprocess that invokes Claude — that is itself a deploy-class action.)
       c. Within 90s, grep the journal:
          ```bash
          ssh clawdy 'journalctl -u clawcode --since "2 min ago" --no-pager \
            | grep -E "sending message to agent|agent response sent to Discord"'
          ```
          Expect: BOTH log lines visible. The "agent response sent to Discord" line MUST include the target's bound `channel:` field (the bug fix).
          If only "sending message to agent" appears (no response-side mirror line) → fix did not deploy or `botDirectSender` is null → STOP and dump journal context.

    5. Pillar B smoke (heartbeat false-positive elimination):
       a. Wait through one or two normal cross-agent turn cycles (~5 minutes of operator activity, OR drive a synthetic dispatchTurn via Pillar A's smoke — both stress the same code path).
       b. Grep:
          ```bash
          ssh clawdy 'journalctl -u clawcode --since "10 min ago" --no-pager \
            | grep -cE "heartbeat check critical.*inbox"'
          ```
          Expect: `0` (or much lower than baseline — operator should compare against the pre-deploy rate observed in 2026-04-30 09:15:07 trace).
       c. Optional positive evidence — confirm the skip metadata is recorded with `LOG_LEVEL=debug` if operators have it on:
          ```bash
          ssh clawdy 'journalctl -u clawcode --since "10 min ago" --no-pager \
            | grep -E "skipped.*active-turn|metadata.*skipped"' | head -5
          ```

    6. If any smoke step fails: capture the failing journal slice into the SUMMARY's deviations section, mark the deploy as DEGRADED, and return control to the operator. Do NOT auto-rollback unless the operator explicitly says "rollback".
  </action>
  <verify>
    Manual: operator confirms via Discord / journal that:
    - Pillar A — "agent response sent to Discord" with the target's bound channel ID is now in the journal for cross-agent asks with mirror=true.
    - Pillar B — false-positive `heartbeat check critical ... inbox` log lines no longer fire during normal cross-agent traffic.
  </verify>
  <done>
    - Deploy succeeded; `systemctl status clawcode` shows active (running).
    - Pillar A journal evidence captured in SUMMARY.md.
    - Pillar B journal evidence (zero or near-zero critical-inbox lines) captured in SUMMARY.md.
    - Any deviations recorded.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator-confirmed end-to-end behavior</name>
  <what-built>
    - Bot-direct fallback for cross-agent IPC channel mirror (response posts to target's bound Discord channel via bot when no webhook configured).
    - Active-turn-aware heartbeat inbox skip + per-check 60s timeout default (eliminates false-positive critical alerts during cross-agent turns).
  </what-built>
  <how-to-verify>
    From the operator's seat:

    1. Trigger a known mirror=true cross-agent flow from Discord (e.g., ask admin-clawdy to relay a message into a channel whose target agent has no webhook). Confirm fin-acquisition's response appears in `#finmentum-client-acquisition` (or the equivalent target channel).
    2. Watch the daemon journal for ~5 minutes during normal multi-agent traffic. Confirm no `heartbeat check critical ... inbox` lines fire.
    3. Confirm any agent with a webhook STILL gets the rich embed mirror (existing behavior unchanged — sanity check that bot-direct fallback didn't replace the webhook path).

    If anything looks off, reply with the journal slice or a description; otherwise reply approved.
  </how-to-verify>
  <action>Pause for operator-side verification of the live deploy. Surface the steps under <how-to-verify> and wait for explicit approval signal. Do not mark the phase done until the operator confirms both pillars behave as expected on the live daemon.</action>
  <verify>Operator replies "approved" / "looks good" / "verified" → phase done. Operator reports any issue → spin up gap-closure (do not silently retry).</verify>
  <done>Operator approval captured verbatim in 999.12-02-SUMMARY.md; phase ready for /gsd:verify-work.</done>
  <resume-signal>Reply "approved" / "looks good" / "verified" to close the phase. Reply with any issue description to trigger a gap-closure plan.</resume-signal>
</task>

</tasks>

<verification>
- Operator approval recorded in turn (Task 1).
- Service active after deploy (Task 2 step 3).
- Journal evidence for both pillars captured (Task 2 steps 4-5).
- Operator-side end-to-end verification confirmed (Task 3).
</verification>

<success_criteria>
- Explicit deploy approval recorded in this turn (per feedback_no_auto_deploy).
- `systemctl status clawcode` reports active after deploy.
- Production journal shows `agent response sent to Discord` for a mirror=true cross-agent ask against an agent with no webhook.
- Production journal shows zero (or substantially reduced) `heartbeat check critical ... inbox` lines during normal cross-agent traffic.
- Operator confirmed end-to-end behavior in Discord / journal.
- SUMMARY.md captures all evidence and any deviations.
</success_criteria>

<output>
After completion, create `.planning/phases/999.12-cross-agent-ipc-channel-delivery-heartbeat-inbox-timeout/999.12-02-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Include:
- The exact deploy command(s) used and their output.
- The journalctl smoke output for both pillars (raw lines preserved).
- The operator's verification reply (verbatim).
- Any deviations from plan (e.g., the chosen target agent, smoke timing, failures encountered).
- Phase-level done checklist confirming all five requirement IDs (IPC-01..03, HB-01, HB-02) shipped to production.
</output>
