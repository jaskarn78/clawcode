---
phase: 999.12-cross-agent-ipc-channel-delivery-heartbeat-inbox-timeout
plan: 01
subsystem: heartbeat + manager-ipc
tags: [tdd, green-impl, wave-1, heartbeat, ipc, ask-agent, bot-direct]
requires:
  - 999.12-00 (Wave 0 RED tests)
  - 999.2-03 (handleAskAgentIpc + AskAgentDeps DI surface)
  - 999.8-03 (heartbeat static check registry)
  - 999.11 quick-task-260419-nic (SessionManager.hasActiveTurn)
provides:
  - bot-direct fallback for ask-agent response mirror when target lacks webhook
  - HeartbeatConfig.inboxTimeoutMs (per-check inbox-only timeout override, default 60s)
  - heartbeat inbox check active-turn short-circuit (no false-positive criticals)
  - defaults.heartbeatInboxTimeoutMs schema field (additive-optional, default 60_000)
affects:
  - cross-agent IPC operator orchestration patterns
  - heartbeat critical-alert noise during cross-agent turns (eliminated)
tech-stack:
  added: []
  patterns:
    - "Webhook-first / bot-direct-fallback delivery (lifted shape from triggerDeliveryFn at daemon.ts:2200-2248)"
    - "Per-check timeout special-case in HeartbeatRunner (vs YAGNI generalized perCheckTimeouts map)"
    - "Active-turn-aware heartbeat skip via sessionManager.hasActiveTurn() short-circuit"
    - "Late-bound mutable-ref closure for botDirectSenderRef (Pitfall 7: pre-bridge boot)"
    - "Additive-optional zod schema field (mirror of preDeploySnapshotMaxAgeHours v999.6 SNAP-04)"
key-files:
  created: []
  modified:
    - src/manager/daemon-ask-agent-ipc.ts (+73 lines net)
    - src/manager/daemon.ts (+30 lines net)
    - src/heartbeat/checks/inbox.ts (+13 lines)
    - src/heartbeat/runner.ts (+14 lines net, replaces line 223)
    - src/heartbeat/types.ts (+13 lines)
    - src/config/schema.ts (+13 lines)
    - src/manager/__tests__/ask-agent-ipc.test.ts (removed @ts-expect-error directives)
    - src/heartbeat/__tests__/runner.test.ts (removed @ts-expect-error directives)
    - src/config/__tests__/schema-heartbeat-inbox-timeout.test.ts (removed @ts-expect-error directives)
key-decisions:
  - "Channel-resolution priority: deps.agentChannels?.get(to)?.[0] FIRST (routing-table snapshot), then deps.configs.find(c=>c.name===to)?.channels?.[0] (test-path fallback). Matches the priority the planner wrote into the IPC-02 contract."
  - "Bot-direct path posts PLAIN TEXT (BotDirectSender.sendText), NOT an embed. Webhook path stays embed-formatted (preserves Phase 999.2 visual contract). Visual asymmetry is acceptable — webhook is the common case; bot-direct is the rare no-webhook fallback."
  - "Routed botDirectSenderRef as a NEW positional parameter on routeMethod (positional 25). Considered closing over it from the outer routeMethod-construction site but routeMethod is exported and called from a single site at daemon.ts:3757 — adding one parameter is cleaner than refactoring the closure pattern."
  - "Added `channels?: readonly string[]` to AskAgentAgentConfigLike. Required for the test-path channel resolution fallback (when agentChannels is empty/undefined). Production wires real values via routingTableRef.current.agentToChannels so this fallback rarely fires there."
  - "HeartbeatConfig.inboxTimeoutMs special-case for check.name === 'inbox' is ONE conditional, NOT a generalized perCheckTimeouts map. YAGNI — only the inbox check has the cross-agent-turn problem; other checks (auto-linker, fs-probe, mcp-reconnect) finish in <1s and the fleet 10s default is correct for them."
  - "HB-02 active-turn skip emits NO log line (operator log noise prevention). The metadata.skipped=true + reason='active-turn' on the CheckResult is the audit trail; operators can grep heartbeat.log for skipped:true if needed."
  - "daemon.ts heartbeatConfig spread pattern: only includes inboxTimeoutMs key when defined (avoids inboxTimeoutMs:undefined being explicitly set on HeartbeatConfig and shadowing the absent → fleet-default fallback semantics in runner.ts)."
requirements-completed: [IPC-01, IPC-02, IPC-03, HB-01, HB-02]
duration: 12 min
completed: 2026-05-01
---

# Phase 999.12 Plan 01: Wave 1 GREEN Implementation Summary

Bot-direct fallback for cross-agent IPC response mirror + per-check inbox-timeout override + active-turn-aware heartbeat skip — closes the 2026-04-30 production bugs (fin-acquisition response disappeared from Discord; false-positive heartbeat-inbox-critical alert during a normal cross-agent turn) in ~62 production LOC across 6 files.

## Implementation Walkthrough

### Task 1: Bot-direct fallback in handleAskAgentIpc (IPC-01..03)

**src/manager/daemon-ask-agent-ipc.ts** — added to AskAgentDeps:
```ts
botDirectSender?: { sendText(channelId: string, text: string): Promise<void> };
agentChannels?: ReadonlyMap<string, readonly string[]>;
```
Plus `channels?: readonly string[]` on `AskAgentAgentConfigLike` for the test-path channel resolution. Then a new branch in `handleAskAgentIpc`:

```ts
if (mirror && response && !deps.webhookManager.hasWebhook(to) && deps.botDirectSender) {
  const channelId = deps.agentChannels?.get(to)?.[0]
    ?? deps.configs.find((c) => c.name === to)?.channels?.[0];
  if (!channelId) { /* warn-and-skip */ }
  else {
    try {
      const MAX = 2000; const ELLIPSIS = "...";
      const truncated = response.length > MAX ? response.slice(0, MAX - 3) + ELLIPSIS : response;
      await deps.botDirectSender.sendText(channelId, truncated);
      deps.log.info({ to, channel: channelId, responseLength: response.length }, "agent response sent to Discord");
    } catch (err) { /* warn, never rethrow */ }
  }
}
```

**src/manager/daemon.ts** — three changes:
1. `routeMethod` signature gains positional 25: `botDirectSenderRef: { current: BotDirectSender | null }`.
2. Single call site (line 3757) passes `botDirectSenderRef`.
3. Inside `case "ask-agent"` body, deps gain a late-bound closure that reads `botDirectSenderRef.current` at call time (Pitfall 7), plus `agentChannels: routingTableRef.current.agentToChannels`.

### Task 2: Heartbeat HB-01 + HB-02

**src/heartbeat/types.ts** — `HeartbeatConfig.inboxTimeoutMs?: number`.

**src/heartbeat/runner.ts** — replaced line 223 with:
```ts
const timeoutMs =
  check.name === "inbox" && this.config.inboxTimeoutMs !== undefined
    ? this.config.inboxTimeoutMs
    : (check.timeout ?? this.config.checkTimeoutSeconds) * 1000;
```

**src/heartbeat/checks/inbox.ts** — short-circuit at top of `execute`:
```ts
if (sessionManager.hasActiveTurn(agentName)) {
  return {
    status: "healthy",
    message: "skipped: turn in flight",
    metadata: { skipped: true, reason: "active-turn" },
  };
}
```

**src/config/schema.ts** — `defaults.heartbeatInboxTimeoutMs: z.number().int().positive().default(60_000).optional()` (sibling of `preDeploySnapshotMaxAgeHours`).

**src/manager/daemon.ts** — `heartbeatConfig` spread now propagates `defaults.heartbeatInboxTimeoutMs` into `HeartbeatConfig.inboxTimeoutMs`.

## Tests

| Slice | Tests | Result |
|-------|-------|--------|
| `src/manager/__tests__/ask-agent-ipc.test.ts` | 12 | 12 passed (4 RED-now-GREEN, 8 unchanged) |
| `src/heartbeat/__tests__/` | 30 | 30 passed (2 RED-now-GREEN, 28 unchanged) |
| `src/config/__tests__/schema-heartbeat-inbox-timeout.test.ts` | 5 | 5 passed (5 RED-now-GREEN) |

`npx vitest run src/manager/__tests__/ask-agent-ipc.test.ts src/heartbeat/__tests__/ src/config/__tests__/schema-heartbeat-inbox-timeout.test.ts` — **8 test files, 77 tests passed, 0 failed.**

## Verification (key-link greps — all return hits)

| Key Link | Hits |
|----------|------|
| `botDirectSender\.sendText` in `daemon-ask-agent-ipc.ts` | 1 (line 286) |
| `agent response sent to Discord` in `daemon-ask-agent-ipc.ts` | 1 (line 289) |
| `botDirectSender:` / `agentChannels:` in `daemon.ts` case "ask-agent" | 1 each |
| `hasActiveTurn` in `inbox.ts` | 1 (line 53) |
| `inboxTimeoutMs` in `runner.ts` + `types.ts` | 4 hits |
| `heartbeatInboxTimeoutMs` in `schema.ts` | 1 (line 1351) |

## Diff size

10 files changed, 583 insertions(+), 8 deletions(-). Net production code (excluding tests + comments + JSDoc): **~62 lines**, matching the planner's ~50-80 LOC estimate.

## No new npm deps

`git diff e884497 HEAD -- package.json package-lock.json` is empty.

## Pre-existing test failures (NOT from this work)

The wider repo test suite has ~17 unrelated pre-existing failures in: `bootstrap-integration.test.ts`, `daemon-openai.test.ts`, `daemon-warmup-probe.test.ts`, `dream-prompt-builder.test.ts`, `restart-greeting.test.ts`, `session-config.test.ts`, `session-manager.test.ts`, `clawcode-yaml-phase100*.test.ts` (the last group fails because `clawcode.yaml` is absent from repo root in this workspace).

None of these failing tests reference `routeMethod`, `handleAskAgentIpc`, or anything Phase 999.12 touched. Verified via `grep -E "routeMethod|botDirectSender" <failure-output>` → empty. They appear to be pre-existing WIP from prior uncommitted changes to: secrets-resolver/secrets-collector/secrets-watcher-bridge/recovery/discord-bridge/message-coalescer (visible in `git diff --name-only HEAD` at session start).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `routeMethod` signature does not expose `botDirectSenderRef`**
- **Found during:** Task 1 (daemon wiring step)
- **Issue:** Plan said "wire `botDirectSenderRef.current` into `handleAskAgentIpc` deps", but `botDirectSenderRef` is in the daemon outer closure, NOT a `routeMethod` parameter. The 24-arg `routeMethod` signature didn't expose it.
- **Fix:** Added `botDirectSenderRef` as positional arg #25 on `routeMethod`. Threaded the same ref from the single call site at `daemon.ts:3757`. The closure inside `case "ask-agent"` reads `.current` at call time.
- **Files modified:** `src/manager/daemon.ts`
- **Verification:** `npx vitest run src/manager/__tests__/ask-agent-ipc.test.ts` → all green; `npx tsc --noEmit` for changed files → clean.
- **Commit:** `ae03d36`

**2. [Rule 3 - Blocking] `HeartbeatConfig` not imported in `daemon.ts`**
- **Found during:** Task 2 (daemon heartbeat wiring step)
- **Issue:** Defining `const heartbeatConfig: HeartbeatConfig = {...}` for the spread pattern needed the type. `daemon.ts` already imported `CheckStatus` from `../heartbeat/types.js` but not `HeartbeatConfig`.
- **Fix:** Extended the existing import line: `import type { CheckStatus, HeartbeatConfig } from "../heartbeat/types.js";`
- **Files modified:** `src/manager/daemon.ts`
- **Commit:** `21c4afb`

**Total deviations:** 2 auto-fixed, both Rule 3 (blocking — wiring detail not surfaced in plan but obvious from reading the call site).
**Impact:** None on contract — both deviations are pure plumbing.

## Authentication Gates

None. Pure code change, no external services touched.

## Self-Check: PASSED

- All 6 modified production files exist with the expected greppable patterns
- All 4 commits present (79c61f0, cc6c8c6, ae03d36, 21c4afb)
- All 9 RED assertions from Plan 00 are now GREEN
- All previously-green tests in touched files remain green
- `npx tsc --noEmit` clean for our changed files (pre-existing tsc errors in unrelated files: TS2305 daemon.ts:220, TS2339 daemon.ts:2107, TS2345 daemon.ts:6205, etc.)
- Cross-link with Plan 00 SUMMARY established (this file references `999.12-00-SUMMARY.md`; Plan 00 references `999.12-01-SUMMARY.md`)

## Production Impact (Post-Deploy Expectations)

- **Cross-agent ask-agent with `mirror_to_target_channel: true`** to a target without a webhook (e.g., fin-acquisition) will now post the response to the target's bound channel as plain text. Operator workflow ("Admin Clawdy relays to a team agent") works end-to-end without a manual webhook setup step.
- **Heartbeat false-positive `heartbeat check critical / inbox / timed out after 10000ms`** during normal 30-90s cross-agent turns: eliminated. HB-02 short-circuits while a turn is in flight; HB-01 gives 60s of headroom for any inbox-check that legitimately runs (the rare path: agent wedged outside a turn).

## Next

Phase 999.12 complete. Ready for `/gsd:verify-work 999.12` then deploy gate (per `feedback_no_auto_deploy`).
Cross-link: `999.12-00-SUMMARY.md` for the Wave 0 RED test contract this plan satisfied.
