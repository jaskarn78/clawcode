---
phase: 999.2-a2a-refactor-rename-sync-reply-correlation-ids
verified: 2026-04-29T22:50:00Z
status: passed
score: 12/12 must-haves verified
re_verification: false
human_verification:
  - test: "Admin-clawdy -> fin-acquisition round-trip after deploy"
    expected: "Caller's LLM response includes fin-acquisition's actual answer (not just 'Message sent to...')"
    why_human: "Requires two live agents running + Discord channel observable — not runnable in static verification"
  - test: "mirror_to_target_channel=true end-to-end"
    expected: "Two Discord embeds appear in target's channel (prompt from caller identity, response from target identity with '> reply to:' blockquote prefix)"
    why_human: "Requires Discord webhooks configured + live agent session"
---

# Phase 999.2: a2a-refactor-rename-sync-reply-correlation-ids Verification Report

**Phase Goal:** Fix the agent-to-agent comms architectural debt — rename SessionManager.sendToAgent → dispatchTurn, rename MCP tools (send_message → ask_agent, send_to_agent → post_to_agent) and IPC methods with backwards-compat aliases, and ship v2 sync-reply behavior on ask_agent (response in tool result, mirror_to_target_channel flag, error propagation).
**Verified:** 2026-04-29T22:50:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `SessionManager.dispatchTurn` exists with identical signature to deleted `sendToAgent` | VERIFIED | `session-manager.ts:1107: async dispatchTurn(` |
| 2 | All 7 production call sites invoke `dispatchTurn` (not `sendToAgent`) | VERIFIED | `grep -rn "sendToAgent" src/` returns 0 matches; daemon.ts has 7 occurrences of `dispatchTurn`, escalation.ts line 127, inbox.ts line 90, turn-dispatcher.ts lines 582+597 |
| 3 | Zero occurrences of `sendToAgent` across all of `src/` | VERIFIED | `grep -rn "sendToAgent" src/` = 0 results |
| 4 | MCP tool `ask_agent` registered before `send_message`; `post_to_agent` registered before `send_to_agent` | VERIFIED | `ask_agent@361 < send_message@372`; `post_to_agent@802 < send_to_agent@812` in server.ts |
| 5 | `send_message` registered as `[DEPRECATED — use ask_agent instead]` alias | VERIFIED | server.ts:373: `"[DEPRECATED — use ask_agent instead] ..."` |
| 6 | `send_to_agent` registered as `[DEPRECATED — use post_to_agent instead]` alias | VERIFIED | server.ts:813: `"[DEPRECATED — use post_to_agent instead] ..."` |
| 7 | `IPC_METHODS` contains all 4 names exactly once each (`ask-agent`, `send-message`, `post-to-agent`, `send-to-agent`) | VERIFIED | protocol.ts:67-70; each name appears exactly once |
| 8 | Daemon stacked-case: `case "ask-agent": case "send-message":` and `case "post-to-agent": case "send-to-agent":` share single bodies | VERIFIED | daemon.ts:4170-4171 and 4269-4270 |
| 9 | `ask_agent` MCP tool surfaces target's reply in tool-result text when target is running | VERIFIED | server.ts:334-341: `result.response !== undefined` → returns `` `${to} replied:\n${result.response}` `` |
| 10 | `mirror_to_target_channel` schema field accepted; webhook mirror best-effort | VERIFIED | server.ts:294: `mirror_to_target_channel: z.boolean().default(false)`; daemon-ask-agent-ipc.ts:173-193 + 199-223 wrap each webhook in try/catch |
| 11 | Silent catch removed — `dispatchTurn` errors propagate; MCP renders `Failed to ask {to}: {msg}` | VERIFIED | daemon-ask-agent-ipc.ts:195: `const response = await deps.dispatchTurn(to, content);` — no surrounding try/catch; server.ts:350-355: catch renders `Failed to ask ${to}: ${msg}` |
| 12 | Offline target → `response: undefined`; MCP renders explicit offline text | VERIFIED | daemon-ask-agent-ipc.ts:167-170; server.ts:343-349: `` `Message queued in ${to}'s inbox. ${to} is not running — no synchronous reply.` `` |

**Score:** 12/12 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/manager/session-manager.ts` | `async dispatchTurn` method | VERIFIED | Line 1107 |
| `src/manager/turn-dispatcher.ts` | 2 call sites use `dispatchTurn` | VERIFIED | Lines 582, 597 |
| `src/manager/daemon.ts` | 4 call sites + stacked-case for ask-agent/send-message and post-to-agent/send-to-agent | VERIFIED | 7 `dispatchTurn` occurrences; stacked cases at 4170-4171, 4269-4270 |
| `src/manager/escalation.ts` | 1 call site uses `dispatchTurn` | VERIFIED | Line 127 |
| `src/heartbeat/checks/inbox.ts` | 1 call site uses `dispatchTurn` | VERIFIED | Line 90 |
| `src/mcp/server.ts` | `ask_agent` + `send_message` dual-registration; `post_to_agent` + `send_to_agent` dual-registration; `askAgentHandler` surfaces response | VERIFIED | Lines 360-378, 801-818; full v2 handler at 309-357 |
| `src/ipc/protocol.ts` | All 4 IPC method names present with deprecated annotations | VERIFIED | Lines 67-70; DEPRECATED comments on lines 68, 70 |
| `src/manager/daemon-ask-agent-ipc.ts` | Pure-DI handler module | VERIFIED | File exists; exports `handleAskAgentIpc` + `buildAskAgentDeps` |
| `src/manager/__tests__/phase-999-2-rename.test.ts` | Static-grep regression tests (4 tests) | VERIFIED | File exists; all 4 tests pass |
| `src/manager/__tests__/ask-agent-ipc.test.ts` | Pure-DI handler tests (8 tests) | VERIFIED | File exists; all 8 tests pass |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `turn-dispatcher.ts` | `session-manager.ts dispatchTurn` | calls at lines 582, 597 | WIRED | Confirmed by grep |
| `daemon.ts` | `session-manager.ts dispatchTurn` | calls at lines 1895, 4209, 5000, 5030, 5654 | WIRED | 7 total occurrences |
| `escalation.ts` | `session-manager.ts dispatchTurn` | line 127 | WIRED | Confirmed |
| `heartbeat/checks/inbox.ts` | `session-manager.ts dispatchTurn` | line 90 | WIRED | Confirmed |
| `mcp/server.ts (ask_agent)` | daemon IPC `ask-agent` | `sendIpcRequest(SOCKET_PATH, "ask-agent", ...)` at line 326 | WIRED | Canonical name used |
| `mcp/server.ts (post_to_agent)` | daemon IPC `post-to-agent` | `sendIpcRequest(SOCKET_PATH, "post-to-agent", ...)` at line 775 | WIRED | Canonical name used |
| `daemon.ts case "ask-agent"` | `daemon-ask-agent-ipc.ts handleAskAgentIpc` | dynamic import at line 4204 | WIRED | Matches established daemon pattern |
| `daemon-ask-agent-ipc.ts` | `dispatchTurn` | `deps.dispatchTurn(to, content)` at line 195 — no try/catch | WIRED + ERROR-PROPAGATING | Silent catch is gone |
| `daemon-ask-agent-ipc.ts (mirror)` | `webhookManager.sendAsAgent` | lines 179, 213 wrapped in narrow try/catch | WIRED (best-effort) | Mirror failure logged, never aborts |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|-------------------|--------|
| `mcp/server.ts askAgentHandler` | `result.response` | `handleAskAgentIpc` → `deps.dispatchTurn` → actual agent session | Yes — propagated from SessionManager turn dispatch | FLOWING |
| `daemon-ask-agent-ipc.ts` | `response` | `deps.dispatchTurn(to, content)` — direct call to session manager | Yes — real agent turn response | FLOWING |
| `ipc/protocol.ts IPC_METHODS` | consumed by `z.enum(IPC_METHODS)` | static literal array, no dynamic state | N/A — config, not data | VERIFIED |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Zero `sendToAgent` in entire src/ | `grep -rn "sendToAgent" src/` | 0 matches | PASS |
| `dispatchTurn` in all 4 required prod call-site files | static-grep test (phase-999-2-rename.test.ts) | 4/4 PASS | PASS |
| `ask_agent` registered before `send_message` | line number comparison: 361 < 372 | ask_agent first | PASS |
| `post_to_agent` registered before `send_to_agent` | line number comparison: 802 < 812 | post_to_agent first | PASS |
| All 4 IPC names in `IPC_METHODS` | membership check in protocol.test.ts | each name exactly once | PASS |
| Daemon stacked-case regex match | `case "ask-agent":\n    case "send-message":` | matches at lines 4170-4171 | PASS |
| `result.response` surfaced in tool result | `grep "result\.response" src/mcp/server.ts` | present at line 334 | PASS |
| `mirror_to_target_channel` in schema | `grep "mirror_to_target_channel: z.boolean" src/mcp/server.ts` | present at line 294 | PASS |
| No try/catch around `dispatchTurn` in pure-DI module | grep of try/catch in daemon-ask-agent-ipc.ts | only mirror paths have try/catch | PASS |
| `Failed to ask ${to}` error template | `grep "Failed to ask" src/mcp/server.ts` | present at line 354 | PASS |
| Offline text in MCP wrapper | `grep "is not running" src/mcp/server.ts` | present at line 347 | PASS |
| All targeted tests GREEN | `npx vitest run [5 files]` | 83/83 passed | PASS |
| TypeScript baseline preserved | `npx tsc --noEmit` error count | 108 (matches documented baseline) | PASS |

---

### Requirements Coverage

| Requirement | Wave | Description | Status | Evidence |
|-------------|------|-------------|--------|----------|
| A2A-01 | Wave 1 (Plan 01) | `SessionManager.sendToAgent` renamed to `dispatchTurn` | SATISFIED | `session-manager.ts:1107`; 0 `sendToAgent` occurrences in src/ |
| A2A-02 | Wave 1 (Plan 01) | All 7 production call sites updated | SATISFIED | 7 verified call sites in turn-dispatcher, daemon, escalation, inbox |
| A2A-03 | Wave 1 (Plan 01) | All test mocks + doc-comment refs updated | SATISFIED | 16 test files updated; static-grep test pins zero occurrences |
| A2A-04 | Wave 2 (Plan 02) | MCP `send_message` → `ask_agent` with alias | SATISFIED | Dual registration at server.ts:360-378 |
| A2A-05 | Wave 2 (Plan 02) | MCP `send_to_agent` → `post_to_agent` with alias | SATISFIED | Dual registration at server.ts:801-818 |
| A2A-06 | Wave 2 (Plan 02) | Canonical names registered first in tools/list | SATISFIED | ask_agent@361 < send_message@372; post_to_agent@802 < send_to_agent@812 |
| A2A-07 | Wave 2 (Plan 02) | IPC `send-message` → `ask-agent` with stacked-case alias | SATISFIED | daemon.ts:4170-4171; protocol.ts:67-68 |
| A2A-08 | Wave 2 (Plan 02) | IPC `send-to-agent` → `post-to-agent` with stacked-case alias | SATISFIED | daemon.ts:4269-4270; protocol.ts:69-70 |
| A2A-09 | Wave 3 (Plan 03) | `ask_agent` surfaces target's reply in tool-result text | SATISFIED | server.ts:334-341: `${to} replied:\n${result.response}` |
| A2A-10 | Wave 3 (Plan 03) | `mirror_to_target_channel=true` triggers two webhook embeds; failure doesn't abort | SATISFIED | daemon-ask-agent-ipc.ts:172-223; narrow try/catch on each webhook only |
| A2A-11 | Wave 3 (Plan 03) | `dispatchTurn` errors propagate; MCP renders `Failed to ask {to}: {msg}` | SATISFIED | daemon-ask-agent-ipc.ts:195 (no catch); server.ts:350-355 (error render) |
| A2A-12 | Wave 3 (Plan 03) | Offline target → `response: undefined`; explicit offline text in MCP | SATISFIED | daemon-ask-agent-ipc.ts:167-170; server.ts:343-349 |

All 12 requirements SATISFIED.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `daemon-ask-agent-ipc.ts` | 118-124 | Empty `() => {}` handlers in `buildAskAgentDeps` | Info | Test helper only — these are default stubs for the factory function used in unit tests. Production path constructs deps from real objects. NOT a stub — confirmed by `buildAskAgentDeps` being called only in test code. |

No blockers or warnings found. The one info-level item is the correct pattern for a pure-DI test helper (mirrors `daemon-rate-limit-ipc.ts` blueprint).

---

### Human Verification Required

#### 1. Live round-trip: admin-clawdy → fin-acquisition

**Test:** With both agents running, invoke `ask_agent({to: "fin-acquisition", content: "what's our LTV target?"})` from admin-clawdy's Discord channel.
**Expected:** Admin-clawdy's turn response includes fin-acquisition's actual answer (not just `Message sent to fin-acquisition (id: ...)`). The tool result should read `fin-acquisition replied: [actual answer text]`.
**Why human:** Requires two live Claude Code agent sessions + Discord channel observable. Static verification confirms the code paths exist and are wired, but cannot run a live MCP round-trip.

#### 2. Mirror flag end-to-end

**Test:** Invoke `ask_agent({to: "fin-acquisition", content: "ping", mirror_to_target_channel: true})` from admin-clawdy.
**Expected:** Two webhook embeds appear in fin-acquisition's Discord channel — first from admin-clawdy's identity (the prompt), second from fin-acquisition's identity (the response with `> reply to admin-clawdy:` blockquote prefix).
**Why human:** Requires Discord webhooks configured for the target agent and a live session. Cannot verify embed content or ordering programmatically.

#### 3. Offline path observable

**Test:** Stop fin-acquisition (`clawcode stop fin-acquisition`) then invoke `ask_agent({to: "fin-acquisition", content: "ping"})`.
**Expected:** Tool result reads: `Message queued in fin-acquisition's inbox. fin-acquisition is not running — no synchronous reply.`
**Why human:** Requires controlling agent lifecycle. Code path verified statically — this is operator confirmation of the UX.

---

### Gaps Summary

No gaps found. All 12 requirements are implemented, wired, and covered by passing tests. The two previously-blocked behaviors (response discarded, silent error catch) are both fixed with verified code paths.

---

## TDD Discipline Verification

All 6 commits follow RED-then-GREEN per Wave:

| Commit | Hash | Type | Description |
|--------|------|------|-------------|
| Wave 1 RED | `8af6f01` | test | Static-grep tests pin sendToAgent → dispatchTurn rename |
| Wave 1 GREEN | `cc4a935` | refactor | Rename SessionManager.sendToAgent → dispatchTurn (D-RNI-01) |
| Wave 2 RED | `73a024b` | test | Pin MCP alias registration + IPC method membership + daemon stacked-case |
| Wave 2 GREEN | `f89c291` | feat | Register MCP/IPC aliases (D-RNX-01..04, D-RNI-IPC-01..04) |
| Wave 3 RED | `41af878` | test | Pin sync-reply, mirror, error propagation, offline-path for ask_agent |
| Wave 3 GREEN | `8bb3825` | feat | v2 sync-reply on ask_agent (D-SYN-01..06, fixes 2026-04-29 smoking-gun bug) |

All 6 commits verified present via `git log`.

---

_Verified: 2026-04-29T22:50:00Z_
_Verifier: Claude (gsd-verifier)_
