# Phase 999.2: a2a refactor — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.

**Date:** 2026-04-29
**Phase:** 999.2-a2a-refactor-rename-sync-reply-correlation-ids
**Mode:** `--auto` (recommended defaults)
**Areas discussed:** rename scope, sync-reply behavior, async correlation IDs deferral, plan structure

---

## Rename scope: Option B vs Option C?

| Option | Description | Selected |
|--------|-------------|----------|
| Option B (rename MCP + IPC, keep SessionManager.sendToAgent) | Lower blast radius, MCP-name cleanup is the high-value win | |
| Option C (rename everything top-to-bottom, including internal method) | Clean naming throughout; 7 internal call sites updated | ✓ |

**Auto-selected:** Option C.
**Notes:** ROADMAP entry was filed at "Option C — full" after operator's reorder direction. Internal method rename is a one-shot mechanical refactor and clears the naming smell entirely.

---

## Internal method alias: needed?

| Option | Description | Selected |
|--------|-------------|----------|
| Pure rename, no alias | All 7 call sites + tests update simultaneously; clean | ✓ |
| Add deprecated `sendToAgent` proxy method | Bigger surface; no external consumer needs it | |

**Auto-selected:** Pure rename. Internal method has zero external consumers.

---

## MCP/IPC alias treatment

| Option | Description | Selected |
|--------|-------------|----------|
| Keep old names as aliases pointing to new handlers | LLMs trained on old names + agent SOULs/skills referencing old names continue to work | ✓ |
| Hard cutover, no aliases | Cleaner but breaks any LLM that picks the old name | |
| Aliases with a hard time-bomb (auto-remove after N days) | Forces migration but requires deploy timing | |

**Auto-selected:** Soft aliases without time-bomb. Aliases stay for 30+ days, removed by a follow-up quick task once operator confirms migration.

---

## Sync-reply: response in tool result?

| Option | Description | Selected |
|--------|-------------|----------|
| Include response in MCP tool returned text | Caller's LLM sees the reply directly; the bug fix the operator asked for | ✓ |
| Discard response (current behavior) | Status quo — the bug | |
| Return structured `{messageId, response}` JSON | More expressive but harder for LLM to consume | |

**Auto-selected:** Plain text with reply.

---

## mirror_to_target_channel default value

| Option | Description | Selected |
|--------|-------------|----------|
| Default `false` (opt-in) | Caller has to ask for visibility — minimal token noise | ✓ |
| Default `true` | Default visible audit trail | |

**Auto-selected:** Default false. Mirror posts cost webhook delivery (Discord rate limiter), so opt-in is the polite default.

---

## Error swallowing on dispatchTurn failure

| Option | Description | Selected |
|--------|-------------|----------|
| Propagate to caller as MCP tool error | Caller's LLM sees the failure and can react | ✓ |
| Keep silent + return `{ok: true}` (current bug) | Status quo | |
| Log + return `{ok: false}` | Mute on stdout but distinct return value | |

**Auto-selected:** Propagate.

---

## Async correlation IDs scope

| Option | Description | Selected |
|--------|-------------|----------|
| Defer to follow-up phase (file as 999.8) | Bigger redesign — sync v2 already solves the bug | ✓ |
| Include in this phase as Plan 04 | Bundles all a2a work into one phase | |
| Drop entirely | Lose the async option | |

**Auto-selected:** Defer to 999.8. The sync-reply v2 in this phase fully addresses the operator-blocking pain. Correlation IDs are a polish and bigger architectural change.

---

## Plan structure

| Option | Description | Selected |
|--------|-------------|----------|
| 3 plans (rename internal, rename external+aliases, sync-reply v2) | Each plan is bounded; clean dependency wave 1→2→3 | ✓ |
| 2 plans (rename everything, then sync-reply) | Bundles renames into one large plan | |
| 1 plan with 6+ tasks | Less overhead but harder to review/verify | |

**Auto-selected:** 3 plans, sequential waves.

---

## Deploy timing

**Auto-selected:** No deploy step in any plan.
**Notes:** Per saved feedback memory, deploys require explicit operator confirmation in a separate turn.

---

## Claude's Discretion

- Exact wording of `[DEPRECATED — use ask_agent instead]` annotation in old-tool descriptions
- Whether D-RNX-04 (registration order) lands in Plan 02 or as a follow-up — recommended yes, in Plan 02
- Embed format for mirror posts (D-SYN-04) — Claude reuses or extends `buildAgentMessageEmbed`

## Deferred Ideas (out-of-scope, captured for future)

- Async correlation IDs (D-CORR-01) — files as backlog 999.8 post-phase
- Hard removal of back-compat aliases (future quick task once migration confirmed)
- Cross-channel ask (covered by Phase 105 specialist routing)
