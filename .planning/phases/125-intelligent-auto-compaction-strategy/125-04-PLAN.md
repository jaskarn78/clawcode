---
phase: 125
plan: 04
title: Tier 3 prose summary + A/B verification fixture + latency regression sentinel
type: execute
wave: 4
depends_on:
  - 125-01
  - 125-02
  - 125-03
files_modified:
  - src/manager/compact-extractors/tier3-prose.ts                     # new
  - src/manager/compact-extractors/tier3-payload-truncator.ts         # new — "tool X returned N bytes" rule
  - src/manager/compact-extractors/index.ts                           # wire tier3 into TierPipeline
  - src/manager/compact-extractors/__tests__/tier3-prose.test.ts      # new
  - src/manager/compact-extractors/__tests__/tier3-truncator.test.ts  # new
  - src/manager/compact-extractors/__tests__/ab-fixture.test.ts       # new — SC-5 fuzzy A/B
  - src/manager/compact-extractors/__tests__/fixtures/ab-pre.json     # new — 20-prompt corpus pre-compaction
  - src/manager/compact-extractors/__tests__/fixtures/ab-post.json    # new — same corpus post-compaction
  - src/manager/compact-extractors/__tests__/latency-sentinel.test.ts # new — SC-6 regression baseline pin
autonomous: true
requirements:
  - SC-5  # A/B fuzzy match: pre/post compaction agreement >90% on client-name + task-state + most-recent-feedback (20-prompt corpus)
  - SC-6  # Latency regression sentinel: first-token <8s on synthetic 6h replay (baseline 30-60s pre-compaction)
sentinel: "[125-04-tier3-prose]"
---

# Phase 125 Plan 04 — Tier 3 Prose Summary + A/B Verification + Latency Sentinel

## Goal

Close the tier pipeline with Tier 3 (prose summary for everything else + tool-payload truncation — "tool X returned N bytes"). Build the SC-5 A/B fuzzy fixture (20-prompt corpus, >90% agreement) and the SC-6 latency regression sentinel (post-compaction first-token < 8s on synthetic 6h replay vs. the 30-60s pre-Phase 125 baseline observed 2026-05-13). This plan also captures the pre-deploy regression sentinel data so post-deploy verification has a measurable target.

## Tasks

<task id="T-01" name="Tier 3 prose summarizer + payload truncator">
  <read_first>
    - .planning/phases/125-intelligent-auto-compaction-strategy/125-CONTEXT.md (D-06 sentinel)
    - .planning/phases/125-intelligent-auto-compaction-strategy/125-BACKLOG-SOURCE.md §"TIER 3 — PROSE SUMMARY"
    - src/memory/session-summarizer.ts:48-180 (proven prose-summary prompt + DI summarize callback — REUSE this exact shape)
    - src/manager/compact-extractors/index.ts (Plan 02/03 seam — wire tier3 in same pattern as tier2)
  </read_first>
  <action>
    1. Create `src/manager/compact-extractors/tier3-payload-truncator.ts` exporting `truncateLargePayloads(turns): ConversationTurn[]`:
       - Detect tool_use turns with content > 4KB (base64 PDFs, large MCP responses): replace content with deterministic stub `tool <name> ran, returned a <inferred_type> of <bytes> bytes` per BACKLOG-SOURCE Tier 3 example.
       - Detect tool_result turns with content > 8KB: same truncation.
       - Pure function; no I/O.

    2. Create `src/manager/compact-extractors/tier3-prose.ts` exporting `summarizeAsProse(text, deps): Promise<string>`. Deps mirror Plan 03 Tier 2 shape: `{ summarize, clock, log, agentName }`.
       - Build prompt instructing Haiku: "Summarize the following turns in 2-3 sentences. Focus on what was done and what state the conversation left things in. NO bullet points, NO YAML — flowing prose."
       - 30s timeout; on failure return deterministic fallback string `"[125-04-tier3-prose] compaction summary unavailable (Haiku fallback)"`.
       - Log `[125-04-tier3-prose]` sentinel on first invocation per agent per process lifetime.

    3. Modify `src/manager/compact-extractors/index.ts`: in `buildTieredExtractor`, after Tier 2 runs, apply `truncateLargePayloads` to `toCompact`, then call `summarizeAsProse` to get the rolling prose summary. Return value of `ExtractMemoriesFn`: union of (tier 2 fact chunks, [tier 3 prose string]) — both flow into `memory.db` via the existing `MemoryStore.addMemoryChunks` path.

    4. Tests:
       - `tier3-truncator.test.ts` ≥ 5 cases: 4KB tool_use truncated, 8KB tool_result truncated, small content untouched, deterministic stub format, base64 detection.
       - `tier3-prose.test.ts` ≥ 5 cases: happy path → summary string; timeout → fallback string; sentinel logged once per agent; deps.summarize called exactly once per invocation; empty input early-returns without Haiku call.
  </action>
  <acceptance_criteria>
    - `truncateLargePayloads` is pure (no I/O); stub format matches BACKLOG-SOURCE spec.
    - `summarizeAsProse` honors 30s timeout; never throws.
    - All T-01 tests pass: `npx vitest run src/manager/compact-extractors/__tests__/tier3-truncator.test.ts src/manager/compact-extractors/__tests__/tier3-prose.test.ts`.
  </acceptance_criteria>
</task>

<task id="T-02" name="SC-5 A/B fuzzy fixture — 20-prompt corpus, >90% agreement">
  <read_first>
    - .planning/phases/125-intelligent-auto-compaction-strategy/125-CONTEXT.md (specifics §"Plan 04's A/B fuzzy fixture is non-negotiable")
    - .planning/phases/125-intelligent-auto-compaction-strategy/125-BACKLOG-SOURCE.md §"Acceptance criteria" (scripted comparison test spec)
    - src/manager/compact-extractors/__tests__/fixtures/synthetic-6h-replay.json (Plan 02 T-03 fixture — reuse as the base session)
  </read_first>
  <action>
    1. Build `ab-pre.json` corpus: 20 operator-style prompts spanning the SC-5 categories — client-name recall ("which client did Ramy mention this morning?"), task-state recall ("what's the status of the Acme valuation?"), most-recent-feedback recall ("what rule did Ramy add about charts?"). Each prompt includes an `expected_keywords` array for fuzzy matching.

    2. Build `ab-post.json`: same 20 prompts. Build a deterministic in-memory pipeline: take the Plan 02 synthetic-6h-replay fixture, run it through `partitionForVerbatim` + `buildTieredExtractor` with canned tier2/tier3 outputs (stubbed `summarize` so the test is deterministic + offline). The "post" view contains the verbatim preserved turns + tier 2 facts + tier 3 prose. Render the responses an agent would generate against each prompt as a deterministic projection: for each prompt's `expected_keywords`, scan post-compaction context for keyword presence and emit a synthesized response string.

    3. `ab-fixture.test.ts`: for each of the 20 prompts, compute fuzzy agreement (`expected_keywords` present in BOTH pre and post projection). Assert aggregate agreement > 90% (≥ 18 of 20). Print agreement-per-category breakdown to test output for operator visibility.

    4. **Acknowledged limitation:** the A/B test is structural (does post-compaction context contain the keywords?) not behavioral (does the live agent generate the same response?). End-to-end agent-level A/B requires a deployed live agent and is deferred to the post-deploy verification window. Document this limitation inline in the test file's header comment.
  </action>
  <acceptance_criteria>
    - Fixture files `ab-pre.json` + `ab-post.json` exist; each has 20 prompts with `expected_keywords` arrays.
    - `ab-fixture.test.ts` runs offline (no network, no Haiku) and passes with ≥ 18/20 (90%) agreement on the deterministic projection.
    - Test output prints per-category agreement breakdown (client-name, task-state, feedback-rule).
    - `npx vitest run src/manager/compact-extractors/__tests__/ab-fixture.test.ts` passes.
  </acceptance_criteria>
</task>

<task id="T-03" name="SC-6 latency regression sentinel + pre-deploy baseline pin">
  <read_first>
    - .planning/phases/124-operator-triggered-session-compaction/124-BACKLOG-SOURCE.md (the 2026-05-13 observation: 30-60s first-token, 8.5 MB session)
    - .planning/phases/125-intelligent-auto-compaction-strategy/125-BACKLOG-SOURCE.md §Goals "first-token latency under 8s"
    - src/manager/compact-extractors/__tests__/fixtures/synthetic-6h-replay.json
    - src/manager/active-state/__tests__/builder.test.ts (clock injection pattern — same DI shape)
  </read_first>
  <action>
    1. `latency-sentinel.test.ts`: measure pure-CPU pipeline latency for the full tiered extractor against the synthetic-6h-replay fixture. Run `buildTieredExtractor({ ...stubbed summarize callbacks with 0ms latency ... })` against the full fixture; measure wall-clock time for `partitionForVerbatim` + Tier 4 drop + Tier 2 (stubbed) + Tier 3 (stubbed). Assert local pipeline overhead < 500ms (the SC-6 budget for the in-process portion — actual first-token latency includes worker swap + Haiku network round-trip which are not exercised in unit test).

    2. **Baseline-pin assertion:** record the pre-Plan 02 baseline (raw text concat byte size pre-extraction) into a JSON sidecar file `src/manager/compact-extractors/__tests__/fixtures/latency-baseline.json` with shape:
       ```json
       {
         "recorded_at": "2026-05-14",
         "fixture_path": "synthetic-6h-replay.json",
         "raw_text_bytes": <number>,
         "post_extraction_bytes": <number>,
         "reduction_pct": <number>,
         "production_baseline_first_token_ms": 30000,
         "production_target_first_token_ms": 8000,
         "note": "Production first-token measurements deferred to deploy window. Local pipeline overhead < 500ms is the in-process budget; production measurement via `clawcode usage` against fin-acquisition agent post-deploy."
       }
       ```
       Test asserts `reduction_pct >= 40` (matches SC-3 from Plan 02 — sanity gate that the full pipeline maintains the byte-reduction floor).

    3. Document the **post-deploy verification procedure** in this test file's header comment so the operator knows what to run after Ramy hold lifts:
       ```
       # Post-deploy SC-6 verification (manual, deferred):
       # 1. ssh clawdy "journalctl -u clawcode --since '24h ago' -g '125-04-tier3-prose'"
       #    → confirm Tier 3 fired in production at least once.
       # 2. clawcode usage fin-acquisition --last 5  (after first auto-compact post-deploy)
       #    → confirm first-token latency < 8s.
       # 3. Compare against the 30-60s baseline recorded in 124-BACKLOG-SOURCE.md (2026-05-13 observation).
       ```
  </action>
  <acceptance_criteria>
    - `latency-sentinel.test.ts` passes: local pipeline overhead < 500ms; `reduction_pct >= 40`.
    - `latency-baseline.json` exists with all fields populated; production baseline + target documented.
    - Test header comment includes the post-deploy verification procedure (verbatim, copy-pasteable).
    - All Phase 125 tests pass in aggregate: `npx vitest run src/manager/compact-extractors/ src/manager/active-state/`.
    - Phase 124 regression suite still green (final regression gate): `npx vitest run src/manager/__tests__/compact-session-integration.test.ts src/manager/__tests__/compact-session-mid-turn.test.ts src/manager/__tests__/compaction-event-log.test.ts src/manager/__tests__/heartbeat-status-telemetry.test.ts src/heartbeat/checks/__tests__/context-fill-auto-trigger.test.ts`.
  </acceptance_criteria>
</task>

## Operator Notes

- **Deploy hold (Ramy-active).** Code lands locally; SC-5 and SC-6 are LOCALLY VERIFIABLE via the deterministic fixtures + baseline-pin. The PRODUCTION half of SC-5 (real-agent A/B) and SC-6 (real `clawcode usage` latency measurement) is explicitly deferred and documented inline in the test file. No clawdy redeploy without "deploy"/"ship it".
- **Sentinel:** `[125-04-tier3-prose]`. Post-deploy proof:
  ```bash
  ssh clawdy "journalctl -u clawcode --since '1h ago' -g '125-04-tier3-prose'"
  ```
- **A/B test is structural, not behavioral.** Per T-02 acknowledged limitation: the offline fixture asserts keyword survival through the pipeline, not agent response parity. End-to-end agent A/B requires a deployed live agent — deferred. The structural test is the local-verifiable proxy and is gate-passable.
- **Anti-pattern: silent-path-bifurcation.** No daemon.ts changes in this plan — Plan 03 already collapsed both sites to the seam. Verify with grep `grep -c 'buildTieredExtractor' src/manager/daemon.ts` still equals 2.
- **Tier 3 reuses session-summarizer pattern.** Same 30s timeout + fallback shape as Tier 2 (Plan 03). Cost: 2 Haiku calls per compaction (Tier 2 + Tier 3) — bounded by the 5-min cooldown.
- **Compaction never fails on Tier 3 failure.** Fallback string is a valid summary; pipeline degrades gracefully (Tier 1 verbatim + Tier 4 drop + fallback prose = still a working compaction).
- **Final regression sweep.** This plan is the last in Phase 125 — the T-03 regression gate runs the full Phase 124 + Phase 125 test surface together. Any pre-existing failures from the 124-04-SUMMARY out-of-scope list (e.g., `runner.test.ts` 12-vs-13 check count) are confirmed unchanged, not "fixed" — scope-boundary rule.

## Verification

Closes SC-5 + SC-6 from ROADMAP Phase 125:

- **SC-5** — A/B fuzzy match: pre/post-compaction agreement >90% on client-name + task-state + most-recent-feedback (20-prompt corpus). Closed by T-02 structural fixture; end-to-end agent-level A/B deferred to deploy window with procedure documented.
- **SC-6** — Latency win (operator-visible): post-compaction first-token < 8s. Closed locally by the in-process pipeline overhead < 500ms assertion (the in-process budget) + baseline JSON pin + post-deploy `clawcode usage` procedure. Production measurement deferred.

SC-1 (Plan 01), SC-2/3/8 (Plan 02), SC-4 (Plan 03), and SC-7 (inherited from Phase 124-04) — all closed across Phase 125 in aggregate.
