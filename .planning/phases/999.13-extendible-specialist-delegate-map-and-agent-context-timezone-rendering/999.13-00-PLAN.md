---
phase: 999.13-extendible-specialist-delegate-map-and-agent-context-timezone-rendering
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/__tests__/schema.test.ts
  - src/config/__tests__/loader.test.ts
  - src/shared/__tests__/agent-visible-time.test.ts
  - src/manager/__tests__/context-assembler.test.ts
  - src/manager/__tests__/session-config.test.ts
  - src/discord/__tests__/bridge.test.ts
  - src/memory/__tests__/context-summary.test.ts
  - src/memory/__tests__/conversation-brief.test.ts
  - src/manager/__tests__/dream-prompt-builder.test.ts
  - src/triggers/__tests__/policy-watcher.test.ts
autonomous: true
requirements:
  - DELEG-01
  - DELEG-02
  - DELEG-03
  - DELEG-04
  - TZ-01
  - TZ-02
  - TZ-03
  - TZ-04
  - TZ-05
must_haves:
  truths:
    - "All Wave 0 RED tests run against current main and FAIL because no production code exists yet."
    - "Test suite never throws import errors — every test references symbols that exist (placeholder helper file ok) or asserts the symbol is missing in a way that flips green when implemented."
    - "DST fixture table reflects 2026 US transitions (spring-forward Mar 8, fall-back Nov 1) per RESEARCH.md Pitfall 1 — NOT the wrong CONTEXT.md row #5 (2026-11-02 → PDT)."
  artifacts:
    - path: "src/shared/__tests__/agent-visible-time.test.ts"
      provides: "TZ-01/TZ-03 RED tests — canonical-format, dst-round-trip, bad-iana-fallback, input-types, timezone-resolution-chain"
      contains: "renderAgentVisibleTimestamp"
    - path: "src/config/__tests__/schema.test.ts"
      provides: "DELEG-01/DELEG-03/DELEG-04 + TZ-02 RED test blocks"
      contains: "delegates"
    - path: "src/config/__tests__/loader.test.ts"
      provides: "DELEG-02/DELEG-04 RED tests for renderDelegatesBlock"
      contains: "renderDelegatesBlock"
    - path: "src/manager/__tests__/context-assembler.test.ts"
      provides: "DELEG-02 RED test for delegatesBlock injection in assembleContext"
      contains: "delegatesBlock"
    - path: "src/discord/__tests__/bridge.test.ts"
      provides: "TZ-04 RED tests for formatDiscordMessage TZ-aware ts"
      contains: "renderAgentVisibleTimestamp"
    - path: "src/memory/__tests__/context-summary.test.ts"
      provides: "TZ-04 RED test for saveSummary TZ-aware Generated header"
      contains: "Generated"
  key_links:
    - from: "src/shared/__tests__/agent-visible-time.test.ts"
      to: "src/shared/agent-visible-time.ts (NOT YET CREATED — Plan 02)"
      via: "import { renderAgentVisibleTimestamp, resolveAgentTimezone }"
      pattern: "from \"\\.\\./agent-visible-time\""
    - from: "src/config/__tests__/loader.test.ts"
      to: "src/config/loader.ts (renderDelegatesBlock NOT YET ADDED — Plan 01)"
      via: "import { renderDelegatesBlock, DELEGATES_DIRECTIVE_HEADER, DELEGATES_DIRECTIVE_FOOTER }"
      pattern: "renderDelegatesBlock"
---

<objective>
Wave 0 — RED tests for both pillars. Land all failing tests on a single commit so the GREEN waves (Plans 01 + 02) can run in parallel and each turn its own subset green without authoring new tests mid-flight.

Purpose: TDD discipline. Pin canonical directive text, DST round-trip behavior, schema validation errors, and back-compat byte-stability BEFORE any implementation lands. Once Plan 01 and Plan 02 GREEN their respective subsets, Plan 03 runs the full suite as the gate.

Output: A failing-but-running test suite covering all 9 phase requirements + back-compat regression + PolicyWatcher reload regression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.13-extendible-specialist-delegate-map-and-agent-context-timezone-rendering/999.13-CONTEXT.md
@.planning/phases/999.13-extendible-specialist-delegate-map-and-agent-context-timezone-rendering/999.13-RESEARCH.md

<interfaces>
<!-- Symbols Wave 0 tests reference. Plans 01 and 02 must export these EXACTLY. -->

From src/shared/agent-visible-time.ts (NEW — Plan 02 creates):
```ts
export function renderAgentVisibleTimestamp(date: Date | string, tz?: string): string;
export function resolveAgentTimezone(configTz: string | undefined): string;
```

From src/config/loader.ts (Plan 01 ADDS next to renderSystemPromptDirectiveBlock at line 672):
```ts
export const DELEGATES_DIRECTIVE_HEADER: string;
export const DELEGATES_DIRECTIVE_FOOTER: string;
export function renderDelegatesBlock(delegates: Readonly<Record<string, string>> | undefined): string;
```

From src/config/schema.ts (Plan 01 ADDS to agentSchema near line 1128):
```ts
delegates: z.record(z.string().min(1), z.string().min(1)).optional()
```

From src/config/schema.ts (Plan 02 ADDS to defaultsSchema near line 1295):
```ts
timezone: z.string().optional()
```

From src/manager/context-assembler.ts (Plan 01 ADDS to ContextSources near line 70):
```ts
readonly delegatesBlock?: string;
```
</interfaces>

<canonical_text>
DELEGATES_DIRECTIVE_HEADER (exact, no leading/trailing whitespace):
```
## Specialist Delegation
For tasks matching a specialty below, delegate via the spawn-subagent-thread skill:
```

DELEGATES_DIRECTIVE_FOOTER (exact):
```
Verify the target is at opus/high before delegating; if mismatch, surface to operator and stop. The subthread posts its summary back to your channel when done.
```

Renderer joins HEADER + bullets + FOOTER with "\n" — single newline between adjacent lines (NOT "\n\n"). Bullets sorted alphabetically by specialty key. Bullet format: `- <key> → <value>` (literal U+2192 right arrow, single spaces).

Example output for `{ research: "fin-research", coding: "coding-agent" }`:
```
## Specialist Delegation
For tasks matching a specialty below, delegate via the spawn-subagent-thread skill:
- coding → coding-agent
- research → fin-research
Verify the target is at opus/high before delegating; if mismatch, surface to operator and stop. The subthread posts its summary back to your channel when done.
```
</canonical_text>

<dst_fixtures_corrected>
RESEARCH.md Pitfall 1 verified: 2026 US DST = spring-forward Mar 8, fall-back Nov 1. CONTEXT.md row #5 (`2026-11-02T08:30:00.000Z` → `01:30:00 PDT`) is wrong. Use these fixtures (timeZone="America/Los_Angeles"):

| Input UTC | Expected output |
|-----------|----------------|
| `2026-01-15T08:32:51.000Z` | `2026-01-15 00:32:51 PST` |
| `2026-04-30T18:32:51.000Z` | `2026-04-30 11:32:51 PDT` |
| `2026-03-08T09:30:00.000Z` | `2026-03-08 01:30:00 PST` (1h before spring-forward) |
| `2026-03-08T10:30:00.000Z` | `2026-03-08 03:30:00 PDT` (post-spring-forward) |
| `2026-11-01T08:30:00.000Z` | `2026-11-01 01:30:00 PDT` (fall-back ambiguous, first) |
| `2026-11-01T09:30:00.000Z` | `2026-11-01 01:30:00 PST` (fall-back ambiguous, second) |
| `2026-11-02T08:30:00.000Z` | `2026-11-02 00:30:00 PST` (firmly PST) |
</dst_fixtures_corrected>

# Existing test conventions to follow:
@src/config/__tests__/schema.test.ts
@src/config/__tests__/loader.test.ts
@src/manager/__tests__/context-assembler.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Pillar A schema + renderer tests</name>
  <files>
    src/config/__tests__/schema.test.ts
    src/config/__tests__/loader.test.ts
    src/manager/__tests__/context-assembler.test.ts
    src/manager/__tests__/session-config.test.ts
    src/triggers/__tests__/policy-watcher.test.ts
  </files>
  <behavior>
    All RED on current main. Tests covered:

    schema.test.ts (extend existing file):
    - `delegates-schema`: agentSchema accepts `delegates: { research: "research" }` and parses to `Record<string,string>`. Empty `delegates: {}` parses (no min-keys constraint at schema level).
    - `delegates-schema-invalid`: rejects empty-string keys (`{ "": "x" }`) and empty-string values (`{ a: "" }`).
    - `delegates-superRefine-unknown-target`: configSchema.parse rejects when an agent has `delegates: { research: "ghost-agent" }` and no agent named `ghost-agent` exists. Error message includes both the offending agent name AND the unknown target name.
    - `delegates-superRefine-valid`: all-known-targets parses cleanly across N=3 agents with cross-references.
    - `delegates-back-compat`: existing fixture (no `delegates` field anywhere) parses byte-identically.

    loader.test.ts (extend existing file):
    - `renderDelegatesBlock-undefined`: returns "" exactly.
    - `renderDelegatesBlock-empty`: `{}` returns "" exactly.
    - `renderDelegatesBlock-single`: `{ research: "fin-research" }` matches the canonical 3-line block from `<canonical_text>` byte-exactly.
    - `renderDelegatesBlock-multi-specialty` (DELEG-04): `{ research: "r1", coding: "c1", legal: "l1" }` produces alphabetically-sorted bullets (coding, legal, research order) — assert with full string equality against expected output.
    - `renderDelegatesBlock-deterministic`: render with `{ b: "B", a: "A" }` and `{ a: "A", b: "B" }` — assert byte-identical output (insertion-order independence).
    - `delegates-canonical-text`: import `DELEGATES_DIRECTIVE_HEADER` and `DELEGATES_DIRECTIVE_FOOTER` constants, assert exact string values from `<canonical_text>` block.

    context-assembler.test.ts (extend existing file):
    - `delegates-block-injection`: passing `sources.delegatesBlock = "X"` makes `assembleContext` produce a stableParts entry containing "X" AFTER the toolDefinitions "## Available Tools" section AND AFTER the filesystem-capability block (i.e., last in the stable prefix's tools-and-capability cluster). Assert via index check on stable parts.
    - `delegates-block-empty-baseline`: omitting `delegatesBlock` (undefined) produces byte-identical stablePrefix to passing `delegatesBlock: ""`. Pin baseline hash so back-compat is enforced.

    session-config.test.ts (extend existing file — back-compat fixture):
    - `back-compat-byte-identical`: an agent fixture WITHOUT `delegates` and WITHOUT any `defaults.timezone` config produces byte-identical `stablePrefix` and `systemPrompt` strings to a snapshot baseline. Snapshot is checked into __snapshots__/.

    policy-watcher.test.ts (extend existing file):
    - `reload-with-delegates`: a config with at least one agent declaring `delegates` reloads cleanly via PolicyWatcher.onReload (regression for Phase 999.11 fix — ensure the new schema field doesn't break reload validation).
  </behavior>
  <action>
    1. Read existing test files to learn the project's vitest conventions (describe/it nesting, fixture patterns, fileExists snapshot patterns):
       - `src/config/__tests__/schema.test.ts`
       - `src/config/__tests__/loader.test.ts`
       - `src/manager/__tests__/context-assembler.test.ts`
       - `src/manager/__tests__/session-config.test.ts`
       - `src/triggers/__tests__/policy-watcher.test.ts`

    2. Append a new `describe("Phase 999.13 — DELEG", () => { ... })` block at the BOTTOM of each file (do not interleave into existing blocks — keep the diff surgical and grep-friendly).

    3. Inside each describe, write the test cases listed in `<behavior>`. Imports:
       - schema.test.ts: `import { agentSchema, configSchema } from "../schema";` (or whatever the existing pattern uses)
       - loader.test.ts: `import { renderDelegatesBlock, DELEGATES_DIRECTIVE_HEADER, DELEGATES_DIRECTIVE_FOOTER } from "../loader";` — these symbols don't exist on main yet, so the test file will fail to import. THIS IS EXPECTED RED.
       - context-assembler.test.ts: pass `delegatesBlock` as a new field on the existing `sources` test fixture; the type system will reject it on main (because `ContextSources` doesn't have the field yet) — RED at the type-check step. To keep vitest runnable for the OTHER pillar's tests, use `as ContextSources` cast OR `// @ts-expect-error` so the file still compiles. Document this with a `// Phase 999.13 RED — Plan 01 adds delegatesBlock to ContextSources.` comment.

    4. Use the canonical text from `<canonical_text>` VERBATIM — do not paraphrase. Copy the exact string into the test fixture. Tests pin canonical drift; if the constants ever wobble, these tests catch it.

    5. For `renderDelegatesBlock-deterministic`, build the input objects via `Object.fromEntries(Array.from([["b","B"],["a","A"]]))` and `Object.fromEntries(Array.from([["a","A"],["b","B"]]))` so the test exercises iteration-order independence rather than relying on JS engine insertion-order.

    6. For `back-compat-byte-identical` snapshot: use vitest's `toMatchSnapshot()` against a no-delegates, no-timezone fixture. Snapshot file lands in `src/manager/__tests__/__snapshots__/session-config.test.ts.snap`. Per RESEARCH.md REG-DETERMINISTIC: this snapshot pins the cache-stable prefix so any future drift fails loud.

    7. For `policy-watcher.test.ts`: add a fixture with `agents: [{ ..., delegates: { research: "research" } }]` and call the reload path the existing tests use. THIS WILL FAIL on main because schema doesn't accept `delegates` yet (RED).

    8. DO NOT create `src/shared/agent-visible-time.ts` or any production file. Wave 0 is RED ONLY.

    9. Run `npx vitest run src/config/__tests__/ src/manager/__tests__/context-assembler.test.ts src/manager/__tests__/session-config.test.ts src/triggers/__tests__/policy-watcher.test.ts` — confirm all NEW test cases FAIL (and existing tests still pass). Capture the count of failing tests for the commit message.

    10. `npx tsc --noEmit` may fail on the test files due to missing exports — that's expected for RED. The implementer in Plan 01 will resolve when symbols land. To avoid blocking the parallel Plan 02 work, gate the missing-symbol failures behind `// @ts-expect-error Phase 999.13 RED — Plan 01 implements` comments where needed so OTHER files in the same `tsc --noEmit` pass don't get false-failures attributed to this plan.

    Reference per D-DELEG-01 (CONTEXT.md): canonical text is the source of truth.
  </action>
  <verify>
    <automated>npx vitest run src/config/__tests__/schema.test.ts src/config/__tests__/loader.test.ts src/manager/__tests__/context-assembler.test.ts src/manager/__tests__/session-config.test.ts src/triggers/__tests__/policy-watcher.test.ts 2>&1 | grep -E "FAIL|✗|expected"</automated>
  </verify>
  <done>
    All Pillar A RED test names listed in `<behavior>` exist as `it(...)` blocks. Vitest reports them as FAILING (or unable-to-import for loader.test.ts due to missing exports — that counts as RED). Existing tests in those files still pass. `tsc --noEmit` passes via `@ts-expect-error` annotations on the missing-symbol references.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED — Pillar B helper + call-site tests</name>
  <files>
    src/shared/__tests__/agent-visible-time.test.ts
    src/discord/__tests__/bridge.test.ts
    src/memory/__tests__/context-summary.test.ts
    src/memory/__tests__/conversation-brief.test.ts
    src/manager/__tests__/dream-prompt-builder.test.ts
    src/config/__tests__/schema.test.ts
  </files>
  <behavior>
    All RED on current main. Tests covered:

    agent-visible-time.test.ts (NEW file):
    - `canonical-format`: `renderAgentVisibleTimestamp("2026-04-30T18:32:51.000Z", "America/Los_Angeles")` returns `"2026-04-30 11:32:51 PDT"` byte-exact.
    - `dst-round-trip`: table-driven across all 7 fixtures from `<dst_fixtures_corrected>`. Each row asserts byte-exact output.
    - `bad-iana-fallback`: `renderAgentVisibleTimestamp("2026-04-30T18:32:51.000Z", "Pacific/LosAngeles")` (typo) does NOT throw; returns the UTC rendering (`"2026-04-30 18:32:51 UTC"` or whatever the helper falls back to). Assert no throw + format matches the canonical pattern.
    - `input-types`: helper accepts both `Date` object and ISO `string` and produces identical output for the same instant.
    - `invalid-date`: helper given `"not-a-date"` returns the literal string `"invalid date"` (per RESEARCH.md helper sketch). Assert exact equality.
    - `timezone-resolution-chain`: `resolveAgentTimezone(undefined)` returns either `process.env.TZ` (when set) OR the host TZ from `Intl.DateTimeFormat().resolvedOptions().timeZone`. `resolveAgentTimezone("America/New_York")` returns `"America/New_York"` exactly.
    - `host-tz-default`: when `tz` arg omitted, `renderAgentVisibleTimestamp(date)` uses the host TZ resolution chain. Test by setting/unsetting `process.env.TZ` is fragile (Pitfall 7) — instead, just assert the output's TZ-abbreviation token is non-empty and matches `[A-Z]{2,5}` (no UTC literal hardcoding because CI host TZ may vary).

    bridge.test.ts (extend existing file):
    - `formatDiscordMessage-channel-ts-tz`: feed a `Message` mock with `createdAt: new Date("2026-04-30T18:32:51.000Z")`, render with `agentTz: "America/Los_Angeles"`. Assert the `<channel ... ts="...">` tag contains `ts="2026-04-30 11:32:51 PDT"` (the new TZ-aware string). Existing test (no agentTz threading) currently asserts ISO UTC — that test MUST also be updated to the new behavior; document that the ISO behavior is gone (the helper renders TZ even with no config — host TZ).
    - `formatDiscordMessage-replyingTo-ts-tz`: same for the `<replying-to ts="...">` tag at line 1068 of bridge.ts.
    - The signature change: `formatDiscordMessage` gains an `agentTz: string` parameter. Tests pass it explicitly; production wiring is Plan 02's job.

    context-summary.test.ts (extend existing file):
    - `saveSummary-generated-tz`: call `saveSummary(memoryDir, agentName, summary, agentTz)` (with new TZ param) and read the written file. Assert `**Generated:**` header line matches `^\*\*Generated:\*\* \d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2} [A-Z]{2,5}$` regex (TZ-aware format, NOT ISO).

    conversation-brief.test.ts (NEW or extend existing — confirm during execution):
    - `renderBrief-tz-date`: per Open Question 1 LOCKED YES, `renderBrief` uses TZ-aware date slicing instead of `mem.createdAt.slice(0, 10)`. Test: a memory with `createdAt: "2026-05-01T02:00:00.000Z"` rendered against agentTz="America/Los_Angeles" produces `### Session from 2026-04-30 ...` (operator-perceived date, NOT UTC date).
    - If the existing test asserts UTC slicing, REPLACE that assertion (note in commit body: small operator-visible behavior change, locked by Plan-phase Q1=YES).

    dream-prompt-builder.test.ts (NEW or extend — confirm during execution):
    - `dream-prompt-tz-timestamps`: per Open Question 2 LOCKED YES, `renderChunk` and `renderSummary` (lines 115, 122) emit TZ-aware timestamps when threaded an agentTz. Test asserts the prompt body contains the canonical `YYYY-MM-DD HH:mm:ss [A-Z]{2,5}` format.

    schema.test.ts (extend — append to the same Phase 999.13 describe block from Task 1):
    - `defaults-timezone-schema`: defaultsSchema accepts `timezone: "America/Los_Angeles"` and parses to string. Optional — absence parses cleanly.
    - `defaults-timezone-iana-validation` (per Open Question 3 LOCKED YES): defaultsSchema rejects `timezone: "Pacific/LosAngeles"` (invalid IANA) at parse time with a clear error message. Implementation note for Plan 02: ~5-line zod refinement using `try { new Intl.DateTimeFormat(undefined, { timeZone: tz }) } catch { ctx.addIssue(...) }`.
  </behavior>
  <action>
    1. Create NEW file `src/shared/__tests__/agent-visible-time.test.ts`. Follow vitest conventions from peer test files. Imports:
       ```ts
       import { describe, it, expect } from "vitest";
       import { renderAgentVisibleTimestamp, resolveAgentTimezone } from "../agent-visible-time";
       ```
       The import will fail on main because `src/shared/agent-visible-time.ts` doesn't exist yet — RED.

       To keep vitest's worker from crashing on missing-module before reporting failures, OPTIONALLY land a stub `src/shared/agent-visible-time.ts` with:
       ```ts
       // Phase 999.13 Wave 0 stub — Plan 02 implements.
       export function renderAgentVisibleTimestamp(_d: Date | string, _tz?: string): string {
         throw new Error("not implemented");
       }
       export function resolveAgentTimezone(_t: string | undefined): string {
         throw new Error("not implemented");
       }
       ```
       This makes the test file IMPORTABLE so vitest reports each `it` as RED (assertion failure: "not implemented" thrown). Plan 02 replaces the stub with the real implementation.

       Decision LOCKED: ship the stub. Cleaner RED than module-not-found errors.

    2. Encode the DST table from `<dst_fixtures_corrected>` as a `const FIXTURES = [{ utc: "...", expected: "..." }, ...]` array. Iterate via `it.each(FIXTURES)("...")` so the report shows each fixture independently.

    3. Extend `src/discord/__tests__/bridge.test.ts`:
       - Locate the existing `formatDiscordMessage` test block (search for "formatDiscordMessage").
       - The function on main has signature `formatDiscordMessage(message, downloadResults?, referencedMessage?)`. Plan 02 will add a 4th `agentTz: string` parameter. For Wave 0, write tests against the NEW signature using `// @ts-expect-error Phase 999.13 RED — Plan 02 adds agentTz param` annotation. The test will compile but fail at runtime because the production function ignores the extra arg and still emits ISO UTC.
       - Update existing tests that assert ISO UTC `ts` to assert the new TZ-aware format. If the existing tests use a real Date and assert `toISOString()`, switch to asserting the regex pattern OR a fixed expected string keyed off the test's known clock fixture.

    4. Extend `src/memory/__tests__/context-summary.test.ts`: add `saveSummary-generated-tz` test calling `saveSummary(memoryDir, agentName, summary, "America/Los_Angeles")`. Plan 02 changes the signature. Use `// @ts-expect-error Phase 999.13 RED — Plan 02 adds tz param`.

    5. For `conversation-brief.test.ts` and `dream-prompt-builder.test.ts`:
       - Check whether each test file already exists (`ls src/memory/__tests__/conversation-brief.test.ts src/manager/__tests__/dream-prompt-builder.test.ts`).
       - If exists: extend with the new RED test cases.
       - If missing: create new file with vitest scaffolding. Skip the bulk of pre-existing-behavior tests (those are out of scope) — just write the NEW Phase 999.13 cases.

    6. Append schema.test.ts cases (`defaults-timezone-schema`, `defaults-timezone-iana-validation`) to the same `describe("Phase 999.13 — DELEG", ...)` block from Task 1 — rename block to `describe("Phase 999.13", ...)` since it now covers both pillars, OR add a sibling `describe("Phase 999.13 — TZ", ...)` block. Prefer two sibling blocks for grep-clarity.

    7. Run `npx vitest run src/shared/__tests__/agent-visible-time.test.ts src/discord/__tests__/bridge.test.ts src/memory/__tests__/context-summary.test.ts src/memory/__tests__/conversation-brief.test.ts src/manager/__tests__/dream-prompt-builder.test.ts src/config/__tests__/schema.test.ts` — confirm all NEW Pillar B test cases FAIL.

    Open question resolutions (locked here, re-state in plans 01/02 for the executor):
    - Q1 (conversation-brief.ts:280 UTC date slice → TZ-aware): YES, convert. Tests in this task encode that.
    - Q2 (dream-prompt-builder.ts:115,122 TZ-aware): YES, convert. Tests encode that.
    - Q3 (schema-time IANA TZ pre-validation): YES, ~5 lines in defaultsSchema. Tests encode that.
    - Q4 (Discord embed timestamps): DEFER — out of scope this phase.
  </action>
  <verify>
    <automated>npx vitest run src/shared/__tests__/agent-visible-time.test.ts src/discord/__tests__/bridge.test.ts src/memory/__tests__/context-summary.test.ts src/manager/__tests__/dream-prompt-builder.test.ts src/memory/__tests__/conversation-brief.test.ts 2>&1 | grep -E "FAIL|✗|expected"</automated>
  </verify>
  <done>
    All Pillar B RED test names listed in `<behavior>` exist. Vitest reports them as failing (assertion failures from stub-throws or wrong-format outputs). `src/shared/agent-visible-time.ts` exists as a 2-function stub that throws. `tsc --noEmit` passes (via `@ts-expect-error` annotations on signature-change tests).
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit Wave 0 RED</name>
  <files>
    (commit only — no new files)
  </files>
  <action>
    1. Stage all test files modified/created in Tasks 1 and 2 plus the stub `src/shared/agent-visible-time.ts`.

    2. Confirm via `git status` that NO production files outside `src/shared/agent-visible-time.ts` (the stub) and `__tests__/` directories are staged. Wave 0 is RED-only.

    3. Confirm via `npx vitest run` that the failure count matches expectations from Tasks 1+2. Existing GREEN tests still GREEN. New tests RED.

    4. Commit with message:
    ```
    test(999.13): add Wave 0 RED tests for delegates + TZ rendering

    DELEG-01..04 + TZ-01..05 RED scaffolding. Pins canonical directive
    text, DST round-trip across 2026 US fixtures (Pitfall 1 corrected),
    schema validation errors, byte-stable back-compat snapshot, and
    PolicyWatcher reload regression. Stub agent-visible-time.ts so the
    test file imports cleanly; Plan 02 replaces with real impl.

    Open questions locked:
    - Q1 conversation-brief date slice → TZ-aware: YES
    - Q2 dream-prompt-builder timestamps → TZ-aware: YES
    - Q3 schema IANA TZ pre-validation: YES
    - Q4 Discord embed timestamps: DEFER
    ```

    Use `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "..." --files <paths>` per the repo's commit pattern.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q "999.13.*Wave 0 RED" && git diff HEAD~1 HEAD --stat | grep -E "test\.ts|agent-visible-time\.ts"</automated>
  </verify>
  <done>
    Single commit containing all RED tests + stub helper. `git log -1` shows the test(999.13) commit. The next two plans (01 and 02) can run in parallel against this RED baseline.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. `npx vitest run` — failure count matches expected number of NEW RED tests (existing tests all green).
2. `npx tsc --noEmit` — clean (all `@ts-expect-error` annotations correctly placed).
3. `git diff HEAD~1 HEAD --stat` — shows only test files + the agent-visible-time.ts stub. Zero production code touched.
4. `grep -rn "Phase 999.13" src/*/[__tests__]*` — every Phase 999.13 test block has the marker comment for grep-discoverability.
</verification>

<success_criteria>
- All 9 phase requirements (DELEG-01..04, TZ-01..05) have at least one RED test case under `__tests__/`.
- Canonical DELEGATES_DIRECTIVE_HEADER and DELEGATES_DIRECTIVE_FOOTER text is pinned VERBATIM in `loader.test.ts` (will GREEN once Plan 01 ships the constants).
- DST table reflects 2026 US transitions, NOT 2025 (Pitfall 1 corrected).
- Back-compat snapshot exists in `__snapshots__/` so any future drift to the no-delegates baseline fails loud.
- Stub `src/shared/agent-visible-time.ts` exports the 2 helpers as throwing-stubs so test imports resolve.
- Open questions Q1, Q2, Q3, Q4 are LOCKED in this plan's RED tests (executors of Plans 01/02 don't decide; they implement).
</success_criteria>

<output>
After completion, create `.planning/phases/999.13-extendible-specialist-delegate-map-and-agent-context-timezone-rendering/999.13-00-SUMMARY.md`.
</output>
