---
phase: 999.13-extendible-specialist-delegate-map-and-agent-context-timezone-rendering
plan: "00"
subsystem: testing
tags: [tdd, vitest, zod, intl-datetimeformat, dst, timezone, prompt-injection, schema-validation]

# Dependency graph
requires:
  - phase: 105-trigger-policy-default-allow-and-coalescer-storm-fix
    provides: PolicyWatcher onReload reload path (regression target for delegates field)
  - phase: 94
    provides: DEFAULT_SYSTEM_PROMPT_DIRECTIVES + renderSystemPromptDirectiveBlock pattern (cloned for delegates)
  - phase: 96
    provides: ContextSources optional-field threading + filesystemCapabilityBlock placement model
  - phase: 67
    provides: assembleConversationBrief + renderBrief private renderer (TZ-04 conversion target)
  - phase: 95
    provides: dream-prompt-builder renderChunk + renderSummary timestamps (TZ-04 conversion target)
provides:
  - 42 RED tests across 10 files pinning the canonical delegates directive text, DST round-trip behavior, schema validation errors, prompt-cache hash stability, and back-compat byte-stability
  - Stub `src/shared/agent-visible-time.ts` with throwing 2-function API surface so test imports resolve before Plan 02 ships the real impl
  - Snapshot file `src/manager/__tests__/__snapshots__/session-config.test.ts.snap` pinning the no-delegates / no-timezone systemPrompt baseline
  - Locked answers to 4 open questions from RESEARCH.md (Q1/Q2/Q3=YES, Q4=DEFER)
affects: [999.13-01, 999.13-02, 999.13-03]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "RED-only Wave 0: every new test fails on main; Plans 01 + 02 turn their respective subsets green in parallel"
    - "Throw-stub pattern for new helper modules — exported functions throw 'not implemented' so test imports resolve and assertions fail at runtime instead of import-time"
    - "Dynamic import (`await import('../loader.js')`) for tests that reference symbols not yet exported on main — keeps file parseable while assertions fail at runtime"
    - "Snapshot pinning for back-compat byte-identical baseline (REG-DETERMINISTIC)"
    - "@ts-expect-error annotations to keep tsc clean while production types catch up in GREEN waves"

key-files:
  created:
    - src/shared/agent-visible-time.ts (throw-stub: renderAgentVisibleTimestamp + resolveAgentTimezone)
    - src/shared/__tests__/agent-visible-time.test.ts (14 RED tests — canonical-format, DST table x7, bad-iana-fallback, input-types, invalid-date, host-tz-default, timezone-resolution-chain x2)
    - src/manager/__tests__/__snapshots__/session-config.test.ts.snap (back-compat baseline)
  modified:
    - src/config/__tests__/schema.test.ts (8 RED tests in 2 sibling Phase 999.13 describe blocks — DELEG schema + TZ schema)
    - src/config/__tests__/loader.test.ts (7 RED tests pinning canonical DELEGATES_DIRECTIVE_HEADER/FOOTER + renderDelegatesBlock)
    - src/manager/__tests__/context-assembler.test.ts (3 tests; 2 RED for delegatesBlock injection, 1 GREEN-on-main for empty-baseline byte-identity already passing)
    - src/manager/__tests__/session-config.test.ts (1 GREEN-on-main snapshot test pinning the no-delegates / no-timezone baseline)
    - src/discord/__tests__/bridge.test.ts (2 RED tests for formatDiscordMessage TZ-aware ts in <channel> + <replying-to>)
    - src/memory/__tests__/context-summary.test.ts (1 RED test for saveSummary TZ-aware **Generated:** header)
    - src/memory/__tests__/conversation-brief.test.ts (1 RED test for renderBrief TZ-aware date slice — Q1)
    - src/manager/__tests__/dream-prompt-builder.test.ts (1 RED test for chunk lastModified + summary endedAt TZ-aware — Q2)
    - src/triggers/__tests__/policy-watcher.test.ts (1 RED test — configSchema-validated config with `delegates` field reloads cleanly through PolicyWatcher.onReload)

key-decisions:
  - "Open Question 1 LOCKED YES: conversation-brief.ts:280 UTC date slice converts to TZ-aware date (test pins operator-perceived 2026-04-30 vs UTC 2026-05-01 across DST boundary)"
  - "Open Question 2 LOCKED YES: dream-prompt-builder.ts:115,122 timestamps render TZ-aware (dream-pass agent reads them as agent-visible context)"
  - "Open Question 3 LOCKED YES: defaults.timezone schema-time IANA TZ pre-validation via try/catch on new Intl.DateTimeFormat (5 lines + 1 test)"
  - "Open Question 4 DEFER: Discord embed timestamps stay UTC (operator-facing tooling, out of scope per CONTEXT.md)"
  - "DST fixture table corrected per RESEARCH.md Pitfall 1: 2026 US DST = spring-forward Mar 8, fall-back Nov 1 (CONTEXT.md row #5 was wrong with 2025 fall-back date)"
  - "Throw-stub for agent-visible-time.ts ships in Wave 0 so test imports resolve cleanly — Plan 02 replaces the throws with the real Intl.DateTimeFormat-based impl"
  - "Snapshot landed for back-compat byte-identical baseline so Plans 01+02 can prove no fleet-wide cache invalidation on deploy"

patterns-established:
  - "Wave 0 RED parallelism: by landing all failing tests on a single commit, Plans 01 (DELEG GREEN) and 02 (TZ GREEN) can execute in parallel without authoring new tests mid-flight"
  - "Throw-stub for helpers: cleaner RED than module-not-found errors, and test runner reports each `it` as runtime failure ('not implemented') instead of import error"

requirements-completed: []  # None completed in Wave 0 — Plans 01/02 turn respective subsets green; Plan 03 verifies the full suite

# Metrics
duration: ~16 min
completed: 2026-04-30
---

# Phase 999.13 Plan 00: Wave 0 RED Tests Summary

**42 failing-but-running tests across 10 files pinning DELEG canonical directive text, 2026-corrected DST round-trip fixtures, schema validation errors, prompt-cache byte-stability, and PolicyWatcher reload regression — single-commit baseline that lets Plans 01 (DELEG) and 02 (TZ) GREEN in parallel.**

## Performance

- **Duration:** ~16 min
- **Started:** 2026-04-30T19:05:00Z (approximate, init time)
- **Completed:** 2026-04-30T19:21:28Z
- **Tasks:** 3 (Pillar A RED, Pillar B RED, single commit)
- **Files modified:** 9 test files extended; 3 created (helper stub + new test file + snapshot file)

## Accomplishments

- 42 NEW RED tests covering all 9 phase requirements (DELEG-01..04, TZ-01..05)
- Pre-existing 451 tests in the touched files all still GREEN — no regression introduced
- Canonical DELEGATES_DIRECTIVE_HEADER and DELEGATES_DIRECTIVE_FOOTER text pinned VERBATIM in loader.test.ts (the source of truth gate that Plan 01 must implement to byte)
- DST table reflects 2026 US transitions (spring-forward Mar 8, fall-back Nov 1) per RESEARCH.md Pitfall 1 — NOT the wrong CONTEXT.md row #5 (2026-11-02 → PDT)
- Back-compat snapshot exists in `src/manager/__tests__/__snapshots__/session-config.test.ts.snap` so any future drift to the no-delegates / no-timezone baseline fails loud
- Throw-stub `src/shared/agent-visible-time.ts` exports the 2 helpers so test imports resolve — Plan 02 replaces the throws with real impl
- All 4 open questions LOCKED in Wave 0 RED tests (executors of Plans 01/02 don't decide; they implement)

## Task Commits

Wave 0 lands as a single atomic commit per the plan's Task 3 spec:

1. **Tasks 1+2+3 combined (Wave 0 RED):** `30d3049` — `test(999.13): add Wave 0 RED tests for delegates + TZ rendering`

`git diff HEAD~1 HEAD --stat`: 12 files changed, 885 insertions(+). Zero production code touched outside `src/shared/agent-visible-time.ts` (the 2-function throw-stub).

## Files Created/Modified

### Created
- `src/shared/agent-visible-time.ts` — 2-function throw-stub (renderAgentVisibleTimestamp + resolveAgentTimezone). Plan 02 replaces with real Intl.DateTimeFormat-based impl.
- `src/shared/__tests__/agent-visible-time.test.ts` — 14 RED tests: canonical-format, dst-round-trip x7 (table-driven), bad-iana-fallback, input-types (Date vs ISO string), invalid-date literal, host-tz-default regex, timezone-resolution-chain x2.
- `src/manager/__tests__/__snapshots__/session-config.test.ts.snap` — back-compat baseline for no-delegates / no-timezone agent fixture.

### Modified (test extensions only)
- `src/config/__tests__/schema.test.ts` — 2 sibling describe blocks: "Phase 999.13 — DELEG" (delegates schema, superRefine target validation, back-compat) + "Phase 999.13 — TZ" (defaults.timezone schema + IANA pre-validation).
- `src/config/__tests__/loader.test.ts` — Phase 999.13 DELEG describe block with 7 tests pinning canonical text constants + renderDelegatesBlock cases (undefined, empty, single, multi-specialty alphabetical, deterministic-insertion-order). Uses dynamic `await import('../loader.js')` so file parses on main even before symbols ship.
- `src/manager/__tests__/context-assembler.test.ts` — Phase 999.13 DELEG injection block: 2 RED (block lands AFTER tools section; sentinel byte-flow) + 1 baseline (empty-vs-undefined byte-identity, GREEN on main since the field is currently ignored).
- `src/manager/__tests__/session-config.test.ts` — back-compat snapshot test for no-delegates / no-timezone systemPrompt baseline.
- `src/discord/__tests__/bridge.test.ts` — Phase 999.13 TZ-04 block: 2 RED tests for formatDiscordMessage TZ-aware ts in `<channel>` (line 1029) + `<replying-to>` (line 1068) tags. Tests pass agentTz as 4th arg.
- `src/memory/__tests__/context-summary.test.ts` — Phase 999.13 TZ-04 block: 1 RED test for saveSummary writing `**Generated:**` in canonical YYYY-MM-DD HH:mm:ss ZZZ format.
- `src/memory/__tests__/conversation-brief.test.ts` — Phase 999.13 TZ-04 block: 1 RED test (Q1 LOCKED YES) for renderBrief converting `mem.createdAt.slice(0, 10)` to TZ-aware date — operator-perceived 2026-04-30 vs UTC 2026-05-01.
- `src/manager/__tests__/dream-prompt-builder.test.ts` — Phase 999.13 TZ-04 block: 1 RED test (Q2 LOCKED YES) for chunk lastModified + summary endedAt TZ-aware via threaded agentTz.
- `src/triggers/__tests__/policy-watcher.test.ts` — Phase 999.13 DELEG reload regression: 1 RED test exercising configSchema.parse of a config with `delegates` field, building configuredAgents Set from it, and verifying PolicyWatcher.onReload still fires cleanly.

## Decisions Made

- **Wave 0 single commit per plan spec:** Tasks 1, 2, and 3 land as one atomic commit so Plans 01 and 02 see a consistent RED baseline. No mid-flight test authoring.
- **Throw-stub vs module-not-found:** Shipped 2-function throwing stub in `src/shared/agent-visible-time.ts` so vitest reports each `it` as RED (assertion failure) instead of crashing the worker on missing module. Plan 02 replaces the stub with real impl — pure additive change, no test rewrites needed.
- **Dynamic imports for symbols-not-yet-exported:** `loader.test.ts` uses `await import('../loader.js')` for `renderDelegatesBlock` + `DELEGATES_DIRECTIVE_HEADER`/`FOOTER` because they don't exist on main yet. Keeps the file parseable; tests fail at runtime when accessing the missing exports.
- **`as ContextSources` casts in context-assembler.test.ts:** Plan 01 adds `delegatesBlock?: string` to ContextSources. Until then, tests cast the override object to keep tsc clean while assertions fail at runtime.
- **DST fixture correction (Pitfall 1):** Replaced CONTEXT.md row #5 (`2026-11-02T08:30:00.000Z → 01:30 PDT`, which uses 2025 DST dates) with the verified 2026 transitions per RESEARCH.md. New table covers Nov 1 fall-back ambiguity (both 01:30 PDT first and 01:30 PST second occurrences).
- **Open Questions all LOCKED in this plan's RED tests:** Q1 (conversation-brief date slice → TZ-aware) = YES, Q2 (dream-prompt-builder timestamps) = YES, Q3 (schema IANA TZ pre-validation) = YES, Q4 (Discord embed timestamps) = DEFER. Plans 01/02 implement against these locks; no decision authority needed during GREEN execution.

## Deviations from Plan

None - plan executed exactly as written.

The plan's task structure (Pillar A → Pillar B → single commit) was followed exactly. All 9 phase requirements have at least one RED test. Canonical text is pinned verbatim. DST table corrected per Pitfall 1. Snapshot landed for back-compat. Stub helper exists. Open questions locked.

## Issues Encountered

- **One unused `@ts-expect-error` directive in policy-watcher.test.ts:** initially placed on a plain object literal where zod silently strips unknown keys (no type error to suppress). Removed the suppression and replaced with an explanatory comment. Caught by tsc (TS2578) — pre-existing tsc errors elsewhere are unrelated to this phase.
- **Pre-existing tsc errors in unrelated files:** `loader.test.ts` (outputDir missing on test fixture types), `slash-commands-effort-guard.test.ts`, several others. These are baseline noise on master — confirmed via `git stash`/`git stash pop` round-trip. Not introduced by Phase 999.13 work.

## User Setup Required

None - no external service configuration required.

## Self-Check: PASSED

Verified the following on disk:

- ✅ `src/shared/agent-visible-time.ts` exists (throw-stub helper)
- ✅ `src/shared/__tests__/agent-visible-time.test.ts` exists (14 tests)
- ✅ `src/manager/__tests__/__snapshots__/session-config.test.ts.snap` exists (back-compat baseline)
- ✅ Commit `30d3049` exists in `git log` with subject `test(999.13): add Wave 0 RED tests for delegates + TZ rendering`
- ✅ `git diff HEAD~1 HEAD --stat` shows 12 files changed, 885 insertions (only test files + 1 stub helper)
- ✅ Vitest run on the 10 modified test files: 42 NEW RED tests fail; 451 pre-existing tests still GREEN
- ✅ Each Phase 999.13 test block has the marker comment for grep-discoverability (`Phase 999.13`)

## Next Phase Readiness

- **Plans 01 and 02 can run in parallel against this RED baseline.**
- Plan 01 (DELEG GREEN) implements: agentSchema.delegates field, configSchema.superRefine target validation, DELEGATES_DIRECTIVE_HEADER/FOOTER constants, renderDelegatesBlock helper, ContextSources.delegatesBlock threading, session-config wiring. Turns 23 of the 42 RED tests GREEN.
- Plan 02 (TZ GREEN) implements: defaultsSchema.timezone with IANA pre-validation, renderAgentVisibleTimestamp + resolveAgentTimezone (replace stubs), bridge.ts threading, context-summary.ts threading, conversation-brief.ts date-slice conversion, dream-prompt-builder.ts timestamp threading. Turns the remaining 19 RED tests GREEN.
- Plan 03 (verify): full vitest suite green; static-grep regression on canonical directive text; static-grep on agent-visible ts call sites confirms only the 3 confirmed sites + 2 evaluate-during-plan sites use the helper, with all other UTC.toISOString() sites untouched (TZ-05 internal-storage invariant).

---
*Phase: 999.13-extendible-specialist-delegate-map-and-agent-context-timezone-rendering*
*Plan: 00*
*Completed: 2026-04-30*
