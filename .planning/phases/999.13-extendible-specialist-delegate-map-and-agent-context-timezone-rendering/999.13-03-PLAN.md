---
phase: 999.13-extendible-specialist-delegate-map-and-agent-context-timezone-rendering
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified: []
autonomous: false
requirements:
  - DELEG-01
  - DELEG-02
  - DELEG-03
  - DELEG-04
  - TZ-01
  - TZ-02
  - TZ-03
  - TZ-04
  - TZ-05
user_setup:
  - service: clawdy-host
    why: "Phase 999.13 yaml fan-out + deploy + smoke verification — host-side data + service restart"
    env_vars: []
    dashboard_config:
      - task: "Edit /home/clawcode/.clawcode/clawcode.yaml — add `delegates:` to 8 agents per CONTEXT.md fan-out table"
        location: "ssh clawdy"
      - task: "systemctl restart clawcode (operator approves)"
        location: "ssh clawdy"
      - task: "journalctl -u clawcode --since '5 min ago' — confirm Specialist Delegation block + PT-formatted timestamps in agent prompts"
        location: "ssh clawdy"
must_haves:
  truths:
    - "Full vitest suite GREEN (Pillars A + B merged)."
    - "tsup build clean (npm run build)."
    - "On the deployed clawdy host, agents see '## Specialist Delegation' block in their system prompt (verified via journalctl grep)."
    - "On the deployed clawdy host, agent-visible timestamps render as 'YYYY-MM-DD HH:mm:ss PDT' (or PST in winter)."
    - "fin-acquisition can spawn a fin-research subthread when prompted for a deep dive (functional smoke)."
    - "No deploy without explicit operator confirmation (per ~/.claude/projects MEMORY.md feedback_no_auto_deploy)."
  artifacts:
    - path: "/home/clawcode/.clawcode/clawcode.yaml (on clawdy host)"
      provides: "8-agent delegates fan-out + (optionally) defaults.timezone setting"
      contains: "delegates:"
  key_links:
    - from: "clawdy systemd service"
      to: "rebuilt npm package containing the delegates renderer + TZ helper"
      via: "operator-driven deploy (npm publish + ssh clawdy update)"
      pattern: "Specialist Delegation"
---

<objective>
Wave 3 — gate the merged Pillar A + Pillar B work, then deploy to clawdy on operator approval and verify end-to-end via journalctl smoke.

Purpose: Two parallel implementation plans (01 = DELEG, 02 = TZ) land independently. This plan is the integration gate: full test suite GREEN, build clean, then the operator approves a deploy and verifies the new behavior in production agent context.

Output: Verified-deployed phase. Deploy is gated by an explicit checkpoint per the no-auto-deploy rule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.13-extendible-specialist-delegate-map-and-agent-context-timezone-rendering/999.13-CONTEXT.md
@.planning/phases/999.13-extendible-specialist-delegate-map-and-agent-context-timezone-rendering/999.13-RESEARCH.md
@.planning/phases/999.13-extendible-specialist-delegate-map-and-agent-context-timezone-rendering/999.13-00-PLAN.md
@.planning/phases/999.13-extendible-specialist-delegate-map-and-agent-context-timezone-rendering/999.13-01-PLAN.md
@.planning/phases/999.13-extendible-specialist-delegate-map-and-agent-context-timezone-rendering/999.13-02-PLAN.md

<yaml_fanout>
Per CONTEXT.md decision table — operator applies this exact fan-out to `/home/clawcode/.clawcode/clawcode.yaml` on the clawdy host:

| Agent | Group | `delegates` to add |
|-------|-------|---------------------|
| fin-acquisition | finmentum | `{ research: fin-research }` |
| fin-tax | finmentum | `{ research: fin-research }` |
| fin-playground | finmentum | `{ research: fin-research }` |
| finmentum-content-creator | finmentum | `{ research: fin-research }` |
| test-agent | non-finmentum | `{ research: research }` |
| personal | non-finmentum | `{ research: research }` |
| general | non-finmentum | `{ research: research }` |
| projects | non-finmentum | `{ research: research }` |
| Admin Clawdy | (skip) | — bespoke SOUL contract from quick task 260430-po4 already covers this |
| research | (specialist itself) | (omit) |
| fin-research | (specialist itself) | (omit) |

YAML field shape:
```yaml
agents:
  - name: fin-acquisition
    # ... existing fields ...
    delegates:
      research: fin-research
```
</yaml_fanout>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite gate</name>
  <files></files>
  <action>
    1. Confirm both prerequisite plans landed:
       ```
       git log --oneline | grep -E "999\.13.*(pillar A|pillar B|DELEG|TZ)" | head -5
       ```
       Expect at least 3 commits: Wave 0 RED, Plan 01 (Pillar A), Plan 02 (Pillar B).

    2. Run the full project test suite:
       ```
       npx vitest run
       ```
       PASS gate: zero failures, zero `it.skip`, zero `it.todo` for Phase 999.13. Expected ~14 new GREEN tests beyond the baseline.

    3. Build:
       ```
       npm run build
       ```
       PASS gate: tsup produces clean ESM dist with no tsc errors.

    4. Static-grep regression on the canonical directive text (catches accidental edits):
       ```
       grep -q "## Specialist Delegation" src/config/loader.ts
       grep -q "spawn-subagent-thread skill" src/config/loader.ts
       grep -q "opus/high before delegating" src/config/loader.ts
       ```
       All three must hit.

    5. Anti-list regression — confirm internal-storage call sites are still UTC ISO:
       ```
       git diff main HEAD --stat src/memory/store.ts src/memory/conversation-store.ts src/triggers/dedup.ts src/triggers/scheduler-source.ts src/heartbeat/runner.ts src/manager/effort-state-store.ts src/manager/op-env-resolver.ts src/manager/turn-dispatcher.ts
       ```
       Expect: zero hunks (no lines changed in the anti-list files).

    6. Diff-size sanity:
       ```
       git diff main HEAD --stat | tail -1
       ```
       Expect ~120 lines total across both pillars (50 + 60ish + tests). If much larger, investigate whether scope crept.
  </action>
  <verify>
    <automated>npx vitest run && npm run build && grep -q "## Specialist Delegation" src/config/loader.ts && grep -q "renderAgentVisibleTimestamp" src/shared/agent-visible-time.ts</automated>
  </verify>
  <done>
    Full test suite GREEN. Build clean. Canonical text grep hits. Anti-list files untouched. Phase code is ready for deploy.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Deploy approval gate</name>
  <files></files>
  <action>
    Present the deployment decision to the operator. Pause execution until operator selects an option. Do NOT proceed to Task 3 without an explicit operator response per ~/.claude/projects MEMORY.md feedback_no_auto_deploy.
  </action>
  <verify>
    <automated>echo "Checkpoint task — operator decision required, no automated verification"</automated>
  </verify>
  <done>
    Operator has selected one of approve-deploy / defer-deploy / approve-tz-only and the choice is recorded in this plan's SUMMARY.
  </done>
  <decision>Approve deploy of Phase 999.13 to clawdy host?</decision>
  <context>
    Phase 999.13 changes agent-visible system prompt content (delegation directive injection) and agent-visible timestamp format (ISO UTC → operator-local TZ). Both changes invalidate the SDK prompt cache for affected agents on first turn after deploy (one-time cost, then cache stabilizes on the new shape per the byte-stable invariants pinned by tests).

    Risks:
    - Prompt-cache miss on first turn for each of the 8 agents getting `delegates` (each one re-pays full system-prompt token cost once).
    - Prompt-cache miss on first turn for ALL 14+ agents from the TZ change (every agent's restart-greeting / context-summary file / Discord message wrapper renders differently).
    - Per ~/.claude/projects MEMORY.md feedback_no_auto_deploy: NEVER deploy without explicit operator confirmation in the same turn.

    What the operator needs to do post-approval:
    1. Publish package: `npm publish` (or whatever the project's release process is — confirm with operator).
    2. Update clawdy host: pull new package, restart `clawcode.service`.
    3. Apply yaml fan-out: edit `/home/clawcode/.clawcode/clawcode.yaml` per the `<yaml_fanout>` table above.
    4. (Optional) Set `defaults.timezone: "America/Los_Angeles"` in clawcode.yaml — though host TZ on clawdy is already PDT/PST, so the helper falls through to host detection and works without this. Adding it makes the resolution explicit and resilient if the host moves.
    5. Restart again to pick up the yaml changes (or rely on PolicyWatcher reload — Plan 00 verified this works).
    6. Run smoke verification (Task 3).
  </context>
  <options>
    <option id="approve-deploy">
      <name>Approve — publish + ssh clawdy + apply yaml + restart + smoke</name>
      <pros>Phase ships, operator gets PT-formatted timestamps and yaml-driven delegation today.</pros>
      <cons>One-time prompt-cache miss across the fleet. Operator must run the manual steps (no auto-deploy).</cons>
    </option>
    <option id="defer-deploy">
      <name>Defer — phase code merges, deploy when operator schedules it</name>
      <pros>Code is in main, can ship anytime.</pros>
      <cons>Operator's UTC→PT prompt-tax persists until deployed. Admin Clawdy's `CLAUDE.md` workaround stays in place longer.</cons>
    </option>
    <option id="approve-tz-only">
      <name>Approve TZ ship — defer DELEG yaml fan-out</name>
      <pros>Catches the higher-impact change (TZ affects every agent every turn) faster. DELEG is opt-in per yaml — agents without `delegates:` see no change, so the TZ ship alone is safe.</pros>
      <cons>Operator runs two deploy windows instead of one.</cons>
    </option>
  </options>
  <resume-signal>Type "approve-deploy", "defer-deploy", "approve-tz-only", or describe an alternative path.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Deploy execution (only if Task 2 approved a ship option)</name>
  <files></files>
  <action>
    Conditional on Task 2 outcome.

    **If Task 2 = "defer-deploy":**
    Skip this task. Phase code is in main; STATE.md notes deploy is deferred. Plan 03 SUMMARY records the gate result and exits.

    **If Task 2 = "approve-deploy":**

    1. Publish the package per project release process (confirm exact command with operator — likely `npm version patch && npm publish` or a project-specific script). Operator runs the publish; agent advises on commands.

    2. SSH the clawdy host and update:
       ```bash
       ssh clawdy
       cd /opt/clawcode  # or wherever the deployment lives per MEMORY.md reference_clawcode_server
       npm install --production  # pulls the new package
       sudo systemctl restart clawcode
       ```

    3. Apply the yaml fan-out per `<yaml_fanout>`. Edit `/home/clawcode/.clawcode/clawcode.yaml` — for each of the 8 agents in the table, add the `delegates:` block at the agent level (NOT under `defaults:`). Save.

    4. Trigger config reload — either restart again or let chokidar pick up the file change (project uses chokidar 4.x per CLAUDE.md stack).

    5. Confirm both changes landed:
       ```bash
       # Pillar A: directive injected
       ssh clawdy 'journalctl -u clawcode --since "5 min ago" --no-pager | grep -E "Specialist Delegation|delegates"' | head

       # Pillar B: timestamps in PT
       ssh clawdy 'journalctl -u clawcode --since "5 min ago" --no-pager | grep -E "ts=\"[0-9]{4}-[0-9]{2}-[0-9]{2} [0-9]{2}:[0-9]{2}:[0-9]{2} (PDT|PST|UTC)\"" ' | head
       ```

    **If Task 2 = "approve-tz-only":**

    1. Steps 1-2 above (publish + clawdy update + restart).
    2. SKIP step 3 (no yaml edits).
    3. Step 5 — only check the TZ side:
       ```bash
       ssh clawdy 'journalctl -u clawcode --since "5 min ago" --no-pager | grep -E "ts=\"[0-9]{4}-[0-9]{2}-[0-9]{2} [0-9]{2}:[0-9]{2}:[0-9]{2} (PDT|PST|UTC)\""'
       ```
    4. Note in SUMMARY: DELEG yaml fan-out deferred; operator schedules a follow-up.

    Per ~/.claude/rules/security.md and feedback_no_auto_deploy: agent does NOT execute the deploy autonomously. Agent provides exact commands; operator runs them. Agent may run `ssh clawdy 'journalctl ...'` for verification ONLY after operator confirms restart completed.
  </action>
  <verify>
    <automated>echo "Manual deploy task — verification in Task 4 (checkpoint:human-verify)"</automated>
  </verify>
  <done>
    Per Task 2 outcome: either deploy executed and journalctl confirms new behavior, deploy deferred (recorded in SUMMARY), or partial (TZ-only) deploy with DELEG queued.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator-verified smoke</name>
  <files></files>
  <action>
    Present the smoke-test instructions to the operator. Pause execution. Operator runs the verification steps (Discord interaction, journalctl grep, sqlite check, Admin Clawdy non-regression check) and reports results. Do NOT proceed to Task 5 until operator confirms.
  </action>
  <verify>
    <automated>echo "Checkpoint task — operator-driven smoke verification, no automated verification"</automated>
  </verify>
  <done>
    Operator has reported smoke results (verified or issues). Results are captured for SUMMARY.
  </done>
  <what-built>
    Phase 999.13 deployed to clawdy host (per Task 2 path). Agents on the host now see:
    - **Pillar A (if not deferred):** `## Specialist Delegation` block at the END of their system prompt, alphabetized by specialty key, listing their `delegates:` map from clawcode.yaml.
    - **Pillar B:** All Discord messages, context-summary headers, dream-prompt timestamps, and conversation-brief session dates render as `YYYY-MM-DD HH:mm:ss PDT` (or PST in winter), NOT ISO UTC.
    - **Internal storage unchanged:** memory store, conversation store, trigger events, heartbeat NDJSON, all DB rows still UTC ISO.
  </what-built>
  <how-to-verify>
    1. **Functional smoke (Pillar A — if deployed):**
       In Discord, message fin-acquisition (or any finmentum agent): "Do a deep dive on the latest 13F filings for Berkshire."
       EXPECTED: agent acknowledges, then spawns a `spawn_subagent_thread` to fin-research (visible as a new thread in the channel). The agent does NOT do the research itself.
       PASS if: a fin-research subthread appears.
       FAIL if: agent does the research inline as before. Symptoms — directive not injected (yaml typo? superRefine ate it? grep journalctl for "Specialist Delegation" — if absent, directive isn't reaching the agent).

    2. **Visual smoke (Pillar B):**
       In Discord, send any message to test-agent or general. Expected response time-format references (if any are in the agent's reply) match operator's wall-clock. The agent's INTERNAL view of the message ts is what we care about — verifiable via:
       ```bash
       ssh clawdy 'journalctl -u clawcode --since "2 min ago" --no-pager | grep -oE "ts=\"[^\"]+\"" | head -5'
       ```
       PASS if: ts values match `\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2} (PDT|PST)`.
       FAIL if: ts values still match ISO UTC (`\d{4}-\d{2}-\d{2}T...Z`). Symptoms — package not published, OR clawdy didn't restart, OR `formatDiscordMessage` caller didn't get threaded the new agentTz arg.

    3. **Internal-storage regression check:**
       ```bash
       ssh clawdy 'sqlite3 /home/clawcode/.clawcode/agents/test-agent/memory.db "SELECT createdAt FROM memory_entries ORDER BY rowid DESC LIMIT 3;"'
       ```
       PASS if: rows return ISO UTC format (`2026-04-30T...Z`).
       FAIL if: rows return TZ-aware format. Means TZ-05 invariant broke — investigate immediately, possibly roll back.

    4. **Cache-cost sanity (optional):**
       Within 30 minutes after restart, check that prompt-cache hit rate recovers. SDK telemetry (if exposed) should show cache hits resuming after the first turn per agent. If cache permanently misses, the stable-prefix bytes are wobbling — debug the alphabetical-sort and empty-baseline invariants.

    5. **Admin Clawdy non-regression:**
       Admin Clawdy was intentionally LEFT OUT of the yaml fan-out (bespoke SOUL contract from quick task 260430-po4). Confirm Admin Clawdy's prompt does NOT contain a duplicate "Specialist Delegation" block. Its hand-rolled delegation contract should be the only source.
       PASS: `delegates:` is absent on Admin Clawdy in clawcode.yaml; the hand-rolled SOUL block is intact.
  </how-to-verify>
  <resume-signal>Type "verified" with the smoke results, or describe issues observed for diagnosis.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Phase wrap + STATE update</name>
  <files>
    .planning/STATE.md
    .planning/phases/999.13-extendible-specialist-delegate-map-and-agent-context-timezone-rendering/999.13-03-SUMMARY.md
    .planning/phases/999.13-extendible-specialist-delegate-map-and-agent-context-timezone-rendering/999.13-PHASE-SUMMARY.md
  </files>
  <action>
    1. Write `999.13-03-SUMMARY.md` summarizing Wave 3 outcome (gate result, Task 2 decision, deploy outcome, smoke results).

    2. Write `999.13-PHASE-SUMMARY.md` rolling up Plans 00 + 01 + 02 + 03:
       - Requirements satisfied: DELEG-01..04, TZ-01..05.
       - Final diff stats.
       - Open follow-ups: per RESEARCH.md, Q4 (Discord embed timestamps for slash-command responses) deferred — flag for future phase if operator surfaces friction.
       - Workaround removal: Admin Clawdy's `CLAUDE.md` rule "always convert ts UTC to PT before quoting time" can now be removed (operator action — note in SUMMARY, do not auto-edit Admin Clawdy's workspace).

    3. Update `.planning/STATE.md` — mark Phase 999.13 complete with deploy status (deployed / deferred / partial).

    4. Final commit:
       ```
       docs(999.13): complete phase — extendible delegates + agent-visible TZ
       ```
  </action>
  <verify>
    <automated>ls .planning/phases/999.13-extendible-specialist-delegate-map-and-agent-context-timezone-rendering/999.13-PHASE-SUMMARY.md && grep -q "999.13" .planning/STATE.md</automated>
  </verify>
  <done>
    Phase summary written. STATE.md updated. Final commit lands. Phase 999.13 closed (or marked partial-pending-deploy if Task 2 deferred).
  </done>
</task>

</tasks>

<verification>
1. Full vitest suite GREEN at gate.
2. `npm run build` clean.
3. Static-grep canonical directive text hits in `src/config/loader.ts`.
4. Anti-list files untouched (`git diff main HEAD --stat <anti-list paths>` zero hunks).
5. Operator-driven deploy gate respects feedback_no_auto_deploy.
6. Smoke verification confirms agent-visible behavior matches expectations on the deployed host.
7. Phase summary + STATE update committed.
</verification>

<success_criteria>
- All 9 phase requirements (DELEG-01..04, TZ-01..05) validated end-to-end.
- Gate (Task 1) green: tests + build + greps + anti-list.
- Deploy decision (Task 2) recorded with operator's explicit choice.
- If deployed: journalctl smoke (Task 4) shows "Specialist Delegation" block + PT-formatted timestamps.
- Internal-storage UTC ISO invariant pinned by Task 4 step 3.
- Admin Clawdy untouched (Task 4 step 5).
- Phase summary + STATE update written and committed.
- No auto-deploy without operator approval.
</success_criteria>

<output>
After completion, create `.planning/phases/999.13-extendible-specialist-delegate-map-and-agent-context-timezone-rendering/999.13-03-SUMMARY.md` and `.planning/phases/999.13-extendible-specialist-delegate-map-and-agent-context-timezone-rendering/999.13-PHASE-SUMMARY.md`.
</output>
