---
phase: 119-a2a-delivery-reliability
plan: 02
title: D-05 no_webhook_fallbacks_total counter + dashboard exposure
type: execute
wave: 2
depends_on: ["119-01"]
files_modified:
  - src/manager/fleet-stats.ts
  - src/manager/daemon-post-to-agent-ipc.ts
  - src/manager/daemon.ts
  - src/ui/dashboard/BenchmarksView.tsx
  - src/manager/__tests__/fleet-stats.test.ts
autonomous: true
requirements: [A2A-01]
gap_closure: false

must_haves:
  truths:
    - "Every bot-direct fallback dispatch AND every inbox-only fallback return increments the Prometheus-style counter `no_webhook_fallbacks_total{agent, channel}` via a single shared helper — two call sites, one increment function, no double-counting."
    - "The counter is exposed via the existing Phase 109 `/api/fleet-stats` endpoint as a labeled gauge slice — dashboard reads it without a new endpoint."
    - "A new dashboard tile renders the counter against a 15-minute rolling window; values > 0 in that window render in the SLO-breach styling already established by Phase 999.38 — not silently absorbed."
    - "Counter value is exactly 0 across a 15-minute post-deploy window when both Plan 01's bot-direct fallback AND the webhook path are healthy — SC-5 satisfied."
  artifacts:
    - path: "src/manager/fleet-stats.ts"
      provides: "New `no_webhook_fallbacks_total` counter map keyed by `{agent, channel}`; new exported `incrementNoWebhookFallback(agent: string, channel: string): void` helper; counter map included in the FleetStatsData shape surfaced via /api/fleet-stats."
      contains: "no_webhook_fallbacks_total"
    - path: "src/manager/daemon-post-to-agent-ipc.ts"
      provides: "Two call sites of `incrementNoWebhookFallback(to, channelId)` — one on the bot-direct rung (Plan 01 Task 1) and one on the inbox-only return path (`inboxOnlyResponse` helper)."
      contains: "incrementNoWebhookFallback"
    - path: "src/ui/dashboard/BenchmarksView.tsx"
      provides: "New dashboard tile reading `no_webhook_fallbacks_total` from /api/fleet-stats; renders count + 15-min rolling window; SLO-breach styling from Phase 999.38 applied when value > 0."
      contains: "no_webhook_fallbacks_total"
    - path: "src/manager/__tests__/fleet-stats.test.ts"
      provides: "Regression test pinning the counter increment + exposure shape — invoke increment N times across mixed {agent, channel} pairs, assert the FleetStatsData snapshot contains the expected map."
      min_lines: 40
  key_links:
    - from: "src/manager/daemon-post-to-agent-ipc.ts (bot-direct rung)"
      to: "incrementNoWebhookFallback(to, channelId)"
      via: "single helper call on the rung's success branch — Plan 01 Task 1 inserts the rung; Plan 02 inserts the counter call inside it"
      pattern: "incrementNoWebhookFallback"
    - from: "src/manager/daemon-post-to-agent-ipc.ts (inboxOnlyResponse helper)"
      to: "incrementNoWebhookFallback(to, channelId)"
      via: "same helper called from the inbox-only return path — primary 'no-webhook' or 'webhook-send-failed' reason"
      pattern: "incrementNoWebhookFallback"
    - from: "src/ui/dashboard/BenchmarksView.tsx (new tile)"
      to: "/api/fleet-stats response body"
      via: "fetch + 15-min window aggregation; Phase 999.38 SLO threshold styling for value > 0"
      pattern: "no_webhook_fallbacks_total"
---

<objective>
Add the `no_webhook_fallbacks_total{agent, channel}` Prometheus-style counter that increments on every fallback dispatch (bot-direct OR inbox-only path), expose it via the existing Phase 109 `/api/fleet-stats` endpoint, and surface it on the BenchmarksView dashboard tile using the Phase 999.38 SLO-breach styling so non-zero values are alerted, not silently absorbed (D-05).

Per the ROADMAP sequencing note + advisor reconciliation: Plan 02 has a hard functional dependency on Plan 01 (counter increment call sites live INSIDE the bot-direct rung that Plan 01 inserts). `depends_on=[119-01]` and wave=2. Same-deploy as Plan 01 is fine because no file overlap — they touch different functions in `daemon-post-to-agent-ipc.ts` and different modules elsewhere.

Output:
- Single counter helper `incrementNoWebhookFallback(agent, channel)` in `src/manager/fleet-stats.ts`.
- Two call sites in `daemon-post-to-agent-ipc.ts` — one inside Plan 01's new bot-direct rung, one inside the existing `inboxOnlyResponse` helper.
- FleetStatsData shape extended with the counter map; `/api/fleet-stats` payload surfaces it.
- New dashboard tile in BenchmarksView reading the counter; SLO-breach styling (Phase 999.38) applied for value > 0.
- Regression test in `fleet-stats.test.ts` pinning the increment + exposure contract.
</objective>

<context>
@.planning/phases/119-a2a-delivery-reliability/119-CONTEXT.md
@.planning/phases/119-a2a-delivery-reliability/119-01-PLAN.md
@src/manager/fleet-stats.ts
@src/manager/daemon-post-to-agent-ipc.ts
@src/ui/dashboard/BenchmarksView.tsx

<interfaces>
<!-- Counter helper shape — single source of truth for both call sites -->
```typescript
// In src/manager/fleet-stats.ts
export function incrementNoWebhookFallback(agent: string, channel: string): void;

export type FleetStatsData = Readonly<{
  // ... existing fields ...
  noWebhookFallbacksTotal: ReadonlyMap<string, number>; // key = `${agent}:${channel}`
}>;
```

<!-- Two call sites in daemon-post-to-agent-ipc.ts (Plan 01 inserts the bot-direct rung; Plan 02 adds the counter call) -->
```typescript
// Site 1 — inside bot-direct rung success branch
await deps.botDirectSender.sendText(channelId, message);
incrementNoWebhookFallback(to, channelId);
// Site 2 — inside inboxOnlyResponse helper
function inboxOnlyResponse(...) {
  if (channelId) incrementNoWebhookFallback(to, channelId);
  // ... existing body ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the counter helper + extend FleetStatsData shape</name>
  <files>src/manager/fleet-stats.ts, src/manager/__tests__/fleet-stats.test.ts</files>
  <read_first>
    - src/manager/fleet-stats.ts (full file — existing FleetStatsData shape + assembly pattern)
    - src/config/loader.ts lines 38-100 (the fleet-stats sync contract — counter map shape must keep this happy)
    - .planning/phases/116-dashboard-redesign-modern-ui-mobile-basic-advanced/116-00-PLAN.md (Phase 999.38 SLO threshold infrastructure — reuse, don't reinvent)
  </read_first>
  <behavior>
    - Add a module-scoped `Map<string, number>` keyed by `${agent}:${channel}` (single colon separator — search-friendly in journalctl).
    - Export `incrementNoWebhookFallback(agent: string, channel: string): void` — pure increment (read current, set current+1). Idempotent for the (agent, channel) key.
    - Extend `FleetStatsData` with `noWebhookFallbacksTotal: ReadonlyMap<string, number>`. Assembly function returns a snapshot of the internal map (NOT a reference — pass a shallow copy so the IPC payload is stable).
    - Test cases (write FIRST): (a) increment same `(agent, channel)` 3 times → snapshot returns value=3; (b) increment 5 different `(agent, channel)` pairs once each → snapshot has 5 entries; (c) snapshot is a copy — mutating the returned map does not affect subsequent calls; (d) zero state — fresh import, snapshot is empty.
  </behavior>
  <acceptance_criteria>
    - `incrementNoWebhookFallback` is exported and increments correctly.
    - `FleetStatsData` includes `noWebhookFallbacksTotal` field.
    - Four new vitest cases pass.
    - Existing fleet-stats tests still pass.
    - `tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/manager/__tests__/fleet-stats.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Counter helper + shape extension committed; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the two call sites in daemon-post-to-agent-ipc.ts</name>
  <files>src/manager/daemon-post-to-agent-ipc.ts, src/manager/__tests__/post-to-agent-ipc.test.ts</files>
  <read_first>
    - .planning/phases/119-a2a-delivery-reliability/119-01-PLAN.md (Task 1 — the bot-direct rung this counter call lands inside)
    - src/manager/daemon-post-to-agent-ipc.ts (lines 193-228 — the no-webhook skip and inboxOnlyResponse helper)
    - src/manager/fleet-stats.ts (the new `incrementNoWebhookFallback` exported by Plan 02 Task 1)
  </read_first>
  <behavior>
    - Site 1: Inside the bot-direct rung Plan 01 Task 1 inserted, immediately after `await deps.botDirectSender.sendText(channelId, message)` succeeds, call `incrementNoWebhookFallback(to, channelId)`. The rung's failure path (sendText throws) does NOT increment — it falls through to inbox, which is Site 2.
    - Site 2: Inside `inboxOnlyResponse` helper, add `if (channelId) incrementNoWebhookFallback(to, channelId)` near the top of the function body. The helper currently doesn't take `channelId` as a parameter — extend its signature to accept an optional `channelId?: string` and thread it through the two call sites at lines 190 and 198 + 225.
    - Test cases (extend post-to-agent-ipc.test.ts): (a) successful bot-direct dispatch → counter incremented by 1 for that (agent, channel); (b) bot-direct sendText throws → fall through to inbox, counter incremented exactly ONCE for that (agent, channel) — NOT twice; (c) webhook path succeeds → counter NOT incremented (only fallback paths increment).
    - The fleet-stats counter import is plain ESM — no DI plumbing through `PostToAgentDeps`. The counter is a module-scoped singleton on purpose (mirrors how Phase 109's other fleet-stats accumulators work).
  </behavior>
  <acceptance_criteria>
    - Bot-direct success increments counter once.
    - Inbox-only return (with channelId resolved) increments counter once.
    - Bot-direct success does NOT also fire the inbox path (no double-counting).
    - Webhook success does NOT increment.
    - Three new vitest cases pass; existing post-to-agent-ipc tests still pass.
    - `tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/manager/__tests__/post-to-agent-ipc.test.ts src/manager/__tests__/fleet-stats.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Both call sites wired; no double-counting; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Expose counter via /api/fleet-stats + dashboard tile</name>
  <files>src/manager/daemon.ts, src/ui/dashboard/BenchmarksView.tsx</files>
  <read_first>
    - src/manager/daemon.ts — locate the `case "fleet-stats":` IPC handler (or wherever FleetStatsData is assembled for the IPC reply)
    - src/ui/dashboard/BenchmarksView.tsx (full file — existing tile patterns, Phase 999.38 SLO threshold styling import location)
    - .planning/phases/116-dashboard-redesign-modern-ui-mobile-basic-advanced/116-00-PLAN.md (dashboard surface contract — match the existing tile shape)
    - Any existing tile that uses the SLO-breach (text-danger) styling — clone its shape
  </read_first>
  <behavior>
    - Daemon-side: the `fleet-stats` IPC handler reads the counter map snapshot from `fleet-stats.ts` and includes it in the response payload under `noWebhookFallbacksTotal`. The /api/fleet-stats HTTP route already pipes the IPC reply to JSON — no route-level change needed unless serialization of `Map` is involved (convert to plain object `Record<string, number>` at the IPC boundary for JSON safety).
    - Dashboard-side: new tile in BenchmarksView. Title: "No-Webhook Fallbacks (15min)". Body: count summed over all `(agent, channel)` entries in the response. Styling: when sum > 0, apply the Phase 999.38 SLO-breach class (`text-danger` or whatever the established token is) — DO NOT invent new styling. When sum === 0, neutral `text-fg-3`. Empty state ("no fallbacks") rendered explicitly.
    - The 15-minute window is enforced client-side by tracking the counter delta — store the last-seen value + timestamp in component state, render the 15-min rolling delta. (The counter itself is monotonic since daemon start; the dashboard window is a delta-over-time visualization.)
    - Operator-visible: no new alerts, no new sounds, no email — just visible-on-dashboard, styled to draw the eye. Per D-05 the alert IS the styling.
  </behavior>
  <acceptance_criteria>
    - /api/fleet-stats response includes `noWebhookFallbacksTotal` as a JSON object (Record-shaped, not Map-shaped — JSON-safe).
    - Dashboard tile renders in BenchmarksView at a logical position (near other delivery/SLO tiles).
    - Tile renders with neutral styling when value === 0, SLO-breach styling when value > 0.
    - `tsc --noEmit` clean.
    - Local smoke: start daemon, hit /api/fleet-stats with curl, verify the `noWebhookFallbacksTotal` field is present (empty object when no fallbacks have occurred).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && curl -s http://localhost:3000/api/fleet-stats 2>/dev/null | grep -c "noWebhookFallbacksTotal" || echo "manual curl required if dev server not running"</automated>
  </verify>
  <done>Counter exposed via /api/fleet-stats; dashboard tile present with correct SLO-breach styling toggle on value > 0.</done>
</task>

</tasks>

<operator_notes>
- **No deploy without explicit "deploy" / "ship it".** Per `feedback_ramy_active_no_deploy` and `feedback_no_auto_deploy`.
- **Wave=2 + same-deploy-as-Plan-01.** Plan 02 depends on the bot-direct rung Plan 01 inserts. They can ship in the same `deploy-clawdy.sh` run because they touch different functions in `daemon-post-to-agent-ipc.ts`. The 24h soak window post-deploy is one window that covers both plans for the SC-5 observation.
- **SC-5 verification timing.** The 15-minute window starts at deploy, NOT at code-land. Post-deploy artifact: screenshot the dashboard tile at T+15min showing counter === 0. This artifact lives in the verification step of the phase summary.
- **D-05 — increment helper is the single mutation point.** Resist the temptation to also increment from `webhook-send-failed` retry paths or from Plan 03's icon failure path. The counter name is `no_webhook_fallbacks_total` — it counts fallbacks (events that bypassed webhook), not failures. Webhook 401/404 + reprovision-success is NOT a fallback (Plan 01 Task 2 path resolves cleanly via webhook).
- **Silent-path-bifurcation prevention.** If the counter never increments in prod (no fallbacks ever happen), that's the desired state. But the counter must be present in `/api/fleet-stats` response even when value === 0 — empty `{}` not absent field. The dashboard explicitly renders `value === 0` state, not absent state.
</operator_notes>

<verification>
**Success Criteria from ROADMAP this plan closes:**

| SC | What | How verified |
|----|------|--------------|
| SC-5 | `no_webhook_fallbacks_total{agent, channel}` counter exposed on dashboard stays at 0 across 15-min post-deploy window; non-zero values alerted, not silently absorbed | Post-deploy: dashboard screenshot at T+15min showing counter === 0 (or > 0 with SLO-breach styling). Pre-deploy: counter helper + dashboard tile present + styled correctly. |

**Pre-merge gates:**
- `npx vitest run` — all green, including the new fleet-stats cases.
- `npx tsc --noEmit` — clean.
- `grep -c "incrementNoWebhookFallback" src/manager/daemon-post-to-agent-ipc.ts` returns exactly `2` (the two call sites — bot-direct success + inbox-only return).
- `grep -c "noWebhookFallbacksTotal" src/ui/dashboard/BenchmarksView.tsx` returns at least `1`.
</verification>

<success_criteria>
- Plan 02 closes SC-5.
- Verification artifact (post-deploy, operator-driven) includes dashboard screenshot at T+15min.
- Code committed (orchestrator handles commit). No deploy in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/119-a2a-delivery-reliability/119-02-SUMMARY.md` capturing:
- Files modified.
- Counter helper signature.
- Two call-site grep counts.
- Dashboard tile screenshot (local dev mode if daemon runs locally, or note that post-deploy screenshot is the verification artifact).
- Vitest + tsc results.
</output>
