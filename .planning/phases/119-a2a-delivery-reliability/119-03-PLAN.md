---
phase: 119-a2a-delivery-reliability
plan: 03
title: A2A-03 queue-state icon mutex state machine (⏳ → 👍 → ✅/❌)
type: execute
wave: 2
depends_on: ["119-01"]
files_modified:
  - src/discord/queue-state-icon.ts
  - src/discord/bridge.ts
  - src/discord/__tests__/queue-state-icon.test.ts
autonomous: true
requirements: [A2A-03]
gap_closure: false

must_haves:
  truths:
    - "Queue-state icon transitions through a typed state machine with exactly four states: QUEUED (⏳), IN_FLIGHT (👍), DELIVERED (✅), FAILED (❌); a per-channel mutex serializes transitions so no two transitions for the same channel can interleave."
    - "Each state transition removes the prior reaction emoji AND adds the new one in one ordered Discord API sequence — operator never observes a double-emoji state on the same message (validated visually, NOT unit-testable via Discord)."
    - "Transitions are debounced at 200ms — repeated state changes within the debounce window collapse to the latest target state to stay inside Discord's reaction rate-limit budget."
    - "On Discord rate-limit response (429), the helper backs off and retries up to 3 attempts; after 3 failures the helper logs `[A2A-03] icon-transition-give-up` and leaves the icon in whatever state Discord most recently accepted."
    - "Plan 03 deploys ONLY after Plan 01 has been observably green in production for ≥24h (D-07 sequencing) — this plan's verification includes operator screenshot of the visible transitions, which requires Plan 01's delivery path to be healthy."
  artifacts:
    - path: "src/discord/queue-state-icon.ts"
      provides: "NEW module — typed state enum, per-channel mutex Map, debounced transition function, retry loop with backoff. Exports `transitionQueueState(channelId, messageId, target: QueueState): Promise<void>`."
      min_lines: 80
    - path: "src/discord/bridge.ts"
      provides: "Updated reaction-add flow — replaces the existing inline emoji-add calls with calls into `transitionQueueState(...)`. The integration point is the existing icon code; the new helper is the single mutation point going forward."
      contains: "transitionQueueState"
    - path: "src/discord/__tests__/queue-state-icon.test.ts"
      provides: "Unit tests pinning the state machine semantics — mutex correctness (concurrent calls serialize), debounce window collapses bursts, rate-limit retry budget bounded at 3, terminal states (DELIVERED/FAILED) are sticky."
      min_lines: 60
  key_links:
    - from: "src/discord/bridge.ts (existing reaction-add flow)"
      to: "transitionQueueState(channelId, messageId, target)"
      via: "single function call replaces inline emoji.add + emoji.remove pairs"
      pattern: "transitionQueueState"
    - from: "src/discord/queue-state-icon.ts (transition function body)"
      to: "Discord API reaction add + remove"
      via: "ordered sequence: get prior emoji from in-memory state, call .reactions.remove(prior) THEN .reactions.add(target); both in one mutex section"
      pattern: "reactions\\.(add|remove)"
---

<objective>
Land the queue-state icon state machine (A2A-03 / D-04 / 999.45 source spec) — replace the current ad-hoc reaction-emoji handling in `bridge.ts` with a typed state machine guarded by a per-channel mutex, atomic transitions (remove-prior-then-add-new in one ordered call), 200ms debounce, and bounded rate-limit retry.

Per D-07 sequencing this plan ships in wave 3, AFTER Plan 01 has been observably green in production for ≥24h. The 24h observation is operator-controlled (not a vitest gate) — Plan 03 lands as a commit + local vitest green; deploy is gated on Plan 01's sentinel staying green for the soak window.

Per D-04 + 999.45 source spec:
- States: `QUEUED` (⏳), `IN_FLIGHT` (👍), `DELIVERED` (✅), `FAILED` (❌).
- Per-channel mutex prevents two transitions interleaving on the same channel/message.
- Each transition removes prior + adds new in one ordered Discord API sequence.
- 200ms debounce — repeated transitions within the window collapse to the latest.
- ≤3 retry attempts on Discord rate-limit (429); after 3, log give-up and leave the icon where Discord last accepted it.

SC-3 is operator-visual ("⏳ → 👍 → ✅/❌, no double-emoji") — can't be unit-tested against Discord. Verification artifact = operator screenshot of a fresh A2A turn captured post-deploy. The unit tests pin the state machine logic; the screenshot pins the Discord-side reality.

Output:
- New `src/discord/queue-state-icon.ts` — typed state machine, per-channel mutex, debounce, retry. Pure module — accepts a `DiscordChannelHandle` interface (DI-shaped) so tests can mock without spinning Discord.
- `bridge.ts` reaction-add flow updated to call `transitionQueueState(...)`; inline emoji manipulation removed.
- `queue-state-icon.test.ts` — unit tests for mutex serialization, debounce collapse, retry budget, sticky terminal states.
- Verification artifact (post-deploy, separate operator turn): screenshot of a real A2A turn showing the ⏳ → 👍 → ✅ progression in the operator's Discord channel.
</objective>

<context>
@.planning/phases/119-a2a-delivery-reliability/119-CONTEXT.md
@.planning/phases/119-a2a-delivery-reliability/119-01-PLAN.md
@.planning/phases/999.45-hourglass-to-thumbs-up-when-prompt-leaves-queue/
@src/discord/bridge.ts

<interfaces>
<!-- Public interface of the new module -->
```typescript
// src/discord/queue-state-icon.ts
export type QueueState = 'QUEUED' | 'IN_FLIGHT' | 'DELIVERED' | 'FAILED';

export const QUEUE_STATE_EMOJI: Readonly<Record<QueueState, string>> = {
  QUEUED: '⏳',
  IN_FLIGHT: '👍',
  DELIVERED: '✅',
  FAILED: '❌',
};

export type DiscordChannelHandle = Readonly<{
  addReaction(channelId: string, messageId: string, emoji: string): Promise<void>;
  removeReaction(channelId: string, messageId: string, emoji: string): Promise<void>;
}>;

export function transitionQueueState(
  channelId: string,
  messageId: string,
  target: QueueState,
  discord: DiscordChannelHandle,
): Promise<void>;
```

<!-- Internal state (module-scoped, per-channel) -->
```typescript
// Per-channel mutex + last-state
const channelMutex = new Map<string, Promise<void>>(); // serializes transitions per channel
const channelLastState = new Map<string, QueueState>(); // remembers prior emoji to remove
const channelPendingTarget = new Map<string, QueueState>(); // debounce — latest target wins
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create queue-state-icon.ts module with state machine + mutex + debounce + retry</name>
  <files>src/discord/queue-state-icon.ts, src/discord/__tests__/queue-state-icon.test.ts</files>
  <read_first>
    - .planning/phases/999.45-hourglass-to-thumbs-up-when-prompt-leaves-queue/ (full source spec — 999.45 backlog has the canonical state transition table)
    - src/discord/bridge.ts (grep for `reactions.add`, `reactions.remove`, and the existing icon manipulation sites — the integration points)
    - discord.js v14.26.2 docs — `MessageReaction.users.remove`, `Message.react`, rate-limit error shape (429 / RetryAfter)
  </read_first>
  <behavior>
    - Define `QueueState` enum + `QUEUE_STATE_EMOJI` mapping per the interfaces block above.
    - Implement `transitionQueueState(channelId, messageId, target, discord)`:
      1. Update `channelPendingTarget[channelId] = target` (debounce — newest target wins).
      2. Acquire per-channel mutex: chain the new transition onto `channelMutex[channelId]` (initialize to `Promise.resolve()`). Single in-flight transition per channel.
      3. After mutex acquired, wait 200ms (debounce window); after the wait, re-read `channelPendingTarget[channelId]` — proceed ONLY if it still equals `target`. If a newer target arrived during the wait, this transition no-ops (the newer one will run after).
      4. Read prior state from `channelLastState[channelId]`. If prior === target, no-op (idempotent).
      5. Terminal-state sticky check: if prior is `DELIVERED` or `FAILED`, refuse further transitions (log + return). Plan-03 default: terminal is sticky for the message's lifetime.
      6. Execute transition: `await discord.removeReaction(channelId, messageId, EMOJI[prior])` (skip if no prior), then `await discord.addReaction(channelId, messageId, EMOJI[target])`. Each call wrapped in retry loop (≤3 attempts on 429-shaped errors with exponential backoff: 200ms, 500ms, 1000ms).
      7. On success: `channelLastState[channelId] = target`.
      8. On terminal failure after retry budget: log `[A2A-03] icon-transition-give-up {channelId, messageId, prior, target}`; do NOT update `channelLastState` (operator sees whatever Discord most recently accepted).
    - Test cases (write FIRST):
      - (a) Single QUEUED → IN_FLIGHT → DELIVERED sequence on one channel — verify exactly removeReaction(⏳) + addReaction(👍), then removeReaction(👍) + addReaction(✅).
      - (b) Concurrent calls on same channel — fire 3 transitions back-to-back without awaiting; verify they serialize (no interleaving of remove/add).
      - (c) Debounce burst — fire QUEUED, then IN_FLIGHT within 50ms; verify the IN_FLIGHT target wins (only one transition executed).
      - (d) Rate-limit retry — mock addReaction to throw 429 twice then succeed; verify exactly 3 add calls, then success; verify backoff timing.
      - (e) Rate-limit budget exceeded — mock addReaction to throw 429 four times; verify 3 attempts then give-up log; verify `channelLastState` unchanged.
      - (f) Terminal sticky — set state to DELIVERED, attempt transition to IN_FLIGHT; verify no Discord calls, log emitted.
      - (g) Idempotent — state already QUEUED, attempt transition to QUEUED; verify no Discord calls.
      - (h) Concurrent across different channels — fire QUEUED on channelA AND channelB; verify both proceed in parallel (no cross-channel blocking).
  </behavior>
  <acceptance_criteria>
    - All 8 vitest cases pass.
    - Module is a pure standalone file with no Discord-client import — uses the `DiscordChannelHandle` DI interface.
    - `tsc --noEmit` clean.
    - `grep -c "QueueState" src/discord/queue-state-icon.ts` returns at least 4 (type + three uses).
    - Per-channel mutex correctness: concurrent calls on same channel produce exactly N transition sequences in order (no interleaving). Concurrent calls on different channels run in parallel.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/discord/__tests__/queue-state-icon.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>State machine module + tests committed.</done>
</task>

<task type="auto">
  <name>Task 2: Wire transitionQueueState into bridge.ts reaction flow</name>
  <files>src/discord/bridge.ts</files>
  <read_first>
    - src/discord/bridge.ts full file — locate the existing reaction-add sites (grep for `reactions.add`, `.react(`, `⏳`, `👍`, `✅`, `❌`)
    - src/discord/queue-state-icon.ts (Plan 03 Task 1 output)
  </read_first>
  <behavior>
    - Construct a `DiscordChannelHandle` adapter at the top of `bridge.ts` that wraps the existing Discord.js client into the simple `addReaction`/`removeReaction` interface. Use the channel-cache + message-fetch pattern already present in bridge.ts; do NOT introduce new Discord client construction.
    - At each existing site where bridge.ts manipulates a queue-state emoji (the ⏳ enqueue site, the 👍 SDK-call-started site, the ✅ delivery site, the ❌ failure site), REPLACE the inline `.react(...)` and `.users.remove(...)` calls with a single call to `transitionQueueState(channelId, messageId, target, discordHandle)`.
    - The state-machine module owns the prior-emoji-to-remove tracking — bridge.ts no longer needs to remember which emoji it last added. This eliminates the source of the existing double-emoji races (bridge currently can race itself when two enqueue-events fire near simultaneously).
    - Add a defensive fallback: if `transitionQueueState` rejects (which it shouldn't — internal retry handles 429), log warn and continue. Reaction failure must NEVER abort the delivery pipeline.
  </behavior>
  <acceptance_criteria>
    - All four queue-state emoji sites in bridge.ts now call `transitionQueueState(...)` — verify via grep: `grep -c "transitionQueueState" src/discord/bridge.ts >= 4`.
    - The inline `.react()` and `.users.remove()` calls for queue-state emojis are removed (NOT replaced for non-queue-state reactions like 💭 advisor or 👋 admin — those are out of scope).
    - `grep -c "⏳\|👍\|✅\|❌" src/discord/bridge.ts` returns 0 (or only inside non-queue-state code paths — verify by inspection if any remain).
    - `tsc --noEmit` clean.
    - `npx vitest run` — full suite green; no regression in bridge.ts behavior tests.
    - Local smoke (if dev Discord channel available): trigger a manual A2A turn, observe ⏳ → 👍 → ✅ in the channel. (Optional pre-merge; mandatory post-deploy.)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit && grep -c "transitionQueueState" src/discord/bridge.ts</automated>
  </verify>
  <done>bridge.ts wired through the state machine; full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator screenshot capture for SC-3 (post-deploy)</name>
  <what-built>
    Plan 03 Tasks 1+2 land the state-machine module and bridge.ts wiring. SC-3 is operator-visual ("⏳ → 👍 → ✅, no double-emoji on the operator's Discord channel") and can't be unit-tested against Discord. This checkpoint captures the operator-side verification artifact post-deploy.
  </what-built>
  <how-to-verify>
    AFTER deploy to clawdy (operator-triggered separately, NOT in this plan):
    1. From any agent's Discord channel, trigger a fresh A2A turn (e.g., admin sends `post_to_agent` to projects with any non-trivial content).
    2. Watch the message's reaction icon transition: it MUST start at ⏳, then become 👍 when the recipient's SDK call starts, then become ✅ on delivery (or ❌ if the turn fails terminally).
    3. At no point should two queue-state emojis be visible simultaneously on the same message.
    4. Screenshot the final state (✅ or ❌) and a screenshot of the intermediate 👍 state if catchable.
    5. Attach screenshots to the phase verification artifact (`.planning/phases/119-a2a-delivery-reliability/119-VERIFICATION.md` or similar).
    6. Repeat the test on at least 2 different agent channels to confirm the per-channel mutex isolates channels correctly.
  </how-to-verify>
  <resume-signal>
    Reply with "approved" once both screenshots are attached to the verification artifact, OR describe the observed double-emoji race or stuck state if SC-3 fails.
  </resume-signal>
</task>

</tasks>

<operator_notes>
- **D-07 SEQUENCING — do NOT deploy Plan 03 until Plan 01 has been observably green in production for ≥24h.** Plan 01's `[A2A-01-sentinel] OK` log must have held across at least one full daemon-restart cycle + one operator-visible A2A turn in that 24h window before Plan 03 ships. This is an OPERATOR-enforced gate, not a tooling-enforced gate. The plan can be CODED + COMMITTED immediately; the DEPLOY is gated.
- **No deploy without explicit "deploy" / "ship it".** Per `feedback_ramy_active_no_deploy` and `feedback_no_auto_deploy`. The screenshot task is a POST-deploy verification, not a deploy trigger.
- **D-04 — debounce is 200ms, retry budget is 3.** Resist the urge to "tune" these. The 200ms was chosen against Discord's per-channel reaction rate-limit; the 3-attempt budget keeps the helper bounded so it can't stall the delivery pipeline.
- **Reaction failure must NEVER abort delivery.** The state machine is observability/UX, NOT a precondition for message delivery. If `transitionQueueState` rejects, bridge.ts logs and continues.
- **Terminal sticky check is intentional** — once a message hits DELIVERED or FAILED, further transitions no-op. This prevents the "downgrade" race where a heartbeat sweep tries to reset a message to ⏳ after it's already ✅.
- **Out of scope for this plan:** the 💭 advisor reaction (Phase 117) and 👋 admin handshake reactions — different state, different mutex, different code path. Don't refactor those.
</operator_notes>

<verification>
**Success Criteria from ROADMAP this plan closes:**

| SC | What | How verified |
|----|------|--------------|
| SC-3 | Queue-state icon visibly transitions ⏳ → 👍 → ✅ (or ❌); mutually exclusive, no double-emoji races | Pre-deploy: 8 vitest cases in `queue-state-icon.test.ts` green. Post-deploy (operator-driven): 2× screenshots from different agent channels showing the transition sequence with no double-emoji moment captured. |

**Pre-merge gates:**
- `npx vitest run` — all green.
- `npx tsc --noEmit` — clean.
- `grep -c "transitionQueueState" src/discord/bridge.ts` returns ≥ 4 (four queue-state sites wired).
- New module exists at `src/discord/queue-state-icon.ts`.
- The plan does NOT trigger a deploy — coded + committed only.
</verification>

<success_criteria>
- Plan 03 Tasks 1+2 close the pre-merge half of SC-3 (state machine correctness via vitest).
- Plan 03 Task 3 (checkpoint) closes the operator-visual half of SC-3 — captured post-deploy in a separate operator turn.
- Code committed (orchestrator handles commit). No deploy in this plan; deploy gated on Plan 01's 24h soak per D-07.
</success_criteria>

<output>
After completion, create `.planning/phases/119-a2a-delivery-reliability/119-03-SUMMARY.md` capturing:
- Files modified.
- Vitest case count + pass/fail.
- Confirmation of D-07 sequencing — the deploy gate (Plan 01 green for 24h) is recorded as an operator-pending item.
- Outstanding: post-deploy operator screenshot for SC-3 (Task 3 checkpoint).
</output>
