---
phase: 100-gsd-via-discord-on-admin-clawdy-operator-self-serve-dev-workflow
plan: 07
plan_title: "clawcode.yaml fixture — admin-clawdy agent block with 5 GSD slashCommands + settingSources + gsd.projectDir"
type: execute
wave: 4
depends_on: ["100-01", "100-04"]
status: pending
estimated_hours: 1.0
files_modified:
  - clawcode.yaml
  - src/config/__tests__/clawcode-yaml-phase100.test.ts
autonomous: true
gap_closure: false
requirements: [REQ-100-01, REQ-100-02, REQ-100-04, REQ-100-09]
must_haves:
  truths:
    - "clawcode.yaml gains a NEW agent entry: `- name: admin-clawdy` with a populated block including `workspace`, `model`, `channels`, `settingSources: [project, user]`, `gsd: { projectDir: /opt/clawcode-projects/sandbox }`, AND the 5 slashCommands entries for the GSD workflow (gsd-autonomous, gsd-plan-phase, gsd-execute-phase, gsd-debug, gsd-quick)"
    - "Each slashCommand entry uses the EXACT shape required by Plan 04's dispatcher: name (gsd-* with hyphens — Discord forbids ':' per RESEARCH.md Standard Stack §slashCommandEntrySchema), description (≤100 chars per schema cap), claudeCommand (rewrites to canonical /gsd:* — e.g. '/gsd:plan-phase {phase}'), options (≤1 typed STRING option for plain-text args)"
    - "claudeCommand templates exactly match Plan 04 expectations: gsd-autonomous → '/gsd:autonomous {args}'; gsd-plan-phase → '/gsd:plan-phase {phase}'; gsd-execute-phase → '/gsd:execute-phase {phase}'; gsd-debug → '/gsd:debug {issue}'; gsd-quick → '/gsd:quick {task}'"
    - "All 5 entry names pass slashCommandEntrySchema regex /^[\\w-]+$/, max 32 chars (verified by RESEARCH.md — gsd-execute-phase is the longest at 17 chars)"
    - "Discord 90-cmd-per-guild cap (RESEARCH.md Pitfall 5): the Phase 96 deploy was at ~16/90; Phase 100 adds 5 entries reaching ~21/90 — verified by a test that loads clawcode.yaml + counts admin-clawdy.slashCommands.length === 5 + asserts total slash commands across the fleet stays ≤ 90"
    - "Admin Clawdy is a NEW agent in this dev clawcode.yaml (the production admin-clawdy lives on the clawdy host's clawcode.yaml — also touched by Plan 08 runbook). The dev fixture's admin-clawdy block carries placeholder values (workspace points to /tmp/admin-clawdy or similar dev path; channels is empty array since no Discord guild binding in dev) — purpose is to give vitest fixtures + Plan 04 dispatcher tests a concrete agent block to load against"
    - "settingSources: [project, user] is set ONLY on admin-clawdy. Other agents (test-agent, fin-acquisition, fin-tax, fin-research, fin-playground, finmentum-content-creator, personal, general, projects, research) do NOT receive this field — they continue with the implicit default of [project] applied by the loader resolver (Plan 01)"
    - "Test file src/config/__tests__/clawcode-yaml-phase100.test.ts (NEW) reads the actual on-disk clawcode.yaml + parses via configSchema + asserts: (a) admin-clawdy entry exists; (b) admin-clawdy.settingSources deep-equals [project, user]; (c) admin-clawdy.gsd.projectDir === /opt/clawcode-projects/sandbox; (d) admin-clawdy.slashCommands has exactly 5 entries with the expected names + claudeCommand strings; (e) every other agent (15+) has settingSources undefined OR resolved to [project]; (f) total slashCommand count across fleet ≤ 90"
  artifacts:
    - path: "clawcode.yaml (EDIT — append admin-clawdy agent block)"
      provides: "New agent block under `agents:` (alphabetical-ish position; placement choice — after personal but before fin-* agents OR at the end of the agents list — matches a coherent grouping). Block contents (verbatim YAML structure): name, model, workspace, channels, settingSources, gsd, slashCommands (array of 5), soul (placeholder), identity (placeholder)"
      contains: "name: admin-clawdy|gsd-autonomous|/opt/clawcode-projects/sandbox"
      min_lines: 75
    - path: "src/config/__tests__/clawcode-yaml-phase100.test.ts (NEW)"
      provides: "8+ unit tests reading the actual clawcode.yaml. YML1 admin-clawdy entry exists; YML2 settingSources = [project, user]; YML3 gsd.projectDir set correctly; YML4 5 slashCommands with exact names; YML5 each claudeCommand template matches Plan 04 contract; YML6 other agents have no settingSources field; YML7 fleet-wide slashCommand count ≤ 90 (Pitfall 5 cap); YML8 entry names pass slashCommandEntrySchema regex"
      min_lines: 240
  key_links:
    - from: "clawcode.yaml admin-clawdy block"
      to: "Plan 04 handleGsdLongRunner cmdDef resolution"
      via: "this.resolvedAgents.find(a => a.name === 'admin-clawdy').slashCommands.find(c => c.name === commandName)"
      pattern: "agent.name === \"admin-clawdy\"|admin-clawdy"
    - from: "claudeCommand template '/gsd:plan-phase {phase}' (etc.)"
      to: "Plan 04 formatCommandMessage substitution"
      via: "Plan 04 reads the template + substitutes {phase} → user-supplied STRING option"
      pattern: "/gsd:plan-phase|/gsd:autonomous|/gsd:execute-phase|/gsd:debug|/gsd:quick"
    - from: "settingSources: [project, user]"
      to: "Plan 02 session-adapter (config.settingSources flow into baseOptions)"
      via: "Loader resolver (Plan 01) reads agent.settingSources verbatim → ResolvedAgentConfig.settingSources → AgentSessionConfig.settingSources → SDK baseOptions"
      pattern: "settingSources: \\[project, user\\]|settingSources:.*user"
    - from: "gsd.projectDir: /opt/clawcode-projects/sandbox"
      to: "Plan 06 sandbox path"
      via: "MUST match DEFAULTS.sandboxDir in src/cli/commands/gsd-install.ts"
      pattern: "/opt/clawcode-projects/sandbox"
---

<objective>
Phase 100 GSD-01 + GSD-02 + GSD-04 + GSD-09: add the `admin-clawdy` agent block to the dev `clawcode.yaml` so vitest fixtures (Plan 04 dispatcher tests + Plan 02 session-adapter tests) have a concrete agent to resolve. Block carries `settingSources: [project, user]` (loads ~/.claude/commands/gsd/), `gsd.projectDir: /opt/clawcode-projects/sandbox` (matches Plan 06 install target), and the 5 slashCommands entries for the GSD workflow.

Per CONTEXT.md decision lock-in: Plan 07 ONLY touches the local dev `clawcode.yaml`. The production deployment of admin-clawdy on the clawdy host's `/etc/clawcode/clawcode.yaml` is documented in Plan 08's runbook (operator runs deploy procedure manually). No SSH from this plan.

Output: 1 modified file (clawcode.yaml +75 lines) + 1 new test file (~240 lines). Zero new npm dependencies. Zero behavior change for the other 10+ existing agents.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-gsd-via-discord-on-admin-clawdy-operator-self-serve-dev-workflow/100-CONTEXT.md
@.planning/phases/100-gsd-via-discord-on-admin-clawdy-operator-self-serve-dev-workflow/100-RESEARCH.md
@.planning/phases/100-gsd-via-discord-on-admin-clawdy-operator-self-serve-dev-workflow/100-04-SUMMARY.md
@.planning/phases/100-gsd-via-discord-on-admin-clawdy-operator-self-serve-dev-workflow/100-06-SUMMARY.md
@clawcode.yaml
@src/config/schema.ts
@src/config/__tests__/schema.test.ts

<interfaces>
<!-- Existing agent block patterns in clawcode.yaml -->

From clawcode.yaml:150-200 — test-agent block (REFERENCE shape; admin-clawdy block follows same conventions):
```yaml
  - name: test-agent
    model: sonnet
    skills:
      - subagent-thread
    channels:
      - "1491623782807244880"
    mcpServers:
      - homeassistant
      - openai
      - anthropic
      - brave-search
      - browserless
    soul: |
      # SOUL.md - Who You Are
      ...
    identity: |
      Name: Clawdy
      ...
```

From clawcode.yaml:236-300 — fin-acquisition block (REFERENCE for slashCommands shape):
```yaml
  - name: fin-acquisition
    workspace: /home/jjagpal/.clawcode/agents/finmentum
    # ... other fields ...
    slashCommands:
      - name: clawcode-status
        description: Get current agent status
        claudeCommand: Report your current status
        options: []
```

From RESEARCH.md Code Examples §2 — Phase 100 admin-clawdy block template (verbatim — this is the target structure):
```yaml
  - name: admin-clawdy
    settingSources: [project, user]
    gsd:
      projectDir: /opt/clawcode-projects/sandbox
    slashCommands:
      - name: gsd-autonomous
        description: Run all remaining phases autonomously
        claudeCommand: "/gsd:autonomous {args}"
        options:
          - name: args
            type: 3
            description: "Optional flags (e.g. --from 100)"
            required: false
      - name: gsd-plan-phase
        description: Create phase plan with verification loop
        claudeCommand: "/gsd:plan-phase {phase}"
        options:
          - name: phase
            type: 3
            description: "Phase number + optional flags"
            required: false
      - name: gsd-execute-phase
        description: Execute all plans in a phase
        claudeCommand: "/gsd:execute-phase {phase}"
        options:
          - name: phase
            type: 3
            description: "Phase number + optional flags"
            required: false
      - name: gsd-debug
        description: Systematic debugging with persistent state
        claudeCommand: "/gsd:debug {issue}"
        options:
          - name: issue
            type: 3
            description: "Issue description"
            required: true
      - name: gsd-quick
        description: Quick task with GSD guarantees
        claudeCommand: "/gsd:quick {task}"
        options:
          - name: task
            type: 3
            description: "Task description"
            required: true
```

From src/config/schema.ts:294-329 — slashCommandEntrySchema constraints:
- name: max 32 chars, regex /^[\w-]+$/  ← gsd-execute-phase = 17 chars ✓
- description: max 100 chars
- options: array of slashCommandOptionSchema (default [])

slashCommandOptionSchema (line 294-312):
- name: min 1 char
- type: int 1..11 (3 = STRING)
- description: min 1 char
- required: boolean default false
</interfaces>

<wave_0_validation>
**Wave 0 for Plan 07 is the NEW clawcode-yaml-phase100.test.ts file.** This file reads the actual on-disk clawcode.yaml + parses via configSchema. New file because Plan 07 is a yaml-modification plan and the existing schema/loader tests run against fixture configs, not the on-disk one.
</wave_0_validation>
</context>

<rules>
<rule id="1" name="Discovery">Level 0 — yaml fixture authoring + parse-regression test. The exact YAML structure is dictated by RESEARCH.md Code Examples §2; the plan adds it verbatim.</rule>
<rule id="2" name="Anti-shallow">read_first AND acceptance_criteria. The 5 claudeCommand templates are CONCRETE STRINGS — verified by grep against the post-edit yaml.</rule>
<rule id="3" name="Lock-in-only-admin">Per CONTEXT.md: settingSources [project, user] is ONLY on admin-clawdy. Test YML6 + YML7 pin this — other agents don't get this field.</rule>
<rule id="4" name="Path-consistency">gsd.projectDir MUST exactly match Plan 06's DEFAULTS.sandboxDir. Plan 07 reads `/opt/clawcode-projects/sandbox` (no trailing slash, absolute).</rule>
<rule id="5" name="Discord-cap">RESEARCH.md Pitfall 5: 90-cmd-per-guild. Plan 07 adds 5 entries; total post-Plan-07 should be well under 90. Test YML7 verifies.</rule>
<rule id="6" name="No-clawdy-deploy">Plan 07 does NOT modify the production /etc/clawcode/clawcode.yaml on clawdy. Plan 08's runbook handles operator deployment to clawdy.</rule>
</rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD RED — write 8 yaml-fixture parse tests targeting admin-clawdy block (failing)</name>
  <files>src/config/__tests__/clawcode-yaml-phase100.test.ts</files>
  <read_first>
    - /home/jjagpal/.openclaw/workspace-coding/clawcode.yaml (full file — current agent layout)
    - /home/jjagpal/.openclaw/workspace-coding/src/config/schema.ts (lines 294-330 — slashCommandEntrySchema + slashCommandOptionSchema)
    - /home/jjagpal/.openclaw/workspace-coding/src/config/__tests__/schema.test.ts (existing patterns — particularly the parse-regression style)
    - /home/jjagpal/.openclaw/workspace-coding/.planning/phases/100-gsd-via-discord-on-admin-clawdy-operator-self-serve-dev-workflow/100-RESEARCH.md (Code Examples §2)
  </read_first>
  <behavior>
    Create a NEW test file: src/config/__tests__/clawcode-yaml-phase100.test.ts. Top of file: imports for readFileSync, YAML.parse, configSchema. Inside describe block: 8 tests (YML1..YML8 from must_haves) all reading the actual on-disk clawcode.yaml.

    All 8 tests RED initially because admin-clawdy block doesn't exist in clawcode.yaml yet.
  </behavior>
  <action>
1. Create file `src/config/__tests__/clawcode-yaml-phase100.test.ts`:

   ```typescript
   import { describe, it, expect } from "vitest";
   import { readFileSync } from "node:fs";
   import { join } from "node:path";
   import { parse as yamlParse } from "yaml";
   import { configSchema, slashCommandEntrySchema } from "../schema.js";

   describe("Phase 100 — clawcode.yaml admin-clawdy fixture", () => {
     const yamlPath = join(process.cwd(), "clawcode.yaml");
     const raw = readFileSync(yamlPath, "utf-8");
     const config = configSchema.parse(yamlParse(raw));
     const adminClawdy = config.agents.find((a) => a.name === "admin-clawdy");

     it("YML1 — admin-clawdy entry exists in clawcode.yaml", () => {
       expect(adminClawdy).toBeDefined();
     });

     it("YML2 — admin-clawdy.settingSources deep-equals [project, user]", () => {
       expect(adminClawdy?.settingSources).toEqual(["project", "user"]);
     });

     it("YML3 — admin-clawdy.gsd.projectDir === /opt/clawcode-projects/sandbox", () => {
       expect(adminClawdy?.gsd?.projectDir).toBe("/opt/clawcode-projects/sandbox");
     });

     it("YML4 — admin-clawdy.slashCommands has exactly 5 entries with expected names", () => {
       const names = adminClawdy?.slashCommands.map((c) => c.name) ?? [];
       expect(names).toContain("gsd-autonomous");
       expect(names).toContain("gsd-plan-phase");
       expect(names).toContain("gsd-execute-phase");
       expect(names).toContain("gsd-debug");
       expect(names).toContain("gsd-quick");
       expect(adminClawdy?.slashCommands).toHaveLength(5);
     });

     it("YML5 — claudeCommand templates match Plan 04 contract", () => {
       const byName = (n: string) => adminClawdy?.slashCommands.find((c) => c.name === n);
       expect(byName("gsd-autonomous")?.claudeCommand).toBe("/gsd:autonomous {args}");
       expect(byName("gsd-plan-phase")?.claudeCommand).toBe("/gsd:plan-phase {phase}");
       expect(byName("gsd-execute-phase")?.claudeCommand).toBe("/gsd:execute-phase {phase}");
       expect(byName("gsd-debug")?.claudeCommand).toBe("/gsd:debug {issue}");
       expect(byName("gsd-quick")?.claudeCommand).toBe("/gsd:quick {task}");
     });

     it("YML6 — only admin-clawdy carries settingSources; other agents stay implicit-default", () => {
       const others = config.agents.filter((a) => a.name !== "admin-clawdy");
       for (const a of others) {
         // Either undefined OR the agent explicitly set ['project'] — but per
         // CONTEXT.md lock-in, none of the dev fleet sets it explicitly today.
         expect(a.settingSources === undefined || (Array.isArray(a.settingSources) && a.settingSources.every((s) => s === "project"))).toBe(true);
       }
     });

     it("YML7 — fleet-wide slashCommand count stays under 90 (Discord cap)", () => {
       const total = config.agents.reduce((acc, a) => acc + a.slashCommands.length, 0);
       expect(total).toBeLessThanOrEqual(90);
     });

     it("YML8 — every admin-clawdy slash entry passes slashCommandEntrySchema regex", () => {
       for (const entry of adminClawdy?.slashCommands ?? []) {
         expect(() => slashCommandEntrySchema.parse(entry)).not.toThrow();
         expect(entry.name).toMatch(/^[\w-]+$/);
         expect(entry.name.length).toBeLessThanOrEqual(32);
       }
     });
   });
   ```

2. Run `npx vitest run src/config/__tests__/clawcode-yaml-phase100.test.ts`. All 8 tests should FAIL (admin-clawdy doesn't exist in clawcode.yaml — adminClawdy is undefined; config.agents.find returns undefined).
3. Commit: `test(100-07): RED — clawcode.yaml admin-clawdy parse-regression tests (8 tests, all failing)`. Body: "Targets admin-clawdy block to be added in Task 2."

DO NOT modify clawcode.yaml in this task.
  </action>
  <verify>
    <automated>npx vitest run src/config/__tests__/clawcode-yaml-phase100.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - test -f /home/jjagpal/.openclaw/workspace-coding/src/config/__tests__/clawcode-yaml-phase100.test.ts → exits 0
    - grep -cE "(YML1|YML2|YML3|YML4|YML5|YML6|YML7|YML8)" /home/jjagpal/.openclaw/workspace-coding/src/config/__tests__/clawcode-yaml-phase100.test.ts → at least 8 occurrences
    - npx vitest run src/config/__tests__/clawcode-yaml-phase100.test.ts → reports >= 6 FAIL (RED) before Task 2 lands
    - git log -1 --pretty=%s → matches `^test\(100-07\): RED`
  </acceptance_criteria>
  <done>
    Failing yaml-fixture test scaffold exists; Task 2 has a Nyquist gate.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — append admin-clawdy block to clawcode.yaml with full GSD configuration</name>
  <files>clawcode.yaml</files>
  <read_first>
    - /home/jjagpal/.openclaw/workspace-coding/clawcode.yaml (full file — confirm where to insert; expected last existing agent is 'research' at line ~325)
    - /home/jjagpal/.openclaw/workspace-coding/.planning/phases/100-gsd-via-discord-on-admin-clawdy-operator-self-serve-dev-workflow/100-RESEARCH.md (Code Examples §2 — verbatim block)
    - /home/jjagpal/.openclaw/workspace-coding/.planning/phases/100-gsd-via-discord-on-admin-clawdy-operator-self-serve-dev-workflow/100-04-SUMMARY.md (Plan 04 hand-off — confirms exact claudeCommand templates expected by dispatcher)
  </read_first>
  <behavior>
    Append the admin-clawdy block to clawcode.yaml (recommended: at the END of the agents list, after 'research'). After this task, all 8 Task 1 tests pass GREEN.
  </behavior>
  <action>
1. Open clawcode.yaml. Locate the last agent block in the `agents:` list (likely 'research' around line 325-340).
2. Append AFTER the last existing agent:

   ```yaml
     # Phase 100 — admin-clawdy: GSD-via-Discord operator self-serve agent.
     # The production admin-clawdy lives on the clawdy host's /etc/clawcode/
     # clawcode.yaml; this entry is the dev fixture that mirrors its production
     # config so vitest can resolve it (Plan 100-04 dispatcher tests + Plan 100-07
     # parse tests). settingSources: [project, user] loads ~/.claude/commands/gsd/
     # (symlinked by `clawcode gsd install` per Plan 100-06). gsd.projectDir
     # points at the sandbox project the install helper bootstraps.
     - name: admin-clawdy
       model: sonnet
       workspace: /tmp/admin-clawdy
       channels: []
       settingSources: [project, user]
       gsd:
         projectDir: /opt/clawcode-projects/sandbox
       slashCommands:
         - name: gsd-autonomous
           description: Run all remaining phases autonomously
           claudeCommand: "/gsd:autonomous {args}"
           options:
             - name: args
               type: 3
               description: "Optional flags (e.g. --from 100)"
               required: false
         - name: gsd-plan-phase
           description: Create phase plan with verification loop
           claudeCommand: "/gsd:plan-phase {phase}"
           options:
             - name: phase
               type: 3
               description: "Phase number + optional flags"
               required: false
         - name: gsd-execute-phase
           description: Execute all plans in a phase
           claudeCommand: "/gsd:execute-phase {phase}"
           options:
             - name: phase
               type: 3
               description: "Phase number + optional flags"
               required: false
         - name: gsd-debug
           description: Systematic debugging with persistent state
           claudeCommand: "/gsd:debug {issue}"
           options:
             - name: issue
               type: 3
               description: "Issue description"
               required: true
         - name: gsd-quick
           description: Quick task with GSD guarantees
           claudeCommand: "/gsd:quick {task}"
           options:
             - name: task
               type: 3
               description: "Task description"
               required: true
       soul: |
         # Admin Clawdy — GSD operator surface

         You drive ClawCode's own GSD workflow from Discord. Operators type /gsd-* in
         #admin-clawdy; long-runners (autonomous, plan-phase, execute-phase) auto-spawn
         a subagent thread (Plan 100-04) and you summarize completion in main channel
         (Plan 100-05 relay extension).

         Your project root is /opt/clawcode-projects/sandbox/. .planning/ work happens
         there; commits are local-only (no git push, no gh CLI per Phase 100 lock-in).

         Stay terse. Quote Phase 100 plans verbatim when uncertain.
       identity: |
         Name: Admin Clawdy
         Role: GSD operator surface for ClawCode itself
         Model: sonnet
         Vibe: Competent, dry, references Phase 100 plans by ID when useful.
   ```

3. Run `npx vitest run src/config/__tests__/clawcode-yaml-phase100.test.ts`. All 8 YML tests should now pass GREEN.
4. Run the broader test suite: `npx vitest run src/config/`. Confirm zero regression (the 15+ other agents still parse correctly).
5. Run `npx tsc --noEmit` — confirm zero new type errors.
6. Optionally validate via gsd-tools: `node $HOME/.claude/get-shit-done/bin/gsd-tools.cjs frontmatter validate clawcode.yaml --schema plan` is N/A (yaml not a plan); skip this step but confirm yaml syntactic validity via `npx tsx -e "import {parse} from 'yaml'; import {readFileSync} from 'fs'; parse(readFileSync('clawcode.yaml', 'utf-8'))"`.
7. Commit: `feat(100-07): add admin-clawdy agent block to dev clawcode.yaml (settingSources + gsd + 5 slashCommands)`. Body: "Plan 04 dispatcher tests now resolve a concrete admin-clawdy ResolvedAgentConfig. Production deploy to /etc/clawcode/clawcode.yaml on clawdy is operator-driven per Plan 08 runbook."

DO NOT touch /etc/clawcode/clawcode.yaml on the clawdy host. DO NOT modify any other agent's block. DO NOT add settingSources or gsd to any agent except admin-clawdy.
  </action>
  <verify>
    <automated>npx vitest run src/config/__tests__/clawcode-yaml-phase100.test.ts src/config/__tests__/loader.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -E "^  - name: admin-clawdy" /home/jjagpal/.openclaw/workspace-coding/clawcode.yaml → exactly 1 match
    - grep -E "settingSources: \[project, user\]" /home/jjagpal/.openclaw/workspace-coding/clawcode.yaml → exactly 1 match
    - grep -E "/opt/clawcode-projects/sandbox" /home/jjagpal/.openclaw/workspace-coding/clawcode.yaml → exactly 1 match
    - grep -cE "name: gsd-(autonomous|plan-phase|execute-phase|debug|quick)" /home/jjagpal/.openclaw/workspace-coding/clawcode.yaml → exactly 5 matches
    - grep -cE "claudeCommand: \"/gsd:(autonomous|plan-phase|execute-phase|debug|quick)" /home/jjagpal/.openclaw/workspace-coding/clawcode.yaml → exactly 5 matches
    - npx vitest run src/config/__tests__/clawcode-yaml-phase100.test.ts → 0 failures, 8 tests passing
    - npx vitest run src/config/__tests__/loader.test.ts → 0 failures (no regression)
    - npx tsc --noEmit → exits 0
    - git log -1 --pretty=%s → matches `^feat\(100-07\):`
  </acceptance_criteria>
  <done>
    admin-clawdy is a parseable agent block in dev clawcode.yaml; Plan 04's dispatcher tests can construct a real ResolvedAgentConfig for it; Plan 06's gsd.projectDir matches DEFAULTS.sandboxDir; Plan 08's runbook can document the production deploy procedure for the same block on the clawdy host.
  </done>
</task>

</tasks>

<verification>
- 8 new Phase 100 yaml-fixture tests GREEN
- All existing config-tier tests pass — no regressions to other agents
- `npx tsc --noEmit` exits 0
- Exactly 5 GSD slashCommand entries on admin-clawdy, with the EXACT claudeCommand templates Plan 04 expects
- settingSources [project, user] is only on admin-clawdy (verified by YML6)
- Fleet slashCommand count ≤ 90 (verified by YML7)
- gsd.projectDir matches Plan 06's DEFAULTS.sandboxDir verbatim
</verification>

<success_criteria>
- admin-clawdy entry exists in clawcode.yaml with settingSources, gsd.projectDir, and 5 GSD slashCommand entries
- Each slashCommand passes slashCommandEntrySchema validation
- Other agents are unchanged (no accidental cascade)
- gsd.projectDir + Plan 06's DEFAULTS.sandboxDir are byte-identical
- Plan 04's dispatcher tests can resolve admin-clawdy's slashCommands
</success_criteria>

<output>
After completion, create `.planning/phases/100-gsd-via-discord-on-admin-clawdy-operator-self-serve-dev-workflow/100-07-SUMMARY.md` documenting:
- The exact line-range in clawcode.yaml where admin-clawdy block lives
- Cross-reference verification: gsd.projectDir matches Plan 06 DEFAULTS.sandboxDir verbatim (`/opt/clawcode-projects/sandbox`)
- Test count (+8)
- Hand-off to Plan 08 runbook: the runbook's "Step: edit /etc/clawcode/clawcode.yaml on clawdy" section MUST replicate this admin-clawdy block verbatim (channels: [...] populated with the actual #admin-clawdy Discord channel ID — placeholder in dev, real ID in prod)
</output>
</content>
</invoke>