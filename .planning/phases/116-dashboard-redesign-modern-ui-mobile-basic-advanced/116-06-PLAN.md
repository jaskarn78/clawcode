---
phase: 116
plan: 06
title: Tier 3 polish + operator-driven cutover gate — F18-F25 + telemetry + redirect flag
wave: 4
depends_on: ["116-01", "116-02", "116-03", "116-04", "116-05"]
files_modified:
  - src/dashboard/server.ts                                    # cutover redirect logic + telemetry
  - src/config/schema.ts                                       # defaults.dashboardCutoverRedirect flag
  - src/dashboard/client/src/components/ActivityHeatmap.tsx    # F18
  - src/dashboard/client/src/components/NotificationFeed.tsx   # F20
  - src/dashboard/client/src/components/ThemeToggle.tsx        # F21
  - src/dashboard/client/src/components/FleetHeatmap.tsx       # F22
  - src/dashboard/client/src/components/AuditLogViewer.tsx     # F23
  - src/dashboard/client/src/routes/graph.tsx                  # F24 re-skin
  # F19 swim-lane: DEFERRED OUT OF PHASE 116 (operator decision 2026-05-11) — see 116-DEFERRED.md
  # F25 already absorbed into F28 in 116-03
autonomous: false  # cutover gate is operator-driven decision
estimated_hours: 6
type: execute
must_haves:
  - F18-F23 polish features render without errors
  - F24 /dashboard/v2/graph route works (knowledge graph visualization re-skinned)
  - Cutover instrumentation: dashboard_v2_page_view + dashboard_v2_error events emit to existing trace store
  - defaults.dashboardCutoverRedirect flag added to schema; when true, `/` 301 redirects to `/dashboard/v2/`
  - Flag default value FALSE — operator flips manually
---

# Plan 116-06: Tier 3 polish + cutover gate

## Features

- **F18** 30-day activity heatmap (per agent) — calendar grid GitHub-style; turn count per day
- ~~**F19** Swim-lane timeline (fleet concurrent activity)~~ — **DEFERRED OUT OF PHASE 116** (operator decision 2026-05-11). Rationale: value (★★★) duplicates F12 trace waterfall which is already in 116-04; cost (high, 6-8h canvas-rendered). Captured in `116-DEFERRED.md` as a backlog candidate for a future phase if operator usage shows demand.
- **F20** Notification feed — header bell + badge-counted slide-over panel; SLO breach, budget exceeded, MCP degradation, dream priority trigger, IPC failure
- **F21** Theme toggle — light/dark/system; shadcn CSS variables; persist to localStorage
- **F22** Fleet activity heatmap — fleet-aggregated; informs operator when to schedule deploys
- **F23** Operator action audit log — append-only log of every dashboard action (restart, config change, veto, migration kick); compliance-relevant
- **F24** `/graph` re-skin — port existing `src/dashboard/static/graph.html` (679 lines D3.js) to React component at `/dashboard/v2/graph`
- **F25** `/tasks` re-skin — already absorbed into F28 Kanban in 116-03; this plan confirms cutover

## Cutover gate (operator-driven)

Per locked operator decision (2026-05-11 plan-mode review): NO calendar gate. Both `/` and `/dashboard/v2` coexist indefinitely. Operator flips `defaults.dashboardCutoverRedirect: false → true` manually when ready. Old `src/dashboard/static/*` files removed in a follow-up commit only after explicit green-light.

## Tasks

<task id="116-06-T01">
  <objective>F18 + F22 activity heatmaps.</objective>
  <action>
    1. `ActivityHeatmap.tsx` — Recharts or custom SVG calendar grid (30 days × 7 days-of-week)
    2. Color intensity = turn count for that day; tooltip shows raw count
    3. F18 = per-agent in F11 drawer; F22 = fleet aggregate on `/dashboard/v2/fleet`
    4. Source: `traces.db` `GROUP BY date(started_at), agent_name`
  </action>
</task>

<task id="116-06-T02">
  <objective>F20 notification feed.</objective>
  <action>
    1. `NotificationFeed.tsx` — bell icon in header with badge count
    2. Click → shadcn `<Sheet>` slide-over from right with notification list
    3. Notification types: SLO breach, budget exceeded (>90% daily), MCP degradation, dream priority trigger, IPC delivery failure
    4. Source: derives from existing SSE events + `/api/fleet-stats`; client-side aggregation
    5. Notifications persist 24h, then auto-dismiss
  </action>
</task>

<task id="116-06-T03">
  <objective>F21 theme toggle.</objective>
  <action>
    1. `ThemeToggle.tsx` — toggle dropdown: System / Light / Dark
    2. Adds/removes `.dark` class on `<html>` per shadcn convention
    3. Persists to localStorage; respects `prefers-color-scheme` for System
  </action>
</task>

<task id="116-06-T04">
  <objective>F23 audit log viewer.</objective>
  <action>
    1. Backend: log every PUT/POST operator action to `audit-trail.ts` (Phase 92 infrastructure if reusable; otherwise new append-only JSONL)
    2. `AuditLogViewer.tsx` — table view at `/dashboard/v2/audit`
    3. Columns: timestamp, operator (single user = jjagpal — placeholder), action, target_agent, before/after diff (for config changes)
    4. Filter by date range, action type, agent
  </action>
</task>

<task id="116-06-T05">
  <objective>F24 /graph re-skin.</objective>
  <action>
    1. Port `src/dashboard/static/graph.html` (679 lines D3.js) to React component at `src/dashboard/client/src/routes/graph.tsx`
    2. Apply new design tokens (colors, fonts) without changing graph layout algorithm
    3. Mount at `/dashboard/v2/graph` route
    4. Old `/graph` route stays live during soak (no edits to `src/dashboard/static/graph.html`)
  </action>
</task>

<task id="116-06-T06" deferred="true">
  <objective>F19 swim-lane timeline — DEFERRED OUT OF PHASE 116.</objective>
  <action>
    Operator decision 2026-05-11: F19 deferred from Phase 116. Reasoning: value-cost trade-off unfavorable when F12 trace waterfall (shipped in 116-04) already provides per-turn timing visibility. Multi-agent concurrent-activity correlation is interesting but operator-usage demand unclear; defer until F12 reveals an actual gap.

    NO IMPLEMENTATION IN THIS PLAN. Captured in `116-DEFERRED.md` for backlog promotion if/when demand surfaces.
  </action>
</task>

<task id="116-06-T07">
  <objective>Cutover instrumentation: telemetry events.</objective>
  <action>
    1. Add `dashboard_v2_page_view` event emission on every SPA page load (lightweight POST to a new `/api/dashboard-telemetry` endpoint or log via existing audit-trail)
    2. Add `dashboard_v2_error` event emission on React error boundary catches
    3. Surface counts in a small badge on the dashboard itself: "v2 page views: N · errors: M (24h)"
  </action>
</task>

<task id="116-06-T08">
  <objective>Cutover gate: feature-flag-driven redirect.</objective>
  <action>
    1. Add to `src/config/schema.ts` defaults: `dashboardCutoverRedirect: boolean` (default `false`)
    2. In `server.ts`, when handling `pathname === "/"`:
       - If `config.defaults.dashboardCutoverRedirect === true`: respond `301 Location: /dashboard/v2/`
       - Else: serve old `index.html` (current behavior unchanged)
    3. Add CLI hint: `clawcode config get defaults.dashboardCutoverRedirect`
    4. Document in `116-VERIFICATION.md`: operator runs `clawcode config set defaults.dashboardCutoverRedirect true` when ready
    5. NO automatic flip — pure manual operator decision
  </action>
  <verify>
    Default state: `/` serves old; `/dashboard/v2/` serves new. After `clawcode config set defaults.dashboardCutoverRedirect true` + daemon hot-reload: `curl -I /` returns 301 with Location: /dashboard/v2/
  </verify>
</task>

## Decommission follow-up (NOT in this plan)

After operator flips the cutover flag AND observes for some period (operator-driven, no fixed window), a separate follow-up commit removes:

- `src/dashboard/static/index.html`
- `src/dashboard/static/app.js`
- `src/dashboard/static/styles.css`
- The `/` static-asset route branch in `server.ts`
- The `dashboardCutoverRedirect` schema field (becomes the default behavior)

This is intentionally a separate commit so the rollback story stays clean during the soak.

## Done

All Tier 3 polish features ship. Telemetry instrumentation in place. Cutover flag added to schema with default `false`. Phase 116 complete pending operator-driven cutover decision.

## Estimated total: 4-7h (F19 deferred — saves 2-3h vs original 6-10h estimate)
