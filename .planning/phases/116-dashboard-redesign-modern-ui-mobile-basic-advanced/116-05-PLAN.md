---
phase: 116
plan: 05
title: Fleet-scale + cost — F16 comparison table, F17 cost dashboard
wave: 3
depends_on: ["116-04"]
files_modified:
  - src/dashboard/server.ts                                       # /api/costs/daily extension
  - src/dashboard/client/src/components/FleetComparisonTable.tsx  # F16
  - src/dashboard/client/src/components/CostDashboard.tsx         # F17
autonomous: true
estimated_hours: 5
type: execute
must_haves:
  - F16 fleet table sortable across all columns; CSV export works; filters by status/model/SLO breach
  - F17 cost dashboard shows today/7d/30d totals + per-agent + per-model + trend + anomaly alert (>2× 30d avg)
---

# Plan 116-05: Fleet-scale + cost

## Features

- **F16** Fleet comparison table — fleet-level view (sidebar nav): one row per agent; sortable columns: avg p95 first_token, end_to_end p95, tool_cache_hit_rate, tier1_budget_pct, dream-pass count (7d), IPC delivery success rate, daily cost, MCP error count; filter by status/model/SLO breach; export CSV
- **F17** Cost dashboard — today/7d/30d total token + image spend; per-agent bar chart sorted by spend; per-model split (opus/sonnet/haiku); trend chart with linear projection; budget gauges (daily/weekly/monthly); anomaly alert (spend >2× 30d average)

## Tasks

<task id="116-05-T01">
  <objective>F16 fleet comparison table.</objective>
  <action>
    1. `FleetComparisonTable.tsx` — full-page route `/dashboard/v2/fleet`
    2. shadcn `<Table>` with sortable columns (click header to sort)
    3. Data sources: aggregate over all `useAgents()` + `useAgentCache(name)` per agent
    4. Columns: agent name, status, model, first_token p50 (SLO-colored), end_to_end p95, tool_cache_hit_rate, tier1_budget_pct, dream count 7d, IPC delivery success rate, daily cost, MCP error count
    5. Filter row above table: status (running/stopped/error), model (opus/sonnet/haiku), SLO breach (any/none)
    6. CSV export button → client-side CSV serialization, browser download
  </action>
  <verify>Table renders all agents; sorting flips order; filter to "model:opus" shows only opus agents; CSV download contains all visible rows</verify>
</task>

<task id="116-05-T02">
  <objective>F17 cost dashboard.</objective>
  <action>
    1. Backend: extend existing `/api/costs` (Phase 1.5 cost tracking) — add `/api/costs/daily?agent=&days=30` for granular trends
    2. Frontend: `CostDashboard.tsx` — full-page route `/dashboard/v2/costs`
    3. Top row: today / 7d / 30d total spend cards
    4. Recharts stacked area chart for trend (one stack per agent or per model — toggle)
    5. Per-model split donut (opus/sonnet/haiku spend)
    6. Budget gauges: daily/weekly/monthly limit vs actuals (read `EscalationBudget` infrastructure)
    7. Anomaly alert banner: if today's spend > 2× 30d average, show warning
    8. Linear projection: extrapolate current 30d trend → projected month-end
  </action>
  <verify>Cards show accurate totals; trend chart renders 30d data; anomaly alert fires when forced (mock 2× spend)</verify>
</task>

## Done

Fleet table sortable + filterable + exportable. Cost dashboard shows totals + per-agent + per-model + trend + budget gauges + anomaly alert.

## Estimated total: 4-6h
