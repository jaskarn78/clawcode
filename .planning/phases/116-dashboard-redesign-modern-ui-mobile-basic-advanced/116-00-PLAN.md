---
phase: 116
plan: 00
title: SPA scaffolding + Phase 115-08 producer regression fix + F02 per-model SLO backend
wave: 0
depends_on: []
files_modified:
  - package.json
  - src/dashboard/server.ts
  - src/performance/slos.ts
  - src/config/schema.ts
  - src/dashboard/client/  # NEW directory tree
  - .gitignore
autonomous: false  # operator confirms F02 Opus threshold value before commit
requirements: []
estimated_hours: 10
type: execute
must_haves:
  - dist/cli/index.js contains "function iterateWithTracing" (Finding B fixed)
  - dist/cli/index.js contains ≥4 occurrences of producer call sites (.addToolExecutionMs?.(, .addToolRoundtripMs?.(, .recordParallelToolCallCount?.()
  - npm run build produces both dist/cli/index.js AND dist/dashboard/spa/index.html
  - /dashboard/v2/ returns Vite-built React shell rendering live agent count from SSE
  - / returns old dashboard byte-identical to pre-116
  - /api/agents/:name/cache includes resolved per-model SLO threshold with source tag
---

# Plan 116-00: SPA scaffolding + Finding B fix + F02 backend

## Goal

Land a buildable, deployable empty SPA at `/dashboard/v2` connected to live SSE, rendering one Tier 1 placeholder. Fix the Phase 115-08 producer regression (Finding B from quick task 260511-mfn) so the new `tool_execution_ms` / `tool_roundtrip_ms` / `parallel_tool_call_count` columns populate in production. Ship the F02 per-model SLO backend (folds Phase 999.38). After this plan, every subsequent plan only adds React files; no infrastructure changes.

## Sub-scopes covered

- Folds Phase 999.38 — per-model SLO recalibration backend
- Fixes Phase 115-08 producer regression (verify in bundle after esbuild cache wipe)
- Establishes SPA build pipeline + serve route + design tokens

## Tasks

<task id="116-00-T01">
  <objective>Repro Phase 115-08 producer regression locally, fix via esbuild cache wipe + rebuild, verify producer call sites land in bundle.</objective>
  <read_first>
    - .planning/quick/260511-mfn-close-out-phase-999-7-item-2-run-tool-la/260511-mfn-AUDIT-FINDINGS.md (Finding B section)
    - src/manager/session-adapter.ts:1336 (confirm `async function iterateWithTracing` present in source)
    - tsup.config.ts (note `clean: true` only wipes dist/, not node_modules/.cache)
  </read_first>
  <action>
    1. Verify current state: `grep -c "function iterateWithTracing\|async function iterateWithTracing" dist/cli/index.js` should return 0
    2. Wipe caches: `rm -rf dist node_modules/.cache 2>/dev/null`
    3. Rebuild: `npm run build`
    4. Verify post-rebuild:
       - `grep -c "function iterateWithTracing\|async function iterateWithTracing" dist/cli/index.js` ≥ 1
       - `grep -cE "\.addToolExecutionMs\?\.\(|\.addToolRoundtripMs\?\.\(|\.recordParallelToolCallCount\?\.\(" dist/cli/index.js` ≥ 4
       - `grep -c "iterateUntilResult" dist/cli/index.js` should be lower than before (or 0 — depends on whether old name persists in comments)
    5. If still broken: deeper investigation needed — tsup tree-shaking bug. Fall back path: F07 ships against `trace_spans` (per-tool table, works today via `/api/agents/:name/tools`). Document the deeper bug as a follow-up task in DEFERRED.md.
  </action>
  <verify>Bundle now contains the producer wiring. Local dev daemon (`npx tsx src/cli/index.ts start-all --foreground`) shows non-NULL `tool_execution_ms` / `tool_roundtrip_ms` / `parallel_tool_call_count` in `traces.db` after one agent turn with tools.</verify>
  <done>Cache fix verified; STATE.md "Open Bugs (post-115-08 deploy)" Finding B can be marked resolved (or marked as needing deeper investigation if cache wipe didn't recover).</done>
</task>

<task id="116-00-T02">
  <objective>F02 backend — extend `slos.ts` with per-model thresholds; add optional per-agent override schema; expose resolved SLO in `/api/agents/:name/cache` response.</objective>
  <read_first>
    - src/performance/slos.ts (current `SloThreshold` shape)
    - src/config/schema.ts (current agent schema; where to add `perf.slos` override)
    - src/dashboard/server.ts:212-238 (current `/api/agents/:name/cache` handler)
    - .planning/phases/115-memory-context-prompt-cache-redesign/perf-comparisons/*.md (Phase 115 wave-4 perf report for Opus baseline derivation)
  </read_first>
  <action>
    1. Extend `SloThreshold` type to record per-model defaults: `{ model: 'opus' | 'sonnet' | 'haiku' | 'default'; first_token_p50_ms: number; end_to_end_p95_ms: number; tool_call_p95_ms: number; }`
    2. Derive Opus first_token_p50 threshold from Phase 115 fleet aggregate data (query against `traces` table, last 30 days, GROUP BY model). Quote the resulting value before commit (target: 4,000-5,000ms based on observed Opus latency).
    3. Add optional `agents[*].perf.slos` field to `clawcode.yaml` schema with the same shape (per-agent override wins over per-model default)
    4. Add resolveSloFor(agent: ResolvedAgentConfig): { first_token_p50_ms, source: 'agent-override' | 'model-default' } helper
    5. Wire into `/api/agents/:name/cache` response: extend the existing CacheTelemetryReport with `slos: { first_token_p50_ms: number, source: string }`
    6. Add unit tests for `resolveSloFor` covering 4 cases: agent-override present, model = opus default, model = sonnet default, model = haiku default
  </action>
  <verify>
    `curl /api/agents/fin-acquisition/cache | jq .slos` returns `{ first_token_p50_ms: <opus-derived>, source: "model-default" }` when fin-acq has no override; returns `{ ..., source: "agent-override" }` when override is set.
  </verify>
  <done>F02 backend is wired. Plan 116-01's F03 agent tile grid can read the per-model SLO and color-code accordingly. Phase 999.38 closes-eligible (visual surface ships in 116-01/02).</done>
</task>

<task id="116-00-T03">
  <objective>Bootstrap Vite + React 19 SPA under `src/dashboard/client/` with locked tech stack.</objective>
  <read_first>
    - 116-CONTEXT.md (tech stack lock section)
    - package.json (current root deps, scripts)
    - tsup.config.ts (note SPA built by Vite NOT tsup)
  </read_first>
  <action>
    1. `cd src/dashboard && npm create vite@latest client -- --template react-ts` (interactive — accept defaults)
    2. Wire Vite output to repo dist: edit `src/dashboard/client/vite.config.ts`:
       - `build.outDir: '../../../dist/dashboard/spa'`
       - `build.emptyOutDir: true`
       - `base: '/dashboard/v2/'`
    3. Install client devDeps at root (single `node_modules`):
       ```
       npm install --save-dev \
         tailwindcss@3.4 postcss autoprefixer \
         @tanstack/react-query@5 recharts@3 lucide-react cmdk \
         class-variance-authority clsx tailwind-merge \
         @radix-ui/react-dialog @radix-ui/react-popover @radix-ui/react-progress \
         @radix-ui/react-separator @radix-ui/react-slot @radix-ui/react-tooltip \
         @testing-library/react @testing-library/jest-dom
       ```
    4. Update `.gitignore` if Vite creates sub-`node_modules` patterns
    5. Smoke test: `cd src/dashboard/client && npx vite build` produces `dist/dashboard/spa/index.html` + assets
  </action>
  <verify>`ls dist/dashboard/spa/` shows `index.html` and `assets/index-*.js` and `assets/index-*.css` after `npm run build:spa`.</verify>
  <done>SPA scaffold present; daemon-side runtime deps unchanged (all client deps in devDependencies).</done>
</task>

<task id="116-00-T04">
  <objective>Integrate SPA build into root `npm run build` pipeline.</objective>
  <read_first>
    - package.json (current `"build"` script: `tsup && npm run copy-assets`)
  </read_first>
  <action>
    1. Edit `package.json`:
       - `"build": "tsup && npm run copy-assets && npm run build:spa"`
       - Add `"build:spa": "cd src/dashboard/client && vite build"`
       - Add `"dev:spa": "cd src/dashboard/client && vite dev"` (developer convenience)
    2. Run `npm run build` from repo root
  </action>
  <verify>Both `dist/cli/index.js` AND `dist/dashboard/spa/index.html` exist after one `npm run build` invocation.</verify>
  <done>Single build command handles both daemon bundle and SPA bundle.</done>
</task>

<task id="116-00-T05">
  <objective>Add new static-asset route in `server.ts` serving `/dashboard/v2` from `dist/dashboard/spa/` without touching the existing `/` route.</objective>
  <read_first>
    - src/dashboard/server.ts:17 (STATIC_DIR resolution pattern)
    - src/dashboard/server.ts:130-163 (existing static route handlers)
  </read_first>
  <action>
    1. Add `STATIC_SPA_DIR = join(import.meta.dirname, "..", "dashboard", "spa")` constant near line 17
    2. Add binary-safe `serveSpaAsset(res, filename, mimeType)` helper (reads with `readFileSync` as Buffer, NOT utf-8 string — needed for `.woff2` fonts)
    3. Extend `MIME_TYPES` map (or wherever MIME inference happens) with: `.woff`, `.woff2`, `.svg`, `.png`, `.json`, `.map`
    4. Add new route branches BEFORE the `/api/*` and existing `/` branches:
       - `pathname === "/dashboard/v2" || pathname === "/dashboard/v2/"` → `serveSpaAsset(res, "index.html", "text/html; charset=utf-8")`
       - `pathname.startsWith("/dashboard/v2/assets/")` → strip prefix, look up in `STATIC_SPA_DIR/assets/`, infer MIME from extension
       - `pathname.startsWith("/dashboard/v2/")` (catch-all for SPA static like `/dashboard/v2/fonts/Geist.woff2`) → serve from `STATIC_SPA_DIR`
    5. Verify existing `/` route is byte-identical (read it, don't refactor)
  </action>
  <verify>
    - `curl http://localhost:<port>/dashboard/v2/` returns Vite-built React shell HTML
    - `curl http://localhost:<port>/dashboard/v2/assets/index-XXXXX.js` returns JS bundle
    - `curl http://localhost:<port>/` returns old `index.html` byte-identical
    - `curl http://localhost:<port>/app.js` returns old vanilla JS unchanged
  </verify>
  <done>Both dashboards coexist; old route untouched.</done>
</task>

<task id="116-00-T06">
  <objective>Configure Tailwind with locked design tokens (palette, fonts, breakpoints); self-host WOFF2 fonts.</objective>
  <read_first>
    - 116-CONTEXT.md (aesthetic lock section)
    - .planning/sketches/dashboard-redesign-2026-05-08/v2-fleet-advanced.html (extract any CSS-var hex codes if uncertain)
  </read_first>
  <action>
    1. Edit `src/dashboard/client/tailwind.config.js`:
       - `theme.extend.colors`:
         - `bg.base: '#0e0e12'`, `bg.elevated: '#16161c'`, `bg.muted: '#1c1c24'`, `bg.s3: '#252530'`
         - `fg.1: '#f4f4f5'`, `fg.2: '#a1a1aa'`, `fg.3: '#71717a'`
         - `primary: '#10b981'`, `info: '#3b82f6'`, `warn: '#f59e0b'`, `danger: '#ef4444'`, `gold: '#eab308'`, `pink: '#ff3366'`
       - `theme.extend.fontFamily`:
         - `display: ['Cabinet Grotesk', 'system-ui', 'sans-serif']`
         - `sans: ['Geist', 'system-ui', 'sans-serif']`
         - `mono: ['JetBrains Mono', 'ui-monospace', 'monospace']`
       - `theme.screens` (OVERRIDES Tailwind defaults): `sm: '375px', md: '768px', lg: '1024px', xl: '1280px', 2xl: '1920px'`
    2. Download Latin-subset WOFF2 files:
       - Cabinet Grotesk (Indian Type Foundry, free) — weights 400, 500, 600, 700
       - Geist (Vercel OFL) — weights 300, 400, 500, 600
       - JetBrains Mono (OFL) — weights 400, 500, 600
       Save to `src/dashboard/client/public/fonts/`
    3. Add `@font-face` declarations in `src/dashboard/client/src/index.css` pointing at `/dashboard/v2/fonts/*.woff2`
  </action>
  <verify>
    Built CSS bundle contains custom hex values; `curl /dashboard/v2/fonts/Geist-400.woff2` returns 200; loaded page DevTools confirms fonts loaded.
  </verify>
  <done>Tailwind config + fonts complete. Tier 1 components in 116-01 can use `font-display`, `bg-bg-elevated`, `text-primary`, `md:grid-cols-2` etc.</done>
</task>

<task id="116-00-T07">
  <objective>Initialize shadcn/ui in SPA, install Tier 1 component set.</objective>
  <action>
    1. `cd src/dashboard/client && npx shadcn@latest init`
       - Style: New York
       - Base color: neutral (we override via Tailwind extend)
       - CSS variables: yes
    2. Install Tier 1 components: `npx shadcn@latest add card badge progress command tooltip separator dialog button skeleton scroll-area`
    3. Verify components land under `src/dashboard/client/src/components/ui/`
  </action>
  <verify>`ls src/dashboard/client/src/components/ui/` shows ≥10 .tsx files; one smoke import `import { Card } from '@/components/ui/card'` resolves at build time.</verify>
  <done>shadcn primitives ready for Tier 1 feature components.</done>
</task>

<task id="116-00-T08">
  <objective>SSE + TanStack Query foundation hooks.</objective>
  <action>
    1. Create `src/dashboard/client/src/hooks/useSse.ts`:
       - Singleton `EventSource` to `/api/events` (mounted at app root)
       - Listens for the 7 known event types: `agent-status`, `schedules`, `health`, `delivery-queue`, `memory-stats`, `task-state-change`, `error`
       - On each event, calls `queryClient.setQueryData([eventName], data)` (bridges SSE → Query cache)
       - Returns `useSseStatus()` hook for connection indicator
    2. Create `src/dashboard/client/src/hooks/useApi.ts`:
       - Thin TanStack Query wrappers
       - One hook per endpoint: `useAgents()` (from `agent-status` SSE cache), `useAgentCache(name)`, `useFleetStats()`, `useSchedules()`, `useHealth()`, `useDeliveryQueue()`, `useMemoryStats()`, `useTasks()`
       - Initial-load fetches via REST; subsequent updates via SSE-pushed cache invalidation
  </action>
  <verify>One-line App.tsx calling `const agents = useAgents()` renders JSON dump of fleet state; DevTools Network shows single `EventSource` connection.</verify>
  <done>Foundation hooks ready for all subsequent React components.</done>
</task>

<task id="116-00-T09">
  <objective>View-mode toggle (Basic / Advanced) with localStorage persistence + viewport-aware default.</objective>
  <action>
    1. Create `src/dashboard/client/src/hooks/useViewMode.ts`:
       - Returns `'basic' | 'advanced'` + setter
       - Default: `'basic'` if viewport width < 1024px, else `'advanced'`
       - Persists to `localStorage['dashboard.viewMode']`
       - Re-reads on first mount
    2. Wrap `main.tsx` with `<ViewModeProvider>` context
    3. Add toggle button in header skeleton (Plan 116-01 will style it)
  </action>
  <verify>Resizing browser <1024px on first visit defaults to Basic; clicking toggle flips to Advanced and persists across reload.</verify>
  <done>View-mode context ready for tier-gated rendering in 116-01.</done>
</task>

<task id="116-00-T10">
  <objective>Smoke component — render N agent cards (name + model badge only) proving full stack works.</objective>
  <action>
    1. Replace Vite's default `App.tsx` with a minimal layout:
       - Header: "ClawCode v2" + view-mode toggle (visual only, not styled)
       - Body: `{agents.map(a => <Card key={a.name}>{a.name} · {a.model}</Card>)}`
    2. Use `useAgents()` from T08
  </action>
  <verify>
    - Visit `http://localhost:<port>/dashboard/v2/` → shows N cards = number of agents in `clawcode.yaml`
    - Stop one agent via `clawcode stop <name>` → card status visually updates within 3s (next SSE tick)
    - Restart agent → card updates back
  </verify>
  <done>Full stack proven: build → serve → SSE → Query → React → render. Plan 116-01 can replace this with real Tier 1 components.</done>
</task>

## Deviation handling

- **If T01 cache wipe doesn't recover producer call sites:** Document in `116-DEFERRED.md`, mark Finding B as needing deeper investigation, proceed with the plan. F07 in 116-02 will fall back to `trace_spans`.
- **If F02 Opus threshold derivation surfaces unexpected fleet data (e.g., Haiku faster than Sonnet on first_token):** Pause T02 and surface to operator before committing the threshold values.
- **If `npx shadcn@latest init` prompts unexpectedly during T07:** Accept defaults; record any deviations from the locked New York / neutral / CSS-variables config.

## Critical files at end of plan

```
package.json                              # build script extended + client devDeps
src/dashboard/server.ts                   # /dashboard/v2 route added; / unchanged
src/performance/slos.ts                   # per-model SLO + resolveSloFor helper
src/config/schema.ts                      # optional agents[*].perf.slos override
src/dashboard/client/                     # NEW SPA root
├── vite.config.ts
├── tailwind.config.js
├── postcss.config.js
├── components.json                       # shadcn config
├── public/fonts/*.woff2                  # self-hosted fonts (subset Latin)
├── src/
│   ├── main.tsx
│   ├── App.tsx                          # smoke component
│   ├── index.css                        # @font-face + Tailwind base
│   ├── hooks/
│   │   ├── useSse.ts
│   │   ├── useApi.ts
│   │   └── useViewMode.ts
│   └── components/ui/                    # shadcn primitives (~10 files)
└── tsconfig.json
dist/cli/index.js                         # rebuilt; contains iterateWithTracing + producer call sites
dist/dashboard/spa/                       # new bundle output
```

## Estimated total: 8-10h
