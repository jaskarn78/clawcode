---
phase: 999.3-specialist-subagent-routing-via-delegateTo-spawn-subagent-thread
plan: 01
subsystem: discord-subagent-spawner
tags: [mcp-tool, ipc-handler, discord-thread, subagent, delegation, recursion-guard, tdd]
requirements: [SPEC-01, SPEC-02, SPEC-03, SPEC-04, SPEC-05, SPEC-06, SPEC-07]
dependency_graph:
  requires:
    - "src/discord/subagent-thread-spawner.ts (Phase 100-fu spawnInThread base shape)"
    - "src/discord/__tests__/subagent-recursion-guard.test.ts (Phase 99-N RG1..RG5 invariant)"
    - "src/manager/session-manager.ts:1829 getAgentConfig (returns config regardless of running state)"
    - "src/shared/errors.ts ManagerError (Phase 85 TOOL-04 verbatim-error pattern)"
  provides:
    - "SubagentThreadConfig.delegateTo?: string field"
    - "spawn_subagent_thread MCP tool delegateTo Zod parameter"
    - "spawn-subagent-thread IPC handler with delegate validation"
    - "DEL-01..DEL-10 spawner-level test coverage for delegation contract"
  affects:
    - "Any agent calling spawn_subagent_thread can opt into specialist delegation by passing delegateTo"
    - "Recursion-guard invariant (Phase 99-N D-TCX-03) extended to delegated subagents"
tech_stack:
  added: []
  patterns:
    - "TDD with separate RED + GREEN commits (Phase 83/84/85/86 blueprint)"
    - "4-surface fan-through (types → spawner → IPC → MCP) for additive parameter rollout"
    - "Verbatim ManagerError surface from IPC to MCP caller (Phase 85 TOOL-04)"
    - "Per-message webhook identity override (delegate's displayName/avatar over caller's webhookUrl)"
key_files:
  created:
    - ".planning/phases/999.3-.../999.3-01-SUMMARY.md (this file)"
  modified:
    - "src/discord/subagent-thread-types.ts (+11 lines — delegateTo field)"
    - "src/discord/subagent-thread-spawner.ts (+72 / -16 lines — delegateTo branch in spawnInThread)"
    - "src/discord/subagent-thread-spawner.test.ts (+207 lines — DEL-01..DEL-10)"
    - "src/manager/daemon.ts (+12 lines — IPC handler validates + forwards delegateTo)"
    - "src/mcp/server.ts (+2 lines — Zod field + sendIpcRequest forward + updated descriptions)"
decisions:
  - "Composition contract: sourceConfig (= delegate ?? caller) provides inherited fields via spread; parentConfig overrides channel-scoped/quota fields after the spread (channels:[], threads:caller's, webhook:composed). disallowedTools recursion guard set OUTSIDE both branches in the final assembly literal — UNCONDITIONAL per D-TCX-03."
  - "Empty-string delegateTo treated as undefined at BOTH IPC layer (D-EDG-04) AND spawner level (defense-in-depth)."
  - "Delegate config validated at IPC boundary (manager.getAgentConfig) so verbatim ManagerError(\"Delegate agent 'X' not found in config\") surfaces to MCP caller per Phase 85 TOOL-04 pattern. Spawner-level re-check (DEL-10) is defense-in-depth."
  - "Webhook composition splits identity: caller's webhookUrl (channel-bound), delegate's displayName + avatarUrl (per-message overrides via discord.js client.send). Verified at webhook-manager.ts:71-75."
  - "Session name `-via-` infix is greppable in logs (operator-friendly: `grep -- -via-` filters all delegated subagent activity)."
  - "Verbatim D-TCX-01 4-line 'Delegation Context' suffix lands at src/discord/subagent-thread-spawner.ts:399-403 with canonical 'acting on behalf of' phrase."
  - "No deploy step performed (D-PLN-02). Operator authorizes deploy explicitly in a separate turn."
metrics:
  duration_minutes: 10
  completed_date: 2026-04-29
  task_count: 4
  files_modified: 5
  files_created: 1
  tests_added: 10
  tests_total: 627
  commits: 4
---

# Phase 999.3 Plan 01: Specialist Subagent Routing via delegateTo — Summary

Pure-additive `delegateTo?: string` parameter fan-through on the `spawn_subagent_thread` MCP tool, threading from MCP Zod schema → IPC handler → SubagentThreadConfig → SubagentThreadSpawner.spawnInThread, so admin-clawdy / fin-acquisition can delegate elevated-thinking work to dedicated specialist agents (research / fin-research) with the thread still landing in the caller's channel.

## Tasks Executed

| #   | Task                                                                                            | Commit                                                | Files                                                                       |
| --- | ----------------------------------------------------------------------------------------------- | ----------------------------------------------------- | --------------------------------------------------------------------------- |
| 1   | RED — DEL-01..DEL-10 delegateTo behavior pins                                                   | `18933b1`                                             | `src/discord/subagent-thread-spawner.test.ts`                               |
| 2   | GREEN — delegateTo branch in spawnInThread + SubagentThreadConfig field                         | `f3a8832`                                             | `src/discord/subagent-thread-types.ts`, `src/discord/subagent-thread-spawner.ts` |
| 3   | IPC handler validates + forwards delegateTo for spawn-subagent-thread                           | `a4aa659`                                             | `src/manager/daemon.ts`                                                     |
| 4   | Expose delegateTo on spawn_subagent_thread MCP tool + update descriptions                       | `c938798`                                             | `src/mcp/server.ts`                                                         |

## Test Coverage

**10 new tests added** in `subagent-thread-spawner.test.ts` under top-level `describe("spawnInThread with delegateTo")`:

| Test ID | Behavior pinned                                                                          | Decision  |
| ------- | ---------------------------------------------------------------------------------------- | --------- |
| DEL-01  | Subagent inherits delegate's model/soul/skills/mcpServers/subagentModel                  | D-INH-01  |
| DEL-02  | Session name uses `${parent}-via-${delegate}-${shortId}` regex                           | D-NAM-01  |
| DEL-03  | threadContext appends "## Delegation Context" w/ "acting on behalf of"                   | D-TCX-01  |
| DEL-04  | Webhook = caller's URL + delegate's displayName + delegate's avatar                      | D-INH-03  |
| DEL-05  | `disallowedTools` recursion guard set even when delegating                               | D-TCX-03  |
| DEL-06  | Caller's `threads.maxThreadSessions` wins over delegate's                                | D-INH-02  |
| DEL-07  | Dormant delegate (`autoStart=false`) is allowed                                          | D-EDG-02  |
| DEL-08  | Self-delegation allowed; `-via-` infix retained                                          | D-EDG-03  |
| DEL-09  | Back-compat — no delegateTo → `-sub-` infix (NOT `-via-`)                                | SPEC-07   |
| DEL-10  | Defense-in-depth — unknown delegate throws ManagerError verbatim at spawner level        | D-EDG-05  |

**RED → GREEN sequence:** 6 of the 10 tests truly RED before Task 2 (DEL-01/02/03/04/08/10); 4 invariant pins passed pre-implementation but guard against future regression (DEL-05/06/07/09).

**Recursion-guard regression preserved:** All Phase 99-N RG1..RG5 tests in `src/discord/__tests__/subagent-recursion-guard.test.ts` stay green — the unconditional `disallowedTools: ["mcp__clawcode__spawn_subagent_thread"]` line lives at the final-assembly literal (single set-site, count = 1).

**Wider sweep:** 627 tests pass across `src/discord/` + `src/mcp/` (57 test files). `npm run typecheck` holds at 108-error baseline (zero new errors).

## 4-Surface Fan-Through

```
MCP tool                           daemon IPC                          SubagentThreadConfig                   SubagentThreadSpawner
src/mcp/server.ts:344-365      →   src/manager/daemon.ts:5042-5078 →   src/discord/subagent-thread-types.ts → src/discord/subagent-thread-spawner.ts:319-440
─────────────────────────          ──────────────────────────────      ───────────────────────────────       ─────────────────────────────────────────────
delegateTo: z.string()             const delegateToRaw = ...           readonly delegateTo?: string;         normalizedDelegateTo = ... (empty→undefined)
  .optional().describe(...)        const delegateTo = (raw && len>0)                                          delegateConfig = sessionManager.getAgentConfig(...)
+ destructure                        ? raw : undefined                                                          if (normalizedDelegateTo && !delegateConfig) throw ManagerError
+ sendIpcRequest forward           if (delegateTo) { manager.getAgentConfig — verbatim                          sourceConfig = delegateConfig ?? parentConfig
                                     ManagerError on miss }                                                     sessionName: ${parent}-via-${delegate}-${shortId}
                                   spawner.spawnInThread({ ..., delegateTo })                                    threadContext: + "## Delegation Context" 4-line block
                                                                                                                 webhook: caller URL + delegate display+avatar
                                                                                                                 subagentConfig: ...sourceConfig spread + caller-side overrides
                                                                                                                 disallowedTools: UNCONDITIONAL recursion guard
```

## Verbatim D-TCX-01 Threadcontext Suffix

The canonical 4-line "Delegation Context" suffix lands at **`src/discord/subagent-thread-spawner.ts:399-403`**:

```ts
"## Delegation Context",
`You are acting on behalf of \`${config.parentAgentName}\` who delegated this work to you.`,
"Use your full identity, skills, and standards. Treat the operator's request",
"as one fulfilled THROUGH you, not BY you.",
```

Pinned by DEL-03 test which asserts `## Delegation Context` + `acting on behalf of` + `agent-a` are all present in the captured `startedConfig.soul`.

## D-TCX-03 Recursion-Guard Invariant Preserved

```
$ grep -c 'disallowedTools.*mcp__clawcode__spawn_subagent_thread' src/discord/subagent-thread-spawner.ts
1
```

Single, unconditional set-site at `src/discord/subagent-thread-spawner.ts:434` — outside both delegate and non-delegate branches. The Phase 99-N recursion guard cannot be bypassed by passing `delegateTo` (DEL-05 pins this).

## Deviations from Plan

None — plan executed exactly as written. All 4 tasks landed atomic, RED → GREEN → IPC → MCP order preserved, no auto-fix required during implementation.

## Operator Next Steps

1. **Authorize deploy** explicitly in a separate turn (per CONTEXT.md D-PLN-02). Local commits live at `master` head; production server at `/opt/clawcode` runs unchanged until operator triggers `clawcode update --restart`.

2. **Live smoke test** (after deploy):

   ```text
   admin-clawdy: spawn_subagent_thread({
     agent: "admin-clawdy",
     threadName: "test-research-delegation",
     task: "Research the difference between sqlite-vec and sqlite-vss",
     delegateTo: "research"
   })
   ```

   Expected: thread created in admin-clawdy's main channel; subagent posts under "Research" displayName + avatar; session name in logs `admin-clawdy-via-research-<6char>`; autoRelay summary lands in admin-clawdy's main channel.

3. **Negative-path smoke test:**

   ```text
   admin-clawdy: spawn_subagent_thread({
     agent: "admin-clawdy",
     threadName: "ghost-test",
     task: "...",
     delegateTo: "non-existent-agent"
   })
   ```

   Expected: `Error: Delegate agent 'non-existent-agent' not found in config` surfaces verbatim to admin-clawdy's tool result.

## Phase 2 Follow-Ups (Deferred per CONTEXT.md `<deferred>`)

- Per-agent `specialists: { research: <name>, coding: <name> }` config in `clawcode.yaml`
- `consult_specialist(role)` convenience tool that resolves role → standing agent name automatically
- Cross-channel delegation (caller specifies a different channel for the thread)
- Memory continuity across spawns via `forkSession` from delegate's running state
- Specialist auto-archive policy

## Self-Check

```
$ [ -f "src/discord/subagent-thread-types.ts" ] && [ -f "src/discord/subagent-thread-spawner.ts" ] && [ -f "src/manager/daemon.ts" ] && [ -f "src/mcp/server.ts" ] && [ -f "src/discord/subagent-thread-spawner.test.ts" ] && echo "all 5 files exist"
all 5 files exist

$ git log --oneline | grep -E "999\.3" | head -5
c938798 feat(999.3): expose delegateTo on spawn_subagent_thread MCP tool + update descriptions
a4aa659 feat(999.3): IPC handler validates + forwards delegateTo for spawn-subagent-thread
f3a8832 feat(999.3): GREEN — delegateTo branch in spawnInThread + SubagentThreadConfig field
18933b1 test(999.3): RED — DEL-01..DEL-10 delegateTo behavior pins (6 failing tests)
02fa31f docs(999.3): create phase plan — delegateTo branch on spawn_subagent_thread
```

## Self-Check: PASSED
