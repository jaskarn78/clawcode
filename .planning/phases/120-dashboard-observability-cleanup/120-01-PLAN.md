---
phase: 120-dashboard-observability-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/120-dashboard-observability-cleanup/120-DIAGNOSTIC.md  # NEW
autonomous: true
requirements: [DASH-01]
tags: [dashboard, observability, diagnostic, sql, trace_spans]

must_haves:
  truths:
    - "Diagnostic SQL has been run against clawdy production trace_spans"
    - "Captured output is committed to 120-DIAGNOSTIC.md"
    - "Conclusion identifies which DASH-01 root cause is confirmed (emitter / IPC binding / frontend)"
    - "Plan 02..04 status section explicitly green-lights or replans downstream work"
  artifacts:
    - path: ".planning/phases/120-dashboard-observability-cleanup/120-DIAGNOSTIC.md"
      provides: "Diagnostic SQL output + root-cause conclusion"
      contains: "## Conclusion"
  key_links:
    - from: "120-DIAGNOSTIC.md"
      to: "120-02-PLAN.md / 120-03-PLAN.md"
      via: "## Plans 120-02..04 status section"
      pattern: "green-light|replan"
---

## Goal

Run the diagnostic SQL on clawdy production trace_spans BEFORE any code lands. Localize DASH-01's root cause (empty-name emitter bug vs IPC space-binding drift vs frontend null-styling) and either green-light or replan Wave 2.

This plan blocks all other Phase 120 plans per CONTEXT D-01 and the ROADMAP sequencing note.

## Context

@/home/jjagpal/.openclaw/workspace-coding/CLAUDE.md
@/home/jjagpal/.openclaw/workspace-coding/.planning/PROJECT.md
@/home/jjagpal/.openclaw/workspace-coding/.planning/ROADMAP.md
@/home/jjagpal/.openclaw/workspace-coding/.planning/phases/120-dashboard-observability-cleanup/120-CONTEXT.md

## Tasks

<task type="auto">
  <name>T-01: Run diagnostic SQL on clawdy production trace_spans</name>
  <files>(read-only against production SQLite; no code changes)</files>
  <read_first>
    - .planning/phases/120-dashboard-observability-cleanup/120-CONTEXT.md (D-01 — the canonical SQL)
    - .planning/ROADMAP.md §"Phase 120 — Sequencing note"
  </read_first>
  <action>
    Run the two diagnostic queries against clawdy's production trace_spans store. Read-only — no writes, no schema changes, no deploy gate (per D-01 + operator note: safe to run anytime).

    Connection: trace_spans lives in the manager's SQLite on clawdy (`/opt/clawcode/data/` per the standard install layout — confirm exact path via `ssh clawdy 'sudo systemctl show clawcode -p Environment'` or `ssh clawdy 'sudo ls /opt/clawcode/data/*.db'`). Use `sudo sqlite3 <db> '<query>'` over SSH. Do NOT copy the DB locally.

    Query A (canonical DASH-01 guard probe — per CONTEXT D-01):
    ```sql
    SELECT name, COUNT(*) FROM trace_spans
    WHERE name LIKE 'tool_call.%' AND LENGTH(name) <= 11
    GROUP BY name;
    ```

    Query B (IPC-binding-vs-emitter discriminator — spans whose `agent` column contains a space; per planner brief):
    ```sql
    SELECT agent, name, COUNT(*) FROM trace_spans
    WHERE name LIKE 'tool_call.%' AND agent LIKE '% %'
    GROUP BY agent, name
    ORDER BY COUNT(*) DESC
    LIMIT 50;
    ```

    Capture FULL stdout from both queries verbatim. If a query returns zero rows, that itself is a finding — note it explicitly.

    Interpretation key (apply during T-02, not during execution):
    - Query A returns rows with empty `name` → emitter bug (spans are landing with blank names).
    - Query A returns rows with truncated names like `tool_call.` → SQL `LENGTH(name) <= 11` guard misfires on the trailing-dot prefix.
    - Query B shows space-bearing agents missing names while no-space agents have names → IPC string-binding bug.
    - Both queries return data with sensible names → frontend rendering bug (group-by collapses).
  </action>
  <verify>
    <automated>test -n "$(ssh clawdy 'sudo sqlite3 -version' 2>&1)" && echo "ssh+sqlite available"</automated>
  </verify>
  <done>
    Both queries executed against clawdy production. Full stdout captured to a local scratch buffer (or pasted into T-02 directly). Connection method documented (which DB path was queried).
  </done>
</task>

<task type="auto">
  <name>T-02: Write 120-DIAGNOSTIC.md with captured output and root-cause conclusion</name>
  <files>.planning/phases/120-dashboard-observability-cleanup/120-DIAGNOSTIC.md</files>
  <read_first>
    - T-01 captured output
    - .planning/phases/120-dashboard-observability-cleanup/120-CONTEXT.md §"DASH-01" + §"D-01"
  </read_first>
  <action>
    Create `120-DIAGNOSTIC.md` with these exact sections (in this order):

    1. `# Phase 120 — DASH-01 Root Cause Diagnostic`
    2. `## Run Metadata` — UTC timestamp, clawdy host, DB path queried, command used.
    3. `## Query A — LENGTH(name) <= 11 guard probe` — the SQL verbatim in a ```sql block, then the captured output in a ```text block. If zero rows, write `(0 rows)`.
    4. `## Query B — Space-bearing agent discriminator` — same shape as Query A.
    5. `## Conclusion` — exactly ONE of the four root-cause labels from CONTEXT (emitter / SQL-guard / IPC-binding / frontend), with 2-3 sentences citing which query output justifies the label. If the data is ambiguous, write `Conclusion: AMBIGUOUS — additional probe required` and list the next probe.
    6. `## Plans 120-02..04 Status` — for EACH of 120-02, 120-03, 120-04, write `GREEN-LIGHT` or `REPLAN: <reason>`. Default: GREEN-LIGHT all three unless Conclusion is AMBIGUOUS or the diagnostic surfaces a root cause that invalidates a plan's scope.

    Do NOT speculate beyond the data. Do NOT write code-fix recommendations — those belong in Plans 02-04.
  </action>
  <verify>
    <automated>test -f .planning/phases/120-dashboard-observability-cleanup/120-DIAGNOSTIC.md && grep -q '^## Conclusion' .planning/phases/120-dashboard-observability-cleanup/120-DIAGNOSTIC.md && grep -q '^## Plans 120-02..04 Status' .planning/phases/120-dashboard-observability-cleanup/120-DIAGNOSTIC.md</automated>
  </verify>
  <done>
    120-DIAGNOSTIC.md exists with all six required sections, both query outputs captured verbatim, Conclusion section names one root cause (or AMBIGUOUS), and Plans 120-02..04 Status section names each downstream plan with a green-light/replan verdict.
  </done>
</task>

## Operator Notes

- **Read-only against production.** Per D-09 the deploy hold continues, but D-01 explicitly carves out diagnostic SQL: this plan touches NO bytes on clawdy and does NOT require operator deploy clearance. Safe to run anytime, including while Ramy is active.
- **No new abstractions** per D-08 — this is investigation, not code.
- **Sequencing gate.** Until 120-DIAGNOSTIC.md ships, Wave 2 (Plans 02/03/04) does not start. The Plans-Status section IS the gate.

## Verification

| Success Criterion | Check |
|---|---|
| (Phase-gate for SC-1) Root cause for DASH-01 is localized before code | `grep -A 3 '^## Conclusion' 120-DIAGNOSTIC.md` names emitter / SQL-guard / IPC-binding / frontend / AMBIGUOUS |
| Downstream plans are explicitly green-lit | `grep -E 'GREEN-LIGHT\|REPLAN' 120-DIAGNOSTIC.md` returns 3 lines (one per 120-02..04) |
| Diagnostic is reproducible | `## Run Metadata` section names DB path + command |

## Output

After completion, no SUMMARY.md needed for Plan 01 — `120-DIAGNOSTIC.md` IS the deliverable. Plans 02-04 read it as their first context.
