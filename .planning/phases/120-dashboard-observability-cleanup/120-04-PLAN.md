---
phase: 120-dashboard-observability-cleanup
plan: 04
type: execute
wave: 2
depends_on: [120-01]
files_modified:
  - .planning/phases/120-dashboard-observability-cleanup/120-04-VERIFICATION.md  # NEW
autonomous: false  # deploy-gated per D-09; pauses for operator deploy clearance
requirements: [DASH-05]
tags: [verification, cli, tool-latency-audit, deploy-gated]

must_haves:
  truths:
    - "clawcode tool-latency-audit CLI exits 0 against clawdy production trace window"
    - "CLI stdout is valid JSON parseable by JSON.parse / jq"
    - "The Phase 106 hotfix fa72303 is verified end-to-end against a non-empty trace window"
  artifacts:
    - path: ".planning/phases/120-dashboard-observability-cleanup/120-04-VERIFICATION.md"
      provides: "DASH-05 closure artifact — CLI output + exit code + non-empty-window confirmation"
      contains: "## CLI Output"
  key_links:
    - from: "operator deploy-clearance signal"
      to: "Plan 04 execution"
      via: "checkpoint:human-action"
      pattern: "deploy clearance"
---

## Goal

Capture the DASH-05 verification artifact: invoke `clawcode tool-latency-audit` on clawdy against a non-empty production trace window, confirm exit code 0 and valid JSON, attach stdout to `120-04-VERIFICATION.md`. No new code — the Phase 106 hotfix `fa72303` already shipped; this plan closes the verification loop per CONTEXT D-04.

## Context

@/home/jjagpal/.openclaw/workspace-coding/CLAUDE.md
@/home/jjagpal/.openclaw/workspace-coding/.planning/PROJECT.md
@/home/jjagpal/.openclaw/workspace-coding/.planning/phases/120-dashboard-observability-cleanup/120-CONTEXT.md
@/home/jjagpal/.openclaw/workspace-coding/.planning/phases/120-dashboard-observability-cleanup/120-DIAGNOSTIC.md
@/home/jjagpal/.openclaw/workspace-coding/src/cli/commands/tool-latency-audit.ts

## Tasks

<task type="checkpoint:human-action" gate="blocking">
  <name>T-00 (gate): Operator deploy-window clearance</name>
  <what-built>
    Plans 02 and 03 have shipped local-tested fixes. Production verification of DASH-05 requires invoking the `clawcode` CLI on clawdy, which means running a binary against the production daemon's data path.
  </what-built>
  <why-blocking>
    Per D-09 + global memory `feedback_ramy_active_no_deploy`: no production-side commands while Ramy is active in #fin-acquisition. The CLI invocation in T-01 is read-only against trace_spans, but it touches the clawdy host and produces operator-visible output. Operator confirms quiet OR emergency-clear before T-01 runs.
  </why-blocking>
  <how-to-verify>
    1. Operator confirms one of: "Ramy is idle / not on a live thread" OR "this verification is acceptable now."
    2. If using deploy script `scripts/deploy-clawdy.sh` for anything during the same window: independent operator confirmation per `feedback_no_auto_deploy`.
  </how-to-verify>
  <resume-signal>Operator says "go ahead with DASH-05 verification" or equivalent green-light.</resume-signal>
</task>

<task type="auto">
  <name>T-01: Invoke clawcode tool-latency-audit on clawdy against non-empty production trace window</name>
  <files>(no code; CLI invocation against clawdy)</files>
  <read_first>
    - src/cli/commands/tool-latency-audit.ts — confirm the CLI accepts the window args you intend to pass (default window vs explicit flags)
    - 120-DIAGNOSTIC.md — confirms the production trace_spans store has data in the target window
    - git: `git show fa72303 -- src/cli/commands/tool-latency-audit.ts` — the Phase 106 hotfix being verified
  </read_first>
  <action>
    Pre-flight (non-empty-window check per CONTEXT §"Specific Requirements"):
    ```bash
    ssh clawdy 'sudo sqlite3 <prod-db-path> "SELECT COUNT(*) FROM trace_spans WHERE name LIKE '\''tool_call.%'\'' AND start_ms > (strftime('\''%s'\'','\''now'\'')*1000 - 3600000);"'
    ```
    If count is 0, DO NOT proceed — either widen the window or wait for live traffic / trigger a synthetic turn through a test agent. Reporting PASS against an empty window violates the requirement.

    Verification invocation:
    ```bash
    ssh clawdy 'sudo /usr/local/bin/clawcode tool-latency-audit' > /tmp/dash-05-stdout.json
    echo "exit=$?"
    ```
    (Adjust the binary path if `deploy-clawdy.sh` installs elsewhere — confirm via `ssh clawdy 'which clawcode'`. Adjust CLI flags to match the actual `tool-latency-audit` signature read from `src/cli/commands/tool-latency-audit.ts`.)

    Assert two things:
    1. Exit code is 0.
    2. `cat /tmp/dash-05-stdout.json | jq .` succeeds (valid JSON, jq exits 0).

    Capture for T-02:
    - Full stdout (verbatim).
    - Exit code.
    - Pre-flight non-empty-window query + COUNT result.
    - UTC timestamp.
    - Window the CLI scanned (from the CLI's own output or from the flags passed).
  </action>
  <verify>
    <automated>test -f /tmp/dash-05-stdout.json && jq . /tmp/dash-05-stdout.json > /dev/null && echo "valid JSON captured"</automated>
  </verify>
  <done>
    CLI exit 0 + valid JSON captured against a confirmed-non-empty trace window. Captured artifacts (stdout + exit + pre-flight count + timestamp + window) are in hand for T-02. If exit != 0: capture the failure mode verbatim for T-02 (do NOT discard — the failure is the verification result).
  </done>
</task>

<task type="auto">
  <name>T-02: Write 120-04-VERIFICATION.md with CLI output</name>
  <files>.planning/phases/120-dashboard-observability-cleanup/120-04-VERIFICATION.md</files>
  <read_first>
    - T-01 captured artifacts
    - CONTEXT D-04 (verification-only, no new code)
  </read_first>
  <action>
    Create `120-04-VERIFICATION.md` with these sections (in order):

    1. `# Phase 120 — DASH-05 Verification (Phase 106 hotfix fa72303 end-to-end)`
    2. `## Run Metadata` — UTC timestamp, clawdy host, binary path, CLI args invoked, deploy-clearance operator + utterance.
    3. `## Pre-Flight: Non-Empty Window Confirmation` — the COUNT(*) SQL + its output. Per CONTEXT specifics, this section is mandatory.
    4. `## CLI Invocation` — exact command line.
    5. `## CLI Output` — captured stdout verbatim inside a ```json fence.
    6. `## Exit Code` — the numeric exit.
    7. `## JSON Validation` — `jq` invocation + result (PASS / FAIL with error).
    8. `## Verdict` — exactly one of:
       - `PASS — DASH-05 closed. Phase 106 hotfix fa72303 verified end-to-end.`
       - `BLOCKED-deploy-pending — operator clearance not yet given; rerun T-00/T-01 when window opens.`
       - `FAIL — CLI exit <N>: <one-line failure summary>. Phase 106 hotfix is NOT verified; escalate to a new bug-fix phase.`

    If verdict is FAIL: do NOT silently fix in this plan. Surface to operator via advisor() and let them decide whether to scope a new fix phase or revert the hotfix.
  </action>
  <verify>
    <automated>test -f .planning/phases/120-dashboard-observability-cleanup/120-04-VERIFICATION.md && grep -qE '^## Verdict' .planning/phases/120-dashboard-observability-cleanup/120-04-VERIFICATION.md && grep -qE 'PASS|BLOCKED-deploy-pending|FAIL' .planning/phases/120-dashboard-observability-cleanup/120-04-VERIFICATION.md</automated>
  </verify>
  <done>
    120-04-VERIFICATION.md exists with all eight sections. Verdict line is one of the three sanctioned strings. If PASS, DASH-05 is closed and SC-5 is met.
  </done>
</task>

## Operator Notes

- **DEPLOY HOLD continues** per D-09 + `feedback_ramy_active_no_deploy`. T-00 is the explicit operator gate. Plan 04 may sit at the T-00 checkpoint for days — that's acceptable; the other three plans (01/02/03) complete without it. T-00 does NOT block the closure of Plans 01-03.
- **Verification-only, no new code** per D-04. If the CLI fails, this plan's job is to capture the failure faithfully, not patch the CLI. Patching is a separate phase.
- **Non-empty-window requirement** per CONTEXT §"Specific Requirements" — reporting PASS against zero spans is not allowed. Pre-flight COUNT(*) gate is mandatory.
- **Silent-path-bifurcation reminder** — the CLI path under verification was itself a silent-path-bifurcation victim (Phase 106 history). If new bifurcation suspected during T-01 (e.g. CLI hits a different code path than expected), reuse Plan 03's static-grep pattern in a follow-up.

## Verification

| Success Criterion | Check |
|---|---|
| SC-5 (CLI exit 0 + valid JSON on clawdy against non-empty window) | `120-04-VERIFICATION.md` §"Verdict" reads `PASS — DASH-05 closed...` |
| Pre-flight non-empty-window enforced | `120-04-VERIFICATION.md` §"Pre-Flight" shows COUNT(*) > 0 |
| Phase 106 fa72303 verified end-to-end | Verdict references the hotfix commit and SC-5 simultaneously |
| Deploy-hold honored | T-00 captured operator clearance utterance in §"Run Metadata" |

## Output

`120-04-VERIFICATION.md` is the artifact. No SUMMARY.md needed unless the verdict is FAIL (in which case write a SUMMARY pointing at the follow-up phase scope).
