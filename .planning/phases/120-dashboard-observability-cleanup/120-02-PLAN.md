---
phase: 120-dashboard-observability-cleanup
plan: 02
type: execute
wave: 2
depends_on: [120-01]
files_modified:
  - src/dashboard/client/src/components/BenchmarksView.tsx
  - src/dashboard/client/src/components/percentileCell.tsx  # NEW (utility per D-05)
  - src/dashboard/client/src/components/__tests__/percentileCell.test.tsx  # NEW
  - src/dashboard/client/src/__tests__/static-grep-text-danger.test.ts  # NEW (D-02/D-10 regression sentinel)
  - (additionally — whichever SQL helper or trace-spans query file the diagnostic points at, if DASH-01 root cause is SQL-side; confirmed at runtime via 120-DIAGNOSTIC.md)
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03]
tags: [dashboard, frontend, BenchmarksView, percentile, regression-test]

must_haves:
  truths:
    - "Tool rollup table renders the actual tool name for every agent (including 'Admin Clawdy' with the embedded space) when underlying span data exists"
    - "Null percentile cells render with text-fg-3 neutral styling and a '—' label across BenchmarksView and any sibling percentile renderer"
    - "Tool rollup table shows the literal string 'No tool spans recorded in window' when an agent has zero spans"
    - "Static-grep regression test rejects text-danger applied to a value-could-be-null branch"
  artifacts:
    - path: "src/dashboard/client/src/components/percentileCell.tsx"
      provides: "Single canonical percentile cell utility per D-05"
      exports: ["percentileCell"]
    - path: "src/dashboard/client/src/__tests__/static-grep-text-danger.test.ts"
      provides: "CI guard against text-danger on null-input branches"
      contains: "grep"
  key_links:
    - from: "BenchmarksView.tsx"
      to: "percentileCell utility"
      via: "import"
      pattern: "from.*percentileCell"
    - from: "BenchmarksView.tsx empty-state branch"
      to: "operator-facing UI"
      via: "literal string render"
      pattern: "No tool spans recorded in window"
---

## Goal

Ship three DASH-tab regressions as a single frontend bundle per CONTEXT D-02 (one component, one render path, one deploy): blank-tool-name rows (DASH-01), null-as-breach styling (DASH-02), and missing empty-state message (DASH-03). Pin the null-styling behavior with a static-grep regression test per D-05/D-10.

## Context

@/home/jjagpal/.openclaw/workspace-coding/CLAUDE.md
@/home/jjagpal/.openclaw/workspace-coding/.planning/PROJECT.md
@/home/jjagpal/.openclaw/workspace-coding/.planning/phases/120-dashboard-observability-cleanup/120-CONTEXT.md
@/home/jjagpal/.openclaw/workspace-coding/.planning/phases/120-dashboard-observability-cleanup/120-DIAGNOSTIC.md
@/home/jjagpal/.openclaw/workspace-coding/src/dashboard/client/src/components/BenchmarksView.tsx

<interfaces>
<!-- Contract that downstream tasks implement against. Per D-05 — single utility, applied universally. -->

src/dashboard/client/src/components/percentileCell.tsx (NEW):
```typescript
export interface PercentileCellProps {
  value: number | null;
  isBreach: boolean;
}

export function percentileCell({ value, isBreach }: PercentileCellProps): JSX.Element;
// Returns <td> with:
//   className = isBreach ? 'text-danger' : value === null ? 'text-fg-3' : 'text-fg-1'
//   children  = value ?? '—'
// NOTE: isBreach must be false when value is null (caller's responsibility — the static-grep
// test enforces that no isBreach computation references a possibly-null value).
```
</interfaces>

## Tasks

<task type="auto" tdd="true">
  <name>T-01: DASH-01 — fix blank tool-name rendering in tool rollup table</name>
  <files>
    src/dashboard/client/src/components/BenchmarksView.tsx
    (+ SQL helper file or IPC handler — determined by 120-DIAGNOSTIC.md Conclusion)
    src/dashboard/client/src/components/__tests__/BenchmarksView.tool-rollup.test.tsx  # NEW or extend
  </files>
  <read_first>
    - .planning/phases/120-dashboard-observability-cleanup/120-DIAGNOSTIC.md §"Conclusion" — names the root-cause layer
    - src/dashboard/client/src/components/BenchmarksView.tsx (full read — find the tool rollup render + the data fetch hook)
  </read_first>
  <behavior>
    - Test 1: Given a tool rollup row with `name = "tool_call.bash"` and `agent = "Admin Clawdy"`, the rendered cell shows `bash` (or `tool_call.bash` — match the existing UX convention found in BenchmarksView), NOT a blank string.
    - Test 2: Given a tool rollup row with `name = ""` (genuinely empty) the cell renders a clearly-attributable placeholder (e.g. `(unnamed)`) — NOT silently blank. This distinguishes "data missing" from "render bug."
    - Test 3: Given 5 rollup rows across 3 agents where one agent's display name contains a space, all 5 names render correctly.
  </behavior>
  <action>
    Branch on 120-DIAGNOSTIC.md Conclusion (per D-01):

    - **If Conclusion = SQL-guard** (e.g. `LENGTH(name) <= 11` misfires on `tool_call.` prefix): locate the helper that builds the rollup query (grep `LENGTH(name)` under `src/manager/` and `src/dashboard/server/`), tighten the guard to filter empty-name spans only (`name <> ''` or `name IS NOT NULL AND name <> ''`), and add a unit test covering the prefix case.
    - **If Conclusion = IPC-binding** (space-bearing agent names lose values in IPC): locate the IPC handler that serializes tool-rollup payloads (grep `'tool-rollup'` or similar method name under `src/ipc/`), verify string-binding doesn't truncate on spaces, fix at the binding site.
    - **If Conclusion = emitter**: locate the span writer (grep `INSERT INTO trace_spans` and the canonical writer from Plan 03 context — `persistent-session-handle.ts`), trace why blank names emit, fix at the writer.
    - **If Conclusion = frontend**: the fix lives entirely in `BenchmarksView.tsx` — the group-by / render path drops names. Fix in component.

    In ALL branches: add a `(unnamed)` fallback in the BenchmarksView render so a future regression produces an attributable label, not a silent blank. This is defense-in-depth, not a substitute for the root-cause fix.

    Use the `percentileCell` utility (T-02) for any percentile column added/touched in this task — do NOT recreate styling inline.

    Reference D-01 in commit message: "fix(120-01): DASH-01 — restore tool name rendering per 120-DIAGNOSTIC.md (Conclusion: <label>)".
  </action>
  <verify>
    <automated>cd src/dashboard/client && npm test -- --run BenchmarksView.tool-rollup 2>&1 | tail -20</automated>
  </verify>
  <done>
    All three behavior tests green. Root cause from 120-DIAGNOSTIC.md is addressed at its layer (not just papered over in the component). `(unnamed)` defense-in-depth fallback present in BenchmarksView. `npm run build` succeeds; `npm run lint` clean for changed files.
  </done>
</task>

<task type="auto" tdd="true">
  <name>T-02: DASH-02 — single percentileCell utility, applied universally + static-grep regression</name>
  <files>
    src/dashboard/client/src/components/percentileCell.tsx  # NEW
    src/dashboard/client/src/components/__tests__/percentileCell.test.tsx  # NEW
    src/dashboard/client/src/__tests__/static-grep-text-danger.test.ts  # NEW
    src/dashboard/client/src/components/BenchmarksView.tsx  # refactor all percentile cells through utility
    (+ any sibling component under src/dashboard/client/src/ that currently uses text-danger — refactor all)
  </files>
  <read_first>
    - Interfaces block above (D-05 contract)
    - src/dashboard/client/src/components/BenchmarksView.tsx (find every text-danger occurrence)
    - Phase 119 static-grep test pattern (reference: CONTEXT §"Reusable Patterns" — Plan 01 T-01 D-09)
  </read_first>
  <behavior>
    percentileCell unit tests:
    - Test 1: `percentileCell({value: 250, isBreach: false})` → renders cell with className `text-fg-1`, content `250`.
    - Test 2: `percentileCell({value: null, isBreach: false})` → renders cell with className `text-fg-3`, content `—`.
    - Test 3: `percentileCell({value: 9999, isBreach: true})` → renders cell with className `text-danger`, content `9999`.
    - Test 4: When `value === null && isBreach === true` (caller bug), the function MUST treat as null-neutral (text-fg-3), NOT breach-red — defensive against the very bug this whole plan exists to prevent.

    Static-grep regression test (D-02/D-10):
    - Greps the entire `src/dashboard/client/src/` tree (excluding `__tests__/` and `percentileCell.tsx` itself) for the literal token `text-danger`.
    - PASS if the only occurrence is via `percentileCell` invocation (i.e. no raw `text-danger` className strings in JSX).
    - FAIL with a message that names the offending file + line + the violating substring.
    - Use `grep -RIn --include='*.tsx' --include='*.ts' --exclude-dir='__tests__' 'text-danger' src/dashboard/client/src/` filtered with `grep -v '^[^:]*:[[:space:]]*//' to skip line comments. Bare `== 0` gates are forbidden per planner rules — assert against an exact expected set, not raw count.
  </behavior>
  <action>
    1. Create `src/dashboard/client/src/components/percentileCell.tsx` implementing the interface from the `<interfaces>` block above. Include Test 4 defensive null-takes-precedence-over-isBreach logic.

    2. Create `percentileCell.test.tsx` with all four behavior tests (RED→GREEN cycle).

    3. Refactor `BenchmarksView.tsx`: replace every percentile `<td>` render with `percentileCell({value, isBreach})`. Walk every `text-danger` occurrence — if it's a percentile cell, route through utility; if it's a genuine breach indicator that is NOT value-driven (e.g. a status icon), leave it but document with `// text-danger: non-value, status indicator` so the static-grep test can be tightened to require the inline-comment exemption.

    4. Repeat refactor for every sibling component under `src/dashboard/client/src/components/` that imports or duplicates percentile rendering (use `grep -rln 'text-danger' src/dashboard/client/src/components/` to enumerate).

    5. Create `static-grep-text-danger.test.ts` per the behavior spec above. Pattern adapted from Phase 119 Plan 01 T-01 D-09 anti-pattern enforcement.

    Per D-08 — do NOT extract a broader `MetricCell` framework. Single utility, exactly the shape in the interface block.
  </action>
  <verify>
    <automated>cd src/dashboard/client && npm test -- --run percentileCell 2>&1 | tail -15 && npm test -- --run static-grep-text-danger 2>&1 | tail -15</automated>
  </verify>
  <done>
    percentileCell utility exists with all 4 unit tests green. Every percentile cell in `src/dashboard/client/src/` routes through the utility. Static-grep regression test green AND fails informatively when a fresh `text-danger` is added to a JSX file (manually verify by adding a probe line, running test, removing probe).
  </done>
</task>

<task type="auto" tdd="true">
  <name>T-03: DASH-03 — explicit empty-state message for zero-span agents</name>
  <files>
    src/dashboard/client/src/components/BenchmarksView.tsx
    src/dashboard/client/src/components/__tests__/BenchmarksView.empty-state.test.tsx  # NEW or extend
  </files>
  <read_first>
    - src/dashboard/client/src/components/BenchmarksView.tsx (locate tool rollup table component + the data length check)
    - CONTEXT D-06 — string is literal, no i18n
  </read_first>
  <behavior>
    - Test 1: Given an agent with zero rollup rows in the window, the tool rollup section renders the EXACT literal string `No tool spans recorded in window` (text-match assertion, not substring).
    - Test 2: Given an agent with ≥1 rollup row, the empty-state message is NOT rendered.
    - Test 3: The empty-state message is NOT visually styled as a breach (no `text-danger`) — uses `text-fg-3` neutral.
  </behavior>
  <action>
    In `BenchmarksView.tsx`, locate the tool rollup table component. Where it currently renders a row of NULL cells for an empty rollup, branch:

    ```tsx
    {rollupRows.length === 0 ? (
      <p className="text-fg-3">No tool spans recorded in window</p>
    ) : (
      // existing table render
    )}
    ```

    Per D-06: the string is the assertion — copy-paste-exact, no template variables, no i18n key, no constant indirection. The literal `"No tool spans recorded in window"` appears in JSX source.

    Per D-08: do NOT extract an `EmptyState` component. Inline the branch.
  </action>
  <verify>
    <automated>cd src/dashboard/client && npm test -- --run BenchmarksView.empty-state 2>&1 | tail -15 && grep -c 'No tool spans recorded in window' src/dashboard/client/src/components/BenchmarksView.tsx</automated>
  </verify>
  <done>
    All three behavior tests green. Literal string appears verbatim in BenchmarksView.tsx source (grep returns ≥1). No `text-danger` on the empty-state element. `npm run build` + `npm run lint` clean.
  </done>
</task>

## Operator Notes

- **DEPLOY HOLD** per D-09 — Ramy is active. Lint/build/test locally; commit changes; do NOT deploy. Production verification of SC-1/SC-2/SC-3 waits for operator-cleared deploy window. Plan 04 captures the deploy-time verification artifact.
- **Silent-path-bifurcation reminder** (Phase 119 D-02 sentinel) — T-02's static-grep test is the prevention mechanism. Do not weaken it with broad allowlists.
- **No new abstractions** per D-08 — single `percentileCell` utility is the entire abstraction footprint of this plan. No `MetricCell`, no `EmptyState`, no `DashboardThemeProvider`.
- **D-01 branching** — T-01's action varies by 120-DIAGNOSTIC.md Conclusion. Read that file first; do NOT assume frontend-only fix.

## Verification

| Success Criterion | Check |
|---|---|
| SC-1 (DASH-01: zero blank-tool-name rows when data exists) | `npm test -- --run BenchmarksView.tool-rollup` green; manual local check with seeded "Admin Clawdy" agent |
| SC-2 (DASH-02: null cells neutral, static-grep test rejects text-danger on null paths) | `npm test -- --run percentileCell` + `npm test -- --run static-grep-text-danger` both green |
| SC-3 (DASH-03: empty-state message renders) | `npm test -- --run BenchmarksView.empty-state` green; `grep -c "No tool spans recorded in window" BenchmarksView.tsx` ≥ 1 |
| Build sanity | `cd src/dashboard/client && npm run build && npm run lint` clean |

## Output

After completion, create `.planning/phases/120-dashboard-observability-cleanup/120-02-SUMMARY.md` recording: which 120-DIAGNOSTIC.md Conclusion branch was taken in T-01, the list of files refactored through `percentileCell`, and the static-grep test pattern shipped.
