---
phase: 17-context-summary-on-resume
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/memory/context-summary.ts
  - src/memory/context-summary.test.ts
  - src/manager/session-manager.ts
autonomous: true
requirements: [CSUM-01, CSUM-02]
---

<objective>
Add context summary persistence and session resume injection.

Purpose: When a session resumes after compaction, inject a structured summary so the agent maintains context continuity.
Output: ContextSummaryStore for persistence, SessionManager reads and injects summaries on resume.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Context summary store and session manager integration</name>
  <files>src/memory/context-summary.ts, src/memory/context-summary.test.ts, src/manager/session-manager.ts</files>
  <action>
    1. Create src/memory/context-summary.ts with:
       - ContextSummary type (id, agentName, summary, sessionId, createdAt)
       - saveSummary(dir, summary) -- writes to {dir}/context-summary.md
       - loadLatestSummary(dir) -- reads from {dir}/context-summary.md, returns string or undefined
       - truncateSummary(text, maxWords) -- keeps summary concise

    2. Create src/memory/context-summary.test.ts testing pure functions

    3. In SessionManager:
       - After compaction creates a summary, persist it via saveSummary
       - In buildSessionConfig, load latest summary and pass as contextSummary parameter
       - contextSummary already accepted by AgentSessionConfig
  </action>
</task>

</tasks>
