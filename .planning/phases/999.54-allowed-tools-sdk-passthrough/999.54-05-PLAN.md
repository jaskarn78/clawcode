---
phase: 999.54-allowed-tools-sdk-passthrough
plan: 05
title: Deploy verification — clawdy restart + live D-05a log grep + D-06 turn-1 advisor-footer confirmation
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
files_modified: []
autonomous: false
requirements: [D-05a, D-06]
gap_closure: false

must_haves:
  truths:
    - "Operator-confirmed deploy via `scripts/deploy-clawdy.sh` completes successfully (build + stage + sudo cp + restart + md5 verify) — only after explicit operator approval per `feedback_no_auto_deploy` + `feedback_ramy_active_no_deploy` memories."
    - "D-05a live grep on clawdy systemd journal returns at least one `phase999.54-resolver` log line per agent in the fleet, with `alwaysLoadServers` payload containing `[\"clawcode\"]` (or a superset)."
    - "D-06 turn-1 advisor footer landing: a fresh subagent spawn in admin-clawdy's Discord channel renders the advisor consultation footer on the FIRST assistant turn — not the second turn after a ToolSearch round-trip."
  artifacts:
    - path: ".planning/phases/999.54-allowed-tools-sdk-passthrough/999.54-05-SUMMARY.md"
      provides: "Operator confirmation log: deploy timestamp, clawdy build md5, journalctl grep output (paste 5-10 lines), Discord screenshot link OR operator-witnessed turn-1 advisor-footer confirmation."
      contains: "phase999.54-resolver"
  key_links:
    - from: "operator (human)"
      to: "scripts/deploy-clawdy.sh (build + stage + restart + md5 verify)"
      via: "explicit 'deploy' / 'ship it' confirmation in the same operator turn — per CLAUDE.md Deploy section + feedback memories"
      pattern: "scripts/deploy-clawdy.sh"
    - from: "scripts/deploy-clawdy.sh (post-deploy)"
      to: "clawdy systemd journal (`journalctl -u clawcode`)"
      via: "Plan 02 Task 2's `console.info('phase999.54-resolver', ...)` calls captured by systemd-journal"
      pattern: "phase999.54-resolver"
    - from: "admin-clawdy session (post-restart)"
      to: "Discord channel — first assistant turn after subagent spawn"
      via: "SDK turn-1 prompt now includes preloaded `clawcode` MCP server tools (no ToolSearch round-trip needed for spawn_subagent_thread / post_to_agent / ask_advisor)"
      pattern: "advisor consultation footer"
---

<objective>
The Phase 999.54 cascade is code-complete and unit-tested after Plans 01-04. The remaining two CONTEXT.md decisions (D-05a live log emission on clawdy + D-06 turn-1 advisor-footer landing) require:

1. A clawdy deploy via `scripts/deploy-clawdy.sh` — gated on explicit operator confirmation per `feedback_no_auto_deploy` + `feedback_ramy_active_no_deploy` memories.
2. An operator-witnessed test on admin-clawdy's Discord channel (spawn a fresh subagent, observe advisor consultation footer on turn 1, not turn 2).

Per CLAUDE.md Deploy section + planning_context's `<downstream_consumer>` block, this plan is marked `autonomous: false` to halt the `--auto` execution flow and request operator confirmation before proceeding. The plan contains TWO checkpoint tasks: one decision/action checkpoint to deploy, one human-verify checkpoint to confirm the turn-1 advisor-footer behavior on Discord.

This plan does NOT modify any source code — the cascade is already done. Its only purpose is to gate-and-verify the live production confirmation that the cascade actually eliminates the ToolSearch round-trip in the wild.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/999.54-allowed-tools-sdk-passthrough/999.54-CONTEXT.md
@.planning/phases/999.54-allowed-tools-sdk-passthrough/999.54-RESEARCH.md
@.planning/phases/999.54-allowed-tools-sdk-passthrough/999.54-01-PLAN.md
@.planning/phases/999.54-allowed-tools-sdk-passthrough/999.54-02-PLAN.md
@.planning/phases/999.54-allowed-tools-sdk-passthrough/999.54-03-PLAN.md
@.planning/phases/999.54-allowed-tools-sdk-passthrough/999.54-04-PLAN.md
@.planning/phases/999.54-allowed-tools-sdk-passthrough/999.54-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Operator-gated deploy decision</name>
  <decision>Deploy Phase 999.54 to clawdy via `scripts/deploy-clawdy.sh`?</decision>
  <context>
The Phase 999.54 cascade (Plans 01-04) is code-complete locally:
- 4 production source files edited (schema.ts, shared/types.ts, manager/types.ts, session-adapter.ts, loader.ts, config/types.ts)
- 4 test files extended with 11 new test cases — all green
- `npx tsc --noEmit` green; `npx vitest run` green

The remaining CONTEXT.md decisions (D-05a live log grep on clawdy, D-06 turn-1 advisor-footer landing in Discord) require a live deploy. Per CLAUDE.md Deploy section + `feedback_no_auto_deploy` + `feedback_ramy_active_no_deploy` memories, deploy requires explicit operator confirmation in the same turn ("deploy" / "ship it") AND confirmation that Ramy is not currently active in #fin-acquisition.
  </context>
  <options>
    <option id="deploy-now">
      <name>Deploy now</name>
      <pros>D-05a + D-06 verifications complete in the same session. Phase 999.54 fully shipped + verified.</pros>
      <cons>Restarts all production agents — disrupts any active client work. Ramy-active hold may block.</cons>
    </option>
    <option id="defer-deploy">
      <name>Defer deploy</name>
      <pros>Keeps the production fleet stable. Plan 01-04 commits land locally; deploy happens in a later operator-chosen window.</pros>
      <cons>D-05a and D-06 remain ⬜ pending until a separate deploy window opens. Phase 999.54 is "feature-complete local" but not "verified shipped" until then.</cons>
    </option>
  </options>
  <resume-signal>Operator types `deploy` (or `ship it`) to proceed to Task 2, OR types `defer` to skip Tasks 2-3 and mark D-05a/D-06 as deferred-pending in the SUMMARY.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute deploy + grep D-05a log line (only if Task 1 == deploy-now)</name>
  <what-built>
The Phase 999.54 cascade is built locally. Plan 02 Task 2 added a `console.info("phase999.54-resolver", JSON.stringify({agent, alwaysLoadServers}))` call site in `src/config/loader.ts` — every agent emits one log line at resolution time listing servers with `alwaysLoad: true`.

After deploy, the log line will appear in clawdy's systemd-journal capture of the `clawcode.service` unit.
  </what-built>
  <how-to-verify>
1. Run `scripts/deploy-clawdy.sh` from this workstation. The script handles: build, stage to clawdy, sudo cp, restart, md5 verify. Reads sudo pw from `~/.clawcode-deploy-pw`. Expected duration: ~60 seconds.

2. After the script exits with success, ssh to clawdy and grep the systemd journal:

```bash
ssh clawdy 'journalctl -u clawcode -n 500 | grep phase999.54-resolver | head -20'
```

3. Expected output: at least one log line per agent in the fleet (15+ agents currently). Each line looks like:

```
... phase999.54-resolver {"agent":"admin-clawdy","alwaysLoadServers":["clawcode"]}
... phase999.54-resolver {"agent":"finmentum","alwaysLoadServers":["clawcode"]}
... (etc., one per agent)
```

4. Confirm that EVERY agent in the fleet has at least one log line, and that the `alwaysLoadServers` payload contains at least `"clawcode"` (per Plan 02 Task 1's auto-inject).

5. If any agent is missing a log line, OR the payload doesn't contain `"clawcode"` for some agent: investigate — likely indicates either (a) the deploy didn't actually restart that agent (md5 mismatch, daemon stale), or (b) Plan 02 Task 1's auto-inject was bypassed by an unexpected operator-yaml override.
  </how-to-verify>
  <resume-signal>Operator types `verified` (with paste of grep output) OR `failed` (with description of what went wrong).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: D-06 turn-1 advisor-footer Discord confirmation (only if Task 2 == verified)</name>
  <what-built>
After Plan 02's auto-inject brings `alwaysLoad: true` to the `clawcode` MCP server for every agent, the SDK no longer defers `mcp__clawcode__spawn_subagent_thread`, `mcp__clawcode__post_to_agent`, `mcp__clawcode__ask_advisor`, and `mcp__clawcode__memory_lookup` behind a ToolSearch round-trip. These hot-path tools land in the turn-1 prompt directly.

Observable effect: when a fresh subagent is spawned on admin-clawdy and uses `ask_advisor`, the advisor consultation footer appears on the FIRST assistant turn — not the second turn after a ToolSearch fetch.
  </what-built>
  <how-to-verify>
1. In Discord, open admin-clawdy's channel.

2. Send a message that will trigger a fresh subagent + advisor consultation. Suggested prompt:

```
Spawn a subagent to evaluate this question: "Should we use server-level alwaysLoad or per-tool meta hints for MCP preload?" Ask the advisor before responding.
```

3. Observe admin-clawdy's response sequence. Expected behavior (post-deploy, D-06 success):
   - 💭 reaction lands on the user message that triggered the turn.
   - admin-clawdy's FIRST assistant reply already includes the advisor consultation footer: `*— consulted advisor (Opus) before responding*` (per CLAUDE.md Phase 117 advisor-pattern section).
   - There is NO intermediate "fetching tool" turn or ToolSearch round-trip turn.

4. Pre-deploy baseline (for comparison reference): the same prompt would historically show a turn-1 response that calls `ToolSearch` for `spawn_subagent_thread` / `ask_advisor`, then a turn-2 response that finally consults the advisor and renders the footer. The Phase 999.54 win is collapsing this from 2 turns to 1 turn.

5. Confirmation criteria:
   - ✅ Footer on turn 1 → D-06 PASS.
   - ❌ Footer on turn 2 with a visible ToolSearch round-trip on turn 1 → D-06 FAIL. Investigate: did the SDK actually pick up the new mcpServers entry? Check `journalctl -u clawcode | grep -i "always.*load\|preload"` for SDK-side debug evidence.

6. Optional cross-check (if 999.49 dashboard fix has landed): query traces.db for `mcp__clawcode__spawn_subagent_thread` ToolSearch call counts per session pre/post deploy — expected count is 0 after deploy.
  </how-to-verify>
  <resume-signal>Operator types `verified` (with screenshot or paste of the turn-1 advisor footer) OR `failed` (with description of where the round-trip still showed up).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workstation → clawdy production | Deploy script transports built artifacts to production host. |
| operator → live fleet | Operator confirmation gate prevents auto-deploy disrupting live client work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.54-10 | Denial of Service | premature deploy disrupting active client work (Ramy in #fin-acquisition) | mitigate | `autonomous: false` plan halts `--auto` mode; operator confirms in same turn per `feedback_ramy_active_no_deploy` memory. Task 1 explicitly surfaces the Ramy-active question. |
| T-999.54-11 | Elevation of Privilege | sudo password in deploy script | accept | `~/.clawcode-deploy-pw` chmod 600, never committed, operator-owned. Pre-existing pattern from `scripts/deploy-clawdy.sh`; no new privilege surface added by this phase. |
</threat_model>

<verification>
- Task 1 produces an explicit operator decision (deploy-now OR defer-deploy).
- If deploy-now: Task 2 confirms `journalctl | grep phase999.54-resolver` returns one line per agent with `alwaysLoadServers` payload containing `"clawcode"`.
- If deploy-now: Task 3 confirms the advisor consultation footer lands on the first assistant turn in Discord.
- If defer-deploy: SUMMARY.md documents D-05a and D-06 as `deferred-pending` with the chosen deploy window noted (or "TBD" if not yet chosen).
</verification>

<success_criteria>
All CONTEXT.md decisions verified end-to-end:
- D-01 / D-02 / D-01a / D-03 / D-03a / D-04 / OQ-3 — Plan 04 (automated tests, complete).
- D-05a (live log line) — Task 2 here (operator-witnessed grep output).
- D-06 (turn-1 advisor footer) — Task 3 here (operator-witnessed Discord interaction).

If deploy is deferred per Task 1, Plan 05 returns in a "feature-complete local, deploy-gated on operator window" state — matching the v2.9 in-flight closeout pattern (`project_v29_in_flight` memory).
</success_criteria>

<output>
After completion, create `.planning/phases/999.54-allowed-tools-sdk-passthrough/999.54-05-SUMMARY.md` documenting:
- Task 1 operator decision (deploy-now / defer).
- If deploy-now:
  - Deploy timestamp + build md5 from `scripts/deploy-clawdy.sh` output.
  - Paste of journalctl grep output (5-10 lines covering at least 3 agents).
  - D-06 Discord confirmation: screenshot URL or paste of the turn-1 footer.
- If defer-deploy:
  - D-05a + D-06 marked deferred-pending with target window noted.
  - Reference Plan 04 SUMMARY for the local feature-complete + unit-tested state.

Finally, append a Phase 999.54 closeout note to `.planning/STATE.md` (current position + status update — feature-complete local vs feature-complete deployed).
</output>
