---
phase: 999.54-allowed-tools-sdk-passthrough
plan: 03
title: NON_RELOADABLE_FIELDS documentation-of-intent for mcpServers alwaysLoad (D-04)
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/config/types.ts
autonomous: true
requirements: [D-04]
gap_closure: false

must_haves:
  truths:
    - "The field paths `defaults.mcpServers.*.alwaysLoad` and `agents.*.mcpServers.*.alwaysLoad` appear in the `NON_RELOADABLE_FIELDS` set in `src/config/types.ts` per the Phase 100 GSD-07 / Phase 115 sub-scope 2/5 documentation-of-intent precedent."
    - "A doc comment block above the entries explains: (a) the field is captured into SDK baseOptions at session-boot inside session-adapter.ts createSession / resumeSession, (b) it is NOT re-read per turn, (c) operators apply via `clawcode restart <agent>`, (d) the classifier in differ.ts:144-149 already falls through to `reloadable: false` for unclassified paths — the explicit entries are grep-target documentation, not behavior."
    - "Plan 04 Task 4 can grep / classify against these path strings as the source of truth for D-04 verification — no dangling test reference."
  artifacts:
    - path: "src/config/types.ts (NON_RELOADABLE_FIELDS set)"
      provides: "Two new path-pattern entries (`defaults.mcpServers.*.alwaysLoad` and `agents.*.mcpServers.*.alwaysLoad`) inside the existing `NON_RELOADABLE_FIELDS` set at line 191-244, appended after the Phase 115 sub-scope 5 cacheBreakpointPlacement entries (currently the last entries before the closing bracket)."
      contains: "mcpServers.*.alwaysLoad"
  key_links:
    - from: "src/config/types.ts:191-244 (NON_RELOADABLE_FIELDS set)"
      to: "src/config/differ.ts:144-149 (classifier fallthrough to reloadable:false for unclassified paths)"
      via: "documentation-of-intent — the set entries do not change classifier behavior; they document the architectural rationale for grep-ability and for Plan 04's regression test"
      pattern: "mcpServers\\.\\*\\.alwaysLoad"
---

<objective>
Add `defaults.mcpServers.*.alwaysLoad` and `agents.*.mcpServers.*.alwaysLoad` to the `NON_RELOADABLE_FIELDS` set in `src/config/types.ts`, matching the existing Phase 100 GSD-07 / Phase 115 sub-scope 2/5 documentation-of-intent precedent. Per CONTEXT.md D-04 and RESEARCH.md Pattern 3, this is grep-target documentation — the classifier in `src/config/differ.ts:144-149` already falls through to `reloadable: false` for any unclassified path, so the explicit entries do NOT change classifier behavior. They exist to:

1. Make the architectural rationale (alwaysLoad is captured into SDK baseOptions at session-boot, not re-read per turn) discoverable by `grep -rn alwaysLoad src/config/` from a maintainer's perspective.
2. Provide Plan 04 Task 4 a stable string to assert against in a regression test.
3. Match the canonical pattern established in Phase 100 (settingSources/gsd) and Phase 115 (excludeDynamicSections/cacheBreakpointPlacement) for SDK-baseOptions fields.

This plan is independent of Plan 02 (different file, no shared edits) and runs in parallel within Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/999.54-allowed-tools-sdk-passthrough/999.54-CONTEXT.md
@.planning/phases/999.54-allowed-tools-sdk-passthrough/999.54-RESEARCH.md
@.planning/phases/999.54-allowed-tools-sdk-passthrough/999.54-01-PLAN.md
@.planning/phases/999.54-allowed-tools-sdk-passthrough/999.54-VALIDATION.md

Existing precedent at src/config/types.ts:228-243 — Phase 115 sub-scope 2/5 entries. Clone the exact doc-comment + entry shape:

```typescript
  // Phase 115 sub-scope 2 — excludeDynamicSections is captured into the SDK's
  // baseOptions inside session-adapter.ts createSession / resumeSession when
  // the session is built; it is NOT re-read per turn. Same architectural
  // pattern as Phase 100's settingSources / gsd above. A clawcode.yaml edit
  // takes effect ONLY on the NEXT agent restart. Operators wanting an
  // immediate change must run: `clawcode restart <agent>`.
  "agents.*.excludeDynamicSections",
  "defaults.excludeDynamicSections",
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append mcpServers alwaysLoad paths to NON_RELOADABLE_FIELDS (D-04)</name>
  <read_first>
    - src/config/types.ts:191-244 (NON_RELOADABLE_FIELDS set — current entries + Phase 100/115 doc-comment precedent)
    - src/config/differ.ts:144-149 (classifier fallthrough — verify that unclassified paths fall through to `reloadable: false`)
    - .planning/phases/999.54-allowed-tools-sdk-passthrough/999.54-RESEARCH.md (Pattern 3, Section 6 step 8, D-04)
    - .planning/phases/999.54-allowed-tools-sdk-passthrough/999.54-CONTEXT.md (D-04)
  </read_first>
  <files>src/config/types.ts</files>
  <action>
Locate the closing bracket of the NON_RELOADABLE_FIELDS set at src/config/types.ts:244 (the line that reads exactly `]);`). Immediately BEFORE that closing line, insert a doc-comment block followed by two new entries. The block to insert (verbatim, including the leading two-space indentation matching surrounding entries):

```
  // Phase 999.54 (D-04) — mcpServers[].alwaysLoad is captured into the SDK's
  // baseOptions inside session-adapter.ts createSession / resumeSession via
  // transformMcpServersForSdk (Plan 01) when the session is built; it is NOT
  // re-read per turn. Same architectural pattern as Phase 100 GSD-07's
  // settingSources / gsd and Phase 115 sub-scope 2/5's excludeDynamicSections /
  // cacheBreakpointPlacement above. A clawcode.yaml edit takes effect ONLY on
  // the NEXT agent restart. Operators wanting an immediate change must run:
  // `clawcode restart <agent>`. The classifier in differ.ts:144-149 falls
  // through to `reloadable: false` for unclassified paths; the explicit
  // entries below match the documentation-of-intent precedent (grep target
  // for future maintainers + Plan 04 regression-test source-of-truth).
  // Phase 999.54 Plan 02 also bakes `alwaysLoad: true` into the auto-injected
  // `clawcode` server at loader.ts:295-304 — fleet-wide preload default;
  // per-agent overrides (inline-object form) win via the existing
  // resolvedMcpMap.has("clawcode") gate.
  "defaults.mcpServers.*.alwaysLoad",
  "agents.*.mcpServers.*.alwaysLoad",
```

CRITICAL constraints:
- The path-pattern strings use the same dot-and-wildcard syntax as existing entries — e.g., `agents.*.excludeDynamicSections` (line 234). The `mcpServers.*.alwaysLoad` segment uses one wildcard (`*`) at the array-index position (matches the existing `agents.*.field` precedent — `*` represents an array index OR an agent-name key for record-shape fields).
- Do NOT modify the existing entries (memoryPath, settingSources, gsd, excludeDynamicSections, cacheBreakpointPlacement).
- Do NOT modify the `RELOADABLE_FIELDS` set at lines 45-186.
- Do NOT modify `src/config/differ.ts` — the classifier already does the right thing (fallthrough to reloadable:false); this plan adds only documentation-of-intent strings.
  </action>
  <verify>
    <automated>grep -n 'defaults\.mcpServers\.\*\.alwaysLoad' src/config/types.ts && grep -n 'agents\.\*\.mcpServers\.\*\.alwaysLoad' src/config/types.ts && grep -n 'Phase 999.54 (D-04)' src/config/types.ts && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'defaults\.mcpServers\.\*\.alwaysLoad' src/config/types.ts` returns EXACTLY ONE matching line, located inside the `NON_RELOADABLE_FIELDS` set (line range 191-250).
    - `grep -n 'agents\.\*\.mcpServers\.\*\.alwaysLoad' src/config/types.ts` returns EXACTLY ONE matching line, located inside the `NON_RELOADABLE_FIELDS` set.
    - `grep -n 'Phase 999.54 (D-04)' src/config/types.ts` returns at least one matching line in the comment block immediately above the new entries.
    - Both new entries appear AFTER the Phase 115 cacheBreakpointPlacement entries (line ~242-243) and BEFORE the closing `]);` of the set — verified by `grep -n 'cacheBreakpointPlacement\|mcpServers.*alwaysLoad\|^]);$' src/config/types.ts | head -10` showing cacheBreakpointPlacement entries first, then the new mcpServers entries, then `]);`.
    - `npx tsc --noEmit` exits 0 — the set entries are plain strings, no type widening needed.
    - `npx vitest run src/config/__tests__/differ.test.ts` exits 0 — no regression in classifier behavior. (Adding entries to NON_RELOADABLE_FIELDS does not change observable classifier output for paths that already fell through to false; Plan 04 Task 4 adds the positive-direction assertion.)
    - `git diff --stat` shows ONLY `src/config/types.ts` modified (no other file touched).
  </acceptance_criteria>
  <done>
Documentation-of-intent entries in place. Plan 04 Task 4 can pin the field-path strings in a regression test against either (a) the NON_RELOADABLE_FIELDS set membership, or (b) the differ.ts classifier output for those paths. The classifier behavior is unchanged — these entries are pure grep-target documentation matching the canonical Phase 100 / Phase 115 pattern.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reload classifier → operator UX | The classifier output determines whether `clawcode reload` reports a field as live-reapplied or "requires restart." Misclassification produces operator confusion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.54-07 | Repudiation | Operator misunderstanding which fields require restart | mitigate | Documentation-of-intent strings make the architectural rationale grep-able from `src/config/types.ts`. Plan 04 Task 4 adds a positive-direction test asserting both paths classify as `reloadable: false`. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run` exits 0 — no regression.
- `grep -c 'mcpServers.\*.alwaysLoad' src/config/types.ts` returns 2 (one for defaults, one for agents).
- `git diff --stat` shows exactly 1 file modified.
</verification>

<success_criteria>
NON_RELOADABLE_FIELDS set documents the architectural intent for `mcpServers[].alwaysLoad` per the Phase 100 / Phase 115 documentation-of-intent precedent. Classifier behavior unchanged. Plan 04 Task 4 has a stable grep target for its regression assertion.
</success_criteria>

<output>
After completion, create `.planning/phases/999.54-allowed-tools-sdk-passthrough/999.54-03-SUMMARY.md` documenting:
- Byte-level diff of `src/config/types.ts` (paste the insertion block).
- Confirmation that only `src/config/types.ts` was modified.
- Confirmation that `npx tsc --noEmit` and `npx vitest run` are both green.
</output>
