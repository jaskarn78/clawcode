---
phase: 999.54
slug: allowed-tools-sdk-passthrough
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-14
---

# Phase 999.54 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (per project STACK; existing `*.test.ts` files confirm) |
| **Config file** | `vitest.config.*` (verify in Wave 0) |
| **Quick run command** | `npx vitest run src/config/__tests__/schema.test.ts src/manager/__tests__/session-adapter.test.ts` |
| **Full suite command** | `npm test` |
| **Estimated runtime** | ~30s quick · full suite varies |

---

## Sampling Rate

- **After every task commit:** `npx vitest run <affected files>` (per Test Map column "Automated Command")
- **After every plan wave:** `npm test` (full suite must pass)
- **Before `/gsd-verify-work`:** Full suite green + 1 live deploy probe on clawdy (D-06)
- **Max feedback latency:** ~30s for affected-file run

---

## Per-Task Verification Map

| Behavior (CONTEXT.md decision) | Test Type | Automated Command | File Exists | Status |
|---|---|---|---|---|
| D-01: `mcpServers[].alwaysLoad` schema-validates as optional boolean | unit | `npx vitest run src/config/__tests__/schema.test.ts -t "alwaysLoad"` | ❌ W0 | ⬜ pending |
| D-01a: `transformMcpServersForSdk` carries `alwaysLoad: true` through to SDK Options | unit | `npx vitest run src/manager/__tests__/session-adapter.test.ts -t "alwaysLoad"` | ❌ W0 (function currently unexported) | ⬜ pending |
| D-01a: `transformMcpServersForSdk` OMITS `alwaysLoad` when unset (byte-stable) | unit | same file as above, separate `it()` | ❌ W0 | ⬜ pending |
| D-03: Per-agent override wins over `defaults.mcpServers` baseline | unit | `npx vitest run src/config/__tests__/loader.test.ts -t "alwaysLoad.*override"` | ❌ W0 (verify file) | ⬜ pending |
| D-03a: Auto-inject of `clawcode` server includes `alwaysLoad: true` after merge | unit | `npx vitest run src/config/__tests__/loader.test.ts -t "auto-inject.*alwaysLoad"` | ❌ W0 | ⬜ pending |
| D-04: Field path appears in `NON_RELOADABLE_FIELDS` (or classifier falls through to non-reloadable) | unit | `npx vitest run src/config/__tests__/differ.test.ts -t "alwaysLoad.*non-reload"` | ❌ W0 (verify file) | ⬜ pending |
| D-05a: Resolver emits `pino.info` log line listing alwaysLoad servers per agent | manual | live operator grep on clawdy logs post-deploy | manual-only | ⬜ pending |
| D-06: Live preload — fresh subagent on admin-clawdy spawns advisor footer on turn 1 (no ToolSearch round-trip) | manual | spawn a subagent in Discord; count turns to first 💭-emoji | manual-only | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Confirm `vitest.config.*` exists at repo root; if not, document `npm test` invocation shape
- [ ] Verify `src/manager/__tests__/session-adapter.test.ts` exists; if not, create with describe scaffolding
- [ ] Verify `src/config/__tests__/loader.test.ts` exists; if not, create with describe scaffolding
- [ ] Verify `src/config/__tests__/differ.test.ts` exists; if not, create with describe scaffolding
- [ ] **Export `transformMcpServersForSdk`** in `src/manager/session-adapter.ts` (currently unexported — required for direct unit test coverage per RESEARCH.md Pitfall 2)

---

## Manual-Only Verifications

| Behavior | Why Manual | Test Instructions |
|---|---|---|
| Resolver log line (D-05a) | Pino logs land on clawdy systemd journal; orchestrator can't tail prod | After deploy, `ssh clawdy 'journalctl -u clawcode -n 200 \| grep alwaysLoad'` — confirm one line per agent listing preload-marked servers |
| Live preload (D-06) | The whole point of the phase — proves the SDK actually preloads the server's tools rather than deferring them behind ToolSearch | (a) `scripts/deploy-clawdy.sh` (operator-confirmed only); (b) in Discord, spawn a subagent via admin-clawdy fresh session; (c) confirm advisor consultation footer arrives on the *first* assistant turn, not after a deferred ToolSearch turn |
| traces.db ToolSearch counter delta | Depends on Phase 999.49 dashboard fix (BACKLOG Related §1) — not blocking, but the dashboard cross-check is the objective metric once 999.49 lands | After 999.49: query traces.db for `mcp__clawcode__spawn_subagent_thread` ToolSearch call counts per session pre/post deploy — expect 0 after |

---

## Risks to Validation

1. **Manual-only D-06 verification is deploy-gated.** Operator confirmation required for clawdy deploy (per `feedback_no_auto_deploy` + `feedback_ramy_active_no_deploy` memories). Plan should NOT auto-deploy.
2. **OQ-1 (`clawcode-broker-discord` doesn't exist):** D-03a target is a phantom. Plan must drop the broker-discord half of D-03a from the landing PR, or operator must point at a real broker server name. Auto-inject site for `clawcode` (Pitfall 1) is the actual leverage point.
3. **OQ-3 (operator-override behavior):** If the auto-inject site hard-codes `alwaysLoad: true`, an operator who later sets `agents.<name>.mcpServers.clawcode.alwaysLoad: false` in yaml may expect it to override the default. Plan must preserve the existing merge order so the per-agent override wins.

---

## Validation Sign-Off

- [ ] All planned tasks have an automated `<verify>` command or a Wave 0 dependency
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify (D-05a and D-06 manual-only entries spread across waves)
- [ ] Wave 0 covers all ❌ MISSING test-file references above
- [ ] No watch-mode flags (`vitest run`, not `vitest --watch`)
- [ ] Feedback latency for affected-file run < 30s
- [ ] `nyquist_compliant: true` set in frontmatter after all unit-test rows go ✅

**Approval:** pending
