# Phase 999.54 — allowed-tools SDK passthrough (actually: `mcpServers[].alwaysLoad`) — Phase Summary

**Status:** Code-complete across Plans 01-04; merged to master + post-deploy hotfix-and-revert cycle resolved (Anthropic API outage misattributed). Plan 05 is operator-gated production verification + does NOT modify source code.
**Phase window:** 2026-05-14 (Plans 01-04 + post-deploy hotfix + revert all landed within 24h).
**Naming note:** Phase dir is `999.54-allowed-tools-sdk-passthrough` (from original BACKLOG-proposed `allowedTools` design). Plan 01 research (D-01) determined SDK 0.2.140's `Options.allowedTools` field has different semantics (auto-allow without permission prompt, not preload). Phase pivoted to `mcpServers[].alwaysLoad: true` SDK passthrough — server-level preload with cleaner ergonomics than per-tool `_meta` injection. Subsequent commits all `999.54-XX … alwaysLoad …`.

## Plans

| Plan | Subject | Commits | Status |
|------|---------|---------|--------|
| 999.54-01 | Schema + type cascade — `alwaysLoad: boolean` in `mcpServerSchema`, `ResolvedAgentConfig.mcpServers[]`, `AgentSessionConfig.mcpServers[]`; export + widen `transformMcpServersForSdk` for passthrough | `f55ac40` `f33a8a1` `f58042b` `953d244` `a5b84a5` | Merged |
| 999.54-02 | Loader cascade — propagate yaml-declared `alwaysLoad` + auto-inject `alwaysLoad: true` on `clawcode` MCP server (D-03a) + emit `console.info("phase999.54-resolver", …)` log line per agent | `e40537b` `53b79f7` `289736d` `d272c99` | Merged |
| 999.54-03 | Document `mcpServers.alwaysLoad` in `NON_RELOADABLE_FIELDS` (D-04 doc-of-intent) | `4776695` `0db923f` `c33d180` | Merged |
| 999.54-04 | Regression test cascade — schema, transform, loader auto-inject + override, NON_RELOADABLE classification, byte-stable preservation via `hasOwnProperty` | `1b228dc` `19a214a` `d35863f` `ce79220` `8b2c40a` `57366a5` `86540ee` | Merged; first-ever direct test coverage for `transformMcpServersForSdk` |
| 999.54-05 | **DEPLOY-GATED** — operator-confirmed `scripts/deploy-clawdy.sh` + journalctl grep `phase999.54-resolver` + Discord turn-1 advisor-footer landing confirmation (no source code changes) | (none — operator action) | Pending operator deploy window |

## Post-deploy hotfix cycle (2026-05-15)

- **Hotfix `a4f794f`** — disabled `clawcode` `alwaysLoad` auto-inject after a post-deploy agent-hang on clawdy.
- **Revert `090332d`** — restored auto-inject. Post-deploy investigation determined the hang was an **Anthropic API outage**, not a 999.54 regression. Phase 02 design intact.

## Success Criteria — verification status

| SC | Description | Status | Evidence |
|----|-------------|--------|----------|
| D-01 | Schema accepts `alwaysLoad: boolean` per `mcpServerSchema` entry | ✅ **Code-complete** | `schema.test.ts` 3 new it cases pin behavior |
| D-01a | `transformMcpServersForSdk` widens passthrough (was stripping everything except `{command, args, env}`) | ✅ **Code-complete** | First direct test added in Plan 04 |
| D-02 | Field named `alwaysLoad` (1:1 with SDK), boolean only — rejected `allowedTools`/`preloadedTools` names | ✅ **Locked in schema** | Schema-level enforcement |
| D-03 | Both `defaults.mcpServers[].alwaysLoad` and per-agent override accepted via existing merge mechanism | ✅ **Code-complete** | Loader cascade test pins both paths |
| D-03a | `clawcode` MCP server auto-flagged `alwaysLoad: true` at the `defaults.mcpServers` level | ✅ **Code-complete + post-deploy survived revert** | Auto-inject site at loader; `clawcode-broker-discord` excluded per scoped policy (operator chose to focus on `clawcode` only initially) |
| D-04 | NON-reloadable classification (SDK session-boot baseOptions) | ✅ **Code-complete** | `NON_RELOADABLE_FIELDS` doc-of-intent + classifier test pin |
| D-05a | Resolver emits structured log line listing servers marked `alwaysLoad: true` per agent | ✅ **Code-complete** | `console.info("phase999.54-resolver", JSON.stringify({...}))` matching Phase 115 quickwin precedent |
| D-06 | Live verification on admin-clawdy — turn-1 advisor footer (no ToolSearch round-trip) | ⏳ **Deploy-gated (Plan 05)** | Operator action: spawn fresh subagent post-deploy, observe 💭 + footer on turn 1 |

## Outstanding operator actions (Plan 05)

1. **Deploy clearance** — `scripts/deploy-clawdy.sh` build + stage + sudo cp + restart + md5 verify. Per `feedback_no_auto_deploy` + `feedback_ramy_active_no_deploy`, gated on explicit operator "deploy" / "ship it" in same turn AND Ramy-quiet window.
2. **D-05a journalctl grep:**
   ```bash
   ssh clawdy "journalctl -u clawcode --since '5 min ago' -g 'phase999.54-resolver'"
   ```
   Expect at least one line per agent in fleet with `alwaysLoadServers` payload containing `["clawcode"]` (or superset).
3. **D-06 turn-1 advisor-footer test** — in admin-clawdy's Discord channel, spawn a fresh subagent and verify the advisor consultation footer renders on the FIRST assistant turn (not turn 2 after ToolSearch round-trip).
4. **Cross-check via `traces.db`:** count `ToolSearch` calls for `mcp__clawcode__spawn_subagent_thread` per session before/after deploy — expectation is 0 after preload.

## Architectural note

This phase is the SDK passthrough plumbing for one specific server preload (`clawcode`). It does NOT generalize to per-tool allowlists (rejected per D-01 + D-02a — that would require a custom SDK shim). Future per-tool granularity work would be a separate phase.

## Net

- 4/5 plans code-complete and merged (Plans 01-04).
- 1/5 plans operator-deploy-gated (Plan 05 — no source changes, pure verification).
- Post-deploy hotfix cycle resolved cleanly (false-alarm revert).
- Test coverage closes RESEARCH.md Pitfalls 2 + 3.

Phase 999.54 closes cleanly when operator captures Plan 05's deploy + journalctl + turn-1 footer evidence into `999.54-05-SUMMARY.md`.
