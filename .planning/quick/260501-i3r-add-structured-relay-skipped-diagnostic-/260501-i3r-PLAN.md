---
phase: 260501-i3r-add-structured-relay-skipped-diagnostic-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/discord/subagent-thread-spawner.ts
autonomous: true
requirements:
  - QUICK-260501-i3r
must_haves:
  truths:
    - "When relayCompletionToParent silently returns, a structured log line is emitted identifying which of the five branches fired"
    - "All five silent-return points (no-turn-dispatcher, no-binding, no-channel-or-not-text, no-bot-messages, empty-content-after-concat) emit distinct reason tags"
    - "Existing relay logic, control flow, happy-path success log, and outer try/catch are unchanged"
    - "TypeScript compilation passes and existing test suite still passes"
  artifacts:
    - path: "src/discord/subagent-thread-spawner.ts"
      provides: "relayCompletionToParent with diagnostic logs at every silent-return site"
      contains: 'reason: "no-turn-dispatcher"'
  key_links:
    - from: "relayCompletionToParent silent-return points"
      to: "this.log.info"
      via: "structured log call before each return"
      pattern: 'this\.log\.info\(\{[^}]*reason:\s*"(no-turn-dispatcher|no-binding|no-channel-or-not-text|no-bot-messages|empty-content-after-concat)"'
---

<objective>
Add structured diagnostic logs at the five silent-return points inside `relayCompletionToParent` in `src/discord/subagent-thread-spawner.ts` so the next time a subagent completion summary fails to land in the parent's main channel, the logs reveal which branch aborted the relay.

Purpose: Pure observability — current silent returns make root-cause analysis impossible.
Output: One modified file with five new `this.log.info(...)` calls; no logic changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@src/discord/subagent-thread-spawner.ts

<interfaces>
<!-- Confirmed via Read on src/discord/subagent-thread-spawner.ts (lines 200-250). -->
<!-- The five silent-return sites in `relayCompletionToParent`: -->

L201 signature: `async relayCompletionToParent(threadId: string): Promise<void>`

Site 1 — L202 (BEFORE the try block):
  `if (!this.turnDispatcher) return;`
  → reason: "no-turn-dispatcher"
  → log fields: { threadId, reason: "no-turn-dispatcher" }

Site 2 — L206 (inside try, after readThreadRegistry/getBindingForThread):
  `if (!binding) return;`
  → reason: "no-binding"
  → log fields: { threadId, reason: "no-binding" }

Site 3 — L208 (inside try, after channels.fetch):
  `if (!channel || !("messages" in channel)) return;`
  → reason: "no-channel-or-not-text"
  → log fields: { threadId, reason: "no-channel-or-not-text" }

Site 4 — L242 (inside try, after walking subagentChunks):
  `if (subagentChunks.length === 0) return;`
  → reason: "no-bot-messages"
  → log fields: { threadId, reason: "no-bot-messages" }

Site 5 — L245 (inside try, after concat+trim):
  `if (!fullSubagentReply) return;`
  → reason: "empty-content-after-concat"
  → log fields: { threadId, reason: "empty-content-after-concat" }

Existing logger pattern at L290-298 (DO NOT modify):
```typescript
this.log.info(
  { threadId, parentAgent, subagentSession, relayLen, artifactCount },
  "subagent completion relayed to parent",
);
```

Existing outer try/catch at L300-305 (DO NOT modify) catches genuine errors.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add five structured relay-skipped diagnostic logs</name>
  <files>src/discord/subagent-thread-spawner.ts</files>
  <action>
    In `relayCompletionToParent` (starts at L201), insert exactly one `this.log.info(...)` call IMMEDIATELY BEFORE each of the five silent `return;` statements. Do NOT change any condition, control-flow, message-fetch logic, the happy-path log at L290-298, or the outer try/catch at L300-305.

    The five insertions, in order:

    1. Before L202 `if (!this.turnDispatcher) return;` — replace the single-line guard with a block:
       ```typescript
       if (!this.turnDispatcher) {
         this.log.info({ threadId, reason: "no-turn-dispatcher" }, "subagent relay skipped");
         return;
       }
       ```

    2. Before L206 `if (!binding) return;` — replace with:
       ```typescript
       if (!binding) {
         this.log.info({ threadId, reason: "no-binding" }, "subagent relay skipped");
         return;
       }
       ```

    3. Before L208 `if (!channel || !("messages" in channel)) return;` — replace with:
       ```typescript
       if (!channel || !("messages" in channel)) {
         this.log.info({ threadId, reason: "no-channel-or-not-text" }, "subagent relay skipped");
         return;
       }
       ```

    4. Before L242 `if (subagentChunks.length === 0) return;` — replace with:
       ```typescript
       if (subagentChunks.length === 0) {
         this.log.info({ threadId, reason: "no-bot-messages" }, "subagent relay skipped");
         return;
       }
       ```

    5. Before L245 `if (!fullSubagentReply) return;` — replace with:
       ```typescript
       if (!fullSubagentReply) {
         this.log.info({ threadId, reason: "empty-content-after-concat" }, "subagent relay skipped");
         return;
       }
       ```

    Constraints (from task_detail and project coding-style.md):
    - Pure observability — DO NOT touch the relay logic, conditions, message-fetch, or the prompt construction.
    - All five log calls use the SAME message string: `"subagent relay skipped"`.
    - Reason tags must be EXACTLY the strings listed above (kebab-case, double-quoted) so log queries are stable.
    - `threadId` is the function parameter and is in scope at all five sites.
    - Do NOT add comments — the field shape is self-explanatory and CLAUDE.md says no comments unless WHY is non-obvious.
    - Keep immutability: no mutation of any object, just adding log calls.
  </action>
  <verify>
    <automated>cd /home/jjagpal/.openclaw/workspace-coding && npx tsc --noEmit -p tsconfig.json 2>&1 | head -20 && grep -c 'reason: "' src/discord/subagent-thread-spawner.ts</automated>
  </verify>
  <done>
    File `src/discord/subagent-thread-spawner.ts` contains exactly five new `this.log.info({ threadId, reason: "..." }, "subagent relay skipped")` calls — one immediately before each of the five silent returns at the original L202/L206/L208/L242/L245 sites. The five reason values are: `"no-turn-dispatcher"`, `"no-binding"`, `"no-channel-or-not-text"`, `"no-bot-messages"`, `"empty-content-after-concat"`. No other lines in the function are changed. TypeScript compiles without new errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run existing test suite to confirm no regressions</name>
  <files>src/discord/subagent-thread-spawner.test.ts</files>
  <action>
    Run the existing subagent-thread-spawner test file to confirm the new log calls did not break any behavior. The tests should still pass — log additions are pure observability and do not change return values, control flow, or side effects.

    Command: `npx vitest run src/discord/subagent-thread-spawner.test.ts` (or `bun test` if the project uses Bun's test runner — check package.json scripts).

    If any test fails, the failure must be due to a logic change you accidentally introduced (the task is supposed to be log-only). Revert and re-apply Task 1 carefully.

    Do NOT add new tests — task_detail explicitly says "the existing test suite passing is sufficient" for this small task.
  </action>
  <verify>
    <automated>cd /home/jjagpal/.openclaw/workspace-coding && npx vitest run src/discord/subagent-thread-spawner.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    `subagent-thread-spawner.test.ts` passes with zero failures. No new tests added.
  </done>
</task>

</tasks>

<verification>
Combined check:
1. `grep -n 'subagent relay skipped' src/discord/subagent-thread-spawner.ts` returns exactly 5 lines.
2. `grep -n 'reason: "no-turn-dispatcher"\|reason: "no-binding"\|reason: "no-channel-or-not-text"\|reason: "no-bot-messages"\|reason: "empty-content-after-concat"' src/discord/subagent-thread-spawner.ts` returns 5 lines (one per reason).
3. `npx tsc --noEmit` exits 0.
4. `npx vitest run src/discord/subagent-thread-spawner.test.ts` exits 0.
5. The happy-path log at the original L290 (`"subagent completion relayed to parent"`) is unchanged — `grep -c 'subagent completion relayed to parent' src/discord/subagent-thread-spawner.ts` returns 1.
6. The outer warn log (`"subagent completion relay failed"`) is unchanged — `grep -c 'subagent completion relay failed' src/discord/subagent-thread-spawner.ts` returns 1.
</verification>

<success_criteria>
- Five new structured `this.log.info` calls present, one per silent-return site, with the exact reason tags specified.
- Zero changes to relay logic, conditions, message-fetch, prompt construction, happy-path log, or outer try/catch.
- TypeScript compiles cleanly.
- Existing test suite for `subagent-thread-spawner` passes.
- Next time the relay drops, ops can grep `"subagent relay skipped"` and see exactly which branch fired.
</success_criteria>

<output>
After completion, create `.planning/quick/260501-i3r-add-structured-relay-skipped-diagnostic-/260501-i3r-01-SUMMARY.md` documenting:
- The five log lines added (with line numbers in the post-edit file)
- Test run result
- Confirmation that no logic changed
</output>
