---
quick_id: 260511-pw2
slug: investigate-post-to-agent-silent-drops-b
date: 2026-05-11
description: Investigate post_to_agent silent drops between top-level agents — add delivery telemetry + structured logging
status: pending
must_haves:
  - Identify the silent-drop failure mode (which code path drops the message + under what condition)
  - Add structured `post-to-agent-skipped` reason-tagged log at every silent-return point (mirror Phase 999.18 `subagent relay skipped` substrate)
  - If a clean root-cause fix is small (< 50 LOC), ship it. Otherwise document and ship only the diagnostic logging
  - Add a sentinel test that pins the production caller chain (anti-pattern: silent path bifurcation)
---

# Quick 260511-pw2 — post_to_agent silent drops

## Context

Observed 2026-05-11 in Discord exchange between Admin Clawdy and Projects agent. Admin Clawdy used `post_to_agent` to deliver a bug report to Projects. Projects never received it. No error surfaced to operator. Symptom: Admin Clawdy reported `queue id pbqzxbJtwphaXlY4aBk31`; Projects' `task_status` returned "not found" because that ID was a post-id (different system from `delegate_task` queue).

Admin Clawdy flagged: *"post_to_agent deliveries may be silently dropping. Worth investigating."*

This is the **top-level → top-level** delivery path, NOT the subagent-relay path that Phase 999.18 + quick `260501-i3r`/`260501-nfe` addressed. Same class of bug ("messages disappear into the system without error"), different surface.

## Investigation tasks

1. Map the `post_to_agent` IPC pipeline end-to-end:
   - Find the IPC method handler in `src/manager/daemon.ts` (likely renamed via Phase 999.2 to `post_to_agent`)
   - Trace the delivery path: sender agent → daemon IPC → recipient agent's input queue → recipient processes
   - Identify every `return` or silent `catch` along the way
2. Compare against Phase 999.18 / quick `260501-i3r` substrate — `relayCompletionToParent` adds structured logs at 5 silent-return points. Same pattern needed here.
3. Check whether `post_to_agent` writes to an inbox that the recipient's main loop actually reads
4. Identify the wrong-path / silent-drop branch

## Implementation tasks

1. Add structured `post-to-agent-skipped` log lines at every silent-return point, with `reason` tag (e.g., `no-recipient-channel`, `recipient-not-running`, `inbox-write-failed`, `cap-exceeded`)
2. Surface delivery status to the sender's tool result: `{ ok: false, reason: "..." }` instead of fire-and-forget when the daemon can detect failure
3. Add a sentinel test that pins the production caller chain (assert `post_to_agent` IPC handler is reachable from `daemon.ts` production entrypoint — anti-pattern guard)
4. If root cause emerges and fix is < 50 LOC, ship it. Otherwise diagnostic-only and document fix proposal

## Constraints

- **NO DEPLOY** — code-only commits, all local
- **AVOID file conflict with Plan 116-00 executor (currently running):** do NOT touch `src/dashboard/server.ts`, `src/dashboard/client/`, `package.json`, `src/performance/slos.ts`, or NEW fields in `src/config/schema.ts`. Pre-existing `schema.ts` IPC-related changes are OK.
- Reference the silent-path-bifurcation anti-pattern (see `~/.claude/projects/-home-jjagpal--openclaw-workspace-coding/memory/feedback_silent_path_bifurcation.md`) when designing the sentinel test
