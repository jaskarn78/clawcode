---
milestone: v2.5
audited: 2026-04-25
status: tech_debt
scores:
  requirements: 10/10
  phases: 2/2
  integration: passed
  flows: passed
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 92-openclaw-clawcode-fin-acquisition-cutover-parity-verifier
    items:
      - "D-12: fin-acquisition has no discrete OpenClaw source identity in MC API. Verifier infrastructure ships green and is reusable for future agent migrations, but is moot against fin-acquisition itself. Documented in 92-CONTEXT.md."
      - "Wiring gap: src/cutover/verify-pipeline.ts only calls discord-ingestor; mc-history-ingestor was implemented + tested in Plan 92-01 but not threaded through the production pipeline. Phase 92.1 follow-up if a future agent migration needs MC enrichment."
      - "Phase 92's CLI cutover-verify + cutover-rollback initially shipped as exit-1 stubs (Plan 92-06 deferred); gap-closed via dedicated commits (8bb5c5f → 18c05a9 trail). Lesson: capstone plans that depend on daemon IPC should explicitly include the IPC handler registration as a task, not relegate to follow-up."
      - "send_attachment MCP tool emits false 'Missing Access' even when bot has full perms — direct Discord REST works. ClawCode harness/MCP bug. Documented in .planning/debug/discord-send-attachment-mcp-false-missing-access.md. Out of v2.5 scope."
  - phase: 93-status-detail-parity-clawhub-public-catalog-defaults-plugin-manifest-url-resilience
    items: []
nyquist:
  compliant_phases: []
  partial_phases: []
  missing_phases: [92, 93]
  overall: not_applicable
  note: "Both phases shipped without RESEARCH.md (--skip-research used); no VALIDATION.md required by workflow rule (research_enabled=false + has_research=false → Nyquist not applicable)."
---

# v2.5 Cutover Parity Verification — Milestone Audit

**Audit date:** 2026-04-25
**Status:** `tech_debt` (no blockers; documented findings need acknowledgment)
**Phases shipped:** 2/2 (Phase 92 + Phase 93)

## Scope

| Phase | Title | Verification | Tests | Plans |
|-------|-------|--------------|-------|-------|
| 92 | OpenClaw → ClawCode fin-acquisition Cutover Parity Verifier | passed | 134 | 6/6 |
| 93 | Status-detail parity + ClawHub public-catalog defaults + plugin manifest-URL resilience | passed | (per phase) | 3/3 |

## Requirements Coverage

All 10 CUT-01..CUT-10 requirements satisfied per phase 92 VERIFICATION.md gap-closure cycle. Phase 93 used internal sub-plan IDs (no formal REQ-IDs — added outside the milestone requirements process).

| Source | Mapped | Verified | Status |
|--------|--------|----------|--------|
| ROADMAP.md Phase 92 reqs | CUT-01..CUT-10 | 10/10 | satisfied |
| Phase 92 VERIFICATION.md | 11 must_haves checked | 11/11 (post gap-closure) | passed |
| SUMMARY.md frontmatter | requirements_addressed lists | aligned | aligned |

No orphaned requirements. No unsatisfied requirements.

## Cross-Phase Integration

Phase 92 and Phase 93 modify disjoint code surfaces:

- **Phase 92:** `src/cutover/**`, new `cutover-*` CLI subcommands, IPC methods `cutover-verify` / `cutover-rollback` / `cutover-button-action`, customId namespace `cutover-`
- **Phase 93:** `src/discord/status-render.ts`, `src/marketplace/catalog.ts`, `src/marketplace/clawhub-client.ts` (manifest-404 distinction)

No integration risk between the two. Phase 91 sync-set-authoritative.ts received an additive precondition gate from Phase 92 (verified by 13/13 sync-set-authoritative tests still passing post-Phase 92).

## End-to-End Flows

**Phase 92 verifier flow (verified live on clawdy):**
1. `clawcode cutover verify --agent fin-acquisition` — connects to daemon IPC ✓
2. Daemon runs verify-pipeline (ingest → profile → probe → diff → apply-additive → canary → write report) ✓
3. Returns `{cutoverReady, gapCount, canaryPassRate, reportPath}` ✓
4. Operator gets `Cutover ready: false` (correct — no source corpus to verify against per D-12) ✓

**Phase 93 (UX fixes):** verified per-plan via VERIFICATION.md.

## Tech Debt (Acknowledged, Non-Blocking)

See frontmatter `tech_debt` block. Notable items:

1. **D-12 finding (Phase 92):** fin-acquisition has no discrete OpenClaw identity. Documented; cutover surface for fin-acquisition is much smaller than originally scoped (single modelByChannel swap, not a full content/skill migration). Verifier infrastructure ships clean and reusable for future migrations.
2. **send_attachment MCP bug:** discovered during operator's own diagnostics. Out of v2.5 scope; tracked in `.planning/debug/discord-send-attachment-mcp-false-missing-access.md`.
3. **MC ingestor not in verify-pipeline:** Plan 92-01 shipped + tested mc-history-ingestor.ts independently, but Plan 92-06's verify-pipeline.ts wires only discord-ingestor. Coincidentally aligned with D-12 finding (no MC agent for fin-acquisition anyway). Phase 92.1 follow-up if needed.

## Nyquist Compliance

`workflow.nyquist_validation` is enabled but neither Phase 92 nor 93 created RESEARCH.md (both used `--skip-research`). Per workflow rule, this also skips VALIDATION.md creation. Status: `not_applicable` for v2.5.

## Disposition

**Recommend:** complete milestone. The single semi-blocker (D-12 finding) is itself the answer — the operator-facing cutover for fin-acquisition is a 1-line modelByChannel edit + gateway restart, not a content migration. Phase 92 infrastructure is reusable for future per-agent migrations where the source IS a discrete OpenClaw entity (`personal`, `projects`, `work`, `general`, `shopping`, `research`, `kimi`, `local-clawdy`).
