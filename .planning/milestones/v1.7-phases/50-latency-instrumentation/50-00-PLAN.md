---
phase: 50-latency-instrumentation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/performance/__tests__/trace-collector.test.ts
  - src/performance/__tests__/trace-store.test.ts
  - src/performance/__tests__/trace-store-persistence.test.ts
  - src/performance/__tests__/percentiles.test.ts
  - src/cli/commands/__tests__/latency.test.ts
  - src/heartbeat/checks/__tests__/trace-retention.test.ts
  - src/dashboard/__tests__/server.test.ts
  - src/discord/__tests__/bridge.test.ts
  - src/manager/__tests__/session-adapter.test.ts
  - src/manager/__tests__/context-assembler.test.ts
  - src/scheduler/__tests__/scheduler.test.ts
autonomous: true
requirements: [PERF-01, PERF-02]
must_haves:
  truths:
    - "All Wave 0 test files exist and fail with 'module not found' errors (red state)"
    - "Every automated command referenced in 50-VALIDATION.md has a matching test file"
    - "Every test describes the expected behavior that downstream waves must implement"
    - "Daemon-restart persistence has a dedicated integration test (close store → reopen on same path → read back)"
    - "Existing non-tracing tests in src/dashboard/__tests__/server.test.ts still pass after Wave 0 edits; only the new latency-block tests fail with unresolved imports"
  artifacts:
    - path: "src/performance/__tests__/trace-collector.test.ts"
      provides: "Red tests for TraceCollector startTurn/startSpan/end/writeTurn"
    - path: "src/performance/__tests__/trace-store.test.ts"
      provides: "Red tests for TraceStore pragmas, schema, batched writeTurn, pruneOlderThan with CASCADE"
    - path: "src/performance/__tests__/trace-store-persistence.test.ts"
      provides: "Red integration test — write via TraceStore on path → close → open NEW TraceStore on same path → read back rows, asserting counts match (daemon-restart semantic for success criterion #4)"
    - path: "src/performance/__tests__/percentiles.test.ts"
      provides: "Red tests for percentile SQL math + since-duration parser (1h/6h/24h/7d)"
    - path: "src/cli/commands/__tests__/latency.test.ts"
      provides: "Red tests for clawcode latency table formatter, --json, --all, --since flags"
    - path: "src/heartbeat/checks/__tests__/trace-retention.test.ts"
      provides: "Red tests for retention heartbeat CheckModule"
    - path: "src/dashboard/__tests__/server.test.ts"
      provides: "Red tests for GET /api/agents/:name/latency route (new block appended; existing tests untouched)"
    - path: "src/discord/__tests__/bridge.test.ts"
      provides: "Red tests for Discord handleMessage receive span + end_to_end turn lifecycle (matches -t 'receive span' and -t 'end_to_end' filters)"
    - path: "src/manager/__tests__/session-adapter.test.ts"
      provides: "Red tests for first_token / tool_call / subagent filter (matches -t filters in 50-VALIDATION.md)"
    - path: "src/manager/__tests__/context-assembler.test.ts"
      provides: "Red tests APPENDED for context_assemble / tracing block (matches -t 'tracing' filter); existing tests untouched"
    - path: "src/scheduler/__tests__/scheduler.test.ts"
      provides: "Red tests APPENDED for scheduler: turnId prefix (matches -t 'trace' filter); existing tests untouched"
  key_links:
    - from: "Wave 0 tests"
      to: "Wave 1/2/3 implementations"
      via: "Named module imports that will resolve once Wave 1/2 create files"
      pattern: "from \"../(trace-collector|trace-store|percentiles|bridge|session-adapter)\\.js\""
---

<objective>
Create all test scaffolding referenced in 50-VALIDATION.md so downstream waves can follow strict RED -> GREEN -> REFACTOR. Every test file and every exact `-t` filter referenced by a Wave 1/2/3 <verify> command has a scaffolded, failing test in place BEFORE implementation begins.

Purpose: Nyquist-compliant feedback loop — every task in later waves must have an automated <verify> command that already exists.
Output: 10 test files (new + extended) in red state (imports unresolved OR test names present but implementation missing), serving as interface contracts for later waves.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-latency-instrumentation/50-CONTEXT.md
@.planning/phases/50-latency-instrumentation/50-RESEARCH.md
@.planning/phases/50-latency-instrumentation/50-VALIDATION.md
@.planning/codebase/CONVENTIONS.md

# Reference implementations to mirror
@src/usage/tracker.test.ts
@src/cli/commands/costs.test.ts
@src/heartbeat/checks/__tests__/attachment-cleanup.test.ts
@src/manager/__tests__/context-assembler.test.ts
@src/scheduler/__tests__/scheduler.test.ts

<interfaces>
<!-- Contracts Wave 0 tests will reference (Wave 1/2 create source files to satisfy these imports) -->

From `src/performance/types.ts` (created in Plan 01):
```typescript
export type SpanName =
  | "receive"
  | "context_assemble"
  | "first_token"
  | "end_to_end"
  | `tool_call.${string}`;

export type TurnStatus = "success" | "error";

export type SpanRecord = {
  readonly name: string;
  readonly startedAt: string;           // ISO 8601
  readonly durationMs: number;
  readonly metadata: Readonly<Record<string, unknown>>;
};

export type TurnRecord = {
  readonly id: string;
  readonly agent: string;
  readonly channelId: string | null;
  readonly startedAt: string;           // ISO 8601
  readonly endedAt: string;             // ISO 8601
  readonly totalMs: number;
  readonly status: TurnStatus;
  readonly spans: readonly SpanRecord[];
};

export type PercentileRow = {
  readonly segment: "end_to_end" | "first_token" | "context_assemble" | "tool_call";
  readonly p50: number | null;
  readonly p95: number | null;
  readonly p99: number | null;
  readonly count: number;
};

export type LatencyReport = {
  readonly agent: string;
  readonly since: string;               // ISO 8601
  readonly segments: readonly PercentileRow[];
};
```

From `src/performance/trace-collector.ts` (created in Plan 01):
```typescript
export class TraceCollector {
  constructor(store: TraceStore, log: Logger);
  startTurn(turnId: string, agent: string, channelId: string | null): Turn;
}

export class Turn {
  startSpan(name: string, metadata?: Record<string, unknown>): Span;
  end(status: TurnStatus): void;
}

export class Span {
  end(): void;
}
```

From `src/performance/trace-store.ts` (created in Plan 01):
```typescript
export class TraceStore {
  constructor(dbPath: string);
  writeTurn(turn: TurnRecord): void;
  pruneOlderThan(cutoffIso: string): number;   // returns rows deleted from traces
  close(): void;
  // Percentile query runs inside the store:
  getPercentiles(agent: string, sinceIso: string): readonly PercentileRow[];
}
```

From `src/performance/percentiles.ts` (created in Plan 01):
```typescript
export function parseSinceDuration(input: string): number;  // returns ms
export function sinceToIso(input: string, now?: Date): string;
```

From `src/heartbeat/types.ts` (EXISTS — do not modify):
```typescript
export type CheckModule = {
  readonly name: string;
  execute(context: CheckContext): Promise<CheckResult>;
};
```

From `src/manager/session-adapter.ts` (Wave 2 will extend — Wave 0 sentinel import must match):
```typescript
// Wave 2 adds optional `turn?: Turn` parameters. Wave 0 import sentinel is the named export `createSdkSessionHandle`.
export function createSdkSessionHandle(...): SessionHandle;
```

From `src/discord/bridge.ts` (Wave 2 will add turn.startSpan("receive") — Wave 0 sentinel is the exported class):
```typescript
export class DiscordBridge { ... }
```

From `src/scheduler/scheduler.ts` (Wave 2 will add turnId prefix — Wave 0 sentinel is the exported Scheduler class):
```typescript
export class Scheduler { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold performance/ subsystem tests (trace-collector, trace-store, trace-store-persistence, percentiles)</name>
  <files>src/performance/__tests__/trace-collector.test.ts, src/performance/__tests__/trace-store.test.ts, src/performance/__tests__/trace-store-persistence.test.ts, src/performance/__tests__/percentiles.test.ts</files>
  <read_first>
    - src/usage/tracker.test.ts (mirror this SQLite-per-agent test pattern: tmpdir, fresh Database instance per test, teardown with rm -rf)
    - src/usage/tracker.ts (constructor signature precedent)
    - .planning/phases/50-latency-instrumentation/50-VALIDATION.md (per-task verification map rows 50-00-01, 50-00-02, 50-00-03)
    - .planning/phases/50-latency-instrumentation/50-RESEARCH.md (code examples section for TraceCollector and percentile SQL)
    - .planning/codebase/CONVENTIONS.md (readonly, Object.freeze, .js extension on imports)
  </read_first>
  <action>
Create four test files in `src/performance/__tests__/`. Use `import { describe, it, expect, beforeEach, afterEach, vi } from "vitest"`. Import source modules with `.js` extension (e.g., `from "../trace-collector.js"`) — imports WILL fail until Plan 01 lands; that is the RED state.

**File 1: `src/performance/__tests__/trace-collector.test.ts`** — exact test names required (referenced by -t filters downstream):
- `describe("TraceCollector")`
  - `it("startTurn returns a Turn with the given id and agent")` — calls `collector.startTurn("msg-123", "alpha", "channel-1")`, asserts returned object has those fields exposed via a getter or readable property.
  - `it("Turn.startSpan returns a Span object")` — assert it is an object with `end()` method.
  - `it("span.end() records a duration_ms > 0")` — start span, sleep 2ms via `await new Promise(r => setTimeout(r, 2))`, end, inspect recorded span (expose via a test-only inspector or via the store mock).
  - `it("turn.end() commits all spans in a single writeTurn call")` — use a mock `TraceStore` with `writeTurn: vi.fn()`; start turn, add 3 spans, end turn; assert `writeTurn` called once with a `TurnRecord` whose `spans.length === 3`.
  - `it("turn.end() freezes the committed TurnRecord")` — assert `Object.isFrozen(writtenTurn) === true` and `Object.isFrozen(writtenTurn.spans) === true`.
  - `it("turn.end('error') records status='error'")` — assert `TurnRecord.status === "error"`.
  - `it("metadata JSON is passed through per span")` — start span with `{ tool: "memory_lookup", len: 42 }`, assert metadata preserved in recorded span.

**File 2: `src/performance/__tests__/trace-store.test.ts`** — exact test names required:
- `describe("TraceStore")`
  - `it("creates traces.db with WAL pragma and foreign_keys ON")` — after construction, query `PRAGMA journal_mode` = "wal", `PRAGMA foreign_keys` = 1.
  - `it("initializes traces and trace_spans tables")` — query sqlite_master, assert both table names present.
  - `it("writeTurn inserts one traces row and N trace_spans rows in one transaction")` — verify via row count after one call.
  - `it("writeTurn is idempotent on duplicate turn id (INSERT OR REPLACE)")` — call twice with same id, assert exactly 1 traces row.
  - `it("pruneOlderThan deletes expired turns")` — insert turns at `now - 10d` and `now - 1d`, call `pruneOlderThan(now - 7d iso)`, assert only recent remains.
  - `it("cascade: pruning a turn deletes its spans", { timeout: 5000 })` — explicit test name `"cascade"` so -t filter from 50-01-02 matches. Insert turn with 3 spans, call pruneOlderThan with future cutoff, assert 0 spans remain.
  - `it("persists across reopen")` — smaller sibling of the dedicated persistence test: write, close, reopen on same path, read back within a single test. Test name MUST contain `"persists"` to match 50-VALIDATION -t filter.
  - `it("getPercentiles returns p50/p95/p99/count rows for each canonical segment")` — insert 100 synthetic turns with known distribution, call getPercentiles, assert row shape.

For db path: use `mkdtempSync` under `os.tmpdir()`, pass path to constructor. Teardown with `rmSync(dir, { recursive: true, force: true })`.

**File 3: `src/performance/__tests__/trace-store-persistence.test.ts`** — DAEMON-RESTART SEMANTIC test (closes success criterion #4 gap flagged by checker BLOCKER 4):
- `describe("TraceStore daemon-restart persistence")`
  - `it("persists across daemon restart: writes survive when a fresh TraceStore is opened on the same path")` — this is the canonical PERF-01 success criterion #4 verification. Scenario:
    ```
    1. mkdtemp -> path
    2. const store1 = new TraceStore(path)
    3. store1.writeTurn(turnA with 2 spans)
    4. store1.writeTurn(turnB with 1 span)
    5. store1.close()
    6. const store2 = new TraceStore(path)   // SIMULATES DAEMON RESTART
    7. const rows = store2.getPercentiles("agent-x", <long-ago-iso>)
    8. assert count===2 for end_to_end segment (or equivalent assertion that both turns are still present)
    9. Additional raw query: use better-sqlite3 directly on the same path and assert traces/trace_spans row counts match what was written
    10. store2.close()
    ```
    This test explicitly exercises the "open NEW store on same path" code path, distinct from the sibling "persists across reopen" unit test in trace-store.test.ts (which may reuse statement caches depending on impl).
  - `it("schema is idempotent across reopen (CREATE TABLE IF NOT EXISTS does not error)")`.
  - `it("WAL checkpoint persists after close")` — write, close, reopen; assert data is visible (no WAL-only writes lost).

File 3 MUST import from `../trace-store.js` (red until Wave 1 lands).

**File 4: `src/performance/__tests__/percentiles.test.ts`** — exact test names required:
- `describe("parseSinceDuration")` — "since parser" literal in at least one test name.
  - `it("since parser accepts 1h / 6h / 24h / 7d")` — assert `parseSinceDuration("1h") === 3_600_000`, `"6h" === 21_600_000`, `"24h" === 86_400_000`, `"7d") === 604_800_000`.
  - `it("since parser also accepts minutes and seconds (30m, 90s)")`.
  - `it("since parser throws on invalid input")` — `expect(() => parseSinceDuration("bogus")).toThrow()`.
- `describe("sinceToIso")`
  - `it("produces ISO string relative to provided now")` — pass fixed Date, assert ISO offset matches expectation.
- `describe("percentile SQL math (via TraceStore.getPercentiles)")` — integration test:
  - `it("returns correct p50/p95/p99 for 100 synthetic spans")` — build a TraceStore, insert known durations 1..100 in one span name, call getPercentiles, assert p50 ~= 50, p95 ~= 95, p99 ~= 99.
  - `it("aggregates tool_call.* into a single tool_call segment row")` — insert `tool_call.memory_lookup` and `tool_call.search_documents` spans, assert one row with `segment: "tool_call"` whose count equals total.

All tests MUST use `.js` import extensions. No watch flags. No network/SDK calls — pure SQLite + in-memory mocks.
  </action>
  <verify>
    <automated>npx vitest run src/performance/__tests__ 2>&1 | grep -E "FAIL|Cannot find module" &amp;&amp; echo "RED (expected)" || echo "UNEXPECTED GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f src/performance/__tests__/trace-collector.test.ts`
    - File exists: `test -f src/performance/__tests__/trace-store.test.ts`
    - File exists: `test -f src/performance/__tests__/trace-store-persistence.test.ts`
    - File exists: `test -f src/performance/__tests__/percentiles.test.ts`
    - `grep -c 'describe("TraceCollector")' src/performance/__tests__/trace-collector.test.ts` returns 1
    - `grep -c 'describe("TraceStore")' src/performance/__tests__/trace-store.test.ts` returns 1
    - `grep -c "cascade" src/performance/__tests__/trace-store.test.ts` returns at least 1 (matches -t "cascade" filter in 50-01-02)
    - `grep -c "persists" src/performance/__tests__/trace-store.test.ts` returns at least 1
    - `grep -c 'daemon-restart' src/performance/__tests__/trace-store-persistence.test.ts` returns at least 1 (new dedicated test)
    - `grep -c 'new TraceStore' src/performance/__tests__/trace-store-persistence.test.ts` returns at least 2 (store1 then store2 on same path)
    - `grep -c "since parser" src/performance/__tests__/percentiles.test.ts` returns at least 1
    - `grep -E 'from "\.\./(trace-collector|trace-store|percentiles)\.js"' src/performance/__tests__/*.test.ts` returns at least 4 matches (imports use `.js` extension)
    - `npx vitest run src/performance/__tests__ 2>&1` shows errors of type "Cannot find module" or "Failed to resolve import" — red state confirmed
    - No `describe.skip`, `it.skip`, `it.only`, `describe.only`, or `--watch` present in any file
  </acceptance_criteria>
  <done>Four test files exist in `src/performance/__tests__/`, each with exact describe/it names matching 50-VALIDATION.md -t filters. The dedicated trace-store-persistence.test.ts explicitly exercises the daemon-restart semantic for PERF-01 success criterion #4. Running `npx vitest run src/performance/__tests__` fails with unresolved module imports (red state). No test is skipped.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold CLI latency test + retention heartbeat test + dashboard server latency block (additive)</name>
  <files>src/cli/commands/__tests__/latency.test.ts, src/heartbeat/checks/__tests__/trace-retention.test.ts, src/dashboard/__tests__/server.test.ts</files>
  <read_first>
    - src/cli/commands/costs.test.ts (mirror CLI test pattern: mock sendIpcRequest, invoke command, assert console.log output)
    - src/cli/commands/costs.ts (mirror command registration; look at --json and --since handling)
    - src/heartbeat/checks/__tests__/attachment-cleanup.test.ts (mirror CheckModule unit-test pattern with mocked CheckContext)
    - src/heartbeat/checks/attachment-cleanup.ts (mirror default-export CheckModule signature)
    - src/heartbeat/types.ts (CheckContext/CheckResult type shape)
    - src/dashboard/server.ts (confirm REST endpoint test harness — check if `__tests__/server.test.ts` already exists; if so, APPEND, don't overwrite)
    - .planning/phases/50-latency-instrumentation/50-VALIDATION.md (rows 50-00-04, 50-00-05, 50-03-02)
    - .planning/phases/50-latency-instrumentation/50-RESEARCH.md (CLI command registration code example and retention check code example)
  </read_first>
  <action>
Create three test files. Use vitest. All relative imports end in `.js`.

**File 1: `src/cli/commands/__tests__/latency.test.ts`** — copy flag-handling shape from `src/cli/commands/costs.test.ts`. Exact test names required:
- `describe("clawcode latency")` — parent.
  - `it("invokes IPC latency method with agent and since defaulting to 24h")` — mock `sendIpcRequest` via `vi.mock("../../../ipc/client.js", () => ({ sendIpcRequest: vi.fn() }))`. Register command, invoke with `["latency", "alpha"]`. Assert `sendIpcRequest` called with `(SOCKET_PATH, "latency", { agent: "alpha", all: false, since: "24h" })`.
  - `it("passes --since 7d through to IPC")` — invoke with `--since 7d`, assert since is "7d".
  - `it("emits json output with --json flag")` — test name MUST contain `"json"`. Mock IPC to return a `LatencyReport`. Spy on `console.log`. Invoke with `--json`. Assert `console.log` called with `JSON.stringify(report, null, 2)`.
  - `it("aggregates across all agents with --all flag")` — test name MUST contain `"all"`. Invoke with `--all`, assert IPC called with `{ all: true, agent: undefined, since: "24h" }`.
  - `it("renders percentile table with four canonical segments")` — mock IPC to return a report with `end_to_end`, `first_token`, `context_assemble`, `tool_call` rows; spy console.log; assert the joined output contains all four segment names and `p50`, `p95`, `p99`, `count` column headers.
  - `it("renders ms unit suffix on numeric cells")` — assert table output contains `"ms"` next to numbers.

Import skeleton:
```typescript
import { describe, it, expect, beforeEach, vi } from "vitest";
import { Command } from "commander";
import { registerLatencyCommand } from "../latency.js";
```

**File 2: `src/heartbeat/checks/__tests__/trace-retention.test.ts`** — mirror `src/heartbeat/checks/__tests__/attachment-cleanup.test.ts`. Exact test names required:
- `describe("trace-retention check")`
  - `it("returns healthy with 'No config' when agentConfig is missing")` — mock CheckContext whose `sessionManager.getAgentConfig(name)` returns `undefined`. Invoke `execute`. Assert result `{ status: "healthy" }` and message includes `"No config"`.
  - `it("returns healthy with 'No trace store' when getTraceStore returns undefined")`.
  - `it("calls store.pruneOlderThan with cutoff equal to now minus traceRetentionDays")` — mock agentConfig with `perf: { traceRetentionDays: 3 }`; mock `getTraceStore` returning `{ pruneOlderThan: vi.fn().mockReturnValue(5) }`. Freeze Date via `vi.useFakeTimers().setSystemTime(new Date("2026-04-13T00:00:00.000Z"))`. Assert pruneOlderThan called with ISO string representing `2026-04-10T00:00:00.000Z`.
  - `it("defaults to 7 days when perf.traceRetentionDays is absent")` — cutoff should be 7 days before frozen `now`.
  - `it("metadata includes deleted count and cutoff")`.
  - `it("module exports name 'trace-retention'")`.

Import skeleton (default export pattern, same as attachment-cleanup):
```typescript
import traceRetentionCheck from "../trace-retention.js";
```

**File 3: `src/dashboard/__tests__/server.test.ts`** — ADDITIVE ONLY. First check `ls src/dashboard/__tests__/server.test.ts`:
- If file exists: APPEND a new `describe("GET /api/agents/:name/latency")` block at the END of the file. Do NOT touch existing describe blocks. Do NOT modify existing imports except to ADD new imports (e.g., `LatencyReport`) if needed.
- If file does not exist: create the directory and file fresh following the dashboard-server test pattern.

The appended block (shared structure either way):
- `describe("latency endpoint")` — test name MUST contain `"latency"` to match `-t "latency"` filter in 50-03-02.
  - `it("latency: returns 200 with LatencyReport json for valid agent")` — start the dashboard HTTP server with a mocked IPC client that resolves `{ agent: "alpha", since: "2026-04-12T00:00:00Z", segments: [...] }`; fetch `/api/agents/alpha/latency`; assert 200 and JSON body equals the mock payload.
  - `it("latency: defaults since to 24h when query param absent")` — assert IPC called with `{ agent: "alpha", since: "24h" }`.
  - `it("latency: passes ?since=7d through to IPC method")`.
  - `it("latency: returns 500 with error message when IPC throws")`.

If `src/dashboard/__tests__/` does not exist, create the directory first.

**Regression guard for File 3:** Before committing this task, run `git diff src/dashboard/__tests__/server.test.ts` and confirm:
1. If pre-existing file: ALL original `describe`/`it` blocks still present unchanged (no deletions or modifications).
2. New block is APPENDED at end (or cleanly inserted without re-ordering).
3. Run `npx vitest run src/dashboard/__tests__/server.test.ts` — pre-existing tests should still PASS with their original assertions; only the new 4 "latency:" tests should fail with "Cannot find module" or missing-route errors. This prevents the Wave 0 scaffolding from breaking the existing dashboard test suite.

All three files must use `.js` import extension and vitest only. No SDK calls. No real IPC sockets — mock at module boundary.
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/__tests__/latency.test.ts src/heartbeat/checks/__tests__/trace-retention.test.ts src/dashboard/__tests__/server.test.ts 2>&1 | grep -E "FAIL|Cannot find module" &amp;&amp; echo "RED (expected)" || echo "UNEXPECTED GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f src/cli/commands/__tests__/latency.test.ts`
    - File exists: `test -f src/heartbeat/checks/__tests__/trace-retention.test.ts`
    - File exists: `test -f src/dashboard/__tests__/server.test.ts`
    - `grep -c 'describe("clawcode latency")' src/cli/commands/__tests__/latency.test.ts` returns 1
    - `grep -c '"json"' src/cli/commands/__tests__/latency.test.ts` returns at least 1 (matches -t "json")
    - `grep -c '"all"' src/cli/commands/__tests__/latency.test.ts` returns at least 1 (matches -t "all")
    - `grep -c 'describe("trace-retention' src/heartbeat/checks/__tests__/trace-retention.test.ts` returns 1
    - `grep -c "latency" src/dashboard/__tests__/server.test.ts` returns at least 2 (matches -t "latency")
    - REGRESSION GUARD: If src/dashboard/__tests__/server.test.ts existed before this task, all pre-existing tests still pass after this task (verified by running `npx vitest run src/dashboard/__tests__/server.test.ts` and confirming only the new "latency:" tests fail with Cannot find module / route not wired — other tests remain green)
    - All three files import using `.js` extension: `grep -E 'from "\.\.[^"]*\.js"' src/cli/commands/__tests__/latency.test.ts src/heartbeat/checks/__tests__/trace-retention.test.ts src/dashboard/__tests__/server.test.ts` returns at least 3 matches
    - `npx vitest run src/cli/commands/__tests__/latency.test.ts src/heartbeat/checks/__tests__/trace-retention.test.ts src/dashboard/__tests__/server.test.ts 2>&1` fails — red state confirmed
    - No `.skip` / `.only` / `--watch` present
  </acceptance_criteria>
  <done>Three additional test files exist with exact describe/it names. Each file's imports resolve to Wave 1/2/3 source files that do not yet exist. Any pre-existing tests in server.test.ts remain untouched and still pass. Full Wave 0 test command fails with unresolved imports — red state confirmed.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold Wave 2 hot-path tests (bridge, session-adapter, context-assembler append, scheduler append)</name>
  <files>src/discord/__tests__/bridge.test.ts, src/manager/__tests__/session-adapter.test.ts, src/manager/__tests__/context-assembler.test.ts, src/scheduler/__tests__/scheduler.test.ts</files>
  <read_first>
    - src/discord/__tests__/bridge-agent-messages.test.ts (mirror test harness: how DiscordBridge is constructed with mock SessionManager, mock routing table, mock discord.js Message)
    - src/discord/bridge.ts (confirm exported class name and handleMessage signature so Wave 0 imports resolve to a real module)
    - src/manager/__tests__/session-manager.test.ts (mirror pattern for session-adapter unit tests)
    - src/manager/session-adapter.ts (confirm `createSdkSessionHandle` or equivalent exported function name — this is the sentinel Wave 0 imports)
    - src/manager/__tests__/context-assembler.test.ts (EXISTS — append new describe block at end; do NOT modify existing assertions)
    - src/scheduler/__tests__/scheduler.test.ts (EXISTS — append new describe block at end; do NOT modify existing assertions)
    - src/scheduler/scheduler.ts (confirm Scheduler class name and start/sendToAgent invocation)
    - .planning/phases/50-latency-instrumentation/50-VALIDATION.md (rows 50-02-01, 50-02-02, 50-02-03, 50-02-04, 50-02-05 — exact -t filter strings)
    - .planning/phases/50-latency-instrumentation/50-RESEARCH.md (Pitfalls 2, 6 — what subagent filter / first_token filtering must assert)
  </read_first>
  <action>
Create two new test files and APPEND describe blocks to two existing test files. All imports use `.js` extension. No skipped tests. No watch flags. RED state is expected: imports and call signatures will either fail to resolve OR assertions will fail against the current non-instrumented Wave 1 implementation.

**File 1 (NEW): `src/discord/__tests__/bridge.test.ts`** — scaffolds receive-span + end_to_end tests for DiscordBridge.

Exact test names required (match the `-t` filters in 50-VALIDATION.md rows 50-02-01 and bridge.test.ts references elsewhere):
- `describe("DiscordBridge tracing")`
  - `it("receive span: starts when handleMessage is entered and ends before session dispatch")` — test name MUST contain `"receive span"` for the -t filter. Construct DiscordBridge with a mock SessionManager exposing `getTraceCollector` returning a mock collector. Mock routing table to resolve channel→agent. Invoke `handleMessage(mockMessage)`. Assert:
    1. `collector.startTurn` called with `(mockMessage.id, "agent-x", mockMessage.channelId)`
    2. `turn.startSpan` called with first argument `"receive"` at least once
    3. `receiveSpan.end()` called before any streaming begins
  - `it("end_to_end: turn.end('success') fires when streamAndPostResponse resolves")` — test name MUST contain `"end_to_end"`. Drive the mock session manager to resolve successfully; assert `turn.end` invoked with `"success"`.
  - `it("end_to_end: turn.end('error') fires when streamAndPostResponse throws")` — drive mock to reject; assert `turn.end` invoked with `"error"`.
  - `it("skips tracing when no TraceCollector is available (non-running agent)")` — getTraceCollector returns undefined; assert handleMessage does not throw and no span calls occur.

Import skeleton:
```typescript
import { describe, it, expect, vi, beforeEach } from "vitest";
import { DiscordBridge } from "../bridge.js";
// Mock discord.js Message + Channel types inline (see bridge-agent-messages.test.ts for pattern)
```

The import `from "../bridge.js"` WILL resolve in Wave 0 (the file exists). RED state comes from:
- Mock collector asserts `startTurn` NOT being called by current bridge impl (Wave 2 adds this)
- `turn.startSpan("receive")` assertion fails (Wave 2 adds this)

**File 2 (NEW): `src/manager/__tests__/session-adapter.test.ts`** — scaffolds first_token / tool_call / subagent tests.

Exact test names required (match -t filters: "first_token", "tool_call", "subagent"):
- `describe("SdkSessionAdapter tracing")`
  - `it("first_token: ends on the first text content block of the parent assistant message")` — test name MUST contain `"first_token"`. Build a mock SDK query iterator that yields:
    1. `{ type: "system", subtype: "init" }` (resume)
    2. `{ type: "assistant", parent_tool_use_id: null, message: { content: [{ type: "text", text: "Hello" }] } }`
    3. `{ type: "result", subtype: "success", result: "Hello" }`
    Assert `turn.startSpan("first_token")` was called AND the returned span's `end()` was invoked after the second iterator message, before the third.
  - `it("tool_call: opens a tool_call.<name> span on each tool_use content block and ends it on the matching tool_use_result")` — test name MUST contain `"tool_call"`. Yield assistant message with content `[{ type: "tool_use", id: "tu-1", name: "memory_lookup" }]`, then a user message `{ type: "user", parent_tool_use_id: "tu-1" }`. Assert `turn.startSpan("tool_call.memory_lookup", { tool_use_id: "tu-1" })` AND that returned span's `end()` was invoked after the user message is processed.
  - `it("subagent: does NOT end first_token when the assistant message has non-null parent_tool_use_id")` — test name MUST contain `"subagent"`. Yield:
    1. `{ type: "assistant", parent_tool_use_id: "some-tool-id", message: { content: [{ type: "text", text: "inner" }] } }` (SUBAGENT — must be filtered)
    2. `{ type: "assistant", parent_tool_use_id: null, message: { content: [{ type: "text", text: "outer" }] } }` (PARENT — should trigger first_token)
    3. result
    Assert `first_token.end()` was called ONCE, only after message 2. The subagent message must NOT have caused first_token.end() to fire.
  - `it("end_to_end: span covers the full stream (start at send dispatch, end at result)")`.
  - `it("sendAndCollect applies the same instrumentation as sendAndStream (Pitfall 2 guard)")` — assert tracing fires on sendAndCollect path too.

Import skeleton:
```typescript
import { describe, it, expect, vi, beforeEach } from "vitest";
// Import the SESSIONHANDLE factory function — confirm exact exported name by reading session-adapter.ts first
import { createSdkSessionHandle } from "../session-adapter.js";  // adjust name if different
```

RED state: Wave 2 adds the tracing hooks; in Wave 0 these tests fail because the current adapter does not call turn.startSpan.

**File 3 (APPEND ONLY): `src/manager/__tests__/context-assembler.test.ts`** — append a new describe block at the BOTTOM of the existing file. Do NOT modify existing tests.

- `describe("context_assemble tracing")` — test name MUST contain `"tracing"` to match `-t "tracing"` filter in 50-02-02.
  - `it("tracing: assembleContextTraced opens a context_assemble span before assembling and ends it after (finally)")` — import `assembleContextTraced` (Wave 2 adds this export). Construct a mock Turn with `startSpan: vi.fn(() => ({ end: vi.fn() }))`. Call `assembleContextTraced(sources, budgets, turn)`. Assert `turn.startSpan` called with first argument `"context_assemble"` and the returned span's `end()` called after assembly completes.
  - `it("tracing: ends the context_assemble span even when assembleContext throws")` — pass a sources object that forces the inner assemble to throw; assert the span's `end()` still fires in a `finally` block.
  - `it("tracing: no-op when turn is undefined (does not call startSpan)")` — invoke `assembleContextTraced(sources, budgets, undefined)`; assert startSpan not called and result equal to the untraced `assembleContext(sources, budgets)`.

APPEND guard: `grep -c '^describe(' src/manager/__tests__/context-assembler.test.ts` must INCREASE by exactly 1 (or more if nested describe added); existing `describe` blocks must remain intact. Run the file before and after edit to confirm existing tests still pass.

RED state: `assembleContextTraced` export doesn't exist yet → `Cannot find name 'assembleContextTraced'` TS error / runtime import miss. Wave 2 Task 2 adds the export.

**File 4 (APPEND ONLY): `src/scheduler/__tests__/scheduler.test.ts`** — append new describe block for scheduler turnId trace prefix. Do NOT modify existing tests.

- `describe("scheduler tracing")` — test name MUST contain `"trace"` to match `-t "trace"` filter in 50-VALIDATION.md row for scheduler.
  - `it("trace: scheduled turns produce turnId prefixed with 'scheduler:'")` — construct Scheduler with a mock SessionManager whose `sendToAgent` captures the `turn?: Turn` argument. Trigger a scheduled tick. Assert the collector's `startTurn` was called with a `turnId` that matches `/^scheduler:/`.
  - `it("trace: scheduler turns are created with null channelId (non-Discord)")` — assert `startTurn(..., agent, null)` (3rd arg is null).
  - `it("trace: scheduler turn.end('success') fires when sendToAgent resolves")`.
  - `it("trace: scheduler turn.end('error') fires when sendToAgent throws")`.

APPEND guard: existing tests must remain intact.

RED state: Wave 2 Task 3 adds `scheduler:` prefix and turn creation. Before that, the mock collector is not invoked by Scheduler → tests fail.

**Shared pattern note:** All four test files reference concrete exported names (DiscordBridge, createSdkSessionHandle, assembleContextTraced, Scheduler). Some names exist in Wave 1 source; others (assembleContextTraced) are added in Wave 2. Mix of "red because missing" and "red because behavior not yet implemented" is acceptable — both are honest RED states. The key property: no test is silently skipped, and every -t filter string resolves to a matching `describe` or `it` name.
  </action>
  <verify>
    <automated>npx vitest run src/discord/__tests__/bridge.test.ts src/manager/__tests__/session-adapter.test.ts src/manager/__tests__/context-assembler.test.ts src/scheduler/__tests__/scheduler.test.ts 2>&1 | grep -E "FAIL|Cannot find module|AssertionError" &amp;&amp; echo "RED (expected)" || echo "UNEXPECTED GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f src/discord/__tests__/bridge.test.ts`
    - File exists: `test -f src/manager/__tests__/session-adapter.test.ts`
    - `grep -c 'describe("DiscordBridge tracing' src/discord/__tests__/bridge.test.ts` returns 1
    - `grep -c 'receive span' src/discord/__tests__/bridge.test.ts` returns at least 1 (matches -t "receive span" filter)
    - `grep -c 'end_to_end' src/discord/__tests__/bridge.test.ts` returns at least 1
    - `grep -c 'describe("SdkSessionAdapter tracing' src/manager/__tests__/session-adapter.test.ts` returns 1
    - `grep -c 'first_token' src/manager/__tests__/session-adapter.test.ts` returns at least 1 (matches -t "first_token")
    - `grep -c 'tool_call' src/manager/__tests__/session-adapter.test.ts` returns at least 1 (matches -t "tool_call")
    - `grep -c 'subagent' src/manager/__tests__/session-adapter.test.ts` returns at least 1 (matches -t "subagent")
    - `grep -c 'parent_tool_use_id' src/manager/__tests__/session-adapter.test.ts` returns at least 2 (both subagent filter test and tool_call test reference it)
    - APPEND-GUARD context-assembler: `grep -c 'tracing' src/manager/__tests__/context-assembler.test.ts` returns at least 1 (matches -t "tracing"); existing top-level describe blocks still present (record baseline before edit, assert unchanged after)
    - APPEND-GUARD scheduler: `grep -c 'trace' src/scheduler/__tests__/scheduler.test.ts` returns at least 1 (matches -t "trace"); existing top-level describe blocks still present
    - All files use `.js` import extension
    - `npx vitest run src/discord/__tests__/bridge.test.ts src/manager/__tests__/session-adapter.test.ts 2>&1` fails — red state
    - RED state confirmed: vitest output shows at minimum 8 failing tests across the four files (4 bridge + 4 session-adapter minimum; context-assembler and scheduler counts additive)
    - No `.skip` / `.only` / `--watch` present in any file
  </acceptance_criteria>
  <done>Four Wave 2 test surfaces are scaffolded: bridge.test.ts and session-adapter.test.ts (new), context-assembler.test.ts and scheduler.test.ts (appended). Every `-t` filter string in 50-VALIDATION.md rows 50-02-01 through 50-02-05 has a matching `describe` or `it` block. Tests fail red — either via unresolved exports (assembleContextTraced) or via failing assertions against the current non-instrumented implementation. Pre-existing tests in context-assembler.test.ts and scheduler.test.ts remain untouched and still pass.</done>
</task>

</tasks>

<verification>
Wave 0 verification: All 10 test files exist (6 new + 4 appended-to / existing) with correct describe/it names that match the `-t` filters in 50-VALIDATION.md. Tests must fail red (unresolved imports OR failing assertions against current code). No test may be skipped or watched. Pre-existing tests in context-assembler.test.ts, scheduler.test.ts, and server.test.ts must remain passing. This plan does NOT create any source files — only tests.
</verification>

<success_criteria>
- [ ] `src/performance/__tests__/trace-collector.test.ts` exists with `describe("TraceCollector")`
- [ ] `src/performance/__tests__/trace-store.test.ts` exists with "cascade" and "persists" test names
- [ ] `src/performance/__tests__/trace-store-persistence.test.ts` exists with daemon-restart persistence test (closes success criterion #4 gap)
- [ ] `src/performance/__tests__/percentiles.test.ts` exists with "since parser" test name
- [ ] `src/cli/commands/__tests__/latency.test.ts` exists with "json" and "all" test names
- [ ] `src/heartbeat/checks/__tests__/trace-retention.test.ts` exists
- [ ] `src/dashboard/__tests__/server.test.ts` exists with "latency" tests; pre-existing tests untouched and passing
- [ ] `src/discord/__tests__/bridge.test.ts` exists with "receive span" and "end_to_end" tests
- [ ] `src/manager/__tests__/session-adapter.test.ts` exists with "first_token", "tool_call", "subagent" tests
- [ ] `src/manager/__tests__/context-assembler.test.ts` has appended "tracing" describe block; existing tests untouched
- [ ] `src/scheduler/__tests__/scheduler.test.ts` has appended "trace" describe block; existing tests untouched
- [ ] Running tests produces red state (unresolved imports or failing assertions), not false green
- [ ] No regression in existing dashboard/context-assembler/scheduler tests
- [ ] Phase 50-VALIDATION.md `wave_0_complete` flag can be flipped to true
</success_criteria>

<output>
After completion, create `.planning/phases/50-latency-instrumentation/50-00-SUMMARY.md` listing: files created + files appended to, total test count per file, exact describe/it names embedded, pre-existing test baseline counts (before/after) for context-assembler.test.ts / scheduler.test.ts / server.test.ts to prove no regression.
</output>
