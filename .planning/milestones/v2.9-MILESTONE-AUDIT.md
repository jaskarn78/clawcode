---
milestone: v2.9
audited: 2026-05-15
status: deploy_pending
scores:
  requirements: 22/22
  phases: 7/7
  integration: passed
  flows: deploy_gated
gaps:
  requirements: []
  integration: []
  flows: ["operator-visual screenshots + 24h soaks across 119/121/122/124/125 deploy-gated on Ramy-quiet window"]
tech_debt:
  - phase: 119-a2a-delivery-reliability
    items:
      - "SC-3 (queue-state icon transition screenshot) — deploy-gated; operator captures on next live A2A turn"
      - "SC-4 (24h HEARTBEAT_OK=0 soak) — agent-workspace `e634b7b` rsync unblocked via 999.55 (`8b1fefe`); next deploy automatically picks up corpus; soak window starts post-deploy + cron recreation"
  - phase: 121-subagent-ux-completion-and-chunk-boundary
    items:
      - "SC-3 (24h `seamGapBytes:0` production soak) — deploy-gated"
      - "`idleWarningEmittedAt` in-memory persistence — first post-restart quiescence may re-emit; persistence opt-in deferred (not blocking)"
      - "One-time migration cleanup markers in `thread-registry.ts` + `daemon.ts` — schedule ~2 weeks post-deploy"
  - phase: 122-discord-table-auto-wrap-universalization
    items:
      - "SC-2 (operator screenshots across 4 channels: webhook / bot-direct / cron / subagent-relay) — deploy-gated"
      - "19 pre-existing test failures in `slash-commands-*.test.ts` and siblings — predate phase, not introduced; logged for housekeeping"
      - "`bridge.sendDirect` deeper behavioral coverage — method is `private`; T-05 static-grep sentinel UWC-5 covers wire presence"
  - phase: 124-operator-triggered-session-compaction
    items:
      - "Post-deploy auto-trigger sentinel grep + dashboard sparkline render — deploy-gated"
      - "Per-agent `cooldownMs` config knob — currently hardcoded 5 min daemon-side; defer if operator pain emerges"
      - "DEFERRED-124-B (Mock<EmbeddingService> type narrow) — logged to deferred-items.md"
      - "Race window between cooldown gate-check and `record(agent)` — mitigated by heartbeat interval ≥ compaction wall-time + `hasActiveTurn` + 5-min cooldown; revisit if production sentinels < cooldown apart"
      - "`runner.test.ts` checkCount mismatch — pre-existing, predates phase"
  - phase: 125-intelligent-auto-compaction-strategy
    items:
      - "SC-5 (A/B agreement on production prompt pair) + SC-6 (first-token < 8 s measurement) — deploy-gated"
      - "Tier-2 Haiku JSON-parse fallback rate monitoring post-deploy (CONTEXT Pattern C — Phase 95 dreaming precedent)"
      - "Per-agent Tier-2 Haiku prompt overrides — current prompt is fleet-wide; `state/active-state.yaml` override handles verbatim patterns"
  - phase: 123-mcp-lifecycle-verification-soak
    items:
      - "SC-4 Variant A — transient orphans (1-2 per cold-restart cycle) reparented to PID 1 then reaped > 4 min later; steady-state clean. Pattern C latent finding flagged for follow-up (probe-wrapper group-kill doesn't cover restart-shutdown reparent path)"
      - "SC-3 Plan 999.14-02 Wave 2 (MCP-06..MCP-10) — NOT RUN against deployed binary; out of session scope; refile as follow-up if needed beyond build-time coverage"
      - "SC-2 Plan 999.6-02 — partial (snapshot/restore round-trip log not captured; deploy script auto-invokes snapshot-manager)"
  - phase: 999.54-allowed-tools-sdk-passthrough
    items:
      - "Plan 05 (live deploy + journalctl grep `phase999.54-resolver` + Discord turn-1 advisor-footer landing) — operator-deploy-gated; no source changes pending"
      - "Phase dir name (`allowed-tools-sdk-passthrough`) reflects original BACKLOG design that pivoted to `mcpServers[].alwaysLoad` after Plan 01 SDK research — kept as-is for git-history continuity"
nyquist:
  compliant_phases: [119, 121, 122, 124, 125]
  partial_phases: [120, 123]
  missing_phases: []
  overall: compliant
  note: "Phases 119/121/122/124/125 have full PHASE-SUMMARY.md + plan SUMMARY artifacts. Phase 120 has 120-FINAL-SUMMARY.md (operator-flagged regression bundle, 5 DASH-XX requirements). Phase 123 has 123-VERIFICATION.md documenting partial soak with Variant-A transient-orphan finding (out-of-scope follow-up)."
deploy_gate:
  reason: "feedback_ramy_active_no_deploy — Ramy in #fin-acquisition during business hours; restart disrupts live client thread. Code-complete; production verification + visual confirmations + 24h soaks all gated on Ramy-quiet window."
  unblocks: "Next `scripts/deploy-clawdy.sh` invocation picks up: (a) daemon binary, (b) per-agent prompt-corpus via 999.55 stanza in `8b1fefe`, (c) all v2.9 phase commits since 119 cut-over."
---

# v2.9 Reliability & Routing — Milestone Audit

**Audit date:** 2026-05-15
**Status:** `deploy_pending` — entire milestone code-complete locally; production verification + visual confirmations + 24h soaks gated on Ramy-quiet window.
**Phases shipped (code):** 7/7 (119, 120, 121, 122, 123, 124, 125)

## Scope

| Phase | Title | Verification | Plans | Plan Status |
|-------|-------|--------------|-------|-------------|
| 119 | A2A Delivery Reliability | partial (2/5 SCs deploy-gated) | 4/4 | All merged |
| 120 | Dashboard Observability Cleanup | complete-with-1-deferral-and-1-deploy-block | 5/5 | All merged |
| 121 | Subagent UX Completion + Chunk-Boundary | partial (SC-3 deploy-gated) | 2/2 | All merged |
| 122 | Discord Table Auto-Wrap Universalization | partial (SC-2 deploy-gated) | 1/1 + deviation | All merged |
| 123 | MCP Lifecycle Verification Soak | partial (Variant A transient orphans; SC-2/SC-3 partial) | 3/3 | Plans written; SC verification in 123-VERIFICATION.md |
| 124 | Operator-Triggered Session Compaction | partial (auto-trigger production tail deploy-gated) | 6/6 | All merged |
| 125 | Intelligent Auto-Compaction Strategy | partial (SC-5 + SC-6 deploy-gated) | 4/4 | All merged |

## Requirements Coverage

All 22 requirements satisfied in code locally:

- **A2A-01..A2A-04** (Phase 119) — post_to_agent no-webhook fallback, queue-state icons, webhook re-provisioning, heartbeat routing fix.
- **D-05** counter (Phase 119) — `no_webhook_fallbacks_total` telemetry.
- **DASH-01..DASH-05** (Phase 120) — Benchmarks tab fixes (tool-rollup, percentile cells, empty-state, split-latency, tool-latency-audit CLI).
- **SUB-01..SUB-02** (Phase 121) — premature completion gate + chunk-boundary seam.
- **DISC-01** (Phase 122) — universal table wrap at transport boundary.
- **MCP-01..MCP-03** (Phase 123) — soak execution (partial; Variant-A transient orphan finding).
- **Phase 124 SC-1..SC-6** — CLI + Discord admin command + memory preservation + mid-turn safety + policy decoupling + telemetry.
- **Phase 125 SC-1..SC-8** — active-state header + Tier 1 verbatim + Tier 4 drop + Tier 2 Haiku + A/B fixture + latency sentinel + auto-compact threshold + per-agent overrides.

## Cross-Phase Integration

**Phase 119 ↔ Phase 122:** Phase 119's new bot-direct fallback path (commit `ae4c8b1`) inherits Phase 122's wrap via the single daemon-side BotDirectSender chokepoint. Structurally proven via UWC-8 + UWC-11 sentinels in `122-universal-wrap-coverage.test.ts`. Future bot-direct A2A messages auto-wrap markdown tables.

**Phase 124 ↔ Phase 125:** Phase 124 ships the compaction primitive (CLI, IPC handler, fork mechanism, auto-trigger wiring, telemetry). Phase 125 replaces ONLY the `extractMemoriesFn` callback with the tiered pipeline behind the `src/manager/compact-extractors/` seam. Both daemon dispatch sites (`daemon.ts:3333` heartbeat + `daemon.ts:10440` manual IPC) collapsed to `buildTieredExtractor(deps)` per silent-path-bifurcation prevention. `compactForAgent` / `CompactionManager.compact` / `handleCompactSession` shape untouched after Phase 124 lock.

**Phase 121 ↔ Phase 119:** Subagent quiescence sweep (`subagent-completion-sweep.ts`) now emits `subagent_idle_warning` instead of firing premature completion relays — independent from but compatible with Phase 119's queue-state icon coherence work.

**Phase 999.55 ↔ Phase 119:** `scripts/deploy-clawdy.sh` extension in `8b1fefe` auto-syncs per-agent prompt-corpus alongside daemon binary; unblocks Phase 119 SC-4 (agent-workspace `e634b7b` reaches clawdy on next deploy without manual rsync).

## End-to-End Flows (deploy-gated)

The following flows are code-complete locally; full E2E verification awaits next deploy:

1. **A2A delivery (Phase 119)** — bot-direct fallback when webhook returns 401/404 → wrap inheritance from Phase 122 → queue-state icon coherence (⏳ → 👍 → ✅/❌) → SC-3 screenshot + SC-4 24h soak.
2. **Subagent relay byte-completeness (Phase 121)** — `streamFullyDrained && deliveryConfirmed` gate → off-by-3 seam fix → 24h production `seamGapBytes:0` soak.
3. **Universal table wrap (Phase 122)** — 4-column fixture across 4 channels (webhook / bot-direct / cron / subagent-relay) — operator screenshots.
4. **Session compaction (Phase 124)** — `clawcode session compact <agent>` CLI → `handleCompactSession` IPC → `SessionHandle.swap()` live hot-swap → memory.db preservation + grow-by-design → tokens sparkline.
5. **Intelligent auto-compaction (Phase 125)** — auto-trigger at `auto-compact-at: 0.7` → tiered extractor (Tier 1 verbatim + Tier 4 drop + Tier 2 Haiku + Tier 3 prose) → A/B agreement + first-token < 8 s latency win.

## Tech Debt (Acknowledged, Non-Blocking)

See frontmatter `tech_debt` block. Notable patterns:

- **Deploy-gated visual / soak verification** dominates the open items list — 5 of 7 phases have SCs gated on operator-witnessed evidence post-next-deploy. None blocking.
- **Silent-path-bifurcation prevention seams** working as designed in Phases 122 + 125 (static-grep regression tests + single-chokepoint architecture).
- **Phase 123 Variant-A transient orphans** is a Pattern-C latent finding (PITFALLS warned of it); the existing reaper cadence catches it but slower than the per-restart sample window. Follow-up: a more aggressive group-kill path on probe-wrapper restart-shutdown (out of scope for v2.9 closeout).
- **Phase 999.54 Plan 05** is the only code-pending-on-deploy item: it is documented as `autonomous: false` to halt the `--auto` execution flow and request operator confirmation per Deploy section.

## Side-Channel Work During v2.9 Window (informational)

Hotfixes and backlogs filed during the v2.9 active window that are NOT formally inside v2.9 scope but were operator-driven during the same period:

- **999.46** — Discord Table Auto-Transform — superseded by Phase 122 (which is the formal milestone home).
- **999.50** — tmux remote-control skill BACKLOG.md filed for future port from OpenClaw. v3.0 scope.
- **999.53** — MCP broker hot-reload — deferred pending Plan 94-08 / config-mutator infra (commit `92b0721`). v3.0 scope (deferred).
- **999.54** — `mcpServers[].alwaysLoad` SDK passthrough — code-complete locally; Plan 05 deploy-gated. Closed under this milestone (see PHASE-SUMMARY).
- **999.55** — `deploy-clawdy.sh` per-agent prompt-corpus rsync stanza — closed (`8b1fefe`). Unblocks Phase 119 SC-4.
- **999.57** — Subagent context isolation (two failure modes) — Plan 01 (factory + heartbeat runner wiring) shipped (8 commits), Plans 02 + 03 written but unexecuted. Partial in v2.9 window; remainder rolls into v3.0.
- **999.58** — manifest-driven plugin SDK — hotfix `24b3548` only (clawcode_share_file instruction). Architectural work deferred to v3.0.
- **999.59** — autonomous skill creation — hotfix `9ff5ab0` only (overflow trigger). Architectural work deferred to v3.0.
- **999.60** — clawcode-as-MCP-server — hotfix `accf2e0` only (subagent file auto-dump). Architectural work deferred to v3.0.
- **999.61** — no-useful-tokens stream timeout — hotfix `91535c7` (subagent discriminates `<system-reminder>` from task) + BACKLOG. v3.0 scope.
- **999.62** — LLM runtime multi-backend + Claude Code interactive fallback — BACKLOG (Anthropic Agent SDK credit policy, 2026-06-15 effective). v3.1 scope (multi-LLM milestone).

## Disposition

**Recommend:** keep milestone status `deploy_pending` until next Ramy-quiet deploy window. Code-complete state is fully captured per-phase. After deploy:

1. Capture operator-visual screenshots (119 SC-3, 122 SC-2).
2. Start 24h soaks (119 SC-4, 121 SC-3).
3. Live grep auto-trigger sentinels (124 SC-6 production tail, 125 SC-5/SC-6, 999.54 D-05a).
4. Append verification evidence to each phase summary's "Outstanding operator actions" section.
5. Flip this audit `status:` from `deploy_pending` to `complete` once all per-phase deploy-gated SCs close.

No structural blockers. v3.0 ROADMAP can be authored and v3.0 work can begin in parallel with the deploy-gated v2.9 closeout — they touch disjoint code surfaces.
