---
phase: 08-tiered-memory-storage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/memory/types.ts
  - src/memory/schema.ts
  - src/memory/tiers.ts
  - src/memory/store.ts
  - src/memory/__tests__/tiers.test.ts
  - src/memory/__tests__/store.test.ts
autonomous: true
requirements: [AMEM-08, AMEM-09]

must_haves:
  truths:
    - "New memories default to warm tier (D-04)"
    - "Pure functions correctly determine tier transitions based on access patterns and relevance scores"
    - "Tier column exists in SQLite with CHECK constraint"
  artifacts:
    - path: "src/memory/tiers.ts"
      provides: "Pure tier transition functions"
      exports: ["shouldPromoteToHot", "shouldDemoteToWarm", "shouldArchiveToCold", "DEFAULT_TIER_CONFIG"]
    - path: "src/memory/types.ts"
      provides: "MemoryTier type and tier field on MemoryEntry"
      contains: "MemoryTier"
    - path: "src/memory/schema.ts"
      provides: "tierConfigSchema zod validator"
      contains: "tierConfigSchema"
    - path: "src/memory/store.ts"
      provides: "Tier column migration and tier-aware queries"
      contains: "tier"
    - path: "src/memory/__tests__/tiers.test.ts"
      provides: "Tests for all tier transition functions"
  key_links:
    - from: "src/memory/tiers.ts"
      to: "src/memory/decay.ts"
      via: "calculateRelevanceScore import"
      pattern: "import.*calculateRelevanceScore.*decay"
    - from: "src/memory/store.ts"
      to: "src/memory/types.ts"
      via: "MemoryTier type usage"
      pattern: "MemoryTier"
---

<objective>
Add the MemoryTier type, tier config schema, pure tier-transition functions, and SQLite migration for the tier column.

Purpose: Establishes the type contracts, decision logic, and database schema that Plan 02 will orchestrate. All tier transition rules (D-05 through D-08) are encoded as testable pure functions.
Output: `tiers.ts` with pure functions, updated `types.ts`/`schema.ts`/`store.ts`, comprehensive tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-tiered-memory-storage/08-CONTEXT.md
@.planning/phases/08-tiered-memory-storage/08-RESEARCH.md
@src/memory/types.ts
@src/memory/schema.ts
@src/memory/store.ts
@src/memory/decay.ts
@src/memory/relevance.ts
@src/memory/index.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/memory/types.ts:
```typescript
export type MemorySource = "conversation" | "manual" | "system" | "consolidation";
export type MemoryEntry = {
  readonly id: string;
  readonly content: string;
  readonly source: MemorySource;
  readonly importance: number;
  readonly accessCount: number;
  readonly tags: readonly string[];
  readonly embedding: Float32Array | null;
  readonly createdAt: string;
  readonly updatedAt: string;
  readonly accessedAt: string;
};
```

From src/memory/decay.ts:
```typescript
export type DecayParams = { readonly halfLifeDays: number; };
export function calculateRelevanceScore(
  importance: number, accessedAt: string, now: Date, config: DecayParams
): number;
```

From src/memory/schema.ts:
```typescript
export const memoryConfigSchema = z.object({
  compactionThreshold: z.number().min(0).max(1).default(0.75),
  searchTopK: z.number().int().min(1).default(10),
  consolidation: consolidationConfigSchema.default(...),
  decay: decayConfigSchema.default(...),
  deduplication: dedupConfigSchema.default(...),
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Types, schema, and pure tier transition functions</name>
  <files>src/memory/types.ts, src/memory/schema.ts, src/memory/tiers.ts, src/memory/index.ts, src/memory/__tests__/tiers.test.ts</files>
  <read_first>src/memory/types.ts, src/memory/schema.ts, src/memory/decay.ts, src/memory/index.ts</read_first>
  <behavior>
    - shouldPromoteToHot returns true when accessCount >= 3 and accessedAt within 7 days (per D-05)
    - shouldPromoteToHot returns false when accessCount < 3
    - shouldPromoteToHot returns false when accessedAt older than 7 days even with high access count
    - shouldDemoteToWarm returns true when accessedAt >= 7 days ago (per D-06)
    - shouldDemoteToWarm returns false when accessedAt < 7 days ago
    - shouldArchiveToCold returns true when relevance score drops below 0.05 (per D-07)
    - shouldArchiveToCold returns false when relevance score is above threshold
    - All thresholds are configurable via TierConfig
    - DEFAULT_TIER_CONFIG has correct defaults: hotAccessThreshold=3, hotAccessWindowDays=7, hotDemotionDays=7, coldRelevanceThreshold=0.05, hotBudget=20
  </behavior>
  <action>
1. In `src/memory/types.ts`:
   - Add `export type MemoryTier = "hot" | "warm" | "cold";` (per D-01/D-02/D-03)
   - Add `readonly tier: MemoryTier;` field to `MemoryEntry` type (after `accessedAt`)
   - Export `MemoryTier` from the type file

2. In `src/memory/schema.ts`:
   - Add `tierConfigSchema` as a zod object with fields per research Pattern 2:
     - `hotAccessThreshold: z.number().int().min(1).default(3)` (D-05)
     - `hotAccessWindowDays: z.number().int().min(1).default(7)` (D-05)
     - `hotDemotionDays: z.number().int().min(1).default(7)` (D-06)
     - `coldRelevanceThreshold: z.number().min(0).max(1).default(0.05)` (D-07)
     - `hotBudget: z.number().int().min(1).default(20)` (D-09)
   - Add `tiers: tierConfigSchema.default(...)` to `memoryConfigSchema`
   - Export `TierConfig` inferred type and `tierConfigSchema`

3. Create `src/memory/tiers.ts` with pure functions following research Pattern 2 exactly:
   - `TierConfig` type (mirrors schema, used by pure functions)
   - `DEFAULT_TIER_CONFIG` constant with all defaults
   - `shouldPromoteToHot(accessCount, accessedAt, now, config)` -- returns true if accessCount >= hotAccessThreshold AND daysSinceAccess <= hotAccessWindowDays (per D-05)
   - `shouldDemoteToWarm(accessedAt, now, config)` -- returns true if daysSinceAccess >= hotDemotionDays (per D-06)
   - `shouldArchiveToCold(importance, accessedAt, now, config)` -- calls `calculateRelevanceScore` from decay.ts, returns true if score < coldRelevanceThreshold (per D-07)
   - Use `date-fns` `differenceInDays` for date arithmetic instead of manual millisecond math (research "Don't Hand-Roll")
   - All functions are pure (no side effects, no I/O)

4. Update `src/memory/index.ts`:
   - Export `MemoryTier` from types
   - Export `shouldPromoteToHot`, `shouldDemoteToWarm`, `shouldArchiveToCold`, `DEFAULT_TIER_CONFIG` from tiers
   - Export `tierConfigSchema`, `TierConfig` from schema

5. Update all existing code that constructs MemoryEntry objects to include `tier: "warm"` (per D-04):
   - `src/memory/store.ts` -- `rowToEntry()` function and `insert()` return
   - `src/memory/search.ts` -- `rowToSearchResult()` function
   - Fix any test files that construct MemoryEntry literals to include tier field
  </action>
  <verify>
    <automated>npx vitest run src/memory/__tests__/tiers.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "MemoryTier" src/memory/types.ts returns 0
    - grep -q "tier.*MemoryTier" src/memory/types.ts returns 0
    - grep -q "tierConfigSchema" src/memory/schema.ts returns 0
    - grep -q "shouldPromoteToHot" src/memory/tiers.ts returns 0
    - grep -q "shouldDemoteToWarm" src/memory/tiers.ts returns 0
    - grep -q "shouldArchiveToCold" src/memory/tiers.ts returns 0
    - grep -q "DEFAULT_TIER_CONFIG" src/memory/tiers.ts returns 0
    - grep -q "differenceInDays" src/memory/tiers.ts returns 0
    - npx vitest run src/memory/__tests__/tiers.test.ts exits 0
  </acceptance_criteria>
  <done>MemoryTier type exists, tierConfigSchema validates config, all three pure tier transition functions pass unit tests with correct thresholds per D-05/D-06/D-07, existing MemoryEntry constructions include tier:"warm"</done>
</task>

<task type="auto">
  <name>Task 2: SQLite tier column migration and tier-aware queries</name>
  <files>src/memory/store.ts, src/memory/__tests__/store.test.ts</files>
  <read_first>src/memory/store.ts, src/memory/__tests__/store.test.ts</read_first>
  <action>
1. In `src/memory/store.ts`, add a `migrateTierColumn()` private method following research Pattern 1:
   - Check if `tier` column exists via `PRAGMA table_info(memories)`
   - If not, run `ALTER TABLE memories ADD COLUMN tier TEXT NOT NULL DEFAULT 'warm' CHECK(tier IN ('hot', 'warm', 'cold'))`
   - Call this method from the constructor after `migrateSchema()` (before `prepareStatements()`)

2. Add a `getEmbedding(id: string): Float32Array | null` method to MemoryStore (research Open Question 2):
   - Query `SELECT embedding FROM vec_memories WHERE memory_id = ?`
   - Return the Float32Array or null if not found
   - This is needed by Plan 02 for cold archival (D-15 requires embedding in archive)

3. Add a `listByTier(tier: MemoryTier, limit: number): readonly MemoryEntry[]` method:
   - `SELECT ... FROM memories WHERE tier = ? ORDER BY accessed_at DESC LIMIT ?`
   - Returns frozen array of MemoryEntry objects
   - Used by TierManager in Plan 02 for hot refresh and tier maintenance

4. Add an `updateTier(id: string, tier: MemoryTier): boolean` method:
   - `UPDATE memories SET tier = ?, updated_at = ? WHERE id = ?`
   - Returns true if row was updated
   - Used by TierManager for tier transitions

5. Update the `MemoryRow` type to include `readonly tier: string`
6. Update `rowToEntry()` to map `row.tier` to the `tier` field (cast as MemoryTier)
7. Update `insertMemory` prepared statement to include tier column (default 'warm' per D-04)
8. Update `listRecent` query to also select the tier column

9. Add tests in store.test.ts:
   - Tier column exists after migration
   - New memories have tier='warm' by default
   - updateTier changes the tier value
   - listByTier filters correctly
   - getEmbedding returns Float32Array for existing memory, null for missing
  </action>
  <verify>
    <automated>npx vitest run src/memory/__tests__/store.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "migrateTierColumn" src/memory/store.ts returns 0
    - grep -q "getEmbedding" src/memory/store.ts returns 0
    - grep -q "listByTier" src/memory/store.ts returns 0
    - grep -q "updateTier" src/memory/store.ts returns 0
    - grep -q "tier.*warm" src/memory/__tests__/store.test.ts returns 0
    - npx vitest run src/memory/__tests__/store.test.ts exits 0
  </acceptance_criteria>
  <done>SQLite migration adds tier column with CHECK constraint, new memories default to warm, getEmbedding/listByTier/updateTier methods exist and pass tests, full store test suite green</done>
</task>

</tasks>

<verification>
npx vitest run src/memory/__tests__/tiers.test.ts src/memory/__tests__/store.test.ts --reporter=verbose
npx vitest run --reporter=verbose
</verification>

<success_criteria>
- MemoryTier type exported from types.ts
- tierConfigSchema in schema.ts with all 5 config fields and defaults matching D-05/D-06/D-07/D-09
- tiers.ts has 3 pure transition functions using date-fns and calculateRelevanceScore
- store.ts migrates tier column, supports listByTier/updateTier/getEmbedding
- All existing tests still pass (no regressions from tier field addition)
- New tier tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-tiered-memory-storage/08-01-SUMMARY.md`
</output>
