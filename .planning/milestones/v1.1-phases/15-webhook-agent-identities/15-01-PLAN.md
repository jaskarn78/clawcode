---
phase: 15-webhook-agent-identities
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/discord/webhook-types.ts
  - src/discord/webhook-manager.ts
  - src/discord/webhook-manager.test.ts
  - src/config/schema.ts
  - src/shared/types.ts
  - src/config/loader.ts
autonomous: true
requirements: [WHID-01, WHID-02]
must_haves:
  truths:
    - "WebhookIdentity type defines displayName, avatarUrl, and optional webhookUrl"
    - "Config schema includes webhook block per agent with displayName, avatarUrl, webhookUrl"
    - "WebhookManager can send messages via webhook URL with custom display name and avatar"
  artifacts:
    - path: "src/discord/webhook-types.ts"
      provides: "WebhookIdentity type, WebhookConfig type"
      exports: ["WebhookIdentity", "WebhookConfig"]
    - path: "src/discord/webhook-manager.ts"
      provides: "WebhookManager class for sending via webhooks"
      exports: ["WebhookManager"]
    - path: "src/discord/webhook-manager.test.ts"
      provides: "Tests for WebhookManager"
    - path: "src/config/schema.ts"
      provides: "webhookConfigSchema in agentSchema"
      contains: "webhookConfigSchema"
    - path: "src/shared/types.ts"
      provides: "webhook field on ResolvedAgentConfig"
      contains: "webhook"
---

<objective>
Create webhook identity types, config schema extension, and WebhookManager for sending messages with custom agent identities in Discord.

Purpose: Foundation types and webhook sending capability that the bridge integration depends on.
Output: Webhook types, config schema with webhook settings, WebhookManager class.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/discord/types.ts
@src/config/schema.ts
@src/shared/types.ts
@src/config/loader.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Webhook types, config schema, and WebhookManager</name>
  <files>src/discord/webhook-types.ts, src/discord/webhook-manager.ts, src/discord/webhook-manager.test.ts, src/config/schema.ts, src/shared/types.ts, src/config/loader.ts</files>
  <read_first>src/discord/types.ts, src/config/schema.ts, src/shared/types.ts, src/config/loader.ts</read_first>
  <behavior>
    - WebhookManager.send: sends a message via webhook URL with displayName and avatarUrl
    - WebhookManager.send: splits messages over 2000 chars into multiple sends
    - WebhookManager.getIdentity: returns the webhook identity for an agent name
    - WebhookManager.hasWebhook: returns true if agent has a webhook configured
  </behavior>
  <action>
    Create `src/discord/webhook-types.ts`:
    ```typescript
    type WebhookIdentity = {
      readonly displayName: string;
      readonly avatarUrl?: string;
      readonly webhookUrl: string;
    };
    type WebhookConfig = {
      readonly displayName: string;
      readonly avatarUrl?: string;
      readonly webhookUrl?: string;
    };
    ```

    Create `src/discord/webhook-manager.ts`:
    - Constructor takes a Map<agentName, WebhookIdentity>
    - `send(agentName, channelId, content)`: sends via WebhookClient
    - `hasWebhook(agentName)`: boolean check
    - `getIdentity(agentName)`: returns WebhookIdentity or undefined
    - Uses discord.js WebhookClient for sending

    Update `src/config/schema.ts`:
    - Add webhookConfigSchema
    - Add webhook field to agentSchema

    Update `src/shared/types.ts`:
    - Add webhook field to ResolvedAgentConfig

    Update `src/config/loader.ts`:
    - Wire webhook into resolveAgentConfig
  </action>
  <verify>
    <automated>cd /home/jjagpal/.openclaw/workspace-coding && npx vitest run src/discord/webhook-manager.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Webhook types exported, WebhookManager tested, config schema includes webhook block, tsc passes</done>
</task>

</tasks>

<verification>
- `npx vitest run src/discord/webhook-manager.test.ts` passes
- `npx tsc --noEmit` passes
- `grep -q "WebhookIdentity" src/discord/webhook-types.ts`
- `grep -q "webhookConfigSchema" src/config/schema.ts`
- `grep -q "webhook" src/shared/types.ts`
</verification>

<success_criteria>
Webhook types exported, WebhookManager can send via webhook URLs, config schema includes webhook block per agent, ResolvedAgentConfig has webhook field.
</success_criteria>

<output>
After completion, create `.planning/phases/15-webhook-agent-identities/15-01-SUMMARY.md`
</output>
