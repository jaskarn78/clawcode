---
phase: 15-webhook-agent-identities
plan: 02
type: execute
wave: 1
depends_on: [15-01]
files_modified:
  - src/manager/daemon.ts
  - src/ipc/protocol.ts
  - src/discord/bridge.ts
  - src/cli/commands/webhooks.ts
  - src/cli/index.ts
autonomous: true
requirements: [WHID-03, WHID-04]
---

<objective>
Wire WebhookManager into the daemon lifecycle, add IPC method for webhook listing, and register CLI webhooks command.

Purpose: Complete integration so webhooks are initialized on startup and queryable via CLI.
Output: Daemon creates WebhookManager, IPC exposes webhooks method, CLI command lists configured webhooks.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Daemon wiring, IPC method, and CLI command</name>
  <files>src/manager/daemon.ts, src/ipc/protocol.ts, src/cli/commands/webhooks.ts, src/cli/index.ts</files>
  <action>
    1. Add "webhooks" to IPC_METHODS in protocol.ts
    2. In daemon.ts: import buildWebhookIdentities and WebhookManager, create after resolving agents, add to shutdown, add IPC "webhooks" case
    3. Create src/cli/commands/webhooks.ts following skills.ts/threads.ts pattern
    4. Register webhooks command in src/cli/index.ts
  </action>
  <verify>
    <automated>cd /home/jjagpal/.openclaw/workspace-coding && npx vitest run src/discord/webhook-manager.test.ts</automated>
  </verify>
</task>

</tasks>
