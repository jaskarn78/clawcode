---
phase: 20-memory-search-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ipc/protocol.ts
  - src/manager/daemon.ts
  - src/cli/commands/memory.ts
  - src/cli/commands/memory.test.ts
  - src/cli/index.ts
autonomous: true
requirements: [MSRC-01, MSRC-02]
---

<objective>
Add memory search and list CLI commands with IPC methods.

Purpose: Enable users to search and browse agent memory from the command line.
Output: IPC memory-search and memory-list methods, CLI clawcode memory command with search and list subcommands.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IPC methods and CLI memory command</name>
  <files>src/ipc/protocol.ts, src/manager/daemon.ts, src/cli/commands/memory.ts, src/cli/commands/memory.test.ts, src/cli/index.ts</files>
  <action>
    1. Add "memory-search" and "memory-list" to IPC_METHODS
    2. Add memory-search and memory-list cases in daemon routeMethod:
       - memory-search: uses agent's memory store and embedder for semantic search
       - memory-list: lists recent memories from the store
    3. Create src/cli/commands/memory.ts with:
       - clawcode memory search <agent> <query> [--top-k N]
       - clawcode memory list <agent> [--limit N]
       - Formatted table output with relevance scores
    4. Create src/cli/commands/memory.test.ts testing formatters
    5. Register in cli/index.ts
  </action>
</task>

</tasks>
