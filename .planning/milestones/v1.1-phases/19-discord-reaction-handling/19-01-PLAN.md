---
phase: 19-discord-reaction-handling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/discord/reactions.ts
  - src/discord/reactions.test.ts
  - src/discord/bridge.ts
  - src/config/schema.ts
  - src/shared/types.ts
  - src/config/loader.ts
autonomous: true
requirements: [DRCT-01, DRCT-02]
---

<objective>
Add Discord reaction event handling to the bridge. Forward reactions in bound channels to agents as structured messages.

Purpose: Agents can respond to emoji reactions, enabling interactive workflows.
Output: Reaction event formatting, bridge event listeners, config extension.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reaction formatting, bridge integration, and config</name>
  <files>src/discord/reactions.ts, src/discord/reactions.test.ts, src/discord/bridge.ts, src/config/schema.ts, src/shared/types.ts, src/config/loader.ts</files>
  <action>
    1. Create src/discord/reactions.ts with:
       - ReactionEvent type
       - formatReactionEvent(event) -> string (structured XML-like format)

    2. Create src/discord/reactions.test.ts testing formatReactionEvent

    3. Update config schema with reactions field (boolean, default true)

    4. Update ResolvedAgentConfig with reactions field

    5. Update bridge.ts to listen for messageReactionAdd/Remove events:
       - Ignore bot reactions
       - Only forward in bound channels
       - Use formatReactionEvent for structured message
       - Forward via sessionManager.forwardToAgent
  </action>
</task>

</tasks>
