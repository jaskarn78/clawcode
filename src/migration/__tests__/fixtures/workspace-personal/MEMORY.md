# Memory

## Test memory section
This is a test memory chunk.

## Discord Setup
Discord bot token lives in 1Password. Channel IDs are hardcoded per-agent.
Activation rules live in clawcode.yaml under agents[].channels.

## Server Topology
clawdy host, /opt/clawcode install, systemd service, EnvironmentFile PATH gotcha.
